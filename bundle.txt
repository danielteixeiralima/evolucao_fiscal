### TREE

  .gitignore
  .replit
  Teste_1.py
  app.py
  bundle.txt
  complete_model_fields.txt
  create_test_excel.py
  file_processor.py
  file_processor_new.py
  forms.py
  main.py
  models.py
  models_new.py
  movements_emp4,147,148,149_fil1_2025-05-01_a_2025-11-01.xlsx
  movements_emp4_fil17_2025-08-01_a_2025-11-01.xlsx
  mybundle.py
  pyproject.toml
  replit.md
  routes.py
  test_data_sample.py
  test_excel_with_company_branch.xlsx
  test_upload.xlsx
  update_models.py
  uv.lock
.local/
.local/state/
.local/state/replit/
.local/state/replit/agent/
  .local/state/replit/agent/.agent_state_05aba192af7e8ac3596adf2bbedb00769214d5b5.bin
  .local/state/replit/agent/.agent_state_07bb8136412cffb0e9aa7af4da131b0a9a99bf53.bin
  .local/state/replit/agent/.agent_state_0e89b04670a3c0be1e1a0e20e69fd443580f7c42.bin
  .local/state/replit/agent/.agent_state_1dda7c638c0f39e228acfbc1463296cf24bed9a2.bin
  .local/state/replit/agent/.agent_state_2ab07f54722c18944af0e7db03b43698648282ef.bin
  .local/state/replit/agent/.agent_state_324acc773300d86b906de3afe2e5f49ad7e4a7b1.bin
  .local/state/replit/agent/.agent_state_397e19cae262afc391c4708d70659963b797b340.bin
  .local/state/replit/agent/.agent_state_463338e9e15e431ca9fd8c01ffd64723116dfbdc.bin
  .local/state/replit/agent/.agent_state_4f6d42148c8906c2a3a431df506da5e5f3cf53d9.bin
  .local/state/replit/agent/.agent_state_52d25e1e6180c60cef40b12117698c89150f5bef.bin
  .local/state/replit/agent/.agent_state_55b7d3140c70991a2c5dee42347741206146bfcc.bin
  .local/state/replit/agent/.agent_state_5ae3740fb9c240433ff7dd867f796168764a831c.bin
  .local/state/replit/agent/.agent_state_5c2b22680eb9067c31d750b3ab3f293153fade23.bin
  .local/state/replit/agent/.agent_state_5c898274f8a5d538d3fb74da7901ae61731cf790.bin
  .local/state/replit/agent/.agent_state_63b06b1c4d8f2439f562c45e898bb0bc71a80fef.bin
  .local/state/replit/agent/.agent_state_6de09519738affc3578aac6a96e53149d6494eb6.bin
  .local/state/replit/agent/.agent_state_8c1584b8dddc9c52a36c13feee6225e64da94bf6.bin
  .local/state/replit/agent/.agent_state_961c47eabfe1f11a4af280609a25f8ad09756989.bin
  .local/state/replit/agent/.agent_state_b6db5f8c7eba76ef3e693bfb9e889b8d1a7e6e26.bin
  .local/state/replit/agent/.agent_state_b7c76b4e390f0ea2ed6aa6f7b4c82b4754e93dd9.bin
  .local/state/replit/agent/.agent_state_d4180d4525d7be190460ea8a1ab019c53047074a.bin
  .local/state/replit/agent/.agent_state_d4d70da5fa9b890057c58145244b6638af9bb5eb.bin
  .local/state/replit/agent/.agent_state_e072ffb2bac53844e48bccec1115a96cc9f35941.bin
  .local/state/replit/agent/.agent_state_main.bin
  .local/state/replit/agent/.latest.json
  .local/state/replit/agent/rapid_build_started
  .local/state/replit/agent/rapid_build_success
  .local/state/replit/agent/repl_state.bin
.local/state/replit/agent/filesystem/
  .local/state/replit/agent/filesystem/filesystem_state.json
attached_assets/
  attached_assets/Pasted--2025-08-18-14-39-30-0000-1014-INFO-Booting-worker-with-pid-1014-INFO-file-processor-Read-Exc-1755528124031_1755528124031.txt
  attached_assets/Pasted--Empresa-Code-Empresa-Nome-Filial-Code-Filial-Nome-Filial-Nome-Descricao-internalId-companyId-moveme-1755539692719_1755539692719.txt
  attached_assets/Pasted--Empresa-Nome-Filial-Code-Filial-Nome-Filial-Nome-Descricao-internalId-companyId-movementId-branchI-1756245969666_1756245969666.txt
  attached_assets/Pasted--Eu-preciso-que-voc-me-ajude-a-criar-uma-aplica-o-em-flask-aonde-vamos-ter-uma-parte-de-admin--1755527301524_1755527301525.txt
  attached_assets/Pasted--coding-utf-8-import-re-import-sys-import-os-import-json-import-time-from-time-impor-1757089815255_1757089815257.txt
  attached_assets/Pasted--companyId-1-movementId-91-sequentialId-1-sequentialNumber-1-productId-812-qu-1755531544491_1755531544492.txt
  attached_assets/Pasted--sa-instance-state-sqlalchemy-orm-state-InstanceState-object-at-0x7fa9d98ee030-accounting-e-1755529082796_1755529082796.txt
  attached_assets/Pasted-ERROR-app-Exception-on-movements-GET-Traceback-most-recent-call-last-File-home-runner-wo-1757090301627_1757090301628.txt
  attached_assets/Pasted-INFO-Handling-signal-winch-ERROR-app-Exception-on-admin-dashboard-GET-Traceback-most-recent-ca-1755537314337_1755537314338.txt
  attached_assets/Pasted-INFO-file-processor-Reading-Excel-file-uploads-a95c624a-ed79-4640-9c65-1a624b0c03a3-rm-movimentos-U-1756245935776_1756245935777.txt
  attached_assets/Pasted-O-Handling-signal-winch-INFO-file-processor-Reading-Excel-file-uploads-43a4e8b8-5aca-471d-a2a7-cd-1755528724777_1755528724777.txt
  attached_assets/image_1755528827670.png
  attached_assets/image_1755529095300.png
  attached_assets/image_1755530970074.png
  attached_assets/image_1755530978320.png
  attached_assets/image_1755531510989.png
  attached_assets/image_1755532013482.png
  attached_assets/image_1755532024197.png
  attached_assets/image_1755532035320.png
  attached_assets/image_1756245948727.png
  attached_assets/image_1756245963176.png
  attached_assets/image_1757090693131.png
  attached_assets/pagina_unica_148_1_1755528242162.xlsx
  attached_assets/pagina_unica_148_1_1755528247290.xlsx
instance/
  instance/financial_data.db
migrations/
  migrations/README
  migrations/alembic.ini
  migrations/env.py
  migrations/script.py.mako
migrations/versions/
  migrations/versions/80fc0baf7fe3_add_customer_vendor_name_and_cnpj_to_.py
static/
static/css/
  static/css/style.css
static/js/
  static/js/main.js
templates/
  templates/base.html
  templates/index.html
templates/admin/
  templates/admin/clear_movements.html
  templates/admin/dashboard.html
  templates/admin/upload.html
  templates/admin/users.html
templates/auth/
  templates/auth/login.html
templates/movements/
  templates/movements/delete_all.html
  templates/movements/detail.html
  templates/movements/list.html


### FILES


----- FILE: Teste_1.py  (sha1:f13b1a2064c1, bytes:32907) -----
# -*- coding: utf-8 -*-
import re
import sys
import os
import json
import time
from time import perf_counter
from time import time as _now
from datetime import datetime
from typing import List, Tuple, Dict, Any, Optional
from concurrent.futures import ThreadPoolExecutor, as_completed
from threading import local, Lock

import requests
from requests.auth import HTTPBasicAuth
from requests.adapters import HTTPAdapter
from urllib3.util.retry import Retry

import pandas as pd
from pandas import json_normalize
from dateutil import tz, parser as dtparser
import argparse
import tempfile, shutil
# ==============================
# CONFIG DE CONEXÃO
# ==============================
HOST = "http://192.168.18.9:8051"
USER = "INTEGRA_INOVAI"
PWD  = "INOVAI.LAB"
auth = HTTPBasicAuth(USER, PWD)

MOV_ENDPOINT = "/api/mov/v1/movements"

# ==============================
# AJUSTES DE REDE / TOLERÂNCIA
# ==============================
MAX_LIST_PAGES           = 3
MAX_PAGES_PER_BRANCH     = 1_000_000

CONNECT_TIMEOUT          = 10
READ_TIMEOUT             = 45
ADAPTER_RETRIES          = 3
MANUAL_RETRIES           = 2
BACKOFF_BASE             = 0.5

BRANCH_DEADLINE_SEC_DEFAULT = 0  # 0 = sem limite (não perde itens)

DEBUG_VERBOSE            = True

# Campos típicos
DATE_FIELD_HINTS = [
    "date","issueDate","emissionDate","movementDate","creationDate","createdDate",
    "entryDate","exitDate","lastEditTime","registrationDate",
    "Date","IssueDate","EmissionDate","MovementDate","CreationDate","CreatedDate",
    "EntryDate","ExitDate","LastEditTime","RegistrationDate"
]
COMPANY_KEYS = ["companyId","CompanyId","companyCode","CompanyCode"]
BRANCH_KEYS  = ["branchId","BranchId","branchCode","BranchCode"]

# $select padrão (pode desligar)
DEFAULT_SELECT_FIELDS = [
    "date","internalId","movementId","documentNumber","type","grossValue","netValue",
    "status","companyId","branchId","partner.name"
]

# ==============================
# HTTP por thread (keep-alive + gzip)
# ==============================
_thread_local = local()

def _build_session() -> requests.Session:
    retry = Retry(
        total=ADAPTER_RETRIES, connect=ADAPTER_RETRIES, read=ADAPTER_RETRIES,
        backoff_factor=0.4, status_forcelist=[502,503,504],
        allowed_methods=["GET","HEAD"], raise_on_status=False,
    )
    adapter = HTTPAdapter(max_retries=retry, pool_connections=64, pool_maxsize=256)
    s = requests.Session()
    s.auth = auth
    s.headers.update({
        "Accept": "application/json",
        "Accept-Encoding": "gzip, deflate",
        # não forçar Connection: close → mantém keep-alive
    })
    s.mount("http://", adapter); s.mount("https://", adapter)
    return s

def _sess() -> requests.Session:
    s = getattr(_thread_local, "session", None)
    if s is None:
        s = _build_session()
        _thread_local.session = s
    return s

def _safe_request(method: str, url: str, *, params=None, timeout=None):
    print(f"DEBUG: chamando {method} {url} com params={params}")

    timeout = timeout or (CONNECT_TIMEOUT, READ_TIMEOUT)
    last_exc = None
    for attempt in range(1, MANUAL_RETRIES + 1):
        t0 = perf_counter()
        try:
            r = _sess().request(method, url, params=params, timeout=timeout)
            elapsed = (perf_counter() - t0) * 1000
            if DEBUG_VERBOSE:
                print(f"      ↪ {method} {url}  ({elapsed:.0f} ms)")
                if params:
                    print(f"        params: {params}")
                print(f"        → HTTP {r.status_code}")
            return r, None, elapsed
        except (requests.exceptions.ReadTimeout, requests.exceptions.ConnectionError) as e:
            elapsed = (perf_counter() - t0) * 1000
            last_exc = e
            sleep_s = BACKOFF_BASE * (2 ** (attempt - 1))
            print(f"      ⚠️ {type(e).__name__} {attempt}/{MANUAL_RETRIES} ({elapsed:.0f} ms). Backoff {sleep_s:.1f}s…")
            time.sleep(sleep_s)
        except Exception as e:
            elapsed = (perf_counter() - t0) * 1000
            print(f"      ❌ Exception inesperada ({elapsed:.0f} ms): {type(e).__name__}: {e}")
            return None, e, elapsed
    return None, last_exc, 0.0

# ==============================
# UTILS
# ==============================
def _norm_code(x: str) -> str:
    s = str(x).strip()
    return str(int(s)) if s.isdigit() else s

def _detect_field_present(obj: Dict[str,Any], candidates: List[str]) -> Optional[str]:
    lower_map = {k.lower(): k for k in obj.keys()}
    for cand in candidates:
        if cand.lower() in lower_map:
            return lower_map[cand.lower()]
    for k in obj.keys():
        if any(re.search(fr"\b{re.escape(cand)}\b", k, flags=re.IGNORECASE) for cand in candidates):
            return k
    return None

def _build_company_branch_filter(company_field: Optional[str], branch_field: Optional[str], comp_code: str, branch_code: str) -> Optional[str]:
    parts=[]
    if company_field:
        val = _norm_code(comp_code)
        parts.append(f"{company_field} eq {val if val.isdigit() else repr(val)}")
    if branch_field and str(branch_code).strip() != "":
        bc = str(branch_code).strip()
        parts.append(f"{branch_field} eq {bc if bc.isdigit() else repr(bc)}")
    return " and ".join(parts) if parts else None

def _date_iso(dt: datetime) -> str:
    return dt.isoformat()

def _guess_datetime_fields_from_sample(items: List[Dict[str,Any]]) -> List[str]:
    found=set()
    for it in items:
        for k,v in it.items():
            if isinstance(v,str):
                try:
                    dtparser.isoparse(v); found.add(k)
                except Exception:
                    pass
    ordered = [f for f in DATE_FIELD_HINTS if f in found]
    for f in sorted(found):
        if f not in ordered:
            ordered.append(f)
    return ordered

def _coerce_datetime_cols(df: pd.DataFrame) -> pd.DataFrame:
    candidates = [
        "date","Date","movementDate","MovementDate","issueDate","IssueDate",
        "emissionDate","EmissionDate","creationDate","CreatedDate","entryDate","exitDate","lastEditTime"
    ]
    for c in candidates:
        if c in df.columns:
            df[c] = pd.to_datetime(df[c], errors="coerce", utc=False)
    return df

def _strip_timezone_for_excel(df: pd.DataFrame, target_tz: str = "America/Sao_Paulo") -> pd.DataFrame:
    from pandas.api.types import is_datetime64_any_dtype
    for col in df.columns:
        if is_datetime64_any_dtype(df[col]):
            tzinfo = getattr(df[col].dtype, "tz", None)
            if tzinfo is not None:
                try:
                    df[col] = df[col].dt.tz_convert(target_tz).dt.tz_localize(None)
                except Exception:
                    df[col] = df[col].dt.tz_localize(None)
    return df

def _jsonify_non_scalars_for_excel(df: pd.DataFrame) -> pd.DataFrame:
    def to_excel_scalar(x):
        if isinstance(x, (list, dict, tuple, set)):
            try:
                return json.dumps(x, ensure_ascii=False)
            except Exception:
                return str(x)
        return x
    for c in df.columns:
        df[c] = df[c].apply(to_excel_scalar)
    return df

def _first_existing(cols: list[str], df_cols: set[str]) -> Optional[str]:
    for c in cols:
        if c in df_cols: return c
    return None

def _smart_sort(df: pd.DataFrame, ordenar_cols: List[str]) -> pd.DataFrame:
    cols = [c for c in ordenar_cols if c in df.columns]
    if not cols:
        maybe_date = _first_existing(["date","Date","movementDate","MovementDate","issueDate","IssueDate"], set(df.columns))
        maybe_id   = _first_existing(["internalId","InternalId","movementId","MovementId"], set(df.columns))
        cols = [c for c in [maybe_date, maybe_id] if c]
    if cols:
        return df.sort_values(cols, kind="stable")
    return df

def _autosize_openpyxl(ws):
    from openpyxl.utils import get_column_letter
    dims = {}
    for row in ws.rows:
        for cell in row:
            if cell.value is None: continue
            val = str(cell.value)
            dims[cell.column] = max(dims.get(cell.column, 0), len(val))
    for col, width in dims.items():
        ws.column_dimensions[get_column_letter(col)].width = min(max(width + 2, 10), 60)

# ==============================
# CATÁLOGO
# ==============================
def get_company_name(company_code: str) -> str:
    # evita chamadas para /api/framework/v1/companies
    return f"Empresa {company_code}"


def list_branches(company_code: str) -> list[dict]:
    # usa sempre a filial informada por parâmetro
    return [{"CompanyCode": str(company_code), "Code": args.filial or "1", "Title": "Filial simulada"}]


def list_companies() -> List[Dict[str, str]]:
    urls = [
        f"{HOST}/api/framework/v1/companies",
        f"{HOST}/api/framework/v1/Companies",
        f"{HOST}/api/corp/v1/companies",
    ]
    out: List[Dict[str,str]] = []
    seen = set()
    for url in urls:
        r, exc, _ = _safe_request("GET", url)
        if not r or r.status_code != 200:
            continue
        try:
            j = r.json()
        except Exception:
            continue
        items = j.get("items", j) if isinstance(j, dict) else j
        if not isinstance(items, list):
            continue
        for obj in items:
            code = str(obj.get("code") or obj.get("Code") or obj.get("companyCode") or obj.get("CompanyCode") or "").strip()
            name = str(obj.get("name") or obj.get("Name") or obj.get("title") or obj.get("Title") or obj.get("description") or "").strip()
            if not code: continue
            code = _norm_code(code)
            if code in seen: continue
            seen.add(code)
            out.append({"Code": code, "Name": name})
        if out:
            break
    return out



# ==============================
# MOVEMENTS
# ==============================


def get_customer_vendor_info(code: str, company_id: str) -> Optional[Dict[str, Any]]:
    if not code:
        return None

    url = f"{HOST}/api/fin/v1/CustomerVendor"
    params = {
        "$filter": f"code eq '{code}' and companyId eq {company_id}",
        "$select": "code,shortName,name,mainNIF"
    }

    r, exc, _ = _safe_request("GET", url, params=params)
    if not r or r.status_code != 200:
        return None

    j = r.json()
    items = j.get("items", [])
    if items:
        return items[0]

    # 🔄 fallback: tenta sem companyId
    params["$filter"] = f"code eq '{code}'"
    r, exc, _ = _safe_request("GET", url, params=params)
    if not r or r.status_code != 200:
        return None

    j = r.json()
    return (j.get("items") or [None])[0]


def _get_movements_page(page: int, page_size: int, odata_filter: Optional[str], select_fields: List[str]):
    """
    Executa a chamada à API /movements, garantindo que o filtro de empresa/filial e de movimentos
    seja combinado corretamente sem ser sobrescrito.
    """
    url = f"{HOST}{MOV_ENDPOINT}"
    params = {
        "page": max(1, page),
        "pageSize": max(1, page_size)
    }

    # Filtro base (empresa + filial)
    base_filter = odata_filter or ""

    # Filtro adicional de movimento
    if getattr(args, "movimento", None):
        movimentos = [m.strip() for m in args.movimento.split(",") if m.strip()]
        if len(movimentos) == 1:
            mov_filtro = f"movementTypeCode eq '{movimentos[0]}'"
        else:
            clauses = [f"movementTypeCode eq '{m}'" for m in movimentos]
            mov_filtro = " or ".join(clauses)

        # ✅ Garante que o filtro de movimento é somado, não sobrescrito
        if base_filter:
            base_filter = f"({base_filter}) and ({mov_filtro})"
        else:
            base_filter = mov_filtro

    # Aplica filtro final
    if base_filter:
        params["$filter"] = base_filter

    # Campos selecionados
    if select_fields:
        params["$select"] = ",".join(select_fields)

    if DEBUG_VERBOSE:
        print(f"DEBUG: chamando GET {url}")
        print(f"       params = {json.dumps(params, indent=2, ensure_ascii=False)}")

    # Execução HTTP
    r, exc, _ = _safe_request("GET", url, params=params)
    meta = {"status": r.status_code if r else -1}

    if not r:
        return [], -1, meta

    if 200 <= r.status_code < 300 or r.status_code == 206:
        try:
            j = r.json()
        except Exception:
            return [], r.status_code, meta

        # Normaliza retorno
        if isinstance(j, dict):
            data = j.get("items") or j.get("Items")
            items = data if isinstance(data, list) else ([j] if not isinstance(j, list) else [])
        elif isinstance(j, list):
            items = j
        else:
            items = []

        return items, r.status_code, meta

    return [], r.status_code, meta


def _try_date_filter_server_side(company_field: Optional[str], branch_field: Optional[str],
                                 comp_code: str, branch_code: str,
                                 dt_ini: datetime, dt_fim: datetime,
                                 sample_date_candidates: List[str],
                                 select_fields: List[str]) -> Tuple[Optional[str], Optional[str]]:
    base_filter = _build_company_branch_filter(company_field, branch_field, comp_code, branch_code)
    candidates = list(dict.fromkeys(DATE_FIELD_HINTS + sample_date_candidates))
    for df in candidates:
        date_part = f"{df} ge {_date_iso(dt_ini)} and {df} le {_date_iso(dt_fim)}"
        odata = f"{base_filter} and {date_part}" if base_filter else date_part
        items, st, _ = _get_movements_page(1, 1, odata, select_fields)
        if st == 200:
            return odata, df
    return None, None

# ==============================
# EXTRAÇÃO SEQUENCIAL POR PÁGINAS (estável e rápida)
# ==============================
def extract_branch_items(comp_norm: str, b_code: str,
                         comp_field: str, br_field: str,
                         dt_ini: datetime, dt_fim: datetime,
                         date_candidates: List[str],
                         page_size_first: int,
                         select_fields: List[str],
                         deadline_sec: int) -> List[Dict[str,Any]]:
    header = f"  → Filial código {b_code or '(vazio)'}"
    print("\n" + header)
    print("  " + "—"*len(header))

    server_odata, used_date_field = _try_date_filter_server_side(
        comp_field, br_field, comp_norm, b_code, dt_ini, dt_fim, date_candidates, select_fields
    )
    if server_odata:
        print(f"    ✅ Filtro server-side aceito: {server_odata}")
    else:
        server_odata = _build_company_branch_filter(comp_field, br_field, comp_norm, b_code)
        print("    ⚠️ Sem campo de data aceito no servidor. Período será filtrado no cliente.")

    # cadeia degradável de page sizes
    ps_chain = [max(1, page_size_first), 50, 25, 10, 5, 1]
    ps_chain = [ps_chain[0]] + [p for p in ps_chain[1:] if p < ps_chain[0]]

    page = 1
    ps_index = 0
    empty_seq = 0
    started_at = _now()
    out: List[Dict[str,Any]] = []

    # dedupe por filial (id pode repetir entre páginas em alguns backends)
    seen_ids = set()
    id_fields = ["internalId","InternalId","movementId","MovementId","id","Id"]

    def in_period_local(rec: Dict[str, Any]) -> bool:
        v = rec.get("date")
        if isinstance(v, str):
            try:
                d = dtparser.isoparse(v)
                return dt_ini <= d <= dt_fim
            except Exception:
                pass
        for key in (date_candidates or []) + DATE_FIELD_HINTS:
            v = rec.get(key)
            if isinstance(v, str):
                try:
                    d = dtparser.isoparse(v)
                    if dt_ini <= d <= dt_fim:
                        return True
                except Exception:
                    pass
        return False
    global_limit = getattr(args, "limit", 0)

    max_pages = getattr(args, "max_pages", 1)
    while page <= min(max_pages, MAX_PAGES_PER_BRANCH):

        if deadline_sec and (_now() - started_at > deadline_sec):
            print(f"      ⏱️ Tempo excedido ({deadline_sec}s) — encerrando filial com {len(out)} itens.")
            break

        page_size = ps_chain[ps_index]
        items, st, _ = _get_movements_page(page, page_size, server_odata, select_fields)

        if st == -1 or st >= 500:
            if ps_index < len(ps_chain) - 1:
                ps_index += 1
                print(f"      ⚠️ st={st} — reduzindo pageSize para {ps_chain[ps_index]} e repetindo pág {page}…")
                continue
            print("      ⚠️ Falha persistente — tentando sem $filter e filtrando localmente…")
            items2, st2, _ = _get_movements_page(page, page_size, None, select_fields)
            if st2 >= 400 or st2 == -1:
                print(f"      ⚠️ HTTP {st2} mesmo sem filtro — encerrando filial.")
                break
            items = items2
            # filtros locais
            if comp_field:
                items = [it for it in items if str(it.get(comp_field)) == _norm_code(comp_norm)]
            if br_field and str(b_code).strip() != "":
                items = [it for it in items if str(it.get(br_field, "")).strip() == str(b_code).strip()]
            if not used_date_field:
                items = [it for it in items if in_period_local(it)]
        elif st >= 400:
            print(f"      ⚠️ HTTP {st} — pulando filial.")
            break

        if not items:
            empty_seq += 1
            print(f"    · Página {page}: 0 itens (vazias seguidas={empty_seq}) | ps={page_size}")
            if empty_seq >= 2:
                print("      ↳ Duas páginas vazias — fim desta filial.")
                break
            page += 1
            continue

        if not used_date_field:
            items = [it for it in items if in_period_local(it)]

        # dedupe por filial
        deduped = []
        for it in items:
            rid = None
            for f in id_fields:
                if f in it: rid = str(it[f]); break
            if rid and rid in seen_ids:
                continue
            if rid: seen_ids.add(rid)
            deduped.append(it)
        items = deduped

        out.extend(items)
        print(f"    · Página {page}: {len(items)} itens (acum={len(out)}) | ps={page_size}")
        if global_limit and len(out) >= global_limit:
            print(f"      ⏹️ Limite global de {global_limit} registros atingido — encerrando coleta desta filial.")
            out = out[:global_limit]
            break


        if len(items) < page_size:
            print("      ↳ Última página (len(items) < pageSize).")
            break
        page += 1

    return out

# ==============================
# MAIN
# ==============================
def run_extract(args):
    TZBR = tz.gettz("America/Sao_Paulo")
    dt_ini = datetime.strptime(args.inicio, "%Y-%m-%d").replace(hour=0, minute=0, second=0, tzinfo=TZBR)
    dt_fim = datetime.strptime(args.fim, "%Y-%m-%d").replace(hour=23, minute=59, second=59, tzinfo=TZBR)

    print(">>> Iniciando extração de movements…")
    print(f"Período: {dt_ini.isoformat()}  até  {dt_fim.isoformat()}")
    print(f"Endpoint: {HOST}{MOV_ENDPOINT}")

    # empresas alvo
    if args.empresa:
        target_companies = [_norm_code(x) for x in args.empresa.split(",")]

        print(f"🏭 Empresa informada por parâmetro: {target_companies[0]}")
        companies_meta = []
    else:
        companies = list_companies()
        if not companies:
            target_companies = ["1", "148"]
            print("⚠️ Não consegui listar empresas automaticamente; usando fallback ['4','148'].")
            companies_meta = []
        else:
            target_companies = [c["Code"] for c in companies]
            companies_meta = companies
            print(f"🏭 Empresas detectadas automaticamente: {', '.join(target_companies)}")

    # $select
    if args.select_fields == "":
        select_fields = []
    elif args.select_fields is None:
        select_fields = DEFAULT_SELECT_FIELDS[:]
    else:
        select_fields = [c.strip() for c in args.select_fields.split(",") if c.strip()]

    page_size_first = max(1, args.page_size)
    branch_deadline = max(0, args.branch_deadline_seconds)

    all_rows: List[Dict[str,Any]] = []
    all_rows_lock = Lock()

    # função por empresa (filiais em paralelo; empresas sequenciais → logs mais legíveis)
    def process_company(comp):
        comp_norm = _norm_code(comp)
        comp_name = get_company_name(comp_norm)
        if not comp_name and companies_meta:
            try:
                comp_name = next((c["Name"] for c in companies_meta if c["Code"] == comp_norm), "")
            except Exception:
                pass

        print("\n" + "="*72)
        print(f"🏢 Empresa {comp_norm} — {comp_name or '(sem nome)'}")
        print("="*72)

        branches = list_branches(comp_norm)
        print(f"  📚 Filiais detectadas: {len(branches)}")

        if args.filial:
            branches = [b for b in branches if str(b.get("Code","")).strip() == str(args.filial).strip()]
            print(f"  🔎 Filtro de filial: {args.filial} → {len(branches)} filial(is)")
        if not branches:
            print("  ⚠️ Nenhuma filial após filtro. Pulando empresa.")
            return

        # probe /movements para detectar campos
        probe_items, status, _ = _get_movements_page(1, 5, None, select_fields)
        if status >= 400 or status == -1 or not probe_items:
            probe_items2, status2, _ = _get_movements_page(1, 1, None, select_fields)
            if status2 >= 400 or status2 == -1 or not probe_items2:
                print(f"  ❌ Probe /movements falhou (HTTP {status2}).")
                return
            probe_items = probe_items2

        probe_row = probe_items[0] if probe_items else {}
        comp_field = _detect_field_present(probe_row, COMPANY_KEYS) or "companyId"
        br_field   = _detect_field_present(probe_row, BRANCH_KEYS)  or "branchId"
        date_candidates = _guess_datetime_fields_from_sample(probe_items)
        print(f"  🔎 Campos /movements → company='{comp_field}', branch='{br_field}'")

        # filiais em paralelo
        futures = []
        with ThreadPoolExecutor(max_workers=args.concurrency_branches) as ex:
            for b in branches:
                b_code = b.get("Code","")
                futures.append(ex.submit(
                    extract_branch_items,
                    comp_norm, b_code, comp_field, br_field,
                    dt_ini, dt_fim, date_candidates,
                    page_size_first, select_fields, branch_deadline
                ))

            total_emp = 0
            for fut in as_completed(futures):
                try:
                    items = fut.result()
                except Exception as e:
                    print(f"    ⚠️ Erro processando filial: {e}")
                    continue
                with all_rows_lock:
                    all_rows.extend(items)
                    total_emp += len(items)
                print(f"  ✅ Parcial empresa {comp_norm}: +{len(items)} itens (acum empresa={total_emp})")

        print(f"  ✅ Empresa {comp_norm} finalizada.")

    # percorre TODAS as empresas (sequencialmente para logs limpos)
    for comp in target_companies:
        process_company(comp)

    # ==============================
    # CONSOLIDAÇÃO
    # ==============================
    print("\n>>> Consolidando em DataFrame…")
    if not all_rows:
        print("(Sem registros coletados.)")
        return

    df = json_normalize(all_rows, sep=".")
    # ============================================
    # 🔍 Enriquecimento: busca CNPJ e Razão Social
    # ============================================
    print("🔍 Enriquecendo com CNPJ e Razão Social...")

    if "customerVendorCode" in df.columns and "companyId" in df.columns:
        unique_pairs = df[["customerVendorCode", "companyId"]].dropna().drop_duplicates()
        lookup = {}

        for _, row in unique_pairs.iterrows():
            code = str(row["customerVendorCode"]).strip()
            comp = str(row["companyId"]).strip()
            info = get_customer_vendor_info(code, comp)
            if info:
                lookup[(code, comp)] = {
                    "cnpj": info.get("mainNIF"),
                    "razao_social": info.get("shortName") or info.get("name")
                }

        df["cnpj"] = df.apply(lambda r: lookup.get((str(r["customerVendorCode"]), str(r["companyId"])), {}).get("cnpj"), axis=1)
        df["razao_social"] = df.apply(lambda r: lookup.get((str(r["customerVendorCode"]), str(r["companyId"])), {}).get("razao_social"), axis=1)
    else:
        print("⚠️ Campos customerVendorCode ou companyId não encontrados; não foi possível enriquecer.")

    df = _coerce_datetime_cols(df)
    df = _strip_timezone_for_excel(df)

    ordenar_cols = [c.strip() for c in (args.ordenar or "").split(",") if c.strip()]
    df = _smart_sort(df, ordenar_cols)

    branch_col  = _first_existing(["branchId","BranchId","branchCode","BranchCode"], set(df.columns))
    company_col = _first_existing(["companyId","CompanyId","companyCode","CompanyCode"], set(df.columns))
    resumo = None
    if branch_col:
        grp_cols = [c for c in [company_col, branch_col] if c]
        resumo = df.groupby(grp_cols, dropna=False).size().reset_index(name="qtd").sort_values("qtd", ascending=False)
        resumo = _strip_timezone_for_excel(resumo)

    # ==============================
    # XLSX
    # ==============================
    base = args.arquivo
    if not base:
        emp = f"emp{args.empresa}" if args.empresa else "empVARIAS"
        fil = f"_fil{args.filial}" if args.filial else ""
        base = f"movements_{emp}{fil}_{args.inicio}_a_{args.fim}.xlsx"
    if not base.lower().endswith(".xlsx"):
        base += ".xlsx"

    from openpyxl import Workbook
    from openpyxl.styles import Font, PatternFill

    wb = Workbook()
    ws_tree = wb.active
    ws_tree.title = "Árvore"
    ws_data = wb.create_sheet("Dados")

    df_excel = _jsonify_non_scalars_for_excel(df.copy())
    ws_data.append(list(df_excel.columns))
    for row in df_excel.itertuples(index=False, name=None):
        ws_data.append(list(row))
    try: _autosize_openpyxl(ws_data)
    except Exception: pass

    bold = Font(bold=True)
    header_fill = PatternFill(start_color="FFECECEC", end_color="FFECECEC", fill_type="solid")
    company_fill = PatternFill(start_color="FFCCE5FF", end_color="FFCCE5FF", fill_type="solid")
    branch_fill  = PatternFill(start_color="FFFFF2CC", end_color="FFFFF2CC", fill_type="solid")

    tree_cols = [c.strip() for c in (args.colunas or "").split(",") if c.strip()]
    if not tree_cols:
        pref = ["date","internalId","movementId","documentNumber","type","grossValue","netValue","partner.name","status"]
        tree_cols = [c for c in pref if c in df.columns] or list(df.columns[:10])

    comp_series = df[company_col] if company_col in df.columns else pd.Series([""]*len(df))
    br_series   = df[branch_col]  if branch_col in df.columns else pd.Series([""]*len(df))
    unique_pairs = pd.DataFrame({"comp": comp_series, "branch": br_series}).drop_duplicates()

    filial_label_map: Dict[Tuple[str,str], str] = {}

    for comp_id in sorted(unique_pairs["comp"].astype(str).unique(), key=lambda x: (x is None, x)):
        comp_mask = (comp_series.astype(str) == str(comp_id))
        df_comp = df[comp_mask]

        ws_tree.append([f"Empresa: {comp_id}"])
        for cell in ws_tree[ws_tree.max_row]:
            cell.font = bold; cell.fill = company_fill

        filiais_comp = sorted(df_comp[branch_col].astype(str).unique()) if branch_col in df_comp.columns else [""]
        for br_id in filiais_comp:
            br_mask = (br_series.astype(str) == str(br_id))

            df_fil = df_comp[br_mask] if branch_col in df_comp.columns else df_comp

            br_name = filial_label_map.get((str(comp_id), str(br_id)), "")
            ws_tree.append([f"  Filial: {br_id} {('- ' + br_name) if br_name else ''}"])
            for cell in ws_tree[ws_tree.max_row]:
                cell.font = bold; cell.fill = branch_fill

            ws_tree.append([""] + tree_cols)
            for cell in ws_tree[ws_tree.max_row]:
                cell.font = bold; cell.fill = header_fill

            df_tree = df_fil[tree_cols].copy()
            df_tree = _jsonify_non_scalars_for_excel(df_tree)
            for _, rec in df_tree.iterrows():
                ws_tree.append([""] + [rec.get(c) for c in tree_cols])

            ws_tree.append([])

    try: _autosize_openpyxl(ws_tree)
    except Exception: pass

    if resumo is not None:
        ws_resumo = wb.create_sheet("Resumo")
        resumo_excel = _jsonify_non_scalars_for_excel(resumo.copy())
        ws_resumo.append(list(resumo_excel.columns))
        for row in resumo_excel.itertuples(index=False, name=None):
            ws_resumo.append(list(row))
        try: _autosize_openpyxl(ws_resumo)
        except Exception: pass

    try:
        # 1. cria arquivo temporário
        tmpfile = tempfile.NamedTemporaryFile(delete=False, suffix=".xlsx").name

        # 2. salva no temp
        wb.save(tmpfile)
        wb.close()  # fecha handle do openpyxl

        # 3. move para destino final (substitui se existir)
        shutil.move(tmpfile, base)

    except Exception as e:
        alt = os.path.splitext(base)[0] + ".csv"
        print(f"⚠️ Falha ao salvar XLSX ({e}). Salvando CSV com dados completos: {alt}")
        df_excel.to_csv(alt, index=False)
        base = alt

    print(f"\n✅ Arquivo gerado: {os.path.abspath(base)}")

def parse_args():
    ap = argparse.ArgumentParser(description="Extrai Movements e gera XLSX em árvore + dados completos (rápido & estável).")
    ap.add_argument("--inicio",   required=True, help="Data inicial (YYYY-MM-DD).")
    ap.add_argument("--fim",      required=True, help="Data final (YYYY-MM-DD).")
    ap.add_argument("--empresa", required=False, help="IDs de empresas separados por vírgula, ex.: 4,147,148,149")

    ap.add_argument("--filial",   required=False, help="branchId (filial), ex.: 17")
    ap.add_argument("--ordenar",  required=False, default="date,internalId", help="Colunas para ordenar (vírgula).")
    ap.add_argument("--colunas",  required=False, default="", help="Colunas para mostrar na aba Árvore (vírgula).")
    ap.add_argument("--arquivo",  required=False, default="", help="Caminho do .xlsx de saída.")

    # performance/completude
    ap.add_argument("--page-size", type=int, default=100, help="Itens por página (o servidor pode limitar; varre até o fim).")
    ap.add_argument("--concurrency-branches", type=int, default=4, help="Filiais processadas em paralelo.")
    ap.add_argument("--branch-deadline-seconds", type=int, default=BRANCH_DEADLINE_SEC_DEFAULT,
                    help="Tempo máx por filial; 0 = sem limite (não perder itens).")

    # $select (string vazia desliga)
    ap.add_argument("--select-fields", type=str, default=",".join(DEFAULT_SELECT_FIELDS),
                    help='Campos em $select (ex.: "date,internalId,movementId,companyId,branchId"). Use "" para desativar.')
    ap.add_argument("--movimento", required=False, help="Lista de códigos de movementTypeCode separados por vírgula, ex.: 1.2.01,1.2.09,1.2.10.")
    ap.add_argument("--limit", type=int, default=0, help="Limite máximo de registros a coletar (0 = sem limite).")
    ap.add_argument("--max-pages", type=int, default=1, help="Número máximo de páginas a buscar por filial (padrão=1).")



    return ap.parse_args()

if __name__ == "__main__":
    args = parse_args()
    try:
        run_extract(args)
    except KeyboardInterrupt:
        print("\nInterrompido pelo usuário.")
        sys.exit(130)
    except Exception as e:
        print(f"\nERRO fatal: {e}")
        sys.exit(2)


----- FILE: app.py  (sha1:28e3f6ed5c45, bytes:4964) -----
import os
import logging
from flask import Flask
from flask_sqlalchemy import SQLAlchemy
from flask_login import LoginManager
from sqlalchemy.orm import DeclarativeBase
from werkzeug.middleware.proxy_fix import ProxyFix
from flask_migrate import Migrate

# ---------------------------------------------
# CONFIGURAÇÃO DE LOG
# ---------------------------------------------
logging.basicConfig(level=logging.DEBUG)


# ---------------------------------------------
# BASE MODEL (usada pelo SQLAlchemy 2.0)
# ---------------------------------------------
class Base(DeclarativeBase):
    pass


# ---------------------------------------------
# INICIALIZAÇÃO DAS EXTENSÕES
# ---------------------------------------------
db = SQLAlchemy(model_class=Base)
login_manager = LoginManager()
migrate = Migrate()


# ---------------------------------------------
# FUNÇÃO FACTORY PRINCIPAL DO APP
# ---------------------------------------------
def create_app():
    app = Flask(__name__)
    @app.template_filter('translate_field')
    def translate_field(field_name):
        """Traduz os nomes técnicos dos campos JSON para português."""
        labels = {
            'companyId': 'Empresa',
            'movementId': 'Movimento',
            'paymentSequentialId': 'Sequência do Pagamento',
            'debitCredit': 'Débito/Crédito',
            'value': 'Valor',
            'paymentType': 'Tipo de Pagamento',
            'dueDate': 'Data de Vencimento',
            'paymentFormId': 'Forma de Pagamento (ID)',
            'entryId': 'Lançamento',
            'cashAccountCompanyId': 'Empresa da Conta Caixa',
            'cashAccount': 'Conta Caixa',
            'administrateTax': 'Administrar Imposto',
            'multiplePayment': 'Pagamento Múltiplo',
            'paymentFormType': 'Tipo de Forma de Pagamento',
            'paymentFormDescription': 'Descrição da Forma de Pagamento',
            'movementItemSequentialId': 'Seq. do Item do Movimento',
            'taxId': 'ID do Imposto',
            'calculationBasis': 'Base de Cálculo',
            'aliquot': 'Alíquota (%)',
            'fullBase': 'Base Completa',
            'edited': 'Editado',
            'description': 'Descrição',
            'registerDate': 'Data de Registro',
            'deliveryDate': 'Data de Entrega',
            'entryDate': 'Data de Entrada',
            'exitDate': 'Data de Saída',
            'creationDate': 'Data de Criação'
        }
        return labels.get(field_name, field_name)

    # -----------------------------
    # CONFIGURAÇÕES GERAIS
    # -----------------------------
    app.secret_key = os.environ.get("SESSION_SECRET", "dev-secret-key-change-in-production")
    app.config["SQLALCHEMY_DATABASE_URI"] = os.environ.get("DATABASE_URL", "sqlite:///financial_data.db")
    app.config["SQLALCHEMY_TRACK_MODIFICATIONS"] = False
    app.config["SQLALCHEMY_ENGINE_OPTIONS"] = {
        "pool_recycle": 300,
        "pool_pre_ping": True,
    }
    app.config["MAX_CONTENT_LENGTH"] = 50 * 1024 * 1024  # 50MB
    app.config["UPLOAD_FOLDER"] = "uploads"

    # Garante que a pasta de upload existe
    os.makedirs(app.config["UPLOAD_FOLDER"], exist_ok=True)

    # -----------------------------
    # INICIALIZA AS EXTENSÕES
    # -----------------------------
    db.init_app(app)
    migrate.init_app(app, db)
    login_manager.init_app(app)
    login_manager.login_view = "auth.login"
    login_manager.login_message = "Please log in to access this page."

    # Corrige URLs em servidores com proxy/reverso (Nginx etc.)
    app.wsgi_app = ProxyFix(app.wsgi_app, x_proto=1, x_host=1)

    # -----------------------------
    # REGISTRA MODELOS E USUÁRIO ADMIN PADRÃO
    # -----------------------------
    with app.app_context():
        import models  # importa todos os modelos para registrar

        db.create_all()  # cria tabelas se ainda não existirem

        # Cria usuário admin se não existir
        from models import User
        from werkzeug.security import generate_password_hash

        admin = User.query.filter_by(username="admin").first()
        if not admin:
            admin = User(
                username="admin",
                email="admin@example.com",
                is_admin=True,
                password_hash=generate_password_hash("admin123"),
            )
            db.session.add(admin)
            db.session.commit()
            logging.info("✅ Created admin user: admin / admin123")

    # -----------------------------
    # REGISTRA BLUEPRINTS
    # -----------------------------
    from routes import main_bp, admin_bp, auth_bp

    app.register_blueprint(main_bp)
    app.register_blueprint(admin_bp, url_prefix="/admin")
    app.register_blueprint(auth_bp, url_prefix="/auth")

    return app


# ---------------------------------------------
# INSTÂNCIA GLOBAL DO APP (usada pelo flask run)
# ---------------------------------------------
app = create_app()


----- FILE: bundle.txt  (sha1:0e42e3d929d9, bytes:537045) -----
### TREE

  .gitignore
  .replit
  Teste_1.py
  app.py
  complete_model_fields.txt
  create_test_excel.py
  file_processor.py
  file_processor_new.py
  forms.py
  main.py
  models.py
  models_new.py
  movements_emp4,147,148,149_fil1_2025-05-01_a_2025-11-01.xlsx
  movements_emp4_fil17_2025-08-01_a_2025-11-01.xlsx
  mybundle.py
  pyproject.toml
  replit.md
  routes.py
  test_data_sample.py
  test_excel_with_company_branch.xlsx
  test_upload.xlsx
  update_models.py
  uv.lock
.local/
.local/state/
.local/state/replit/
.local/state/replit/agent/
  .local/state/replit/agent/.agent_state_05aba192af7e8ac3596adf2bbedb00769214d5b5.bin
  .local/state/replit/agent/.agent_state_07bb8136412cffb0e9aa7af4da131b0a9a99bf53.bin
  .local/state/replit/agent/.agent_state_0e89b04670a3c0be1e1a0e20e69fd443580f7c42.bin
  .local/state/replit/agent/.agent_state_1dda7c638c0f39e228acfbc1463296cf24bed9a2.bin
  .local/state/replit/agent/.agent_state_2ab07f54722c18944af0e7db03b43698648282ef.bin
  .local/state/replit/agent/.agent_state_324acc773300d86b906de3afe2e5f49ad7e4a7b1.bin
  .local/state/replit/agent/.agent_state_397e19cae262afc391c4708d70659963b797b340.bin
  .local/state/replit/agent/.agent_state_463338e9e15e431ca9fd8c01ffd64723116dfbdc.bin
  .local/state/replit/agent/.agent_state_4f6d42148c8906c2a3a431df506da5e5f3cf53d9.bin
  .local/state/replit/agent/.agent_state_52d25e1e6180c60cef40b12117698c89150f5bef.bin
  .local/state/replit/agent/.agent_state_55b7d3140c70991a2c5dee42347741206146bfcc.bin
  .local/state/replit/agent/.agent_state_5ae3740fb9c240433ff7dd867f796168764a831c.bin
  .local/state/replit/agent/.agent_state_5c2b22680eb9067c31d750b3ab3f293153fade23.bin
  .local/state/replit/agent/.agent_state_5c898274f8a5d538d3fb74da7901ae61731cf790.bin
  .local/state/replit/agent/.agent_state_63b06b1c4d8f2439f562c45e898bb0bc71a80fef.bin
  .local/state/replit/agent/.agent_state_6de09519738affc3578aac6a96e53149d6494eb6.bin
  .local/state/replit/agent/.agent_state_8c1584b8dddc9c52a36c13feee6225e64da94bf6.bin
  .local/state/replit/agent/.agent_state_961c47eabfe1f11a4af280609a25f8ad09756989.bin
  .local/state/replit/agent/.agent_state_b6db5f8c7eba76ef3e693bfb9e889b8d1a7e6e26.bin
  .local/state/replit/agent/.agent_state_b7c76b4e390f0ea2ed6aa6f7b4c82b4754e93dd9.bin
  .local/state/replit/agent/.agent_state_d4180d4525d7be190460ea8a1ab019c53047074a.bin
  .local/state/replit/agent/.agent_state_d4d70da5fa9b890057c58145244b6638af9bb5eb.bin
  .local/state/replit/agent/.agent_state_e072ffb2bac53844e48bccec1115a96cc9f35941.bin
  .local/state/replit/agent/.agent_state_main.bin
  .local/state/replit/agent/.latest.json
  .local/state/replit/agent/rapid_build_started
  .local/state/replit/agent/rapid_build_success
  .local/state/replit/agent/repl_state.bin
.local/state/replit/agent/filesystem/
  .local/state/replit/agent/filesystem/filesystem_state.json
attached_assets/
  attached_assets/Pasted--2025-08-18-14-39-30-0000-1014-INFO-Booting-worker-with-pid-1014-INFO-file-processor-Read-Exc-1755528124031_1755528124031.txt
  attached_assets/Pasted--Empresa-Code-Empresa-Nome-Filial-Code-Filial-Nome-Filial-Nome-Descricao-internalId-companyId-moveme-1755539692719_1755539692719.txt
  attached_assets/Pasted--Empresa-Nome-Filial-Code-Filial-Nome-Filial-Nome-Descricao-internalId-companyId-movementId-branchI-1756245969666_1756245969666.txt
  attached_assets/Pasted--Eu-preciso-que-voc-me-ajude-a-criar-uma-aplica-o-em-flask-aonde-vamos-ter-uma-parte-de-admin--1755527301524_1755527301525.txt
  attached_assets/Pasted--coding-utf-8-import-re-import-sys-import-os-import-json-import-time-from-time-impor-1757089815255_1757089815257.txt
  attached_assets/Pasted--companyId-1-movementId-91-sequentialId-1-sequentialNumber-1-productId-812-qu-1755531544491_1755531544492.txt
  attached_assets/Pasted--sa-instance-state-sqlalchemy-orm-state-InstanceState-object-at-0x7fa9d98ee030-accounting-e-1755529082796_1755529082796.txt
  attached_assets/Pasted-ERROR-app-Exception-on-movements-GET-Traceback-most-recent-call-last-File-home-runner-wo-1757090301627_1757090301628.txt
  attached_assets/Pasted-INFO-Handling-signal-winch-ERROR-app-Exception-on-admin-dashboard-GET-Traceback-most-recent-ca-1755537314337_1755537314338.txt
  attached_assets/Pasted-INFO-file-processor-Reading-Excel-file-uploads-a95c624a-ed79-4640-9c65-1a624b0c03a3-rm-movimentos-U-1756245935776_1756245935777.txt
  attached_assets/Pasted-O-Handling-signal-winch-INFO-file-processor-Reading-Excel-file-uploads-43a4e8b8-5aca-471d-a2a7-cd-1755528724777_1755528724777.txt
  attached_assets/image_1755528827670.png
  attached_assets/image_1755529095300.png
  attached_assets/image_1755530970074.png
  attached_assets/image_1755530978320.png
  attached_assets/image_1755531510989.png
  attached_assets/image_1755532013482.png
  attached_assets/image_1755532024197.png
  attached_assets/image_1755532035320.png
  attached_assets/image_1756245948727.png
  attached_assets/image_1756245963176.png
  attached_assets/image_1757090693131.png
  attached_assets/pagina_unica_148_1_1755528242162.xlsx
  attached_assets/pagina_unica_148_1_1755528247290.xlsx
instance/
  instance/financial_data.db
migrations/
  migrations/README
  migrations/alembic.ini
  migrations/env.py
  migrations/script.py.mako
migrations/versions/
  migrations/versions/80fc0baf7fe3_add_customer_vendor_name_and_cnpj_to_.py
static/
static/css/
  static/css/style.css
static/js/
  static/js/main.js
templates/
  templates/base.html
  templates/index.html
templates/admin/
  templates/admin/clear_movements.html
  templates/admin/dashboard.html
  templates/admin/upload.html
  templates/admin/users.html
templates/auth/
  templates/auth/login.html
templates/movements/
  templates/movements/delete_all.html
  templates/movements/detail.html
  templates/movements/list.html


### FILES


----- FILE: Teste_1.py  (sha1:f13b1a2064c1, bytes:32907) -----
# -*- coding: utf-8 -*-
import re
import sys
import os
import json
import time
from time import perf_counter
from time import time as _now
from datetime import datetime
from typing import List, Tuple, Dict, Any, Optional
from concurrent.futures import ThreadPoolExecutor, as_completed
from threading import local, Lock

import requests
from requests.auth import HTTPBasicAuth
from requests.adapters import HTTPAdapter
from urllib3.util.retry import Retry

import pandas as pd
from pandas import json_normalize
from dateutil import tz, parser as dtparser
import argparse
import tempfile, shutil
# ==============================
# CONFIG DE CONEXÃO
# ==============================
HOST = "http://192.168.18.9:8051"
USER = "INTEGRA_INOVAI"
PWD  = "INOVAI.LAB"
auth = HTTPBasicAuth(USER, PWD)

MOV_ENDPOINT = "/api/mov/v1/movements"

# ==============================
# AJUSTES DE REDE / TOLERÂNCIA
# ==============================
MAX_LIST_PAGES           = 3
MAX_PAGES_PER_BRANCH     = 1_000_000

CONNECT_TIMEOUT          = 10
READ_TIMEOUT             = 45
ADAPTER_RETRIES          = 3
MANUAL_RETRIES           = 2
BACKOFF_BASE             = 0.5

BRANCH_DEADLINE_SEC_DEFAULT = 0  # 0 = sem limite (não perde itens)

DEBUG_VERBOSE            = True

# Campos típicos
DATE_FIELD_HINTS = [
    "date","issueDate","emissionDate","movementDate","creationDate","createdDate",
    "entryDate","exitDate","lastEditTime","registrationDate",
    "Date","IssueDate","EmissionDate","MovementDate","CreationDate","CreatedDate",
    "EntryDate","ExitDate","LastEditTime","RegistrationDate"
]
COMPANY_KEYS = ["companyId","CompanyId","companyCode","CompanyCode"]
BRANCH_KEYS  = ["branchId","BranchId","branchCode","BranchCode"]

# $select padrão (pode desligar)
DEFAULT_SELECT_FIELDS = [
    "date","internalId","movementId","documentNumber","type","grossValue","netValue",
    "status","companyId","branchId","partner.name"
]

# ==============================
# HTTP por thread (keep-alive + gzip)
# ==============================
_thread_local = local()

def _build_session() -> requests.Session:
    retry = Retry(
        total=ADAPTER_RETRIES, connect=ADAPTER_RETRIES, read=ADAPTER_RETRIES,
        backoff_factor=0.4, status_forcelist=[502,503,504],
        allowed_methods=["GET","HEAD"], raise_on_status=False,
    )
    adapter = HTTPAdapter(max_retries=retry, pool_connections=64, pool_maxsize=256)
    s = requests.Session()
    s.auth = auth
    s.headers.update({
        "Accept": "application/json",
        "Accept-Encoding": "gzip, deflate",
        # não forçar Connection: close → mantém keep-alive
    })
    s.mount("http://", adapter); s.mount("https://", adapter)
    return s

def _sess() -> requests.Session:
    s = getattr(_thread_local, "session", None)
    if s is None:
        s = _build_session()
        _thread_local.session = s
    return s

def _safe_request(method: str, url: str, *, params=None, timeout=None):
    print(f"DEBUG: chamando {method} {url} com params={params}")

    timeout = timeout or (CONNECT_TIMEOUT, READ_TIMEOUT)
    last_exc = None
    for attempt in range(1, MANUAL_RETRIES + 1):
        t0 = perf_counter()
        try:
            r = _sess().request(method, url, params=params, timeout=timeout)
            elapsed = (perf_counter() - t0) * 1000
            if DEBUG_VERBOSE:
                print(f"      ↪ {method} {url}  ({elapsed:.0f} ms)")
                if params:
                    print(f"        params: {params}")
                print(f"        → HTTP {r.status_code}")
            return r, None, elapsed
        except (requests.exceptions.ReadTimeout, requests.exceptions.ConnectionError) as e:
            elapsed = (perf_counter() - t0) * 1000
            last_exc = e
            sleep_s = BACKOFF_BASE * (2 ** (attempt - 1))
            print(f"      ⚠️ {type(e).__name__} {attempt}/{MANUAL_RETRIES} ({elapsed:.0f} ms). Backoff {sleep_s:.1f}s…")
            time.sleep(sleep_s)
        except Exception as e:
            elapsed = (perf_counter() - t0) * 1000
            print(f"      ❌ Exception inesperada ({elapsed:.0f} ms): {type(e).__name__}: {e}")
            return None, e, elapsed
    return None, last_exc, 0.0

# ==============================
# UTILS
# ==============================
def _norm_code(x: str) -> str:
    s = str(x).strip()
    return str(int(s)) if s.isdigit() else s

def _detect_field_present(obj: Dict[str,Any], candidates: List[str]) -> Optional[str]:
    lower_map = {k.lower(): k for k in obj.keys()}
    for cand in candidates:
        if cand.lower() in lower_map:
            return lower_map[cand.lower()]
    for k in obj.keys():
        if any(re.search(fr"\b{re.escape(cand)}\b", k, flags=re.IGNORECASE) for cand in candidates):
            return k
    return None

def _build_company_branch_filter(company_field: Optional[str], branch_field: Optional[str], comp_code: str, branch_code: str) -> Optional[str]:
    parts=[]
    if company_field:
        val = _norm_code(comp_code)
        parts.append(f"{company_field} eq {val if val.isdigit() else repr(val)}")
    if branch_field and str(branch_code).strip() != "":
        bc = str(branch_code).strip()
        parts.append(f"{branch_field} eq {bc if bc.isdigit() else repr(bc)}")
    return " and ".join(parts) if parts else None

def _date_iso(dt: datetime) -> str:
    return dt.isoformat()

def _guess_datetime_fields_from_sample(items: List[Dict[str,Any]]) -> List[str]:
    found=set()
    for it in items:
        for k,v in it.items():
            if isinstance(v,str):
                try:
                    dtparser.isoparse(v); found.add(k)
                except Exception:
                    pass
    ordered = [f for f in DATE_FIELD_HINTS if f in found]
    for f in sorted(found):
        if f not in ordered:
            ordered.append(f)
    return ordered

def _coerce_datetime_cols(df: pd.DataFrame) -> pd.DataFrame:
    candidates = [
        "date","Date","movementDate","MovementDate","issueDate","IssueDate",
        "emissionDate","EmissionDate","creationDate","CreatedDate","entryDate","exitDate","lastEditTime"
    ]
    for c in candidates:
        if c in df.columns:
            df[c] = pd.to_datetime(df[c], errors="coerce", utc=False)
    return df

def _strip_timezone_for_excel(df: pd.DataFrame, target_tz: str = "America/Sao_Paulo") -> pd.DataFrame:
    from pandas.api.types import is_datetime64_any_dtype
    for col in df.columns:
        if is_datetime64_any_dtype(df[col]):
            tzinfo = getattr(df[col].dtype, "tz", None)
            if tzinfo is not None:
                try:
                    df[col] = df[col].dt.tz_convert(target_tz).dt.tz_localize(None)
                except Exception:
                    df[col] = df[col].dt.tz_localize(None)
    return df

def _jsonify_non_scalars_for_excel(df: pd.DataFrame) -> pd.DataFrame:
    def to_excel_scalar(x):
        if isinstance(x, (list, dict, tuple, set)):
            try:
                return json.dumps(x, ensure_ascii=False)
            except Exception:
                return str(x)
        return x
    for c in df.columns:
        df[c] = df[c].apply(to_excel_scalar)
    return df

def _first_existing(cols: list[str], df_cols: set[str]) -> Optional[str]:
    for c in cols:
        if c in df_cols: return c
    return None

def _smart_sort(df: pd.DataFrame, ordenar_cols: List[str]) -> pd.DataFrame:
    cols = [c for c in ordenar_cols if c in df.columns]
    if not cols:
        maybe_date = _first_existing(["date","Date","movementDate","MovementDate","issueDate","IssueDate"], set(df.columns))
        maybe_id   = _first_existing(["internalId","InternalId","movementId","MovementId"], set(df.columns))
        cols = [c for c in [maybe_date, maybe_id] if c]
    if cols:
        return df.sort_values(cols, kind="stable")
    return df

def _autosize_openpyxl(ws):
    from openpyxl.utils import get_column_letter
    dims = {}
    for row in ws.rows:
        for cell in row:
            if cell.value is None: continue
            val = str(cell.value)
            dims[cell.column] = max(dims.get(cell.column, 0), len(val))
    for col, width in dims.items():
        ws.column_dimensions[get_column_letter(col)].width = min(max(width + 2, 10), 60)

# ==============================
# CATÁLOGO
# ==============================
def get_company_name(company_code: str) -> str:
    # evita chamadas para /api/framework/v1/companies
    return f"Empresa {company_code}"


def list_branches(company_code: str) -> list[dict]:
    # usa sempre a filial informada por parâmetro
    return [{"CompanyCode": str(company_code), "Code": args.filial or "1", "Title": "Filial simulada"}]


def list_companies() -> List[Dict[str, str]]:
    urls = [
        f"{HOST}/api/framework/v1/companies",
        f"{HOST}/api/framework/v1/Companies",
        f"{HOST}/api/corp/v1/companies",
    ]
    out: List[Dict[str,str]] = []
    seen = set()
    for url in urls:
        r, exc, _ = _safe_request("GET", url)
        if not r or r.status_code != 200:
            continue
        try:
            j = r.json()
        except Exception:
            continue
        items = j.get("items", j) if isinstance(j, dict) else j
        if not isinstance(items, list):
            continue
        for obj in items:
            code = str(obj.get("code") or obj.get("Code") or obj.get("companyCode") or obj.get("CompanyCode") or "").strip()
            name = str(obj.get("name") or obj.get("Name") or obj.get("title") or obj.get("Title") or obj.get("description") or "").strip()
            if not code: continue
            code = _norm_code(code)
            if code in seen: continue
            seen.add(code)
            out.append({"Code": code, "Name": name})
        if out:
            break
    return out



# ==============================
# MOVEMENTS
# ==============================


def get_customer_vendor_info(code: str, company_id: str) -> Optional[Dict[str, Any]]:
    if not code:
        return None

    url = f"{HOST}/api/fin/v1/CustomerVendor"
    params = {
        "$filter": f"code eq '{code}' and companyId eq {company_id}",
        "$select": "code,shortName,name,mainNIF"
    }

    r, exc, _ = _safe_request("GET", url, params=params)
    if not r or r.status_code != 200:
        return None

    j = r.json()
    items = j.get("items", [])
    if items:
        return items[0]

    # 🔄 fallback: tenta sem companyId
    params["$filter"] = f"code eq '{code}'"
    r, exc, _ = _safe_request("GET", url, params=params)
    if not r or r.status_code != 200:
        return None

    j = r.json()
    return (j.get("items") or [None])[0]


def _get_movements_page(page: int, page_size: int, odata_filter: Optional[str], select_fields: List[str]):
    """
    Executa a chamada à API /movements, garantindo que o filtro de empresa/filial e de movimentos
    seja combinado corretamente sem ser sobrescrito.
    """
    url = f"{HOST}{MOV_ENDPOINT}"
    params = {
        "page": max(1, page),
        "pageSize": max(1, page_size)
    }

    # Filtro base (empresa + filial)
    base_filter = odata_filter or ""

    # Filtro adicional de movimento
    if getattr(args, "movimento", None):
        movimentos = [m.strip() for m in args.movimento.split(",") if m.strip()]
        if len(movimentos) == 1:
            mov_filtro = f"movementTypeCode eq '{movimentos[0]}'"
        else:
            clauses = [f"movementTypeCode eq '{m}'" for m in movimentos]
            mov_filtro = " or ".join(clauses)

        # ✅ Garante que o filtro de movimento é somado, não sobrescrito
        if base_filter:
            base_filter = f"({base_filter}) and ({mov_filtro})"
        else:
            base_filter = mov_filtro

    # Aplica filtro final
    if base_filter:
        params["$filter"] = base_filter

    # Campos selecionados
    if select_fields:
        params["$select"] = ",".join(select_fields)

    if DEBUG_VERBOSE:
        print(f"DEBUG: chamando GET {url}")
        print(f"       params = {json.dumps(params, indent=2, ensure_ascii=False)}")

    # Execução HTTP
    r, exc, _ = _safe_request("GET", url, params=params)
    meta = {"status": r.status_code if r else -1}

    if not r:
        return [], -1, meta

    if 200 <= r.status_code < 300 or r.status_code == 206:
        try:
            j = r.json()
        except Exception:
            return [], r.status_code, meta

        # Normaliza retorno
        if isinstance(j, dict):
            data = j.get("items") or j.get("Items")
            items = data if isinstance(data, list) else ([j] if not isinstance(j, list) else [])
        elif isinstance(j, list):
            items = j
        else:
            items = []

        return items, r.status_code, meta

    return [], r.status_code, meta


def _try_date_filter_server_side(company_field: Optional[str], branch_field: Optional[str],
                                 comp_code: str, branch_code: str,
                                 dt_ini: datetime, dt_fim: datetime,
                                 sample_date_candidates: List[str],
                                 select_fields: List[str]) -> Tuple[Optional[str], Optional[str]]:
    base_filter = _build_company_branch_filter(company_field, branch_field, comp_code, branch_code)
    candidates = list(dict.fromkeys(DATE_FIELD_HINTS + sample_date_candidates))
    for df in candidates:
        date_part = f"{df} ge {_date_iso(dt_ini)} and {df} le {_date_iso(dt_fim)}"
        odata = f"{base_filter} and {date_part}" if base_filter else date_part
        items, st, _ = _get_movements_page(1, 1, odata, select_fields)
        if st == 200:
            return odata, df
    return None, None

# ==============================
# EXTRAÇÃO SEQUENCIAL POR PÁGINAS (estável e rápida)
# ==============================
def extract_branch_items(comp_norm: str, b_code: str,
                         comp_field: str, br_field: str,
                         dt_ini: datetime, dt_fim: datetime,
                         date_candidates: List[str],
                         page_size_first: int,
                         select_fields: List[str],
                         deadline_sec: int) -> List[Dict[str,Any]]:
    header = f"  → Filial código {b_code or '(vazio)'}"
    print("\n" + header)
    print("  " + "—"*len(header))

    server_odata, used_date_field = _try_date_filter_server_side(
        comp_field, br_field, comp_norm, b_code, dt_ini, dt_fim, date_candidates, select_fields
    )
    if server_odata:
        print(f"    ✅ Filtro server-side aceito: {server_odata}")
    else:
        server_odata = _build_company_branch_filter(comp_field, br_field, comp_norm, b_code)
        print("    ⚠️ Sem campo de data aceito no servidor. Período será filtrado no cliente.")

    # cadeia degradável de page sizes
    ps_chain = [max(1, page_size_first), 50, 25, 10, 5, 1]
    ps_chain = [ps_chain[0]] + [p for p in ps_chain[1:] if p < ps_chain[0]]

    page = 1
    ps_index = 0
    empty_seq = 0
    started_at = _now()
    out: List[Dict[str,Any]] = []

    # dedupe por filial (id pode repetir entre páginas em alguns backends)
    seen_ids = set()
    id_fields = ["internalId","InternalId","movementId","MovementId","id","Id"]

    def in_period_local(rec: Dict[str, Any]) -> bool:
        v = rec.get("date")
        if isinstance(v, str):
            try:
                d = dtparser.isoparse(v)
                return dt_ini <= d <= dt_fim
            except Exception:
                pass
        for key in (date_candidates or []) + DATE_FIELD_HINTS:
            v = rec.get(key)
            if isinstance(v, str):
                try:
                    d = dtparser.isoparse(v)
                    if dt_ini <= d <= dt_fim:
                        return True
                except Exception:
                    pass
        return False
    global_limit = getattr(args, "limit", 0)

    max_pages = getattr(args, "max_pages", 1)
    while page <= min(max_pages, MAX_PAGES_PER_BRANCH):

        if deadline_sec and (_now() - started_at > deadline_sec):
            print(f"      ⏱️ Tempo excedido ({deadline_sec}s) — encerrando filial com {len(out)} itens.")
            break

        page_size = ps_chain[ps_index]
        items, st, _ = _get_movements_page(page, page_size, server_odata, select_fields)

        if st == -1 or st >= 500:
            if ps_index < len(ps_chain) - 1:
                ps_index += 1
                print(f"      ⚠️ st={st} — reduzindo pageSize para {ps_chain[ps_index]} e repetindo pág {page}…")
                continue
            print("      ⚠️ Falha persistente — tentando sem $filter e filtrando localmente…")
            items2, st2, _ = _get_movements_page(page, page_size, None, select_fields)
            if st2 >= 400 or st2 == -1:
                print(f"      ⚠️ HTTP {st2} mesmo sem filtro — encerrando filial.")
                break
            items = items2
            # filtros locais
            if comp_field:
                items = [it for it in items if str(it.get(comp_field)) == _norm_code(comp_norm)]
            if br_field and str(b_code).strip() != "":
                items = [it for it in items if str(it.get(br_field, "")).strip() == str(b_code).strip()]
            if not used_date_field:
                items = [it for it in items if in_period_local(it)]
        elif st >= 400:
            print(f"      ⚠️ HTTP {st} — pulando filial.")
            break

        if not items:
            empty_seq += 1
            print(f"    · Página {page}: 0 itens (vazias seguidas={empty_seq}) | ps={page_size}")
            if empty_seq >= 2:
                print("      ↳ Duas páginas vazias — fim desta filial.")
                break
            page += 1
            continue

        if not used_date_field:
            items = [it for it in items if in_period_local(it)]

        # dedupe por filial
        deduped = []
        for it in items:
            rid = None
            for f in id_fields:
                if f in it: rid = str(it[f]); break
            if rid and rid in seen_ids:
                continue
            if rid: seen_ids.add(rid)
            deduped.append(it)
        items = deduped

        out.extend(items)
        print(f"    · Página {page}: {len(items)} itens (acum={len(out)}) | ps={page_size}")
        if global_limit and len(out) >= global_limit:
            print(f"      ⏹️ Limite global de {global_limit} registros atingido — encerrando coleta desta filial.")
            out = out[:global_limit]
            break


        if len(items) < page_size:
            print("      ↳ Última página (len(items) < pageSize).")
            break
        page += 1

    return out

# ==============================
# MAIN
# ==============================
def run_extract(args):
    TZBR = tz.gettz("America/Sao_Paulo")
    dt_ini = datetime.strptime(args.inicio, "%Y-%m-%d").replace(hour=0, minute=0, second=0, tzinfo=TZBR)
    dt_fim = datetime.strptime(args.fim, "%Y-%m-%d").replace(hour=23, minute=59, second=59, tzinfo=TZBR)

    print(">>> Iniciando extração de movements…")
    print(f"Período: {dt_ini.isoformat()}  até  {dt_fim.isoformat()}")
    print(f"Endpoint: {HOST}{MOV_ENDPOINT}")

    # empresas alvo
    if args.empresa:
        target_companies = [_norm_code(x) for x in args.empresa.split(",")]

        print(f"🏭 Empresa informada por parâmetro: {target_companies[0]}")
        companies_meta = []
    else:
        companies = list_companies()
        if not companies:
            target_companies = ["1", "148"]
            print("⚠️ Não consegui listar empresas automaticamente; usando fallback ['4','148'].")
            companies_meta = []
        else:
            target_companies = [c["Code"] for c in companies]
            companies_meta = companies
            print(f"🏭 Empresas detectadas automaticamente: {', '.join(target_companies)}")

    # $select
    if args.select_fields == "":
        select_fields = []
    elif args.select_fields is None:
        select_fields = DEFAULT_SELECT_FIELDS[:]
    else:
        select_fields = [c.strip() for c in args.select_fields.split(",") if c.strip()]

    page_size_first = max(1, args.page_size)
    branch_deadline = max(0, args.branch_deadline_seconds)

    all_rows: List[Dict[str,Any]] = []
    all_rows_lock = Lock()

    # função por empresa (filiais em paralelo; empresas sequenciais → logs mais legíveis)
    def process_company(comp):
        comp_norm = _norm_code(comp)
        comp_name = get_company_name(comp_norm)
        if not comp_name and companies_meta:
            try:
                comp_name = next((c["Name"] for c in companies_meta if c["Code"] == comp_norm), "")
            except Exception:
                pass

        print("\n" + "="*72)
        print(f"🏢 Empresa {comp_norm} — {comp_name or '(sem nome)'}")
        print("="*72)

        branches = list_branches(comp_norm)
        print(f"  📚 Filiais detectadas: {len(branches)}")

        if args.filial:
            branches = [b for b in branches if str(b.get("Code","")).strip() == str(args.filial).strip()]
            print(f"  🔎 Filtro de filial: {args.filial} → {len(branches)} filial(is)")
        if not branches:
            print("  ⚠️ Nenhuma filial após filtro. Pulando empresa.")
            return

        # probe /movements para detectar campos
        probe_items, status, _ = _get_movements_page(1, 5, None, select_fields)
        if status >= 400 or status == -1 or not probe_items:
            probe_items2, status2, _ = _get_movements_page(1, 1, None, select_fields)
            if status2 >= 400 or status2 == -1 or not probe_items2:
                print(f"  ❌ Probe /movements falhou (HTTP {status2}).")
                return
            probe_items = probe_items2

        probe_row = probe_items[0] if probe_items else {}
        comp_field = _detect_field_present(probe_row, COMPANY_KEYS) or "companyId"
        br_field   = _detect_field_present(probe_row, BRANCH_KEYS)  or "branchId"
        date_candidates = _guess_datetime_fields_from_sample(probe_items)
        print(f"  🔎 Campos /movements → company='{comp_field}', branch='{br_field}'")

        # filiais em paralelo
        futures = []
        with ThreadPoolExecutor(max_workers=args.concurrency_branches) as ex:
            for b in branches:
                b_code = b.get("Code","")
                futures.append(ex.submit(
                    extract_branch_items,
                    comp_norm, b_code, comp_field, br_field,
                    dt_ini, dt_fim, date_candidates,
                    page_size_first, select_fields, branch_deadline
                ))

            total_emp = 0
            for fut in as_completed(futures):
                try:
                    items = fut.result()
                except Exception as e:
                    print(f"    ⚠️ Erro processando filial: {e}")
                    continue
                with all_rows_lock:
                    all_rows.extend(items)
                    total_emp += len(items)
                print(f"  ✅ Parcial empresa {comp_norm}: +{len(items)} itens (acum empresa={total_emp})")

        print(f"  ✅ Empresa {comp_norm} finalizada.")

    # percorre TODAS as empresas (sequencialmente para logs limpos)
    for comp in target_companies:
        process_company(comp)

    # ==============================
    # CONSOLIDAÇÃO
    # ==============================
    print("\n>>> Consolidando em DataFrame…")
    if not all_rows:
        print("(Sem registros coletados.)")
        return

    df = json_normalize(all_rows, sep=".")
    # ============================================
    # 🔍 Enriquecimento: busca CNPJ e Razão Social
    # ============================================
    print("🔍 Enriquecendo com CNPJ e Razão Social...")

    if "customerVendorCode" in df.columns and "companyId" in df.columns:
        unique_pairs = df[["customerVendorCode", "companyId"]].dropna().drop_duplicates()
        lookup = {}

        for _, row in unique_pairs.iterrows():
            code = str(row["customerVendorCode"]).strip()
            comp = str(row["companyId"]).strip()
            info = get_customer_vendor_info(code, comp)
            if info:
                lookup[(code, comp)] = {
                    "cnpj": info.get("mainNIF"),
                    "razao_social": info.get("shortName") or info.get("name")
                }

        df["cnpj"] = df.apply(lambda r: lookup.get((str(r["customerVendorCode"]), str(r["companyId"])), {}).get("cnpj"), axis=1)
        df["razao_social"] = df.apply(lambda r: lookup.get((str(r["customerVendorCode"]), str(r["companyId"])), {}).get("razao_social"), axis=1)
    else:
        print("⚠️ Campos customerVendorCode ou companyId não encontrados; não foi possível enriquecer.")

    df = _coerce_datetime_cols(df)
    df = _strip_timezone_for_excel(df)

    ordenar_cols = [c.strip() for c in (args.ordenar or "").split(",") if c.strip()]
    df = _smart_sort(df, ordenar_cols)

    branch_col  = _first_existing(["branchId","BranchId","branchCode","BranchCode"], set(df.columns))
    company_col = _first_existing(["companyId","CompanyId","companyCode","CompanyCode"], set(df.columns))
    resumo = None
    if branch_col:
        grp_cols = [c for c in [company_col, branch_col] if c]
        resumo = df.groupby(grp_cols, dropna=False).size().reset_index(name="qtd").sort_values("qtd", ascending=False)
        resumo = _strip_timezone_for_excel(resumo)

    # ==============================
    # XLSX
    # ==============================
    base = args.arquivo
    if not base:
        emp = f"emp{args.empresa}" if args.empresa else "empVARIAS"
        fil = f"_fil{args.filial}" if args.filial else ""
        base = f"movements_{emp}{fil}_{args.inicio}_a_{args.fim}.xlsx"
    if not base.lower().endswith(".xlsx"):
        base += ".xlsx"

    from openpyxl import Workbook
    from openpyxl.styles import Font, PatternFill

    wb = Workbook()
    ws_tree = wb.active
    ws_tree.title = "Árvore"
    ws_data = wb.create_sheet("Dados")

    df_excel = _jsonify_non_scalars_for_excel(df.copy())
    ws_data.append(list(df_excel.columns))
    for row in df_excel.itertuples(index=False, name=None):
        ws_data.append(list(row))
    try: _autosize_openpyxl(ws_data)
    except Exception: pass

    bold = Font(bold=True)
    header_fill = PatternFill(start_color="FFECECEC", end_color="FFECECEC", fill_type="solid")
    company_fill = PatternFill(start_color="FFCCE5FF", end_color="FFCCE5FF", fill_type="solid")
    branch_fill  = PatternFill(start_color="FFFFF2CC", end_color="FFFFF2CC", fill_type="solid")

    tree_cols = [c.strip() for c in (args.colunas or "").split(",") if c.strip()]
    if not tree_cols:
        pref = ["date","internalId","movementId","documentNumber","type","grossValue","netValue","partner.name","status"]
        tree_cols = [c for c in pref if c in df.columns] or list(df.columns[:10])

    comp_series = df[company_col] if company_col in df.columns else pd.Series([""]*len(df))
    br_series   = df[branch_col]  if branch_col in df.columns else pd.Series([""]*len(df))
    unique_pairs = pd.DataFrame({"comp": comp_series, "branch": br_series}).drop_duplicates()

    filial_label_map: Dict[Tuple[str,str], str] = {}

    for comp_id in sorted(unique_pairs["comp"].astype(str).unique(), key=lambda x: (x is None, x)):
        comp_mask = (comp_series.astype(str) == str(comp_id))
        df_comp = df[comp_mask]

        ws_tree.append([f"Empresa: {comp_id}"])
        for cell in ws_tree[ws_tree.max_row]:
            cell.font = bold; cell.fill = company_fill

        filiais_comp = sorted(df_comp[branch_col].astype(str).unique()) if branch_col in df_comp.columns else [""]
        for br_id in filiais_comp:
            br_mask = (br_series.astype(str) == str(br_id))

            df_fil = df_comp[br_mask] if branch_col in df_comp.columns else df_comp

            br_name = filial_label_map.get((str(comp_id), str(br_id)), "")
            ws_tree.append([f"  Filial: {br_id} {('- ' + br_name) if br_name else ''}"])
            for cell in ws_tree[ws_tree.max_row]:
                cell.font = bold; cell.fill = branch_fill

            ws_tree.append([""] + tree_cols)
            for cell in ws_tree[ws_tree.max_row]:
                cell.font = bold; cell.fill = header_fill

            df_tree = df_fil[tree_cols].copy()
            df_tree = _jsonify_non_scalars_for_excel(df_tree)
            for _, rec in df_tree.iterrows():
                ws_tree.append([""] + [rec.get(c) for c in tree_cols])

            ws_tree.append([])

    try: _autosize_openpyxl(ws_tree)
    except Exception: pass

    if resumo is not None:
        ws_resumo = wb.create_sheet("Resumo")
        resumo_excel = _jsonify_non_scalars_for_excel(resumo.copy())
        ws_resumo.append(list(resumo_excel.columns))
        for row in resumo_excel.itertuples(index=False, name=None):
            ws_resumo.append(list(row))
        try: _autosize_openpyxl(ws_resumo)
        except Exception: pass

    try:
        # 1. cria arquivo temporário
        tmpfile = tempfile.NamedTemporaryFile(delete=False, suffix=".xlsx").name

        # 2. salva no temp
        wb.save(tmpfile)
        wb.close()  # fecha handle do openpyxl

        # 3. move para destino final (substitui se existir)
        shutil.move(tmpfile, base)

    except Exception as e:
        alt = os.path.splitext(base)[0] + ".csv"
        print(f"⚠️ Falha ao salvar XLSX ({e}). Salvando CSV com dados completos: {alt}")
        df_excel.to_csv(alt, index=False)
        base = alt

    print(f"\n✅ Arquivo gerado: {os.path.abspath(base)}")

def parse_args():
    ap = argparse.ArgumentParser(description="Extrai Movements e gera XLSX em árvore + dados completos (rápido & estável).")
    ap.add_argument("--inicio",   required=True, help="Data inicial (YYYY-MM-DD).")
    ap.add_argument("--fim",      required=True, help="Data final (YYYY-MM-DD).")
    ap.add_argument("--empresa", required=False, help="IDs de empresas separados por vírgula, ex.: 4,147,148,149")

    ap.add_argument("--filial",   required=False, help="branchId (filial), ex.: 17")
    ap.add_argument("--ordenar",  required=False, default="date,internalId", help="Colunas para ordenar (vírgula).")
    ap.add_argument("--colunas",  required=False, default="", help="Colunas para mostrar na aba Árvore (vírgula).")
    ap.add_argument("--arquivo",  required=False, default="", help="Caminho do .xlsx de saída.")

    # performance/completude
    ap.add_argument("--page-size", type=int, default=100, help="Itens por página (o servidor pode limitar; varre até o fim).")
    ap.add_argument("--concurrency-branches", type=int, default=4, help="Filiais processadas em paralelo.")
    ap.add_argument("--branch-deadline-seconds", type=int, default=BRANCH_DEADLINE_SEC_DEFAULT,
                    help="Tempo máx por filial; 0 = sem limite (não perder itens).")

    # $select (string vazia desliga)
    ap.add_argument("--select-fields", type=str, default=",".join(DEFAULT_SELECT_FIELDS),
                    help='Campos em $select (ex.: "date,internalId,movementId,companyId,branchId"). Use "" para desativar.')
    ap.add_argument("--movimento", required=False, help="Lista de códigos de movementTypeCode separados por vírgula, ex.: 1.2.01,1.2.09,1.2.10.")
    ap.add_argument("--limit", type=int, default=0, help="Limite máximo de registros a coletar (0 = sem limite).")
    ap.add_argument("--max-pages", type=int, default=1, help="Número máximo de páginas a buscar por filial (padrão=1).")



    return ap.parse_args()

if __name__ == "__main__":
    args = parse_args()
    try:
        run_extract(args)
    except KeyboardInterrupt:
        print("\nInterrompido pelo usuário.")
        sys.exit(130)
    except Exception as e:
        print(f"\nERRO fatal: {e}")
        sys.exit(2)


----- FILE: app.py  (sha1:28e3f6ed5c45, bytes:4964) -----
import os
import logging
from flask import Flask
from flask_sqlalchemy import SQLAlchemy
from flask_login import LoginManager
from sqlalchemy.orm import DeclarativeBase
from werkzeug.middleware.proxy_fix import ProxyFix
from flask_migrate import Migrate

# ---------------------------------------------
# CONFIGURAÇÃO DE LOG
# ---------------------------------------------
logging.basicConfig(level=logging.DEBUG)


# ---------------------------------------------
# BASE MODEL (usada pelo SQLAlchemy 2.0)
# ---------------------------------------------
class Base(DeclarativeBase):
    pass


# ---------------------------------------------
# INICIALIZAÇÃO DAS EXTENSÕES
# ---------------------------------------------
db = SQLAlchemy(model_class=Base)
login_manager = LoginManager()
migrate = Migrate()


# ---------------------------------------------
# FUNÇÃO FACTORY PRINCIPAL DO APP
# ---------------------------------------------
def create_app():
    app = Flask(__name__)
    @app.template_filter('translate_field')
    def translate_field(field_name):
        """Traduz os nomes técnicos dos campos JSON para português."""
        labels = {
            'companyId': 'Empresa',
            'movementId': 'Movimento',
            'paymentSequentialId': 'Sequência do Pagamento',
            'debitCredit': 'Débito/Crédito',
            'value': 'Valor',
            'paymentType': 'Tipo de Pagamento',
            'dueDate': 'Data de Vencimento',
            'paymentFormId': 'Forma de Pagamento (ID)',
            'entryId': 'Lançamento',
            'cashAccountCompanyId': 'Empresa da Conta Caixa',
            'cashAccount': 'Conta Caixa',
            'administrateTax': 'Administrar Imposto',
            'multiplePayment': 'Pagamento Múltiplo',
            'paymentFormType': 'Tipo de Forma de Pagamento',
            'paymentFormDescription': 'Descrição da Forma de Pagamento',
            'movementItemSequentialId': 'Seq. do Item do Movimento',
            'taxId': 'ID do Imposto',
            'calculationBasis': 'Base de Cálculo',
            'aliquot': 'Alíquota (%)',
            'fullBase': 'Base Completa',
            'edited': 'Editado',
            'description': 'Descrição',
            'registerDate': 'Data de Registro',
            'deliveryDate': 'Data de Entrega',
            'entryDate': 'Data de Entrada',
            'exitDate': 'Data de Saída',
            'creationDate': 'Data de Criação'
        }
        return labels.get(field_name, field_name)

    # -----------------------------
    # CONFIGURAÇÕES GERAIS
    # -----------------------------
    app.secret_key = os.environ.get("SESSION_SECRET", "dev-secret-key-change-in-production")
    app.config["SQLALCHEMY_DATABASE_URI"] = os.environ.get("DATABASE_URL", "sqlite:///financial_data.db")
    app.config["SQLALCHEMY_TRACK_MODIFICATIONS"] = False
    app.config["SQLALCHEMY_ENGINE_OPTIONS"] = {
        "pool_recycle": 300,
        "pool_pre_ping": True,
    }
    app.config["MAX_CONTENT_LENGTH"] = 50 * 1024 * 1024  # 50MB
    app.config["UPLOAD_FOLDER"] = "uploads"

    # Garante que a pasta de upload existe
    os.makedirs(app.config["UPLOAD_FOLDER"], exist_ok=True)

    # -----------------------------
    # INICIALIZA AS EXTENSÕES
    # -----------------------------
    db.init_app(app)
    migrate.init_app(app, db)
    login_manager.init_app(app)
    login_manager.login_view = "auth.login"
    login_manager.login_message = "Please log in to access this page."

    # Corrige URLs em servidores com proxy/reverso (Nginx etc.)
    app.wsgi_app = ProxyFix(app.wsgi_app, x_proto=1, x_host=1)

    # -----------------------------
    # REGISTRA MODELOS E USUÁRIO ADMIN PADRÃO
    # -----------------------------
    with app.app_context():
        import models  # importa todos os modelos para registrar

        db.create_all()  # cria tabelas se ainda não existirem

        # Cria usuário admin se não existir
        from models import User
        from werkzeug.security import generate_password_hash

        admin = User.query.filter_by(username="admin").first()
        if not admin:
            admin = User(
                username="admin",
                email="admin@example.com",
                is_admin=True,
                password_hash=generate_password_hash("admin123"),
            )
            db.session.add(admin)
            db.session.commit()
            logging.info("✅ Created admin user: admin / admin123")

    # -----------------------------
    # REGISTRA BLUEPRINTS
    # -----------------------------
    from routes import main_bp, admin_bp, auth_bp

    app.register_blueprint(main_bp)
    app.register_blueprint(admin_bp, url_prefix="/admin")
    app.register_blueprint(auth_bp, url_prefix="/auth")

    return app


# ---------------------------------------------
# INSTÂNCIA GLOBAL DO APP (usada pelo flask run)
# ---------------------------------------------
app = create_app()


----- FILE: complete_model_fields.txt  (sha1:70db8a0fe187, bytes:13123) -----
# Complete column mapping for file_processor.py:
COLUMN_MAPPING = {
    'internalId': 'internal_id',
    'companyId': 'company_id',
    'movementId': 'movement_id',
    'branchId': 'branch_id',
    'warehouseCode': 'warehouse_code',
    'destinyWarehouseCode': 'destiny_warehouse_code',
    'number': 'number',
    'series': 'series',
    'movementTypeCode': 'movement_type_code',
    'type': 'type',
    'status': 'status',
    'printed': 'printed',
    'documentPrinted': 'document_printed',
    'billPrinted': 'bill_printed',
    'registerDate': 'register_date',
    'exitDate': 'exit_date',
    'commercialRepresentativeCharge': 'commercial_representative_charge',
    'grossValue': 'gross_value',
    'netValue': 'net_value',
    'informedNetValue': 'informed_net_value',
    'otherValues': 'other_values',
    'discountPercentage': 'discount_percentage',
    'expensePercentage': 'expense_percentage',
    'expenseValue': 'expense_value',
    'extraPercentage1': 'extra_percentage1',
    'extraValue1': 'extra_value1',
    'extraPercentage2': 'extra_percentage2',
    'extraValue2': 'extra_value2',
    'transportedProductNetWeight': 'transported_product_net_weight',
    'transportedProductGrossWeight': 'transported_product_gross_weight',
    'classificationTable5Code': 'classification_table5_code',
    'financialOptionalTable2Code': 'financial_optional_table2_code',
    'netValueCurrencyCode': 'net_value_currency_code',
    'date': 'date',
    'hasGeneratedBill': 'has_generated_bill',
    'auxCustomerVendorCode': 'aux_customer_vendor_code',
    'auxCustomerVendorCompanyId': 'aux_customer_vendor_company_id',
    'costCenterCode': 'cost_center_code',
    'salesman1Code': 'salesman1_code',
    'chargePercentage': 'charge_percentage',
    'salesman2ChargePercentage': 'salesman2_charge_percentage',
    'salesman3ChargePercentage': 'salesman3_charge_percentage',
    'salesman4ChargePercentage': 'salesman4_charge_percentage',
    'userCode': 'user_code',
    'destinyBranchId': 'destiny_branch_id',
    'lotGenerated': 'lot_generated',
    'accountingExportStatus': 'accounting_export_status',
    'deliveryDate': 'delivery_date',
    'hasGeneratedWorkAccount': 'has_generated_work_account',
    'workAccountGenerated': 'work_account_generated',
    'lastEditTime': 'last_edit_time',
    'indicateObjectUse': 'indicate_object_use',
    'bonumIntegrated': 'bonum_integrated',
    'processedFlag': 'processed_flag',
    'icmsDeductionValue': 'icms_deduction_value',
    'creationUser': 'creation_user',
    'creationDate': 'creation_date',
    'emailStatus': 'email_status',
    'internalGrossValue': 'internal_gross_value',
    'otherCompanyINSSBaseValue': 'other_company_inss_base_value',
    'conditionalDiscountValue': 'conditional_discount_value',
    'conditionalExpenseValue': 'conditional_expense_value',
    'affectStockOrder': 'affect_stock_order',
    'commercialAutomationExported': 'commercial_automation_exported',
    'aplicationIntegration': 'aplication_integration',
    'entryDate': 'entry_date',
    'extemporaneous': 'extemporaneous',
    'merchandiseValue': 'merchandise_value',
    'usesFinancialValueApportionment': 'uses_financial_value_apportionment',
    'conclusionFlag': 'conclusion_flag',
    'paradigmaStatus': 'paradigma_status',
    'paradigmaAutoIntegrated': 'paradigma_auto_integrated',
    'originalGrossValue': 'original_gross_value',
    'originalNetValue': 'original_net_value',
    'originalOtherValues': 'original_other_values',
    'operationId': 'operation_id',
    'scpBranchId': 'scp_branch_id',
    'movementItems': 'movement_items',
    'payments': 'payments',
    'costCenterApportionments': 'cost_center_apportionments',
    'departmentApportionments': 'department_apportionments',
    'taxes': 'taxes',
    'fiscal': 'fiscal',
    'norm': 'norm',
    'cargoComponent': 'cargo_component',
    'thirdPartyNF': 'third_party_nf',
    'safetyDevice': 'safety_device',
    'nfe': 'nfe',
    'inputCTRC': 'input_ctrc',
    'outputCTRC': 'output_ctrc',
    'ctrc': 'ctrc',
    'transportData': 'transport_data',
    'documentAuthorization': 'document_authorization',
    'judicialProcess': 'judicial_process',
    'serviceOrder': 'service_order',
    'relatedMovement': 'related_movement',
    'exportRelatedMovement': 'export_related_movement',
    'linkedMovement': 'linked_movement',
    'cTe': 'c_te',
    'eaiIntegration': 'eai_integration',
    'electronicInvoiceFreeFields': 'electronic_invoice_free_fields',
    'customerVendorCode': 'customer_vendor_code',
    'paymentTermCode': 'payment_term_code',
    'observation': 'observation',
    'financialOptionalTable1Code': 'financial_optional_table1_code',
    'financialEntryMovementId': 'financial_entry_movement_id',
    'generatedEntryNumber': 'generated_entry_number',
    'openEntryNumber': 'open_entry_number',
    'cashAccountCode': 'cash_account_code',
    'customerVendorCompanyId': 'customer_vendor_company_id',
    'fluxusGroupedFlag': 'fluxus_grouped_flag',
    'cashAccountCompanyId': 'cash_account_company_id',
    'longHistory': 'long_history',
}

# Fields for FinancialMovement model:
    internal_id = db.Column(db.String(50), nullable=True)
    company_id = db.Column(db.String(50), nullable=True)
    movement_id = db.Column(db.String(50), nullable=True)
    branch_id = db.Column(db.String(50), nullable=True)
    warehouse_code = db.Column(db.String(50), nullable=True)
    destiny_warehouse_code = db.Column(db.String(50), nullable=True)
    number = db.Column(db.String(255), nullable=True)
    series = db.Column(db.String(255), nullable=True)
    movement_type_code = db.Column(db.String(50), nullable=True)
    type = db.Column(db.String(255), nullable=True)
    status = db.Column(db.String(255), nullable=True)
    printed = db.Column(db.Boolean, nullable=True)
    document_printed = db.Column(db.Boolean, nullable=True)
    bill_printed = db.Column(db.Boolean, nullable=True)
    register_date = db.Column(db.DateTime, nullable=True)
    exit_date = db.Column(db.DateTime, nullable=True)
    commercial_representative_charge = db.Column(db.Float, nullable=True)
    gross_value = db.Column(db.Float, nullable=True)
    net_value = db.Column(db.Float, nullable=True)
    informed_net_value = db.Column(db.Float, nullable=True)
    other_values = db.Column(db.String(255), nullable=True)
    discount_percentage = db.Column(db.Float, nullable=True)
    expense_percentage = db.Column(db.Float, nullable=True)
    expense_value = db.Column(db.Float, nullable=True)
    extra_percentage1 = db.Column(db.String(255), nullable=True)
    extra_value1 = db.Column(db.String(255), nullable=True)
    extra_percentage2 = db.Column(db.String(255), nullable=True)
    extra_value2 = db.Column(db.String(255), nullable=True)
    transported_product_net_weight = db.Column(db.Float, nullable=True)
    transported_product_gross_weight = db.Column(db.Float, nullable=True)
    classification_table5_code = db.Column(db.String(50), nullable=True)
    financial_optional_table2_code = db.Column(db.String(50), nullable=True)
    net_value_currency_code = db.Column(db.String(50), nullable=True)
    date = db.Column(db.String(255), nullable=True)
    has_generated_bill = db.Column(db.Boolean, nullable=True)
    aux_customer_vendor_code = db.Column(db.String(50), nullable=True)
    aux_customer_vendor_company_id = db.Column(db.String(50), nullable=True)
    cost_center_code = db.Column(db.String(50), nullable=True)
    salesman1_code = db.Column(db.String(50), nullable=True)
    charge_percentage = db.Column(db.Float, nullable=True)
    salesman2_charge_percentage = db.Column(db.Float, nullable=True)
    salesman3_charge_percentage = db.Column(db.Float, nullable=True)
    salesman4_charge_percentage = db.Column(db.Float, nullable=True)
    user_code = db.Column(db.String(50), nullable=True)
    destiny_branch_id = db.Column(db.String(50), nullable=True)
    lot_generated = db.Column(db.String(255), nullable=True)
    accounting_export_status = db.Column(db.String(255), nullable=True)
    delivery_date = db.Column(db.DateTime, nullable=True)
    has_generated_work_account = db.Column(db.Boolean, nullable=True)
    work_account_generated = db.Column(db.String(255), nullable=True)
    last_edit_time = db.Column(db.DateTime, nullable=True)
    indicate_object_use = db.Column(db.String(255), nullable=True)
    bonum_integrated = db.Column(db.Boolean, nullable=True)
    processed_flag = db.Column(db.Boolean, nullable=True)
    icms_deduction_value = db.Column(db.Float, nullable=True)
    creation_user = db.Column(db.String(255), nullable=True)
    creation_date = db.Column(db.DateTime, nullable=True)
    email_status = db.Column(db.String(255), nullable=True)
    internal_gross_value = db.Column(db.Float, nullable=True)
    other_company_inss_base_value = db.Column(db.Float, nullable=True)
    conditional_discount_value = db.Column(db.Float, nullable=True)
    conditional_expense_value = db.Column(db.Float, nullable=True)
    affect_stock_order = db.Column(db.String(255), nullable=True)
    commercial_automation_exported = db.Column(db.String(255), nullable=True)
    aplication_integration = db.Column(db.String(255), nullable=True)
    entry_date = db.Column(db.DateTime, nullable=True)
    extemporaneous = db.Column(db.String(255), nullable=True)
    merchandise_value = db.Column(db.Float, nullable=True)
    uses_financial_value_apportionment = db.Column(db.Boolean, nullable=True)
    conclusion_flag = db.Column(db.Boolean, nullable=True)
    paradigma_status = db.Column(db.String(255), nullable=True)
    paradigma_auto_integrated = db.Column(db.Boolean, nullable=True)
    original_gross_value = db.Column(db.Float, nullable=True)
    original_net_value = db.Column(db.Float, nullable=True)
    original_other_values = db.Column(db.String(255), nullable=True)
    operation_id = db.Column(db.String(50), nullable=True)
    scp_branch_id = db.Column(db.String(50), nullable=True)
    movement_items = db.Column(db.Text, nullable=True)  # JSON field
    payments = db.Column(db.Text, nullable=True)  # JSON field
    cost_center_apportionments = db.Column(db.Text, nullable=True)  # JSON field
    department_apportionments = db.Column(db.Text, nullable=True)  # JSON field
    taxes = db.Column(db.Text, nullable=True)  # JSON field
    fiscal = db.Column(db.Text, nullable=True)  # JSON field
    norm = db.Column(db.Text, nullable=True)  # JSON field
    cargo_component = db.Column(db.Text, nullable=True)  # JSON field
    third_party_nf = db.Column(db.Text, nullable=True)  # JSON field
    safety_device = db.Column(db.Text, nullable=True)  # JSON field
    nfe = db.Column(db.Text, nullable=True)  # JSON field
    input_ctrc = db.Column(db.Text, nullable=True)  # JSON field
    output_ctrc = db.Column(db.Text, nullable=True)  # JSON field
    ctrc = db.Column(db.Text, nullable=True)  # JSON field
    transport_data = db.Column(db.Text, nullable=True)  # JSON field
    document_authorization = db.Column(db.Text, nullable=True)  # JSON field
    judicial_process = db.Column(db.Text, nullable=True)  # JSON field
    service_order = db.Column(db.Text, nullable=True)  # JSON field
    related_movement = db.Column(db.Text, nullable=True)  # JSON field
    export_related_movement = db.Column(db.Text, nullable=True)  # JSON field
    linked_movement = db.Column(db.Text, nullable=True)  # JSON field
    c_te = db.Column(db.Text, nullable=True)  # JSON field
    eai_integration = db.Column(db.Text, nullable=True)  # JSON field
    electronic_invoice_free_fields = db.Column(db.Text, nullable=True)  # JSON field
    customer_vendor_code = db.Column(db.String(50), nullable=True)
    payment_term_code = db.Column(db.String(50), nullable=True)
    observation = db.Column(db.String(255), nullable=True)
    financial_optional_table1_code = db.Column(db.String(50), nullable=True)
    financial_entry_movement_id = db.Column(db.String(50), nullable=True)
    generated_entry_number = db.Column(db.String(50), nullable=True)
    open_entry_number = db.Column(db.String(50), nullable=True)
    cash_account_code = db.Column(db.String(50), nullable=True)
    customer_vendor_company_id = db.Column(db.String(50), nullable=True)
    fluxus_grouped_flag = db.Column(db.Boolean, nullable=True)
    cash_account_company_id = db.Column(db.String(50), nullable=True)
    long_history = db.Column(db.String(255), nullable=True)

# JSON field processing methods:
    def get_json_field(self, field_name):
        """Get a JSON field as a Python object"""
        field_value = getattr(self, field_name, None)
        if field_value:
            try:
                return json.loads(field_value) if isinstance(field_value, str) else field_value
            except (json.JSONDecodeError, TypeError):
                return []
        return []

    def set_json_field(self, field_name, value):
        """Set a JSON field from a Python object"""
        if value:
            json_value = json.dumps(value) if not isinstance(value, str) else value
            setattr(self, field_name, json_value)
        else:
            setattr(self, field_name, None)


----- FILE: create_test_excel.py  (sha1:796d3b505bc3, bytes:4766) -----
import pandas as pd
from datetime import datetime
import json

# Create a test Excel file with the new company and branch fields
data = []

# Create 5 test records with the new structure
for i in range(1, 6):
    record = {
        # New company and branch fields (first 4 columns)
        'Empresa_Code': f'EMP_{i:03d}',
        'Empresa_Nome': f'Empresa Teste {i} Ltda',
        'Filial_Code': f'FIL_{i:03d}',
        'Filial_Nome': f'Filial {i} - Centro',
        
        # Existing core fields
        'internalId': f'{i}|{400+i}',
        'companyId': i,
        'movementId': 400+i,
        'branchId': i,
        'warehouseCode': str(i),
        'destinyWarehouseCode': str(i),
        'number': 12000+i,
        'series': 'A',
        'movementTypeCode': '1.2.02',
        'type': 'P',
        'status': 'F',
        'printed': False,
        'documentPrinted': False,
        'billPrinted': False,
        'registerDate': datetime.now(),
        'exitDate': datetime.now(),
        'commercialRepresentativeCharge': 0.0,
        'grossValue': 100.0 * i,
        'netValue': 100.0 * i,
        'informedNetValue': 0.0,
        'otherValues': 100.0 * i,
        'discountPercentage': 0.0,
        'expensePercentage': 0.0,
        'expenseValue': 0.0,
        'extraPercentage1': 0.0,
        'extraValue1': 0.0,
        'extraPercentage2': 0.0,
        'extraValue2': 0.0,
        'transportedProductNetWeight': 0.0,
        'transportedProductGrossWeight': 0.0,
        'classificationTable5Code': '',
        'financialOptionalTable2Code': '',
        'netValueCurrencyCode': 'R$',
        'date': datetime.now(),
        'hasGeneratedBill': False,
        'auxCustomerVendorCode': f'C{i:05d}',
        'auxCustomerVendorCompanyId': 0,
        'costCenterCode': '',
        'salesman1Code': '',
        'chargePercentage': 0.0,
        'salesman2ChargePercentage': 0.0,
        'salesman3ChargePercentage': 0.0,
        'salesman4ChargePercentage': 0.0,
        'userCode': 'testuser',
        'destinyBranchId': i,
        'lotGenerated': False,
        'accountingExportStatus': '0',
        'deliveryDate': '',
        'hasGeneratedWorkAccount': False,
        'workAccountGenerated': 'false',
        'lastEditTime': datetime.now(),
        'indicateObjectUse': '0',
        'bonumIntegrated': False,
        'processedFlag': False,
        'icmsDeductionValue': 0.0,
        'creationUser': 'testuser',
        'creationDate': datetime.now(),
        'emailStatus': 'false',
        'internalGrossValue': 100.0 * i,
        'otherCompanyINSSBaseValue': 0.0,
        'conditionalDiscountValue': 0.0,
        'conditionalExpenseValue': 0.0,
        'affectStockOrder': str(400+i),
        'commercialAutomationExported': 0,
        'aplicationIntegration': 'T',
        'entryDate': datetime.now(),
        'extemporaneous': 0,
        'merchandiseValue': 0.0,
        'usesFinancialValueApportionment': False,
        'conclusionFlag': 0,
        'paradigmaStatus': 'N',
        'paradigmaAutoIntegrated': False,
        'originalGrossValue': 0.0,
        'originalNetValue': 0.0,
        'originalOtherValues': 0.0,
        'operationId': 0,
        'scpBranchId': 0,
        'movementItems': '[]',
        'payments': '[]',
        'costCenterApportionments': '[]',
        'departmentApportionments': '[]',
        'taxes': '[]',
        'fiscal': '[]',
        'norm': '[]',
        'cargoComponent': '[]',
        'thirdPartyNF': '[]',
        'safetyDevice': '[]',
        'nfe': '[]',
        'inputCTRC': '[]',
        'outputCTRC': '[]',
        'ctrc': '[]',
        'transportData': '[]',
        'documentAuthorization': '[]',
        'judicialProcess': '[]',
        'serviceOrder': '[]',
        'relatedMovement': '[]',
        'exportRelatedMovement': '[]',
        'linkedMovement': '[]',
        'cTe': '[]',
        'eaiIntegration': '[]',
        'electronicInvoiceFreeFields': '[]',
        'customerVendorCode': f'C{i:05d}',
        'paymentTermCode': '30.0',
        'observation': f'Teste de movimento {i} com dados de empresa',
        'financialOptionalTable1Code': '2.0',
        'financialEntryMovementId': f'{400+i}.0',
        'generatedEntryNumber': '1.0',
        'openEntryNumber': '1.0',
        'cashAccountCode': '2.0',
        'customerVendorCompanyId': 0,
        'fluxusGroupedFlag': False,
        'cashAccountCompanyId': i
    }
    data.append(record)

# Create DataFrame and save to Excel
df = pd.DataFrame(data)
filename = 'test_excel_with_company_branch.xlsx'
df.to_excel(filename, index=False)
print(f"Created test Excel file: {filename}")
print(f"Columns: {len(df.columns)}")
print(f"Records: {len(df)}")
print("First 4 columns (new):")
for col in df.columns[:4]:
    print(f"  {col}: {df[col].iloc[0]}")

----- FILE: file_processor.py  (sha1:d5fa2a1fb139, bytes:22866) -----
import pandas as pd
import json
import uuid
import ast
from datetime import datetime
from typing import Dict, List, Tuple, Any
import logging

logger = logging.getLogger(__name__)

class FinancialDataProcessor:
    """Process Excel files containing financial movement data"""
    
    # Complete column mapping for all 117 fields (added 4 new company/branch fields)
    COLUMN_MAPPING = {
        'internalId': 'internal_id',
        'companyId': 'company_id',
        'movementId': 'movement_id',
        'branchId': 'branch_id',
        
        # New company and branch information fields
        'Empresa_Code': 'empresa_code',
        'Empresa_Nome': 'empresa_nome',
        'Filial_Code': 'filial_code',
        'Filial_Nome': 'filial_nome',
        'warehouseCode': 'warehouse_code',
        'destinyWarehouseCode': 'destiny_warehouse_code',
        'number': 'number',
        'series': 'series',
        'movementTypeCode': 'movement_type_code',
        'type': 'type',
        'status': 'status',
        'printed': 'printed',
        'documentPrinted': 'document_printed',
        'billPrinted': 'bill_printed',
        'registerDate': 'register_date',
        'exitDate': 'exit_date',
        'commercialRepresentativeCharge': 'commercial_representative_charge',
        'grossValue': 'gross_value',
        'netValue': 'net_value',
        'informedNetValue': 'informed_net_value',
        'otherValues': 'other_values',
        'discountPercentage': 'discount_percentage',
        'expensePercentage': 'expense_percentage',
        'expenseValue': 'expense_value',
        'extraPercentage1': 'extra_percentage1',
        'extraValue1': 'extra_value1',
        'extraPercentage2': 'extra_percentage2',
        'extraValue2': 'extra_value2',
        'transportedProductNetWeight': 'transported_product_net_weight',
        'transportedProductGrossWeight': 'transported_product_gross_weight',
        'classificationTable5Code': 'classification_table5_code',
        'financialOptionalTable2Code': 'financial_optional_table2_code',
        'netValueCurrencyCode': 'net_value_currency_code',
        'date': 'date',
        'hasGeneratedBill': 'has_generated_bill',
        'auxCustomerVendorCode': 'aux_customer_vendor_code',
        'customerVendorName': 'customer_vendor_name',
        'customerVendorCNPJ': 'customer_vendor_cnpj',
        'cnpj': 'customer_vendor_cnpj',
        'razao_social': 'customer_vendor_name',
        'auxCustomerVendorCompanyId': 'aux_customer_vendor_company_id',
        'customerVendorName': 'customer_vendor_name',
        'customerVendorCNPJ': 'customer_vendor_cnpj',
        'auxCustomerVendorCompanyId': 'aux_customer_vendor_company_id',
        'costCenterCode': 'cost_center_code',
        'salesman1Code': 'salesman1_code',
        'chargePercentage': 'charge_percentage',
        'salesman2ChargePercentage': 'salesman2_charge_percentage',
        'salesman3ChargePercentage': 'salesman3_charge_percentage',
        'salesman4ChargePercentage': 'salesman4_charge_percentage',
        'userCode': 'user_code',
        'destinyBranchId': 'destiny_branch_id',
        'lotGenerated': 'lot_generated',
        'accountingExportStatus': 'accounting_export_status',
        'deliveryDate': 'delivery_date',
        'hasGeneratedWorkAccount': 'has_generated_work_account',
        'workAccountGenerated': 'work_account_generated',
        'lastEditTime': 'last_edit_time',
        'indicateObjectUse': 'indicate_object_use',
        'bonumIntegrated': 'bonum_integrated',
        'processedFlag': 'processed_flag',
        'icmsDeductionValue': 'icms_deduction_value',
        'creationUser': 'creation_user',
        'creationDate': 'creation_date',
        'emailStatus': 'email_status',
        'internalGrossValue': 'internal_gross_value',
        'otherCompanyINSSBaseValue': 'other_company_inss_base_value',
        'conditionalDiscountValue': 'conditional_discount_value',
        'conditionalExpenseValue': 'conditional_expense_value',
        'affectStockOrder': 'affect_stock_order',
        'commercialAutomationExported': 'commercial_automation_exported',
        'aplicationIntegration': 'aplication_integration',
        'entryDate': 'entry_date',
        'extemporaneous': 'extemporaneous',
        'merchandiseValue': 'merchandise_value',
        'usesFinancialValueApportionment': 'uses_financial_value_apportionment',
        'conclusionFlag': 'conclusion_flag',
        'paradigmaStatus': 'paradigma_status',
        'paradigmaAutoIntegrated': 'paradigma_auto_integrated',
        'originalGrossValue': 'original_gross_value',
        'originalNetValue': 'original_net_value',
        'originalOtherValues': 'original_other_values',
        'operationId': 'operation_id',
        'scpBranchId': 'scp_branch_id',
        'movementItems': 'movement_items',
        'payments': 'payments',
        'costCenterApportionments': 'cost_center_apportionments',
        'departmentApportionments': 'department_apportionments',
        'taxes': 'taxes',
        'fiscal': 'fiscal',
        'norm': 'norm',
        'cargoComponent': 'cargo_component',
        'thirdPartyNF': 'third_party_nf',
        'safetyDevice': 'safety_device',
        'nfe': 'nfe',
        'inputCTRC': 'input_ctrc',
        'outputCTRC': 'output_ctrc',
        'ctrc': 'ctrc',
        'transportData': 'transport_data',
        'documentAuthorization': 'document_authorization',
        'judicialProcess': 'judicial_process',
        'serviceOrder': 'service_order',
        'relatedMovement': 'related_movement',
        'exportRelatedMovement': 'export_related_movement',
        'linkedMovement': 'linked_movement',
        'cTe': 'c_te',
        'eaiIntegration': 'eai_integration',
        'electronicInvoiceFreeFields': 'electronic_invoice_free_fields',
        'customerVendorCode': 'customer_vendor_code',
        'paymentTermCode': 'payment_term_code',
        'observation': 'observation',
        'financialOptionalTable1Code': 'financial_optional_table1_code',
        'financialEntryMovementId': 'financial_entry_movement_id',
        'generatedEntryNumber': 'generated_entry_number',
        'openEntryNumber': 'open_entry_number',
        'cashAccountCode': 'cash_account_code',
        'customerVendorCompanyId': 'customer_vendor_company_id',
        'fluxusGroupedFlag': 'fluxus_grouped_flag',
        'cashAccountCompanyId': 'cash_account_company_id',
        'longHistory': 'long_history',
    }
    
    # JSON fields that need special processing
    JSON_FIELDS = {
        'movement_items', 'payments', 'cost_center_apportionments', 'department_apportionments',
        'taxes', 'fiscal', 'norm', 'cargo_component', 'third_party_nf', 'safety_device',
        'nfe', 'input_ctrc', 'output_ctrc', 'ctrc', 'transport_data', 'document_authorization',
        'judicial_process', 'service_order', 'related_movement', 'export_related_movement',
        'linked_movement', 'c_te', 'eai_integration', 'electronic_invoice_free_fields'
    }
    
    # Date fields that need special processing
    DATE_FIELDS = {
        'register_date', 'exit_date', 'delivery_date', 'creation_date', 'last_edit_time', 
        'date', 'entry_date'
    }

    def __init__(self):
        self.batch_id = None

    def process_file(self, file_path: str, user_id: int) -> Tuple[str, int, int, List[str], List[dict]]:
        """
        Process an Excel file and return results
        
        Returns:
            Tuple containing:
            - batch_id (str): Unique identifier for this upload batch
            - success_count (int): Number of successfully processed records
            - error_count (int): Number of records with errors
            - errors (List[str]): List of error messages
            - processed_data (List[dict]): List of processed records for database insertion
        """
        from app import db
        from models import FinancialMovement, UploadHistory
        
        self.batch_id = str(uuid.uuid4())
        errors = []
        processed_data = []
        success_count = 0
        error_count = 0
        
        try:
            # Read Excel file with increased column width limits
            logger.info(f"Reading Excel file: {file_path}")
            
            # Configure pandas to handle large text fields better
            pd.set_option('display.max_colwidth', None)
            
            # First, check if the file has multiple sheets
            excel_file = pd.ExcelFile(file_path)
            available_sheets = excel_file.sheet_names
            logger.info(f"Available sheets in Excel file: {available_sheets}")
            
            # Determine which sheet to use
            target_sheet = self._select_data_sheet(available_sheets)
            logger.info(f"Using sheet: '{target_sheet}' for data processing")
            
            # Read the selected sheet
            df = pd.read_excel(file_path, sheet_name=target_sheet, dtype=str)  # Read all as strings first
            logger.info(f"Read Excel file with {len(df)} rows from sheet '{target_sheet}'")
            
            # Process each row
            for index, row in df.iterrows():
                # Ensure proper handling of row index
                row_num = index + 1 if isinstance(index, int) else int(index) + 1
                
                try:
                    # Convert row to dictionary and map columns
                    record_data = self._process_row(row)
                    
                    # Add tracking fields
                    record_data['upload_batch_id'] = self.batch_id
                    record_data['uploaded_by'] = user_id
                    record_data['uploaded_at'] = datetime.utcnow()
                    
                    processed_data.append(record_data)
                    success_count += 1
                    
                except Exception as e:
                    error_msg = f"Row {row_num}: {str(e)}"
                    errors.append(error_msg)
                    error_count += 1
                    logger.error(error_msg)
            
            # Create database records
            if processed_data:
                self._save_to_database(processed_data, file_path, user_id, success_count, error_count, errors)
            
            return self.batch_id, success_count, error_count, errors, processed_data
            
        except Exception as e:
            error_msg = f"Failed to process file: {str(e)}"
            logger.error(error_msg)
            return self.batch_id or str(uuid.uuid4()), 0, 1, [error_msg], []

    def _process_row(self, row: pd.Series) -> Dict[str, Any]:
        """Process a single row of data"""
        processed = {}
        
        for excel_col, db_col in self.COLUMN_MAPPING.items():
            if excel_col in row.index:
                value = row[excel_col]
                
                # Handle None/NaN values
                if pd.isna(value):
                    processed[db_col] = None
                    continue
                
                # Process JSON fields
                if db_col in self.JSON_FIELDS:
                    processed[db_col] = self._process_json_field(value)
                
                # Process date fields
                elif db_col in self.DATE_FIELDS:
                    processed[db_col] = self._process_date_field(value)
                
                # Process boolean fields
                elif db_col.endswith('_flag') or db_col.startswith('has_') or db_col in ['printed', 'document_printed', 'bill_printed', 'lot_generated', 'bonum_integrated', 'processed_flag', 'uses_financial_value_apportionment', 'paradigma_auto_integrated', 'fluxus_grouped_flag']:
                    processed[db_col] = self._process_boolean_field(value)
                
                # Process numeric fields
                elif db_col in ['gross_value', 'net_value', 'informed_net_value', 'other_values', 
                               'discount_percentage', 'expense_percentage', 'expense_value',
                               'extra_percentage1', 'extra_value1', 'extra_percentage2', 'extra_value2',
                               'transported_product_net_weight', 'transported_product_gross_weight',
                               'commercial_representative_charge', 'charge_percentage',
                               'salesman2_charge_percentage', 'salesman3_charge_percentage', 
                               'salesman4_charge_percentage', 'icms_deduction_value',
                               'internal_gross_value', 'other_company_inss_base_value',
                               'conditional_discount_value', 'conditional_expense_value',
                               'merchandise_value', 'original_gross_value', 'original_net_value',
                               'original_other_values']:
                    try:
                        str_val = str(value).strip()
                        processed[db_col] = float(str_val) if str_val and str_val != 'nan' else 0.0
                    except (ValueError, TypeError):
                        processed[db_col] = 0.0
                elif db_col in ['company_id', 'movement_id', 'branch_id', 'aux_customer_vendor_company_id',
                               'destiny_branch_id', 'customer_vendor_company_id', 'cash_account_company_id',
                               'financial_entry_movement_id', 'generated_entry_number', 'open_entry_number',
                               'operation_id', 'scp_branch_id']:
                    try:
                        str_val = str(value).strip()
                        processed[db_col] = int(float(str_val)) if str_val and str_val != 'nan' else None
                    except (ValueError, TypeError):
                        processed[db_col] = None
                else:
                    processed[db_col] = str(value) if value is not None else None
            else:
                # Set default values for missing columns based on their type
                if db_col in ['gross_value', 'net_value', 'informed_net_value', 'other_values', 
                             'discount_percentage', 'expense_percentage', 'expense_value',
                             'extra_percentage1', 'extra_value1', 'extra_percentage2', 'extra_value2',
                             'transported_product_net_weight', 'transported_product_gross_weight',
                             'commercial_representative_charge', 'charge_percentage',
                             'salesman2_charge_percentage', 'salesman3_charge_percentage', 
                             'salesman4_charge_percentage', 'icms_deduction_value',
                             'internal_gross_value', 'other_company_inss_base_value',
                             'conditional_discount_value', 'conditional_expense_value',
                             'merchandise_value', 'original_gross_value', 'original_net_value',
                             'original_other_values']:
                    processed[db_col] = 0.0
                elif db_col in self.JSON_FIELDS:
                    processed[db_col] = json.dumps([])
                elif db_col.endswith('_flag') or db_col.startswith('has_') or db_col in ['printed', 'document_printed', 'bill_printed', 'lot_generated', 'bonum_integrated', 'processed_flag', 'uses_financial_value_apportionment', 'paradigma_auto_integrated', 'fluxus_grouped_flag']:
                    processed[db_col] = False
                else:
                    processed[db_col] = None
        # --- Complementa campos de cliente/fornecedor se vierem com nomes alternativos ---
        if not processed.get('customer_vendor_name'):
            for alt_col in ['CustomerVendorName', 'Cliente', 'Fornecedor', 'RazaoSocial']:
                if alt_col in row.index and not pd.isna(row[alt_col]):
                    processed['customer_vendor_name'] = str(row[alt_col]).strip()
                    break

        if not processed.get('customer_vendor_cnpj'):
            for alt_col in ['CustomerVendorCNPJ', 'CNPJ', 'CPF', 'Documento']:
                if alt_col in row.index and not pd.isna(row[alt_col]):
                    processed['customer_vendor_cnpj'] = self._format_cnpj(str(row[alt_col]).strip())
                    break

        return processed

    def _process_json_field(self, value: Any) -> str:
        """Process JSON field values - handles Python literal format from Excel with truncation recovery"""
        if value is None or pd.isna(value):
            return json.dumps([])
        
        if isinstance(value, str):
            # Check if it's empty string or just whitespace
            if not value.strip():
                return json.dumps([])
            
            fixed_value = value.strip()
            
            # First, try to parse as standard JSON
            try:
                parsed = json.loads(fixed_value)
                return fixed_value
            except (json.JSONDecodeError, TypeError):
                pass
            
            # Try to parse as Python literal (with single quotes)
            try:
                parsed = ast.literal_eval(fixed_value)
                # Convert to proper JSON string
                return json.dumps(parsed)
            except (ValueError, SyntaxError) as e:
                # Check if the value looks like it was truncated
                if (fixed_value.startswith('[') and not fixed_value.endswith(']')) or \
                   (fixed_value.startswith('{') and not fixed_value.endswith('}')):
                    logger.warning(f"JSON field appears truncated, returning empty array: {str(value)[:50]}...")
                    return json.dumps([])
                
                # Try to fix common truncation issues
                try:
                    # If it starts with [ but doesn't end properly, try to close it
                    if fixed_value.startswith('[') and not fixed_value.endswith(']'):
                        # Find the last complete object and close the array
                        last_brace_close = fixed_value.rfind('}')
                        if last_brace_close != -1:
                            truncated_fix = fixed_value[:last_brace_close + 1] + ']'
                            parsed = ast.literal_eval(truncated_fix)
                            logger.info(f"Successfully recovered truncated JSON array")
                            return json.dumps(parsed)
                    
                    # If it's a single object that got cut off
                    if fixed_value.startswith('{') and not fixed_value.endswith('}'):
                        logger.warning(f"Truncated object detected, returning empty array")
                        return json.dumps([])
                        
                except (ValueError, SyntaxError):
                    pass
                
                # Log the error for debugging but don't break the import
                logger.warning(f"Could not parse JSON field: {str(value)[:100]}... Error: {str(e)}")
                # Return empty array to avoid breaking the system
                return json.dumps([])
        
        if isinstance(value, list):
            return json.dumps(value)
        
        # For other types, wrap in array
        return json.dumps([value] if value else [])

    def _process_date_field(self, value: Any):
        """Process date field values"""
        if value is None or pd.isna(value):
            return None
        
        if isinstance(value, datetime):
            return value
        
        if isinstance(value, str):
            try:
                return datetime.fromisoformat(value.replace('Z', '+00:00'))
            except ValueError:
                try:
                    return pd.to_datetime(value).to_pydatetime()
                except (ValueError, TypeError):
                    return None
        
        if hasattr(value, 'to_pydatetime'):
            return value.to_pydatetime()
        
        return None

    def _process_boolean_field(self, value: Any) -> bool:
        """Process boolean field values"""
        if value is None or pd.isna(value):
            return False
        
        str_val = str(value).strip().upper()
        return str_val in ['TRUE', '1', 'YES', 'Y', 'T', 'VERDADEIRO'] if str_val else False

    def _save_to_database(self, processed_data: List[Dict], filename: str, 
                         user_id: int, success_count: int, error_count: int, errors: List[str]):
        """Save processed data to database"""
        from app import db
        from models import FinancialMovement, UploadHistory
        
        # Create upload history record
        upload_history = UploadHistory()
        upload_history.batch_id = self.batch_id
        upload_history.filename = filename.split('/')[-1]  # Get just filename without path
        upload_history.uploaded_by = user_id
        upload_history.total_rows = len(processed_data)
        upload_history.success_count = success_count
        upload_history.error_count = error_count
        upload_history.errors = json.dumps(errors) if errors else None
        upload_history.status = 'completed' if error_count == 0 else 'completed_with_errors'
        
        # Create movement records
        movements = [FinancialMovement(**data) for data in processed_data]
        
        # Save to database
        db.session.add(upload_history)
        db.session.add_all(movements)
        db.session.commit()
        
        logger.info(f"Saved {success_count} records to database with batch_id {self.batch_id}")
    
    def _select_data_sheet(self, available_sheets: List[str]) -> str:
        """
        Select the appropriate sheet to process based on the new Excel structure.
        Priority: 'Dados' > 'Data' > first sheet with substantial data
        """
        # Check for preferred sheet names (new structure)
        preferred_sheets = ['Dados', 'Data', 'dados', 'data']
        
        for preferred in preferred_sheets:
            if preferred in available_sheets:
                logger.info(f"Found preferred sheet: '{preferred}'")
                return preferred
        
        # Check for sheets that are likely to contain movement data
        data_sheets = []
        skip_sheets = ['Árvore', 'árvore', 'Tree', 'tree', 'Resumo', 'resumo', 'Summary', 'summary']
        
        for sheet in available_sheets:
            if sheet not in skip_sheets:
                data_sheets.append(sheet)
        
        if data_sheets:
            selected = data_sheets[0]
            logger.info(f"Selected data sheet: '{selected}' from available data sheets: {data_sheets}")
            return selected
        
        # Fallback to first sheet
        logger.warning(f"No preferred data sheet found, using first sheet: '{available_sheets[0]}'")
        return available_sheets[0]

----- FILE: file_processor_new.py  (sha1:df649784c688, bytes:12948) -----
import pandas as pd
import json
import uuid
from datetime import datetime
from typing import Dict, List, Tuple, Any
import logging

logger = logging.getLogger(__name__)

class FinancialDataProcessor:
    """Process Excel files containing financial movement data"""
    
    # Complete column mapping for all 113 fields
    COLUMN_MAPPING = {
        'internalId': 'internal_id',
        'companyId': 'company_id',
        'movementId': 'movement_id',
        'branchId': 'branch_id',
        'warehouseCode': 'warehouse_code',
        'destinyWarehouseCode': 'destiny_warehouse_code',
        'number': 'number',
        'series': 'series',
        'movementTypeCode': 'movement_type_code',
        'type': 'type',
        'status': 'status',
        'printed': 'printed',
        'documentPrinted': 'document_printed',
        'billPrinted': 'bill_printed',
        'registerDate': 'register_date',
        'exitDate': 'exit_date',
        'commercialRepresentativeCharge': 'commercial_representative_charge',
        'grossValue': 'gross_value',
        'netValue': 'net_value',
        'informedNetValue': 'informed_net_value',
        'otherValues': 'other_values',
        'discountPercentage': 'discount_percentage',
        'expensePercentage': 'expense_percentage',
        'expenseValue': 'expense_value',
        'extraPercentage1': 'extra_percentage1',
        'extraValue1': 'extra_value1',
        'extraPercentage2': 'extra_percentage2',
        'extraValue2': 'extra_value2',
        'transportedProductNetWeight': 'transported_product_net_weight',
        'transportedProductGrossWeight': 'transported_product_gross_weight',
        'classificationTable5Code': 'classification_table5_code',
        'financialOptionalTable2Code': 'financial_optional_table2_code',
        'netValueCurrencyCode': 'net_value_currency_code',
        'date': 'date',
        'hasGeneratedBill': 'has_generated_bill',
        'auxCustomerVendorCode': 'aux_customer_vendor_code',
        'auxCustomerVendorCompanyId': 'aux_customer_vendor_company_id',
        'costCenterCode': 'cost_center_code',
        'salesman1Code': 'salesman1_code',
        'chargePercentage': 'charge_percentage',
        'salesman2ChargePercentage': 'salesman2_charge_percentage',
        'salesman3ChargePercentage': 'salesman3_charge_percentage',
        'salesman4ChargePercentage': 'salesman4_charge_percentage',
        'userCode': 'user_code',
        'destinyBranchId': 'destiny_branch_id',
        'lotGenerated': 'lot_generated',
        'accountingExportStatus': 'accounting_export_status',
        'deliveryDate': 'delivery_date',
        'hasGeneratedWorkAccount': 'has_generated_work_account',
        'workAccountGenerated': 'work_account_generated',
        'lastEditTime': 'last_edit_time',
        'indicateObjectUse': 'indicate_object_use',
        'bonumIntegrated': 'bonum_integrated',
        'processedFlag': 'processed_flag',
        'icmsDeductionValue': 'icms_deduction_value',
        'creationUser': 'creation_user',
        'creationDate': 'creation_date',
        'emailStatus': 'email_status',
        'internalGrossValue': 'internal_gross_value',
        'otherCompanyINSSBaseValue': 'other_company_inss_base_value',
        'conditionalDiscountValue': 'conditional_discount_value',
        'conditionalExpenseValue': 'conditional_expense_value',
        'affectStockOrder': 'affect_stock_order',
        'commercialAutomationExported': 'commercial_automation_exported',
        'aplicationIntegration': 'aplication_integration',
        'entryDate': 'entry_date',
        'extemporaneous': 'extemporaneous',
        'merchandiseValue': 'merchandise_value',
        'usesFinancialValueApportionment': 'uses_financial_value_apportionment',
        'conclusionFlag': 'conclusion_flag',
        'paradigmaStatus': 'paradigma_status',
        'paradigmaAutoIntegrated': 'paradigma_auto_integrated',
        'originalGrossValue': 'original_gross_value',
        'originalNetValue': 'original_net_value',
        'originalOtherValues': 'original_other_values',
        'operationId': 'operation_id',
        'scpBranchId': 'scp_branch_id',
        'movementItems': 'movement_items',
        'payments': 'payments',
        'costCenterApportionments': 'cost_center_apportionments',
        'departmentApportionments': 'department_apportionments',
        'taxes': 'taxes',
        'fiscal': 'fiscal',
        'norm': 'norm',
        'cargoComponent': 'cargo_component',
        'thirdPartyNF': 'third_party_nf',
        'safetyDevice': 'safety_device',
        'nfe': 'nfe',
        'inputCTRC': 'input_ctrc',
        'outputCTRC': 'output_ctrc',
        'ctrc': 'ctrc',
        'transportData': 'transport_data',
        'documentAuthorization': 'document_authorization',
        'judicialProcess': 'judicial_process',
        'serviceOrder': 'service_order',
        'relatedMovement': 'related_movement',
        'exportRelatedMovement': 'export_related_movement',
        'linkedMovement': 'linked_movement',
        'cTe': 'c_te',
        'eaiIntegration': 'eai_integration',
        'electronicInvoiceFreeFields': 'electronic_invoice_free_fields',
        'customerVendorCode': 'customer_vendor_code',
        'paymentTermCode': 'payment_term_code',
        'observation': 'observation',
        'financialOptionalTable1Code': 'financial_optional_table1_code',
        'financialEntryMovementId': 'financial_entry_movement_id',
        'generatedEntryNumber': 'generated_entry_number',
        'openEntryNumber': 'open_entry_number',
        'cashAccountCode': 'cash_account_code',
        'customerVendorCompanyId': 'customer_vendor_company_id',
        'fluxusGroupedFlag': 'fluxus_grouped_flag',
        'cashAccountCompanyId': 'cash_account_company_id',
        'longHistory': 'long_history',
    }
    
    # JSON fields that need special processing
    JSON_FIELDS = {
        'movement_items', 'payments', 'cost_center_apportionments', 'department_apportionments',
        'taxes', 'fiscal', 'norm', 'cargo_component', 'third_party_nf', 'safety_device',
        'nfe', 'input_ctrc', 'output_ctrc', 'ctrc', 'transport_data', 'document_authorization',
        'judicial_process', 'service_order', 'related_movement', 'export_related_movement',
        'linked_movement', 'c_te', 'eai_integration', 'electronic_invoice_free_fields'
    }
    
    # Date fields that need special processing
    DATE_FIELDS = {
        'register_date', 'exit_date', 'delivery_date', 'creation_date', 'last_edit_time', 
        'date', 'entry_date'
    }

    def __init__(self):
        self.batch_id = None

    def process_file(self, file_path: str, user_id: int) -> Tuple[str, int, int, List[str], List[dict]]:
        """
        Process an Excel file and return results
        
        Returns:
            Tuple containing:
            - batch_id (str): Unique identifier for this upload batch
            - success_count (int): Number of successfully processed records
            - error_count (int): Number of records with errors
            - errors (List[str]): List of error messages
            - processed_data (List[dict]): List of processed records for database insertion
        """
        from app import db
        from models import FinancialMovement, UploadHistory
        
        self.batch_id = str(uuid.uuid4())
        errors = []
        processed_data = []
        success_count = 0
        error_count = 0
        
        try:
            # Read Excel file
            logger.info(f"Reading Excel file: {file_path}")
            df = pd.read_excel(file_path)
            logger.info(f"Read Excel file with {len(df)} rows")
            
            # Process each row
            for index, row in df.iterrows():
                try:
                    # Convert row to dictionary and map columns
                    record_data = self._process_row(row)
                    
                    # Add tracking fields
                    record_data['upload_batch_id'] = self.batch_id
                    record_data['uploaded_by'] = user_id
                    record_data['uploaded_at'] = datetime.utcnow()
                    
                    processed_data.append(record_data)
                    success_count += 1
                    
                except Exception as e:
                    error_msg = f"Row {index + 1}: {str(e)}"
                    errors.append(error_msg)
                    error_count += 1
                    logger.error(error_msg)
            
            # Create database records
            if processed_data:
                self._save_to_database(processed_data, file_path, user_id, success_count, error_count, errors)
            
            return self.batch_id, success_count, error_count, errors, processed_data
            
        except Exception as e:
            error_msg = f"Failed to process file: {str(e)}"
            logger.error(error_msg)
            return self.batch_id or str(uuid.uuid4()), 0, 1, [error_msg], []

    def _process_row(self, row: pd.Series) -> Dict[str, Any]:
        """Process a single row of data"""
        processed = {}
        
        for excel_col, db_col in self.COLUMN_MAPPING.items():
            if excel_col in row:
                value = row[excel_col]
                
                # Handle None/NaN values
                if pd.isna(value):
                    processed[db_col] = None
                    continue
                
                # Process JSON fields
                if db_col in self.JSON_FIELDS:
                    processed[db_col] = self._process_json_field(value)
                
                # Process date fields
                elif db_col in self.DATE_FIELDS:
                    processed[db_col] = self._process_date_field(value)
                
                # Process boolean fields
                elif isinstance(value, bool) or db_col.endswith('_flag') or db_col.startswith('has_'):
                    processed[db_col] = bool(value) if value is not None else None
                
                # Process numeric fields
                elif isinstance(value, (int, float)):
                    processed[db_col] = value
                
                # Process string fields
                else:
                    processed[db_col] = str(value) if value is not None else None
            else:
                processed[db_col] = None
                
        return processed

    def _process_json_field(self, value: Any) -> str:
        """Process JSON field values"""
        if value is None or pd.isna(value):
            return None
        
        if isinstance(value, str):
            # If it's already a JSON string, return as is
            try:
                json.loads(value)
                return value
            except (json.JSONDecodeError, TypeError):
                return json.dumps([])
        
        if isinstance(value, list):
            return json.dumps(value)
        
        # For other types, wrap in array
        return json.dumps([value] if value else [])

    def _process_date_field(self, value: Any) -> datetime:
        """Process date field values"""
        if value is None or pd.isna(value):
            return None
        
        if isinstance(value, datetime):
            return value
        
        if isinstance(value, str):
            try:
                return datetime.fromisoformat(value.replace('Z', '+00:00'))
            except ValueError:
                try:
                    return pd.to_datetime(value).to_pydatetime()
                except (ValueError, TypeError):
                    return None
        
        if hasattr(value, 'to_pydatetime'):
            return value.to_pydatetime()
        
        return None

    def _save_to_database(self, processed_data: List[Dict], filename: str, 
                         user_id: int, success_count: int, error_count: int, errors: List[str]):
        """Save processed data to database"""
        from app import db
        from models import FinancialMovement, UploadHistory
        
        # Create upload history record
        upload_history = UploadHistory(
            batch_id=self.batch_id,
            filename=filename.split('/')[-1],  # Get just filename without path
            uploaded_by=user_id,
            total_rows=len(processed_data),
            success_count=success_count,
            error_count=error_count,
            errors=json.dumps(errors) if errors else None,
            status='completed' if error_count == 0 else 'completed_with_errors'
        )
        
        # Create movement records
        movements = [FinancialMovement(**data) for data in processed_data]
        
        # Save to database
        db.session.add(upload_history)
        db.session.add_all(movements)
        db.session.commit()
        
        logger.info(f"Saved {success_count} records to database with batch_id {self.batch_id}")

----- FILE: forms.py  (sha1:1ca433563365, bytes:3299) -----
from flask_wtf import FlaskForm
from flask_wtf.file import FileField, FileRequired, FileAllowed
from wtforms import StringField, PasswordField, BooleanField, SubmitField, SelectField, TextAreaField
from wtforms.validators import DataRequired, Email, Length, EqualTo, ValidationError
from models import User

class LoginForm(FlaskForm):
    username = StringField('Username', validators=[DataRequired()])
    password = PasswordField('Password', validators=[DataRequired()])
    remember_me = BooleanField('Remember Me')
    submit = SubmitField('Sign In')

class UserForm(FlaskForm):
    username = StringField('Username', validators=[
        DataRequired(), 
        Length(min=3, max=80)
    ])
    email = StringField('Email', validators=[
        DataRequired(), 
        Email()
    ])
    password = PasswordField('Password', validators=[
        DataRequired(), 
        Length(min=6)
    ])
    password2 = PasswordField('Repeat Password', validators=[
        DataRequired(), 
        EqualTo('password', message='Passwords must match')
    ])
    is_admin = BooleanField('Administrator')
    submit = SubmitField('Create User')
    
    def __init__(self, original_username=None, original_email=None, *args, **kwargs):
        super(UserForm, self).__init__(*args, **kwargs)
        self.original_username = original_username
        self.original_email = original_email
    
    def validate_username(self, username):
        if username.data != self.original_username:
            user = User.query.filter_by(username=username.data).first()
            if user is not None:
                raise ValidationError('Username already exists.')
    
    def validate_email(self, email):
        if email.data != self.original_email:
            user = User.query.filter_by(email=email.data).first()
            if user is not None:
                raise ValidationError('Email already registered.')

class EditUserForm(FlaskForm):
    username = StringField('Username', validators=[
        DataRequired(), 
        Length(min=3, max=80)
    ])
    email = StringField('Email', validators=[
        DataRequired(), 
        Email()
    ])
    is_admin = BooleanField('Administrator')
    submit = SubmitField('Update User')
    
    def __init__(self, original_username=None, original_email=None, *args, **kwargs):
        super(EditUserForm, self).__init__(*args, **kwargs)
        self.original_username = original_username
        self.original_email = original_email
    
    def validate_username(self, username):
        if username.data != self.original_username:
            user = User.query.filter_by(username=username.data).first()
            if user is not None:
                raise ValidationError('Username already exists.')
    
    def validate_email(self, email):
        if email.data != self.original_email:
            user = User.query.filter_by(email=email.data).first()
            if user is not None:
                raise ValidationError('Email already registered.')

class FileUploadForm(FlaskForm):
    file = FileField('Excel File', validators=[
        FileRequired(),
        FileAllowed(['xlsx', 'xls'], 'Only Excel files (.xlsx, .xls) are allowed!')
    ])
    description = TextAreaField('Description (Optional)')
    submit = SubmitField('Upload File')


----- FILE: main.py  (sha1:c30053183d1f, bytes:99) -----
from app import app

if __name__ == "__main__":
    app.run(host="0.0.0.0", port=5000, debug=True)


----- FILE: models.py  (sha1:99a9acf5338b, bytes:9860) -----
from app import db
from flask_login import UserMixin
from datetime import datetime
import json

class User(UserMixin, db.Model):
    id = db.Column(db.Integer, primary_key=True)
    username = db.Column(db.String(80), unique=True, nullable=False)
    email = db.Column(db.String(120), unique=True, nullable=False)
    password_hash = db.Column(db.String(256), nullable=False)
    is_admin = db.Column(db.Boolean, default=False)
    created_at = db.Column(db.DateTime, default=datetime.utcnow)
    
    def __repr__(self):
        return f'<User {self.username}>'

class FinancialMovement(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    
    # Core identification fields - all 117 fields from Excel (added 4 new company/branch info fields)
    internal_id = db.Column(db.String(50), nullable=True)
    company_id = db.Column(db.Integer, nullable=True)
    movement_id = db.Column(db.Integer, nullable=True)
    branch_id = db.Column(db.Integer, nullable=True)
    
    # Company and Branch information (new fields)
    empresa_code = db.Column(db.String(50), nullable=True)
    empresa_nome = db.Column(db.String(255), nullable=True)
    filial_code = db.Column(db.String(50), nullable=True)
    filial_nome = db.Column(db.String(255), nullable=True)
    warehouse_code = db.Column(db.String(50), nullable=True)
    destiny_warehouse_code = db.Column(db.String(50), nullable=True)
    number = db.Column(db.Integer, nullable=True)
    series = db.Column(db.String(10), nullable=True)
    movement_type_code = db.Column(db.String(50), nullable=True)
    type = db.Column(db.String(10), nullable=True)
    status = db.Column(db.String(10), nullable=True)
    printed = db.Column(db.Boolean, nullable=True)
    document_printed = db.Column(db.Boolean, nullable=True)
    bill_printed = db.Column(db.Boolean, nullable=True)
    register_date = db.Column(db.DateTime, nullable=True)
    exit_date = db.Column(db.DateTime, nullable=True)
    commercial_representative_charge = db.Column(db.Float, nullable=True)
    gross_value = db.Column(db.Float, nullable=True)
    net_value = db.Column(db.Float, nullable=True)
    informed_net_value = db.Column(db.Float, nullable=True)
    other_values = db.Column(db.Float, nullable=True)
    discount_percentage = db.Column(db.Float, nullable=True)
    expense_percentage = db.Column(db.Float, nullable=True)
    expense_value = db.Column(db.Float, nullable=True)
    extra_percentage1 = db.Column(db.Float, nullable=True)
    extra_value1 = db.Column(db.Float, nullable=True)
    extra_percentage2 = db.Column(db.Float, nullable=True)
    extra_value2 = db.Column(db.Float, nullable=True)
    transported_product_net_weight = db.Column(db.Float, nullable=True)
    transported_product_gross_weight = db.Column(db.Float, nullable=True)
    classification_table5_code = db.Column(db.String(50), nullable=True)
    financial_optional_table2_code = db.Column(db.String(50), nullable=True)
    net_value_currency_code = db.Column(db.String(10), nullable=True)
    date = db.Column(db.DateTime, nullable=True)
    has_generated_bill = db.Column(db.Boolean, nullable=True)
    aux_customer_vendor_code = db.Column(db.String(50), nullable=True)
    customer_vendor_name = db.Column(db.String(255), nullable=True)
    customer_vendor_cnpj = db.Column(db.String(30), nullable=True)
    aux_customer_vendor_company_id = db.Column(db.Integer, nullable=True)
    cost_center_code = db.Column(db.String(50), nullable=True)
    salesman1_code = db.Column(db.String(50), nullable=True)
    charge_percentage = db.Column(db.Float, nullable=True)
    salesman2_charge_percentage = db.Column(db.Float, nullable=True)
    salesman3_charge_percentage = db.Column(db.Float, nullable=True)
    salesman4_charge_percentage = db.Column(db.Float, nullable=True)
    user_code = db.Column(db.String(50), nullable=True)
    destiny_branch_id = db.Column(db.Integer, nullable=True)
    lot_generated = db.Column(db.Boolean, nullable=True)
    accounting_export_status = db.Column(db.String(50), nullable=True)
    delivery_date = db.Column(db.DateTime, nullable=True)
    has_generated_work_account = db.Column(db.Boolean, nullable=True)
    work_account_generated = db.Column(db.String(50), nullable=True)
    last_edit_time = db.Column(db.DateTime, nullable=True)
    indicate_object_use = db.Column(db.String(50), nullable=True)
    bonum_integrated = db.Column(db.Boolean, nullable=True)
    processed_flag = db.Column(db.Boolean, nullable=True)
    icms_deduction_value = db.Column(db.Float, nullable=True)
    creation_user = db.Column(db.String(50), nullable=True)
    creation_date = db.Column(db.DateTime, nullable=True)
    email_status = db.Column(db.String(50), nullable=True)
    internal_gross_value = db.Column(db.Float, nullable=True)
    other_company_inss_base_value = db.Column(db.Float, nullable=True)
    conditional_discount_value = db.Column(db.Float, nullable=True)
    conditional_expense_value = db.Column(db.Float, nullable=True)
    affect_stock_order = db.Column(db.String(50), nullable=True)
    commercial_automation_exported = db.Column(db.Integer, nullable=True)
    aplication_integration = db.Column(db.String(10), nullable=True)
    entry_date = db.Column(db.DateTime, nullable=True)
    extemporaneous = db.Column(db.Integer, nullable=True)
    merchandise_value = db.Column(db.Float, nullable=True)
    uses_financial_value_apportionment = db.Column(db.Boolean, nullable=True)
    conclusion_flag = db.Column(db.Integer, nullable=True)
    paradigma_status = db.Column(db.String(10), nullable=True)
    paradigma_auto_integrated = db.Column(db.Boolean, nullable=True)
    original_gross_value = db.Column(db.Float, nullable=True)
    original_net_value = db.Column(db.Float, nullable=True)
    original_other_values = db.Column(db.Float, nullable=True)
    operation_id = db.Column(db.Integer, nullable=True)
    scp_branch_id = db.Column(db.Integer, nullable=True)
    
    # JSON fields - properly formatted as Text columns
    movement_items = db.Column(db.Text, nullable=True)
    payments = db.Column(db.Text, nullable=True)
    cost_center_apportionments = db.Column(db.Text, nullable=True)
    department_apportionments = db.Column(db.Text, nullable=True)
    taxes = db.Column(db.Text, nullable=True)
    fiscal = db.Column(db.Text, nullable=True)
    norm = db.Column(db.Text, nullable=True)
    cargo_component = db.Column(db.Text, nullable=True)
    third_party_nf = db.Column(db.Text, nullable=True)
    safety_device = db.Column(db.Text, nullable=True)
    nfe = db.Column(db.Text, nullable=True)
    input_ctrc = db.Column(db.Text, nullable=True)
    output_ctrc = db.Column(db.Text, nullable=True)
    ctrc = db.Column(db.Text, nullable=True)
    transport_data = db.Column(db.Text, nullable=True)
    document_authorization = db.Column(db.Text, nullable=True)
    judicial_process = db.Column(db.Text, nullable=True)
    service_order = db.Column(db.Text, nullable=True)
    related_movement = db.Column(db.Text, nullable=True)
    export_related_movement = db.Column(db.Text, nullable=True)
    linked_movement = db.Column(db.Text, nullable=True)
    c_te = db.Column(db.Text, nullable=True)
    eai_integration = db.Column(db.Text, nullable=True)
    electronic_invoice_free_fields = db.Column(db.Text, nullable=True)
    
    # Remaining fields
    customer_vendor_code = db.Column(db.String(50), nullable=True)
    payment_term_code = db.Column(db.String(50), nullable=True)
    observation = db.Column(db.Text, nullable=True)
    financial_optional_table1_code = db.Column(db.String(50), nullable=True)
    financial_entry_movement_id = db.Column(db.String(50), nullable=True)
    generated_entry_number = db.Column(db.String(50), nullable=True)
    open_entry_number = db.Column(db.String(50), nullable=True)
    cash_account_code = db.Column(db.String(50), nullable=True)
    customer_vendor_company_id = db.Column(db.Integer, nullable=True)
    fluxus_grouped_flag = db.Column(db.Boolean, nullable=True)
    cash_account_company_id = db.Column(db.Integer, nullable=True)
    long_history = db.Column(db.Text, nullable=True)
    
    # Upload tracking
    upload_batch_id = db.Column(db.String(36))
    uploaded_by = db.Column(db.Integer, db.ForeignKey('user.id'))
    uploaded_at = db.Column(db.DateTime, default=datetime.utcnow)
    
    def get_json_field(self, field_name):
        """Get a JSON field as a Python object"""
        field_value = getattr(self, field_name, None)
        if field_value:
            try:
                return json.loads(field_value) if isinstance(field_value, str) else field_value
            except (json.JSONDecodeError, TypeError):
                return []
        return []

    def set_json_field(self, field_name, value):
        """Set a JSON field from a Python object"""
        if value:
            json_value = json.dumps(value) if not isinstance(value, str) else value
            setattr(self, field_name, json_value)
        else:
            setattr(self, field_name, None)
    
    def __repr__(self):
        return f'<FinancialMovement {self.internal_id}>'

class UploadHistory(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    batch_id = db.Column(db.String(36), unique=True, nullable=False)
    filename = db.Column(db.String(255), nullable=False)
    uploaded_by = db.Column(db.Integer, db.ForeignKey('user.id'), nullable=False)
    uploaded_at = db.Column(db.DateTime, default=datetime.utcnow)
    total_rows = db.Column(db.Integer, default=0)
    success_count = db.Column(db.Integer, default=0)
    error_count = db.Column(db.Integer, default=0)
    errors = db.Column(db.Text)
    status = db.Column(db.String(20), default='processing')
    
    # Relationship
    uploader = db.relationship('User', backref='uploads')
    
    def __repr__(self):
        return f'<UploadHistory {self.filename}>'

----- FILE: models_new.py  (sha1:61d2678030ae, bytes:9393) -----
from app import db
from flask_login import UserMixin
from datetime import datetime
import json

class User(UserMixin, db.Model):
    id = db.Column(db.Integer, primary_key=True)
    username = db.Column(db.String(80), unique=True, nullable=False)
    email = db.Column(db.String(120), unique=True, nullable=False)
    password_hash = db.Column(db.String(256), nullable=False)
    is_admin = db.Column(db.Boolean, default=False)
    created_at = db.Column(db.DateTime, default=datetime.utcnow)
    
    def __repr__(self):
        return f'<User {self.username}>'

class FinancialMovement(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    
    # Core identification fields - all 113 fields from Excel
    internal_id = db.Column(db.String(50), nullable=True)
    company_id = db.Column(db.Integer, nullable=True)
    movement_id = db.Column(db.Integer, nullable=True)
    branch_id = db.Column(db.Integer, nullable=True)
    warehouse_code = db.Column(db.String(50), nullable=True)
    destiny_warehouse_code = db.Column(db.String(50), nullable=True)
    number = db.Column(db.Integer, nullable=True)
    series = db.Column(db.String(10), nullable=True)
    movement_type_code = db.Column(db.String(50), nullable=True)
    type = db.Column(db.String(10), nullable=True)
    status = db.Column(db.String(10), nullable=True)
    printed = db.Column(db.Boolean, nullable=True)
    document_printed = db.Column(db.Boolean, nullable=True)
    bill_printed = db.Column(db.Boolean, nullable=True)
    register_date = db.Column(db.DateTime, nullable=True)
    exit_date = db.Column(db.DateTime, nullable=True)
    commercial_representative_charge = db.Column(db.Float, nullable=True)
    gross_value = db.Column(db.Float, nullable=True)
    net_value = db.Column(db.Float, nullable=True)
    informed_net_value = db.Column(db.Float, nullable=True)
    other_values = db.Column(db.Float, nullable=True)
    discount_percentage = db.Column(db.Float, nullable=True)
    expense_percentage = db.Column(db.Float, nullable=True)
    expense_value = db.Column(db.Float, nullable=True)
    extra_percentage1 = db.Column(db.Float, nullable=True)
    extra_value1 = db.Column(db.Float, nullable=True)
    extra_percentage2 = db.Column(db.Float, nullable=True)
    extra_value2 = db.Column(db.Float, nullable=True)
    transported_product_net_weight = db.Column(db.Float, nullable=True)
    transported_product_gross_weight = db.Column(db.Float, nullable=True)
    classification_table5_code = db.Column(db.String(50), nullable=True)
    financial_optional_table2_code = db.Column(db.String(50), nullable=True)
    net_value_currency_code = db.Column(db.String(10), nullable=True)
    date = db.Column(db.DateTime, nullable=True)
    has_generated_bill = db.Column(db.Boolean, nullable=True)
    aux_customer_vendor_code = db.Column(db.String(50), nullable=True)
    aux_customer_vendor_company_id = db.Column(db.Integer, nullable=True)
    cost_center_code = db.Column(db.String(50), nullable=True)
    salesman1_code = db.Column(db.String(50), nullable=True)
    charge_percentage = db.Column(db.Float, nullable=True)
    salesman2_charge_percentage = db.Column(db.Float, nullable=True)
    salesman3_charge_percentage = db.Column(db.Float, nullable=True)
    salesman4_charge_percentage = db.Column(db.Float, nullable=True)
    user_code = db.Column(db.String(50), nullable=True)
    destiny_branch_id = db.Column(db.Integer, nullable=True)
    lot_generated = db.Column(db.Boolean, nullable=True)
    accounting_export_status = db.Column(db.String(50), nullable=True)
    delivery_date = db.Column(db.DateTime, nullable=True)
    has_generated_work_account = db.Column(db.Boolean, nullable=True)
    work_account_generated = db.Column(db.String(50), nullable=True)
    last_edit_time = db.Column(db.DateTime, nullable=True)
    indicate_object_use = db.Column(db.String(50), nullable=True)
    bonum_integrated = db.Column(db.Boolean, nullable=True)
    processed_flag = db.Column(db.Boolean, nullable=True)
    icms_deduction_value = db.Column(db.Float, nullable=True)
    creation_user = db.Column(db.String(50), nullable=True)
    creation_date = db.Column(db.DateTime, nullable=True)
    email_status = db.Column(db.String(50), nullable=True)
    internal_gross_value = db.Column(db.Float, nullable=True)
    other_company_inss_base_value = db.Column(db.Float, nullable=True)
    conditional_discount_value = db.Column(db.Float, nullable=True)
    conditional_expense_value = db.Column(db.Float, nullable=True)
    affect_stock_order = db.Column(db.String(50), nullable=True)
    commercial_automation_exported = db.Column(db.Integer, nullable=True)
    aplication_integration = db.Column(db.String(10), nullable=True)
    entry_date = db.Column(db.DateTime, nullable=True)
    extemporaneous = db.Column(db.Integer, nullable=True)
    merchandise_value = db.Column(db.Float, nullable=True)
    uses_financial_value_apportionment = db.Column(db.Boolean, nullable=True)
    conclusion_flag = db.Column(db.Integer, nullable=True)
    paradigma_status = db.Column(db.String(10), nullable=True)
    paradigma_auto_integrated = db.Column(db.Boolean, nullable=True)
    original_gross_value = db.Column(db.Float, nullable=True)
    original_net_value = db.Column(db.Float, nullable=True)
    original_other_values = db.Column(db.Float, nullable=True)
    operation_id = db.Column(db.Integer, nullable=True)
    scp_branch_id = db.Column(db.Integer, nullable=True)
    
    # JSON fields - properly formatted as Text columns
    movement_items = db.Column(db.Text, nullable=True)
    payments = db.Column(db.Text, nullable=True)
    cost_center_apportionments = db.Column(db.Text, nullable=True)
    department_apportionments = db.Column(db.Text, nullable=True)
    taxes = db.Column(db.Text, nullable=True)
    fiscal = db.Column(db.Text, nullable=True)
    norm = db.Column(db.Text, nullable=True)
    cargo_component = db.Column(db.Text, nullable=True)
    third_party_nf = db.Column(db.Text, nullable=True)
    safety_device = db.Column(db.Text, nullable=True)
    nfe = db.Column(db.Text, nullable=True)
    input_ctrc = db.Column(db.Text, nullable=True)
    output_ctrc = db.Column(db.Text, nullable=True)
    ctrc = db.Column(db.Text, nullable=True)
    transport_data = db.Column(db.Text, nullable=True)
    document_authorization = db.Column(db.Text, nullable=True)
    judicial_process = db.Column(db.Text, nullable=True)
    service_order = db.Column(db.Text, nullable=True)
    related_movement = db.Column(db.Text, nullable=True)
    export_related_movement = db.Column(db.Text, nullable=True)
    linked_movement = db.Column(db.Text, nullable=True)
    c_te = db.Column(db.Text, nullable=True)
    eai_integration = db.Column(db.Text, nullable=True)
    electronic_invoice_free_fields = db.Column(db.Text, nullable=True)
    
    # Remaining fields
    customer_vendor_code = db.Column(db.String(50), nullable=True)
    payment_term_code = db.Column(db.String(50), nullable=True)
    observation = db.Column(db.Text, nullable=True)
    financial_optional_table1_code = db.Column(db.String(50), nullable=True)
    financial_entry_movement_id = db.Column(db.String(50), nullable=True)
    generated_entry_number = db.Column(db.String(50), nullable=True)
    open_entry_number = db.Column(db.String(50), nullable=True)
    cash_account_code = db.Column(db.String(50), nullable=True)
    customer_vendor_company_id = db.Column(db.Integer, nullable=True)
    fluxus_grouped_flag = db.Column(db.Boolean, nullable=True)
    cash_account_company_id = db.Column(db.Integer, nullable=True)
    long_history = db.Column(db.Text, nullable=True)
    
    # Upload tracking
    upload_batch_id = db.Column(db.String(36))
    uploaded_by = db.Column(db.Integer, db.ForeignKey('user.id'))
    uploaded_at = db.Column(db.DateTime, default=datetime.utcnow)
    
    def get_json_field(self, field_name):
        """Get a JSON field as a Python object"""
        field_value = getattr(self, field_name, None)
        if field_value:
            try:
                return json.loads(field_value) if isinstance(field_value, str) else field_value
            except (json.JSONDecodeError, TypeError):
                return []
        return []

    def set_json_field(self, field_name, value):
        """Set a JSON field from a Python object"""
        if value:
            json_value = json.dumps(value) if not isinstance(value, str) else value
            setattr(self, field_name, json_value)
        else:
            setattr(self, field_name, None)
    
    def __repr__(self):
        return f'<FinancialMovement {self.internal_id}>'

class UploadHistory(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    batch_id = db.Column(db.String(36), unique=True, nullable=False)
    filename = db.Column(db.String(255), nullable=False)
    uploaded_by = db.Column(db.Integer, db.ForeignKey('user.id'), nullable=False)
    uploaded_at = db.Column(db.DateTime, default=datetime.utcnow)
    total_rows = db.Column(db.Integer, default=0)
    success_count = db.Column(db.Integer, default=0)
    error_count = db.Column(db.Integer, default=0)
    errors = db.Column(db.Text)
    status = db.Column(db.String(20), default='processing')
    
    # Relationship
    uploader = db.relationship('User', backref='uploads')
    
    def __repr__(self):
        return f'<UploadHistory {self.filename}>'

----- FILE: mybundle.py  (sha1:995d1eb04172, bytes:2349) -----
# make_bundle.py
import os, hashlib

ROOT = os.getcwd()
EXCLUDE_DIRS = {
    '.git','node_modules','venv','.venv','__pycache__','.pytest_cache',
    'dist','build','.next','.idea','.vscode',
    'rpa_screenshots','rpa_artifacts','uploads','logs',
    # 'instance',  # <- descomente para não incluir nada de instance/
}
INCLUDE_EXTS = {
    '.py','.ts','.tsx','.js','.jsx','.json','.yml','.yaml','.toml',
    '.ini','.cfg','.txt','.md','.sql','.html','.css','.env.example',
    '.jinja','.jinja2','.j2',
}
MAX_BYTES = 800_000  # sobe um pouco pra evitar truncar texto útil

def rel(p): 
    r = os.path.relpath(p, ROOT)
    return '.' if r == '.' else r.replace('\\','/')

lines = []

# 1) Árvore do projeto
lines.append("### TREE\n")
for base, dirs, files in os.walk(ROOT):
    dirs[:] = [d for d in dirs if d not in EXCLUDE_DIRS]
    base_rel = rel(base)
    if base_rel != '.':
        lines.append(f"{base_rel}/")
    for f in sorted(files):
        lines.append(f"  {rel(os.path.join(base,f))}")

# 2) Conteúdo dos arquivos de texto
lines.append("\n\n### FILES\n")
rpa_sha = ""
files_count = 0

for base, dirs, files in os.walk(ROOT):
    dirs[:] = [d for d in dirs if d not in EXCLUDE_DIRS]
    for f in sorted(files):
        p = os.path.join(base, f)
        ext = os.path.splitext(f)[1].lower()
        if ext in INCLUDE_EXTS and os.path.getsize(p) <= MAX_BYTES:
            with open(p, 'rb') as fh:
                raw = fh.read()
            # tenta decodificar como texto
            txt = None
            for enc in ('utf-8', 'latin-1'):
                try:
                    txt = raw.decode(enc)
                    break
                except UnicodeDecodeError:
                    continue
            if txt is None:
                continue
            sha = hashlib.sha1(raw).hexdigest()[:12]
            files_count += 1
            if rel(p).endswith("rpa.py"):
                rpa_sha = sha
            lines.append(
                f"\n----- FILE: {rel(p)}  (sha1:{sha}, bytes:{len(raw)}) -----\n{txt}"
            )

with open('bundle.txt','w',encoding='utf-8') as f:
    f.write('\n'.join(lines))

print(f"Gerado bundle.txt na pasta do projeto. Arquivos incluídos: {files_count}")
if rpa_sha:
    print(f"SHA do rpa.py no bundle: {rpa_sha}")


----- FILE: pyproject.toml  (sha1:23aa5db92878, bytes:534) -----
[project]
name = "repl-nix-workspace"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "email-validator>=2.2.0",
    "flask-dance>=7.1.0",
    "flask>=3.1.1",
    "flask-sqlalchemy>=3.1.1",
    "gunicorn>=23.0.0",
    "psycopg2-binary>=2.9.10",
    "pyjwt>=2.10.1",
    "flask-login>=0.6.3",
    "oauthlib>=3.3.1",
    "werkzeug>=3.1.3",
    "flask-wtf>=1.2.2",
    "wtforms>=3.2.1",
    "pandas>=2.3.1",
    "openpyxl>=3.1.5",
    "xlrd>=2.0.2",
    "sqlalchemy>=2.0.43",
]


----- FILE: replit.md  (sha1:94db9cb2fc1b, bytes:3613) -----
# Overview

This is a Flask-based financial data management system designed for importing, storing, and analyzing financial movement data from Excel files. The application features a role-based user system with administrative controls for user management and data upload capabilities. Users can browse and search through financial movement records, while administrators can upload Excel files containing structured financial data and manage user accounts.

**Recent Update (Aug 2025)**: System now supports complete 117-field Excel structure with all JSON fields properly processed and displayed. Added 4 new company/branch identification fields: `Empresa_Code`, `Empresa_Nome`, `Filial_Code`, `Filial_Nome`. Upload functionality fully working with batch tracking and error handling.

# User Preferences

Preferred communication style: Simple, everyday language.

# System Architecture

## Web Framework
- **Flask** with SQLAlchemy ORM for database operations
- **Flask-Login** for user session management and authentication
- **Flask-WTF** for form handling and CSRF protection
- Blueprint-based routing for modular organization (main, admin, auth)

## Authentication & Authorization
- Password-based authentication with hashed storage using Werkzeug
- Role-based access control with admin/regular user distinction
- Session-based login with "remember me" functionality
- Decorator-based route protection for admin-only features

## Database Design
- SQLAlchemy with SQLite default (configurable via DATABASE_URL)
- **User model**: Basic user management with admin flags
- **FinancialMovement model**: Comprehensive financial transaction records with 40+ fields
- **UploadHistory model**: Tracks file upload batches and processing results
- Connection pooling and automatic table creation

## Data Processing
- **FinancialDataProcessor class**: Handles Excel file parsing and data transformation
- Column mapping from Excel headers to database fields
- Batch processing with error handling and validation
- Support for complex financial data structures including dates, currencies, and business logic fields

## Frontend Architecture
- Bootstrap 5 dark theme for responsive UI
- jQuery and DataTables for enhanced table functionality
- Template inheritance using Jinja2
- Progressive enhancement with JavaScript for user interactions
- File upload interface with drag-and-drop support

## File Management
- Secure file upload handling with size limits (50MB)
- Excel file processing with pandas
- Upload history tracking with success/failure metrics
- Organized upload directory structure

# External Dependencies

## Core Framework Dependencies
- **Flask**: Web application framework
- **SQLAlchemy/Flask-SQLAlchemy**: Database ORM and integration
- **Flask-Login**: User session management
- **Flask-WTF/WTForms**: Form handling and validation
- **Werkzeug**: WSGI utilities and security helpers

## Data Processing
- **pandas**: Excel file reading and data manipulation
- **openpyxl**: Excel file format support

## Frontend Libraries (CDN)
- **Bootstrap 5**: UI framework with dark theme
- **DataTables**: Enhanced table functionality
- **Font Awesome**: Icon library
- **jQuery**: JavaScript utilities

## Infrastructure
- **SQLite**: Default database (production can use PostgreSQL via DATABASE_URL)
- **ProxyFix**: WSGI middleware for proper URL generation behind proxies
- File system storage for uploaded Excel files

## Security Features
- CSRF protection through Flask-WTF
- Password hashing with Werkzeug
- Secure filename handling for uploads
- Session-based authentication with configurable secrets

----- FILE: routes.py  (sha1:7c0afead735f, bytes:15786) -----
import os
import uuid
from datetime import datetime
from flask import Blueprint, render_template, request, redirect, url_for, flash, jsonify, current_app
from flask_login import login_user, logout_user, login_required, current_user
from werkzeug.security import check_password_hash, generate_password_hash
from werkzeug.utils import secure_filename
from sqlalchemy import or_, desc
from app import db, login_manager
from models import User, FinancialMovement, UploadHistory
from forms import LoginForm, UserForm, EditUserForm, FileUploadForm
from file_processor import FinancialDataProcessor
import logging

logger = logging.getLogger(__name__)

# User loader for Flask-Login
@login_manager.user_loader
def load_user(user_id):
    return User.query.get(int(user_id))

# Blueprint definitions
main_bp = Blueprint('main', __name__)
admin_bp = Blueprint('admin', __name__)
auth_bp = Blueprint('auth', __name__)

def admin_required(f):
    """Decorator to require admin access"""
    from functools import wraps
    @wraps(f)
    @login_required
    def decorated_function(*args, **kwargs):
        if not current_user.is_admin:
            flash('You need administrator privileges to access this page.', 'error')
            return redirect(url_for('main.index'))
        return f(*args, **kwargs)
    return decorated_function

# Main routes
@main_bp.route('/')
def index():
    """Homepage - show recent movements"""
    recent_movements = FinancialMovement.query.order_by(desc(FinancialMovement.uploaded_at)).limit(10).all()
    return render_template('index.html', movements=recent_movements)

@main_bp.route('/movements')
@login_required
def movements():
    """List all movements with search, filters and pagination"""
    page = request.args.get('page', 1, type=int)
    search = request.args.get('search', '', type=str)
    company_filter = request.args.get('company', '', type=str)
    branch_filter = request.args.get('branch', '', type=str)
    status_filter = request.args.get('status', '', type=str)
    per_page = 25
    query = FinancialMovement.query
    
    # Apply search filter
    # Apply search filter
    if search:
        s = search.strip()

        # Busca textual abrangente
        text_conds = [
            FinancialMovement.internal_id.ilike(f"%{s}%"),
            FinancialMovement.aux_customer_vendor_code.ilike(f"%{s}%"),
            FinancialMovement.customer_vendor_name.ilike(f"%{s}%"),
            FinancialMovement.customer_vendor_cnpj.ilike(f"%{s}%"),
            FinancialMovement.observation.ilike(f"%{s}%"),
            FinancialMovement.empresa_nome.ilike(f"%{s}%"),
            FinancialMovement.filial_nome.ilike(f"%{s}%"),
            FinancialMovement.movement_type_code.ilike(f"%{s}%"),  # 🔹 para "1.2.01"
        ]

        if s.isdigit():
            text_conds.append(FinancialMovement.number == int(s))

        query = query.filter(or_(*text_conds))


    
    # Apply company filter
    if company_filter:
        query = query.filter(FinancialMovement.empresa_nome.contains(company_filter))
    
    # Apply branch filter  
    if branch_filter:
        query = query.filter(FinancialMovement.filial_nome.contains(branch_filter))
    
    # Apply status filter
    if status_filter:
        query = query.filter(FinancialMovement.status == status_filter)
    
    movements = query.order_by(desc(FinancialMovement.uploaded_at)).paginate(
        page=page, per_page=per_page, error_out=False
    )
    
    # Get unique values for filter dropdowns
    companies = db.session.query(FinancialMovement.empresa_nome).distinct().filter(
        FinancialMovement.empresa_nome.isnot(None)
    ).order_by(FinancialMovement.empresa_nome).all()
    companies = [c[0] for c in companies if c[0]]
    
    branches = db.session.query(FinancialMovement.filial_nome).distinct().filter(
        FinancialMovement.filial_nome.isnot(None)
    ).order_by(FinancialMovement.filial_nome).all()
    branches = [b[0] for b in branches if b[0]]
    
    statuses = db.session.query(FinancialMovement.status).distinct().filter(
        FinancialMovement.status.isnot(None)
    ).order_by(FinancialMovement.status).all()
    statuses = [s[0] for s in statuses if s[0]]
    
    return render_template('movements/list.html', 
                         movements=movements, 
                         search=search,
                         company_filter=company_filter,
                         branch_filter=branch_filter,
                         status_filter=status_filter,
                         companies=companies,
                         branches=branches,
                         statuses=statuses)



@main_bp.route('/movements/<int:id>')
@login_required
def movement_detail(id):
    """Show detailed view of a specific movement"""
    movement = FinancialMovement.query.get_or_404(id)
    return render_template('movements/detail.html', movement=movement)

@main_bp.route('/movements/<int:id>/delete', methods=['POST'])
@login_required
def delete_movement(id):
    """Delete a specific movement"""
    movement = FinancialMovement.query.get_or_404(id)
    
    try:
        movement_name = movement.internal_id or f"#{movement.id}"
        db.session.delete(movement)
        db.session.commit()
        flash(f'Movement {movement_name} deleted successfully!', 'success')
    except Exception as e:
        db.session.rollback()
        flash(f'Error deleting movement: {str(e)}', 'error')
        logger.error(f"Error deleting movement {id}: {str(e)}")
    
    return redirect(url_for('main.movements'))

@main_bp.route('/movements/delete-all', methods=['GET', 'POST'])
@login_required
def delete_all_movements():
    """Delete all movements from database"""
    if request.method == 'POST':
        confirmation = request.form.get('confirmation', '').strip().lower()
        
        if confirmation == 'delete all movements':
            try:
                movement_count = FinancialMovement.query.count()
                upload_count = UploadHistory.query.count()
                
                db.session.query(FinancialMovement).delete()
                db.session.query(UploadHistory).delete()
                db.session.commit()
                
                flash(f'All data deleted successfully! {movement_count} movements and {upload_count} upload records removed.', 'success')
                return redirect(url_for('main.movements'))
                
            except Exception as e:
                db.session.rollback()
                flash(f'Error deleting data: {str(e)}', 'error')
                logger.error(f"Error deleting all movements: {str(e)}")
                
        else:
            flash('Invalid confirmation text. Type exactly "delete all movements" to confirm.', 'warning')
    
    # Count current records for display
    movement_count = FinancialMovement.query.count()
    upload_count = UploadHistory.query.count()
    
    return render_template('movements/delete_all.html', 
                          movement_count=movement_count, 
                          upload_count=upload_count)

@main_bp.route('/api/movements/<int:id>/json/<field>')
@login_required
def get_movement_json_field(id, field):
    """API endpoint to get formatted JSON field data"""
    movement = FinancialMovement.query.get_or_404(id)
    json_data = movement.get_json_field(field)
    return jsonify(json_data)

# Authentication routes
@auth_bp.route('/login', methods=['GET', 'POST'])
def login():
    """User login"""
    if current_user.is_authenticated:
        return redirect(url_for('main.index'))
    
    form = LoginForm()
    if form.validate_on_submit():
        user = User.query.filter_by(username=form.username.data).first()
        if user and user.password_hash and check_password_hash(user.password_hash, form.password.data):
            login_user(user, remember=form.remember_me.data)
            next_page = request.args.get('next')
            if not next_page:
                next_page = url_for('admin.dashboard') if user.is_admin else url_for('main.index')
            flash(f'Welcome back, {user.username}!', 'success')
            return redirect(next_page)
        flash('Invalid username or password', 'error')
    
    return render_template('auth/login.html', form=form)

@auth_bp.route('/logout')
@login_required
def logout():
    """User logout"""
    logout_user()
    flash('You have been logged out.', 'info')
    return redirect(url_for('main.index'))

# Admin routes
@admin_bp.route('/dashboard')
@admin_required
def dashboard():
    """Admin dashboard with statistics"""
    stats = {
        'total_users': User.query.count(),
        'total_movements': FinancialMovement.query.count(),
        'total_uploads': UploadHistory.query.count(),
        'recent_uploads': UploadHistory.query.order_by(desc(UploadHistory.uploaded_at)).limit(5).all()
    }
    return render_template('admin/dashboard.html', stats=stats)

@admin_bp.route('/users')
@admin_required
def users():
    """List all users"""
    page = request.args.get('page', 1, type=int)
    users = User.query.order_by(User.created_at.desc()).paginate(
        page=page, per_page=20, error_out=False
    )
    return render_template('admin/users.html', users=users)

@admin_bp.route('/users/new', methods=['GET', 'POST'])
@admin_required
def create_user():
    """Create new user"""
    form = UserForm()
    if form.validate_on_submit():
        password_data = form.password.data
        if password_data:
            user = User(
                username=form.username.data,
                email=form.email.data,
                is_admin=form.is_admin.data,
                password_hash=generate_password_hash(password_data)
            )
        else:
            flash('Password is required', 'error')
            return render_template('admin/users.html', form=form, action='create')
        db.session.add(user)
        db.session.commit()
        flash(f'User {user.username} created successfully!', 'success')
        return redirect(url_for('admin.users'))
    
    return render_template('admin/users.html', form=form, action='create')

@admin_bp.route('/users/<int:id>/edit', methods=['GET', 'POST'])
@admin_required
def edit_user(id):
    """Edit existing user"""
    user = User.query.get_or_404(id)
    form = EditUserForm(
        original_username=user.username,
        original_email=user.email,
        obj=user
    )
    
    if form.validate_on_submit():
        user.username = form.username.data
        user.email = form.email.data
        user.is_admin = form.is_admin.data
        db.session.commit()
        flash(f'User {user.username} updated successfully!', 'success')
        return redirect(url_for('admin.users'))
    
    return render_template('admin/users.html', form=form, user=user, action='edit')

@admin_bp.route('/users/<int:id>/delete', methods=['POST'])
@admin_required
def delete_user(id):
    """Delete user"""
    user = User.query.get_or_404(id)
    if user.id == current_user.id:
        flash('You cannot delete your own account!', 'error')
        return redirect(url_for('admin.users'))
    
    db.session.delete(user)
    db.session.commit()
    flash(f'User {user.username} deleted successfully!', 'success')
    return redirect(url_for('admin.users'))

@admin_bp.route('/upload', methods=['GET', 'POST'])
@admin_required
def upload_file():
    """Upload Excel file with financial data"""
    form = FileUploadForm()
    if form.validate_on_submit():
        file = form.file.data
        filename = secure_filename(file.filename)
        
        # Generate unique filename
        unique_filename = f"{uuid.uuid4()}_{filename}"
        file_path = os.path.join(current_app.config['UPLOAD_FOLDER'], unique_filename)
        
        try:
            # Save file
            file.save(file_path)
            
            # Process file
            # Process file
            processor = FinancialDataProcessor()
            batch_id, success_count, error_count, errors, processed_data = processor.process_file(
                file_path, current_user.id
            )

            # 🔹 NOVO: tenta fechar handle explicitamente (se processor tiver método close)
            if hasattr(processor, "close"):
                try:
                    processor.close()
                except Exception:
                    pass

            # 🔹 NOVO: adiar a exclusão para só depois
            import time
            for i in range(5):  # tenta até 5 vezes
                try:
                    if os.path.exists(file_path):
                        os.remove(file_path)
                    break
                except PermissionError:
                    time.sleep(0.5)  # espera meio segundo e tenta de novo

            
            if error_count > 0:
                flash(f'File processed with {success_count} successful records and {error_count} errors. Check upload history for details.', 'warning')
            else:
                flash(f'File uploaded successfully! {success_count} records processed.', 'success')
            
            return redirect(url_for('admin.upload_history'))
            
        except Exception as e:
            # Clean up file if processing failed
            if os.path.exists(file_path):
                os.remove(file_path)
            flash(f'Error processing file: {str(e)}', 'error')
            logger.error(f"Upload processing error: {str(e)}")
    
    return render_template('admin/upload.html', form=form)

@admin_bp.route('/upload-history')
@admin_required
def upload_history():
    """View upload history"""
    page = request.args.get('page', 1, type=int)
    uploads = UploadHistory.query.order_by(desc(UploadHistory.uploaded_at)).paginate(
        page=page, per_page=20, error_out=False
    )
    return render_template('admin/upload.html', uploads=uploads, show_history=True)

@admin_bp.route('/upload-history/<int:id>')
@admin_required
def upload_history_detail(id):
    """View detailed upload history"""
    upload = UploadHistory.query.get_or_404(id)
    movements = FinancialMovement.query.filter_by(upload_batch_id=upload.batch_id).all()
    return render_template('admin/upload.html', upload=upload, movements=movements, show_detail=True)

@admin_bp.route('/clear-movements', methods=['GET', 'POST'])
@admin_required
def clear_movements():
    """Clear all financial movements from database"""
    if request.method == 'POST':
        confirmation = request.form.get('confirmation', '').strip().lower()
        
        if confirmation == 'delete all movements':
            try:
                # Delete all financial movements and their upload history
                movement_count = FinancialMovement.query.count()
                upload_count = UploadHistory.query.count()
                
                db.session.query(FinancialMovement).delete()
                db.session.query(UploadHistory).delete()
                db.session.commit()
                
                flash(f'Database cleared successfully! {movement_count} financial movements and {upload_count} upload records deleted.', 'success')
                return redirect(url_for('admin.dashboard'))
                
            except Exception as e:
                db.session.rollback()
                flash(f'Error clearing database: {str(e)}', 'error')
                
        else:
            flash('Invalid confirmation text. Type exactly "delete all movements" to confirm.', 'warning')
    
    # Count current records
    movement_count = FinancialMovement.query.count()
    upload_count = UploadHistory.query.count()
    
    return render_template('admin/clear_movements.html', 
                          movement_count=movement_count, 
                          upload_count=upload_count)


----- FILE: test_data_sample.py  (sha1:574f5dbc32a6, bytes:5548) -----
#!/usr/bin/env python3
"""
Script to create a test Excel file with sample data to test the upload functionality
"""

import pandas as pd
import json
from datetime import datetime

# Sample data that mimics the structure from the user's file
sample_data = {
    'Empresa_Nome': ['SA CAVALCANTE COMESTIVEIS LTDA'],
    'Filial_Code': [17],
    'Filial_Nome': ['16 - BOB´S LARANJEIRAS'],
    'Filial_Nome_Descricao': ['16 - SA CAVALCANTE COMESTIVEIS LTDA'],
    'internalId': ['1|138'],
    'companyId': [1],
    'movementId': [138],
    'branchId': [1],
    'warehouseCode': ['01'],
    'destinyWarehouseCode': ['01'],
    'customerVendorCode': ['F00227'],
    'number': ['001408'],
    'series': ['U'],
    'movementTypeCode': ['1.2.02'],
    'type': ['P'],
    'status': ['F'],
    'printed': [False],
    'documentPrinted': [False],
    'billPrinted': [False],
    'registerDate': ['2003-08-06T00:00:00-03:00'],
    'exitDate': ['2003-08-11T00:00:00-03:00'],
    'commercialRepresentativeCharge': [0],
    'paymentTermCode': [30],
    'grossValue': [232],
    'netValue': [232],
    'informedNetValue': [0],
    'otherValues': [232],
    'observation': ['PROGRAMA SOL'],
    'discountPercentage': [0],
    'expensePercentage': [0],
    'expenseValue': [0],
    'extraPercentage1': [0],
    'extraValue1': [0],
    'extraPercentage2': [0],
    'extraValue2': [0],
    'transportedProductNetWeight': [0],
    'transportedProductGrossWeight': [0],
    'financialOptionalTable1Code': ['02'],
    'financialEntryMovementId': [138],
    'netValueCurrencyCode': ['R$'],
    'date': ['2003-08-06T00:00:00-03:00'],
    'generatedEntryNumber': [1],
    'hasGeneratedBill': [False],
    'openEntryNumber': [1],
    'auxCustomerVendorCode': ['F00227'],
    'auxCustomerVendorCompanyId': [0],
    'cashAccountCode': ['02'],
    'chargePercentage': [0],
    'salesman2ChargePercentage': [0],
    'salesman3ChargePercentage': [0],
    'salesman4ChargePercentage': [0],
    'customerVendorCompanyId': [0],
    'userCode': ['cmufumba'],
    'destinyBranchId': [1],
    'fluxusGroupedFlag': [0],
    'cashAccountCompanyId': [1],
    'lotGenerated': [False],
    'accountingExportStatus': [0],
    'hasGeneratedWorkAccount': [False],
    'workAccountGenerated': [False],
    'lastEditTime': ['2003-08-11T09:08:41-03:00'],
    'indicateObjectUse': [0],
    'bonumIntegrated': [False],
    'processedFlag': [False],
    'icmsDeductionValue': [0],
    'creationUser': ['cmufumba'],
    'creationDate': ['2003-08-11T00:00:00-03:00'],
    'emailStatus': [False],
    'internalGrossValue': [232],
    'otherCompanyINSSBaseValue': [0],
    'conditionalDiscountValue': [0],
    'conditionalExpenseValue': [0],
    'affectStockOrder': [138],
    'commercialAutomationExported': [0],
    'aplicationIntegration': ['T'],
    'entryDate': ['2003-08-11T00:00:00-03:00'],
    'extemporaneous': [0],
    'merchandiseValue': [0],
    'usesFinancialValueApportionment': [False],
    'conclusionFlag': [0],
    'paradigmaStatus': ['N'],
    'paradigmaAutoIntegrated': [False],
    'originalGrossValue': [0],
    'originalNetValue': [0],
    'originalOtherValues': [0],
    'operationId': [0],
    'scpBranchId': [0],
    # JSON fields with sample data
    'movementItems': [json.dumps([{
        'companyId': 1,
        'movementId': 138,
        'sequentialId': 1,
        'sequentialNumber': 1,
        'productId': 90,
        'quantity': 4.0,
        'unitPrice': 58.0,
        'tablePrice': 0.0,
        'discountValue': 0.0,
        'registerDate': '2003-08-06T00:00:00-03:00',
        'measureUnitCode': 'GL',
        'productFantasyName': 'GALVEST BRANCO 3600 ML',
        'productCode': '01.01.0007'
    }])],
    'payments': [json.dumps([])],
    'costCenterApportionments': [json.dumps([{
        'companyId': 1,
        'apportionmentId': 7,
        'movementId': 138,
        'costCenterCode': '1.4.008',
        'value': 232.0,
        'costCenterName': 'EXPANSÃO SPC'
    }])],
    'departmentApportionments': [json.dumps([{
        'companyId': 1,
        'movementId': 138,
        'branchId': 1,
        'departmentCode': '10',
        'value': 232.0,
        'departmentName': 'OBRAS'
    }])],
    'taxes': [json.dumps([{
        'companyId': 1,
        'movementId': 138,
        'movementItemSequentialId': 0,
        'taxId': 'PIS',
        'calculationBasis': 232.0,
        'aliquot': 1.65,
        'value': 3.83,
        'calculatedCalculationBasis': 232.0,
        'edited': 0,
        'fullBase': 0.0
    }])],
    'fiscal': [json.dumps([])],
    'norm': [json.dumps([])],
    'cargoComponent': [json.dumps([])],
    'thirdPartyNF': [json.dumps([])],
    'safetyDevice': [json.dumps([])],
    'nfe': [json.dumps([])],
    'inputCTRC': [json.dumps([])],
    'outputCTRC': [json.dumps([])],
    'ctrc': [json.dumps([])],
    'transportData': [json.dumps([])],
    'documentAuthorization': [json.dumps([])],
    'judicialProcess': [json.dumps([])],
    'serviceOrder': [json.dumps([])],
    'relatedMovement': [json.dumps([])],
    'exportRelatedMovement': [json.dumps([])],
    'linkedMovement': [json.dumps([])],
    'cTe': [json.dumps([])],
    'eaiIntegration': [json.dumps([])],
    'electronicInvoiceFreeFields': [json.dumps([])]
}

if __name__ == "__main__":
    # Create DataFrame
    df = pd.DataFrame(sample_data)
    
    # Save to Excel
    filename = "test_financial_movements.xlsx"
    df.to_excel(filename, index=False)
    print(f"Test file created: {filename}")
    print(f"Data shape: {df.shape}")
    print(f"Columns: {list(df.columns)}")

----- FILE: update_models.py  (sha1:5d6f83401594, bytes:5295) -----
#!/usr/bin/env python3
"""
Script to generate complete FinancialMovement model with all 113 fields
"""

# All 113 fields from the Excel file
EXCEL_COLUMNS = [
    'internalId', 'companyId', 'movementId', 'branchId', 'warehouseCode', 'destinyWarehouseCode', 
    'number', 'series', 'movementTypeCode', 'type', 'status', 'printed', 'documentPrinted', 
    'billPrinted', 'registerDate', 'exitDate', 'commercialRepresentativeCharge', 'grossValue', 
    'netValue', 'informedNetValue', 'otherValues', 'discountPercentage', 'expensePercentage', 
    'expenseValue', 'extraPercentage1', 'extraValue1', 'extraPercentage2', 'extraValue2', 
    'transportedProductNetWeight', 'transportedProductGrossWeight', 'classificationTable5Code', 
    'financialOptionalTable2Code', 'netValueCurrencyCode', 'date', 'hasGeneratedBill', 
    'auxCustomerVendorCode', 'auxCustomerVendorCompanyId', 'costCenterCode', 'salesman1Code', 
    'chargePercentage', 'salesman2ChargePercentage', 'salesman3ChargePercentage', 
    'salesman4ChargePercentage', 'userCode', 'destinyBranchId', 'lotGenerated', 
    'accountingExportStatus', 'deliveryDate', 'hasGeneratedWorkAccount', 'workAccountGenerated', 
    'lastEditTime', 'indicateObjectUse', 'bonumIntegrated', 'processedFlag', 'icmsDeductionValue', 
    'creationUser', 'creationDate', 'emailStatus', 'internalGrossValue', 'otherCompanyINSSBaseValue', 
    'conditionalDiscountValue', 'conditionalExpenseValue', 'affectStockOrder', 
    'commercialAutomationExported', 'aplicationIntegration', 'entryDate', 'extemporaneous', 
    'merchandiseValue', 'usesFinancialValueApportionment', 'conclusionFlag', 'paradigmaStatus', 
    'paradigmaAutoIntegrated', 'originalGrossValue', 'originalNetValue', 'originalOtherValues', 
    'operationId', 'scpBranchId', 'movementItems', 'payments', 'costCenterApportionments', 
    'departmentApportionments', 'taxes', 'fiscal', 'norm', 'cargoComponent', 'thirdPartyNF', 
    'safetyDevice', 'nfe', 'inputCTRC', 'outputCTRC', 'ctrc', 'transportData', 
    'documentAuthorization', 'judicialProcess', 'serviceOrder', 'relatedMovement', 
    'exportRelatedMovement', 'linkedMovement', 'cTe', 'eaiIntegration', 
    'electronicInvoiceFreeFields', 'customerVendorCode', 'paymentTermCode', 'observation', 
    'financialOptionalTable1Code', 'financialEntryMovementId', 'generatedEntryNumber', 
    'openEntryNumber', 'cashAccountCode', 'customerVendorCompanyId', 'fluxusGroupedFlag', 
    'cashAccountCompanyId', 'longHistory'
]

def camel_to_snake(name):
    """Convert camelCase to snake_case"""
    import re
    s1 = re.sub('(.)([A-Z][a-z]+)', r'\1_\2', name)
    return re.sub('([a-z0-9])([A-Z])', r'\1_\2', s1).lower()

# JSON fields that should be stored as JSON text
JSON_FIELDS = {
    'movementItems', 'payments', 'costCenterApportionments', 'departmentApportionments', 
    'taxes', 'fiscal', 'norm', 'cargoComponent', 'thirdPartyNF', 'safetyDevice', 
    'nfe', 'inputCTRC', 'outputCTRC', 'ctrc', 'transportData', 'documentAuthorization', 
    'judicialProcess', 'serviceOrder', 'relatedMovement', 'exportRelatedMovement', 
    'linkedMovement', 'cTe', 'eaiIntegration', 'electronicInvoiceFreeFields'
}

# Generate column mapping
print("# Complete column mapping for file_processor.py:")
print("COLUMN_MAPPING = {")
for col in EXCEL_COLUMNS:
    snake_name = camel_to_snake(col)
    print(f"    '{col}': '{snake_name}',")
print("}")
print()

# Generate model fields
print("# Fields for FinancialMovement model:")
for col in EXCEL_COLUMNS:
    snake_name = camel_to_snake(col)
    
    if col in JSON_FIELDS:
        print(f"    {snake_name} = db.Column(db.Text, nullable=True)  # JSON field")
    elif col.endswith('Date') or col.endswith('Time'):
        print(f"    {snake_name} = db.Column(db.DateTime, nullable=True)")
    elif col.endswith('Id') or col.endswith('Code') or col.endswith('Number'):
        print(f"    {snake_name} = db.Column(db.String(50), nullable=True)")
    elif col.endswith('Value') or col.endswith('Percentage') or col.endswith('Charge') or col.endswith('Weight'):
        print(f"    {snake_name} = db.Column(db.Float, nullable=True)")
    elif col.endswith('Flag') or col.startswith('has') or col.startswith('is') or col in ['printed', 'documentPrinted', 'billPrinted', 'bonumIntegrated', 'processedFlag', 'usesFinancialValueApportionment', 'paradigmaAutoIntegrated']:
        print(f"    {snake_name} = db.Column(db.Boolean, nullable=True)")
    else:
        print(f"    {snake_name} = db.Column(db.String(255), nullable=True)")

print()
print("# JSON field processing methods:")
print("""    def get_json_field(self, field_name):
        \"\"\"Get a JSON field as a Python object\"\"\"
        field_value = getattr(self, field_name, None)
        if field_value:
            try:
                return json.loads(field_value) if isinstance(field_value, str) else field_value
            except (json.JSONDecodeError, TypeError):
                return []
        return []

    def set_json_field(self, field_name, value):
        \"\"\"Set a JSON field from a Python object\"\"\"
        if value:
            json_value = json.dumps(value) if not isinstance(value, str) else value
            setattr(self, field_name, json_value)
        else:
            setattr(self, field_name, None)""")

----- FILE: .local/state/replit/agent/.latest.json  (sha1:30ac0ad80407, bytes:18) -----
{"latest": "main"}

----- FILE: .local/state/replit/agent/filesystem/filesystem_state.json  (sha1:f9eafd04b5b5, bytes:102146) -----
{"file_contents":{"app.py":{"content":"import os\nimport logging\nfrom flask import Flask\nfrom flask_sqlalchemy import SQLAlchemy\nfrom flask_login import LoginManager\nfrom sqlalchemy.orm import DeclarativeBase\nfrom werkzeug.middleware.proxy_fix import ProxyFix\n\n# Configure logging\nlogging.basicConfig(level=logging.DEBUG)\n\nclass Base(DeclarativeBase):\n    pass\n\n# Initialize extensions\ndb = SQLAlchemy(model_class=Base)\nlogin_manager = LoginManager()\n\ndef create_app():\n    app = Flask(__name__)\n    \n    # Configuration\n    app.secret_key = os.environ.get(\"SESSION_SECRET\", \"dev-secret-key-change-in-production\")\n    app.config[\"SQLALCHEMY_DATABASE_URI\"] = os.environ.get(\"DATABASE_URL\", \"sqlite:///financial_data.db\")\n    app.config[\"SQLALCHEMY_TRACK_MODIFICATIONS\"] = False\n    app.config[\"SQLALCHEMY_ENGINE_OPTIONS\"] = {\n        \"pool_recycle\": 300,\n        \"pool_pre_ping\": True,\n    }\n    app.config[\"MAX_CONTENT_LENGTH\"] = 50 * 1024 * 1024  # 50MB max file size\n    app.config[\"UPLOAD_FOLDER\"] = \"uploads\"\n    \n    # Ensure upload directory exists\n    os.makedirs(app.config[\"UPLOAD_FOLDER\"], exist_ok=True)\n    \n    # Initialize extensions with app\n    db.init_app(app)\n    login_manager.init_app(app)\n    login_manager.login_view = 'auth.login'\n    login_manager.login_message = 'Please log in to access this page.'\n    \n    # ProxyFix for proper URL generation\n    app.wsgi_app = ProxyFix(app.wsgi_app, x_proto=1, x_host=1)\n    \n    # Import models to ensure they are registered\n    with app.app_context():\n        import models\n        db.create_all()\n        \n        # Create admin user if it doesn't exist\n        from models import User\n        from werkzeug.security import generate_password_hash\n        \n        admin = User.query.filter_by(username='admin').first()\n        if not admin:\n            admin = User(\n                username='admin',\n                email='admin@example.com',\n                is_admin=True,\n                password_hash=generate_password_hash('admin123')\n            )\n            db.session.add(admin)\n            db.session.commit()\n            logging.info(\"Created admin user: admin/admin123\")\n    \n    # Register blueprints\n    from routes import main_bp, admin_bp, auth_bp\n    app.register_blueprint(main_bp)\n    app.register_blueprint(admin_bp, url_prefix='/admin')\n    app.register_blueprint(auth_bp, url_prefix='/auth')\n    \n    return app\n\n# Create app instance\napp = create_app()\n","size_bytes":2402},"create_test_excel.py":{"content":"import pandas as pd\nfrom datetime import datetime\nimport json\n\n# Create a test Excel file with the new company and branch fields\ndata = []\n\n# Create 5 test records with the new structure\nfor i in range(1, 6):\n    record = {\n        # New company and branch fields (first 4 columns)\n        'Empresa_Code': f'EMP_{i:03d}',\n        'Empresa_Nome': f'Empresa Teste {i} Ltda',\n        'Filial_Code': f'FIL_{i:03d}',\n        'Filial_Nome': f'Filial {i} - Centro',\n        \n        # Existing core fields\n        'internalId': f'{i}|{400+i}',\n        'companyId': i,\n        'movementId': 400+i,\n        'branchId': i,\n        'warehouseCode': str(i),\n        'destinyWarehouseCode': str(i),\n        'number': 12000+i,\n        'series': 'A',\n        'movementTypeCode': '1.2.02',\n        'type': 'P',\n        'status': 'F',\n        'printed': False,\n        'documentPrinted': False,\n        'billPrinted': False,\n        'registerDate': datetime.now(),\n        'exitDate': datetime.now(),\n        'commercialRepresentativeCharge': 0.0,\n        'grossValue': 100.0 * i,\n        'netValue': 100.0 * i,\n        'informedNetValue': 0.0,\n        'otherValues': 100.0 * i,\n        'discountPercentage': 0.0,\n        'expensePercentage': 0.0,\n        'expenseValue': 0.0,\n        'extraPercentage1': 0.0,\n        'extraValue1': 0.0,\n        'extraPercentage2': 0.0,\n        'extraValue2': 0.0,\n        'transportedProductNetWeight': 0.0,\n        'transportedProductGrossWeight': 0.0,\n        'classificationTable5Code': '',\n        'financialOptionalTable2Code': '',\n        'netValueCurrencyCode': 'R$',\n        'date': datetime.now(),\n        'hasGeneratedBill': False,\n        'auxCustomerVendorCode': f'C{i:05d}',\n        'auxCustomerVendorCompanyId': 0,\n        'costCenterCode': '',\n        'salesman1Code': '',\n        'chargePercentage': 0.0,\n        'salesman2ChargePercentage': 0.0,\n        'salesman3ChargePercentage': 0.0,\n        'salesman4ChargePercentage': 0.0,\n        'userCode': 'testuser',\n        'destinyBranchId': i,\n        'lotGenerated': False,\n        'accountingExportStatus': '0',\n        'deliveryDate': '',\n        'hasGeneratedWorkAccount': False,\n        'workAccountGenerated': 'false',\n        'lastEditTime': datetime.now(),\n        'indicateObjectUse': '0',\n        'bonumIntegrated': False,\n        'processedFlag': False,\n        'icmsDeductionValue': 0.0,\n        'creationUser': 'testuser',\n        'creationDate': datetime.now(),\n        'emailStatus': 'false',\n        'internalGrossValue': 100.0 * i,\n        'otherCompanyINSSBaseValue': 0.0,\n        'conditionalDiscountValue': 0.0,\n        'conditionalExpenseValue': 0.0,\n        'affectStockOrder': str(400+i),\n        'commercialAutomationExported': 0,\n        'aplicationIntegration': 'T',\n        'entryDate': datetime.now(),\n        'extemporaneous': 0,\n        'merchandiseValue': 0.0,\n        'usesFinancialValueApportionment': False,\n        'conclusionFlag': 0,\n        'paradigmaStatus': 'N',\n        'paradigmaAutoIntegrated': False,\n        'originalGrossValue': 0.0,\n        'originalNetValue': 0.0,\n        'originalOtherValues': 0.0,\n        'operationId': 0,\n        'scpBranchId': 0,\n        'movementItems': '[]',\n        'payments': '[]',\n        'costCenterApportionments': '[]',\n        'departmentApportionments': '[]',\n        'taxes': '[]',\n        'fiscal': '[]',\n        'norm': '[]',\n        'cargoComponent': '[]',\n        'thirdPartyNF': '[]',\n        'safetyDevice': '[]',\n        'nfe': '[]',\n        'inputCTRC': '[]',\n        'outputCTRC': '[]',\n        'ctrc': '[]',\n        'transportData': '[]',\n        'documentAuthorization': '[]',\n        'judicialProcess': '[]',\n        'serviceOrder': '[]',\n        'relatedMovement': '[]',\n        'exportRelatedMovement': '[]',\n        'linkedMovement': '[]',\n        'cTe': '[]',\n        'eaiIntegration': '[]',\n        'electronicInvoiceFreeFields': '[]',\n        'customerVendorCode': f'C{i:05d}',\n        'paymentTermCode': '30.0',\n        'observation': f'Teste de movimento {i} com dados de empresa',\n        'financialOptionalTable1Code': '2.0',\n        'financialEntryMovementId': f'{400+i}.0',\n        'generatedEntryNumber': '1.0',\n        'openEntryNumber': '1.0',\n        'cashAccountCode': '2.0',\n        'customerVendorCompanyId': 0,\n        'fluxusGroupedFlag': False,\n        'cashAccountCompanyId': i\n    }\n    data.append(record)\n\n# Create DataFrame and save to Excel\ndf = pd.DataFrame(data)\nfilename = 'test_excel_with_company_branch.xlsx'\ndf.to_excel(filename, index=False)\nprint(f\"Created test Excel file: {filename}\")\nprint(f\"Columns: {len(df.columns)}\")\nprint(f\"Records: {len(df)}\")\nprint(\"First 4 columns (new):\")\nfor col in df.columns[:4]:\n    print(f\"  {col}: {df[col].iloc[0]}\")","size_bytes":4766},"file_processor.py":{"content":"import pandas as pd\nimport json\nimport uuid\nimport ast\nfrom datetime import datetime\nfrom typing import Dict, List, Tuple, Any\nimport logging\n\nlogger = logging.getLogger(__name__)\n\nclass FinancialDataProcessor:\n    \"\"\"Process Excel files containing financial movement data\"\"\"\n    \n    # Complete column mapping for all 117 fields (added 4 new company/branch fields)\n    COLUMN_MAPPING = {\n        'internalId': 'internal_id',\n        'companyId': 'company_id',\n        'movementId': 'movement_id',\n        'branchId': 'branch_id',\n        \n        # New company and branch information fields\n        'Empresa_Code': 'empresa_code',\n        'Empresa_Nome': 'empresa_nome',\n        'Filial_Code': 'filial_code',\n        'Filial_Nome': 'filial_nome',\n        'warehouseCode': 'warehouse_code',\n        'destinyWarehouseCode': 'destiny_warehouse_code',\n        'number': 'number',\n        'series': 'series',\n        'movementTypeCode': 'movement_type_code',\n        'type': 'type',\n        'status': 'status',\n        'printed': 'printed',\n        'documentPrinted': 'document_printed',\n        'billPrinted': 'bill_printed',\n        'registerDate': 'register_date',\n        'exitDate': 'exit_date',\n        'commercialRepresentativeCharge': 'commercial_representative_charge',\n        'grossValue': 'gross_value',\n        'netValue': 'net_value',\n        'informedNetValue': 'informed_net_value',\n        'otherValues': 'other_values',\n        'discountPercentage': 'discount_percentage',\n        'expensePercentage': 'expense_percentage',\n        'expenseValue': 'expense_value',\n        'extraPercentage1': 'extra_percentage1',\n        'extraValue1': 'extra_value1',\n        'extraPercentage2': 'extra_percentage2',\n        'extraValue2': 'extra_value2',\n        'transportedProductNetWeight': 'transported_product_net_weight',\n        'transportedProductGrossWeight': 'transported_product_gross_weight',\n        'classificationTable5Code': 'classification_table5_code',\n        'financialOptionalTable2Code': 'financial_optional_table2_code',\n        'netValueCurrencyCode': 'net_value_currency_code',\n        'date': 'date',\n        'hasGeneratedBill': 'has_generated_bill',\n        'auxCustomerVendorCode': 'aux_customer_vendor_code',\n        'auxCustomerVendorCompanyId': 'aux_customer_vendor_company_id',\n        'costCenterCode': 'cost_center_code',\n        'salesman1Code': 'salesman1_code',\n        'chargePercentage': 'charge_percentage',\n        'salesman2ChargePercentage': 'salesman2_charge_percentage',\n        'salesman3ChargePercentage': 'salesman3_charge_percentage',\n        'salesman4ChargePercentage': 'salesman4_charge_percentage',\n        'userCode': 'user_code',\n        'destinyBranchId': 'destiny_branch_id',\n        'lotGenerated': 'lot_generated',\n        'accountingExportStatus': 'accounting_export_status',\n        'deliveryDate': 'delivery_date',\n        'hasGeneratedWorkAccount': 'has_generated_work_account',\n        'workAccountGenerated': 'work_account_generated',\n        'lastEditTime': 'last_edit_time',\n        'indicateObjectUse': 'indicate_object_use',\n        'bonumIntegrated': 'bonum_integrated',\n        'processedFlag': 'processed_flag',\n        'icmsDeductionValue': 'icms_deduction_value',\n        'creationUser': 'creation_user',\n        'creationDate': 'creation_date',\n        'emailStatus': 'email_status',\n        'internalGrossValue': 'internal_gross_value',\n        'otherCompanyINSSBaseValue': 'other_company_inss_base_value',\n        'conditionalDiscountValue': 'conditional_discount_value',\n        'conditionalExpenseValue': 'conditional_expense_value',\n        'affectStockOrder': 'affect_stock_order',\n        'commercialAutomationExported': 'commercial_automation_exported',\n        'aplicationIntegration': 'aplication_integration',\n        'entryDate': 'entry_date',\n        'extemporaneous': 'extemporaneous',\n        'merchandiseValue': 'merchandise_value',\n        'usesFinancialValueApportionment': 'uses_financial_value_apportionment',\n        'conclusionFlag': 'conclusion_flag',\n        'paradigmaStatus': 'paradigma_status',\n        'paradigmaAutoIntegrated': 'paradigma_auto_integrated',\n        'originalGrossValue': 'original_gross_value',\n        'originalNetValue': 'original_net_value',\n        'originalOtherValues': 'original_other_values',\n        'operationId': 'operation_id',\n        'scpBranchId': 'scp_branch_id',\n        'movementItems': 'movement_items',\n        'payments': 'payments',\n        'costCenterApportionments': 'cost_center_apportionments',\n        'departmentApportionments': 'department_apportionments',\n        'taxes': 'taxes',\n        'fiscal': 'fiscal',\n        'norm': 'norm',\n        'cargoComponent': 'cargo_component',\n        'thirdPartyNF': 'third_party_nf',\n        'safetyDevice': 'safety_device',\n        'nfe': 'nfe',\n        'inputCTRC': 'input_ctrc',\n        'outputCTRC': 'output_ctrc',\n        'ctrc': 'ctrc',\n        'transportData': 'transport_data',\n        'documentAuthorization': 'document_authorization',\n        'judicialProcess': 'judicial_process',\n        'serviceOrder': 'service_order',\n        'relatedMovement': 'related_movement',\n        'exportRelatedMovement': 'export_related_movement',\n        'linkedMovement': 'linked_movement',\n        'cTe': 'c_te',\n        'eaiIntegration': 'eai_integration',\n        'electronicInvoiceFreeFields': 'electronic_invoice_free_fields',\n        'customerVendorCode': 'customer_vendor_code',\n        'paymentTermCode': 'payment_term_code',\n        'observation': 'observation',\n        'financialOptionalTable1Code': 'financial_optional_table1_code',\n        'financialEntryMovementId': 'financial_entry_movement_id',\n        'generatedEntryNumber': 'generated_entry_number',\n        'openEntryNumber': 'open_entry_number',\n        'cashAccountCode': 'cash_account_code',\n        'customerVendorCompanyId': 'customer_vendor_company_id',\n        'fluxusGroupedFlag': 'fluxus_grouped_flag',\n        'cashAccountCompanyId': 'cash_account_company_id',\n        'longHistory': 'long_history',\n    }\n    \n    # JSON fields that need special processing\n    JSON_FIELDS = {\n        'movement_items', 'payments', 'cost_center_apportionments', 'department_apportionments',\n        'taxes', 'fiscal', 'norm', 'cargo_component', 'third_party_nf', 'safety_device',\n        'nfe', 'input_ctrc', 'output_ctrc', 'ctrc', 'transport_data', 'document_authorization',\n        'judicial_process', 'service_order', 'related_movement', 'export_related_movement',\n        'linked_movement', 'c_te', 'eai_integration', 'electronic_invoice_free_fields'\n    }\n    \n    # Date fields that need special processing\n    DATE_FIELDS = {\n        'register_date', 'exit_date', 'delivery_date', 'creation_date', 'last_edit_time', \n        'date', 'entry_date'\n    }\n\n    def __init__(self):\n        self.batch_id = None\n\n    def process_file(self, file_path: str, user_id: int) -> Tuple[str, int, int, List[str], List[dict]]:\n        \"\"\"\n        Process an Excel file and return results\n        \n        Returns:\n            Tuple containing:\n            - batch_id (str): Unique identifier for this upload batch\n            - success_count (int): Number of successfully processed records\n            - error_count (int): Number of records with errors\n            - errors (List[str]): List of error messages\n            - processed_data (List[dict]): List of processed records for database insertion\n        \"\"\"\n        from app import db\n        from models import FinancialMovement, UploadHistory\n        \n        self.batch_id = str(uuid.uuid4())\n        errors = []\n        processed_data = []\n        success_count = 0\n        error_count = 0\n        \n        try:\n            # Read Excel file\n            logger.info(f\"Reading Excel file: {file_path}\")\n            df = pd.read_excel(file_path)\n            logger.info(f\"Read Excel file with {len(df)} rows\")\n            \n            # Process each row\n            for index, row in df.iterrows():\n                try:\n                    # Convert row to dictionary and map columns\n                    record_data = self._process_row(row)\n                    \n                    # Add tracking fields\n                    record_data['upload_batch_id'] = self.batch_id\n                    record_data['uploaded_by'] = user_id\n                    record_data['uploaded_at'] = datetime.utcnow()\n                    \n                    processed_data.append(record_data)\n                    success_count += 1\n                    \n                except Exception as e:\n                    error_msg = f\"Row {index + 1}: {str(e)}\"\n                    errors.append(error_msg)\n                    error_count += 1\n                    logger.error(error_msg)\n            \n            # Create database records\n            if processed_data:\n                self._save_to_database(processed_data, file_path, user_id, success_count, error_count, errors)\n            \n            return self.batch_id, success_count, error_count, errors, processed_data\n            \n        except Exception as e:\n            error_msg = f\"Failed to process file: {str(e)}\"\n            logger.error(error_msg)\n            return self.batch_id or str(uuid.uuid4()), 0, 1, [error_msg], []\n\n    def _process_row(self, row: pd.Series) -> Dict[str, Any]:\n        \"\"\"Process a single row of data\"\"\"\n        processed = {}\n        \n        for excel_col, db_col in self.COLUMN_MAPPING.items():\n            if excel_col in row:\n                value = row[excel_col]\n                \n                # Handle None/NaN values\n                if pd.isna(value):\n                    processed[db_col] = None\n                    continue\n                \n                # Process JSON fields\n                if db_col in self.JSON_FIELDS:\n                    processed[db_col] = self._process_json_field(value)\n                \n                # Process date fields\n                elif db_col in self.DATE_FIELDS:\n                    processed[db_col] = self._process_date_field(value)\n                \n                # Process boolean fields\n                elif isinstance(value, bool) or db_col.endswith('_flag') or db_col.startswith('has_'):\n                    processed[db_col] = bool(value) if value is not None else None\n                \n                # Process numeric fields\n                elif isinstance(value, (int, float)):\n                    processed[db_col] = value\n                \n                # Process string fields\n                else:\n                    processed[db_col] = str(value) if value is not None else None\n            else:\n                processed[db_col] = None\n                \n        return processed\n\n    def _process_json_field(self, value: Any) -> str:\n        \"\"\"Process JSON field values - handles Python literal format from Excel\"\"\"\n        if value is None or pd.isna(value):\n            return json.dumps([])\n        \n        if isinstance(value, str):\n            # Check if it's empty string or just whitespace\n            if not value.strip():\n                return json.dumps([])\n            \n            fixed_value = value.strip()\n            \n            # First, try to parse as standard JSON\n            try:\n                parsed = json.loads(fixed_value)\n                return fixed_value\n            except (json.JSONDecodeError, TypeError):\n                pass\n            \n            # Try to parse as Python literal (with single quotes)\n            try:\n                parsed = ast.literal_eval(fixed_value)\n                # Convert to proper JSON string\n                return json.dumps(parsed)\n            except (ValueError, SyntaxError) as e:\n                # Log the error for debugging\n                logger.warning(f\"Both JSON and literal_eval failed for value: {str(value)[:100]}... Error: {str(e)}\")\n                # Return empty array to avoid breaking the system\n                return json.dumps([])\n        \n        if isinstance(value, list):\n            return json.dumps(value)\n        \n        # For other types, wrap in array\n        return json.dumps([value] if value else [])\n\n    def _process_date_field(self, value: Any) -> datetime:\n        \"\"\"Process date field values\"\"\"\n        if value is None or pd.isna(value):\n            return None\n        \n        if isinstance(value, datetime):\n            return value\n        \n        if isinstance(value, str):\n            try:\n                return datetime.fromisoformat(value.replace('Z', '+00:00'))\n            except ValueError:\n                try:\n                    return pd.to_datetime(value).to_pydatetime()\n                except (ValueError, TypeError):\n                    return None\n        \n        if hasattr(value, 'to_pydatetime'):\n            return value.to_pydatetime()\n        \n        return None\n\n    def _save_to_database(self, processed_data: List[Dict], filename: str, \n                         user_id: int, success_count: int, error_count: int, errors: List[str]):\n        \"\"\"Save processed data to database\"\"\"\n        from app import db\n        from models import FinancialMovement, UploadHistory\n        \n        # Create upload history record\n        upload_history = UploadHistory(\n            batch_id=self.batch_id,\n            filename=filename.split('/')[-1],  # Get just filename without path\n            uploaded_by=user_id,\n            total_rows=len(processed_data),\n            success_count=success_count,\n            error_count=error_count,\n            errors=json.dumps(errors) if errors else None,\n            status='completed' if error_count == 0 else 'completed_with_errors'\n        )\n        \n        # Create movement records\n        movements = [FinancialMovement(**data) for data in processed_data]\n        \n        # Save to database\n        db.session.add(upload_history)\n        db.session.add_all(movements)\n        db.session.commit()\n        \n        logger.info(f\"Saved {success_count} records to database with batch_id {self.batch_id}\")","size_bytes":14020},"file_processor_new.py":{"content":"import pandas as pd\nimport json\nimport uuid\nfrom datetime import datetime\nfrom typing import Dict, List, Tuple, Any\nimport logging\n\nlogger = logging.getLogger(__name__)\n\nclass FinancialDataProcessor:\n    \"\"\"Process Excel files containing financial movement data\"\"\"\n    \n    # Complete column mapping for all 113 fields\n    COLUMN_MAPPING = {\n        'internalId': 'internal_id',\n        'companyId': 'company_id',\n        'movementId': 'movement_id',\n        'branchId': 'branch_id',\n        'warehouseCode': 'warehouse_code',\n        'destinyWarehouseCode': 'destiny_warehouse_code',\n        'number': 'number',\n        'series': 'series',\n        'movementTypeCode': 'movement_type_code',\n        'type': 'type',\n        'status': 'status',\n        'printed': 'printed',\n        'documentPrinted': 'document_printed',\n        'billPrinted': 'bill_printed',\n        'registerDate': 'register_date',\n        'exitDate': 'exit_date',\n        'commercialRepresentativeCharge': 'commercial_representative_charge',\n        'grossValue': 'gross_value',\n        'netValue': 'net_value',\n        'informedNetValue': 'informed_net_value',\n        'otherValues': 'other_values',\n        'discountPercentage': 'discount_percentage',\n        'expensePercentage': 'expense_percentage',\n        'expenseValue': 'expense_value',\n        'extraPercentage1': 'extra_percentage1',\n        'extraValue1': 'extra_value1',\n        'extraPercentage2': 'extra_percentage2',\n        'extraValue2': 'extra_value2',\n        'transportedProductNetWeight': 'transported_product_net_weight',\n        'transportedProductGrossWeight': 'transported_product_gross_weight',\n        'classificationTable5Code': 'classification_table5_code',\n        'financialOptionalTable2Code': 'financial_optional_table2_code',\n        'netValueCurrencyCode': 'net_value_currency_code',\n        'date': 'date',\n        'hasGeneratedBill': 'has_generated_bill',\n        'auxCustomerVendorCode': 'aux_customer_vendor_code',\n        'auxCustomerVendorCompanyId': 'aux_customer_vendor_company_id',\n        'costCenterCode': 'cost_center_code',\n        'salesman1Code': 'salesman1_code',\n        'chargePercentage': 'charge_percentage',\n        'salesman2ChargePercentage': 'salesman2_charge_percentage',\n        'salesman3ChargePercentage': 'salesman3_charge_percentage',\n        'salesman4ChargePercentage': 'salesman4_charge_percentage',\n        'userCode': 'user_code',\n        'destinyBranchId': 'destiny_branch_id',\n        'lotGenerated': 'lot_generated',\n        'accountingExportStatus': 'accounting_export_status',\n        'deliveryDate': 'delivery_date',\n        'hasGeneratedWorkAccount': 'has_generated_work_account',\n        'workAccountGenerated': 'work_account_generated',\n        'lastEditTime': 'last_edit_time',\n        'indicateObjectUse': 'indicate_object_use',\n        'bonumIntegrated': 'bonum_integrated',\n        'processedFlag': 'processed_flag',\n        'icmsDeductionValue': 'icms_deduction_value',\n        'creationUser': 'creation_user',\n        'creationDate': 'creation_date',\n        'emailStatus': 'email_status',\n        'internalGrossValue': 'internal_gross_value',\n        'otherCompanyINSSBaseValue': 'other_company_inss_base_value',\n        'conditionalDiscountValue': 'conditional_discount_value',\n        'conditionalExpenseValue': 'conditional_expense_value',\n        'affectStockOrder': 'affect_stock_order',\n        'commercialAutomationExported': 'commercial_automation_exported',\n        'aplicationIntegration': 'aplication_integration',\n        'entryDate': 'entry_date',\n        'extemporaneous': 'extemporaneous',\n        'merchandiseValue': 'merchandise_value',\n        'usesFinancialValueApportionment': 'uses_financial_value_apportionment',\n        'conclusionFlag': 'conclusion_flag',\n        'paradigmaStatus': 'paradigma_status',\n        'paradigmaAutoIntegrated': 'paradigma_auto_integrated',\n        'originalGrossValue': 'original_gross_value',\n        'originalNetValue': 'original_net_value',\n        'originalOtherValues': 'original_other_values',\n        'operationId': 'operation_id',\n        'scpBranchId': 'scp_branch_id',\n        'movementItems': 'movement_items',\n        'payments': 'payments',\n        'costCenterApportionments': 'cost_center_apportionments',\n        'departmentApportionments': 'department_apportionments',\n        'taxes': 'taxes',\n        'fiscal': 'fiscal',\n        'norm': 'norm',\n        'cargoComponent': 'cargo_component',\n        'thirdPartyNF': 'third_party_nf',\n        'safetyDevice': 'safety_device',\n        'nfe': 'nfe',\n        'inputCTRC': 'input_ctrc',\n        'outputCTRC': 'output_ctrc',\n        'ctrc': 'ctrc',\n        'transportData': 'transport_data',\n        'documentAuthorization': 'document_authorization',\n        'judicialProcess': 'judicial_process',\n        'serviceOrder': 'service_order',\n        'relatedMovement': 'related_movement',\n        'exportRelatedMovement': 'export_related_movement',\n        'linkedMovement': 'linked_movement',\n        'cTe': 'c_te',\n        'eaiIntegration': 'eai_integration',\n        'electronicInvoiceFreeFields': 'electronic_invoice_free_fields',\n        'customerVendorCode': 'customer_vendor_code',\n        'paymentTermCode': 'payment_term_code',\n        'observation': 'observation',\n        'financialOptionalTable1Code': 'financial_optional_table1_code',\n        'financialEntryMovementId': 'financial_entry_movement_id',\n        'generatedEntryNumber': 'generated_entry_number',\n        'openEntryNumber': 'open_entry_number',\n        'cashAccountCode': 'cash_account_code',\n        'customerVendorCompanyId': 'customer_vendor_company_id',\n        'fluxusGroupedFlag': 'fluxus_grouped_flag',\n        'cashAccountCompanyId': 'cash_account_company_id',\n        'longHistory': 'long_history',\n    }\n    \n    # JSON fields that need special processing\n    JSON_FIELDS = {\n        'movement_items', 'payments', 'cost_center_apportionments', 'department_apportionments',\n        'taxes', 'fiscal', 'norm', 'cargo_component', 'third_party_nf', 'safety_device',\n        'nfe', 'input_ctrc', 'output_ctrc', 'ctrc', 'transport_data', 'document_authorization',\n        'judicial_process', 'service_order', 'related_movement', 'export_related_movement',\n        'linked_movement', 'c_te', 'eai_integration', 'electronic_invoice_free_fields'\n    }\n    \n    # Date fields that need special processing\n    DATE_FIELDS = {\n        'register_date', 'exit_date', 'delivery_date', 'creation_date', 'last_edit_time', \n        'date', 'entry_date'\n    }\n\n    def __init__(self):\n        self.batch_id = None\n\n    def process_file(self, file_path: str, user_id: int) -> Tuple[str, int, int, List[str], List[dict]]:\n        \"\"\"\n        Process an Excel file and return results\n        \n        Returns:\n            Tuple containing:\n            - batch_id (str): Unique identifier for this upload batch\n            - success_count (int): Number of successfully processed records\n            - error_count (int): Number of records with errors\n            - errors (List[str]): List of error messages\n            - processed_data (List[dict]): List of processed records for database insertion\n        \"\"\"\n        from app import db\n        from models import FinancialMovement, UploadHistory\n        \n        self.batch_id = str(uuid.uuid4())\n        errors = []\n        processed_data = []\n        success_count = 0\n        error_count = 0\n        \n        try:\n            # Read Excel file\n            logger.info(f\"Reading Excel file: {file_path}\")\n            df = pd.read_excel(file_path)\n            logger.info(f\"Read Excel file with {len(df)} rows\")\n            \n            # Process each row\n            for index, row in df.iterrows():\n                try:\n                    # Convert row to dictionary and map columns\n                    record_data = self._process_row(row)\n                    \n                    # Add tracking fields\n                    record_data['upload_batch_id'] = self.batch_id\n                    record_data['uploaded_by'] = user_id\n                    record_data['uploaded_at'] = datetime.utcnow()\n                    \n                    processed_data.append(record_data)\n                    success_count += 1\n                    \n                except Exception as e:\n                    error_msg = f\"Row {index + 1}: {str(e)}\"\n                    errors.append(error_msg)\n                    error_count += 1\n                    logger.error(error_msg)\n            \n            # Create database records\n            if processed_data:\n                self._save_to_database(processed_data, file_path, user_id, success_count, error_count, errors)\n            \n            return self.batch_id, success_count, error_count, errors, processed_data\n            \n        except Exception as e:\n            error_msg = f\"Failed to process file: {str(e)}\"\n            logger.error(error_msg)\n            return self.batch_id or str(uuid.uuid4()), 0, 1, [error_msg], []\n\n    def _process_row(self, row: pd.Series) -> Dict[str, Any]:\n        \"\"\"Process a single row of data\"\"\"\n        processed = {}\n        \n        for excel_col, db_col in self.COLUMN_MAPPING.items():\n            if excel_col in row:\n                value = row[excel_col]\n                \n                # Handle None/NaN values\n                if pd.isna(value):\n                    processed[db_col] = None\n                    continue\n                \n                # Process JSON fields\n                if db_col in self.JSON_FIELDS:\n                    processed[db_col] = self._process_json_field(value)\n                \n                # Process date fields\n                elif db_col in self.DATE_FIELDS:\n                    processed[db_col] = self._process_date_field(value)\n                \n                # Process boolean fields\n                elif isinstance(value, bool) or db_col.endswith('_flag') or db_col.startswith('has_'):\n                    processed[db_col] = bool(value) if value is not None else None\n                \n                # Process numeric fields\n                elif isinstance(value, (int, float)):\n                    processed[db_col] = value\n                \n                # Process string fields\n                else:\n                    processed[db_col] = str(value) if value is not None else None\n            else:\n                processed[db_col] = None\n                \n        return processed\n\n    def _process_json_field(self, value: Any) -> str:\n        \"\"\"Process JSON field values\"\"\"\n        if value is None or pd.isna(value):\n            return None\n        \n        if isinstance(value, str):\n            # If it's already a JSON string, return as is\n            try:\n                json.loads(value)\n                return value\n            except (json.JSONDecodeError, TypeError):\n                return json.dumps([])\n        \n        if isinstance(value, list):\n            return json.dumps(value)\n        \n        # For other types, wrap in array\n        return json.dumps([value] if value else [])\n\n    def _process_date_field(self, value: Any) -> datetime:\n        \"\"\"Process date field values\"\"\"\n        if value is None or pd.isna(value):\n            return None\n        \n        if isinstance(value, datetime):\n            return value\n        \n        if isinstance(value, str):\n            try:\n                return datetime.fromisoformat(value.replace('Z', '+00:00'))\n            except ValueError:\n                try:\n                    return pd.to_datetime(value).to_pydatetime()\n                except (ValueError, TypeError):\n                    return None\n        \n        if hasattr(value, 'to_pydatetime'):\n            return value.to_pydatetime()\n        \n        return None\n\n    def _save_to_database(self, processed_data: List[Dict], filename: str, \n                         user_id: int, success_count: int, error_count: int, errors: List[str]):\n        \"\"\"Save processed data to database\"\"\"\n        from app import db\n        from models import FinancialMovement, UploadHistory\n        \n        # Create upload history record\n        upload_history = UploadHistory(\n            batch_id=self.batch_id,\n            filename=filename.split('/')[-1],  # Get just filename without path\n            uploaded_by=user_id,\n            total_rows=len(processed_data),\n            success_count=success_count,\n            error_count=error_count,\n            errors=json.dumps(errors) if errors else None,\n            status='completed' if error_count == 0 else 'completed_with_errors'\n        )\n        \n        # Create movement records\n        movements = [FinancialMovement(**data) for data in processed_data]\n        \n        # Save to database\n        db.session.add(upload_history)\n        db.session.add_all(movements)\n        db.session.commit()\n        \n        logger.info(f\"Saved {success_count} records to database with batch_id {self.batch_id}\")","size_bytes":12948},"forms.py":{"content":"from flask_wtf import FlaskForm\nfrom flask_wtf.file import FileField, FileRequired, FileAllowed\nfrom wtforms import StringField, PasswordField, BooleanField, SubmitField, SelectField, TextAreaField\nfrom wtforms.validators import DataRequired, Email, Length, EqualTo, ValidationError\nfrom models import User\n\nclass LoginForm(FlaskForm):\n    username = StringField('Username', validators=[DataRequired()])\n    password = PasswordField('Password', validators=[DataRequired()])\n    remember_me = BooleanField('Remember Me')\n    submit = SubmitField('Sign In')\n\nclass UserForm(FlaskForm):\n    username = StringField('Username', validators=[\n        DataRequired(), \n        Length(min=3, max=80)\n    ])\n    email = StringField('Email', validators=[\n        DataRequired(), \n        Email()\n    ])\n    password = PasswordField('Password', validators=[\n        DataRequired(), \n        Length(min=6)\n    ])\n    password2 = PasswordField('Repeat Password', validators=[\n        DataRequired(), \n        EqualTo('password', message='Passwords must match')\n    ])\n    is_admin = BooleanField('Administrator')\n    submit = SubmitField('Create User')\n    \n    def __init__(self, original_username=None, original_email=None, *args, **kwargs):\n        super(UserForm, self).__init__(*args, **kwargs)\n        self.original_username = original_username\n        self.original_email = original_email\n    \n    def validate_username(self, username):\n        if username.data != self.original_username:\n            user = User.query.filter_by(username=username.data).first()\n            if user is not None:\n                raise ValidationError('Username already exists.')\n    \n    def validate_email(self, email):\n        if email.data != self.original_email:\n            user = User.query.filter_by(email=email.data).first()\n            if user is not None:\n                raise ValidationError('Email already registered.')\n\nclass EditUserForm(FlaskForm):\n    username = StringField('Username', validators=[\n        DataRequired(), \n        Length(min=3, max=80)\n    ])\n    email = StringField('Email', validators=[\n        DataRequired(), \n        Email()\n    ])\n    is_admin = BooleanField('Administrator')\n    submit = SubmitField('Update User')\n    \n    def __init__(self, original_username=None, original_email=None, *args, **kwargs):\n        super(EditUserForm, self).__init__(*args, **kwargs)\n        self.original_username = original_username\n        self.original_email = original_email\n    \n    def validate_username(self, username):\n        if username.data != self.original_username:\n            user = User.query.filter_by(username=username.data).first()\n            if user is not None:\n                raise ValidationError('Username already exists.')\n    \n    def validate_email(self, email):\n        if email.data != self.original_email:\n            user = User.query.filter_by(email=email.data).first()\n            if user is not None:\n                raise ValidationError('Email already registered.')\n\nclass FileUploadForm(FlaskForm):\n    file = FileField('Excel File', validators=[\n        FileRequired(),\n        FileAllowed(['xlsx', 'xls'], 'Only Excel files (.xlsx, .xls) are allowed!')\n    ])\n    description = TextAreaField('Description (Optional)')\n    submit = SubmitField('Upload File')\n","size_bytes":3299},"main.py":{"content":"from app import app\n\nif __name__ == \"__main__\":\n    app.run(host=\"0.0.0.0\", port=5000, debug=True)\n","size_bytes":99},"models.py":{"content":"from app import db\nfrom flask_login import UserMixin\nfrom datetime import datetime\nimport json\n\nclass User(UserMixin, db.Model):\n    id = db.Column(db.Integer, primary_key=True)\n    username = db.Column(db.String(80), unique=True, nullable=False)\n    email = db.Column(db.String(120), unique=True, nullable=False)\n    password_hash = db.Column(db.String(256), nullable=False)\n    is_admin = db.Column(db.Boolean, default=False)\n    created_at = db.Column(db.DateTime, default=datetime.utcnow)\n    \n    def __repr__(self):\n        return f'<User {self.username}>'\n\nclass FinancialMovement(db.Model):\n    id = db.Column(db.Integer, primary_key=True)\n    \n    # Core identification fields - all 117 fields from Excel (added 4 new company/branch info fields)\n    internal_id = db.Column(db.String(50), nullable=True)\n    company_id = db.Column(db.Integer, nullable=True)\n    movement_id = db.Column(db.Integer, nullable=True)\n    branch_id = db.Column(db.Integer, nullable=True)\n    \n    # Company and Branch information (new fields)\n    empresa_code = db.Column(db.String(50), nullable=True)\n    empresa_nome = db.Column(db.String(255), nullable=True)\n    filial_code = db.Column(db.String(50), nullable=True)\n    filial_nome = db.Column(db.String(255), nullable=True)\n    warehouse_code = db.Column(db.String(50), nullable=True)\n    destiny_warehouse_code = db.Column(db.String(50), nullable=True)\n    number = db.Column(db.Integer, nullable=True)\n    series = db.Column(db.String(10), nullable=True)\n    movement_type_code = db.Column(db.String(50), nullable=True)\n    type = db.Column(db.String(10), nullable=True)\n    status = db.Column(db.String(10), nullable=True)\n    printed = db.Column(db.Boolean, nullable=True)\n    document_printed = db.Column(db.Boolean, nullable=True)\n    bill_printed = db.Column(db.Boolean, nullable=True)\n    register_date = db.Column(db.DateTime, nullable=True)\n    exit_date = db.Column(db.DateTime, nullable=True)\n    commercial_representative_charge = db.Column(db.Float, nullable=True)\n    gross_value = db.Column(db.Float, nullable=True)\n    net_value = db.Column(db.Float, nullable=True)\n    informed_net_value = db.Column(db.Float, nullable=True)\n    other_values = db.Column(db.Float, nullable=True)\n    discount_percentage = db.Column(db.Float, nullable=True)\n    expense_percentage = db.Column(db.Float, nullable=True)\n    expense_value = db.Column(db.Float, nullable=True)\n    extra_percentage1 = db.Column(db.Float, nullable=True)\n    extra_value1 = db.Column(db.Float, nullable=True)\n    extra_percentage2 = db.Column(db.Float, nullable=True)\n    extra_value2 = db.Column(db.Float, nullable=True)\n    transported_product_net_weight = db.Column(db.Float, nullable=True)\n    transported_product_gross_weight = db.Column(db.Float, nullable=True)\n    classification_table5_code = db.Column(db.String(50), nullable=True)\n    financial_optional_table2_code = db.Column(db.String(50), nullable=True)\n    net_value_currency_code = db.Column(db.String(10), nullable=True)\n    date = db.Column(db.DateTime, nullable=True)\n    has_generated_bill = db.Column(db.Boolean, nullable=True)\n    aux_customer_vendor_code = db.Column(db.String(50), nullable=True)\n    aux_customer_vendor_company_id = db.Column(db.Integer, nullable=True)\n    cost_center_code = db.Column(db.String(50), nullable=True)\n    salesman1_code = db.Column(db.String(50), nullable=True)\n    charge_percentage = db.Column(db.Float, nullable=True)\n    salesman2_charge_percentage = db.Column(db.Float, nullable=True)\n    salesman3_charge_percentage = db.Column(db.Float, nullable=True)\n    salesman4_charge_percentage = db.Column(db.Float, nullable=True)\n    user_code = db.Column(db.String(50), nullable=True)\n    destiny_branch_id = db.Column(db.Integer, nullable=True)\n    lot_generated = db.Column(db.Boolean, nullable=True)\n    accounting_export_status = db.Column(db.String(50), nullable=True)\n    delivery_date = db.Column(db.DateTime, nullable=True)\n    has_generated_work_account = db.Column(db.Boolean, nullable=True)\n    work_account_generated = db.Column(db.String(50), nullable=True)\n    last_edit_time = db.Column(db.DateTime, nullable=True)\n    indicate_object_use = db.Column(db.String(50), nullable=True)\n    bonum_integrated = db.Column(db.Boolean, nullable=True)\n    processed_flag = db.Column(db.Boolean, nullable=True)\n    icms_deduction_value = db.Column(db.Float, nullable=True)\n    creation_user = db.Column(db.String(50), nullable=True)\n    creation_date = db.Column(db.DateTime, nullable=True)\n    email_status = db.Column(db.String(50), nullable=True)\n    internal_gross_value = db.Column(db.Float, nullable=True)\n    other_company_inss_base_value = db.Column(db.Float, nullable=True)\n    conditional_discount_value = db.Column(db.Float, nullable=True)\n    conditional_expense_value = db.Column(db.Float, nullable=True)\n    affect_stock_order = db.Column(db.String(50), nullable=True)\n    commercial_automation_exported = db.Column(db.Integer, nullable=True)\n    aplication_integration = db.Column(db.String(10), nullable=True)\n    entry_date = db.Column(db.DateTime, nullable=True)\n    extemporaneous = db.Column(db.Integer, nullable=True)\n    merchandise_value = db.Column(db.Float, nullable=True)\n    uses_financial_value_apportionment = db.Column(db.Boolean, nullable=True)\n    conclusion_flag = db.Column(db.Integer, nullable=True)\n    paradigma_status = db.Column(db.String(10), nullable=True)\n    paradigma_auto_integrated = db.Column(db.Boolean, nullable=True)\n    original_gross_value = db.Column(db.Float, nullable=True)\n    original_net_value = db.Column(db.Float, nullable=True)\n    original_other_values = db.Column(db.Float, nullable=True)\n    operation_id = db.Column(db.Integer, nullable=True)\n    scp_branch_id = db.Column(db.Integer, nullable=True)\n    \n    # JSON fields - properly formatted as Text columns\n    movement_items = db.Column(db.Text, nullable=True)\n    payments = db.Column(db.Text, nullable=True)\n    cost_center_apportionments = db.Column(db.Text, nullable=True)\n    department_apportionments = db.Column(db.Text, nullable=True)\n    taxes = db.Column(db.Text, nullable=True)\n    fiscal = db.Column(db.Text, nullable=True)\n    norm = db.Column(db.Text, nullable=True)\n    cargo_component = db.Column(db.Text, nullable=True)\n    third_party_nf = db.Column(db.Text, nullable=True)\n    safety_device = db.Column(db.Text, nullable=True)\n    nfe = db.Column(db.Text, nullable=True)\n    input_ctrc = db.Column(db.Text, nullable=True)\n    output_ctrc = db.Column(db.Text, nullable=True)\n    ctrc = db.Column(db.Text, nullable=True)\n    transport_data = db.Column(db.Text, nullable=True)\n    document_authorization = db.Column(db.Text, nullable=True)\n    judicial_process = db.Column(db.Text, nullable=True)\n    service_order = db.Column(db.Text, nullable=True)\n    related_movement = db.Column(db.Text, nullable=True)\n    export_related_movement = db.Column(db.Text, nullable=True)\n    linked_movement = db.Column(db.Text, nullable=True)\n    c_te = db.Column(db.Text, nullable=True)\n    eai_integration = db.Column(db.Text, nullable=True)\n    electronic_invoice_free_fields = db.Column(db.Text, nullable=True)\n    \n    # Remaining fields\n    customer_vendor_code = db.Column(db.String(50), nullable=True)\n    payment_term_code = db.Column(db.String(50), nullable=True)\n    observation = db.Column(db.Text, nullable=True)\n    financial_optional_table1_code = db.Column(db.String(50), nullable=True)\n    financial_entry_movement_id = db.Column(db.String(50), nullable=True)\n    generated_entry_number = db.Column(db.String(50), nullable=True)\n    open_entry_number = db.Column(db.String(50), nullable=True)\n    cash_account_code = db.Column(db.String(50), nullable=True)\n    customer_vendor_company_id = db.Column(db.Integer, nullable=True)\n    fluxus_grouped_flag = db.Column(db.Boolean, nullable=True)\n    cash_account_company_id = db.Column(db.Integer, nullable=True)\n    long_history = db.Column(db.Text, nullable=True)\n    \n    # Upload tracking\n    upload_batch_id = db.Column(db.String(36))\n    uploaded_by = db.Column(db.Integer, db.ForeignKey('user.id'))\n    uploaded_at = db.Column(db.DateTime, default=datetime.utcnow)\n    \n    def get_json_field(self, field_name):\n        \"\"\"Get a JSON field as a Python object\"\"\"\n        field_value = getattr(self, field_name, None)\n        if field_value:\n            try:\n                return json.loads(field_value) if isinstance(field_value, str) else field_value\n            except (json.JSONDecodeError, TypeError):\n                return []\n        return []\n\n    def set_json_field(self, field_name, value):\n        \"\"\"Set a JSON field from a Python object\"\"\"\n        if value:\n            json_value = json.dumps(value) if not isinstance(value, str) else value\n            setattr(self, field_name, json_value)\n        else:\n            setattr(self, field_name, None)\n    \n    def __repr__(self):\n        return f'<FinancialMovement {self.internal_id}>'\n\nclass UploadHistory(db.Model):\n    id = db.Column(db.Integer, primary_key=True)\n    batch_id = db.Column(db.String(36), unique=True, nullable=False)\n    filename = db.Column(db.String(255), nullable=False)\n    uploaded_by = db.Column(db.Integer, db.ForeignKey('user.id'), nullable=False)\n    uploaded_at = db.Column(db.DateTime, default=datetime.utcnow)\n    total_rows = db.Column(db.Integer, default=0)\n    success_count = db.Column(db.Integer, default=0)\n    error_count = db.Column(db.Integer, default=0)\n    errors = db.Column(db.Text)\n    status = db.Column(db.String(20), default='processing')\n    \n    # Relationship\n    uploader = db.relationship('User', backref='uploads')\n    \n    def __repr__(self):\n        return f'<UploadHistory {self.filename}>'","size_bytes":9725},"models_new.py":{"content":"from app import db\nfrom flask_login import UserMixin\nfrom datetime import datetime\nimport json\n\nclass User(UserMixin, db.Model):\n    id = db.Column(db.Integer, primary_key=True)\n    username = db.Column(db.String(80), unique=True, nullable=False)\n    email = db.Column(db.String(120), unique=True, nullable=False)\n    password_hash = db.Column(db.String(256), nullable=False)\n    is_admin = db.Column(db.Boolean, default=False)\n    created_at = db.Column(db.DateTime, default=datetime.utcnow)\n    \n    def __repr__(self):\n        return f'<User {self.username}>'\n\nclass FinancialMovement(db.Model):\n    id = db.Column(db.Integer, primary_key=True)\n    \n    # Core identification fields - all 113 fields from Excel\n    internal_id = db.Column(db.String(50), nullable=True)\n    company_id = db.Column(db.Integer, nullable=True)\n    movement_id = db.Column(db.Integer, nullable=True)\n    branch_id = db.Column(db.Integer, nullable=True)\n    warehouse_code = db.Column(db.String(50), nullable=True)\n    destiny_warehouse_code = db.Column(db.String(50), nullable=True)\n    number = db.Column(db.Integer, nullable=True)\n    series = db.Column(db.String(10), nullable=True)\n    movement_type_code = db.Column(db.String(50), nullable=True)\n    type = db.Column(db.String(10), nullable=True)\n    status = db.Column(db.String(10), nullable=True)\n    printed = db.Column(db.Boolean, nullable=True)\n    document_printed = db.Column(db.Boolean, nullable=True)\n    bill_printed = db.Column(db.Boolean, nullable=True)\n    register_date = db.Column(db.DateTime, nullable=True)\n    exit_date = db.Column(db.DateTime, nullable=True)\n    commercial_representative_charge = db.Column(db.Float, nullable=True)\n    gross_value = db.Column(db.Float, nullable=True)\n    net_value = db.Column(db.Float, nullable=True)\n    informed_net_value = db.Column(db.Float, nullable=True)\n    other_values = db.Column(db.Float, nullable=True)\n    discount_percentage = db.Column(db.Float, nullable=True)\n    expense_percentage = db.Column(db.Float, nullable=True)\n    expense_value = db.Column(db.Float, nullable=True)\n    extra_percentage1 = db.Column(db.Float, nullable=True)\n    extra_value1 = db.Column(db.Float, nullable=True)\n    extra_percentage2 = db.Column(db.Float, nullable=True)\n    extra_value2 = db.Column(db.Float, nullable=True)\n    transported_product_net_weight = db.Column(db.Float, nullable=True)\n    transported_product_gross_weight = db.Column(db.Float, nullable=True)\n    classification_table5_code = db.Column(db.String(50), nullable=True)\n    financial_optional_table2_code = db.Column(db.String(50), nullable=True)\n    net_value_currency_code = db.Column(db.String(10), nullable=True)\n    date = db.Column(db.DateTime, nullable=True)\n    has_generated_bill = db.Column(db.Boolean, nullable=True)\n    aux_customer_vendor_code = db.Column(db.String(50), nullable=True)\n    aux_customer_vendor_company_id = db.Column(db.Integer, nullable=True)\n    cost_center_code = db.Column(db.String(50), nullable=True)\n    salesman1_code = db.Column(db.String(50), nullable=True)\n    charge_percentage = db.Column(db.Float, nullable=True)\n    salesman2_charge_percentage = db.Column(db.Float, nullable=True)\n    salesman3_charge_percentage = db.Column(db.Float, nullable=True)\n    salesman4_charge_percentage = db.Column(db.Float, nullable=True)\n    user_code = db.Column(db.String(50), nullable=True)\n    destiny_branch_id = db.Column(db.Integer, nullable=True)\n    lot_generated = db.Column(db.Boolean, nullable=True)\n    accounting_export_status = db.Column(db.String(50), nullable=True)\n    delivery_date = db.Column(db.DateTime, nullable=True)\n    has_generated_work_account = db.Column(db.Boolean, nullable=True)\n    work_account_generated = db.Column(db.String(50), nullable=True)\n    last_edit_time = db.Column(db.DateTime, nullable=True)\n    indicate_object_use = db.Column(db.String(50), nullable=True)\n    bonum_integrated = db.Column(db.Boolean, nullable=True)\n    processed_flag = db.Column(db.Boolean, nullable=True)\n    icms_deduction_value = db.Column(db.Float, nullable=True)\n    creation_user = db.Column(db.String(50), nullable=True)\n    creation_date = db.Column(db.DateTime, nullable=True)\n    email_status = db.Column(db.String(50), nullable=True)\n    internal_gross_value = db.Column(db.Float, nullable=True)\n    other_company_inss_base_value = db.Column(db.Float, nullable=True)\n    conditional_discount_value = db.Column(db.Float, nullable=True)\n    conditional_expense_value = db.Column(db.Float, nullable=True)\n    affect_stock_order = db.Column(db.String(50), nullable=True)\n    commercial_automation_exported = db.Column(db.Integer, nullable=True)\n    aplication_integration = db.Column(db.String(10), nullable=True)\n    entry_date = db.Column(db.DateTime, nullable=True)\n    extemporaneous = db.Column(db.Integer, nullable=True)\n    merchandise_value = db.Column(db.Float, nullable=True)\n    uses_financial_value_apportionment = db.Column(db.Boolean, nullable=True)\n    conclusion_flag = db.Column(db.Integer, nullable=True)\n    paradigma_status = db.Column(db.String(10), nullable=True)\n    paradigma_auto_integrated = db.Column(db.Boolean, nullable=True)\n    original_gross_value = db.Column(db.Float, nullable=True)\n    original_net_value = db.Column(db.Float, nullable=True)\n    original_other_values = db.Column(db.Float, nullable=True)\n    operation_id = db.Column(db.Integer, nullable=True)\n    scp_branch_id = db.Column(db.Integer, nullable=True)\n    \n    # JSON fields - properly formatted as Text columns\n    movement_items = db.Column(db.Text, nullable=True)\n    payments = db.Column(db.Text, nullable=True)\n    cost_center_apportionments = db.Column(db.Text, nullable=True)\n    department_apportionments = db.Column(db.Text, nullable=True)\n    taxes = db.Column(db.Text, nullable=True)\n    fiscal = db.Column(db.Text, nullable=True)\n    norm = db.Column(db.Text, nullable=True)\n    cargo_component = db.Column(db.Text, nullable=True)\n    third_party_nf = db.Column(db.Text, nullable=True)\n    safety_device = db.Column(db.Text, nullable=True)\n    nfe = db.Column(db.Text, nullable=True)\n    input_ctrc = db.Column(db.Text, nullable=True)\n    output_ctrc = db.Column(db.Text, nullable=True)\n    ctrc = db.Column(db.Text, nullable=True)\n    transport_data = db.Column(db.Text, nullable=True)\n    document_authorization = db.Column(db.Text, nullable=True)\n    judicial_process = db.Column(db.Text, nullable=True)\n    service_order = db.Column(db.Text, nullable=True)\n    related_movement = db.Column(db.Text, nullable=True)\n    export_related_movement = db.Column(db.Text, nullable=True)\n    linked_movement = db.Column(db.Text, nullable=True)\n    c_te = db.Column(db.Text, nullable=True)\n    eai_integration = db.Column(db.Text, nullable=True)\n    electronic_invoice_free_fields = db.Column(db.Text, nullable=True)\n    \n    # Remaining fields\n    customer_vendor_code = db.Column(db.String(50), nullable=True)\n    payment_term_code = db.Column(db.String(50), nullable=True)\n    observation = db.Column(db.Text, nullable=True)\n    financial_optional_table1_code = db.Column(db.String(50), nullable=True)\n    financial_entry_movement_id = db.Column(db.String(50), nullable=True)\n    generated_entry_number = db.Column(db.String(50), nullable=True)\n    open_entry_number = db.Column(db.String(50), nullable=True)\n    cash_account_code = db.Column(db.String(50), nullable=True)\n    customer_vendor_company_id = db.Column(db.Integer, nullable=True)\n    fluxus_grouped_flag = db.Column(db.Boolean, nullable=True)\n    cash_account_company_id = db.Column(db.Integer, nullable=True)\n    long_history = db.Column(db.Text, nullable=True)\n    \n    # Upload tracking\n    upload_batch_id = db.Column(db.String(36))\n    uploaded_by = db.Column(db.Integer, db.ForeignKey('user.id'))\n    uploaded_at = db.Column(db.DateTime, default=datetime.utcnow)\n    \n    def get_json_field(self, field_name):\n        \"\"\"Get a JSON field as a Python object\"\"\"\n        field_value = getattr(self, field_name, None)\n        if field_value:\n            try:\n                return json.loads(field_value) if isinstance(field_value, str) else field_value\n            except (json.JSONDecodeError, TypeError):\n                return []\n        return []\n\n    def set_json_field(self, field_name, value):\n        \"\"\"Set a JSON field from a Python object\"\"\"\n        if value:\n            json_value = json.dumps(value) if not isinstance(value, str) else value\n            setattr(self, field_name, json_value)\n        else:\n            setattr(self, field_name, None)\n    \n    def __repr__(self):\n        return f'<FinancialMovement {self.internal_id}>'\n\nclass UploadHistory(db.Model):\n    id = db.Column(db.Integer, primary_key=True)\n    batch_id = db.Column(db.String(36), unique=True, nullable=False)\n    filename = db.Column(db.String(255), nullable=False)\n    uploaded_by = db.Column(db.Integer, db.ForeignKey('user.id'), nullable=False)\n    uploaded_at = db.Column(db.DateTime, default=datetime.utcnow)\n    total_rows = db.Column(db.Integer, default=0)\n    success_count = db.Column(db.Integer, default=0)\n    error_count = db.Column(db.Integer, default=0)\n    errors = db.Column(db.Text)\n    status = db.Column(db.String(20), default='processing')\n    \n    # Relationship\n    uploader = db.relationship('User', backref='uploads')\n    \n    def __repr__(self):\n        return f'<UploadHistory {self.filename}>'","size_bytes":9393},"pyproject.toml":{"content":"[project]\nname = \"repl-nix-workspace\"\nversion = \"0.1.0\"\ndescription = \"Add your description here\"\nrequires-python = \">=3.11\"\ndependencies = [\n    \"email-validator>=2.2.0\",\n    \"flask-dance>=7.1.0\",\n    \"flask>=3.1.1\",\n    \"flask-sqlalchemy>=3.1.1\",\n    \"gunicorn>=23.0.0\",\n    \"psycopg2-binary>=2.9.10\",\n    \"pyjwt>=2.10.1\",\n    \"flask-login>=0.6.3\",\n    \"oauthlib>=3.3.1\",\n    \"werkzeug>=3.1.3\",\n    \"flask-wtf>=1.2.2\",\n    \"wtforms>=3.2.1\",\n    \"pandas>=2.3.1\",\n    \"openpyxl>=3.1.5\",\n    \"xlrd>=2.0.2\",\n    \"sqlalchemy>=2.0.43\",\n]\n","size_bytes":534},"replit.md":{"content":"# Overview\n\nThis is a Flask-based financial data management system designed for importing, storing, and analyzing financial movement data from Excel files. The application features a role-based user system with administrative controls for user management and data upload capabilities. Users can browse and search through financial movement records, while administrators can upload Excel files containing structured financial data and manage user accounts.\n\n**Recent Update (Aug 2025)**: System now supports complete 117-field Excel structure with all JSON fields properly processed and displayed. Added 4 new company/branch identification fields: `Empresa_Code`, `Empresa_Nome`, `Filial_Code`, `Filial_Nome`. Upload functionality fully working with batch tracking and error handling.\n\n# User Preferences\n\nPreferred communication style: Simple, everyday language.\n\n# System Architecture\n\n## Web Framework\n- **Flask** with SQLAlchemy ORM for database operations\n- **Flask-Login** for user session management and authentication\n- **Flask-WTF** for form handling and CSRF protection\n- Blueprint-based routing for modular organization (main, admin, auth)\n\n## Authentication & Authorization\n- Password-based authentication with hashed storage using Werkzeug\n- Role-based access control with admin/regular user distinction\n- Session-based login with \"remember me\" functionality\n- Decorator-based route protection for admin-only features\n\n## Database Design\n- SQLAlchemy with SQLite default (configurable via DATABASE_URL)\n- **User model**: Basic user management with admin flags\n- **FinancialMovement model**: Comprehensive financial transaction records with 40+ fields\n- **UploadHistory model**: Tracks file upload batches and processing results\n- Connection pooling and automatic table creation\n\n## Data Processing\n- **FinancialDataProcessor class**: Handles Excel file parsing and data transformation\n- Column mapping from Excel headers to database fields\n- Batch processing with error handling and validation\n- Support for complex financial data structures including dates, currencies, and business logic fields\n\n## Frontend Architecture\n- Bootstrap 5 dark theme for responsive UI\n- jQuery and DataTables for enhanced table functionality\n- Template inheritance using Jinja2\n- Progressive enhancement with JavaScript for user interactions\n- File upload interface with drag-and-drop support\n\n## File Management\n- Secure file upload handling with size limits (50MB)\n- Excel file processing with pandas\n- Upload history tracking with success/failure metrics\n- Organized upload directory structure\n\n# External Dependencies\n\n## Core Framework Dependencies\n- **Flask**: Web application framework\n- **SQLAlchemy/Flask-SQLAlchemy**: Database ORM and integration\n- **Flask-Login**: User session management\n- **Flask-WTF/WTForms**: Form handling and validation\n- **Werkzeug**: WSGI utilities and security helpers\n\n## Data Processing\n- **pandas**: Excel file reading and data manipulation\n- **openpyxl**: Excel file format support\n\n## Frontend Libraries (CDN)\n- **Bootstrap 5**: UI framework with dark theme\n- **DataTables**: Enhanced table functionality\n- **Font Awesome**: Icon library\n- **jQuery**: JavaScript utilities\n\n## Infrastructure\n- **SQLite**: Default database (production can use PostgreSQL via DATABASE_URL)\n- **ProxyFix**: WSGI middleware for proper URL generation behind proxies\n- File system storage for uploaded Excel files\n\n## Security Features\n- CSRF protection through Flask-WTF\n- Password hashing with Werkzeug\n- Secure filename handling for uploads\n- Session-based authentication with configurable secrets","size_bytes":3613},"routes.py":{"content":"import os\nimport uuid\nfrom datetime import datetime\nfrom flask import Blueprint, render_template, request, redirect, url_for, flash, jsonify, current_app\nfrom flask_login import login_user, logout_user, login_required, current_user\nfrom werkzeug.security import check_password_hash, generate_password_hash\nfrom werkzeug.utils import secure_filename\nfrom sqlalchemy import or_, desc\nfrom app import db, login_manager\nfrom models import User, FinancialMovement, UploadHistory\nfrom forms import LoginForm, UserForm, EditUserForm, FileUploadForm\nfrom file_processor import FinancialDataProcessor\nimport logging\n\nlogger = logging.getLogger(__name__)\n\n# User loader for Flask-Login\n@login_manager.user_loader\ndef load_user(user_id):\n    return User.query.get(int(user_id))\n\n# Blueprint definitions\nmain_bp = Blueprint('main', __name__)\nadmin_bp = Blueprint('admin', __name__)\nauth_bp = Blueprint('auth', __name__)\n\ndef admin_required(f):\n    \"\"\"Decorator to require admin access\"\"\"\n    from functools import wraps\n    @wraps(f)\n    @login_required\n    def decorated_function(*args, **kwargs):\n        if not current_user.is_admin:\n            flash('You need administrator privileges to access this page.', 'error')\n            return redirect(url_for('main.index'))\n        return f(*args, **kwargs)\n    return decorated_function\n\n# Main routes\n@main_bp.route('/')\ndef index():\n    \"\"\"Homepage - show recent movements\"\"\"\n    recent_movements = FinancialMovement.query.order_by(desc(FinancialMovement.uploaded_at)).limit(10).all()\n    return render_template('index.html', movements=recent_movements)\n\n@main_bp.route('/movements')\n@login_required\ndef movements():\n    \"\"\"List all movements with search, filters and pagination\"\"\"\n    page = request.args.get('page', 1, type=int)\n    search = request.args.get('search', '', type=str)\n    company_filter = request.args.get('company', '', type=str)\n    branch_filter = request.args.get('branch', '', type=str)\n    status_filter = request.args.get('status', '', type=str)\n    per_page = 25\n    \n    query = FinancialMovement.query\n    \n    # Apply search filter\n    if search:\n        query = query.filter(\n            or_(\n                FinancialMovement.internal_id.contains(search),\n                FinancialMovement.number.contains(search),\n                FinancialMovement.aux_customer_vendor_code.contains(search),\n                FinancialMovement.observation.contains(search)\n            )\n        )\n    \n    # Apply company filter\n    if company_filter:\n        query = query.filter(FinancialMovement.empresa_nome.contains(company_filter))\n    \n    # Apply branch filter  \n    if branch_filter:\n        query = query.filter(FinancialMovement.filial_nome.contains(branch_filter))\n    \n    # Apply status filter\n    if status_filter:\n        query = query.filter(FinancialMovement.status == status_filter)\n    \n    movements = query.order_by(desc(FinancialMovement.uploaded_at)).paginate(\n        page=page, per_page=per_page, error_out=False\n    )\n    \n    # Get unique values for filter dropdowns\n    companies = db.session.query(FinancialMovement.empresa_nome).distinct().filter(\n        FinancialMovement.empresa_nome.isnot(None)\n    ).order_by(FinancialMovement.empresa_nome).all()\n    companies = [c[0] for c in companies if c[0]]\n    \n    branches = db.session.query(FinancialMovement.filial_nome).distinct().filter(\n        FinancialMovement.filial_nome.isnot(None)\n    ).order_by(FinancialMovement.filial_nome).all()\n    branches = [b[0] for b in branches if b[0]]\n    \n    statuses = db.session.query(FinancialMovement.status).distinct().filter(\n        FinancialMovement.status.isnot(None)\n    ).order_by(FinancialMovement.status).all()\n    statuses = [s[0] for s in statuses if s[0]]\n    \n    return render_template('movements/list.html', \n                         movements=movements, \n                         search=search,\n                         company_filter=company_filter,\n                         branch_filter=branch_filter,\n                         status_filter=status_filter,\n                         companies=companies,\n                         branches=branches,\n                         statuses=statuses)\n\n@main_bp.route('/movements/<int:id>')\n@login_required\ndef movement_detail(id):\n    \"\"\"Show detailed view of a specific movement\"\"\"\n    movement = FinancialMovement.query.get_or_404(id)\n    return render_template('movements/detail.html', movement=movement)\n\n@main_bp.route('/api/movements/<int:id>/json/<field>')\n@login_required\ndef get_movement_json_field(id, field):\n    \"\"\"API endpoint to get formatted JSON field data\"\"\"\n    movement = FinancialMovement.query.get_or_404(id)\n    json_data = movement.get_json_field(field)\n    return jsonify(json_data)\n\n# Authentication routes\n@auth_bp.route('/login', methods=['GET', 'POST'])\ndef login():\n    \"\"\"User login\"\"\"\n    if current_user.is_authenticated:\n        return redirect(url_for('main.index'))\n    \n    form = LoginForm()\n    if form.validate_on_submit():\n        user = User.query.filter_by(username=form.username.data).first()\n        if user and user.password_hash and check_password_hash(user.password_hash, form.password.data):\n            login_user(user, remember=form.remember_me.data)\n            next_page = request.args.get('next')\n            if not next_page:\n                next_page = url_for('admin.dashboard') if user.is_admin else url_for('main.index')\n            flash(f'Welcome back, {user.username}!', 'success')\n            return redirect(next_page)\n        flash('Invalid username or password', 'error')\n    \n    return render_template('auth/login.html', form=form)\n\n@auth_bp.route('/logout')\n@login_required\ndef logout():\n    \"\"\"User logout\"\"\"\n    logout_user()\n    flash('You have been logged out.', 'info')\n    return redirect(url_for('main.index'))\n\n# Admin routes\n@admin_bp.route('/dashboard')\n@admin_required\ndef dashboard():\n    \"\"\"Admin dashboard with statistics\"\"\"\n    stats = {\n        'total_users': User.query.count(),\n        'total_movements': FinancialMovement.query.count(),\n        'total_uploads': UploadHistory.query.count(),\n        'recent_uploads': UploadHistory.query.order_by(desc(UploadHistory.uploaded_at)).limit(5).all()\n    }\n    return render_template('admin/dashboard.html', stats=stats)\n\n@admin_bp.route('/users')\n@admin_required\ndef users():\n    \"\"\"List all users\"\"\"\n    page = request.args.get('page', 1, type=int)\n    users = User.query.order_by(User.created_at.desc()).paginate(\n        page=page, per_page=20, error_out=False\n    )\n    return render_template('admin/users.html', users=users)\n\n@admin_bp.route('/users/new', methods=['GET', 'POST'])\n@admin_required\ndef create_user():\n    \"\"\"Create new user\"\"\"\n    form = UserForm()\n    if form.validate_on_submit():\n        password_data = form.password.data\n        if password_data:\n            user = User(\n                username=form.username.data,\n                email=form.email.data,\n                is_admin=form.is_admin.data,\n                password_hash=generate_password_hash(password_data)\n            )\n        else:\n            flash('Password is required', 'error')\n            return render_template('admin/users.html', form=form, action='create')\n        db.session.add(user)\n        db.session.commit()\n        flash(f'User {user.username} created successfully!', 'success')\n        return redirect(url_for('admin.users'))\n    \n    return render_template('admin/users.html', form=form, action='create')\n\n@admin_bp.route('/users/<int:id>/edit', methods=['GET', 'POST'])\n@admin_required\ndef edit_user(id):\n    \"\"\"Edit existing user\"\"\"\n    user = User.query.get_or_404(id)\n    form = EditUserForm(\n        original_username=user.username,\n        original_email=user.email,\n        obj=user\n    )\n    \n    if form.validate_on_submit():\n        user.username = form.username.data\n        user.email = form.email.data\n        user.is_admin = form.is_admin.data\n        db.session.commit()\n        flash(f'User {user.username} updated successfully!', 'success')\n        return redirect(url_for('admin.users'))\n    \n    return render_template('admin/users.html', form=form, user=user, action='edit')\n\n@admin_bp.route('/users/<int:id>/delete', methods=['POST'])\n@admin_required\ndef delete_user(id):\n    \"\"\"Delete user\"\"\"\n    user = User.query.get_or_404(id)\n    if user.id == current_user.id:\n        flash('You cannot delete your own account!', 'error')\n        return redirect(url_for('admin.users'))\n    \n    db.session.delete(user)\n    db.session.commit()\n    flash(f'User {user.username} deleted successfully!', 'success')\n    return redirect(url_for('admin.users'))\n\n@admin_bp.route('/upload', methods=['GET', 'POST'])\n@admin_required\ndef upload_file():\n    \"\"\"Upload Excel file with financial data\"\"\"\n    form = FileUploadForm()\n    if form.validate_on_submit():\n        file = form.file.data\n        filename = secure_filename(file.filename)\n        \n        # Generate unique filename\n        unique_filename = f\"{uuid.uuid4()}_{filename}\"\n        file_path = os.path.join(current_app.config['UPLOAD_FOLDER'], unique_filename)\n        \n        try:\n            # Save file\n            file.save(file_path)\n            \n            # Process file\n            processor = FinancialDataProcessor()\n            batch_id, success_count, error_count, errors, processed_data = processor.process_file(\n                file_path, current_user.id\n            )\n            \n            # Note: processor.process_file already saves data to database\n            # No need to save processed_data again\n            \n            # Clean up uploaded file\n            if os.path.exists(file_path):\n                os.remove(file_path)\n            \n            if error_count > 0:\n                flash(f'File processed with {success_count} successful records and {error_count} errors. Check upload history for details.', 'warning')\n            else:\n                flash(f'File uploaded successfully! {success_count} records processed.', 'success')\n            \n            return redirect(url_for('admin.upload_history'))\n            \n        except Exception as e:\n            # Clean up file if processing failed\n            if os.path.exists(file_path):\n                os.remove(file_path)\n            flash(f'Error processing file: {str(e)}', 'error')\n            logger.error(f\"Upload processing error: {str(e)}\")\n    \n    return render_template('admin/upload.html', form=form)\n\n@admin_bp.route('/upload-history')\n@admin_required\ndef upload_history():\n    \"\"\"View upload history\"\"\"\n    page = request.args.get('page', 1, type=int)\n    uploads = UploadHistory.query.order_by(desc(UploadHistory.uploaded_at)).paginate(\n        page=page, per_page=20, error_out=False\n    )\n    return render_template('admin/upload.html', uploads=uploads, show_history=True)\n\n@admin_bp.route('/upload-history/<int:id>')\n@admin_required\ndef upload_history_detail(id):\n    \"\"\"View detailed upload history\"\"\"\n    upload = UploadHistory.query.get_or_404(id)\n    movements = FinancialMovement.query.filter_by(upload_batch_id=upload.batch_id).all()\n    return render_template('admin/upload.html', upload=upload, movements=movements, show_detail=True)\n\n@admin_bp.route('/clear-movements', methods=['GET', 'POST'])\n@admin_required\ndef clear_movements():\n    \"\"\"Clear all financial movements from database\"\"\"\n    if request.method == 'POST':\n        confirmation = request.form.get('confirmation', '').strip().lower()\n        \n        if confirmation == 'delete all movements':\n            try:\n                # Delete all financial movements and their upload history\n                movement_count = FinancialMovement.query.count()\n                upload_count = UploadHistory.query.count()\n                \n                db.session.query(FinancialMovement).delete()\n                db.session.query(UploadHistory).delete()\n                db.session.commit()\n                \n                flash(f'Database cleared successfully! {movement_count} financial movements and {upload_count} upload records deleted.', 'success')\n                return redirect(url_for('admin.dashboard'))\n                \n            except Exception as e:\n                db.session.rollback()\n                flash(f'Error clearing database: {str(e)}', 'error')\n                \n        else:\n            flash('Invalid confirmation text. Type exactly \"delete all movements\" to confirm.', 'warning')\n    \n    # Count current records\n    movement_count = FinancialMovement.query.count()\n    upload_count = UploadHistory.query.count()\n    \n    return render_template('admin/clear_movements.html', \n                          movement_count=movement_count, \n                          upload_count=upload_count)\n","size_bytes":12731},"update_models.py":{"content":"#!/usr/bin/env python3\n\"\"\"\nScript to generate complete FinancialMovement model with all 113 fields\n\"\"\"\n\n# All 113 fields from the Excel file\nEXCEL_COLUMNS = [\n    'internalId', 'companyId', 'movementId', 'branchId', 'warehouseCode', 'destinyWarehouseCode', \n    'number', 'series', 'movementTypeCode', 'type', 'status', 'printed', 'documentPrinted', \n    'billPrinted', 'registerDate', 'exitDate', 'commercialRepresentativeCharge', 'grossValue', \n    'netValue', 'informedNetValue', 'otherValues', 'discountPercentage', 'expensePercentage', \n    'expenseValue', 'extraPercentage1', 'extraValue1', 'extraPercentage2', 'extraValue2', \n    'transportedProductNetWeight', 'transportedProductGrossWeight', 'classificationTable5Code', \n    'financialOptionalTable2Code', 'netValueCurrencyCode', 'date', 'hasGeneratedBill', \n    'auxCustomerVendorCode', 'auxCustomerVendorCompanyId', 'costCenterCode', 'salesman1Code', \n    'chargePercentage', 'salesman2ChargePercentage', 'salesman3ChargePercentage', \n    'salesman4ChargePercentage', 'userCode', 'destinyBranchId', 'lotGenerated', \n    'accountingExportStatus', 'deliveryDate', 'hasGeneratedWorkAccount', 'workAccountGenerated', \n    'lastEditTime', 'indicateObjectUse', 'bonumIntegrated', 'processedFlag', 'icmsDeductionValue', \n    'creationUser', 'creationDate', 'emailStatus', 'internalGrossValue', 'otherCompanyINSSBaseValue', \n    'conditionalDiscountValue', 'conditionalExpenseValue', 'affectStockOrder', \n    'commercialAutomationExported', 'aplicationIntegration', 'entryDate', 'extemporaneous', \n    'merchandiseValue', 'usesFinancialValueApportionment', 'conclusionFlag', 'paradigmaStatus', \n    'paradigmaAutoIntegrated', 'originalGrossValue', 'originalNetValue', 'originalOtherValues', \n    'operationId', 'scpBranchId', 'movementItems', 'payments', 'costCenterApportionments', \n    'departmentApportionments', 'taxes', 'fiscal', 'norm', 'cargoComponent', 'thirdPartyNF', \n    'safetyDevice', 'nfe', 'inputCTRC', 'outputCTRC', 'ctrc', 'transportData', \n    'documentAuthorization', 'judicialProcess', 'serviceOrder', 'relatedMovement', \n    'exportRelatedMovement', 'linkedMovement', 'cTe', 'eaiIntegration', \n    'electronicInvoiceFreeFields', 'customerVendorCode', 'paymentTermCode', 'observation', \n    'financialOptionalTable1Code', 'financialEntryMovementId', 'generatedEntryNumber', \n    'openEntryNumber', 'cashAccountCode', 'customerVendorCompanyId', 'fluxusGroupedFlag', \n    'cashAccountCompanyId', 'longHistory'\n]\n\ndef camel_to_snake(name):\n    \"\"\"Convert camelCase to snake_case\"\"\"\n    import re\n    s1 = re.sub('(.)([A-Z][a-z]+)', r'\\1_\\2', name)\n    return re.sub('([a-z0-9])([A-Z])', r'\\1_\\2', s1).lower()\n\n# JSON fields that should be stored as JSON text\nJSON_FIELDS = {\n    'movementItems', 'payments', 'costCenterApportionments', 'departmentApportionments', \n    'taxes', 'fiscal', 'norm', 'cargoComponent', 'thirdPartyNF', 'safetyDevice', \n    'nfe', 'inputCTRC', 'outputCTRC', 'ctrc', 'transportData', 'documentAuthorization', \n    'judicialProcess', 'serviceOrder', 'relatedMovement', 'exportRelatedMovement', \n    'linkedMovement', 'cTe', 'eaiIntegration', 'electronicInvoiceFreeFields'\n}\n\n# Generate column mapping\nprint(\"# Complete column mapping for file_processor.py:\")\nprint(\"COLUMN_MAPPING = {\")\nfor col in EXCEL_COLUMNS:\n    snake_name = camel_to_snake(col)\n    print(f\"    '{col}': '{snake_name}',\")\nprint(\"}\")\nprint()\n\n# Generate model fields\nprint(\"# Fields for FinancialMovement model:\")\nfor col in EXCEL_COLUMNS:\n    snake_name = camel_to_snake(col)\n    \n    if col in JSON_FIELDS:\n        print(f\"    {snake_name} = db.Column(db.Text, nullable=True)  # JSON field\")\n    elif col.endswith('Date') or col.endswith('Time'):\n        print(f\"    {snake_name} = db.Column(db.DateTime, nullable=True)\")\n    elif col.endswith('Id') or col.endswith('Code') or col.endswith('Number'):\n        print(f\"    {snake_name} = db.Column(db.String(50), nullable=True)\")\n    elif col.endswith('Value') or col.endswith('Percentage') or col.endswith('Charge') or col.endswith('Weight'):\n        print(f\"    {snake_name} = db.Column(db.Float, nullable=True)\")\n    elif col.endswith('Flag') or col.startswith('has') or col.startswith('is') or col in ['printed', 'documentPrinted', 'billPrinted', 'bonumIntegrated', 'processedFlag', 'usesFinancialValueApportionment', 'paradigmaAutoIntegrated']:\n        print(f\"    {snake_name} = db.Column(db.Boolean, nullable=True)\")\n    else:\n        print(f\"    {snake_name} = db.Column(db.String(255), nullable=True)\")\n\nprint()\nprint(\"# JSON field processing methods:\")\nprint(\"\"\"    def get_json_field(self, field_name):\n        \\\"\\\"\\\"Get a JSON field as a Python object\\\"\\\"\\\"\n        field_value = getattr(self, field_name, None)\n        if field_value:\n            try:\n                return json.loads(field_value) if isinstance(field_value, str) else field_value\n            except (json.JSONDecodeError, TypeError):\n                return []\n        return []\n\n    def set_json_field(self, field_name, value):\n        \\\"\\\"\\\"Set a JSON field from a Python object\\\"\\\"\\\"\n        if value:\n            json_value = json.dumps(value) if not isinstance(value, str) else value\n            setattr(self, field_name, json_value)\n        else:\n            setattr(self, field_name, None)\"\"\")","size_bytes":5295},"static/css/style.css":{"content":"/* Custom styles for Financial Data Management System */\n/* Following Bootstrap 5 dark theme variables and minimal custom overrides */\n\n:root {\n    --custom-border-radius: 0.375rem;\n    --custom-box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075);\n    --custom-transition: all 0.15s ease-in-out;\n}\n\n/* Global improvements */\nbody {\n    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;\n}\n\n/* Custom card enhancements */\n.card {\n    border: 1px solid var(--bs-border-color);\n    box-shadow: var(--custom-box-shadow);\n    transition: var(--custom-transition);\n}\n\n.card:hover {\n    box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);\n}\n\n/* Enhanced navigation */\n.navbar-brand {\n    font-weight: 600;\n}\n\n/* Table improvements */\n.table-responsive {\n    border-radius: var(--custom-border-radius);\n}\n\n.table th {\n    font-weight: 600;\n    border-top: none;\n}\n\n/* Code elements */\ncode {\n    color: var(--bs-info);\n    background-color: rgba(var(--bs-info-rgb), 0.1);\n    padding: 0.125rem 0.25rem;\n    border-radius: 0.25rem;\n    font-size: 0.875em;\n}\n\n/* Badge improvements */\n.badge {\n    font-weight: 500;\n}\n\n/* Button enhancements */\n.btn {\n    font-weight: 500;\n    transition: var(--custom-transition);\n}\n\n.btn-group .btn {\n    border-radius: 0;\n}\n\n.btn-group .btn:first-child {\n    border-top-left-radius: var(--custom-border-radius);\n    border-bottom-left-radius: var(--custom-border-radius);\n}\n\n.btn-group .btn:last-child {\n    border-top-right-radius: var(--custom-border-radius);\n    border-bottom-right-radius: var(--custom-border-radius);\n}\n\n/* Form improvements */\n.form-control:focus,\n.form-select:focus {\n    border-color: var(--bs-primary);\n    box-shadow: 0 0 0 0.2rem rgba(var(--bs-primary-rgb), 0.25);\n}\n\n.form-label {\n    font-weight: 500;\n    margin-bottom: 0.5rem;\n}\n\n/* Alert enhancements */\n.alert {\n    border: none;\n    border-radius: var(--custom-border-radius);\n}\n\n.alert .fas,\n.alert .far {\n    width: 1.25rem;\n    text-align: center;\n}\n\n/* Jumbotron style for hero sections */\n.jumbotron {\n    background: linear-gradient(135deg, var(--bs-primary), var(--bs-info));\n    border-radius: var(--custom-border-radius);\n}\n\n/* Pagination improvements */\n.pagination .page-link {\n    border-radius: var(--custom-border-radius);\n    margin: 0 0.125rem;\n    border: 1px solid var(--bs-border-color);\n}\n\n.pagination .page-item.active .page-link {\n    background-color: var(--bs-primary);\n    border-color: var(--bs-primary);\n}\n\n/* Statistics cards */\n.card.text-white .card-footer a:hover {\n    text-decoration: underline !important;\n}\n\n/* Accordion improvements */\n.accordion-button:not(.collapsed) {\n    background-color: rgba(var(--bs-primary-rgb), 0.1);\n    color: var(--bs-primary);\n}\n\n.accordion-button:focus {\n    box-shadow: 0 0 0 0.2rem rgba(var(--bs-primary-rgb), 0.25);\n}\n\n/* File upload area */\n.form-control[type=\"file\"] {\n    padding: 0.75rem;\n}\n\n/* DataTables integration */\n.dataTables_wrapper .dataTables_length select,\n.dataTables_wrapper .dataTables_filter input {\n    background-color: var(--bs-body-bg);\n    border: 1px solid var(--bs-border-color);\n    color: var(--bs-body-color);\n}\n\n.dataTables_wrapper .dataTables_paginate .paginate_button {\n    border: 1px solid var(--bs-border-color);\n    background-color: var(--bs-body-bg);\n    color: var(--bs-body-color) !important;\n}\n\n.dataTables_wrapper .dataTables_paginate .paginate_button:hover {\n    background-color: var(--bs-secondary-bg);\n    border-color: var(--bs-border-color);\n}\n\n.dataTables_wrapper .dataTables_paginate .paginate_button.current {\n    background-color: var(--bs-primary) !important;\n    border-color: var(--bs-primary) !important;\n    color: white !important;\n}\n\n/* Loading states */\n.loading {\n    opacity: 0.6;\n    pointer-events: none;\n}\n\n.spinner-border-sm {\n    width: 1rem;\n    height: 1rem;\n}\n\n/* Mobile responsiveness improvements */\n@media (max-width: 768px) {\n    .d-md-flex {\n        flex-direction: column;\n        gap: 0.5rem;\n    }\n    \n    .btn-group {\n        flex-direction: column;\n    }\n    \n    .btn-group .btn {\n        border-radius: var(--custom-border-radius) !important;\n        margin-bottom: 0.25rem;\n    }\n    \n    .card-body .row > div {\n        margin-bottom: 1rem;\n    }\n}\n\n/* Print styles */\n@media print {\n    .navbar,\n    .btn,\n    .pagination,\n    .card-footer,\n    .alert,\n    .badge {\n        display: none !important;\n    }\n    \n    .card {\n        border: 1px solid #000;\n        box-shadow: none;\n    }\n    \n    .table {\n        font-size: 0.8rem;\n    }\n}\n\n/* Custom utility classes */\n.text-truncate-2 {\n    display: -webkit-box;\n    -webkit-line-clamp: 2;\n    -webkit-box-orient: vertical;\n    overflow: hidden;\n}\n\n.border-start-primary {\n    border-left: 0.25rem solid var(--bs-primary) !important;\n}\n\n.border-start-success {\n    border-left: 0.25rem solid var(--bs-success) !important;\n}\n\n.border-start-warning {\n    border-left: 0.25rem solid var(--bs-warning) !important;\n}\n\n.border-start-danger {\n    border-left: 0.25rem solid var(--bs-danger) !important;\n}\n\n/* Animation utilities */\n@keyframes fadeIn {\n    from { opacity: 0; transform: translateY(10px); }\n    to { opacity: 1; transform: translateY(0); }\n}\n\n.fade-in {\n    animation: fadeIn 0.3s ease-in-out;\n}\n\n/* Focus improvements for accessibility */\n.btn:focus,\n.form-control:focus,\n.form-select:focus {\n    outline: 2px solid var(--bs-primary);\n    outline-offset: 2px;\n}\n\n/* High contrast mode support */\n@media (prefers-contrast: high) {\n    .card {\n        border-width: 2px;\n    }\n    \n    .btn {\n        border-width: 2px;\n    }\n}\n\n/* Reduced motion support */\n@media (prefers-reduced-motion: reduce) {\n    * {\n        animation-duration: 0.01ms !important;\n        animation-iteration-count: 1 !important;\n        transition-duration: 0.01ms !important;\n    }\n}\n","size_bytes":5794},"static/js/main.js":{"content":"// Main JavaScript file for Financial Data Management System\n// Enhanced functionality for better user experience\n\n$(document).ready(function() {\n    // Initialize all components\n    initializeComponents();\n    initializeDataTables();\n    initializeFormValidation();\n    initializeTooltips();\n    initializeFileUpload();\n    initializeSearchEnhancements();\n});\n\n// Initialize main components\nfunction initializeComponents() {\n    // Add fade-in animation to cards\n    $('.card').addClass('fade-in');\n    \n    // Enhanced dropdown behaviors\n    $('.dropdown-toggle').on('click', function(e) {\n        e.stopPropagation();\n    });\n    \n    // Auto-dismiss alerts after 5 seconds\n    setTimeout(function() {\n        $('.alert').alert('close');\n    }, 5000);\n    \n    // Smooth scrolling for anchor links\n    $('a[href^=\"#\"]').on('click', function(e) {\n        e.preventDefault();\n        const target = $($(this).attr('href'));\n        if (target.length) {\n            $('html, body').animate({\n                scrollTop: target.offset().top - 70\n            }, 500);\n        }\n    });\n}\n\n// Initialize DataTables with enhanced features\nfunction initializeDataTables() {\n    // Common DataTable configuration\n    const commonConfig = {\n        responsive: true,\n        pageLength: 25,\n        lengthMenu: [[10, 25, 50, 100, -1], [10, 25, 50, 100, \"All\"]],\n        language: {\n            search: \"Search records:\",\n            lengthMenu: \"Show _MENU_ records per page\",\n            info: \"Showing _START_ to _END_ of _TOTAL_ records\",\n            infoEmpty: \"No records available\",\n            infoFiltered: \"(filtered from _MAX_ total records)\",\n            paginate: {\n                first: \"First\",\n                last: \"Last\",\n                next: \"Next\",\n                previous: \"Previous\"\n            }\n        },\n        dom: '<\"row\"<\"col-sm-12 col-md-6\"l><\"col-sm-12 col-md-6\"f>>' +\n             '<\"row\"<\"col-sm-12\"tr>>' +\n             '<\"row\"<\"col-sm-12 col-md-5\"i><\"col-sm-12 col-md-7\"p>>',\n        initComplete: function() {\n            // Add Bootstrap styling to DataTables elements\n            $('.dataTables_length select').addClass('form-select form-select-sm');\n            $('.dataTables_filter input').addClass('form-control form-control-sm');\n        }\n    };\n    \n    // Initialize specific tables\n    if ($('#movementsTable').length) {\n        $('#movementsTable').DataTable({\n            ...commonConfig,\n            order: [[7, \"desc\"]], // Sort by uploaded date\n            columnDefs: [\n                { targets: [-1], orderable: false }, // Disable sorting on actions column\n                { targets: [4], type: 'currency' }, // Currency sorting for net value\n            ]\n        });\n    }\n    \n    if ($('#usersTable').length) {\n        $('#usersTable').DataTable({\n            ...commonConfig,\n            order: [[3, \"desc\"]], // Sort by created date\n            columnDefs: [\n                { targets: [-1], orderable: false }, // Disable sorting on actions column\n            ]\n        });\n    }\n    \n    if ($('#uploadsTable').length) {\n        $('#uploadsTable').DataTable({\n            ...commonConfig,\n            order: [[5, \"desc\"]], // Sort by upload date\n            columnDefs: [\n                { targets: [-1], orderable: false }, // Disable sorting on actions column\n            ]\n        });\n    }\n}\n\n// Initialize form validation\nfunction initializeFormValidation() {\n    // Real-time validation for forms\n    $('form').on('submit', function(e) {\n        const form = $(this);\n        let isValid = true;\n        \n        // Check required fields\n        form.find('[required]').each(function() {\n            const field = $(this);\n            if (!field.val().trim()) {\n                field.addClass('is-invalid');\n                isValid = false;\n            } else {\n                field.removeClass('is-invalid');\n            }\n        });\n        \n        // Email validation\n        form.find('input[type=\"email\"]').each(function() {\n            const email = $(this);\n            const emailPattern = /^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/;\n            if (email.val() && !emailPattern.test(email.val())) {\n                email.addClass('is-invalid');\n                isValid = false;\n            }\n        });\n        \n        // Password confirmation\n        const password = form.find('input[name=\"password\"]');\n        const password2 = form.find('input[name=\"password2\"]');\n        if (password.length && password2.length) {\n            if (password.val() !== password2.val()) {\n                password2.addClass('is-invalid');\n                isValid = false;\n            }\n        }\n        \n        if (!isValid) {\n            e.preventDefault();\n            showNotification('Please correct the highlighted fields', 'error');\n        }\n    });\n    \n    // Remove validation classes on input\n    $('input, select, textarea').on('input change', function() {\n        $(this).removeClass('is-invalid');\n    });\n}\n\n// Initialize tooltips\nfunction initializeTooltips() {\n    // Enable Bootstrap tooltips\n    const tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle=\"tooltip\"]'));\n    tooltipTriggerList.map(function(tooltipTriggerEl) {\n        return new bootstrap.Tooltip(tooltipTriggerEl);\n    });\n    \n    // Add tooltips to truncated text\n    $('.text-truncate, .text-truncate-2').each(function() {\n        const element = $(this);\n        if (element[0].scrollWidth > element[0].clientWidth) {\n            element.attr('data-bs-toggle', 'tooltip');\n            element.attr('title', element.text());\n            new bootstrap.Tooltip(element[0]);\n        }\n    });\n}\n\n// Initialize file upload enhancements\nfunction initializeFileUpload() {\n    $('input[type=\"file\"]').on('change', function() {\n        const file = this.files[0];\n        const fileInfo = $(this).siblings('.file-info');\n        \n        if (file) {\n            const size = (file.size / (1024 * 1024)).toFixed(2);\n            const info = `Selected: ${file.name} (${size} MB)`;\n            \n            if (fileInfo.length) {\n                fileInfo.text(info);\n            } else {\n                $(this).after(`<div class=\"file-info text-muted mt-1\">${info}</div>`);\n            }\n            \n            // Validate file size (50MB limit)\n            if (file.size > 50 * 1024 * 1024) {\n                showNotification('File size exceeds 50MB limit', 'error');\n                $(this).val('');\n                $(this).siblings('.file-info').remove();\n            }\n        } else {\n            fileInfo.remove();\n        }\n    });\n    \n    // Drag and drop enhancement\n    $('.form-control[type=\"file\"]').each(function() {\n        const fileInput = $(this);\n        const dropZone = $('<div class=\"drop-zone border-2 border-dashed p-4 text-center rounded mt-2\">' +\n                          '<i class=\"fas fa-cloud-upload-alt fa-2x text-muted\"></i><br>' +\n                          '<span class=\"text-muted\">Drag and drop files here or click to browse</span>' +\n                          '</div>');\n        \n        fileInput.after(dropZone);\n        fileInput.hide();\n        \n        dropZone.on('click', function() {\n            fileInput.click();\n        });\n        \n        dropZone.on('dragover', function(e) {\n            e.preventDefault();\n            $(this).addClass('border-primary bg-primary bg-opacity-10');\n        });\n        \n        dropZone.on('dragleave', function() {\n            $(this).removeClass('border-primary bg-primary bg-opacity-10');\n        });\n        \n        dropZone.on('drop', function(e) {\n            e.preventDefault();\n            $(this).removeClass('border-primary bg-primary bg-opacity-10');\n            \n            const files = e.originalEvent.dataTransfer.files;\n            if (files.length > 0) {\n                fileInput[0].files = files;\n                fileInput.trigger('change');\n            }\n        });\n    });\n}\n\n// Initialize search enhancements\nfunction initializeSearchEnhancements() {\n    // Auto-complete for search fields\n    $('input[name=\"search\"]').on('input', function() {\n        const query = $(this).val();\n        if (query.length >= 2) {\n            // Debounce search suggestions\n            clearTimeout(this.searchTimeout);\n            this.searchTimeout = setTimeout(() => {\n                // You can implement search suggestions here\n                console.log('Search suggestions for:', query);\n            }, 300);\n        }\n    });\n    \n    // Advanced search toggle\n    $('.advanced-search-toggle').on('click', function() {\n        $('.advanced-search-panel').slideToggle();\n    });\n    \n    // Search history (localStorage)\n    const searchHistory = JSON.parse(localStorage.getItem('searchHistory') || '[]');\n    \n    $('form').on('submit', function() {\n        const searchInput = $(this).find('input[name=\"search\"]');\n        if (searchInput.length && searchInput.val().trim()) {\n            const query = searchInput.val().trim();\n            if (!searchHistory.includes(query)) {\n                searchHistory.unshift(query);\n                searchHistory.splice(10); // Keep only last 10 searches\n                localStorage.setItem('searchHistory', JSON.stringify(searchHistory));\n            }\n        }\n    });\n}\n\n// Utility functions\nfunction showNotification(message, type = 'info') {\n    const alertClass = type === 'error' ? 'alert-danger' : `alert-${type}`;\n    const icon = type === 'error' ? 'fas fa-exclamation-triangle' : \n                 type === 'success' ? 'fas fa-check-circle' : 'fas fa-info-circle';\n    \n    const alert = $(`\n        <div class=\"alert ${alertClass} alert-dismissible fade show\" role=\"alert\">\n            <i class=\"${icon} me-2\"></i>\n            ${message}\n            <button type=\"button\" class=\"btn-close\" data-bs-dismiss=\"alert\"></button>\n        </div>\n    `);\n    \n    // Insert at top of main container\n    $('main.container').prepend(alert);\n    \n    // Auto-dismiss after 5 seconds\n    setTimeout(() => {\n        alert.alert('close');\n    }, 5000);\n}\n\nfunction formatCurrency(amount, currency = 'R$') {\n    return `${currency} ${parseFloat(amount).toFixed(2)}`;\n}\n\nfunction formatDate(dateString) {\n    if (!dateString) return '-';\n    const date = new Date(dateString);\n    return date.toLocaleDateString('pt-BR');\n}\n\nfunction formatDateTime(dateString) {\n    if (!dateString) return '-';\n    const date = new Date(dateString);\n    return date.toLocaleString('pt-BR');\n}\n\n// Export functionality\nfunction exportTableToCSV(tableId, filename = 'export.csv') {\n    const table = document.getElementById(tableId);\n    if (!table) return;\n    \n    const rows = Array.from(table.querySelectorAll('tr'));\n    const csv = rows.map(row => {\n        const cells = Array.from(row.querySelectorAll('td, th'));\n        return cells.slice(0, -1).map(cell => { // Exclude actions column\n            let text = cell.textContent.trim();\n            // Handle commas and quotes in CSV\n            if (text.includes(',') || text.includes('\"')) {\n                text = '\"' + text.replace(/\"/g, '\"\"') + '\"';\n            }\n            return text;\n        }).join(',');\n    }).join('\\n');\n    \n    // Download CSV\n    const blob = new Blob([csv], { type: 'text/csv' });\n    const url = window.URL.createObjectURL(blob);\n    const a = document.createElement('a');\n    a.href = url;\n    a.download = filename;\n    a.style.display = 'none';\n    document.body.appendChild(a);\n    a.click();\n    document.body.removeChild(a);\n    window.URL.revokeObjectURL(url);\n}\n\n// JSON data formatting for display\nfunction formatJSONField(data, fieldName) {\n    if (!data || data.length === 0) {\n        return '<span class=\"text-muted\">No data</span>';\n    }\n    \n    if (Array.isArray(data)) {\n        return `<span class=\"badge bg-info\">${data.length} items</span>`;\n    }\n    \n    return '<span class=\"badge bg-secondary\">Object</span>';\n}\n\n// Performance monitoring\nfunction measurePerformance(name, fn) {\n    const start = performance.now();\n    const result = fn();\n    const end = performance.now();\n    console.log(`${name} took ${end - start} milliseconds`);\n    return result;\n}\n\n// Error handling\nwindow.addEventListener('error', function(e) {\n    console.error('JavaScript error:', e.error);\n    showNotification('An unexpected error occurred. Please refresh the page.', 'error');\n});\n\n// Keyboard shortcuts\ndocument.addEventListener('keydown', function(e) {\n    // Ctrl+/ or Cmd+/ for search focus\n    if ((e.ctrlKey || e.metaKey) && e.key === '/') {\n        e.preventDefault();\n        const searchInput = document.querySelector('input[name=\"search\"]');\n        if (searchInput) {\n            searchInput.focus();\n        }\n    }\n    \n    // Escape to clear search\n    if (e.key === 'Escape') {\n        const searchInput = document.querySelector('input[name=\"search\"]:focus');\n        if (searchInput) {\n            searchInput.value = '';\n            searchInput.blur();\n        }\n    }\n});\n\n// Mobile enhancements\nif (window.innerWidth <= 768) {\n    // Mobile-specific enhancements\n    $(document).ready(function() {\n        // Collapse long text on mobile\n        $('.table td').each(function() {\n            const text = $(this).text();\n            if (text.length > 30) {\n                $(this).attr('title', text);\n                $(this).text(text.substring(0, 30) + '...');\n            }\n        });\n        \n        // Mobile navigation improvements\n        $('.navbar-toggler').on('click', function() {\n            $('body').toggleClass('nav-open');\n        });\n    });\n}\n\n// Progressive Web App features\nif ('serviceWorker' in navigator) {\n    window.addEventListener('load', function() {\n        // Service worker registration would go here\n        console.log('Service Worker support detected');\n    });\n}\n","size_bytes":13803}}}

----- FILE: attached_assets/Pasted--2025-08-18-14-39-30-0000-1014-INFO-Booting-worker-with-pid-1014-INFO-file-processor-Read-Exc-1755528124031_1755528124031.txt  (sha1:70c05f5acbba, bytes:3072) -----
[2025-08-18 14:39:30 +0000] [1014] [INFO] Booting worker with pid: 1014
INFO:file_processor:Read Excel file with 50 rows
ERROR:app:Exception on /movements/50 [GET]
Traceback (most recent call last):
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 1511, in wsgi_app
    response = self.full_dispatch_request()
               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 919, in full_dispatch_request
    rv = self.handle_user_exception(e)
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 917, in full_dispatch_request
    rv = self.dispatch_request()
         ^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 902, in dispatch_request
    return self.ensure_sync(self.view_functions[rule.endpoint])(**view_args)  # type: ignore[no-any-return]
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask_login/utils.py", line 290, in decorated_view
    return current_app.ensure_sync(func)(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/routes.py", line 77, in movement_detail
    return render_template('movements/detail.html', movement=movement)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/templating.py", line 150, in render_template
    return _render(app, template, context)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/templating.py", line 131, in _render
    rv = template.render(context)
         ^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/environment.py", line 1295, in render
    self.environment.handle_exception()
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/environment.py", line 942, in handle_exception
    raise rewrite_traceback_stack(source=source)
  File "/home/runner/workspace/templates/movements/detail.html", line 1, in top-level template code
    {% extends "base.html" %}
  File "/home/runner/workspace/templates/base.html", line 126, in top-level template code
    {% block content %}{% endblock %}
    ^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/templates/movements/detail.html", line 248, in block 'content'
    Item {{ loop.index }} - Product ID: {{ item.get('productId', 'N/A') }}
    ^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/utils.py", line 92, in from_obj
    if hasattr(obj, "jinja_pass_arg"):
       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
jinja2.exceptions.UndefinedError: 'str object' has no attribute 'get'
[2025-08-18 14:41:44 +0000] [1003] [INFO] Handling signal: winch

----- FILE: attached_assets/Pasted--Empresa-Code-Empresa-Nome-Filial-Code-Filial-Nome-Filial-Nome-Descricao-internalId-companyId-moveme-1755539692719_1755539692719.txt  (sha1:76af6f6cefeb, bytes:2078) -----

Empresa_Code	Empresa_Nome	Filial_Code	Filial_Nome	Filial_Nome_Descricao	internalId	companyId	movementId	branchId	warehouseCode	destinyWarehouseCode	number	series	movementTypeCode	type	status	printed	documentPrinted	billPrinted	registerDate	exitDate	commercialRepresentativeCharge	grossValue	netValue	informedNetValue	otherValues	discountPercentage	expensePercentage	expenseValue	extraPercentage1	extraValue1	extraPercentage2	extraValue2	transportedProductNetWeight	transportedProductGrossWeight	classificationTable5Code	financialOptionalTable2Code	netValueCurrencyCode	date	hasGeneratedBill	auxCustomerVendorCode	auxCustomerVendorCompanyId	costCenterCode	salesman1Code	chargePercentage	salesman2ChargePercentage	salesman3ChargePercentage	salesman4ChargePercentage	userCode	destinyBranchId	lotGenerated	accountingExportStatus	deliveryDate	hasGeneratedWorkAccount	workAccountGenerated	lastEditTime	indicateObjectUse	bonumIntegrated	processedFlag	icmsDeductionValue	creationUser	creationDate	emailStatus	internalGrossValue	otherCompanyINSSBaseValue	conditionalDiscountValue	conditionalExpenseValue	affectStockOrder	commercialAutomationExported	aplicationIntegration	entryDate	extemporaneous	merchandiseValue	usesFinancialValueApportionment	conclusionFlag	paradigmaStatus	paradigmaAutoIntegrated	originalGrossValue	originalNetValue	originalOtherValues	operationId	scpBranchId	movementItems	payments	costCenterApportionments	departmentApportionments	taxes	fiscal	norm	cargoComponent	thirdPartyNF	safetyDevice	nfe	inputCTRC	outputCTRC	ctrc	transportData	documentAuthorization	judicialProcess	serviceOrder	relatedMovement	exportRelatedMovement	linkedMovement	cTe	eaiIntegration	electronicInvoiceFreeFields	customerVendorCode	paymentTermCode	observation	financialOptionalTable1Code	financialEntryMovementId	generatedEntryNumber	openEntryNumber	cashAccountCode	customerVendorCompanyId	fluxusGroupedFlag	cashAccountCompanyId	longHistory
  Essas sao as colunas que estou subindo. E toda base tem os registros em json. 

Consegue verificar o que pode ta acontecendo com os campos json?

----- FILE: attached_assets/Pasted--Empresa-Nome-Filial-Code-Filial-Nome-Filial-Nome-Descricao-internalId-companyId-movementId-branchI-1756245969666_1756245969666.txt  (sha1:0bb5aaddaf2b, bytes:5641) -----
\	Empresa_Nome	Filial_Code	Filial_Nome	Filial_Nome_Descricao	internalId	companyId	movementId	branchId	warehouseCode	destinyWarehouseCode	customerVendorCode	number	series	movementTypeCode	type	status	printed	documentPrinted	billPrinted	registerDate	exitDate	commercialRepresentativeCharge	paymentTermCode	grossValue	netValue	informedNetValue	otherValues	observation	discountPercentage	expensePercentage	expenseValue	extraPercentage1	extraValue1	extraPercentage2	extraValue2	transportedProductNetWeight	transportedProductGrossWeight	financialOptionalTable1Code	financialEntryMovementId	netValueCurrencyCode	date	generatedEntryNumber	hasGeneratedBill	openEntryNumber	auxCustomerVendorCode	auxCustomerVendorCompanyId	cashAccountCode	chargePercentage	salesman2ChargePercentage	salesman3ChargePercentage	salesman4ChargePercentage	customerVendorCompanyId	userCode	destinyBranchId	fluxusGroupedFlag	cashAccountCompanyId	lotGenerated	accountingExportStatus	hasGeneratedWorkAccount	workAccountGenerated	lastEditTime	indicateObjectUse	bonumIntegrated	processedFlag	icmsDeductionValue	creationUser	creationDate	emailStatus	internalGrossValue	otherCompanyINSSBaseValue	conditionalDiscountValue	conditionalExpenseValue	affectStockOrder	commercialAutomationExported	aplicationIntegration	entryDate	extemporaneous	merchandiseValue	usesFinancialValueApportionment	conclusionFlag	paradigmaStatus	paradigmaAutoIntegrated	originalGrossValue	originalNetValue	originalOtherValues	operationId	scpBranchId	movementItems	payments	costCenterApportionments	departmentApportionments	taxes	fiscal	norm	cargoComponent	thirdPartyNF	safetyDevice	nfe	inputCTRC	outputCTRC	ctrc	transportData	documentAuthorization	judicialProcess	serviceOrder	relatedMovement	exportRelatedMovement	linkedMovement	cTe	eaiIntegration	electronicInvoiceFreeFields
4	SA CAVALCANTE COMESTIVEIS LTDA	17	16 - BOB´S LARANJEIRAS	16 - SA CAVALCANTE COMESTIVEIS LTDA	1|138	1	138	1	01	01	F00227	001408	U	1.2.02	P	F	FALSE	FALSE	FALSE	2003-08-06T00:00:00-03:00	2003-08-11T00:00:00-03:00	0	30	232	232	0	232	PROGRAMA SOL	0	0	0	0	0	0	0	0	0	02	138	R$	2003-08-06T00:00:00-03:00	1	FALSE	1	F00227	0	02	0	0	0	0	0	cmufumba	1	0	1	FALSE	0	FALSE	FALSE	2003-08-11T09:08:41-03:00	0	FALSE	FALSE	0	cmufumba	2003-08-11T00:00:00-03:00	FALSE	232	0	0	0	138	0	T	2003-08-11T00:00:00-03:00	0	0	FALSE	0	N	FALSE	0	0	0	0	0	[{'companyId': 1, 'movementId': 138, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 90, 'quantity': 4.0, 'unitPrice': 58.0, 'tablePrice': 0.0, 'discountValue': 0.0, 'registerDate': '2003-08-06T00:00:00-03:00', 'measureUnitCode': 'GL', 'receivableQuantity': 4.0, 'partialBillingReceivedValue': 0.0, 'completedValue': 0.0, 'stockEffectFlag': 1, 'unitValue': 58.0, 'financialValue': 232.0, 'ordinationTaxAliquot': 0.0, 'originalQuantity': 4.0, 'flag': 0, 'blockObject': 0, 'rebillingFlag': 0, 'measureUnitConversionRate': 0.0, 'totalValue': 232.0, 'branchId': 1, 'sortedQuantity': 0.0, 'commercialRepresentativeChargePercentage': 0.0, 'fiscalBookkeepingValue': 0.0, 'orderFinancialValue': 0.0, 'ctrcFreightValue': 0.0, 'optionalFormulaValue1': 0.0, 'optionalFormulaValue2': 0.0, 'editedPrice': False, 'unitaryOccupiedVolumeNumber': 1, 'editedTotalPrice': 0, 'conditionalDiscountValue': 0.0, 'conditionalExpenseValue': 0.0, 'budgetDate': '2014-12-04T00:00:00-03:00', 'freightApportionment': 0.0, 'insuranceApportionment': 0.0, 'discountApportionment': 0.0, 'expenseApportionment': 0.0, 'extraValue1Apportionment': 0.0, 'extraValue2Apportionment': 0.0, 'ctrcFreightApportionment': 0.0, 'materialDeductionApportionment': 0.0, 'subcontractingDeductionApportionment': 0.0, 'otherDeductionApportionment': 0.0, 'budgetUnitPrice': 0.0, 'nFeServiceValue': 0.0, 'warehouseCode': '01', 'heritageValue': 0.0, 'heritageBaseDepreciationValue': 0.0, 'netValue': 232.0, 'grossValue': 0.0, 'originalGrossValue': 0.0, 'costCenterDepartmentApportionmentValue': 0.0, 'replenishmentCost': 0.0, 'bReplenishmentCost': 0.0, 'thirdPartyFinancialValue': 0.0, 'totalQuantity': 4.0, 'isSubstituteProduct': 0, 'unitPriceSelectionType': 0, 'financialValueApportionment': 0.0, 'completedQuantity': 0.0, 'aplicationIntegration': 'T', 'productFantasyName': 'GALVEST BRANCO 3600 ML', 'productCode': '01.01.0007', 'productReducedCode': '90', 'manufacturerProductNumber': '01.01.0007', 'manufacturerCode': '00001', 'complementaryFields': {}, 'costCenterApportionments': [], 'departmentApportionments': [], 'taxes': [{'companyId': 1, 'movementId': 138, 'movementItemSequentialId': 1, 'taxId': 'ICMS', 'calculationBasis': 232.0, 'aliquot': 17.0, 'value': 39.44, 'calculatedCalculationBasis': 232.0, 'edited': 0, 'fullBase': 0.0}, {'companyId': 1, 'movementId': 138, 'movementItemSequentialId': 1, 'taxId': 'IPI', 'calculationBasis': 232.0, 'aliquot': 0.0, 'value': 0.0, 'calculatedCalculationBasis': 232.0, 'edited': 0, 'fullBase': 0.0}], 'itemLots': [], 'itemGrids': [], 'itemSerialNumber': [], 'itemHeritage': [], 'siscoServFitting': [], 'fiscal': [], 'reserves': [], 'relatedItem': [], 'exportRelatedItem': [], 'linkedItem': [], 'exportMemo': [], 'judicialProcess': [], 'electronicInvoiceFreeFields': []}]	[]	[{'companyId': 1, 'apportionmentId': 7, 'movementId': 138, 'costCenterCode': '1.4.008', 'value': 232.0, 'costCenterName': 'EXPANSÃO SPC'}]	[{'companyId': 1, 'movementId': 138, 'branchId': 1, 'departmentCode': '10', 'value': 232.0, 'departmentName': 'OBRAS'}]	[{'companyId': 1, 'movementId': 138, 'movementItemSequentialId': 0, 'taxId': 'PIS', 'calculationBasis': 232.0, 'aliquot': 1.65, 'value': 3.83, 'calculatedCalculationBasis': 232.0, 'edited': 0, 'fullBase': 0.0}]	[]	[]	[]	[]	[]	[]	[]	[]	[]	[]	[]	[]	[]	[]	[]	[]	[]	[]	[]


----- FILE: attached_assets/Pasted--Eu-preciso-que-voc-me-ajude-a-criar-uma-aplica-o-em-flask-aonde-vamos-ter-uma-parte-de-admin--1755527301524_1755527301525.txt  (sha1:abfd0809e324, bytes:13253) -----





Eu preciso que você me ajude a criar uma aplicação em flask aonde vamos ter uma parte de admin, aonde o admin vai poder cadastrar novos usuários. 

Alem disso, o Admin vai poder subir documentos em lotes. Esse documento será um xlsx estruturado com essas colunas na sequencia. Vou te mandar o nome da coluna e o formato que vai vir o dado:   internalId - 
1|91
1|122
1|123

companyId - 
1
1
1
movementId -
91
122
123
branchId -
1
1
1
warehouseCode - 
01
01
01
destinyWarehouseCode -
01
01
01
number -
000002
128652
128635
series - 
OR
U
U
movementTypeCode -
5.1.02
1.2.02
1.2.02
type -
S
P
P
status -
N
F
F
printed -
FALSO
FALSO
FALSO
documentPrinted -
FALSO
FALSO
FALSO
billPrinted -
FALSO
FALSO
FALSO
registerDate -

2003-08-01T00:00:00-03:00
2003-08-04T00:00:00-03:00
2003-08-04T00:00:00-03:00
exitDate-
2003-08-01T00:00:00-03:00
2003-08-07T00:00:00-03:00
2003-08-07T00:00:00-03:00
commercialRepresentativeCharge -
0
0
0
grossValue -
500
570
1710
netValue - 
500
627
1881
informedNetValue -
0
0
0
otherValues - 
500
570
1710
discountPercentage -
0
0
0
expensePercentage -
0
0
0
expenseValue -
0
0
0
extraPercentage1 -
0
0
0
extraValue1 -
0
0
0
extraPercentage2 -
0
0
0
extraValue2 -
0
0
0
transportedProductNetWeight -
0
0
0
transportedProductGrossWeight -
0
0
0
classificationTable5Code -
12305016


financialOptionalTable2Code -
1.2.3.05.016


netValueCurrencyCode -
R$
R$
R$
date - 
2003-08-01T00:00:00-03:00
2003-08-04T00:00:00-03:00
2003-08-04T00:00:00-03:00
hasGeneratedBill -
FALSO
FALSO
FALSO
auxCustomerVendorCode -
CXXXXXXXXXX
F00007
F00007
auxCustomerVendorCompanyId -
0
0
0
costCenterCode -
1.2.020


salesman1Code -
salesman1Code
00001


chargePercentage -
0
0
0
salesman2ChargePercentage -
0
0
0
salesman3ChargePercentage -
0
0
0
salesman4ChargePercentage - 
0
0
0
userCode -
mestre
mestre
mestre
destinyBranchId -
1
1
1
lotGenerated -
FALSO
FALSO
FALSO
accountingExportStatus -
0
0
0
deliveryDate -
2003-08-01T00:00:00-03:00


hasGeneratedWorkAccount -
FALSO
FALSO
FALSO
workAccountGenerated -
FALSO
FALSO
FALSO
lastEditTime -
2003-08-01T09:19:24-03:00
2003-08-08T15:34:41-03:00
2003-08-08T15:33:33-03:00
indicateObjectUse -
0
0
0
bonumIntegrated -
FALSO
FALSO
FALSO
processedFlag -
FALSO
FALSO
FALSO
icmsDeductionValue -
0
0
0
creationUser -
mestre
mestre
mestre
creationDate -
2003-08-01T00:00:00-03:00
2003-08-07T00:00:00-03:00
2003-08-07T00:00:00-03:00
emailStatus -
FALSO
FALSO
FALSO
internalGrossValue -
500
570
1710
otherCompanyINSSBaseValue -
0
0
0
conditionalDiscountValue -
0
0
0
conditionalExpenseValue -
0
0
0
affectStockOrder -
91
122
123
commercialAutomationExported -
0
0
0
aplicationIntegration -
T
T
T
entryDate -
2003-08-01T00:00:00-03:00
2003-08-07T00:00:00-03:00
2003-08-07T00:00:00-03:00
extemporaneous -
0
0
0
merchandiseValue -
0
0
0
usesFinancialValueApportionment -
FALSO
FALSO
FALSO
conclusionFlag -
0
0
0
paradigmaStatus -
N
N
N
paradigmaAutoIntegrated -
FALSO
FALSO
FALSO
originalGrossValue -
0
0
0
originalNetValue -
0
0
0
originalOtherValues - 
0
0
0
operationId - 
0
0
0
scpBranchId -
0
0
0
movementItems - 

[{'companyId': 1, 'movementId': 91, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 812, 'quantity': 1.0, 'unitPrice': 500.0, 'tablePrice': 0.0, 'discountValue': 0.0, 'registerDate': '2003-08-01T00:00:00-03:00', 'classificationTable5Code': '12305016', 'financialOptionalTable2Code': '1.2.3.05.016', 'measureUnitCode': 'vb', 'receivableQuantity': 1.0, 'deliveryDate': '2003-08-01T00:00:00-03:00', 'partialBillingReceivedValue': 0.0, 'completedValue': 0.0, 'unitValue': 0.0, 'financialValue': 0.0, 'ordinationTaxAliquot': 0.0, 'originalQuantity': 1.0, 'flag': 0, 'blockObject': 0, 'rebillingFlag': 0, 'measureUnitConversionRate': 0.0, 'totalValue': 500.0, 'branchId': 1, 'sortedQuantity': 0.0, 'commercialRepresentativeChargePercentage': 0.0, 'fiscalBookkeepingValue': 0.0, 'orderFinancialValue': 0.0, 'ctrcFreightValue': 0.0, 'optionalFormulaValue1': 0.0, 'optionalFormulaValue2': 0.0, 'editedPrice': False, 'unitaryOccupiedVolumeNumber': 1, 'editedTotalPrice': 0, 'conditionalDiscountValue': 0.0, 'conditionalExpenseValue': 0.0, 'budgetDate': '2014-12-04T00:00:00-03:00', 'freightApportionment': 0.0, 'insuranceApportionment': 0.0, 'discountApportionment': 0.0, 'expenseApportionment': 0.0, 'extraValue1Apportionment': 0.0, 'extraValue2Apportionment': 0.0, 'ctrcFreightApportionment': 0.0, 'materialDeductionApportionment': 0.0, 'subcontractingDeductionApportionment': 0.0, 'otherDeductionApportionment': 0.0, 'budgetUnitPrice': 0.0, 'nFeServiceValue': 0.0, 'warehouseCode': '01', 'heritageValue': 0.0, 'heritageBaseDepreciationValue': 0.0, 'netValue': 500.0, 'grossValue': 0.0, 'originalGrossValue': 0.0, 'costCenterDepartmentApportionmentValue': 0.0, 'replenishmentCost': 0.0, 'bReplenishmentCost': 0.0, 'thirdPartyFinancialValue': 0.0, 'totalQuantity': 1.0, 'isSubstituteProduct': 0, 'unitPriceSelectionType': 0, 'financialValueApportionment': 0.0, 'completedQuantity': 0.0, 'aplicationIntegration': 'T', 'productFantasyName': 'SERVICOS EM GERAL', 'productCode': '99.01.0001', 'productReducedCode': '812', 'manufacturerProductNumber': '99.01.0000', 'manufacturerCode': '00001', 'complementaryFields': {}, 'costCenterApportionments': [], 'departmentApportionments': [], 'taxes': [], 'itemLots': [], 'itemGrids': [], 'itemSerialNumber': [], 'itemHeritage': [], 'siscoServFitting': [], 'fiscal': [], 'reserves': [], 'relatedItem': [], 'exportRelatedItem': [], 'linkedItem': [], 'exportMemo': [], 'judicialProcess': [], 'electronicInvoiceFreeFields': []}]
[{'companyId': 1, 'movementId': 122, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 146, 'quantity': 100.0, 'unitPrice': 5.7, 'tablePrice': 0.0, 'discountValue': 0.0, 'registerDate': '2003-08-04T00:00:00-03:00', 'measureUnitCode': 'SC', 'receivableQuantity': 100.0, 'partialBillingReceivedValue': 0.0, 'completedValue': 0.0, 'stockEffectFlag': 1, 'unitValue': 6.27, 'financialValue': 627.0, 'ordinationTaxAliquot': 0.0, 'originalQuantity': 100.0, 'flag': 0, 'blockObject': 0, 'rebillingFlag': 0, 'measureUnitConversionRate': 0.0, 'totalValue': 570.0, 'branchId': 1, 'sortedQuantity': 0.0, 'commercialRepresentativeChargePercentage': 0.0, 'fiscalBookkeepingValue': 0.0, 'orderFinancialValue': 0.0, 'ctrcFreightValue': 0.0, 'optionalFormulaValue1': 0.0, 'optionalFormulaValue2': 0.0, 'editedPrice': False, 'unitaryOccupiedVolumeNumber': 1, 'editedTotalPrice': 0, 'conditionalDiscountValue': 0.0, 'conditionalExpenseValue': 0.0, 'budgetDate': '2014-12-04T00:00:00-03:00', 'freightApportionment': 0.0, 'insuranceApportionment': 0.0, 'discountApportionment': 0.0, 'expenseApportionment': 0.0, 'extraValue1Apportionment': 0.0, 'extraValue2Apportionment': 0.0, 'ctrcFreightApportionment': 0.0, 'materialDeductionApportionment': 0.0, 'subcontractingDeductionApportionment': 0.0, 'otherDeductionApportionment': 0.0, 'budgetUnitPrice': 0.0, 'nFeServiceValue': 0.0, 'warehouseCode': '01', 'heritageValue': 0.0, 'heritageBaseDepreciationValue': 0.0, 'netValue': 570.0, 'grossValue': 0.0, 'originalGrossValue': 0.0, 'costCenterDepartmentApportionmentValue': 0.0, 'replenishmentCost': 0.0, 'bReplenishmentCost': 0.0, 'thirdPartyFinancialValue': 0.0, 'totalQuantity': 100.0, 'isSubstituteProduct': 0, 'unitPriceSelectionType': 0, 'financialValueApportionment': 0.0, 'completedQuantity': 0.0, 'aplicationIntegration': 'T', 'productFantasyName': 'ARGAMASSA TRADICIONAL', 'productCode': '01.14.0002', 'productReducedCode': '146', 'manufacturerProductNumber': '01.14.0002', 'manufacturerCode': '00001', 'complementaryFields': {}, 'costCenterApportionments': [], 'departmentApportionments': [], 'taxes': [{'companyId': 1, 'movementId': 122, 'movementItemSequentialId': 1, 'taxId': 'ICMS', 'calculationBasis': 627.0, 'aliquot': 0.0, 'value': 0.0, 'calculatedCalculationBasis': 627.0, 'edited': 0, 'fullBase': 0.0}, {'companyId': 1, 'movementId': 122, 'movementItemSequentialId': 1, 'taxId': 'IPI', 'calculationBasis': 570.0, 'aliquot': 10.0, 'value': 57.0, 'calculatedCalculationBasis': 570.0, 'edited': 0, 'fullBase': 0.0}], 'itemLots': [], 'itemGrids': [], 'itemSerialNumber': [], 'itemHeritage': [], 'siscoServFitting': [], 'fiscal': [], 'reserves': [], 'relatedItem': [], 'exportRelatedItem': [], 'linkedItem': [], 'exportMemo': [], 'judicialProcess': [], 'electronicInvoiceFreeFields': []}]
[{'companyId': 1, 'movementId': 123, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 146, 'quantity': 300.0, 'unitPrice': 5.7, 'tablePrice': 0.0, 'discountValue': 0.0, 'registerDate': '2003-08-04T00:00:00-03:00', 'measureUnitCode': 'SC', 'receivableQuantity': 300.0, 'partialBillingReceivedValue': 0.0, 'completedValue': 0.0, 'stockEffectFlag': 1, 'unitValue': 6.27, 'financialValue': 1881.0, 'ordinationTaxAliquot': 0.0, 'originalQuantity': 300.0, 'flag': 0, 'blockObject': 0, 'rebillingFlag': 0, 'measureUnitConversionRate': 0.0, 'totalValue': 1710.0, 'branchId': 1, 'sortedQuantity': 0.0, 'commercialRepresentativeChargePercentage': 0.0, 'fiscalBookkeepingValue': 0.0, 'orderFinancialValue': 0.0, 'ctrcFreightValue': 0.0, 'optionalFormulaValue1': 0.0, 'optionalFormulaValue2': 0.0, 'editedPrice': False, 'unitaryOccupiedVolumeNumber': 1, 'editedTotalPrice': 0, 'conditionalDiscountValue': 0.0, 'conditionalExpenseValue': 0.0, 'budgetDate': '2014-12-04T00:00:00-03:00', 'freightApportionment': 0.0, 'insuranceApportionment': 0.0, 'discountApportionment': 0.0, 'expenseApportionment': 0.0, 'extraValue1Apportionment': 0.0, 'extraValue2Apportionment': 0.0, 'ctrcFreightApportionment': 0.0, 'materialDeductionApportionment': 0.0, 'subcontractingDeductionApportionment': 0.0, 'otherDeductionApportionment': 0.0, 'budgetUnitPrice': 0.0, 'nFeServiceValue': 0.0, 'warehouseCode': '01', 'heritageValue': 0.0, 'heritageBaseDepreciationValue': 0.0, 'netValue': 1710.0, 'grossValue': 0.0, 'originalGrossValue': 0.0, 'costCenterDepartmentApportionmentValue': 0.0, 'replenishmentCost': 0.0, 'bReplenishmentCost': 0.0, 'thirdPartyFinancialValue': 0.0, 'totalQuantity': 300.0, 'isSubstituteProduct': 0, 'unitPriceSelectionType': 0, 'financialValueApportionment': 0.0, 'completedQuantity': 0.0, 'aplicationIntegration': 'T', 'productFantasyName': 'ARGAMASSA TRADICIONAL', 'productCode': '01.14.0002', 'productReducedCode': '146', 'manufacturerProductNumber': '01.14.0002', 'manufacturerCode': '00001', 'complementaryFields': {}, 'costCenterApportionments': [], 'departmentApportionments': [], 'taxes': [{'companyId': 1, 'movementId': 123, 'movementItemSequentialId': 1, 'taxId': 'ICMS', 'calculationBasis': 1881.0, 'aliquot': 0.0, 'value': 0.0, 'calculatedCalculationBasis': 1881.0, 'edited': 0, 'fullBase': 0.0}, {'companyId': 1, 'movementId': 123, 'movementItemSequentialId': 1, 'taxId': 'IPI', 'calculationBasis': 1710.0, 'aliquot': 10.0, 'value': 171.0, 'calculatedCalculationBasis': 1710.0, 'edited': 0, 'fullBase': 0.0}], 'itemLots': [], 'itemGrids': [], 'itemSerialNumber': [], 'itemHeritage': [], 'siscoServFitting': [], 'fiscal': [], 'reserves': [], 'relatedItem': [], 'exportRelatedItem': [], 'linkedItem': [], 'exportMemo': [], 'judicialProcess': [], 'electronicInvoiceFreeFields': []}]
payments -
[]
[]
[]
costCenterApportionments -

[]
[{'companyId': 1, 'apportionmentId': 1, 'movementId': 122, 'costCenterCode': '1.4.008', 'value': 627.0, 'costCenterName': 'EXPANSÃO SPC'}]
[{'companyId': 1, 'apportionmentId': 2, 'movementId': 123, 'costCenterCode': '1.4.008', 'value': 1881.0, 'costCenterName': 'EXPANSÃO SPC'}]
departmentApportionments -
[]
[{'companyId': 1, 'movementId': 122, 'branchId': 1, 'departmentCode': '10', 'value': 627.0, 'departmentName': 'OBRAS'}]
[{'companyId': 1, 'movementId': 123, 'branchId': 1, 'departmentCode': '10', 'value': 1881.0, 'departmentName': 'OBRAS'}]
taxes -
[]
[{'companyId': 1, 'movementId': 122, 'movementItemSequentialId': 0, 'taxId': 'PIS', 'calculationBasis': 627.0, 'aliquot': 1.65, 'value': 10.35, 'calculatedCalculationBasis': 627.0, 'edited': 0, 'fullBase': 0.0}]
[{'companyId': 1, 'movementId': 123, 'movementItemSequentialId': 0, 'taxId': 'PIS', 'calculationBasis': 1881.0, 'aliquot': 1.65, 'value': 31.04, 'calculatedCalculationBasis': 1881.0, 'edited': 0, 'fullBase': 0.0}]
fiscal -
[]
[]
[]
norm -
[]
[]
[]
cargoComponent -
[]
[]
[]
thirdPartyNF -
[]
[]
[]
safetyDevice -
[]
[]
[]
nfe -
[]
[]
[]
inputCTRC -
[]
[]
[]
outputCTRC -
[]
[]
[]
ctrc -
[]
[]
[]
transportData -
[]
[]
[]
documentAuthorization -
[]
[]
[]
judicialProcess -
[]
[]
[]
serviceOrder -
[]
[]
[]
relatedMovement -
[]
[]
[]
exportRelatedMovement -
[]
[]
[]
linkedMovement -
[]
[]
[]
cTe -
[]
[]
[]
eaiIntegration -
[]
[]
[]
electronicInvoiceFreeFields -
[]
[]
[]
customerVendorCode -

F00007
F00007
paymentTermCode -

45
45
observation -


CANTRATO ARCO IRIS
contraro arco iris
financialOptionalTable1Code - 

02
02
financialEntryMovementId -

122
123
generatedEntryNumber -

1
1
openEntryNumber -

1
1
cashAccountCode -

02
02
customerVendorCompanyId -

0
0
fluxusGroupedFlag -

0
0
cashAccountCompanyId -

1
1
longHistory -




O usuário final vai poder ver no detalhe o que tiver na base que o admin vai subir. Consegue me ajudar a criar essa aplicação de forma completa? Veja as colunas aonde temos json estruturados, para exibirmos as informações formatadas corretamente pro usuário. 

 








	


----- FILE: attached_assets/Pasted--coding-utf-8-import-re-import-sys-import-os-import-json-import-time-from-time-impor-1757089815255_1757089815257.txt  (sha1:fcda1dd549d4, bytes:28366) -----
# -*- coding: utf-8 -*-
import re
import sys
import os
import json
import time
from time import perf_counter
from time import time as _now
from datetime import datetime
from typing import List, Tuple, Dict, Any, Optional

import requests
from requests.auth import HTTPBasicAuth
from requests.adapters import HTTPAdapter
from urllib3.util.retry import Retry

import pandas as pd
from pandas import json_normalize
from dateutil import tz, parser as dtparser
import argparse

# python teste_andrey.py --inicio 2025-01-01 --fim 2025-01-02 --empresa 4 --filial 17 --arquivo movimentos_4_17.xlsx

# ==============================
# CONFIG DE CONEXÃO
# ==============================
HOST = "http://192.168.18.9:8051"
USER = "INTEGRA_INOVAI"
PWD  = "INOVAI.LAB"
auth = HTTPBasicAuth(USER, PWD)

MOV_ENDPOINT = "/api/mov/v1/movements"

# ==============================
# AJUSTES DE REDE / TOLERÂNCIA
# ==============================
PAGE_SIZE_CHAIN         = [25, 10, 5, 1]   # degradar até funcionar
MAX_LIST_PAGES          = 100000
MAX_PAGES_PER_BRANCH    = 999999

CONNECT_TIMEOUT         = 10
READ_TIMEOUT            = 45
ADAPTER_RETRIES         = 3
MANUAL_RETRIES          = 2
BACKOFF_BASE            = 1.0

BRANCH_DEADLINE_SEC     = 90
SLEEP_BETWEEN           = 0.05
PAGE_ATTEMPT_PAUSE      = 0.2
DEBUG_VERBOSE           = True

# Campos de data comuns no RM (no seu ambiente 'date' já funcionou)
DATE_FIELD_HINTS = [
    "date","issueDate","emissionDate","movementDate","creationDate","createdDate",
    "entryDate","exitDate","lastEditTime","registrationDate",
    "Date","IssueDate","EmissionDate","MovementDate","CreationDate","CreatedDate",
    "EntryDate","ExitDate","LastEditTime","RegistrationDate"
]
COMPANY_KEYS = ["companyId","CompanyId","companyCode","CompanyCode"]
BRANCH_KEYS  = ["branchId","BranchId","branchCode","BranchCode"]

# ==============================
# INFRA HTTP
# ==============================
def _make_session() -> requests.Session:
    retry = Retry(
        total=ADAPTER_RETRIES, connect=ADAPTER_RETRIES, read=ADAPTER_RETRIES,
        backoff_factor=0.4, status_forcelist=[502,503,504],
        allowed_methods=["GET","HEAD"], raise_on_status=False,
    )
    adapter = HTTPAdapter(max_retries=retry, pool_connections=10, pool_maxsize=20)
    s = requests.Session()
    s.auth = auth
    s.headers.update({
        "Accept": "application/json",
        "Connection": "close",
        "Accept-Encoding": "identity"
    })
    s.mount("http://", adapter); s.mount("https://", adapter)
    return s

def _safe_request(s: requests.Session, method: str, url: str, **kwargs) -> Tuple[Optional[requests.Response], Optional[Exception], float]:
    kwargs.setdefault("timeout", (CONNECT_TIMEOUT, READ_TIMEOUT))
    last_exc = None
    for attempt in range(1, MANUAL_RETRIES + 1):
        t0 = perf_counter()
        try:
            r = s.request(method, url, **kwargs)
            elapsed = (perf_counter() - t0) * 1000
            if DEBUG_VERBOSE:
                print(f"      ↪ {method} {url}  ({elapsed:.0f} ms)")
                if "params" in kwargs and kwargs["params"]:
                    print(f"        params: {kwargs['params']}")
                print(f"        → HTTP {r.status_code}")
            return r, None, elapsed
        except (requests.exceptions.ReadTimeout, requests.exceptions.ConnectionError) as e:
            elapsed = (perf_counter() - t0) * 1000
            last_exc = e
            sleep_s = BACKOFF_BASE * (2 ** (attempt - 1))
            print(f"      ⚠️ {type(e).__name__} {attempt}/{MANUAL_RETRIES} ({elapsed:.0f} ms). Backoff {sleep_s:.1f}s…")
            time.sleep(sleep_s)
        except Exception as e:
            elapsed = (perf_counter() - t0) * 1000
            if DEBUG_VERBOSE:
                print(f"      ❌ Exception inesperada ({elapsed:.0f} ms): {type(e).__name__}: {e}")
            return None, e, elapsed
    return None, last_exc, 0.0

# ==============================
# UTILS
# ==============================
def _norm_code(x: str) -> str:
    s = str(x).strip()
    return str(int(s)) if s.isdigit() else s

def _detect_field_present(obj: Dict[str,Any], candidates: List[str]) -> Optional[str]:
    lower_map = {k.lower(): k for k in obj.keys()}
    for cand in candidates:
        if cand.lower() in lower_map:
            return lower_map[cand.lower()]
    for k in obj.keys():
        if any(re.search(fr"\b{re.escape(cand)}\b", k, flags=re.IGNORECASE) for cand in candidates):
            return k
    return None

def _build_company_branch_filter(company_field: Optional[str], branch_field: Optional[str], comp_code: str, branch_code: str) -> Optional[str]:
    parts=[]
    if company_field:
        val = _norm_code(comp_code)
        parts.append(f"{company_field} eq {val if val.isdigit() else repr(val)}")
    if branch_field and str(branch_code).strip() != "":
        bc = str(branch_code).strip()
        parts.append(f"{branch_field} eq {bc if bc.isdigit() else repr(bc)}")
    return " and ".join(parts) if parts else None

def _date_iso(dt: datetime) -> str:
    return dt.isoformat()

def _guess_datetime_fields_from_sample(items: List[Dict[str,Any]]) -> List[str]:
    found=set()
    for it in items:
        for k,v in it.items():
            if isinstance(v,str):
                try:
                    dtparser.isoparse(v); found.add(k)
                except Exception:
                    pass
    ordered = [f for f in DATE_FIELD_HINTS if f in found]
    for f in sorted(found):
        if f not in ordered:
            ordered.append(f)
    return ordered

def _coerce_datetime_cols(df: pd.DataFrame) -> pd.DataFrame:
    candidates = [
        "date","Date","movementDate","MovementDate","issueDate","IssueDate",
        "emissionDate","EmissionDate","creationDate","CreatedDate","entryDate","exitDate","lastEditTime"
    ]
    for c in candidates:
        if c in df.columns:
            df[c] = pd.to_datetime(df[c], errors="coerce", utc=False)
    return df

def _strip_timezone_for_excel(df: pd.DataFrame, target_tz: str = "America/Sao_Paulo") -> pd.DataFrame:
    """Excel não aceita timezone; converte para horário local e remove tz."""
    from pandas.api.types import is_datetime64_any_dtype
    for col in df.columns:
        if is_datetime64_any_dtype(df[col]):
            tzinfo = getattr(df[col].dtype, "tz", None)
            if tzinfo is not None:
                try:
                    df[col] = df[col].dt.tz_convert(target_tz).dt.tz_localize(None)
                except Exception:
                    df[col] = df[col].dt.tz_localize(None)
    return df

def _jsonify_non_scalars_for_excel(df: pd.DataFrame) -> pd.DataFrame:
    """Transforma listas/dicts em JSON string para o Excel aceitar."""
    def to_excel_scalar(x):
        if isinstance(x, (list, dict, tuple, set)):
            try:
                return json.dumps(x, ensure_ascii=False)
            except Exception:
                return str(x)
        return x
    for c in df.columns:
        df[c] = df[c].apply(to_excel_scalar)
    return df

def _first_existing(cols: list[str], df_cols: set[str]) -> Optional[str]:
    for c in cols:
        if c in df_cols: return c
    return None

def _smart_sort(df: pd.DataFrame, ordenar_cols: List[str]) -> pd.DataFrame:
    cols = [c for c in ordenar_cols if c in df.columns]
    if not cols:
        maybe_date = _first_existing(["date","Date","movementDate","MovementDate","issueDate","IssueDate"], set(df.columns))
        maybe_id   = _first_existing(["internalId","InternalId","movementId","MovementId"], set(df.columns))
        cols = [c for c in [maybe_date, maybe_id] if c]
    if cols:
        return df.sort_values(cols, kind="stable")
    return df

def _autosize_openpyxl(ws):
    """Ajusta larguras de colunas numa worksheet openpyxl."""
    from openpyxl.utils import get_column_letter
    dims = {}
    for row in ws.rows:
        for cell in row:
            if cell.value is None: continue
            val = str(cell.value)
            dims[cell.column] = max(dims.get(cell.column, 0), len(val))
    for col, width in dims.items():
        ws.column_dimensions[get_column_letter(col)].width = min(max(width + 2, 10), 60)

# ==============================
# CATÁLOGO
# ==============================
def get_company_name(s: requests.Session, company_code: str) -> str:
    code = _norm_code(company_code)
    urls = [
        f"{HOST}/api/framework/v1/companies/{code}",
        f"{HOST}/api/framework/v1/Companies/{code}",
        f"{HOST}/api/corp/v1/companies/{code}",
    ]
    keys = ["name","title","description","CompanyName","Nome","Title","Description"]
    for url in urls:
        r, exc, _ = _safe_request(s, "GET", url)
        if exc or not r: continue
        if r.status_code == 200:
            try: j = r.json()
            except Exception: continue
            if isinstance(j, dict):
                for k in keys:
                    v = j.get(k) or (j.get("company", {}) if isinstance(j.get("company"), dict) else {}).get(k)
                    if v: return str(v).strip()
    return ""

def list_branches(s: requests.Session, company_code: str) -> List[Dict[str,str]]:
    c = _norm_code(company_code)
    branches = []
    r, exc, _ = _safe_request(s, "GET", f"{HOST}/api/framework/v1/Branches", params={"companyCode": c})
    if exc or not r: return branches
    if r.status_code == 200:
        try: j = r.json()
        except Exception: return branches
        data = j.get("items", j) if isinstance(j, dict) else j
        if isinstance(data, list):
            for obj in data:
                if str(obj.get("CompanyCode")) == c:
                    branches.append({
                        "CompanyCode": str(obj.get("CompanyCode","")),
                        "Code":        str(obj.get("Code","")),
                        "Title":       str(obj.get("Title","") or ""),
                        "Description": str(obj.get("Description","") or ""),
                    })
    seen=set(); uniq=[]
    for b in branches:
        key=(b["CompanyCode"], b["Code"])
        if key not in seen:
            seen.add(key); uniq.append(b)
    return uniq

# ==============================
# MOVEMENTS
# ==============================
def _get_movements_page(
    s: requests.Session,
    page: int,
    page_size: int,
    odata_filter: Optional[str] = None,
) -> Tuple[List[Dict[str,Any]], int, Dict[str,Any]]:
    url = f"{HOST}{MOV_ENDPOINT}"
    params = {"page": max(1,page), "pageSize": max(1,page_size)}
    if odata_filter: params["$filter"] = odata_filter
    meta = {"had_items_key": None, "raw_len": None, "status": None, "body_preview": None, "page_size": page_size}
    r, exc, _ = _safe_request(s, "GET", url, params=params)
    if not r:
        meta["status"] = -1
        meta["body_preview"] = f"{type(exc).__name__}: {exc}" if exc else None
        return [], -1, meta
    meta["status"] = r.status_code
    try: meta["body_preview"] = (r.text or "")[:300].replace("\n"," ")
    except Exception: meta["body_preview"] = None
    items=[]
    if 200 <= r.status_code < 300 or r.status_code == 206:
        try: j = r.json()
        except Exception: return [], r.status_code, meta
        if isinstance(j, dict):
            meta["had_items_key"] = "items" if "items" in j else ("Items" if "Items" in j else None)
            data = j.get("items") or j.get("Items")
            if isinstance(data, list): items = data; meta["raw_len"] = len(items)
            elif isinstance(j, list): items = j; meta["raw_len"] = len(items)
            else: items = [j]; meta["raw_len"] = 1
        elif isinstance(j, list):
            items = j; meta["raw_len"] = len(j)
    return items, r.status_code, meta

def _try_date_filter_server_side(
    s: requests.Session,
    company_field: Optional[str],
    branch_field: Optional[str],
    comp_code: str,
    branch_code: str,
    dt_ini: datetime,
    dt_fim: datetime,
    sample_date_candidates: List[str]
) -> Tuple[Optional[str], Optional[str]]:
    base_filter = _build_company_branch_filter(company_field, branch_field, comp_code, branch_code)
    candidates = list(dict.fromkeys(DATE_FIELD_HINTS + sample_date_candidates))
    for df in candidates:
        date_part = f"{df} ge {_date_iso(dt_ini)} and {df} le {_date_iso(dt_fim)}"
        odata = f"{base_filter} and {date_part}" if base_filter else date_part
        items, st, _ = _get_movements_page(s, page=1, page_size=1, odata_filter=odata)
        if st == 200:
            return odata, df
    return None, None

# ==============================
# MAIN (EXTRAÇÃO + EXCEL EM ÁRVORE)
# ==============================
def run_extract(args):
    TZBR = tz.gettz("America/Sao_Paulo")
    dt_ini = datetime.strptime(args.inicio, "%Y-%m-%d").replace(hour=0, minute=0, second=0, tzinfo=TZBR)
    dt_fim = datetime.strptime(args.fim, "%Y-%m-%d").replace(hour=23, minute=59, second=59, tzinfo=TZBR)

    s = _make_session()

    print(">>> Iniciando extração de movements…")
    print(f"Período: {dt_ini.isoformat()}  até  {dt_fim.isoformat()}")
    print(f"Endpoint: {HOST}{MOV_ENDPOINT}")

    target_companies = [args.empresa] if args.empresa else ["4", "148"]
    all_rows: List[Dict[str,Any]] = []
    labels: List[Tuple[str,str,str]] = []  # (companyId, branchId, branchLabel)

    for comp in target_companies:
        comp_norm = _norm_code(comp)
        comp_name = get_company_name(s, comp_norm)
        print("\n" + "="*72)
        print(f"🏢 Empresa {comp_norm} — {comp_name or '(sem nome)'}")
        print("="*72)

        branches = list_branches(s, comp_norm)
        print(f"  📚 Filiais detectadas: {len(branches)}")

        if args.filial:
            branches = [b for b in branches if str(b.get("Code","")).strip() == str(args.filial).strip()]
            print(f"  🔎 Filtro de filial: {args.filial} → {len(branches)} filial(is)")

        if not branches:
            print("  ⚠️ Nenhuma filial encontrada após filtro. Pulando empresa.")
            continue

        # probe mínimo
        probe_items, status, _ = _get_movements_page(s, page=1, page_size=5, odata_filter=None)
        if status >= 400 or status == -1 or not probe_items:
            probe_items2, status2, meta2 = _get_movements_page(s, page=1, page_size=1, odata_filter=None)
            if status2 >= 400 or status2 == -1 or not probe_items2:
                print(f"  ❌ Probe /movements falhou (HTTP {status2}). Body: {meta2.get('body_preview') if isinstance(meta2, dict) else None}")
                continue
            probe_items = probe_items2

        probe_row = probe_items[0] if probe_items else {}
        comp_field = _detect_field_present(probe_row, COMPANY_KEYS) or "companyId"
        br_field   = _detect_field_present(probe_row, BRANCH_KEYS)  or "branchId"
        date_candidates = _guess_datetime_fields_from_sample(probe_items)
        print(f"  🔎 Campos /movements → company='{comp_field}', branch='{br_field}'")

        for idx, b in enumerate(branches, start=1):
            b_code = b.get("Code","")
            b_tag  = (b.get("Title") or b.get("Description") or "(sem nome)").strip()
            header = f"  → Filial {idx}/{len(branches)}: código {b_code or '(vazio)'} — {b_tag}"
            print("\n" + header)
            print("  " + "—"*len(header))

            labels.append((str(comp_norm), str(b_code), b_tag))

            server_odata, used_date_field = _try_date_filter_server_side(
                s, comp_field, br_field, comp_norm, b_code, dt_ini, dt_fim, date_candidates
            )
            if server_odata:
                print(f"    ✅ Filtro server-side aceito: {server_odata}")
            else:
                server_odata = _build_company_branch_filter(comp_field, br_field, comp_norm, b_code)
                print("    ⚠️ Sem campo de data aceito no servidor. Período será filtrado no cliente.")

            # paginação
            page = 1
            empty_seq = 0
            ps_index = 0
            started_at = _now()

            while page <= min(MAX_LIST_PAGES, MAX_PAGES_PER_BRANCH):
                if _now() - started_at > BRANCH_DEADLINE_SEC:
                    print(f"      ⏱️ Tempo excedido ({BRANCH_DEADLINE_SEC}s) nesta filial — pulando.")
                    break

                page_size = PAGE_SIZE_CHAIN[ps_index]
                items, st, _ = _get_movements_page(s, page=page, page_size=page_size, odata_filter=server_odata)

                if st == -1 or st >= 500:
                    if ps_index < len(PAGE_SIZE_CHAIN) - 1:
                        ps_index += 1
                        print(f"      ⚠️ st={st} — reduzindo pageSize para {PAGE_SIZE_CHAIN[ps_index]} e repetindo página {page}…")
                        time.sleep(PAGE_ATTEMPT_PAUSE)
                        continue
                    # último recurso: sem $filter e filtro local
                    print("      ⚠️ Falha persistente — tentando sem $filter e filtrando localmente…")
                    items2, st2, _ = _get_movements_page(s, page=page, page_size=page_size, odata_filter=None)
                    if st2 >= 400 or st2 == -1:
                        print(f"      ⚠️ HTTP {st2} mesmo sem filtro — encerrando varredura desta filial.")
                        break
                    items = items2
                    if comp_field:
                        items = [it for it in items if str(it.get(comp_field)) == _norm_code(comp_norm)]
                    if br_field and str(b_code).strip() != "":
                        items = [it for it in items if str(it.get(br_field, "")).strip() == str(b_code).strip()]
                    if not used_date_field:
                        # SUBSTITUA este bloco pela versão abaixo
                        def in_period_local(rec: Dict[str, Any]) -> bool:
                            # tenta campo 'date' primeiro
                            v = rec.get("date")
                            if isinstance(v, str):
                                try:
                                    d = dtparser.isoparse(v)
                                    return dt_ini <= d <= dt_fim
                                except Exception:
                                    pass
                            # tenta demais candidatos
                            for key in (date_candidates or []) + DATE_FIELD_HINTS:
                                v = rec.get(key)
                                if isinstance(v, str):
                                    try:
                                        d = dtparser.isoparse(v)
                                        if dt_ini <= d <= dt_fim:
                                            return True
                                    except Exception:
                                        pass
                            return False

                        items = [it for it in items if in_period_local(it)]

                elif st >= 400:
                    print(f"      ⚠️ HTTP {st} — pulando página.")
                    break

                if not items:
                    empty_seq += 1
                    print(f"    · Página {page}: 0 itens (vazias seguidas={empty_seq}) | ps={page_size}")
                    if empty_seq >= 2:
                        print("      ↳ Duas páginas vazias — fim desta filial.")
                        break
                    page += 1
                    time.sleep(SLEEP_BETWEEN)
                    continue

                if not used_date_field:
                    # SUBSTITUA este bloco pela versão abaixo
                    def in_period_client(rec: Dict[str, Any]) -> bool:
                        # tenta campo 'date' primeiro
                        v = rec.get("date")
                        if isinstance(v, str):
                            try:
                                d = dtparser.isoparse(v)
                                return dt_ini <= d <= dt_fim
                            except Exception:
                                pass
                        # tenta demais candidatos
                        for key in (date_candidates or []) + DATE_FIELD_HINTS:
                            v = rec.get(key)
                            if isinstance(v, str):
                                try:
                                    d = dtparser.isoparse(v)
                                    if dt_ini <= d <= dt_fim:
                                        return True
                                except Exception:
                                    pass
                        return False

                    items = [it for it in items if in_period_client(it)]

                all_rows.extend(items)

                print(f"    · Página {page}: {len(items)} itens  | ps={page_size}")
                if len(items) < page_size:
                    print("      ↳ Última página (len(items) < pageSize).")
                    break
                page += 1
                time.sleep(SLEEP_BETWEEN)

    # ==============================
    # CONSOLIDAÇÃO
    # ==============================
    print("\n>>> Consolidando em DataFrame…")
    if not all_rows:
        print("(Sem registros coletados.)")
        return

    df = json_normalize(all_rows, sep=".")
    df = _coerce_datetime_cols(df)
    df = _strip_timezone_for_excel(df)

    # ordenar
    ordenar_cols = [c.strip() for c in (args.ordenar or "").split(",") if c.strip()]
    df = _smart_sort(df, ordenar_cols)

    # resumo por empresa/filial
    branch_col  = _first_existing(["branchId","BranchId","branchCode","BranchCode"], set(df.columns))
    company_col = _first_existing(["companyId","CompanyId","companyCode","CompanyCode"], set(df.columns))
    resumo = None
    if branch_col:
        grp_cols = [c for c in [company_col, branch_col] if c]
        resumo = df.groupby(grp_cols, dropna=False).size().reset_index(name="qtd").sort_values("qtd", ascending=False)
        resumo = _strip_timezone_for_excel(resumo)

    # ==============================
    # XLSX EM “ÁRVORE” + DADOS COMPLETOS
    # ==============================
    base = args.arquivo
    if not base:
        emp = f"emp{args.empresa}" if args.empresa else "empVARIAS"
        fil = f"_fil{args.filial}" if args.filial else ""
        base = f"movements_{emp}{fil}_{args.inicio}_a_{args.fim}.xlsx"
    if not base.lower().endswith(".xlsx"):
        base += ".xlsx"

    from openpyxl import Workbook
    from openpyxl.styles import Font, PatternFill

    wb = Workbook()
    ws_tree = wb.active
    ws_tree.title = "Árvore"
    ws_data = wb.create_sheet("Dados")

    # —— folha Dados (dataset completo tipo DataFrame)
    df_excel = _jsonify_non_scalars_for_excel(df.copy())   # <—— torna listas/dicts em JSON
    ws_data.append(list(df_excel.columns))
    for row in df_excel.itertuples(index=False, name=None):
        ws_data.append(list(row))
    try:
        _autosize_openpyxl(ws_data)
    except Exception:
        pass

    # —— folha Árvore (Empresa → Filial → Movimentos)
    bold = Font(bold=True)
    header_fill = PatternFill(start_color="FFECECEC", end_color="FFECECEC", fill_type="solid")
    company_fill = PatternFill(start_color="FFCCE5FF", end_color="FFCCE5FF", fill_type="solid")
    branch_fill  = PatternFill(start_color="FFFFF2CC", end_color="FFFFF2CC", fill_type="solid")

    # colunas a mostrar na árvore
    tree_cols = [c.strip() for c in (args.colunas or "").split(",") if c.strip()]
    if not tree_cols:
        pref = ["date","internalId","movementId","documentNumber","type","grossValue","netValue","partner.name","status"]
        tree_cols = [c for c in pref if c in df.columns] or list(df.columns[:10])

    # índice rápido por empresa/filial
    comp_series = df[company_col] if company_col in df.columns else pd.Series([""]*len(df))
    br_series   = df[branch_col]  if branch_col in df.columns else pd.Series([""]*len(df))
    unique_pairs = pd.DataFrame({"comp": comp_series, "branch": br_series}).drop_duplicates()

    # map de label de filial (montado durante varredura)
    filial_label_map: Dict[Tuple[str,str], str] = {}
    # (opcional: se quiser, podemos popular labels a partir de /Branches e usar aqui)

    # escrever em “árvore”
    for comp_id in sorted(unique_pairs["comp"].astype(str).unique(), key=lambda x: (x is None, x)):
        comp_mask = (comp_series.astype(str) == str(comp_id))
        df_comp = df[comp_mask]

        ws_tree.append([f"Empresa: {comp_id}"])
        for cell in ws_tree[ws_tree.max_row]:
            cell.font = bold; cell.fill = company_fill

        filiais_comp = sorted(df_comp[branch_col].astype(str).unique()) if branch_col in df_comp.columns else [""]
        for br_id in filiais_comp:
            br_mask = (br_series.astype(str) == str(br_id))
            df_fil = df_comp[br_mask] if branch_col in df_comp.columns else df_comp

            br_name = filial_label_map.get((str(comp_id), str(br_id)), "")
            ws_tree.append([f"  Filial: {br_id} {('- ' + br_name) if br_name else ''}"])
            for cell in ws_tree[ws_tree.max_row]:
                cell.font = bold; cell.fill = branch_fill

            # Header das colunas
            ws_tree.append([""] + tree_cols)
            for cell in ws_tree[ws_tree.max_row]:
                cell.font = bold; cell.fill = header_fill

            # Linhas dos movimentos (sanitizando listas/dicts)
            df_tree = df_fil[tree_cols].copy()
            df_tree = _jsonify_non_scalars_for_excel(df_tree)
            for _, rec in df_tree.iterrows():
                ws_tree.append([""] + [rec.get(c) for c in tree_cols])

            ws_tree.append([])  # linha em branco entre filiais

    try:
        _autosize_openpyxl(ws_tree)
    except Exception:
        pass

    # aba Resumo
    if resumo is not None:
        ws_resumo = wb.create_sheet("Resumo")
        resumo_excel = _jsonify_non_scalars_for_excel(resumo.copy())
        ws_resumo.append(list(resumo_excel.columns))
        for row in resumo_excel.itertuples(index=False, name=None):
            ws_resumo.append(list(row))
        try:
            _autosize_openpyxl(ws_resumo)
        except Exception:
            pass

    # salvar
    try:
        wb.save(base)
    except Exception as e:
        alt = os.path.splitext(base)[0] + ".csv"
        print(f"⚠️ Falha ao salvar XLSX ({e}). Salvando CSV com dados completos: {alt}")
        df_excel.to_csv(alt, index=False)
        base = alt

    print(f"\n✅ Arquivo gerado: {os.path.abspath(base)}")

def parse_args():
    ap = argparse.ArgumentParser(description="Extrai Movements e gera XLSX em árvore + dados completos.")
    ap.add_argument("--inicio",   required=True, help="Data inicial (YYYY-MM-DD).")
    ap.add_argument("--fim",      required=True, help="Data final (YYYY-MM-DD).")
    ap.add_argument("--empresa",  required=False, help="companyId (coligada), ex.: 4")
    ap.add_argument("--filial",   required=False, help="branchId (filial), ex.: 17")
    ap.add_argument("--ordenar",  required=False, default="date,internalId", help="Colunas para ordenar (vírgula).")
    ap.add_argument("--colunas",  required=False, default="", help="Colunas para mostrar na aba Árvore (vírgula).")
    ap.add_argument("--arquivo",  required=False, default="", help="Caminho do .xlsx de saída.")
    return ap.parse_args()

if __name__ == "__main__":
    args = parse_args()
    try:
        run_extract(args)
    except KeyboardInterrupt:
        print("\nInterrompido pelo usuário.")
        sys.exit(130)
    except Exception as e:
        print(f"\nERRO fatal: {e}")
        sys.exit(2)


----- FILE: attached_assets/Pasted--companyId-1-movementId-91-sequentialId-1-sequentialNumber-1-productId-812-qu-1755531544491_1755531544492.txt  (sha1:e610fe28c588, bytes:8037) -----

[{'companyId': 1, 'movementId': 91, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 812, 'quantity': 1.0, 'unitPrice': 500.0, 'tablePrice': 0.0, 'discountValue': 0.0, 'registerDate': '2003-08-01T00:00:00-03:00', 'classificationTable5Code': '12305016', 'financialOptionalTable2Code': '1.2.3.05.016', 'measureUnitCode': 'vb', 'receivableQuantity': 1.0, 'deliveryDate': '2003-08-01T00:00:00-03:00', 'partialBillingReceivedValue': 0.0, 'completedValue': 0.0, 'unitValue': 0.0, 'financialValue': 0.0, 'ordinationTaxAliquot': 0.0, 'originalQuantity': 1.0, 'flag': 0, 'blockObject': 0, 'rebillingFlag': 0, 'measureUnitConversionRate': 0.0, 'totalValue': 500.0, 'branchId': 1, 'sortedQuantity': 0.0, 'commercialRepresentativeChargePercentage': 0.0, 'fiscalBookkeepingValue': 0.0, 'orderFinancialValue': 0.0, 'ctrcFreightValue': 0.0, 'optionalFormulaValue1': 0.0, 'optionalFormulaValue2': 0.0, 'editedPrice': False, 'unitaryOccupiedVolumeNumber': 1, 'editedTotalPrice': 0, 'conditionalDiscountValue': 0.0, 'conditionalExpenseValue': 0.0, 'budgetDate': '2014-12-04T00:00:00-03:00', 'freightApportionment': 0.0, 'insuranceApportionment': 0.0, 'discountApportionment': 0.0, 'expenseApportionment': 0.0, 'extraValue1Apportionment': 0.0, 'extraValue2Apportionment': 0.0, 'ctrcFreightApportionment': 0.0, 'materialDeductionApportionment': 0.0, 'subcontractingDeductionApportionment': 0.0, 'otherDeductionApportionment': 0.0, 'budgetUnitPrice': 0.0, 'nFeServiceValue': 0.0, 'warehouseCode': '01', 'heritageValue': 0.0, 'heritageBaseDepreciationValue': 0.0, 'netValue': 500.0, 'grossValue': 0.0, 'originalGrossValue': 0.0, 'costCenterDepartmentApportionmentValue': 0.0, 'replenishmentCost': 0.0, 'bReplenishmentCost': 0.0, 'thirdPartyFinancialValue': 0.0, 'totalQuantity': 1.0, 'isSubstituteProduct': 0, 'unitPriceSelectionType': 0, 'financialValueApportionment': 0.0, 'completedQuantity': 0.0, 'aplicationIntegration': 'T', 'productFantasyName': 'SERVICOS EM GERAL', 'productCode': '99.01.0001', 'productReducedCode': '812', 'manufacturerProductNumber': '99.01.0000', 'manufacturerCode': '00001', 'complementaryFields': {}, 'costCenterApportionments': [], 'departmentApportionments': [], 'taxes': [], 'itemLots': [], 'itemGrids': [], 'itemSerialNumber': [], 'itemHeritage': [], 'siscoServFitting': [], 'fiscal': [], 'reserves': [], 'relatedItem': [], 'exportRelatedItem': [], 'linkedItem': [], 'exportMemo': [], 'judicialProcess': [], 'electronicInvoiceFreeFields': []}]
[{'companyId': 1, 'movementId': 122, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 146, 'quantity': 100.0, 'unitPrice': 5.7, 'tablePrice': 0.0, 'discountValue': 0.0, 'registerDate': '2003-08-04T00:00:00-03:00', 'measureUnitCode': 'SC', 'receivableQuantity': 100.0, 'partialBillingReceivedValue': 0.0, 'completedValue': 0.0, 'stockEffectFlag': 1, 'unitValue': 6.27, 'financialValue': 627.0, 'ordinationTaxAliquot': 0.0, 'originalQuantity': 100.0, 'flag': 0, 'blockObject': 0, 'rebillingFlag': 0, 'measureUnitConversionRate': 0.0, 'totalValue': 570.0, 'branchId': 1, 'sortedQuantity': 0.0, 'commercialRepresentativeChargePercentage': 0.0, 'fiscalBookkeepingValue': 0.0, 'orderFinancialValue': 0.0, 'ctrcFreightValue': 0.0, 'optionalFormulaValue1': 0.0, 'optionalFormulaValue2': 0.0, 'editedPrice': False, 'unitaryOccupiedVolumeNumber': 1, 'editedTotalPrice': 0, 'conditionalDiscountValue': 0.0, 'conditionalExpenseValue': 0.0, 'budgetDate': '2014-12-04T00:00:00-03:00', 'freightApportionment': 0.0, 'insuranceApportionment': 0.0, 'discountApportionment': 0.0, 'expenseApportionment': 0.0, 'extraValue1Apportionment': 0.0, 'extraValue2Apportionment': 0.0, 'ctrcFreightApportionment': 0.0, 'materialDeductionApportionment': 0.0, 'subcontractingDeductionApportionment': 0.0, 'otherDeductionApportionment': 0.0, 'budgetUnitPrice': 0.0, 'nFeServiceValue': 0.0, 'warehouseCode': '01', 'heritageValue': 0.0, 'heritageBaseDepreciationValue': 0.0, 'netValue': 570.0, 'grossValue': 0.0, 'originalGrossValue': 0.0, 'costCenterDepartmentApportionmentValue': 0.0, 'replenishmentCost': 0.0, 'bReplenishmentCost': 0.0, 'thirdPartyFinancialValue': 0.0, 'totalQuantity': 100.0, 'isSubstituteProduct': 0, 'unitPriceSelectionType': 0, 'financialValueApportionment': 0.0, 'completedQuantity': 0.0, 'aplicationIntegration': 'T', 'productFantasyName': 'ARGAMASSA TRADICIONAL', 'productCode': '01.14.0002', 'productReducedCode': '146', 'manufacturerProductNumber': '01.14.0002', 'manufacturerCode': '00001', 'complementaryFields': {}, 'costCenterApportionments': [], 'departmentApportionments': [], 'taxes': [{'companyId': 1, 'movementId': 122, 'movementItemSequentialId': 1, 'taxId': 'ICMS', 'calculationBasis': 627.0, 'aliquot': 0.0, 'value': 0.0, 'calculatedCalculationBasis': 627.0, 'edited': 0, 'fullBase': 0.0}, {'companyId': 1, 'movementId': 122, 'movementItemSequentialId': 1, 'taxId': 'IPI', 'calculationBasis': 570.0, 'aliquot': 10.0, 'value': 57.0, 'calculatedCalculationBasis': 570.0, 'edited': 0, 'fullBase': 0.0}], 'itemLots': [], 'itemGrids': [], 'itemSerialNumber': [], 'itemHeritage': [], 'siscoServFitting': [], 'fiscal': [], 'reserves': [], 'relatedItem': [], 'exportRelatedItem': [], 'linkedItem': [], 'exportMemo': [], 'judicialProcess': [], 'electronicInvoiceFreeFields': []}]
[{'companyId': 1, 'movementId': 123, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 146, 'quantity': 300.0, 'unitPrice': 5.7, 'tablePrice': 0.0, 'discountValue': 0.0, 'registerDate': '2003-08-04T00:00:00-03:00', 'measureUnitCode': 'SC', 'receivableQuantity': 300.0, 'partialBillingReceivedValue': 0.0, 'completedValue': 0.0, 'stockEffectFlag': 1, 'unitValue': 6.27, 'financialValue': 1881.0, 'ordinationTaxAliquot': 0.0, 'originalQuantity': 300.0, 'flag': 0, 'blockObject': 0, 'rebillingFlag': 0, 'measureUnitConversionRate': 0.0, 'totalValue': 1710.0, 'branchId': 1, 'sortedQuantity': 0.0, 'commercialRepresentativeChargePercentage': 0.0, 'fiscalBookkeepingValue': 0.0, 'orderFinancialValue': 0.0, 'ctrcFreightValue': 0.0, 'optionalFormulaValue1': 0.0, 'optionalFormulaValue2': 0.0, 'editedPrice': False, 'unitaryOccupiedVolumeNumber': 1, 'editedTotalPrice': 0, 'conditionalDiscountValue': 0.0, 'conditionalExpenseValue': 0.0, 'budgetDate': '2014-12-04T00:00:00-03:00', 'freightApportionment': 0.0, 'insuranceApportionment': 0.0, 'discountApportionment': 0.0, 'expenseApportionment': 0.0, 'extraValue1Apportionment': 0.0, 'extraValue2Apportionment': 0.0, 'ctrcFreightApportionment': 0.0, 'materialDeductionApportionment': 0.0, 'subcontractingDeductionApportionment': 0.0, 'otherDeductionApportionment': 0.0, 'budgetUnitPrice': 0.0, 'nFeServiceValue': 0.0, 'warehouseCode': '01', 'heritageValue': 0.0, 'heritageBaseDepreciationValue': 0.0, 'netValue': 1710.0, 'grossValue': 0.0, 'originalGrossValue': 0.0, 'costCenterDepartmentApportionmentValue': 0.0, 'replenishmentCost': 0.0, 'bReplenishmentCost': 0.0, 'thirdPartyFinancialValue': 0.0, 'totalQuantity': 300.0, 'isSubstituteProduct': 0, 'unitPriceSelectionType': 0, 'financialValueApportionment': 0.0, 'completedQuantity': 0.0, 'aplicationIntegration': 'T', 'productFantasyName': 'ARGAMASSA TRADICIONAL', 'productCode': '01.14.0002', 'productReducedCode': '146', 'manufacturerProductNumber': '01.14.0002', 'manufacturerCode': '00001', 'complementaryFields': {}, 'costCenterApportionments': [], 'departmentApportionments': [], 'taxes': [{'companyId': 1, 'movementId': 123, 'movementItemSequentialId': 1, 'taxId': 'ICMS', 'calculationBasis': 1881.0, 'aliquot': 0.0, 'value': 0.0, 'calculatedCalculationBasis': 1881.0, 'edited': 0, 'fullBase': 0.0}, {'companyId': 1, 'movementId': 123, 'movementItemSequentialId': 1, 'taxId': 'IPI', 'calculationBasis': 1710.0, 'aliquot': 10.0, 'value': 171.0, 'calculatedCalculationBasis': 1710.0, 'edited': 0, 'fullBase': 0.0}], 'itemLots': [], 'itemGrids': [], 'itemSerialNumber': [], 'itemHeritage': [], 'siscoServFitting': [], 'fiscal': [], 'reserves': [], 'relatedItem': [], 'exportRelatedItem': [], 'linkedItem': [], 'exportMemo': [], 'judicialProcess': [], 'electronicInvoiceFreeFields': []}]


----- FILE: attached_assets/Pasted--sa-instance-state-sqlalchemy-orm-state-InstanceState-object-at-0x7fa9d98ee030-accounting-e-1755529082796_1755529082796.txt  (sha1:8833ffe4b9b8, bytes:3595) -----
{'_sa_instance_state': <sqlalchemy.orm.state.InstanceState object at 0x7fa9d98ee030>,
 'accounting_export_status': '0',
 'affect_stock_order': '380',
 'aplication_integration': 'T',
 'aux_customer_vendor_code': 'F00411',
 'aux_customer_vendor_company_id': 0,
 'bill_printed': False,
 'bonum_integrated': False,
 'branch_id': 1,
 'c_te': '[]',
 'cargo_component': '[]',
 'cash_account_code': '2.0',
 'cash_account_company_id': 1,
 'charge_percentage': 0.0,
 'classification_table5_code': None,
 'commercial_automation_exported': 0,
 'commercial_representative_charge': 0.0,
 'company_id': 1,
 'conclusion_flag': 0,
 'conditional_discount_value': 0.0,
 'conditional_expense_value': 0.0,
 'cost_center_apportionments': '[]',
 'cost_center_code': None,
 'creation_date': datetime.datetime(2003, 9, 16, 3, 0),
 'creation_user': 'cmufumba',
 'ctrc': '[]',
 'customer_vendor_code': 'F00411',
 'customer_vendor_company_id': 0,
 'date': datetime.datetime(2003, 9, 11, 3, 0),
 'delivery_date': None,
 'department_apportionments': '[]',
 'destiny_branch_id': 1,
 'destiny_warehouse_code': '1',
 'discount_percentage': 0.0,
 'document_authorization': '[]',
 'document_printed': False,
 'eai_integration': '[]',
 'electronic_invoice_free_fields': '[]',
 'email_status': 'false',
 'entry_date': datetime.datetime(2003, 9, 16, 3, 0),
 'exit_date': datetime.datetime(2003, 9, 16, 3, 0),
 'expense_percentage': 0.0,
 'expense_value': 0.0,
 'export_related_movement': '[]',
 'extemporaneous': 0,
 'extra_percentage1': 0.0,
 'extra_percentage2': 0.0,
 'extra_value1': 0.0,
 'extra_value2': 0.0,
 'financial_entry_movement_id': '380.0',
 'financial_optional_table1_code': '2.0',
 'financial_optional_table2_code': None,
 'fiscal': '[]',
 'fluxus_grouped_flag': False,
 'generated_entry_number': '1.0',
 'gross_value': 80.0,
 'has_generated_bill': False,
 'has_generated_work_account': False,
 'icms_deduction_value': 0.0,
 'id': 250,
 'indicate_object_use': '0',
 'informed_net_value': 0.0,
 'input_ctrc': '[]',
 'internal_gross_value': 80.0,
 'internal_id': '1|380',
 'judicial_process': '[]',
 'last_edit_time': datetime.datetime(2003, 9, 16, 12, 59, 40),
 'linked_movement': '[]',
 'long_history': 'COSERTO DA PRANCHA',
 'lot_generated': False,
 'merchandise_value': 0.0,
 'movement_id': 380,
 'movement_items': '[]',
 'movement_type_code': '1.2.02',
 'net_value': 80.0,
 'net_value_currency_code': 'R$',
 'nfe': '[]',
 'norm': '[]',
 'number': 11179,
 'observation': None,
 'open_entry_number': '1.0',
 'operation_id': 0,
 'original_gross_value': 0.0,
 'original_net_value': 0.0,
 'original_other_values': 0.0,
 'other_company_inss_base_value': 0.0,
 'other_values': 80.0,
 'output_ctrc': '[]',
 'paradigma_auto_integrated': False,
 'paradigma_status': 'N',
 'payment_term_code': '30.0',
 'payments': '[]',
 'printed': False,
 'processed_flag': False,
 'register_date': datetime.datetime(2003, 9, 11, 3, 0),
 'related_movement': '[]',
 'safety_device': '[]',
 'salesman1_code': None,
 'salesman2_charge_percentage': 0.0,
 'salesman3_charge_percentage': 0.0,
 'salesman4_charge_percentage': 0.0,
 'scp_branch_id': 0,
 'series': 'U',
 'service_order': '[]',
 'status': 'F',
 'taxes': '[]',
 'third_party_nf': '[]',
 'transport_data': '[]',
 'transported_product_gross_weight': 0.0,
 'transported_product_net_weight': 0.0,
 'type': 'P',
 'upload_batch_id': '042bed78-bf01-4389-accb-ed032646b4fc',
 'uploaded_at': datetime.datetime(2025, 8, 18, 14, 56, 9, 423486),
 'uploaded_by': 1,
 'user_code': 'cmufumba',
 'uses_financial_value_apportionment': False,
 'warehouse_code': '1',
 'work_account_generated': 'false'}

----- FILE: attached_assets/Pasted-ERROR-app-Exception-on-movements-GET-Traceback-most-recent-call-last-File-home-runner-wo-1757090301627_1757090301628.txt  (sha1:5b4a42c7e6bd, bytes:12473) -----
ERROR:app:Exception on /movements [GET]
Traceback (most recent call last):
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 1511, in wsgi_app
    response = self.full_dispatch_request()
               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 919, in full_dispatch_request
    rv = self.handle_user_exception(e)
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 917, in full_dispatch_request
    rv = self.dispatch_request()
         ^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 902, in dispatch_request
    return self.ensure_sync(self.view_functions[rule.endpoint])(**view_args)  # type: ignore[no-any-return]
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask_login/utils.py", line 290, in decorated_view
    return current_app.ensure_sync(func)(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/routes.py", line 102, in movements
    return render_template('movements/list.html',
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/templating.py", line 150, in render_template
    return _render(app, template, context)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/templating.py", line 131, in _render
    rv = template.render(context)
         ^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/environment.py", line 1295, in render
    self.environment.handle_exception()
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/environment.py", line 942, in handle_exception
    raise rewrite_traceback_stack(source=source)
  File "/home/runner/workspace/templates/movements/list.html", line 1, in top-level template code
    {% extends "base.html" %}
  File "/home/runner/workspace/templates/base.html", line 126, in top-level template code
    {% block content %}{% endblock %}
    ^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/templates/movements/list.html", line 277, in block 'content'
    {% set total_value = movements.items | sum(attribute='net_value') %}
    ^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/async_utils.py", line 48, in wrapper
    return normal_func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/filters.py", line 1336, in sync_do_sum
    return sum(iterable, start)  # type: ignore[no-any-return, call-overload]
           ^^^^^^^^^^^^^^^^^^^^
TypeError: unsupported operand type(s) for +: 'int' and 'NoneType'
[2025-09-05 16:36:09 +0000] [793] [INFO] Handling signal: winch
ERROR:app:Exception on /movements [GET]
Traceback (most recent call last):
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 1511, in wsgi_app
    response = self.full_dispatch_request()
               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 919, in full_dispatch_request
    rv = self.handle_user_exception(e)
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 917, in full_dispatch_request
    rv = self.dispatch_request()
         ^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 902, in dispatch_request
    return self.ensure_sync(self.view_functions[rule.endpoint])(**view_args)  # type: ignore[no-any-return]
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask_login/utils.py", line 290, in decorated_view
    return current_app.ensure_sync(func)(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/routes.py", line 102, in movements
    return render_template('movements/list.html',
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/templating.py", line 150, in render_template
    return _render(app, template, context)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/templating.py", line 131, in _render
    rv = template.render(context)
         ^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/environment.py", line 1295, in render
    self.environment.handle_exception()
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/environment.py", line 942, in handle_exception
    raise rewrite_traceback_stack(source=source)
  File "/home/runner/workspace/templates/movements/list.html", line 1, in top-level template code
    {% extends "base.html" %}
  File "/home/runner/workspace/templates/base.html", line 126, in top-level template code
    {% block content %}{% endblock %}
    ^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/templates/movements/list.html", line 277, in block 'content'
    {% set total_value = movements.items | sum(attribute='net_value') %}
    ^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/async_utils.py", line 48, in wrapper
    return normal_func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/filters.py", line 1336, in sync_do_sum
    return sum(iterable, start)  # type: ignore[no-any-return, call-overload]
           ^^^^^^^^^^^^^^^^^^^^
TypeError: unsupported operand type(s) for +: 'int' and 'NoneType'
ERROR:app:Exception on /movements [GET]
Traceback (most recent call last):
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 1511, in wsgi_app
    response = self.full_dispatch_request()
               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 919, in full_dispatch_request
    rv = self.handle_user_exception(e)
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 917, in full_dispatch_request
    rv = self.dispatch_request()
         ^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 902, in dispatch_request
    return self.ensure_sync(self.view_functions[rule.endpoint])(**view_args)  # type: ignore[no-any-return]
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask_login/utils.py", line 290, in decorated_view
    return current_app.ensure_sync(func)(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/routes.py", line 102, in movements
    return render_template('movements/list.html',
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/templating.py", line 150, in render_template
    return _render(app, template, context)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/templating.py", line 131, in _render
    rv = template.render(context)
         ^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/environment.py", line 1295, in render
    self.environment.handle_exception()
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/environment.py", line 942, in handle_exception
    raise rewrite_traceback_stack(source=source)
  File "/home/runner/workspace/templates/movements/list.html", line 1, in top-level template code
    {% extends "base.html" %}
  File "/home/runner/workspace/templates/base.html", line 126, in top-level template code
    {% block content %}{% endblock %}
    ^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/templates/movements/list.html", line 277, in block 'content'
    {% set total_value = movements.items | sum(attribute='net_value') %}
    ^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/async_utils.py", line 48, in wrapper
    return normal_func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/filters.py", line 1336, in sync_do_sum
    return sum(iterable, start)  # type: ignore[no-any-return, call-overload]
           ^^^^^^^^^^^^^^^^^^^^
TypeError: unsupported operand type(s) for +: 'int' and 'NoneType'
ERROR:app:Exception on /movements/3739 [GET]
Traceback (most recent call last):
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 1511, in wsgi_app
    response = self.full_dispatch_request()
               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 919, in full_dispatch_request
    rv = self.handle_user_exception(e)
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 917, in full_dispatch_request
    rv = self.dispatch_request()
         ^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 902, in dispatch_request
    return self.ensure_sync(self.view_functions[rule.endpoint])(**view_args)  # type: ignore[no-any-return]
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask_login/utils.py", line 290, in decorated_view
    return current_app.ensure_sync(func)(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/routes.py", line 117, in movement_detail
    return render_template('movements/detail.html', movement=movement)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/templating.py", line 150, in render_template
    return _render(app, template, context)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/templating.py", line 131, in _render
    rv = template.render(context)
         ^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/environment.py", line 1295, in render
    self.environment.handle_exception()
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/environment.py", line 942, in handle_exception
    raise rewrite_traceback_stack(source=source)
  File "/home/runner/workspace/templates/movements/detail.html", line 1, in top-level template code
    {% extends "base.html" %}
  File "/home/runner/workspace/templates/base.html", line 126, in top-level template code
    {% block content %}{% endblock %}
    ^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/templates/movements/detail.html", line 463, in block 'content'
    <strong class="text-success">R$ {{ "%.2f"|format(item.value) }}</strong>
    ^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/filters.py", line 1043, in do_format
    return soft_str(value) % (kwargs or args)
           ~~~~~~~~~~~~~~~~^~~~~~~~~~~~~~~~~~
jinja2.exceptions.UndefinedError: 'dict object' has no attribute 'value'
^C[2025-09-05 16:37:54 +0000] [793] [INFO] Handling signal: int
[2025-09-05 16:37:55 +0000] [793] [INFO] Shutting down: Master

----- FILE: attached_assets/Pasted-INFO-Handling-signal-winch-ERROR-app-Exception-on-admin-dashboard-GET-Traceback-most-recent-ca-1755537314337_1755537314338.txt  (sha1:01cf3a3b51de, bytes:2835) -----
INFO] Handling signal: winch
ERROR:app:Exception on /admin/dashboard [GET]
Traceback (most recent call last):
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 1511, in wsgi_app
    response = self.full_dispatch_request()
               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 919, in full_dispatch_request
    rv = self.handle_user_exception(e)
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 917, in full_dispatch_request
    rv = self.dispatch_request()
         ^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 902, in dispatch_request
    return self.ensure_sync(self.view_functions[rule.endpoint])(**view_args)  # type: ignore[no-any-return]
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask_login/utils.py", line 290, in decorated_view
    return current_app.ensure_sync(func)(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/routes.py", line 36, in decorated_function
    return f(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/routes.py", line 127, in dashboard
    return render_template('admin/dashboard.html', stats=stats)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/templating.py", line 150, in render_template
    return _render(app, template, context)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/templating.py", line 131, in _render
    rv = template.render(context)
         ^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/environment.py", line 1295, in render
    self.environment.handle_exception()
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/environment.py", line 942, in handle_exception
    raise rewrite_traceback_stack(source=source)
  File "/home/runner/workspace/templates/admin/dashboard.html", line 1, in top-level template code
    {% extends "base.html" %}
  File "/home/runner/workspace/templates/base.html", line 126, in top-level template code
    {% block content %}{% endblock %}
    ^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/templates/admin/dashboard.html", line 144, in block 'content'
    {% if upload.failed_records > 0 %}
    ^^^^^^^^^^^^^^^^^^^^^^^^^
jinja2.exceptions.UndefinedError: 'models.UploadHistory object' has no attribute 'failed_records'

----- FILE: attached_assets/Pasted-INFO-file-processor-Reading-Excel-file-uploads-a95c624a-ed79-4640-9c65-1a624b0c03a3-rm-movimentos-U-1756245935776_1756245935777.txt  (sha1:6b5b2242247e, bytes:5836) -----
INFO:file_processor:Reading Excel file: uploads/a95c624a-ed79-4640-9c65-1a624b0c03a3_rm_movimentos_ULTIMO_DIA_empresas_4_148.xlsx
INFO:file_processor:Read Excel file with 210 rows
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
INFO:file_processor:Saved 210 records to database with batch_id c93a7db9-d33f-4f26-950e-b7d3f34934d2
[2025-08-26 22:05:10 +0000] [142] [INFO] Handling signal: winch
[2025-08-26 22:05:10 +0000] [142] [INFO] Handling signal: winch
[2025-08-26 22:05:10 +0000] [142] [INFO] Handling signal: winch
[2025-08-26 22:05:10 +0000] [142] [INFO] Handling signal: winch
[2025-08-26 22:05:11 +0000] [142] [INFO] Handling signal: winch

----- FILE: attached_assets/Pasted-O-Handling-signal-winch-INFO-file-processor-Reading-Excel-file-uploads-43a4e8b8-5aca-471d-a2a7-cd-1755528724777_1755528724777.txt  (sha1:f8b40ad75060, bytes:3134) -----
O] Handling signal: winch
INFO:file_processor:Reading Excel file: uploads/43a4e8b8-5aca-471d-a2a7-cda9a5fd54d1_pagina_unica_148_1.xlsx
INFO:file_processor:Read Excel file with 50 rows
INFO:file_processor:Saved 50 records to database with batch_id 64459794-e416-4a28-9e4f-0b6fa4237c64
ERROR:app:Exception on /admin/upload-history [GET]
Traceback (most recent call last):
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 1511, in wsgi_app
    response = self.full_dispatch_request()
               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 919, in full_dispatch_request
    rv = self.handle_user_exception(e)
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 917, in full_dispatch_request
    rv = self.dispatch_request()
         ^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 902, in dispatch_request
    return self.ensure_sync(self.view_functions[rule.endpoint])(**view_args)  # type: ignore[no-any-return]
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask_login/utils.py", line 290, in decorated_view
    return current_app.ensure_sync(func)(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/routes.py", line 36, in decorated_function
    return f(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/routes.py", line 252, in upload_history
    return render_template('admin/upload.html', uploads=uploads, show_history=True)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/templating.py", line 150, in render_template
    return _render(app, template, context)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/templating.py", line 131, in _render
    rv = template.render(context)
         ^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/environment.py", line 1295, in render
    self.environment.handle_exception()
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/environment.py", line 942, in handle_exception
    raise rewrite_traceback_stack(source=source)
  File "/home/runner/workspace/templates/admin/upload.html", line 1, in top-level template code
    {% extends "base.html" %}
  File "/home/runner/workspace/templates/base.html", line 126, in top-level template code
    {% block content %}{% endblock %}
    ^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/templates/admin/upload.html", line 185, in block 'content'
    {% if upload.failed_records > 0 %}
    ^^^^^^^^^^^^^^^^^^^^^^^^^
jinja2.exceptions.UndefinedError: 'models.UploadHistory object' has no attribute 'failed_records'

----- FILE: migrations/alembic.ini  (sha1:6b652007c8ee, bytes:857) -----
# A generic, single database configuration.

[alembic]
# template used to generate migration files
# file_template = %%(rev)s_%%(slug)s

# set to 'true' to run the environment during
# the 'revision' command, regardless of autogenerate
# revision_environment = false


# Logging configuration
[loggers]
keys = root,sqlalchemy,alembic,flask_migrate

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console
qualname =

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[logger_flask_migrate]
level = INFO
handlers =
qualname = flask_migrate

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S


----- FILE: migrations/env.py  (sha1:14d8f8952082, bytes:3344) -----
import logging
from logging.config import fileConfig

from flask import current_app

from alembic import context

# this is the Alembic Config object, which provides
# access to the values within the .ini file in use.
config = context.config

# Interpret the config file for Python logging.
# This line sets up loggers basically.
fileConfig(config.config_file_name)
logger = logging.getLogger('alembic.env')


def get_engine():
    try:
        # this works with Flask-SQLAlchemy<3 and Alchemical
        return current_app.extensions['migrate'].db.get_engine()
    except (TypeError, AttributeError):
        # this works with Flask-SQLAlchemy>=3
        return current_app.extensions['migrate'].db.engine


def get_engine_url():
    try:
        return get_engine().url.render_as_string(hide_password=False).replace(
            '%', '%%')
    except AttributeError:
        return str(get_engine().url).replace('%', '%%')


# add your model's MetaData object here
# for 'autogenerate' support
# from myapp import mymodel
# target_metadata = mymodel.Base.metadata
config.set_main_option('sqlalchemy.url', get_engine_url())
target_db = current_app.extensions['migrate'].db

# other values from the config, defined by the needs of env.py,
# can be acquired:
# my_important_option = config.get_main_option("my_important_option")
# ... etc.


def get_metadata():
    if hasattr(target_db, 'metadatas'):
        return target_db.metadatas[None]
    return target_db.metadata


def run_migrations_offline():
    """Run migrations in 'offline' mode.

    This configures the context with just a URL
    and not an Engine, though an Engine is acceptable
    here as well.  By skipping the Engine creation
    we don't even need a DBAPI to be available.

    Calls to context.execute() here emit the given string to the
    script output.

    """
    url = config.get_main_option("sqlalchemy.url")
    context.configure(
        url=url, target_metadata=get_metadata(), literal_binds=True
    )

    with context.begin_transaction():
        context.run_migrations()


def run_migrations_online():
    """Run migrations in 'online' mode.

    In this scenario we need to create an Engine
    and associate a connection with the context.

    """

    # this callback is used to prevent an auto-migration from being generated
    # when there are no changes to the schema
    # reference: http://alembic.zzzcomputing.com/en/latest/cookbook.html
    def process_revision_directives(context, revision, directives):
        if getattr(config.cmd_opts, 'autogenerate', False):
            script = directives[0]
            if script.upgrade_ops.is_empty():
                directives[:] = []
                logger.info('No changes in schema detected.')

    conf_args = current_app.extensions['migrate'].configure_args
    if conf_args.get("process_revision_directives") is None:
        conf_args["process_revision_directives"] = process_revision_directives

    connectable = get_engine()

    with connectable.connect() as connection:
        context.configure(
            connection=connection,
            target_metadata=get_metadata(),
            **conf_args
        )

        with context.begin_transaction():
            context.run_migrations()


if context.is_offline_mode():
    run_migrations_offline()
else:
    run_migrations_online()


----- FILE: migrations/versions/80fc0baf7fe3_add_customer_vendor_name_and_cnpj_to_.py  (sha1:4a228821e108, bytes:1019) -----
"""add customer_vendor_name and cnpj to FinancialMovement

Revision ID: 80fc0baf7fe3
Revises: 
Create Date: 2025-10-27 18:52:46.020242

"""
from alembic import op
import sqlalchemy as sa


# revision identifiers, used by Alembic.
revision = '80fc0baf7fe3'
down_revision = None
branch_labels = None
depends_on = None


def upgrade():
    # ### commands auto generated by Alembic - please adjust! ###
    with op.batch_alter_table('financial_movement', schema=None) as batch_op:
        batch_op.add_column(sa.Column('customer_vendor_name', sa.String(length=255), nullable=True))
        batch_op.add_column(sa.Column('customer_vendor_cnpj', sa.String(length=30), nullable=True))

    # ### end Alembic commands ###


def downgrade():
    # ### commands auto generated by Alembic - please adjust! ###
    with op.batch_alter_table('financial_movement', schema=None) as batch_op:
        batch_op.drop_column('customer_vendor_cnpj')
        batch_op.drop_column('customer_vendor_name')

    # ### end Alembic commands ###


----- FILE: static/css/style.css  (sha1:fa688f7c9dd7, bytes:5794) -----
/* Custom styles for Financial Data Management System */
/* Following Bootstrap 5 dark theme variables and minimal custom overrides */

:root {
    --custom-border-radius: 0.375rem;
    --custom-box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075);
    --custom-transition: all 0.15s ease-in-out;
}

/* Global improvements */
body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}

/* Custom card enhancements */
.card {
    border: 1px solid var(--bs-border-color);
    box-shadow: var(--custom-box-shadow);
    transition: var(--custom-transition);
}

.card:hover {
    box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);
}

/* Enhanced navigation */
.navbar-brand {
    font-weight: 600;
}

/* Table improvements */
.table-responsive {
    border-radius: var(--custom-border-radius);
}

.table th {
    font-weight: 600;
    border-top: none;
}

/* Code elements */
code {
    color: var(--bs-info);
    background-color: rgba(var(--bs-info-rgb), 0.1);
    padding: 0.125rem 0.25rem;
    border-radius: 0.25rem;
    font-size: 0.875em;
}

/* Badge improvements */
.badge {
    font-weight: 500;
}

/* Button enhancements */
.btn {
    font-weight: 500;
    transition: var(--custom-transition);
}

.btn-group .btn {
    border-radius: 0;
}

.btn-group .btn:first-child {
    border-top-left-radius: var(--custom-border-radius);
    border-bottom-left-radius: var(--custom-border-radius);
}

.btn-group .btn:last-child {
    border-top-right-radius: var(--custom-border-radius);
    border-bottom-right-radius: var(--custom-border-radius);
}

/* Form improvements */
.form-control:focus,
.form-select:focus {
    border-color: var(--bs-primary);
    box-shadow: 0 0 0 0.2rem rgba(var(--bs-primary-rgb), 0.25);
}

.form-label {
    font-weight: 500;
    margin-bottom: 0.5rem;
}

/* Alert enhancements */
.alert {
    border: none;
    border-radius: var(--custom-border-radius);
}

.alert .fas,
.alert .far {
    width: 1.25rem;
    text-align: center;
}

/* Jumbotron style for hero sections */
.jumbotron {
    background: linear-gradient(135deg, var(--bs-primary), var(--bs-info));
    border-radius: var(--custom-border-radius);
}

/* Pagination improvements */
.pagination .page-link {
    border-radius: var(--custom-border-radius);
    margin: 0 0.125rem;
    border: 1px solid var(--bs-border-color);
}

.pagination .page-item.active .page-link {
    background-color: var(--bs-primary);
    border-color: var(--bs-primary);
}

/* Statistics cards */
.card.text-white .card-footer a:hover {
    text-decoration: underline !important;
}

/* Accordion improvements */
.accordion-button:not(.collapsed) {
    background-color: rgba(var(--bs-primary-rgb), 0.1);
    color: var(--bs-primary);
}

.accordion-button:focus {
    box-shadow: 0 0 0 0.2rem rgba(var(--bs-primary-rgb), 0.25);
}

/* File upload area */
.form-control[type="file"] {
    padding: 0.75rem;
}

/* DataTables integration */
.dataTables_wrapper .dataTables_length select,
.dataTables_wrapper .dataTables_filter input {
    background-color: var(--bs-body-bg);
    border: 1px solid var(--bs-border-color);
    color: var(--bs-body-color);
}

.dataTables_wrapper .dataTables_paginate .paginate_button {
    border: 1px solid var(--bs-border-color);
    background-color: var(--bs-body-bg);
    color: var(--bs-body-color) !important;
}

.dataTables_wrapper .dataTables_paginate .paginate_button:hover {
    background-color: var(--bs-secondary-bg);
    border-color: var(--bs-border-color);
}

.dataTables_wrapper .dataTables_paginate .paginate_button.current {
    background-color: var(--bs-primary) !important;
    border-color: var(--bs-primary) !important;
    color: white !important;
}

/* Loading states */
.loading {
    opacity: 0.6;
    pointer-events: none;
}

.spinner-border-sm {
    width: 1rem;
    height: 1rem;
}

/* Mobile responsiveness improvements */
@media (max-width: 768px) {
    .d-md-flex {
        flex-direction: column;
        gap: 0.5rem;
    }
    
    .btn-group {
        flex-direction: column;
    }
    
    .btn-group .btn {
        border-radius: var(--custom-border-radius) !important;
        margin-bottom: 0.25rem;
    }
    
    .card-body .row > div {
        margin-bottom: 1rem;
    }
}

/* Print styles */
@media print {
    .navbar,
    .btn,
    .pagination,
    .card-footer,
    .alert,
    .badge {
        display: none !important;
    }
    
    .card {
        border: 1px solid #000;
        box-shadow: none;
    }
    
    .table {
        font-size: 0.8rem;
    }
}

/* Custom utility classes */
.text-truncate-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

.border-start-primary {
    border-left: 0.25rem solid var(--bs-primary) !important;
}

.border-start-success {
    border-left: 0.25rem solid var(--bs-success) !important;
}

.border-start-warning {
    border-left: 0.25rem solid var(--bs-warning) !important;
}

.border-start-danger {
    border-left: 0.25rem solid var(--bs-danger) !important;
}

/* Animation utilities */
@keyframes fadeIn {
    from { opacity: 0; transform: translateY(10px); }
    to { opacity: 1; transform: translateY(0); }
}

.fade-in {
    animation: fadeIn 0.3s ease-in-out;
}

/* Focus improvements for accessibility */
.btn:focus,
.form-control:focus,
.form-select:focus {
    outline: 2px solid var(--bs-primary);
    outline-offset: 2px;
}

/* High contrast mode support */
@media (prefers-contrast: high) {
    .card {
        border-width: 2px;
    }
    
    .btn {
        border-width: 2px;
    }
}

/* Reduced motion support */
@media (prefers-reduced-motion: reduce) {
    * {
        animation-duration: 0.01ms !important;
        animation-iteration-count: 1 !important;
        transition-duration: 0.01ms !important;
    }
}


----- FILE: static/js/main.js  (sha1:5731a14c205e, bytes:13840) -----
// Main JavaScript file for Financial Data Management System
// Enhanced functionality for better user experience

$(document).ready(function() {
    // Initialize all components
    initializeComponents();
    initializeDataTables();
    initializeFormValidation();
    initializeTooltips();
    initializeFileUpload();
    initializeSearchEnhancements();
});

// Initialize main components
function initializeComponents() {
    // Add fade-in animation to cards
    $('.card').addClass('fade-in');
    
    // Enhanced dropdown behaviors
    $('.dropdown-toggle').on('click', function(e) {
        e.stopPropagation();
    });
    
    // Auto-dismiss alerts after 5 seconds
    setTimeout(function() {
        $('.alert').alert('close');
    }, 5000);
    
    // Smooth scrolling for anchor links
    $('a[href^="#"]').on('click', function(e) {
        e.preventDefault();
        const target = $($(this).attr('href'));
        if (target.length) {
            $('html, body').animate({
                scrollTop: target.offset().top - 70
            }, 500);
        }
    });
}

// Initialize DataTables with enhanced features
function initializeDataTables() {
    // Common DataTable configuration
    const commonConfig = {
        responsive: true,
        pageLength: 25,
        lengthMenu: [[10, 25, 50, 100, -1], [10, 25, 50, 100, "All"]],
        language: {
            search: "Buscar movimentos:",
            lengthMenu: "Mostrar _MENU_ registros por página",
            info: "Exibindo _START_ a _END_ de _TOTAL_ registros",
            infoEmpty: "Nenhum registro disponível",
            infoFiltered: "(filtrado de _MAX_ registros no total)",
            paginate: {
                first: "Primeiro",
                last: "Último",
                next: "Próximo",
                previous: "Anterior"
            }
        },

        dom: '<"row"<"col-sm-12 col-md-6"l><"col-sm-12 col-md-6"f>>' +
             '<"row"<"col-sm-12"tr>>' +
             '<"row"<"col-sm-12 col-md-5"i><"col-sm-12 col-md-7"p>>',
        initComplete: function() {
            // Add Bootstrap styling to DataTables elements
            $('.dataTables_length select').addClass('form-select form-select-sm');
            $('.dataTables_filter input').addClass('form-control form-control-sm');
        }
    };
    
    // Initialize specific tables
    if ($('#movementsTable').length) {
        $('#movementsTable').DataTable({
            ...commonConfig,
            order: [[7, "desc"]], // Sort by uploaded date
            columnDefs: [
                { targets: [-1], orderable: false }, // Disable sorting on actions column
                { targets: [4], type: 'currency' }, // Currency sorting for net value
            ]
        });
    }
    
    if ($('#usersTable').length) {
        $('#usersTable').DataTable({
            ...commonConfig,
            order: [[3, "desc"]], // Sort by created date
            columnDefs: [
                { targets: [-1], orderable: false }, // Disable sorting on actions column
            ]
        });
    }
    
    if ($('#uploadsTable').length) {
        $('#uploadsTable').DataTable({
            ...commonConfig,
            order: [[5, "desc"]], // Sort by upload date
            columnDefs: [
                { targets: [-1], orderable: false }, // Disable sorting on actions column
            ]
        });
    }
}

// Initialize form validation
function initializeFormValidation() {
    // Real-time validation for forms
    $('form').on('submit', function(e) {
        const form = $(this);
        let isValid = true;
        
        // Check required fields
        form.find('[required]').each(function() {
            const field = $(this);
            if (!field.val().trim()) {
                field.addClass('is-invalid');
                isValid = false;
            } else {
                field.removeClass('is-invalid');
            }
        });
        
        // Email validation
        form.find('input[type="email"]').each(function() {
            const email = $(this);
            const emailPattern = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
            if (email.val() && !emailPattern.test(email.val())) {
                email.addClass('is-invalid');
                isValid = false;
            }
        });
        
        // Password confirmation
        const password = form.find('input[name="password"]');
        const password2 = form.find('input[name="password2"]');
        if (password.length && password2.length) {
            if (password.val() !== password2.val()) {
                password2.addClass('is-invalid');
                isValid = false;
            }
        }
        
        if (!isValid) {
            e.preventDefault();
            showNotification('Please correct the highlighted fields', 'error');
        }
    });
    
    // Remove validation classes on input
    $('input, select, textarea').on('input change', function() {
        $(this).removeClass('is-invalid');
    });
}

// Initialize tooltips
function initializeTooltips() {
    // Enable Bootstrap tooltips
    const tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'));
    tooltipTriggerList.map(function(tooltipTriggerEl) {
        return new bootstrap.Tooltip(tooltipTriggerEl);
    });
    
    // Add tooltips to truncated text
    $('.text-truncate, .text-truncate-2').each(function() {
        const element = $(this);
        if (element[0].scrollWidth > element[0].clientWidth) {
            element.attr('data-bs-toggle', 'tooltip');
            element.attr('title', element.text());
            new bootstrap.Tooltip(element[0]);
        }
    });
}

// Initialize file upload enhancements
function initializeFileUpload() {
    $('input[type="file"]').on('change', function() {
        const file = this.files[0];
        const fileInfo = $(this).siblings('.file-info');
        
        if (file) {
            const size = (file.size / (1024 * 1024)).toFixed(2);
            const info = `Selected: ${file.name} (${size} MB)`;
            
            if (fileInfo.length) {
                fileInfo.text(info);
            } else {
                $(this).after(`<div class="file-info text-muted mt-1">${info}</div>`);
            }
            
            // Validate file size (50MB limit)
            if (file.size > 50 * 1024 * 1024) {
                showNotification('File size exceeds 50MB limit', 'error');
                $(this).val('');
                $(this).siblings('.file-info').remove();
            }
        } else {
            fileInfo.remove();
        }
    });
    
    // Drag and drop enhancement
    $('.form-control[type="file"]').each(function() {
        const fileInput = $(this);
        const dropZone = $('<div class="drop-zone border-2 border-dashed p-4 text-center rounded mt-2">' +
                          '<i class="fas fa-cloud-upload-alt fa-2x text-muted"></i><br>' +
                          '<span class="text-muted">Drag and drop files here or click to browse</span>' +
                          '</div>');
        
        fileInput.after(dropZone);
        fileInput.hide();
        
        dropZone.on('click', function() {
            fileInput.click();
        });
        
        dropZone.on('dragover', function(e) {
            e.preventDefault();
            $(this).addClass('border-primary bg-primary bg-opacity-10');
        });
        
        dropZone.on('dragleave', function() {
            $(this).removeClass('border-primary bg-primary bg-opacity-10');
        });
        
        dropZone.on('drop', function(e) {
            e.preventDefault();
            $(this).removeClass('border-primary bg-primary bg-opacity-10');
            
            const files = e.originalEvent.dataTransfer.files;
            if (files.length > 0) {
                fileInput[0].files = files;
                fileInput.trigger('change');
            }
        });
    });
}

// Initialize search enhancements
function initializeSearchEnhancements() {
    // Auto-complete for search fields
    $('input[name="search"]').on('input', function() {
        const query = $(this).val();
        if (query.length >= 2) {
            // Debounce search suggestions
            clearTimeout(this.searchTimeout);
            this.searchTimeout = setTimeout(() => {
                // You can implement search suggestions here
                console.log('Search suggestions for:', query);
            }, 300);
        }
    });
    
    // Advanced search toggle
    $('.advanced-search-toggle').on('click', function() {
        $('.advanced-search-panel').slideToggle();
    });
    
    // Search history (localStorage)
    const searchHistory = JSON.parse(localStorage.getItem('searchHistory') || '[]');
    
    $('form').on('submit', function() {
        const searchInput = $(this).find('input[name="search"]');
        if (searchInput.length && searchInput.val().trim()) {
            const query = searchInput.val().trim();
            if (!searchHistory.includes(query)) {
                searchHistory.unshift(query);
                searchHistory.splice(10); // Keep only last 10 searches
                localStorage.setItem('searchHistory', JSON.stringify(searchHistory));
            }
        }
    });
}

// Utility functions
function showNotification(message, type = 'info') {
    const alertClass = type === 'error' ? 'alert-danger' : `alert-${type}`;
    const icon = type === 'error' ? 'fas fa-exclamation-triangle' : 
                 type === 'success' ? 'fas fa-check-circle' : 'fas fa-info-circle';
    
    const alert = $(`
        <div class="alert ${alertClass} alert-dismissible fade show" role="alert">
            <i class="${icon} me-2"></i>
            ${message}
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    `);
    
    // Insert at top of main container
    $('main.container').prepend(alert);
    
    // Auto-dismiss after 5 seconds
    setTimeout(() => {
        alert.alert('close');
    }, 5000);
}

function formatCurrency(amount, currency = 'R$') {
    return `${currency} ${parseFloat(amount).toFixed(2)}`;
}

function formatDate(dateString) {
    if (!dateString) return '-';
    const date = new Date(dateString);
    return date.toLocaleDateString('pt-BR');
}

function formatDateTime(dateString) {
    if (!dateString) return '-';
    const date = new Date(dateString);
    return date.toLocaleString('pt-BR');
}

// Export functionality
function exportTableToCSV(tableId, filename = 'export.csv') {
    const table = document.getElementById(tableId);
    if (!table) return;
    
    const rows = Array.from(table.querySelectorAll('tr'));
    const csv = rows.map(row => {
        const cells = Array.from(row.querySelectorAll('td, th'));
        return cells.slice(0, -1).map(cell => { // Exclude actions column
            let text = cell.textContent.trim();
            // Handle commas and quotes in CSV
            if (text.includes(',') || text.includes('"')) {
                text = '"' + text.replace(/"/g, '""') + '"';
            }
            return text;
        }).join(',');
    }).join('\n');
    
    // Download CSV
    const blob = new Blob([csv], { type: 'text/csv' });
    const url = window.URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = filename;
    a.style.display = 'none';
    document.body.appendChild(a);
    a.click();
    document.body.removeChild(a);
    window.URL.revokeObjectURL(url);
}

// JSON data formatting for display
function formatJSONField(data, fieldName) {
    if (!data || data.length === 0) {
        return '<span class="text-muted">No data</span>';
    }
    
    if (Array.isArray(data)) {
        return `<span class="badge bg-info">${data.length} items</span>`;
    }
    
    return '<span class="badge bg-secondary">Object</span>';
}

// Performance monitoring
function measurePerformance(name, fn) {
    const start = performance.now();
    const result = fn();
    const end = performance.now();
    console.log(`${name} took ${end - start} milliseconds`);
    return result;
}

// Error handling
window.addEventListener('error', function(e) {
    console.error('JavaScript error:', e.error);
    // showNotification('An unexpected error occurred. Please refresh the page.', 'error');
});

// Keyboard shortcuts
document.addEventListener('keydown', function(e) {
    // Ctrl+/ or Cmd+/ for search focus
    if ((e.ctrlKey || e.metaKey) && e.key === '/') {
        e.preventDefault();
        const searchInput = document.querySelector('input[name="search"]');
        if (searchInput) {
            searchInput.focus();
        }
    }
    
    // Escape to clear search
    if (e.key === 'Escape') {
        const searchInput = document.querySelector('input[name="search"]:focus');
        if (searchInput) {
            searchInput.value = '';
            searchInput.blur();
        }
    }
});

// Mobile enhancements
if (window.innerWidth <= 768) {
    // Mobile-specific enhancements
    $(document).ready(function() {
        // Collapse long text on mobile
        $('.table td').each(function() {
            const text = $(this).text();
            if (text.length > 30) {
                $(this).attr('title', text);
                $(this).text(text.substring(0, 30) + '...');
            }
        });
        
        // Mobile navigation improvements
        $('.navbar-toggler').on('click', function() {
            $('body').toggleClass('nav-open');
        });
    });
}

// Progressive Web App features
if ('serviceWorker' in navigator) {
    window.addEventListener('load', function() {
        // Service worker registration would go here
        console.log('Service Worker support detected');
    });
}


----- FILE: templates/base.html  (sha1:256838da5a60, bytes:6867) -----
<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Financial Data Management{% endblock %}</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.replit.com/agent/bootstrap-agent-dark-theme.min.css" rel="stylesheet">
    
    <!-- DataTables CSS -->
    <link href="https://cdn.datatables.net/1.13.6/css/dataTables.bootstrap5.min.css" rel="stylesheet">
    
    <!-- Font Awesome Icons -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    
    <!-- Custom CSS -->
    <link href="{{ url_for('static', filename='css/style.css') }}" rel="stylesheet">
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark border-bottom">
        <div class="container">
            <a class="navbar-brand" href="{{ url_for('main.index') }}">
                <i class="fas fa-chart-line me-2"></i>
                Financial Data Management
            </a>
            
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ url_for('main.index') }}">
                            <i class="fas fa-home me-1"></i>Home
                        </a>
                    </li>
                    {% if current_user.is_authenticated %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ url_for('main.movements') }}">
                            <i class="fas fa-list me-1"></i>Movements
                        </a>
                    </li>
                    {% endif %}
                    {% if current_user.is_authenticated and current_user.is_admin %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                            <i class="fas fa-cog me-1"></i>Admin
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="{{ url_for('admin.dashboard') }}">
                                <i class="fas fa-tachometer-alt me-2"></i>Dashboard
                            </a></li>
                            <li><a class="dropdown-item" href="{{ url_for('admin.users') }}">
                                <i class="fas fa-users me-2"></i>Users
                            </a></li>
                            <li><a class="dropdown-item" href="{{ url_for('admin.upload_file') }}">
                                <i class="fas fa-upload me-2"></i>Upload Data
                            </a></li>
                            <li><a class="dropdown-item" href="{{ url_for('admin.upload_history') }}">
                                <i class="fas fa-history me-2"></i>Upload History
                            </a></li>
                        </ul>
                    </li>
                    {% endif %}
                </ul>
                
                <ul class="navbar-nav">
                    {% if current_user.is_authenticated %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                            <i class="fas fa-user me-1"></i>{{ current_user.username }}
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li><h6 class="dropdown-header">
                                {{ current_user.email }}
                                {% if current_user.is_admin %}
                                <span class="badge bg-primary ms-2">Admin</span>
                                {% endif %}
                            </h6></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="{{ url_for('auth.logout') }}">
                                <i class="fas fa-sign-out-alt me-2"></i>Logout
                            </a></li>
                        </ul>
                    </li>
                    {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ url_for('auth.login') }}">
                            <i class="fas fa-sign-in-alt me-1"></i>Login
                        </a>
                    </li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </nav>

    <!-- Flash Messages -->
        <!-- Flash Messages -->
    {% with messages = get_flashed_messages(with_categories=true) %}
        {% if messages %}
            {% for category, message in messages %}
                {% if "An unexpected error occurred" not in message %}
                    <div class="alert alert-{{ category }} alert-dismissible fade show" role="alert">
                        {{ message }}
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                {% endif %}
            {% endfor %}
        {% endif %}
    {% endwith %}



    <!-- Main Content -->
    <main class="container mt-4">
        {% block content %}{% endblock %}
    </main>

    <!-- Footer -->
    <footer class="bg-dark text-light py-4 mt-5">
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <h5>Financial Data Management</h5>
                    <p class="text-muted">Comprehensive financial movement tracking and analysis.</p>
                </div>
                <div class="col-md-6 text-end">
                    <p class="text-muted">
                        &copy; 2025 Financial Data Management System
                    </p>
                </div>
            </div>
        </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- jQuery -->
    <script src="https://code.jquery.com/jquery-3.7.0.min.js"></script>
    
    <!-- DataTables JS -->
    <script src="https://cdn.datatables.net/1.13.6/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/1.13.6/js/dataTables.bootstrap5.min.js"></script>
    
    <!-- Custom JS -->
    <script src="{{ url_for('static', filename='js/main.js') }}"></script>
    
    {% block scripts %}{% endblock %}
</body>
</html>


----- FILE: templates/index.html  (sha1:a62c87b17a05, bytes:8231) -----
{% extends "base.html" %}

{% block title %}Home - Financial Data Management{% endblock %}

{% block content %}
<div class="row">
    <div class="col-12">
        <div class="jumbotron bg-primary text-white rounded p-5 mb-4">
            <h1 class="display-4">
                <i class="fas fa-chart-line me-3"></i>
                Financial Data Management
            </h1>
            <p class="lead">
                Comprehensive system for managing and analyzing financial movement data
            </p>
            {% if not current_user.is_authenticated %}
            <a class="btn btn-light btn-lg" href="{{ url_for('auth.login') }}" role="button">
                <i class="fas fa-sign-in-alt me-2"></i>Get Started
            </a>
            {% endif %}
        </div>
    </div>
</div>

{% if current_user.is_authenticated %}
<div class="row mb-4">
    <div class="col-md-4">
        <div class="card">
            <div class="card-body text-center">
                <i class="fas fa-list fa-3x text-primary mb-3"></i>
                <h5 class="card-title">View Movements</h5>
                <p class="card-text">Browse and search through financial movement records</p>
                <a href="{{ url_for('main.movements') }}" class="btn btn-primary">
                    <i class="fas fa-arrow-right me-2"></i>Browse
                </a>
            </div>
        </div>
    </div>
    
    {% if current_user.is_admin %}
    <div class="col-md-4">
        <div class="card">
            <div class="card-body text-center">
                <i class="fas fa-upload fa-3x text-success mb-3"></i>
                <h5 class="card-title">Upload Data</h5>
                <p class="card-text">Import financial data from Excel files</p>
                <a href="{{ url_for('admin.upload_file') }}" class="btn btn-success">
                    <i class="fas fa-upload me-2"></i>Upload
                </a>
            </div>
        </div>
    </div>
    
    <div class="col-md-4">
        <div class="card">
            <div class="card-body text-center">
                <i class="fas fa-users fa-3x text-info mb-3"></i>
                <h5 class="card-title">Manage Users</h5>
                <p class="card-text">Create and manage user accounts</p>
                <a href="{{ url_for('admin.users') }}" class="btn btn-info">
                    <i class="fas fa-cog me-2"></i>Manage
                </a>
            </div>
        </div>
    </div>
    {% endif %}
</div>

{% if movements %}
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header d-flex justify-content-between align-items-center">
                <h5 class="mb-0">
                    <i class="fas fa-clock me-2"></i>Recent Movements
                </h5>
                <a href="{{ url_for('main.movements') }}" class="btn btn-outline-primary btn-sm">
                    View All
                </a>
            </div>
            <div class="card-body p-0">
                <div class="table-responsive">
                    <table class="table table-hover mb-0">
                        <thead class="table-dark">
                            <tr>
                                <th>N° do documento fiscal</th>
                                <th>Cliente/Fornecedor</th>
                                <th>Valor total da nota</th>
                                <th>Data</th>
                                <th>Ações</th>
                                <th>ID interno</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for movement in movements %}
                            <tr>
                                <td>{{ movement.number or '-' }}</td>
                                <td>
                                    {% if movement.customer_vendor_name and movement.customer_vendor_cnpj %}
                                        {{ movement.customer_vendor_name }}<br>
                                        <small class="text-muted">CNPJ: {{ movement.customer_vendor_cnpj }}</small>
                                    {% elif movement.aux_customer_vendor_code %}
                                        <strong>{{ movement.aux_customer_vendor_code }}</strong>
                                    {% else %}
                                        -
                                    {% endif %}
                                </td>
                                <td>
                                    {% if movement.gross_value is not none %}
                                        <span class="badge bg-success">
                                            {{ movement.net_value_currency_code or 'R$' }} {{ "%.2f"|format(movement.gross_value) }}
                                        </span>
                                    {% else %}
                                        -
                                    {% endif %}
                                </td>
                                <td>
                                    {% if movement.date %}
                                        {{ movement.date.strftime('%d/%m/%Y') }}
                                    {% else %}
                                        -
                                    {% endif %}
                                </td>
                                <td>
                                    <a href="{{ url_for('main.movement_detail', id=movement.id) }}" class="btn btn-outline-primary btn-sm">
                                        <i class="fas fa-eye me-1"></i>Ver
                                    </a>
                                </td>
                                <td><code>{{ movement.internal_id or '-' }}</code></td>
                            </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
{% endif %}

{% else %}
<!-- Not authenticated content -->
<div class="row">
    <div class="col-lg-6">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">
                    <i class="fas fa-database me-2 text-primary"></i>
                    Data Management
                </h5>
                <p class="card-text">
                    Efficiently manage large volumes of financial movement data with advanced search and filtering capabilities.
                </p>
                <ul class="list-unstyled">
                    <li><i class="fas fa-check text-success me-2"></i>Bulk Excel file uploads</li>
                    <li><i class="fas fa-check text-success me-2"></i>Complex data structure support</li>
                    <li><i class="fas fa-check text-success me-2"></i>Real-time data validation</li>
                </ul>
            </div>
        </div>
    </div>
    <div class="col-lg-6">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">
                    <i class="fas fa-analytics me-2 text-info"></i>
                    Advanced Analytics
                </h5>
                <p class="card-text">
                    Comprehensive view of financial movements with detailed breakdowns and nested data visualization.
                </p>
                <ul class="list-unstyled">
                    <li><i class="fas fa-check text-success me-2"></i>Detailed movement records</li>
                    <li><i class="fas fa-check text-success me-2"></i>JSON field parsing</li>
                    <li><i class="fas fa-check text-success me-2"></i>Export capabilities</li>
                </ul>
            </div>
        </div>
    </div>
</div>

<div class="text-center mt-4">
    <p class="lead">Ready to get started?</p>
    <a href="{{ url_for('auth.login') }}" class="btn btn-primary btn-lg">
        <i class="fas fa-sign-in-alt me-2"></i>Login Now
    </a>
</div>
{% endif %}

{% endblock %}


----- FILE: templates/admin/clear_movements.html  (sha1:bedbcbed17dd, bytes:7183) -----
{% extends "base.html" %}

{% block title %}Clear All Movements - Admin{% endblock %}

{% block content %}
<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <h2 class="mb-4">
                <i class="fas fa-exclamation-triangle text-warning me-2"></i>
                Clear All Financial Movements
            </h2>
        </div>
    </div>
    
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="alert alert-danger">
                <h5 class="alert-heading">
                    <i class="fas fa-skull-crossbones me-2"></i>
                    DANGER ZONE
                </h5>
                <p class="mb-0">This action will permanently delete ALL financial movement data and upload history from the database. This action cannot be undone!</p>
            </div>
            
            <div class="card border-danger">
                <div class="card-header bg-danger text-white">
                    <h5 class="mb-0">
                        <i class="fas fa-database me-2"></i>
                        Database Status
                    </h5>
                </div>
                <div class="card-body">
                    <div class="row text-center">
                        <div class="col-md-6">
                            <div class="card bg-primary bg-opacity-10 border-primary">
                                <div class="card-body">
                                    <h2 class="text-primary">{{ movement_count }}</h2>
                                    <p class="mb-0">Financial Movements</p>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="card bg-info bg-opacity-10 border-info">
                                <div class="card-body">
                                    <h2 class="text-info">{{ upload_count }}</h2>
                                    <p class="mb-0">Upload Records</p>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    {% if movement_count == 0 and upload_count == 0 %}
                    <div class="alert alert-success mt-3">
                        <i class="fas fa-check-circle me-2"></i>
                        Database is already empty. No data to delete.
                    </div>
                    {% else %}
                    <form method="POST" class="mt-4">
                        <div class="alert alert-warning">
                            <h6 class="alert-heading">Confirmation Required</h6>
                            <p class="mb-3">
                                To confirm deletion of ALL financial movement data, type exactly:
                                <strong class="text-danger">"delete all movements"</strong>
                            </p>
                            
                            <div class="form-group">
                                <input type="text" 
                                       name="confirmation" 
                                       class="form-control form-control-lg" 
                                       placeholder="Type: delete all movements"
                                       required
                                       autocomplete="off">
                            </div>
                        </div>
                        
                        <div class="d-flex justify-content-between align-items-center">
                            <a href="{{ url_for('admin.dashboard') }}" class="btn btn-secondary">
                                <i class="fas fa-arrow-left me-2"></i>
                                Cancel & Go Back
                            </a>
                            
                            <button type="submit" 
                                    class="btn btn-danger btn-lg"
                                    onclick="return confirm('Are you absolutely sure? This will delete ALL financial movement data permanently!')">
                                <i class="fas fa-trash-alt me-2"></i>
                                DELETE ALL MOVEMENTS
                            </button>
                        </div>
                    </form>
                    {% endif %}
                </div>
            </div>
            
            <div class="card mt-4">
                <div class="card-header">
                    <h6 class="mb-0">
                        <i class="fas fa-info-circle me-2"></i>
                        What will be deleted?
                    </h6>
                </div>
                <div class="card-body">
                    <ul class="list-unstyled">
                        <li class="mb-2">
                            <i class="fas fa-times text-danger me-2"></i>
                            All financial movement records ({{ movement_count }} records)
                        </li>
                        <li class="mb-2">
                            <i class="fas fa-times text-danger me-2"></i>
                            All upload history records ({{ upload_count }} records)
                        </li>
                        <li class="mb-2">
                            <i class="fas fa-times text-danger me-2"></i>
                            All batch tracking information
                        </li>
                    </ul>
                    
                    <h6 class="text-success mt-3">What will be kept?</h6>
                    <ul class="list-unstyled">
                        <li class="mb-2">
                            <i class="fas fa-check text-success me-2"></i>
                            All user accounts and login information
                        </li>
                        <li class="mb-2">
                            <i class="fas fa-check text-success me-2"></i>
                            System configuration and settings
                        </li>
                        <li class="mb-2">
                            <i class="fas fa-check text-success me-2"></i>
                            Database structure (tables will remain, just empty)
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block scripts %}
<script>
$(document).ready(function() {
    // Add visual feedback when typing confirmation
    $('input[name="confirmation"]').on('input', function() {
        const value = $(this).val().toLowerCase().trim();
        const submitBtn = $('button[type="submit"]');
        
        if (value === 'delete all movements') {
            $(this).removeClass('is-invalid').addClass('is-valid');
            submitBtn.prop('disabled', false);
        } else {
            $(this).removeClass('is-valid').addClass('is-invalid');
            submitBtn.prop('disabled', true);
        }
    });
    
    // Initially disable submit button
    $('button[type="submit"]').prop('disabled', true);
});
</script>
{% endblock %}

----- FILE: templates/admin/dashboard.html  (sha1:b63c6f02a3ee, bytes:10688) -----
{% extends "base.html" %}

{% block title %}Admin Dashboard - Financial Data Management{% endblock %}

{% block content %}
<div class="d-flex justify-content-between align-items-center mb-4">
    <h1>
        <i class="fas fa-tachometer-alt me-3"></i>
        Admin Dashboard
    </h1>
</div>

<!-- Statistics Cards -->
<div class="row mb-4">
    <div class="col-md-3">
        <div class="card text-white bg-primary">
            <div class="card-body">
                <div class="d-flex justify-content-between">
                    <div>
                        <h5 class="card-title">Total Users</h5>
                        <h2>{{ stats.total_users }}</h2>
                    </div>
                    <div class="align-self-center">
                        <i class="fas fa-users fa-2x"></i>
                    </div>
                </div>
            </div>
            <div class="card-footer">
                <a href="{{ url_for('admin.users') }}" class="text-white text-decoration-none">
                    View Details <i class="fas fa-arrow-right"></i>
                </a>
            </div>
        </div>
    </div>
    
    <div class="col-md-3">
        <div class="card text-white bg-success">
            <div class="card-body">
                <div class="d-flex justify-content-between">
                    <div>
                        <h5 class="card-title">Total Movements</h5>
                        <h2>{{ "{:,}".format(stats.total_movements) }}</h2>
                    </div>
                    <div class="align-self-center">
                        <i class="fas fa-chart-line fa-2x"></i>
                    </div>
                </div>
            </div>
            <div class="card-footer">
                <a href="{{ url_for('main.movements') }}" class="text-white text-decoration-none">
                    View Details <i class="fas fa-arrow-right"></i>
                </a>
            </div>
        </div>
    </div>
    
    <div class="col-md-3">
        <div class="card text-white bg-info">
            <div class="card-body">
                <div class="d-flex justify-content-between">
                    <div>
                        <h5 class="card-title">Total Uploads</h5>
                        <h2>{{ stats.total_uploads }}</h2>
                    </div>
                    <div class="align-self-center">
                        <i class="fas fa-upload fa-2x"></i>
                    </div>
                </div>
            </div>
            <div class="card-footer">
                <a href="{{ url_for('admin.upload_history') }}" class="text-white text-decoration-none">
                    View Details <i class="fas fa-arrow-right"></i>
                </a>
            </div>
        </div>
    </div>
    
    <div class="col-md-3">
        <div class="card text-white bg-warning">
            <div class="card-body">
                <div class="d-flex justify-content-between">
                    <div>
                        <h5 class="card-title">Quick Actions</h5>
                        <p class="mb-0">Manage System</p>
                    </div>
                    <div class="align-self-center">
                        <i class="fas fa-cogs fa-2x"></i>
                    </div>
                </div>
            </div>
            <div class="card-footer">
                <div class="d-flex justify-content-between align-items-center">
                    <a href="{{ url_for('admin.upload_file') }}" class="text-white text-decoration-none">
                        Upload Data <i class="fas fa-arrow-right"></i>
                    </a>
                    <a href="{{ url_for('admin.clear_movements') }}" class="text-white text-decoration-none small" title="Clear All Movements">
                        <i class="fas fa-trash-alt"></i>
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Recent Uploads -->
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header d-flex justify-content-between align-items-center">
                <h5 class="mb-0">
                    <i class="fas fa-clock me-2"></i>Recent Uploads
                </h5>
                <a href="{{ url_for('admin.upload_history') }}" class="btn btn-outline-primary btn-sm">
                    View All
                </a>
            </div>
            <div class="card-body">
                {% if stats.recent_uploads %}
                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th>Filename</th>
                                <th>Total Records</th>
                                <th>Successful</th>
                                <th>Failed</th>
                                <th>Uploaded</th>
                                <th>Status</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for upload in stats.recent_uploads %}
                            <tr>
                                <td>
                                    <i class="fas fa-file-excel text-success me-2"></i>
                                    {{ upload.filename }}
                                </td>
                                <td>{{ upload.total_rows }}</td>
                                <td>
                                    <span class="badge bg-success">{{ upload.success_count }}</span>
                                </td>
                                <td>
                                    {% if upload.error_count > 0 %}
                                    <span class="badge bg-danger">{{ upload.error_count }}</span>
                                    {% else %}
                                    <span class="badge bg-secondary">0</span>
                                    {% endif %}
                                </td>
                                <td>
                                    <small class="text-muted">
                                        {{ upload.uploaded_at.strftime('%Y-%m-%d %H:%M') }}
                                        <br>
                                        by {{ upload.uploader.username }}
                                    </small>
                                </td>
                                <td>
                                    {% if upload.error_count == 0 %}
                                    <span class="badge bg-success">
                                        <i class="fas fa-check me-1"></i>Success
                                    </span>
                                    {% else %}
                                    <span class="badge bg-warning">
                                        <i class="fas fa-exclamation-triangle me-1"></i>Partial
                                    </span>
                                    {% endif %}
                                </td>
                                <td>
                                    <a href="{{ url_for('admin.upload_history_detail', id=upload.id) }}" 
                                       class="btn btn-outline-primary btn-sm">
                                        <i class="fas fa-eye me-1"></i>View
                                    </a>
                                </td>
                            </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
                {% else %}
                <div class="text-center py-4">
                    <i class="fas fa-inbox fa-3x text-muted mb-3"></i>
                    <p class="text-muted">No uploads yet. Start by uploading your first Excel file!</p>
                    <a href="{{ url_for('admin.upload_file') }}" class="btn btn-primary">
                        <i class="fas fa-upload me-2"></i>Upload Data
                    </a>
                </div>
                {% endif %}
            </div>
        </div>
    </div>
</div>

<!-- Quick Actions -->
<div class="row mt-4">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0">
                    <i class="fas fa-bolt me-2"></i>Quick Actions
                </h5>
            </div>
            <div class="card-body">
                <div class="row">
                    <div class="col-md-4">
                        <div class="d-grid">
                            <a href="{{ url_for('admin.users', action='create') }}" class="btn btn-outline-primary">
                                <i class="fas fa-user-plus me-2"></i>Create New User
                            </a>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="d-grid">
                            <a href="{{ url_for('admin.upload_file') }}" class="btn btn-outline-success">
                                <i class="fas fa-upload me-2"></i>Upload Excel File
                            </a>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="d-grid">
                            <a href="{{ url_for('main.movements') }}" class="btn btn-outline-info">
                                <i class="fas fa-search me-2"></i>Search Movements
                            </a>
                        </div>
                    </div>
                </div>
                
                <!-- Danger Zone -->
                <div class="row mt-3">
                    <div class="col-12">
                        <div class="alert alert-danger">
                            <h6 class="alert-heading mb-2">
                                <i class="fas fa-exclamation-triangle me-2"></i>Danger Zone
                            </h6>
                            <div class="d-flex justify-content-between align-items-center">
                                <span class="small">Clear all financial movement data from the database</span>
                                <a href="{{ url_for('admin.clear_movements') }}" class="btn btn-danger btn-sm">
                                    <i class="fas fa-trash-alt me-2"></i>Clear All Movements
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}


----- FILE: templates/admin/upload.html  (sha1:0749c5176658, bytes:16961) -----
{% extends "base.html" %}

{% block title %}Data Upload - Financial Data Management{% endblock %}

{% block content %}
<div class="d-flex justify-content-between align-items-center mb-4">
    <h1>
        <i class="fas fa-upload me-3"></i>
        {% if show_detail %}
        Upload Details
        {% elif show_history %}
        Upload History
        {% else %}
        Upload Data
        {% endif %}
    </h1>
    {% if show_history or show_detail %}
    <a href="{{ url_for('admin.upload_file') }}" class="btn btn-primary">
        <i class="fas fa-upload me-2"></i>Upload New File
    </a>
    {% elif not show_detail %}
    <a href="{{ url_for('admin.upload_history') }}" class="btn btn-outline-secondary">
        <i class="fas fa-history me-2"></i>Upload History
    </a>
    {% endif %}
</div>

{% if show_detail and upload %}
<!-- Upload Detail View -->
<div class="row mb-4">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0">
                    <i class="fas fa-file-excel text-success me-2"></i>
                    {{ upload.filename }}
                </h5>
            </div>
            <div class="card-body">
                <div class="row">
                    <div class="col-md-3">
                        <div class="card bg-light">
                            <div class="card-body text-center">
                                <h4 class="text-primary">{{ upload.total_records }}</h4>
                                <p class="mb-0">Total Records</p>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="card bg-light">
                            <div class="card-body text-center">
                                <h4 class="text-success">{{ upload.successful_records }}</h4>
                                <p class="mb-0">Successful</p>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="card bg-light">
                            <div class="card-body text-center">
                                <h4 class="text-danger">{{ upload.failed_records }}</h4>
                                <p class="mb-0">Failed</p>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="card bg-light">
                            <div class="card-body text-center">
                                <h4 class="text-info">{{ upload.uploaded_at.strftime('%H:%M') }}</h4>
                                <p class="mb-0">{{ upload.uploaded_at.strftime('%Y-%m-%d') }}</p>
                            </div>
                        </div>
                    </div>
                </div>
                
                <div class="mt-3">
                    <strong>Uploaded by:</strong> {{ upload.uploader.username }} ({{ upload.uploader.email }})
                </div>
                
                {% if upload.error_log %}
                <div class="mt-3">
                    <h6>Error Log:</h6>
                    <div class="alert alert-danger">
                        <pre class="mb-0" style="white-space: pre-wrap;">{{ upload.error_log }}</pre>
                    </div>
                </div>
                {% endif %}
            </div>
        </div>
    </div>
</div>

<!-- Associated Movements -->
{% if movements %}
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0">
                    <i class="fas fa-list me-2"></i>
                    Uploaded Movements ({{ movements|length }})
                </h5>
            </div>
            <div class="card-body p-0">
                <div class="table-responsive">
                    <table class="table table-hover mb-0" id="movementsTable">
                        <thead class="table-dark">
                            <tr>
                                <th>Internal ID</th>
                                <th>Number</th>
                                <th>Customer/Vendor</th>
                                <th>Net Value</th>
                                <th>Date</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for movement in movements %}
                            <tr>
                                <td><code>{{ movement.internal_id or '-' }}</code></td>
                                <td>{{ movement.number or '-' }}</td>
                                <td>{{ movement.aux_customer_vendor_code or '-' }}</td>
                                <td>
                                    {% if movement.net_value %}
                                    <span class="badge bg-success">
                                        {{ movement.net_value_currency_code or 'R$' }} {{ "%.2f"|format(movement.net_value) }}
                                    </span>
                                    {% else %}
                                    -
                                    {% endif %}
                                </td>
                                <td>
                                    {% if movement.date %}
                                    {{ movement.date.strftime('%Y-%m-%d') }}
                                    {% else %}
                                    -
                                    {% endif %}
                                </td>
                                <td>
                                    <a href="{{ url_for('main.movement_detail', id=movement.id) }}" class="btn btn-outline-primary btn-sm">
                                        <i class="fas fa-eye me-1"></i>View
                                    </a>
                                </td>
                            </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
{% endif %}

{% elif show_history %}
<!-- Upload History -->
<div class="card">
    <div class="card-body">
        {% if uploads.items %}
        <div class="table-responsive">
            <table class="table table-hover" id="uploadsTable">
                <thead class="table-dark">
                    <tr>
                        <th>Filename</th>
                        <th>Total Records</th>
                        <th>Successful</th>
                        <th>Failed</th>
                        <th>Uploaded By</th>
                        <th>Date</th>
                        <th>Status</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for upload in uploads.items %}
                    <tr>
                        <td>
                            <i class="fas fa-file-excel text-success me-2"></i>
                            {{ upload.filename }}
                        </td>
                        <td>{{ upload.total_rows }}</td>
                        <td>
                            <span class="badge bg-success">{{ upload.success_count }}</span>
                        </td>
                        <td>
                            {% if upload.error_count > 0 %}
                            <span class="badge bg-danger">{{ upload.error_count }}</span>
                            {% else %}
                            <span class="badge bg-secondary">0</span>
                            {% endif %}
                        </td>
                        <td>
                            <small>
                                {{ upload.uploader.username }}
                                <br>
                                <span class="text-muted">{{ upload.uploader.email }}</span>
                            </small>
                        </td>
                        <td>
                            <small class="text-muted">
                                {{ upload.uploaded_at.strftime('%Y-%m-%d') }}
                                <br>
                                {{ upload.uploaded_at.strftime('%H:%M:%S') }}
                            </small>
                        </td>
                        <td>
                            {% if upload.error_count == 0 %}
                            <span class="badge bg-success">
                                <i class="fas fa-check me-1"></i>Success
                            </span>
                            {% else %}
                            <span class="badge bg-warning">
                                <i class="fas fa-exclamation-triangle me-1"></i>Partial
                            </span>
                            {% endif %}
                        </td>
                        <td>
                            <a href="{{ url_for('admin.upload_history_detail', id=upload.id) }}" 
                               class="btn btn-outline-primary btn-sm">
                                <i class="fas fa-eye me-1"></i>Details
                            </a>
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
        
        <!-- Pagination -->
        {% if uploads.pages > 1 %}
        <nav aria-label="Uploads pagination" class="mt-3">
            <ul class="pagination justify-content-center">
                {% if uploads.has_prev %}
                <li class="page-item">
                    <a class="page-link" href="{{ url_for('admin.upload_history', page=uploads.prev_num) }}">Previous</a>
                </li>
                {% endif %}
                
                {% for page_num in uploads.iter_pages() %}
                    {% if page_num %}
                        {% if page_num != uploads.page %}
                        <li class="page-item">
                            <a class="page-link" href="{{ url_for('admin.upload_history', page=page_num) }}">{{ page_num }}</a>
                        </li>
                        {% else %}
                        <li class="page-item active">
                            <span class="page-link">{{ page_num }}</span>
                        </li>
                        {% endif %}
                    {% else %}
                    <li class="page-item disabled">
                        <span class="page-link">...</span>
                    </li>
                    {% endif %}
                {% endfor %}
                
                {% if uploads.has_next %}
                <li class="page-item">
                    <a class="page-link" href="{{ url_for('admin.upload_history', page=uploads.next_num) }}">Next</a>
                </li>
                {% endif %}
            </ul>
        </nav>
        {% endif %}
        
        {% else %}
        <div class="text-center py-4">
            <i class="fas fa-inbox fa-3x text-muted mb-3"></i>
            <p class="text-muted">No uploads yet.</p>
            <a href="{{ url_for('admin.upload_file') }}" class="btn btn-primary">
                <i class="fas fa-upload me-2"></i>Upload First File
            </a>
        </div>
        {% endif %}
    </div>
</div>

{% else %}
<!-- File Upload Form -->
<div class="row">
    <div class="col-md-8">
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0">
                    <i class="fas fa-file-upload me-2"></i>
                    Upload Excel File
                </h5>
            </div>
            <div class="card-body">
                <form method="POST" enctype="multipart/form-data">
                    {{ form.hidden_tag() }}
                    
                    <div class="mb-3">
                        {{ form.file.label(class="form-label") }}
                        {{ form.file(class="form-control", accept=".xlsx,.xls") }}
                        {% if form.file.errors %}
                        <div class="text-danger mt-1">
                            {% for error in form.file.errors %}
                            <small>{{ error }}</small><br>
                            {% endfor %}
                        </div>
                        {% endif %}
                        <div class="form-text">
                            Only Excel files (.xlsx, .xls) are allowed. Maximum file size: 50MB.
                        </div>
                    </div>
                    
                    <div class="mb-3">
                        {{ form.description.label(class="form-label") }}
                        {{ form.description(class="form-control", rows="3") }}
                        <div class="form-text">
                            Optional description for this upload batch.
                        </div>
                    </div>
                    
                    <div class="d-flex gap-2">
                        {{ form.submit(class="btn btn-primary") }}
                        <a href="{{ url_for('admin.dashboard') }}" class="btn btn-secondary">Cancel</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
    
    <div class="col-md-4">
        <div class="card bg-light">
            <div class="card-header">
                <h6 class="mb-0">
                    <i class="fas fa-info-circle me-2"></i>
                    Upload Requirements
                </h6>
            </div>
            <div class="card-body">
                <h6>File Format:</h6>
                <ul class="list-unstyled">
                    <li><i class="fas fa-check text-success me-2"></i>Excel format (.xlsx, .xls)</li>
                    <li><i class="fas fa-check text-success me-2"></i>Maximum 50MB file size</li>
                    <li><i class="fas fa-check text-success me-2"></i>Structured financial data</li>
                </ul>
                
                <h6>Expected Columns:</h6>
                <ul class="list-unstyled small">
                    <li><code>Empresa_Code</code> <span class="text-success">(NEW)</span></li>
                    <li><code>Empresa_Nome</code> <span class="text-success">(NEW)</span></li>
                    <li><code>Filial_Code</code> <span class="text-success">(NEW)</span></li>
                    <li><code>Filial_Nome</code> <span class="text-success">(NEW)</span></li>
                    <li><code>internalId</code></li>
                    <li><code>companyId</code></li>
                    <li><code>movementId</code></li>
                    <li><code>number</code></li>
                    <li><code>netValue</code></li>
                    <li><code>grossValue</code></li>
                    <li class="text-muted">...and 108+ more columns</li>
                </ul>
                
                <div class="alert alert-info mt-3">
                    <i class="fas fa-lightbulb me-2"></i>
                    <strong>Tip:</strong> The system will automatically validate and process your data. Any errors will be logged for review.
                </div>
            </div>
        </div>
    </div>
</div>
{% endif %}

{% endblock %}

{% block scripts %}
<script>
$(document).ready(function() {
    // Initialize DataTables for different tables
    if ($('#uploadsTable').length) {
        // Check if DataTable is already initialized
        if ($.fn.DataTable.isDataTable('#uploadsTable')) {
            $('#uploadsTable').DataTable().destroy();
        }
        
        $('#uploadsTable').DataTable({
            "pageLength": 25,
            "ordering": true,
            "searching": true,
            "info": true,
            "responsive": true,
            "order": [[5, "desc"]] // Sort by date descending
        });
    }
    
    if ($('#movementsTable').length) {
        // Check if DataTable is already initialized
        if ($.fn.DataTable.isDataTable('#movementsTable')) {
            $('#movementsTable').DataTable().destroy();
        }
        
        $('#movementsTable').DataTable({
            "pageLength": 25,
            "ordering": true,
            "searching": true,
            "info": true,
            "responsive": true
        });
    }
    
    // File upload progress (if needed)
    $('form[enctype="multipart/form-data"]').on('submit', function() {
        const submitBtn = $(this).find('input[type="submit"]');
        submitBtn.prop('disabled', true).val('Uploading...');
        
        // Show loading spinner or progress bar if desired
        $(this).find('.card-body').append('<div class="text-center mt-3"><i class="fas fa-spinner fa-spin"></i> Processing file...</div>');
    });
});
</script>
{% endblock %}


----- FILE: templates/admin/users.html  (sha1:2b4b1fa94301, bytes:11580) -----
{% extends "base.html" %}

{% block title %}User Management - Financial Data Management{% endblock %}

{% block content %}
<div class="d-flex justify-content-between align-items-center mb-4">
    <h1>
        <i class="fas fa-users me-3"></i>
        User Management
    </h1>
    {% if not (form and action) %}
    <a href="{{ url_for('admin.create_user') }}" class="btn btn-primary">
        <i class="fas fa-user-plus me-2"></i>Add User
    </a>
    {% endif %}
</div>

{% if form and action %}
<!-- User Form (Create/Edit) -->
<div class="row">
    <div class="col-md-8">
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0">
                    {% if action == 'create' %}
                    <i class="fas fa-user-plus me-2"></i>Create New User
                    {% else %}
                    <i class="fas fa-user-edit me-2"></i>Edit User: {{ user.username }}
                    {% endif %}
                </h5>
            </div>
            <div class="card-body">
                <form method="POST">
                    {{ form.hidden_tag() }}
                    
                    <div class="row">
                        <div class="col-md-6">
                            <div class="mb-3">
                                {{ form.username.label(class="form-label") }}
                                {{ form.username(class="form-control") }}
                                {% if form.username.errors %}
                                <div class="text-danger">
                                    {% for error in form.username.errors %}
                                    <small>{{ error }}</small><br>
                                    {% endfor %}
                                </div>
                                {% endif %}
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="mb-3">
                                {{ form.email.label(class="form-label") }}
                                {{ form.email(class="form-control") }}
                                {% if form.email.errors %}
                                <div class="text-danger">
                                    {% for error in form.email.errors %}
                                    <small>{{ error }}</small><br>
                                    {% endfor %}
                                </div>
                                {% endif %}
                            </div>
                        </div>
                    </div>
                    
                    {% if action == 'create' %}
                    <div class="row">
                        <div class="col-md-6">
                            <div class="mb-3">
                                {{ form.password.label(class="form-label") }}
                                {{ form.password(class="form-control") }}
                                {% if form.password.errors %}
                                <div class="text-danger">
                                    {% for error in form.password.errors %}
                                    <small>{{ error }}</small><br>
                                    {% endfor %}
                                </div>
                                {% endif %}
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="mb-3">
                                {{ form.password2.label(class="form-label") }}
                                {{ form.password2(class="form-control") }}
                                {% if form.password2.errors %}
                                <div class="text-danger">
                                    {% for error in form.password2.errors %}
                                    <small>{{ error }}</small><br>
                                    {% endfor %}
                                </div>
                                {% endif %}
                            </div>
                        </div>
                    </div>
                    {% endif %}
                    
                    <div class="mb-3">
                        <div class="form-check">
                            {{ form.is_admin(class="form-check-input") }}
                            {{ form.is_admin.label(class="form-check-label") }}
                        </div>
                        <small class="form-text text-muted">
                            Administrator users can manage other users and upload data files.
                        </small>
                    </div>
                    
                    <div class="d-flex gap-2">
                        {{ form.submit(class="btn btn-primary") }}
                        <a href="{{ url_for('admin.users') }}" class="btn btn-secondary">Cancel</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

{% else %}
<!-- Users List -->
<div class="card">
    <div class="card-body">
        {% if users.items %}
        <div class="table-responsive">
            <table class="table table-hover" id="usersTable">
                <thead class="table-dark">
                    <tr>
                        <th>Username</th>
                        <th>Email</th>
                        <th>Role</th>
                        <th>Created</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for user in users.items %}
                    <tr>
                        <td>
                            <i class="fas fa-user me-2"></i>
                            {{ user.username }}
                        </td>
                        <td>{{ user.email }}</td>
                        <td>
                            {% if user.is_admin %}
                            <span class="badge bg-primary">
                                <i class="fas fa-crown me-1"></i>Administrator
                            </span>
                            {% else %}
                            <span class="badge bg-secondary">
                                <i class="fas fa-user me-1"></i>User
                            </span>
                            {% endif %}
                        </td>
                        <td>
                            <small class="text-muted">
                                {{ user.created_at.strftime('%Y-%m-%d %H:%M') }}
                            </small>
                        </td>
                        <td>
                            <div class="btn-group btn-group-sm" role="group">
                                <a href="{{ url_for('admin.edit_user', id=user.id) }}" 
                                   class="btn btn-outline-primary">
                                    <i class="fas fa-edit"></i>
                                </a>
                                {% if user.id != current_user.id %}
                                <button type="button" class="btn btn-outline-danger" 
                                        data-bs-toggle="modal" 
                                        data-bs-target="#deleteModal{{ user.id }}">
                                    <i class="fas fa-trash"></i>
                                </button>
                                {% endif %}
                            </div>
                        </td>
                    </tr>
                    
                    <!-- Delete Modal -->
                    {% if user.id != current_user.id %}
                    <div class="modal fade" id="deleteModal{{ user.id }}" tabindex="-1">
                        <div class="modal-dialog">
                            <div class="modal-content">
                                <div class="modal-header">
                                    <h5 class="modal-title">Confirm Delete</h5>
                                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                                </div>
                                <div class="modal-body">
                                    <p>Are you sure you want to delete user <strong>{{ user.username }}</strong>?</p>
                                    <div class="alert alert-warning">
                                        <i class="fas fa-exclamation-triangle me-2"></i>
                                        This action cannot be undone.
                                    </div>
                                </div>
                                <div class="modal-footer">
                                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                                    <form method="POST" action="{{ url_for('admin.delete_user', id=user.id) }}" class="d-inline">
                                        <button type="submit" class="btn btn-danger">Delete</button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    </div>
                    {% endif %}
                    {% endfor %}
                </tbody>
            </table>
        </div>
        
        <!-- Pagination -->
        {% if users.pages > 1 %}
        <nav aria-label="Users pagination">
            <ul class="pagination justify-content-center">
                {% if users.has_prev %}
                <li class="page-item">
                    <a class="page-link" href="{{ url_for('admin.users', page=users.prev_num) }}">Previous</a>
                </li>
                {% endif %}
                
                {% for page_num in users.iter_pages() %}
                    {% if page_num %}
                        {% if page_num != users.page %}
                        <li class="page-item">
                            <a class="page-link" href="{{ url_for('admin.users', page=page_num) }}">{{ page_num }}</a>
                        </li>
                        {% else %}
                        <li class="page-item active">
                            <span class="page-link">{{ page_num }}</span>
                        </li>
                        {% endif %}
                    {% else %}
                    <li class="page-item disabled">
                        <span class="page-link">...</span>
                    </li>
                    {% endif %}
                {% endfor %}
                
                {% if users.has_next %}
                <li class="page-item">
                    <a class="page-link" href="{{ url_for('admin.users', page=users.next_num) }}">Next</a>
                </li>
                {% endif %}
            </ul>
        </nav>
        {% endif %}
        
        {% else %}
        <div class="text-center py-4">
            <i class="fas fa-users fa-3x text-muted mb-3"></i>
            <p class="text-muted">No users found.</p>
            <a href="{{ url_for('admin.create_user') }}" class="btn btn-primary">
                <i class="fas fa-user-plus me-2"></i>Create First User
            </a>
        </div>
        {% endif %}
    </div>
</div>
{% endif %}
{% endblock %}

{% block scripts %}
<script>
$(document).ready(function() {
    $('#usersTable').DataTable({
        "pageLength": 25,
        "ordering": true,
        "searching": true,
        "info": true,
        "responsive": true
    });
});
</script>
{% endblock %}


----- FILE: templates/auth/login.html  (sha1:6ba36fac4b54, bytes:5697) -----
{% extends "base.html" %}

{% block title %}Login - Financial Data Management{% endblock %}

{% block content %}
<div class="row justify-content-center">
    <div class="col-md-6 col-lg-4">
        <div class="card">
            <div class="card-header text-center">
                <h4 class="mb-0">
                    <i class="fas fa-sign-in-alt me-2"></i>
                    Sign In
                </h4>
            </div>
            <div class="card-body">
                <form method="POST">
                    {{ form.hidden_tag() }}
                    
                    <div class="mb-3">
                        {{ form.username.label(class="form-label") }}
                        {{ form.username(class="form-control", placeholder="Enter your username") }}
                        {% if form.username.errors %}
                        <div class="text-danger mt-1">
                            {% for error in form.username.errors %}
                            <small>{{ error }}</small><br>
                            {% endfor %}
                        </div>
                        {% endif %}
                    </div>
                    
                    <div class="mb-3">
                        {{ form.password.label(class="form-label") }}
                        {{ form.password(class="form-control", placeholder="Enter your password") }}
                        {% if form.password.errors %}
                        <div class="text-danger mt-1">
                            {% for error in form.password.errors %}
                            <small>{{ error }}</small><br>
                            {% endfor %}
                        </div>
                        {% endif %}
                    </div>
                    
                    <div class="mb-3 form-check">
                        {{ form.remember_me(class="form-check-input") }}
                        {{ form.remember_me.label(class="form-check-label") }}
                    </div>
                    
                    <div class="d-grid">
                        {{ form.submit(class="btn btn-primary") }}
                    </div>
                </form>
            </div>
            <div class="card-footer text-center">
                <small class="text-muted">
                    <i class="fas fa-info-circle me-1"></i>
                    Contact your administrator for account access
                </small>
            </div>
        </div>
        
        <!-- Demo Credentials Info -->
        <div class="card mt-3">
            <div class="card-body">
                <h6 class="card-title">
                    <i class="fas fa-key me-2"></i>
                    Default Admin Access
                </h6>
                <p class="card-text">
                    <strong>Username:</strong> <code>admin</code><br>
                    <strong>Password:</strong> <code>admin123</code>
                </p>
                <div class="alert alert-warning">
                    <i class="fas fa-exclamation-triangle me-2"></i>
                    <strong>Important:</strong> Change the default password after first login.
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Additional Info -->
<div class="row justify-content-center mt-4">
    <div class="col-md-8">
        <div class="card bg-light">
            <div class="card-body text-center">
                <h5 class="card-title">
                    <i class="fas fa-chart-line me-2"></i>
                    Financial Data Management System
                </h5>
                <p class="card-text">
                    Comprehensive platform for managing and analyzing financial movement data.
                    Upload Excel files, manage users, and explore detailed financial records.
                </p>
                <div class="row text-center">
                    <div class="col-md-4">
                        <i class="fas fa-upload fa-2x text-primary mb-2"></i>
                        <p><strong>Bulk Upload</strong><br>Excel file processing</p>
                    </div>
                    <div class="col-md-4">
                        <i class="fas fa-search fa-2x text-success mb-2"></i>
                        <p><strong>Advanced Search</strong><br>Filter and find records</p>
                    </div>
                    <div class="col-md-4">
                        <i class="fas fa-chart-bar fa-2x text-info mb-2"></i>
                        <p><strong>Detailed Analysis</strong><br>JSON data visualization</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block scripts %}
<script>
$(document).ready(function() {
    // Focus username field on page load
    $('#username').focus();
    
    // Form validation feedback
    $('form').on('submit', function(e) {
        const username = $('#username').val().trim();
        const password = $('#password').val();
        
        if (!username || !password) {
            e.preventDefault();
            
            if (!username) {
                $('#username').addClass('is-invalid');
            } else {
                $('#username').removeClass('is-invalid');
            }
            
            if (!password) {
                $('#password').addClass('is-invalid');
            } else {
                $('#password').removeClass('is-invalid');
            }
            
            return false;
        }
    });
    
    // Remove validation classes on input
    $('#username, #password').on('input', function() {
        $(this).removeClass('is-invalid');
    });
});
</script>
{% endblock %}


----- FILE: templates/movements/delete_all.html  (sha1:b4fe26eba296, bytes:5621) -----
{% extends "base.html" %}

{% block title %}Delete All Movements - Financial Data Management{% endblock %}

{% block content %}
<div class="d-flex justify-content-between align-items-center mb-4">
    <h1>
        <i class="fas fa-exclamation-triangle text-danger me-3"></i>
        Delete All Movements
    </h1>
    <a href="{{ url_for('main.movements') }}" class="btn btn-outline-secondary">
        <i class="fas fa-arrow-left me-2"></i>Back to Movements
    </a>
</div>

<!-- Warning Card -->
<div class="card border-danger mb-4">
    <div class="card-header bg-danger text-white">
        <h5 class="mb-0">
            <i class="fas fa-exclamation-triangle me-2"></i>
            DANGER ZONE
        </h5>
    </div>
    <div class="card-body">
        <div class="alert alert-warning" role="alert">
            <h6 class="alert-heading">
                <i class="fas fa-warning me-2"></i>Warning!
            </h6>
            <p class="mb-0">This action will permanently delete <strong>ALL</strong> financial movement data from the system. This includes:</p>
            <ul class="mt-2 mb-0">
                <li><strong>{{ "{:,}".format(movement_count) }} financial movement records</strong></li>
                <li><strong>{{ "{:,}".format(upload_count) }} upload history records</strong></li>
                <li>All associated JSON data (items, taxes, cost centers)</li>
                <li>All file processing history</li>
            </ul>
        </div>
        
        <div class="alert alert-danger" role="alert">
            <h6 class="alert-heading">
                <i class="fas fa-times-circle me-2"></i>This action cannot be undone!
            </h6>
            <p class="mb-0">Once deleted, all data will be permanently lost. Make sure you have backups if needed.</p>
        </div>
    </div>
</div>

<!-- Current Data Summary -->
<div class="card mb-4">
    <div class="card-header">
        <h6 class="mb-0">
            <i class="fas fa-database me-2"></i>Current Database Content
        </h6>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-md-6">
                <div class="d-flex align-items-center p-3 bg-primary bg-opacity-10 rounded">
                    <i class="fas fa-list-alt fa-2x text-primary me-3"></i>
                    <div>
                        <h5 class="mb-0">{{ "{:,}".format(movement_count) }}</h5>
                        <small class="text-muted">Financial Movements</small>
                    </div>
                </div>
            </div>
            <div class="col-md-6">
                <div class="d-flex align-items-center p-3 bg-info bg-opacity-10 rounded">
                    <i class="fas fa-upload fa-2x text-info me-3"></i>
                    <div>
                        <h5 class="mb-0">{{ "{:,}".format(upload_count) }}</h5>
                        <small class="text-muted">Upload Records</small>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

{% if movement_count > 0 or upload_count > 0 %}
<!-- Delete Confirmation Form -->
<div class="card border-danger">
    <div class="card-header bg-danger bg-opacity-10">
        <h6 class="mb-0 text-danger">
            <i class="fas fa-trash me-2"></i>Confirm Deletion
        </h6>
    </div>
    <div class="card-body">
        <form method="POST">
            <div class="mb-3">
                <label for="confirmation" class="form-label">
                    To confirm deletion, type exactly: <code>delete all movements</code>
                </label>
                <input type="text" 
                       class="form-control" 
                       id="confirmation" 
                       name="confirmation" 
                       placeholder="delete all movements"
                       autocomplete="off"
                       required>
            </div>
            
            <div class="d-flex justify-content-between">
                <a href="{{ url_for('main.movements') }}" class="btn btn-secondary">
                    <i class="fas fa-arrow-left me-2"></i>Cancel
                </a>
                <button type="submit" class="btn btn-danger" onclick="return confirmDeletion()">
                    <i class="fas fa-trash me-2"></i>Delete All Data
                </button>
            </div>
        </form>
    </div>
</div>
{% else %}
<!-- No Data Card -->
<div class="card">
    <div class="card-body text-center py-5">
        <i class="fas fa-inbox fa-4x text-muted mb-3"></i>
        <h5 class="text-muted">No Data to Delete</h5>
        <p class="text-muted">The database is already empty.</p>
        <a href="{{ url_for('main.movements') }}" class="btn btn-primary">
            <i class="fas fa-arrow-left me-2"></i>Back to Movements
        </a>
    </div>
</div>
{% endif %}

<script>
function confirmDeletion() {
    const confirmation = document.getElementById('confirmation').value.toLowerCase();
    
    if (confirmation !== 'delete all movements') {
        alert('Please type exactly "delete all movements" to confirm deletion.');
        return false;
    }
    
    return confirm(
        'This will permanently delete ALL financial movement data!\n\n' +
        'Are you absolutely sure you want to continue?\n\n' +
        'This action CANNOT be undone!'
    );
}

// Auto-focus the confirmation input
document.addEventListener('DOMContentLoaded', function() {
    const confirmationInput = document.getElementById('confirmation');
    if (confirmationInput) {
        confirmationInput.focus();
    }
});
</script>
{% endblock %}

----- FILE: templates/movements/detail.html  (sha1:e2318a7077f3, bytes:61591) -----
{% extends "base.html" %}

{% block title %}Movement {{ movement.internal_id or movement.id }} - Financial Data Management{% endblock %}

{% block content %}
<div class="d-flex justify-content-between align-items-center mb-4">
    <h1>
        <i class="fas fa-file-alt me-3"></i>
        Detalhes de movimento
    </h1>
    <div class="btn-group" role="group">
        <a href="{{ url_for('main.movements') }}" class="btn btn-outline-secondary">
            <i class="fas fa-arrow-left me-2"></i>Back to List
        </a>
        <button type="button" class="btn btn-outline-danger" 
                onclick="confirmDelete({{ movement.id }}, '{{ movement.internal_id or movement.id }}')"
                title="Delete Movement">
            <i class="fas fa-trash me-2"></i>Delete
        </button>
    </div>
</div>

<!-- Movement Header -->
<div class="card mb-4">
    <div class="card-header">
        <h5 class="mb-0">
            <i class="fas fa-info-circle me-2"></i>
            {% if movement.internal_id %}
            Movimento {{ movement.internal_id }}
            {% else %}
            Movimento {{ movement.id }}
            {% endif %}
            {% if movement.status %}
            <span class="badge {% if movement.status == 'F' %}bg-success{% elif movement.status == 'N' %}bg-warning{% else %}bg-secondary{% endif %} ms-2">
                {{ movement.status }}
            </span>
            {% endif %}
        </h5>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-md-3">
                <strong>Document Number:</strong>
                <p>{{ movement.number or '-' }}</p>
            </div>
            <div class="col-md-3">
                <strong>Series:</strong>
                <p>
                    {% if movement.series %}
                    <span class="badge bg-info">{{ movement.series }}</span>
                    {% else %}
                    -
                    {% endif %}
                </p>
            </div>
            <div class="col-md-3">
                <strong>N° do movimento:</strong>
                <p>{{ movement.movement_type_code or '-' }}</p>
            </div>
            <div class="col-md-3">
                <strong>Data:</strong>
                <p>{{ movement.date.strftime('%d/%m/%Y') if movement.date else '-' }}</p>
            </div>
        </div>
        
        <!-- Company and Branch Information -->
        <div class="row mt-3">
            <div class="col-md-6">
                <div class="card bg-primary bg-opacity-10 border-primary border-opacity-25">
                    <div class="card-body py-2">
                        <h6 class="card-title text-primary mb-1 fs-6">
                            <i class="fas fa-building me-2"></i>Empresa
                        </h6>
                        <div class="row">
                            <div class="col-8">
                                <small class="text-muted d-block">Razão Social:</small>
                                <strong class="small">{{ movement.customer_vendor_name or '-' }}</strong>
                            </div>
                            <div class="col-4">
                                <small class="text-muted d-block">ID da Empresa:</small>
                                <strong class="small">{{ movement.company_id or '-' }}</strong>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-md-6">
                <div class="card bg-info bg-opacity-10 border-info border-opacity-25">
                    <div class="card-body py-2">
                        <h6 class="card-title text-info mb-1 fs-6">
                            <i class="fas fa-map-marker-alt me-2"></i>Filial
                        </h6>
                        <div class="row">
                            <div class="col-8">
                                <small class="text-muted d-block">Nome da Filial:</small>
                                <strong class="small">{{ movement.filial_nome or '-' }}</strong>
                            </div>
                            <div class="col-4">
                                <small class="text-muted d-block">ID da Filial:</small>
                                <strong class="small">{{ movement.branch_id or '-' }}</strong>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>


        
        {% if movement.observation %}
        <div class="mt-3">
            <strong>Observation:</strong>
            <p class="text-muted">{{ movement.observation }}</p>
        </div>
        {% endif %}
    </div>
</div>

<!-- Informações Financeiras -->
<div class="row mb-4">
    <div class="col-md-6">
        <div class="card h-100">
            <div class="card-header">
                <h6 class="mb-0">
                    <i class="fas fa-dollar-sign me-2"></i>Informações Financeiras
                </h6>
            </div>
            <div class="card-body">
                <div class="row">
                    <div class="col-sm-6">
                        <strong>Valor Bruto:</strong>
                        <p class="h5 text-primary">
                            {{ movement.net_value_currency_code or 'R$' }} {{ "%.2f"|format(movement.gross_value or 0) }}
                        </p>
                    </div>
                    <div class="col-sm-6">
                        <strong>Valor Líquido:</strong>
                        <p class="h5 text-success">
                            {{ movement.net_value_currency_code or 'R$' }} {{ "%.2f"|format(movement.net_value or 0) }}
                        </p>
                    </div>
                </div>
                <hr>
                <div class="row">
                    <div class="col-sm-6">
                        <strong>Desconto (valor):</strong>
                        <p>{{ movement.net_value_currency_code or 'R$' }} {{ "%.2f"|format(movement.discount_percentage_value or 0) }}</p>
                    </div>
                    <div class="col-sm-6">
                        <strong>Despesa (valor):</strong>
                        <p>{{ movement.net_value_currency_code or 'R$' }} {{ "%.2f"|format(movement.expense_value or 0) }}</p>
                    </div>
                </div>
                <div class="row">
                    <div class="col-sm-6">
                        <strong>Frete:</strong>
                        <p>{{ movement.net_value_currency_code or 'R$' }} {{ "%.2f"|format(movement.freight_value or 0) }}</p>
                    </div>
                    <div class="col-sm-6">
                        <strong>Seguro:</strong>
                        <p>{{ movement.net_value_currency_code or 'R$' }} {{ "%.2f"|format(movement.insurance_value or 0) }}</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <div class="col-md-6">
        <div class="card h-100">
            <div class="card-header">
                <h6 class="mb-0">
                    <i class="fas fa-building me-2"></i>Informações da Empresa
                </h6>
            </div>
            <div class="card-body">
                <div class="row">
                    <div class="col-sm-6">
                        <strong>ID da Empresa:</strong>
                        <p>{{ movement.company_id or '-' }}</p>
                    </div>
                    <div class="col-sm-6">
                        <strong>ID da Filial:</strong>
                        <p>{{ movement.branch_id or '-' }}</p>
                    </div>
                </div>
                
                <div class="row">
                    <div class="col-sm-6">
                        <strong>Depósito:</strong>
                        <p>{{ movement.warehouse_code or '-' }}</p>
                    </div>
                    <div class="col-sm-6">
                        <strong>Depósito de Destino:</strong>
                        <p>{{ movement.destiny_warehouse_code or '-' }}</p>
                    </div>
                </div>
                
                <div class="row">
                    
                    <div class="col-sm-6">
                        <strong>Código do Usuário:</strong>
                        <p>{{ movement.user_code or '-' }}</p>
                    </div>
                </div>

                {# --- INÍCIO: Rateio de Centros de Custo dentro de Informações da Empresa --- #}
                {% set cost_center_apportionments = movement.get_json_field('cost_center_apportionments') %}
                {% if cost_center_apportionments and cost_center_apportionments|length > 0 %}

                <hr class="my-3">

                <h6 class="mb-2">
                <i class="fas fa-chart-pie me-2"></i>
                Rateio de Centros de Custo
                <span class="badge bg-secondary ms-2">{{ cost_center_apportionments|length }}</span>
                </h6>

                <div class="table-responsive">
                <table class="table table-sm table-hover">
                    <thead>
                    <tr>
                        <th>Código do Centro de Custo</th>
                        <th>Nome do Centro de Custo</th>
                        <th class="text-end">Valor</th>
                        <th class="text-center">Ações</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for item in cost_center_apportionments %}
                    <tr>
                        <td><code class="text-info">{{ item.costCenterCode }}</code></td>
                        <td>{{ item.costCenterName or '-' }}</td>
                        <td class="text-end">
                        <strong class="text-success">R$ {{ "%.2f"|format(item.value or item.amount or item.totalValue or 0) }}</strong>
                        </td>
                        <td class="text-center">
                        <button class="btn btn-outline-primary btn-sm" data-bs-toggle="modal" data-bs-target="#costCenterModal{{ loop.index }}">
                            <i class="fas fa-eye"></i>
                        </button>
                        </td>
                    </tr>
                    {% endfor %}
                    </tbody>
                </table>
                </div>

                {# Modais podem ficar em qualquer lugar do DOM; incluir aqui para manter o botão funcionando #}
                {% for item in cost_center_apportionments %}
                <div class="modal fade" id="costCenterModal{{ loop.index }}" tabindex="-1">
                <div class="modal-dialog modal-lg">
                    <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title"><i class="fas fa-chart-pie me-2"></i> Detalhes do Rateio de Centro de Custo</h5>
                        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                    </div>
                    <div class="modal-body">
                        <div class="row">
                        <div class="col-md-12">
                            <div class="card border-primary mb-3">
                            <div class="card-header bg-primary text-white">
                                <small><i class="fas fa-info-circle me-1"></i>Informações Fiscais do Item</small>
                            </div>
                            <div class="card-body">
                                <!-- (reaproveite aqui exatamente o conteúdo de campos do seu modal original) -->
                                <div class="row mb-2"><div class="col-sm-6"><strong>Natureza Orçamentária:</strong></div><div class="col-sm-6">{{ item.natureza_orcamentaria or '-' }}</div></div>
                                <div class="row mb-2"><div class="col-sm-6"><strong>NCM:</strong></div><div class="col-sm-6">{{ item.ncm or '-' }}</div></div>
                                <div class="row mb-2"><div class="col-sm-6"><strong>Procedência:</strong></div><div class="col-sm-6">{{ item.procedencia or '-' }}</div></div>
                                <div class="row mb-2"><div class="col-sm-6"><strong>Unidade de Medida:</strong></div><div class="col-sm-6">{{ item.measureUnitCode or '-' }}</div></div>
                                <div class="row mb-2"><div class="col-sm-6"><strong>CFOP de Entrada:</strong></div><div class="col-sm-6">{{ item.cfop_entrada or '-' }}</div></div>
                                <div class="row mb-2"><div class="col-sm-6"><strong>CST:</strong></div><div class="col-sm-6">{{ item.cst or '-' }}</div></div>
                                <div class="row mb-2"><div class="col-sm-6"><strong>Tipo de Tributação:</strong></div><div class="col-sm-6">{{ item.tipo_tributacao or '-' }}</div></div>
                                <div class="row mb-2"><div class="col-sm-6"><strong>Classificação Fiscal:</strong></div><div class="col-sm-6">{{ item.classificacao_fiscal or '-' }}</div></div>
                                <div class="row mb-2"><div class="col-sm-6"><strong>Conta Contábil:</strong></div><div class="col-sm-6">{{ item.conta_contabil or '-' }}</div></div>
                                <div class="row mb-2"><div class="col-sm-6"><strong>Centro de Custo:</strong></div><div class="col-sm-6">{{ item.costCenterCode or '-' }}</div></div>
                                <hr>
                                <div class="row mb-2"><div class="col-sm-6"><strong>Base de Cálculo ICMS:</strong></div><div class="col-sm-6">{{ item.base_calculo_icms or '-' }}</div></div>
                                <div class="row mb-2"><div class="col-sm-6"><strong>Valor ICMS:</strong></div><div class="col-sm-6">{{ item.valor_icms or '-' }}</div></div>
                                <div class="row mb-2"><div class="col-sm-6"><strong>Alíquota ICMS:</strong></div><div class="col-sm-6">{{ item.aliquota_icms or '-' }}</div></div>
                                <div class="row mb-2"><div class="col-sm-6"><strong>Valor IPI:</strong></div><div class="col-sm-6">{{ item.valor_ipi or '-' }}</div></div>
                                <div class="row mb-2"><div class="col-sm-6"><strong>Alíquota IPI:</strong></div><div class="col-sm-6">{{ item.aliquota_ipi or '-' }}</div></div>
                                <div class="row mb-2"><div class="col-sm-6"><strong>ISS:</strong></div><div class="col-sm-6">{{ item.valor_iss or '-' }}</div></div>
                                <div class="row mb-2"><div class="col-sm-6"><strong>PIS:</strong></div><div class="col-sm-6">{{ item.valor_pis or '-' }}</div></div>
                                <div class="row mb-2"><div class="col-sm-6"><strong>COFINS:</strong></div><div class="col-sm-6">{{ item.valor_cofins or '-' }}</div></div>
                                <div class="row mb-2"><div class="col-sm-6"><strong>IR:</strong></div><div class="col-sm-6">{{ item.valor_ir or '-' }}</div></div>
                                <div class="row mb-2"><div class="col-sm-6"><strong>CSLL:</strong></div><div class="col-sm-6">{{ item.valor_csll or '-' }}</div></div>

                                {% if current_user.is_admin %}
                                <hr>
                                <div class="card border-secondary">
                                <div class="card-header bg-secondary text-white"><small><i class="fas fa-code me-1"></i>Dados Brutos (Apenas Administradores)</small></div>
                                <div class="card-body"><pre class="mb-0"><code>{{ item|tojson(indent=2) }}</code></pre></div>
                                </div>
                                {% endif %}
                            </div>
                            </div>
                        </div>
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Fechar</button>
                    </div>
                    </div>
                </div>
                </div>
                {% endfor %}
                {% endif %}
                {# --- FIM: Rateio de Centros de Custo dentro de Informações da Empresa --- #}

            </div>
        </div>
    </div>
</div>

<!-- Informações do Cliente/Fornecedor -->
<div class="card mb-4">
    <div class="card-header">
        <h6 class="mb-0">
            <i class="fas fa-handshake me-2"></i>Informações do Cliente/Fornecedor
        </h6>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-md-4">
                <strong>Código do Cliente/Fornecedor:</strong>
                <p>{{ movement.customer_vendor_code or movement.aux_customer_vendor_code or '-' }}</p>
            </div>
            <div class="col-md-4">
                <strong>Condição de Pagamento:</strong>
                <p>{{ movement.payment_term_code or '-' }}</p>
            </div>
            <div class="col-md-4">
                <strong>Vendedor:</strong>
                <p>{{ movement.salesman1_code or '-' }}</p>
            </div>
        </div>
    </div>
</div>


<!-- Dates -->
<div class="card mb-4">
    <div class="card-header">
        <h6 class="mb-0">
            <i class="fas fa-calendar me-2"></i>Datas importantes
        </h6>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-md-4">
                <strong>Data de emissão:</strong>
                <p>{{ movement.register_date.strftime('%d/%m/%Y %H:%M') if movement.register_date else '-' }}</p>
            </div>
            <div class="col-md-4">
                <strong>Data de evolução:</strong>
                <p>{{ movement.entry_date.strftime('%d/%m/%Y %H:%M') if movement.entry_date else '-' }}</p>
            </div>
            <div class="col-md-4">
                <strong>Última edição:</strong>
                <p>{{ movement.last_edit_time.strftime('%d/%m/%Y %H:%M') if movement.last_edit_time else '-' }}</p>
            </div>
        </div>
    </div>
</div>

<!-- Movement Items (Special Section) -->
{% set movement_items = movement.get_json_field('movement_items') %}
{% if movement_items and movement_items|length > 0 %}
<div class="card mb-4">
    <div class="card-header">
        <h6 class="mb-0">
            <i class="fas fa-boxes me-2"></i>Itens do documento fiscal ({{ movement_items|length }} itens)
        </h6>
    </div>
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-sm table-hover">
                <thead class="table-dark">
                    <tr>
                        <th>Seq</th>
                        <th>Produto</th>
                        <th>Descrição</th>
                        <th>Quantidade</th>
                        <th>Valor unitário</th>
                        <th>Unidade</th>
                        <th>Valor total</th>
                        <th>Data</th>
                        <th>Ações</th>
                    </tr>
                </thead>
                <tbody>
                    {% for item in movement_items %}
                    <tr>
                        <td>
                            <span class="badge bg-primary">{{ item.sequentialNumber or item.sequentialId or loop.index }}</span>
                        </td>
                        <td>
                        {% if item.productFantasyName %}
                            <strong class="text-primary">{{ item.productFantasyName }}</strong>
                            
                        {% else %}
                            <strong class="text-primary">{{ item.productCode or '-' }}</strong>
                        {% endif %}
                    </td>


                        <td>
                            {{ item.description or '-' }}
                        </td>
                        <td>
                            <span class="badge bg-info">{{ item.quantity or 0 }}</span>
                        </td>
                        <td>
                            {% if item.unitPrice %}
                            <span class="text-success fw-bold">R$ {{ "%.2f"|format(item.unitPrice) }}</span>
                            {% else %}
                            -
                            {% endif %}
                        </td>
                        <td>
                            {% if item.measureUnitCode %}
                            <small class="badge bg-secondary">{{ item.measureUnitCode }}</small>
                            {% else %}
                            -
                            {% endif %}
                        </td>
                        <td>
                            {% if item.unitPrice and item.quantity %}
                                <strong class="text-success">
                                    R$ {{ "%.2f"|format((item.unitPrice | float) * (item.quantity | float)) }}
                                </strong>
                            {% else %}
                                <span class="text-muted">-</span>
                            {% endif %}
                        </td>


                        <td>
                            {% if item.registerDate %}
                                {% set d = item.registerDate[:10].split('-') %}
                                <small class="text-muted">{{ d[2] }}/{{ d[1] }}/{{ d[0] }}</small>
                            {% else %}
                                -
                            {% endif %}
                        </td>

                        <td>
                            <button class="btn btn-outline-primary btn-sm" 
                                    onclick="showItemDetails({{ loop.index0 }})" 
                                    title="Ver detalhes">
                                <i class="fas fa-eye"></i>
                            </button>
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>
{% endif %}

<!-- JSON Data Sections -->
{% set json_fields = [
    ('payments', 'Pagamentos', 'fas fa-credit-card'),
    ('department_apportionments', 'Rateio de Departamentos', 'fas fa-sitemap'),
    ('taxes', 'Impostos', 'fas fa-percent')
] %}

{% for field_name, title, icon in json_fields %}
    {% set json_data = movement.get_json_field(field_name) %}
    {% if json_data and json_data|length > 0 %}
    <div class="card mb-4">
        <div class="card-header">
            <h6 class="mb-0">
                <i class="{{ icon }} me-2"></i>{{ title }}
                <span class="badge bg-secondary ms-2">{{ json_data|length }}</span>
            </h6>
        </div>
        <div class="card-body">
            <div class="accordion" id="accordion{{ field_name }}">
                {% for item in json_data %}
                <div class="accordion-item">
                    <h2 class="accordion-header" id="heading{{ field_name }}{{ loop.index }}">
                        <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" 
                                data-bs-target="#collapse{{ field_name }}{{ loop.index }}" aria-expanded="false">
                            {% if field_name == 'movement_items' %}
                                {% if item is mapping %}
                                    Item {{ loop.index }} - ID do Produto: {{ item.get('productId', 'N/A') }}
                                {% else %}
                                    Item {{ loop.index }} - {{ item }}
                                {% endif %}
                            {% elif field_name == 'payments' %}
                                Pagamento {{ loop.index }}
                            {% elif field_name == 'taxes' %}
                                {% if item is mapping %}
                                    {{ item.get('taxId', 'Imposto') }} - {{ "%.2f"|format(item.get('value', 0)) }}
                                {% else %}
                                    Imposto {{ loop.index }} - {{ item }}
                                {% endif %}
                            {% else %}
                                {{ title }} {{ loop.index }}
                            {% endif %}
                        </button>
                    </h2>
                    <div id="collapse{{ field_name }}{{ loop.index }}" class="accordion-collapse collapse" 
                         data-bs-parent="#accordion{{ field_name }}">
                        <div class="accordion-body">
                            <div class="table-responsive">
                                <table class="table table-sm table-borderless">
                                    <tbody>
                                        {% if item is mapping %}
                                            {% for key, value in item.items() %}
                                            <tr>
                                                <td><strong>{{ key|translate_field }}:</strong></td>

                                                <td>
                                                    {% if key == 'dueDate' and value %}
                                                        {{ value[:10] | replace('-', '/') | datetimeformat('%d/%m/%Y') if false else value[:10].split('-')[2] ~ '/' ~ value[:10].split('-')[1] ~ '/' ~ value[:10].split('-')[0] }}
                                                    {% elif value is number %}
                                                        {{ "%.2f"|format(value) if value % 1 else value|int }}
                                                    {% elif value is string and value|length > 50 %}
                                                        <span title="{{ value }}">{{ value[:50] }}...</span>
                                                    {% else %}
                                                        {{ value or '-' }}
                                                    {% endif %}
                                                </td>
                                            </tr>
                                            {% endfor %}

                                        {% else %}
                                            <tr>
                                                <td><strong>Valor:</strong></td>
                                                <td>{{ item }}</td>
                                            </tr>
                                        {% endif %}
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
                {% endfor %}
            </div>
        </div>
    </div>
    {% endif %}
{% endfor %}




<!-- Taxes (Special Section) -->
{% set taxes = movement.get_json_field('taxes') %}
{% if taxes and taxes|length > 0 %}
<div class="card mb-4">
    <div class="card-header">
        <h6 class="mb-0">
            <i class="fas fa-calculator me-2"></i>
            Taxes ({{ taxes|length }} {% if taxes|length == 1 %}item{% else %}items{% endif %})
        </h6>
    </div>
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-sm table-hover">
                <thead>
                    <tr>
                        <th>Tax ID</th>
                        <th class="text-end">Calculation Basis</th>
                        <th class="text-end">Aliquot (%)</th>
                        <th class="text-end">Tax Value</th>
                        <th class="text-center">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for item in taxes %}
                    <tr>
                        <td>
                            <span class="badge bg-info">{{ item.taxId }}</span>
                        </td>
                        <td class="text-end">
                            <span class="text-muted">R$ {{ "%.2f"|format(item.calculationBasis) }}</span>
                        </td>
                        <td class="text-end">
                            <span class="text-warning">{{ "%.2f"|format(item.get('aliquot', 0)) }}%</span>
                        </td>
                        <td class="text-end">
                            <strong class="text-success">R$ {{ "%.2f"|format(item.value or item.amount or item.totalValue or 0) }}</strong>
                        </td>
                        <td class="text-center">
                            <button class="btn btn-outline-primary btn-sm" 
                                    data-bs-toggle="modal" 
                                    data-bs-target="#taxModal{{ loop.index }}">
                                <i class="fas fa-eye"></i>
                            </button>
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>

<!-- Tax Detail Modals -->
{% for item in taxes %}
<div class="modal fade" id="taxModal{{ loop.index }}" tabindex="-1">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">
                    <i class="fas fa-calculator me-2"></i>
                    Tax Details - {{ item.taxId }}
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <div class="row">
                    <!-- Basic Information -->
                    <div class="col-md-6">
                        <div class="card border-primary mb-3">
                            <div class="card-header bg-primary text-white">
                                <small><i class="fas fa-info-circle me-1"></i>Basic Information</small>
                            </div>
                            <div class="card-body">
                                <div class="row mb-2">
                                    <div class="col-sm-6"><strong>Tax ID:</strong></div>
                                    <div class="col-sm-6">
                                        <span class="badge bg-info">{{ item.taxId }}</span>
                                    </div>
                                </div>
                                <div class="row mb-2">
                                    <div class="col-sm-6"><strong>Company ID:</strong></div>
                                    <div class="col-sm-6">{{ item.companyId }}</div>
                                </div>
                                <div class="row mb-2">
                                    <div class="col-sm-6"><strong>Movement ID:</strong></div>
                                    <div class="col-sm-6">{{ item.movementId }}</div>
                                </div>
                                <div class="row mb-2">
                                    <div class="col-sm-6"><strong>Item Sequential:</strong></div>
                                    <div class="col-sm-6">{{ item.movementItemSequentialId }}</div>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <!-- Tax Calculation -->
                    <div class="col-md-6">
                        <div class="card border-success mb-3">
                            <div class="card-header bg-success text-white">
                                <small><i class="fas fa-calculator me-1"></i>Tax Calculation</small>
                            </div>
                            <div class="card-body">
                                <div class="row mb-2">
                                    <div class="col-sm-7"><strong>Calculation Basis:</strong></div>
                                    <div class="col-sm-5 text-end">
                                        <span class="text-muted">R$ {{ "%.2f"|format(item.calculationBasis) }}</span>
                                    </div>
                                </div>
                                <div class="row mb-2">
                                    <div class="col-sm-7"><strong>Aliquot:</strong></div>
                                    <div class="col-sm-5 text-end">
                                        <span class="text-warning">{{ "%.2f"|format(item.get('aliquot', 0)) }}%</span>
                                    </div>
                                </div>
                                <div class="row mb-2">
                                    <div class="col-sm-7"><strong>Tax Value:</strong></div>
                                    <div class="col-sm-5 text-end">
                                        <strong class="text-success">R$ {{ "%.2f"|format(item.value or item.amount or item.totalValue or 0) }}</strong>
                                    </div>
                                </div>
                                <div class="row mb-2">
                                    <div class="col-sm-7"><strong>Calculated Basis:</strong></div>
                                    <div class="col-sm-5 text-end">
                                        <span class="text-muted">R$ {{ "%.2f"|format(item.calculatedCalculationBasis) }}</span>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                
                <!-- Status Information -->
                <div class="card border-warning mb-3">
                    <div class="card-header bg-warning text-dark">
                        <small><i class="fas fa-flag me-1"></i>Status & Flags</small>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-6">
                                <div class="row mb-2">
                                    <div class="col-sm-6"><strong>Edited:</strong></div>
                                    <div class="col-sm-6">
                                        {% if item.edited %}
                                            <span class="badge bg-warning">Yes</span>
                                        {% else %}
                                            <span class="badge bg-secondary">No</span>
                                        {% endif %}
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="row mb-2">
                                    <div class="col-sm-6"><strong>Full Base:</strong></div>
                                    <div class="col-sm-6 text-end">
                                        R$ {{ "%.2f"|format(item.fullBase) }}
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                
                <!-- Raw JSON (Admin Only) -->
                {% if current_user.is_admin %}
                <div class="card border-secondary">
                    <div class="card-header bg-secondary text-white">
                        <small><i class="fas fa-code me-1"></i>Raw Data (Admin Only)</small>
                    </div>
                    <div class="card-body">
                        <pre class="mb-0"><code>{{ item|tojson(indent=2) }}</code></pre>
                    </div>
                </div>
                {% endif %}
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>
{% endfor %}
{% endif %}

<!-- System Information -->
<div class="card mb-4">
    <div class="card-header">
        <h6 class="mb-0">
            <i class="fas fa-cogs me-2"></i>Informações do Sistema
        </h6>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-md-4">
                <strong>Lote de Upload:</strong>
                <p><code>{{ movement.upload_batch_id or '-' }}</code></p>
            </div>
            <div class="col-md-4">
                <strong>Enviado em:</strong>
                <p>{{ movement.uploaded_at.strftime('%d/%m/%Y %H:%M:%S') if movement.uploaded_at else '-' }}</p>
            </div>
            <div class="col-md-4">
                <strong>Criado por:</strong>
                <p>{{ movement.creation_user or '-' }}</p>
            </div>
        </div>
        
        <div class="row mt-3">
            <div class="col-md-12">
                <strong>Flags:</strong>
                <div class="mt-2">
                    {% set flags = [
                        (movement.printed, 'Impresso', 'primary'),
                        (movement.document_printed, 'Documento Impresso', 'info'),
                        (movement.bill_printed, 'Boleto Impresso', 'success'),
                        (movement.has_generated_bill, 'Boleto Gerado', 'warning'),
                        (movement.processed_flag, 'Processado', 'secondary')
                    ] %}
                    
                    {% for flag_value, flag_name, color in flags %}
                        {% if flag_value %}
                        <span class="badge bg-{{ color }} me-2">{{ flag_name }}</span>
                        {% endif %}
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>
</div>

{% if current_user.is_admin %}
<div class="card">
    <div class="card-header">
        <button class="btn btn-link p-0 text-decoration-none w-100 text-start" type="button" data-bs-toggle="collapse" 
                data-bs-target="#rawData" aria-expanded="false">
            <h6 class="mb-0 d-flex justify-content-between align-items-center">
                <span>
                    <i class="fas fa-database me-2"></i>Dados Brutos (Apenas Administradores)
                </span>
                <i class="fas fa-chevron-down"></i>
            </h6>
        </button>
    </div>
    <div class="collapse" id="rawData">
        <div class="card-body">
            <div class="row">
                <div class="col-md-6">
                    <h6 class="text-primary mb-3"><i class="fas fa-info-circle me-1"></i> Informações Principais</h6>
                    <table class="table table-sm table-hover">
                        <tbody>
                            <tr><td><strong>ID Interno:</strong></td><td><code>{{ movement.internal_id or '-' }}</code></td></tr>
                            <tr><td><strong>ID do Movimento:</strong></td><td>{{ movement.movement_id or '-' }}</td></tr>
                            <tr><td><strong>ID da Empresa:</strong></td><td>{{ movement.company_id or '-' }}</td></tr>
                            <tr><td><strong>ID da Filial:</strong></td><td>{{ movement.branch_id or '-' }}</td></tr>
                            <tr><td><strong>Código da Empresa:</strong></td><td><span class="badge bg-primary">{{ movement.empresa_code or '-' }}</span></td></tr>
                            <tr><td><strong>Nome da Empresa:</strong></td><td>{{ movement.empresa_nome or '-' }}</td></tr>
                            <tr><td><strong>Código da Filial:</strong></td><td><span class="badge bg-info">{{ movement.filial_code or '-' }}</span></td></tr>
                            <tr><td><strong>Nome da Filial:</strong></td><td>{{ movement.filial_nome or '-' }}</td></tr>
                            <tr><td><strong>Status:</strong></td><td><span class="badge bg-info">{{ movement.status or '-' }}</span></td></tr>
                            <tr><td><strong>Tipo:</strong></td><td><span class="badge bg-secondary">{{ movement.type or '-' }}</span></td></tr>
                            <tr><td><strong>Tipo de Movimento:</strong></td><td><code>{{ movement.movement_type_code or '-' }}</code></td></tr>
                            <tr><td><strong>Série:</strong></td><td>{{ movement.series or '-' }}</td></tr>
                        </tbody>
                    </table>
                </div>
                
                <div class="col-md-6">
                    <h6 class="text-success mb-3"><i class="fas fa-money-bill-wave me-1"></i> Detalhes Financeiros</h6>
                    <table class="table table-sm table-hover">
                        <tbody>
                            <tr><td><strong>Valor Líquido:</strong></td><td><span class="text-success">{{ "R$ %.2f"|format(movement.net_value) if movement.net_value else '-' }}</span></td></tr>
                            <tr><td><strong>Valor Bruto:</strong></td><td><span class="text-success">{{ "R$ %.2f"|format(movement.gross_value) if movement.gross_value else '-' }}</span></td></tr>
                            <tr><td><strong>Outros Valores:</strong></td><td>{{ "R$ %.2f"|format(movement.other_values) if movement.other_values else '-' }}</td></tr>
                            <tr><td><strong>Bruto Interno:</strong></td><td>{{ "R$ %.2f"|format(movement.internal_gross_value) if movement.internal_gross_value else '-' }}</td></tr>
                            <tr><td><strong>Valor da Mercadoria:</strong></td><td>{{ "R$ %.2f"|format(movement.merchandise_value) if movement.merchandise_value else '-' }}</td></tr>
                            <tr><td><strong>Moeda:</strong></td><td>{{ movement.net_value_currency_code or '-' }}</td></tr>
                            <tr><td><strong>Condição de Pagamento:</strong></td><td>{{ movement.payment_term_code or '-' }}</td></tr>
                            <tr><td><strong>Conta Caixa:</strong></td><td>{{ movement.cash_account_code or '-' }}</td></tr>
                        </tbody>
                    </table>
                </div>
            </div>
            
            <div class="row mt-4">
                <div class="col-md-6">
                    <h6 class="text-info mb-3"><i class="fas fa-user-tie me-1"></i> Cliente/Fornecedor</h6>
                    <table class="table table-sm table-hover">
                        <tbody>
                            <tr><td><strong>Código:</strong></td><td><code>{{ movement.customer_vendor_code or '-' }}</code></td></tr>
                            <tr><td><strong>Código Auxiliar:</strong></td><td><code>{{ movement.aux_customer_vendor_code or '-' }}</code></td></tr>
                            <tr><td><strong>ID da Empresa:</strong></td><td>{{ movement.customer_vendor_company_id or '-' }}</td></tr>
                            <tr><td><strong>ID Auxiliar da Empresa:</strong></td><td>{{ movement.aux_customer_vendor_company_id or '-' }}</td></tr>
                        </tbody>
                    </table>
                </div>
                
                <div class="col-md-6">
                    <h6 class="text-warning mb-3"><i class="fas fa-warehouse me-1"></i> Armazém e Operações</h6>
                    <table class="table table-sm table-hover">
                        <tbody>
                            <tr><td><strong>Depósito:</strong></td><td>{{ movement.warehouse_code or '-' }}</td></tr>
                            <tr><td><strong>Depósito de Destino:</strong></td><td>{{ movement.destiny_warehouse_code or '-' }}</td></tr>
                            <tr><td><strong>Filial de Destino:</strong></td><td>{{ movement.destiny_branch_id or '-' }}</td></tr>
                            <tr><td><strong>ID da Operação:</strong></td><td>{{ movement.operation_id or '-' }}</td></tr>
                            <tr><td><strong>Centro de Custo:</strong></td><td>{{ movement.cost_center_code or '-' }}</td></tr>
                        </tbody>
                    </table>
                </div>
            </div>
            
            <div class="row mt-4">
                <div class="col-md-6">
                    <h6 class="text-secondary mb-3"><i class="fas fa-calendar-alt me-1"></i> Datas e Usuários</h6>
                    <table class="table table-sm table-hover">
                        <tbody>
                            <tr><td><strong>Data de Criação:</strong></td><td>{{ movement.creation_date.strftime('%d/%m/%Y %H:%M') if movement.creation_date else '-' }}</td></tr>
                            <tr><td><strong>Data de Registro:</strong></td><td>{{ movement.register_date.strftime('%d/%m/%Y %H:%M') if movement.register_date else '-' }}</td></tr>
                            <tr><td><strong>Data de Entrada:</strong></td><td>{{ movement.entry_date.strftime('%d/%m/%Y %H:%M') if movement.entry_date else '-' }}</td></tr>
                            <tr><td><strong>Data de Saída:</strong></td><td>{{ movement.exit_date.strftime('%d/%m/%Y %H:%M') if movement.exit_date else '-' }}</td></tr>
                            <tr><td><strong>Data de Entrega:</strong></td><td>{{ movement.delivery_date.strftime('%d/%m/%Y') if movement.delivery_date else '-' }}</td></tr>
                            <tr><td><strong>Última Edição:</strong></td><td>{{ movement.last_edit_time.strftime('%d/%m/%Y %H:%M:%S') if movement.last_edit_time else '-' }}</td></tr>
                            <tr><td><strong>Usuário de Criação:</strong></td><td><span class="badge bg-dark">{{ movement.creation_user or '-' }}</span></td></tr>
                            <tr><td><strong>Código do Usuário:</strong></td><td><span class="badge bg-dark">{{ movement.user_code or '-' }}</span></td></tr>
                        </tbody>
                    </table>
                </div>
                
                <div class="col-md-6">
                    <h6 class="text-danger mb-3"><i class="fas fa-percentage me-1"></i> Percentuais e Encargos</h6>
                    <table class="table table-sm table-hover">
                        <tbody>
                            <tr><td><strong>Desconto %:</strong></td><td>{{ "%.2f%%"|format(movement.discount_percentage) if movement.discount_percentage else '0.00%' }}</td></tr>
                            <tr><td><strong>Despesa %:</strong></td><td>{{ "%.2f%%"|format(movement.expense_percentage) if movement.expense_percentage else '0.00%' }}</td></tr>
                            <tr><td><strong>Encargo %:</strong></td><td>{{ "%.2f%%"|format(movement.charge_percentage) if movement.charge_percentage else '0.00%' }}</td></tr>
                            <tr><td><strong>Extra % 1:</strong></td><td>{{ "%.2f%%"|format(movement.extra_percentage1) if movement.extra_percentage1 else '0.00%' }}</td></tr>
                            <tr><td><strong>Extra % 2:</strong></td><td>{{ "%.2f%%"|format(movement.extra_percentage2) if movement.extra_percentage2 else '0.00%' }}</td></tr>
                            <tr><td><strong>Representante Comercial %:</strong></td><td>{{ "%.2f%%"|format(movement.commercial_representative_charge) if movement.commercial_representative_charge else '0.00%' }}</td></tr>
                            <tr><td><strong>Vendedor 2 %:</strong></td><td>{{ "%.2f%%"|format(movement.salesman2_charge_percentage) if movement.salesman2_charge_percentage else '0.00%' }}</td></tr>
                            <tr><td><strong>Vendedor 3 %:</strong></td><td>{{ "%.2f%%"|format(movement.salesman3_charge_percentage) if movement.salesman3_charge_percentage else '0.00%' }}</td></tr>
                        </tbody>
                    </table>
                </div>
            </div>
            
            <div class="row mt-4">
                <div class="col-12">
                    <h6 class="text-primary mb-3"><i class="fas fa-flag me-1"></i> Status e Flags</h6>
                    <div class="d-flex flex-wrap gap-2">
                        {% set all_flags = [
                            (movement.printed, 'Impresso', 'primary'),
                            (movement.document_printed, 'Documento Impresso', 'info'),
                            (movement.bill_printed, 'Boleto Impresso', 'success'),
                            (movement.has_generated_bill, 'Boleto Gerado', 'warning'),
                            (movement.processed_flag, 'Processado', 'secondary'),
                            (movement.bonum_integrated, 'Integrado Bonum', 'dark'),
                            (movement.paradigma_auto_integrated, 'Auto Integrado Paradigma', 'primary'),
                            (movement.fluxus_grouped_flag, 'Agrupado Fluxus', 'info'),
                            (movement.lot_generated, 'Lote Gerado', 'success'),
                            (movement.has_generated_work_account, 'Conta de Trabalho Gerada', 'warning'),
                            (movement.uses_financial_value_apportionment, 'Rateio Financeiro', 'secondary')
                        ] %}
                        
                        {% for flag_value, flag_name, color in all_flags %}
                            {% if flag_value %}
                                <span class="badge bg-{{ color }}">{{ flag_name }}</span>
                            {% else %}
                                <span class="badge bg-light text-dark">{{ flag_name }} ✗</span>
                            {% endif %}
                        {% endfor %}
                    </div>
                </div>
            </div>
            
            <div class="row mt-4">
                <div class="col-12">
                    <h6 class="text-success mb-3"><i class="fas fa-upload me-1"></i> Informações de Upload</h6>
                    <div class="alert alert-success">
                        <div class="row">
                            <div class="col-md-4">
                                <strong>ID do Lote:</strong><br>
                                <code>{{ movement.upload_batch_id or '-' }}</code>
                            </div>
                            <div class="col-md-4">
                                <strong>Enviado em:</strong><br>
                                {{ movement.uploaded_at.strftime('%d/%m/%Y %H:%M:%S') if movement.uploaded_at else '-' }}
                            </div>
                            <div class="col-md-4">
                                <strong>ID do Usuário que Enviou:</strong><br>
                                {{ movement.uploaded_by or '-' }}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endif %}


<!-- Movement Item Details Modal -->
<div class="modal fade" id="itemDetailsModal" tabindex="-1" aria-labelledby="itemDetailsModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="itemDetailsModalLabel">
                    <i class="fas fa-box me-2"></i>Movement Item Details
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <div id="itemDetailsContent">
                    <!-- Content will be loaded here -->
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>

{% endblock %}

{% block scripts %}
<script>
// Movement items data for JavaScript
{% set movement_items = movement.get_json_field('movement_items') %}
const movementItems = {{ movement_items|tojson if movement_items else '[]'|safe }};

function showItemDetails(itemIndex) {
    const item = movementItems[itemIndex];
    if (!item) {
        alert('Item not found');
        return;
    }
    
    // Build the details HTML
    let detailsHtml = `
        <div class="card">
            <div class="card-header bg-primary text-white">
                <h6 class="mb-0">Item ${item.sequentialNumber || item.sequentialId || (itemIndex + 1)} - Código do Produto: ${item.productId || 'N/A'}</h6>
            </div>
            <div class="card-body">
                <div class="row">
    `;
    
    // Key-value pairs organized in sections
    const sections = [
        {
            title: 'Informações Básicas',
            icon: 'fas fa-info-circle',
            color: 'primary',
            fields: [
                ['ID da Empresa', item.companyId],
                ['ID do Movimento', item.movementId],
                ['ID Sequencial', item.sequentialId],
                ['Número Sequencial', item.sequentialNumber],
                ['Código do Produto', item.productId],
                ['Código Reduzido', item.productCode],
                ['Unidade de Medida', item.measureUnitCode]
            ]
        },
        {
            title: 'Quantidades e Valores',
            icon: 'fas fa-calculator',
            color: 'success',
            fields: [
                ['Quantidade', item.quantity],
                ['Preço Unitário', item.unitPrice ? 'R$ ' + parseFloat(item.unitPrice).toFixed(2) : null],
                ['Preço de Tabela', item.tablePrice ? 'R$ ' + parseFloat(item.tablePrice).toFixed(2) : null],
                ['Valor de Desconto', item.discountValue ? 'R$ ' + parseFloat(item.discountValue).toFixed(2) : null],
                ['Valor Unitário', item.unitValue ? 'R$ ' + parseFloat(item.unitValue).toFixed(2) : null],
                ['Valor Financeiro', item.financialValue ? 'R$ ' + parseFloat(item.financialValue).toFixed(2) : null],
                ['Quantidade a Receber', item.receivableQuantity],
                ['Quantidade Original', item.originalQuantity]
            ]
        },
        {
            title: 'Datas',
            icon: 'fas fa-calendar',
            color: 'info',
            fields: [
                ['Data de Registro', item.registerDate ? new Date(item.registerDate).toLocaleDateString('pt-BR') : null],
                ['Data de Entrega', item.deliveryDate ? new Date(item.deliveryDate).toLocaleDateString('pt-BR') : null]
            ]
        },
        {
            title: 'Classificações e Códigos',
            icon: 'fas fa-tags',
            color: 'warning',
            fields: [
                ['Tabela de Classificação 5', item.classificationTable5Code],
                ['Tabela Financeira Opcional 2', item.financialOptionalTable2Code],
                ['Flag de Efeito de Estoque', item.stockEffectFlag],
                ['Flag', item.flag],
                ['Objeto de Bloqueio', item.blockObject]
            ]
        }
    ];

    
    sections.forEach(section => {
        detailsHtml += `
            <div class="col-12 mb-3">
                <div class="card border-${section.color} border-opacity-25">
                    <div class="card-header bg-${section.color} bg-opacity-10 border-${section.color} border-opacity-25">
                        <h6 class="mb-0 text-${section.color}">
                            <i class="${section.icon} me-2"></i>${section.title}
                        </h6>
                    </div>
                    <div class="card-body">
                        <div class="row">
        `;
        
        section.fields.forEach(([label, value]) => {
            if (value !== null && value !== undefined && value !== '') {
                detailsHtml += `
                    <div class="col-md-6 mb-2">
                        <strong class="text-muted small">${label}:</strong>
                        <div class="ms-2">${value}</div>
                    </div>
                `;
            }
        });
        
        detailsHtml += `
                        </div>
                    </div>
                </div>
            </div>
        `;
    });
    
    // Add other fields section for any remaining fields
    const displayedFields = new Set([
    'companyid', 'movementid', 'sequentialid', 'sequentialnumber', 'productcode',
    'quantity', 'unitprice', 'tableprice', 'discountvalue', 'registerdate',
    'deliverydate', 'classificationtable5code', 'financialoptionaltable2code',
    'measureunitcode', 'receivablequantity', 'partialbillingreceivedvalue',
    'completedvalue', 'stockeffectflag', 'unitvalue', 'financialvalue',
    'ordinationtaxaliquot', 'originalquantity', 'flag', 'blockobject'
    ]);

    
    const otherFields = [];
    Object.keys(item).forEach(key => {
        const normalizedKey = key.toLowerCase();
        if (!displayedFields.has(normalizedKey)) {
            otherFields.push([key, item[key]]);
        }
    });
    
    if (otherFields.length > 0) {
        detailsHtml += `
            <div class="col-12">
                <div class="card border-secondary border-opacity-25">
                    <div class="card-header bg-secondary bg-opacity-10">
                        <h6 class="mb-0 text-secondary">
                            <i class="fas fa-list me-2"></i>Other Fields
                        </h6>
                    </div>
                    <div class="card-body">
                        <div class="row">
        `;
        
        otherFields.forEach(([key, value]) => {
            if (value !== null && value !== undefined && value !== '') {
                detailsHtml += `
                    <div class="col-md-6 mb-2">
                        <strong class="text-muted small">${key}:</strong>
                        <div class="ms-2 small">${value}</div>
                    </div>
                `;
            }
        });
        
        detailsHtml += `
                        </div>
                    </div>
                </div>
            </div>
        `;
    }
    
    detailsHtml += `
                </div>
            </div>
        </div>
    `;
    
    // Insert the content and show the modal
    document.getElementById('itemDetailsContent').innerHTML = detailsHtml;
    const modal = new bootstrap.Modal(document.getElementById('itemDetailsModal'));
    modal.show();
}

$(document).ready(function() {
    // Auto-expand first accordion item if there are any items
    $('.accordion .accordion-item:first-child .accordion-button').removeClass('collapsed');
    $('.accordion .accordion-item:first-child .accordion-collapse').addClass('show');
});

function confirmDelete(movementId, movementName) {
    document.getElementById('movementName').textContent = movementName;
    document.getElementById('deleteForm').action = `/movements/${movementId}/delete`;
    new bootstrap.Modal(document.getElementById('deleteModal')).show();
}
</script>

<!-- Delete Confirmation Modal -->
<div class="modal fade" id="deleteModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">
                    <i class="fas fa-exclamation-triangle text-warning me-2"></i>
                    Confirm Delete
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <p>Are you sure you want to delete movement <strong id="movementName"></strong>?</p>
                <p class="text-muted">This action cannot be undone.</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                <form id="deleteForm" method="POST" style="display: inline;">
                    <button type="submit" class="btn btn-danger">
                        <i class="fas fa-trash me-2"></i>Delete
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>

{% endblock %}


----- FILE: templates/movements/list.html  (sha1:acdd5752048f, bytes:19811) -----
{% extends "base.html" %}

{% block title %}Financial Movements - Financial Data Management{% endblock %}

{% block content %}
<div class="d-flex justify-content-between align-items-center mb-4">
    <h1>
        <i class="fas fa-list me-3"></i>
        Financial Movements
    </h1>
</div>

<!-- Search and Filters -->
<div class="card mb-4">
    <div class="card-header">
        <h6 class="mb-0">
            <i class="fas fa-filter me-2"></i>Search & Filters
        </h6>
    </div>
    <div class="card-body"><form method="GET" class="row g-3">
  <div class="col-md-6">
    <label for="search" class="form-label fw-bold">
      <i class="fas fa-search me-2 text-primary"></i>Buscar movimento
    </label>
    <input
      type="text"
      class="form-control"
      id="search"
      name="search"
      value="{{ search }}"
      placeholder="Digite para filtrar os movimentos..."
    />
    <div
      id="searchResults"
      class="list-group position-absolute w-100"
      style="z-index:1000; max-height:300px; overflow:auto; display:none;"
    ></div>
  </div>

  <div class="col-md-3">
    <label for="start_date" class="form-label">Período inicial</label>
    <input
      type="date"
      class="form-control"
      id="start_date"
      name="start_date"
      value="{{ start_date }}"
    />
  </div>

  <div class="col-md-3">
    <label for="end_date" class="form-label">Período final</label>
    <input
      type="date"
      class="form-control"
      id="end_date"
      name="end_date"
      value="{{ end_date }}"
    />
  </div>

  <div class="col-md-12 d-flex align-items-end">
    <div class="btn-group w-100" role="group">
      <button type="submit" class="btn btn-primary">
        <i class="fas fa-search me-2"></i>Aplicar filtros
      </button>
      <a href="{{ url_for('main.movements') }}" class="btn btn-outline-secondary">
        <i class="fas fa-times me-2"></i>Limpar tudo
      </a>
    </div>
  </div>
</form>
</div>
</div>

<!-- Results -->
<div class="card">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="mb-0">
            <i class="fas fa-chart-line me-2"></i>
            Movement Records
            {% if movements.total %}
            <span class="badge bg-secondary ms-2">{{ "{:,}".format(movements.total) }} total</span>
            {% endif %}
        </h5>
        {% if movements.total > 0 %}
        <div class="btn-group btn-group-sm" role="group">
            <button type="button" class="btn btn-outline-secondary" onclick="exportData()">
                <i class="fas fa-download me-1"></i>Export
            </button>
            <a href="{{ url_for('main.delete_all_movements') }}" class="btn btn-outline-danger">
                <i class="fas fa-trash me-1"></i>Delete All
            </a>
        </div>
        {% endif %}
    </div>
    <div class="card-body p-0">
        {% if movements.items %}
        <div class="table-responsive">
            <table class="table table-hover mb-0" id="movementsTable">
                <thead class="table-dark">
                    <tr>
                        <th>N° da NF</th>
                        <th style="display:none;">Número do Movimento (oculto)</th>
                        <!-- <th>Empresa</th> -->
                        <!-- <th>Filial</th> -->
                        <th>Cliente/Fornecedor</th>
                        <th>Valor total da nota</th>
                        <th>Data</th>
                        <!-- <th>Status</th> -->
                        <th>ID interno</th>
                        <th>Ações</th>
                        
                    </tr>
                </thead>
                <tbody>
                    {% for movement in movements.items %}
                    <tr>
                        <td>{{ movement.number or '-' }}</td>
                        <td style="display:none;">{{ movement.movement_type_code or '' }}</td>
                        <!-- <td>
                            {% if movement.empresa_nome %}
                                {{ movement.empresa_nome }}
                            {% else %}
                                <span class="text-muted">-</span>
                            {% endif %}
                        </td>

                        <td>
                            {% if movement.filial_code %}
                                {{ movement.filial_code }}
                            {% elif movement.branch_id %}
                                {{ movement.branch_id }}
                            {% else %}
                                <span class="text-muted">-</span>
                            {% endif %}
                        </td> -->


                        <td>
                            {% if movement.aux_customer_vendor_code and movement.customer_vendor_name and movement.customer_vendor_cnpj %}
                                {{ movement.aux_customer_vendor_code }} - {{ movement.customer_vendor_name }} - {{ movement.customer_vendor_cnpj }}
                            {% elif movement.aux_customer_vendor_code and movement.customer_vendor_name %}
                                {{ movement.aux_customer_vendor_code }} - {{ movement.customer_vendor_name }}
                            {% elif movement.aux_customer_vendor_code %}
                                {{ movement.aux_customer_vendor_code }}
                            {% else %}
                                <span class="text-muted">-</span>
                            {% endif %}
                        </td>

                        <td>
                            {% if movement.gross_value is not none %}
                                <span class="badge bg-success">
                                    {{ movement.net_value_currency_code or 'R$' }} {{ "%.2f"|format(movement.gross_value) }}
                                </span>
                            {% else %}
                                <span class="text-muted">-</span>
                            {% endif %}
                        </td>
                        <td>
                            {% if movement.date %}
                                <small class="text-muted">
                                    {{ movement.date.strftime('%d/%m/%Y') }}
                                </small>
                            {% else %}
                                <span class="text-muted">-</span>
                            {% endif %}
                        </td>
                        <!-- <td>
                            {% if movement.status %}
                                <span class="badge {% if movement.status == 'F' %}bg-success{% elif movement.status == 'N' %}bg-warning{% else %}bg-secondary{% endif %}">
                                    {{ movement.status }}
                                </span>
                            {% else %}
                                <span class="text-muted">-</span>
                            {% endif %}
                        </td> -->
                        
                        <td>
                            <code class="text-primary">{{ movement.internal_id or '-' }}</code>
                        </td>
                        <td>
                            <div class="btn-group btn-group-sm" role="group">
                                <a href="{{ url_for('main.movement_detail', id=movement.id) }}" 
                                   class="btn btn-outline-primary" title="Ver detalhes">
                                    <i class="fas fa-eye"></i>
                                </a>
                                <button type="button" class="btn btn-outline-danger" 
                                        onclick="confirmDelete({{ movement.id }}, '{{ movement.internal_id or movement.id }}')"
                                        title="Excluir movimento">
                                    <i class="fas fa-trash"></i>
                                </button>
                            </div>
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
        
        <!-- Pagination -->
        {% if movements.pages > 1 %}
        <div class="card-footer">
            <nav aria-label="Movements pagination">
                <ul class="pagination justify-content-center mb-0">
                    {% if movements.has_prev %}
                    <li class="page-item">
                        <a class="page-link" href="{{ url_for('main.movements', page=movements.prev_num, search=search) }}">
                            <i class="fas fa-chevron-left"></i> Previous
                        </a>
                    </li>
                    {% endif %}
                    
                    {% for page_num in movements.iter_pages() %}
                        {% if page_num %}
                            {% if page_num != movements.page %}
                            <li class="page-item">
                                <a class="page-link" href="{{ url_for('main.movements', page=page_num, search=search) }}">{{ page_num }}</a>
                            </li>
                            {% else %}
                            <li class="page-item active">
                                <span class="page-link">{{ page_num }}</span>
                            </li>
                            {% endif %}
                        {% else %}
                        <li class="page-item disabled">
                            <span class="page-link">...</span>
                        </li>
                        {% endif %}
                    {% endfor %}
                    
                    {% if movements.has_next %}
                    <li class="page-item">
                        <a class="page-link" href="{{ url_for('main.movements', page=movements.next_num, search=search) }}">
                            Next <i class="fas fa-chevron-right"></i>
                        </a>
                    </li>
                    {% endif %}
                </ul>
            </nav>
            
            <!-- Pagination Info -->
            <div class="text-center mt-2">
                <small class="text-muted">
                    Showing {{ movements.per_page * (movements.page - 1) + 1 }} to 
                    {{ movements.per_page * movements.page if movements.page < movements.pages else movements.total }} 
                    of {{ "{:,}".format(movements.total) }} entries
                </small>
            </div>
        </div>
        {% endif %}
        
        {% else %}
        <div class="text-center py-5">
            {% if search %}
            <i class="fas fa-search fa-3x text-muted mb-3"></i>
            <h5 class="text-muted">No results found</h5>
            <p class="text-muted">No movements match your search criteria: "{{ search }}"</p>
            <a href="{{ url_for('main.movements') }}" class="btn btn-outline-primary">
                <i class="fas fa-times me-2"></i>Clear Search
            </a>
            {% else %}
            <i class="fas fa-inbox fa-3x text-muted mb-3"></i>
            <h5 class="text-muted">No movements found</h5>
            <p class="text-muted">No financial movement records have been uploaded yet.</p>
            {% if current_user.is_admin %}
            <a href="{{ url_for('admin.upload_file') }}" class="btn btn-primary">
                <i class="fas fa-upload me-2"></i>Upload Data
            </a>
            {% endif %}
            {% endif %}
        </div>
        {% endif %}
    </div>
</div>

<!-- Quick Stats -->
{% if movements.items %}
<div class="row mt-4">
    <div class="col-md-4">
        <div class="card text-center">
            <div class="card-body">
                <h5 class="card-title text-primary">Total Value</h5>
                <p class="card-text">
                    <span class="h4">
                        {% set total_value = movements.items | map(attribute='net_value') | select('number') | sum %}
                        R$ {{ "%.2f"|format(total_value or 0) }}
                    </span>
                </p>
            </div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card text-center">
            <div class="card-body">
                <h5 class="card-title text-success">Completed</h5>
                <p class="card-text">
                    <span class="h4">
                        {% set completed = movements.items | selectattr('status', 'equalto', 'F') | list | length %}
                        {{ completed }}
                    </span>
                </p>
            </div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card text-center">
            <div class="card-body">
                <h5 class="card-title text-warning">Pending</h5>
                <p class="card-text">
                    <span class="h4">
                        {% set pending = movements.items | selectattr('status', 'equalto', 'N') | list | length %}
                        {{ pending }}
                    </span>
                </p>
            </div>
        </div>
    </div>
</div>
{% endif %}

<!-- Delete Confirmation Modal -->
<div class="modal fade" id="deleteModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">
                    <i class="fas fa-exclamation-triangle text-warning me-2"></i>
                    Confirm Delete
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <p>Are you sure you want to delete movement <strong id="movementName"></strong>?</p>
                <p class="text-muted">This action cannot be undone.</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                <form id="deleteForm" method="POST" style="display: inline;">
                    <button type="submit" class="btn btn-danger">
                        <i class="fas fa-trash me-2"></i>Delete
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>

{% endblock %}

{% block scripts %}
<script>
   $(function () {
  // Função debounce — evita chamadas contínuas durante digitação
  const debounce = (fn, wait = 600) => {
    let t;
    return (...args) => {
      clearTimeout(t);
      t = setTimeout(() => fn.apply(this, args), wait);
    };
  };

  const updateResults = (val) => {
    try {
      if (typeof val === "undefined" || val === null) val = "";
      val = String(val).trim();

      const params = new URLSearchParams(window.location.search);
      const currentSearch = params.get("search") || "";

      // Se o campo estiver vazio → limpa e recarrega só se antes havia busca
      if (val === "" && currentSearch !== "") {
        params.delete("search");
        params.set("page", 1);
        window.location.href = `${window.location.pathname}?${params.toString()}`;
        return;
      }

      // Se o campo estiver vazio e não havia busca → não faz nada
      if (val === "") return;

      // Atualiza apenas se o valor realmente mudou
      if (val !== currentSearch) {
        params.set("search", val);
        params.set("page", 1);
        window.location.href = `${window.location.pathname}?${params.toString()}`;
      }
    } catch (err) {
      console.error("Search update error:", err);
    }
  };

  // Detecta input (com debounce)
  $("#search").on(
    "input",
    debounce(function () {
      const val = this.value ?? "";
      updateResults(val);
    }, 600)
  );

  // Pressionar Enter faz a busca imediatamente
  $("#search").on("keydown", function (e) {
    if (e.key === "Enter") {
      e.preventDefault();
      updateResults(this.value);
    }
  });
});



// Fecha resultados ao clicar fora
$(document).on('click', function(e) {
    if (!$(e.target).closest('#search, #searchResults').length) {
        $('#searchResults').hide();
    }
});

$(document).ready(function() {
    if (!$('#search').val()) {
        $('#search').focus();
    }
    
    $('#search').keypress(function(e) {
        if (e.which == 13) {
            $(this).closest('form').submit();
        }
    });

    if ($('#movementsTable').length) {
        if (!$.fn.DataTable.isDataTable('#movementsTable')) {
            $('#movementsTable').DataTable({
                responsive: true,
                pageLength: 25,
                lengthMenu: [[10, 25, 50, 100, -1], [10, 25, 50, 100, "All"]],
                language: {
                    search: "Buscar movimentos:",
                    lengthMenu: "Mostrar _MENU_ registros por página",
                    info: "Exibindo _START_ a _END_ de _TOTAL_ registros",
                    infoEmpty: "Nenhum registro disponível",
                    infoFiltered: "(filtrado de _MAX_ registros no total)",
                    paginate: {
                        first: "Primeiro",
                        last: "Último",
                        next: "Próximo",
                        previous: "Anterior"
                    }
                },
                order: [],
                columnDefs: [
                    { targets: [-1], orderable: false },
                    { targets: [4], type: 'currency' },
                    { targets: [1], visible: false, searchable: true }
                ],
                dom: '<"row"<"col-sm-12 col-md-6"l><"col-sm-12 col-md-6"f>>' +
                    '<"row"<"col-sm-12"tr>>' +
                    '<"row"<"col-sm-12 col-md-5"i><"col-sm-12 col-md-7"p>>',
                initComplete: function() {
                    $('.dataTables_length select').addClass('form-select form-select-sm');
                    $('.dataTables_filter input').addClass('form-control form-control-sm');
                }
            });
        }
    }

});

function confirmDelete(movementId, movementName) {
    document.getElementById('movementName').textContent = movementName;
    document.getElementById('deleteForm').action = `/movements/${movementId}/delete`;
    new bootstrap.Modal(document.getElementById('deleteModal')).show();
}

function exportData() {
    let table = document.getElementById('movementsTable');
    let csv = [];
    let rows = table.querySelectorAll('tr');
    
    for (let i = 0; i < rows.length; i++) {
        let row = [], cols = rows[i].querySelectorAll('td, th');
        
        for (let j = 0; j < cols.length - 1; j++) {
            row.push(cols[j].innerText);
        }
        
        csv.push(row.join(','));
    }
    
    let csvFile = new Blob([csv.join('\n')], { type: 'text/csv' });
    let downloadLink = document.createElement('a');
    downloadLink.download = 'financial_movements.csv';
    downloadLink.href = window.URL.createObjectURL(csvFile);
    downloadLink.style.display = 'none';
    document.body.appendChild(downloadLink);
    downloadLink.click();
    document.body.removeChild(downloadLink);
}
</script>
{% endblock %}


----- FILE: complete_model_fields.txt  (sha1:70db8a0fe187, bytes:13123) -----
# Complete column mapping for file_processor.py:
COLUMN_MAPPING = {
    'internalId': 'internal_id',
    'companyId': 'company_id',
    'movementId': 'movement_id',
    'branchId': 'branch_id',
    'warehouseCode': 'warehouse_code',
    'destinyWarehouseCode': 'destiny_warehouse_code',
    'number': 'number',
    'series': 'series',
    'movementTypeCode': 'movement_type_code',
    'type': 'type',
    'status': 'status',
    'printed': 'printed',
    'documentPrinted': 'document_printed',
    'billPrinted': 'bill_printed',
    'registerDate': 'register_date',
    'exitDate': 'exit_date',
    'commercialRepresentativeCharge': 'commercial_representative_charge',
    'grossValue': 'gross_value',
    'netValue': 'net_value',
    'informedNetValue': 'informed_net_value',
    'otherValues': 'other_values',
    'discountPercentage': 'discount_percentage',
    'expensePercentage': 'expense_percentage',
    'expenseValue': 'expense_value',
    'extraPercentage1': 'extra_percentage1',
    'extraValue1': 'extra_value1',
    'extraPercentage2': 'extra_percentage2',
    'extraValue2': 'extra_value2',
    'transportedProductNetWeight': 'transported_product_net_weight',
    'transportedProductGrossWeight': 'transported_product_gross_weight',
    'classificationTable5Code': 'classification_table5_code',
    'financialOptionalTable2Code': 'financial_optional_table2_code',
    'netValueCurrencyCode': 'net_value_currency_code',
    'date': 'date',
    'hasGeneratedBill': 'has_generated_bill',
    'auxCustomerVendorCode': 'aux_customer_vendor_code',
    'auxCustomerVendorCompanyId': 'aux_customer_vendor_company_id',
    'costCenterCode': 'cost_center_code',
    'salesman1Code': 'salesman1_code',
    'chargePercentage': 'charge_percentage',
    'salesman2ChargePercentage': 'salesman2_charge_percentage',
    'salesman3ChargePercentage': 'salesman3_charge_percentage',
    'salesman4ChargePercentage': 'salesman4_charge_percentage',
    'userCode': 'user_code',
    'destinyBranchId': 'destiny_branch_id',
    'lotGenerated': 'lot_generated',
    'accountingExportStatus': 'accounting_export_status',
    'deliveryDate': 'delivery_date',
    'hasGeneratedWorkAccount': 'has_generated_work_account',
    'workAccountGenerated': 'work_account_generated',
    'lastEditTime': 'last_edit_time',
    'indicateObjectUse': 'indicate_object_use',
    'bonumIntegrated': 'bonum_integrated',
    'processedFlag': 'processed_flag',
    'icmsDeductionValue': 'icms_deduction_value',
    'creationUser': 'creation_user',
    'creationDate': 'creation_date',
    'emailStatus': 'email_status',
    'internalGrossValue': 'internal_gross_value',
    'otherCompanyINSSBaseValue': 'other_company_inss_base_value',
    'conditionalDiscountValue': 'conditional_discount_value',
    'conditionalExpenseValue': 'conditional_expense_value',
    'affectStockOrder': 'affect_stock_order',
    'commercialAutomationExported': 'commercial_automation_exported',
    'aplicationIntegration': 'aplication_integration',
    'entryDate': 'entry_date',
    'extemporaneous': 'extemporaneous',
    'merchandiseValue': 'merchandise_value',
    'usesFinancialValueApportionment': 'uses_financial_value_apportionment',
    'conclusionFlag': 'conclusion_flag',
    'paradigmaStatus': 'paradigma_status',
    'paradigmaAutoIntegrated': 'paradigma_auto_integrated',
    'originalGrossValue': 'original_gross_value',
    'originalNetValue': 'original_net_value',
    'originalOtherValues': 'original_other_values',
    'operationId': 'operation_id',
    'scpBranchId': 'scp_branch_id',
    'movementItems': 'movement_items',
    'payments': 'payments',
    'costCenterApportionments': 'cost_center_apportionments',
    'departmentApportionments': 'department_apportionments',
    'taxes': 'taxes',
    'fiscal': 'fiscal',
    'norm': 'norm',
    'cargoComponent': 'cargo_component',
    'thirdPartyNF': 'third_party_nf',
    'safetyDevice': 'safety_device',
    'nfe': 'nfe',
    'inputCTRC': 'input_ctrc',
    'outputCTRC': 'output_ctrc',
    'ctrc': 'ctrc',
    'transportData': 'transport_data',
    'documentAuthorization': 'document_authorization',
    'judicialProcess': 'judicial_process',
    'serviceOrder': 'service_order',
    'relatedMovement': 'related_movement',
    'exportRelatedMovement': 'export_related_movement',
    'linkedMovement': 'linked_movement',
    'cTe': 'c_te',
    'eaiIntegration': 'eai_integration',
    'electronicInvoiceFreeFields': 'electronic_invoice_free_fields',
    'customerVendorCode': 'customer_vendor_code',
    'paymentTermCode': 'payment_term_code',
    'observation': 'observation',
    'financialOptionalTable1Code': 'financial_optional_table1_code',
    'financialEntryMovementId': 'financial_entry_movement_id',
    'generatedEntryNumber': 'generated_entry_number',
    'openEntryNumber': 'open_entry_number',
    'cashAccountCode': 'cash_account_code',
    'customerVendorCompanyId': 'customer_vendor_company_id',
    'fluxusGroupedFlag': 'fluxus_grouped_flag',
    'cashAccountCompanyId': 'cash_account_company_id',
    'longHistory': 'long_history',
}

# Fields for FinancialMovement model:
    internal_id = db.Column(db.String(50), nullable=True)
    company_id = db.Column(db.String(50), nullable=True)
    movement_id = db.Column(db.String(50), nullable=True)
    branch_id = db.Column(db.String(50), nullable=True)
    warehouse_code = db.Column(db.String(50), nullable=True)
    destiny_warehouse_code = db.Column(db.String(50), nullable=True)
    number = db.Column(db.String(255), nullable=True)
    series = db.Column(db.String(255), nullable=True)
    movement_type_code = db.Column(db.String(50), nullable=True)
    type = db.Column(db.String(255), nullable=True)
    status = db.Column(db.String(255), nullable=True)
    printed = db.Column(db.Boolean, nullable=True)
    document_printed = db.Column(db.Boolean, nullable=True)
    bill_printed = db.Column(db.Boolean, nullable=True)
    register_date = db.Column(db.DateTime, nullable=True)
    exit_date = db.Column(db.DateTime, nullable=True)
    commercial_representative_charge = db.Column(db.Float, nullable=True)
    gross_value = db.Column(db.Float, nullable=True)
    net_value = db.Column(db.Float, nullable=True)
    informed_net_value = db.Column(db.Float, nullable=True)
    other_values = db.Column(db.String(255), nullable=True)
    discount_percentage = db.Column(db.Float, nullable=True)
    expense_percentage = db.Column(db.Float, nullable=True)
    expense_value = db.Column(db.Float, nullable=True)
    extra_percentage1 = db.Column(db.String(255), nullable=True)
    extra_value1 = db.Column(db.String(255), nullable=True)
    extra_percentage2 = db.Column(db.String(255), nullable=True)
    extra_value2 = db.Column(db.String(255), nullable=True)
    transported_product_net_weight = db.Column(db.Float, nullable=True)
    transported_product_gross_weight = db.Column(db.Float, nullable=True)
    classification_table5_code = db.Column(db.String(50), nullable=True)
    financial_optional_table2_code = db.Column(db.String(50), nullable=True)
    net_value_currency_code = db.Column(db.String(50), nullable=True)
    date = db.Column(db.String(255), nullable=True)
    has_generated_bill = db.Column(db.Boolean, nullable=True)
    aux_customer_vendor_code = db.Column(db.String(50), nullable=True)
    aux_customer_vendor_company_id = db.Column(db.String(50), nullable=True)
    cost_center_code = db.Column(db.String(50), nullable=True)
    salesman1_code = db.Column(db.String(50), nullable=True)
    charge_percentage = db.Column(db.Float, nullable=True)
    salesman2_charge_percentage = db.Column(db.Float, nullable=True)
    salesman3_charge_percentage = db.Column(db.Float, nullable=True)
    salesman4_charge_percentage = db.Column(db.Float, nullable=True)
    user_code = db.Column(db.String(50), nullable=True)
    destiny_branch_id = db.Column(db.String(50), nullable=True)
    lot_generated = db.Column(db.String(255), nullable=True)
    accounting_export_status = db.Column(db.String(255), nullable=True)
    delivery_date = db.Column(db.DateTime, nullable=True)
    has_generated_work_account = db.Column(db.Boolean, nullable=True)
    work_account_generated = db.Column(db.String(255), nullable=True)
    last_edit_time = db.Column(db.DateTime, nullable=True)
    indicate_object_use = db.Column(db.String(255), nullable=True)
    bonum_integrated = db.Column(db.Boolean, nullable=True)
    processed_flag = db.Column(db.Boolean, nullable=True)
    icms_deduction_value = db.Column(db.Float, nullable=True)
    creation_user = db.Column(db.String(255), nullable=True)
    creation_date = db.Column(db.DateTime, nullable=True)
    email_status = db.Column(db.String(255), nullable=True)
    internal_gross_value = db.Column(db.Float, nullable=True)
    other_company_inss_base_value = db.Column(db.Float, nullable=True)
    conditional_discount_value = db.Column(db.Float, nullable=True)
    conditional_expense_value = db.Column(db.Float, nullable=True)
    affect_stock_order = db.Column(db.String(255), nullable=True)
    commercial_automation_exported = db.Column(db.String(255), nullable=True)
    aplication_integration = db.Column(db.String(255), nullable=True)
    entry_date = db.Column(db.DateTime, nullable=True)
    extemporaneous = db.Column(db.String(255), nullable=True)
    merchandise_value = db.Column(db.Float, nullable=True)
    uses_financial_value_apportionment = db.Column(db.Boolean, nullable=True)
    conclusion_flag = db.Column(db.Boolean, nullable=True)
    paradigma_status = db.Column(db.String(255), nullable=True)
    paradigma_auto_integrated = db.Column(db.Boolean, nullable=True)
    original_gross_value = db.Column(db.Float, nullable=True)
    original_net_value = db.Column(db.Float, nullable=True)
    original_other_values = db.Column(db.String(255), nullable=True)
    operation_id = db.Column(db.String(50), nullable=True)
    scp_branch_id = db.Column(db.String(50), nullable=True)
    movement_items = db.Column(db.Text, nullable=True)  # JSON field
    payments = db.Column(db.Text, nullable=True)  # JSON field
    cost_center_apportionments = db.Column(db.Text, nullable=True)  # JSON field
    department_apportionments = db.Column(db.Text, nullable=True)  # JSON field
    taxes = db.Column(db.Text, nullable=True)  # JSON field
    fiscal = db.Column(db.Text, nullable=True)  # JSON field
    norm = db.Column(db.Text, nullable=True)  # JSON field
    cargo_component = db.Column(db.Text, nullable=True)  # JSON field
    third_party_nf = db.Column(db.Text, nullable=True)  # JSON field
    safety_device = db.Column(db.Text, nullable=True)  # JSON field
    nfe = db.Column(db.Text, nullable=True)  # JSON field
    input_ctrc = db.Column(db.Text, nullable=True)  # JSON field
    output_ctrc = db.Column(db.Text, nullable=True)  # JSON field
    ctrc = db.Column(db.Text, nullable=True)  # JSON field
    transport_data = db.Column(db.Text, nullable=True)  # JSON field
    document_authorization = db.Column(db.Text, nullable=True)  # JSON field
    judicial_process = db.Column(db.Text, nullable=True)  # JSON field
    service_order = db.Column(db.Text, nullable=True)  # JSON field
    related_movement = db.Column(db.Text, nullable=True)  # JSON field
    export_related_movement = db.Column(db.Text, nullable=True)  # JSON field
    linked_movement = db.Column(db.Text, nullable=True)  # JSON field
    c_te = db.Column(db.Text, nullable=True)  # JSON field
    eai_integration = db.Column(db.Text, nullable=True)  # JSON field
    electronic_invoice_free_fields = db.Column(db.Text, nullable=True)  # JSON field
    customer_vendor_code = db.Column(db.String(50), nullable=True)
    payment_term_code = db.Column(db.String(50), nullable=True)
    observation = db.Column(db.String(255), nullable=True)
    financial_optional_table1_code = db.Column(db.String(50), nullable=True)
    financial_entry_movement_id = db.Column(db.String(50), nullable=True)
    generated_entry_number = db.Column(db.String(50), nullable=True)
    open_entry_number = db.Column(db.String(50), nullable=True)
    cash_account_code = db.Column(db.String(50), nullable=True)
    customer_vendor_company_id = db.Column(db.String(50), nullable=True)
    fluxus_grouped_flag = db.Column(db.Boolean, nullable=True)
    cash_account_company_id = db.Column(db.String(50), nullable=True)
    long_history = db.Column(db.String(255), nullable=True)

# JSON field processing methods:
    def get_json_field(self, field_name):
        """Get a JSON field as a Python object"""
        field_value = getattr(self, field_name, None)
        if field_value:
            try:
                return json.loads(field_value) if isinstance(field_value, str) else field_value
            except (json.JSONDecodeError, TypeError):
                return []
        return []

    def set_json_field(self, field_name, value):
        """Set a JSON field from a Python object"""
        if value:
            json_value = json.dumps(value) if not isinstance(value, str) else value
            setattr(self, field_name, json_value)
        else:
            setattr(self, field_name, None)


----- FILE: create_test_excel.py  (sha1:796d3b505bc3, bytes:4766) -----
import pandas as pd
from datetime import datetime
import json

# Create a test Excel file with the new company and branch fields
data = []

# Create 5 test records with the new structure
for i in range(1, 6):
    record = {
        # New company and branch fields (first 4 columns)
        'Empresa_Code': f'EMP_{i:03d}',
        'Empresa_Nome': f'Empresa Teste {i} Ltda',
        'Filial_Code': f'FIL_{i:03d}',
        'Filial_Nome': f'Filial {i} - Centro',
        
        # Existing core fields
        'internalId': f'{i}|{400+i}',
        'companyId': i,
        'movementId': 400+i,
        'branchId': i,
        'warehouseCode': str(i),
        'destinyWarehouseCode': str(i),
        'number': 12000+i,
        'series': 'A',
        'movementTypeCode': '1.2.02',
        'type': 'P',
        'status': 'F',
        'printed': False,
        'documentPrinted': False,
        'billPrinted': False,
        'registerDate': datetime.now(),
        'exitDate': datetime.now(),
        'commercialRepresentativeCharge': 0.0,
        'grossValue': 100.0 * i,
        'netValue': 100.0 * i,
        'informedNetValue': 0.0,
        'otherValues': 100.0 * i,
        'discountPercentage': 0.0,
        'expensePercentage': 0.0,
        'expenseValue': 0.0,
        'extraPercentage1': 0.0,
        'extraValue1': 0.0,
        'extraPercentage2': 0.0,
        'extraValue2': 0.0,
        'transportedProductNetWeight': 0.0,
        'transportedProductGrossWeight': 0.0,
        'classificationTable5Code': '',
        'financialOptionalTable2Code': '',
        'netValueCurrencyCode': 'R$',
        'date': datetime.now(),
        'hasGeneratedBill': False,
        'auxCustomerVendorCode': f'C{i:05d}',
        'auxCustomerVendorCompanyId': 0,
        'costCenterCode': '',
        'salesman1Code': '',
        'chargePercentage': 0.0,
        'salesman2ChargePercentage': 0.0,
        'salesman3ChargePercentage': 0.0,
        'salesman4ChargePercentage': 0.0,
        'userCode': 'testuser',
        'destinyBranchId': i,
        'lotGenerated': False,
        'accountingExportStatus': '0',
        'deliveryDate': '',
        'hasGeneratedWorkAccount': False,
        'workAccountGenerated': 'false',
        'lastEditTime': datetime.now(),
        'indicateObjectUse': '0',
        'bonumIntegrated': False,
        'processedFlag': False,
        'icmsDeductionValue': 0.0,
        'creationUser': 'testuser',
        'creationDate': datetime.now(),
        'emailStatus': 'false',
        'internalGrossValue': 100.0 * i,
        'otherCompanyINSSBaseValue': 0.0,
        'conditionalDiscountValue': 0.0,
        'conditionalExpenseValue': 0.0,
        'affectStockOrder': str(400+i),
        'commercialAutomationExported': 0,
        'aplicationIntegration': 'T',
        'entryDate': datetime.now(),
        'extemporaneous': 0,
        'merchandiseValue': 0.0,
        'usesFinancialValueApportionment': False,
        'conclusionFlag': 0,
        'paradigmaStatus': 'N',
        'paradigmaAutoIntegrated': False,
        'originalGrossValue': 0.0,
        'originalNetValue': 0.0,
        'originalOtherValues': 0.0,
        'operationId': 0,
        'scpBranchId': 0,
        'movementItems': '[]',
        'payments': '[]',
        'costCenterApportionments': '[]',
        'departmentApportionments': '[]',
        'taxes': '[]',
        'fiscal': '[]',
        'norm': '[]',
        'cargoComponent': '[]',
        'thirdPartyNF': '[]',
        'safetyDevice': '[]',
        'nfe': '[]',
        'inputCTRC': '[]',
        'outputCTRC': '[]',
        'ctrc': '[]',
        'transportData': '[]',
        'documentAuthorization': '[]',
        'judicialProcess': '[]',
        'serviceOrder': '[]',
        'relatedMovement': '[]',
        'exportRelatedMovement': '[]',
        'linkedMovement': '[]',
        'cTe': '[]',
        'eaiIntegration': '[]',
        'electronicInvoiceFreeFields': '[]',
        'customerVendorCode': f'C{i:05d}',
        'paymentTermCode': '30.0',
        'observation': f'Teste de movimento {i} com dados de empresa',
        'financialOptionalTable1Code': '2.0',
        'financialEntryMovementId': f'{400+i}.0',
        'generatedEntryNumber': '1.0',
        'openEntryNumber': '1.0',
        'cashAccountCode': '2.0',
        'customerVendorCompanyId': 0,
        'fluxusGroupedFlag': False,
        'cashAccountCompanyId': i
    }
    data.append(record)

# Create DataFrame and save to Excel
df = pd.DataFrame(data)
filename = 'test_excel_with_company_branch.xlsx'
df.to_excel(filename, index=False)
print(f"Created test Excel file: {filename}")
print(f"Columns: {len(df.columns)}")
print(f"Records: {len(df)}")
print("First 4 columns (new):")
for col in df.columns[:4]:
    print(f"  {col}: {df[col].iloc[0]}")

----- FILE: file_processor.py  (sha1:d5fa2a1fb139, bytes:22866) -----
import pandas as pd
import json
import uuid
import ast
from datetime import datetime
from typing import Dict, List, Tuple, Any
import logging

logger = logging.getLogger(__name__)

class FinancialDataProcessor:
    """Process Excel files containing financial movement data"""
    
    # Complete column mapping for all 117 fields (added 4 new company/branch fields)
    COLUMN_MAPPING = {
        'internalId': 'internal_id',
        'companyId': 'company_id',
        'movementId': 'movement_id',
        'branchId': 'branch_id',
        
        # New company and branch information fields
        'Empresa_Code': 'empresa_code',
        'Empresa_Nome': 'empresa_nome',
        'Filial_Code': 'filial_code',
        'Filial_Nome': 'filial_nome',
        'warehouseCode': 'warehouse_code',
        'destinyWarehouseCode': 'destiny_warehouse_code',
        'number': 'number',
        'series': 'series',
        'movementTypeCode': 'movement_type_code',
        'type': 'type',
        'status': 'status',
        'printed': 'printed',
        'documentPrinted': 'document_printed',
        'billPrinted': 'bill_printed',
        'registerDate': 'register_date',
        'exitDate': 'exit_date',
        'commercialRepresentativeCharge': 'commercial_representative_charge',
        'grossValue': 'gross_value',
        'netValue': 'net_value',
        'informedNetValue': 'informed_net_value',
        'otherValues': 'other_values',
        'discountPercentage': 'discount_percentage',
        'expensePercentage': 'expense_percentage',
        'expenseValue': 'expense_value',
        'extraPercentage1': 'extra_percentage1',
        'extraValue1': 'extra_value1',
        'extraPercentage2': 'extra_percentage2',
        'extraValue2': 'extra_value2',
        'transportedProductNetWeight': 'transported_product_net_weight',
        'transportedProductGrossWeight': 'transported_product_gross_weight',
        'classificationTable5Code': 'classification_table5_code',
        'financialOptionalTable2Code': 'financial_optional_table2_code',
        'netValueCurrencyCode': 'net_value_currency_code',
        'date': 'date',
        'hasGeneratedBill': 'has_generated_bill',
        'auxCustomerVendorCode': 'aux_customer_vendor_code',
        'customerVendorName': 'customer_vendor_name',
        'customerVendorCNPJ': 'customer_vendor_cnpj',
        'cnpj': 'customer_vendor_cnpj',
        'razao_social': 'customer_vendor_name',
        'auxCustomerVendorCompanyId': 'aux_customer_vendor_company_id',
        'customerVendorName': 'customer_vendor_name',
        'customerVendorCNPJ': 'customer_vendor_cnpj',
        'auxCustomerVendorCompanyId': 'aux_customer_vendor_company_id',
        'costCenterCode': 'cost_center_code',
        'salesman1Code': 'salesman1_code',
        'chargePercentage': 'charge_percentage',
        'salesman2ChargePercentage': 'salesman2_charge_percentage',
        'salesman3ChargePercentage': 'salesman3_charge_percentage',
        'salesman4ChargePercentage': 'salesman4_charge_percentage',
        'userCode': 'user_code',
        'destinyBranchId': 'destiny_branch_id',
        'lotGenerated': 'lot_generated',
        'accountingExportStatus': 'accounting_export_status',
        'deliveryDate': 'delivery_date',
        'hasGeneratedWorkAccount': 'has_generated_work_account',
        'workAccountGenerated': 'work_account_generated',
        'lastEditTime': 'last_edit_time',
        'indicateObjectUse': 'indicate_object_use',
        'bonumIntegrated': 'bonum_integrated',
        'processedFlag': 'processed_flag',
        'icmsDeductionValue': 'icms_deduction_value',
        'creationUser': 'creation_user',
        'creationDate': 'creation_date',
        'emailStatus': 'email_status',
        'internalGrossValue': 'internal_gross_value',
        'otherCompanyINSSBaseValue': 'other_company_inss_base_value',
        'conditionalDiscountValue': 'conditional_discount_value',
        'conditionalExpenseValue': 'conditional_expense_value',
        'affectStockOrder': 'affect_stock_order',
        'commercialAutomationExported': 'commercial_automation_exported',
        'aplicationIntegration': 'aplication_integration',
        'entryDate': 'entry_date',
        'extemporaneous': 'extemporaneous',
        'merchandiseValue': 'merchandise_value',
        'usesFinancialValueApportionment': 'uses_financial_value_apportionment',
        'conclusionFlag': 'conclusion_flag',
        'paradigmaStatus': 'paradigma_status',
        'paradigmaAutoIntegrated': 'paradigma_auto_integrated',
        'originalGrossValue': 'original_gross_value',
        'originalNetValue': 'original_net_value',
        'originalOtherValues': 'original_other_values',
        'operationId': 'operation_id',
        'scpBranchId': 'scp_branch_id',
        'movementItems': 'movement_items',
        'payments': 'payments',
        'costCenterApportionments': 'cost_center_apportionments',
        'departmentApportionments': 'department_apportionments',
        'taxes': 'taxes',
        'fiscal': 'fiscal',
        'norm': 'norm',
        'cargoComponent': 'cargo_component',
        'thirdPartyNF': 'third_party_nf',
        'safetyDevice': 'safety_device',
        'nfe': 'nfe',
        'inputCTRC': 'input_ctrc',
        'outputCTRC': 'output_ctrc',
        'ctrc': 'ctrc',
        'transportData': 'transport_data',
        'documentAuthorization': 'document_authorization',
        'judicialProcess': 'judicial_process',
        'serviceOrder': 'service_order',
        'relatedMovement': 'related_movement',
        'exportRelatedMovement': 'export_related_movement',
        'linkedMovement': 'linked_movement',
        'cTe': 'c_te',
        'eaiIntegration': 'eai_integration',
        'electronicInvoiceFreeFields': 'electronic_invoice_free_fields',
        'customerVendorCode': 'customer_vendor_code',
        'paymentTermCode': 'payment_term_code',
        'observation': 'observation',
        'financialOptionalTable1Code': 'financial_optional_table1_code',
        'financialEntryMovementId': 'financial_entry_movement_id',
        'generatedEntryNumber': 'generated_entry_number',
        'openEntryNumber': 'open_entry_number',
        'cashAccountCode': 'cash_account_code',
        'customerVendorCompanyId': 'customer_vendor_company_id',
        'fluxusGroupedFlag': 'fluxus_grouped_flag',
        'cashAccountCompanyId': 'cash_account_company_id',
        'longHistory': 'long_history',
    }
    
    # JSON fields that need special processing
    JSON_FIELDS = {
        'movement_items', 'payments', 'cost_center_apportionments', 'department_apportionments',
        'taxes', 'fiscal', 'norm', 'cargo_component', 'third_party_nf', 'safety_device',
        'nfe', 'input_ctrc', 'output_ctrc', 'ctrc', 'transport_data', 'document_authorization',
        'judicial_process', 'service_order', 'related_movement', 'export_related_movement',
        'linked_movement', 'c_te', 'eai_integration', 'electronic_invoice_free_fields'
    }
    
    # Date fields that need special processing
    DATE_FIELDS = {
        'register_date', 'exit_date', 'delivery_date', 'creation_date', 'last_edit_time', 
        'date', 'entry_date'
    }

    def __init__(self):
        self.batch_id = None

    def process_file(self, file_path: str, user_id: int) -> Tuple[str, int, int, List[str], List[dict]]:
        """
        Process an Excel file and return results
        
        Returns:
            Tuple containing:
            - batch_id (str): Unique identifier for this upload batch
            - success_count (int): Number of successfully processed records
            - error_count (int): Number of records with errors
            - errors (List[str]): List of error messages
            - processed_data (List[dict]): List of processed records for database insertion
        """
        from app import db
        from models import FinancialMovement, UploadHistory
        
        self.batch_id = str(uuid.uuid4())
        errors = []
        processed_data = []
        success_count = 0
        error_count = 0
        
        try:
            # Read Excel file with increased column width limits
            logger.info(f"Reading Excel file: {file_path}")
            
            # Configure pandas to handle large text fields better
            pd.set_option('display.max_colwidth', None)
            
            # First, check if the file has multiple sheets
            excel_file = pd.ExcelFile(file_path)
            available_sheets = excel_file.sheet_names
            logger.info(f"Available sheets in Excel file: {available_sheets}")
            
            # Determine which sheet to use
            target_sheet = self._select_data_sheet(available_sheets)
            logger.info(f"Using sheet: '{target_sheet}' for data processing")
            
            # Read the selected sheet
            df = pd.read_excel(file_path, sheet_name=target_sheet, dtype=str)  # Read all as strings first
            logger.info(f"Read Excel file with {len(df)} rows from sheet '{target_sheet}'")
            
            # Process each row
            for index, row in df.iterrows():
                # Ensure proper handling of row index
                row_num = index + 1 if isinstance(index, int) else int(index) + 1
                
                try:
                    # Convert row to dictionary and map columns
                    record_data = self._process_row(row)
                    
                    # Add tracking fields
                    record_data['upload_batch_id'] = self.batch_id
                    record_data['uploaded_by'] = user_id
                    record_data['uploaded_at'] = datetime.utcnow()
                    
                    processed_data.append(record_data)
                    success_count += 1
                    
                except Exception as e:
                    error_msg = f"Row {row_num}: {str(e)}"
                    errors.append(error_msg)
                    error_count += 1
                    logger.error(error_msg)
            
            # Create database records
            if processed_data:
                self._save_to_database(processed_data, file_path, user_id, success_count, error_count, errors)
            
            return self.batch_id, success_count, error_count, errors, processed_data
            
        except Exception as e:
            error_msg = f"Failed to process file: {str(e)}"
            logger.error(error_msg)
            return self.batch_id or str(uuid.uuid4()), 0, 1, [error_msg], []

    def _process_row(self, row: pd.Series) -> Dict[str, Any]:
        """Process a single row of data"""
        processed = {}
        
        for excel_col, db_col in self.COLUMN_MAPPING.items():
            if excel_col in row.index:
                value = row[excel_col]
                
                # Handle None/NaN values
                if pd.isna(value):
                    processed[db_col] = None
                    continue
                
                # Process JSON fields
                if db_col in self.JSON_FIELDS:
                    processed[db_col] = self._process_json_field(value)
                
                # Process date fields
                elif db_col in self.DATE_FIELDS:
                    processed[db_col] = self._process_date_field(value)
                
                # Process boolean fields
                elif db_col.endswith('_flag') or db_col.startswith('has_') or db_col in ['printed', 'document_printed', 'bill_printed', 'lot_generated', 'bonum_integrated', 'processed_flag', 'uses_financial_value_apportionment', 'paradigma_auto_integrated', 'fluxus_grouped_flag']:
                    processed[db_col] = self._process_boolean_field(value)
                
                # Process numeric fields
                elif db_col in ['gross_value', 'net_value', 'informed_net_value', 'other_values', 
                               'discount_percentage', 'expense_percentage', 'expense_value',
                               'extra_percentage1', 'extra_value1', 'extra_percentage2', 'extra_value2',
                               'transported_product_net_weight', 'transported_product_gross_weight',
                               'commercial_representative_charge', 'charge_percentage',
                               'salesman2_charge_percentage', 'salesman3_charge_percentage', 
                               'salesman4_charge_percentage', 'icms_deduction_value',
                               'internal_gross_value', 'other_company_inss_base_value',
                               'conditional_discount_value', 'conditional_expense_value',
                               'merchandise_value', 'original_gross_value', 'original_net_value',
                               'original_other_values']:
                    try:
                        str_val = str(value).strip()
                        processed[db_col] = float(str_val) if str_val and str_val != 'nan' else 0.0
                    except (ValueError, TypeError):
                        processed[db_col] = 0.0
                elif db_col in ['company_id', 'movement_id', 'branch_id', 'aux_customer_vendor_company_id',
                               'destiny_branch_id', 'customer_vendor_company_id', 'cash_account_company_id',
                               'financial_entry_movement_id', 'generated_entry_number', 'open_entry_number',
                               'operation_id', 'scp_branch_id']:
                    try:
                        str_val = str(value).strip()
                        processed[db_col] = int(float(str_val)) if str_val and str_val != 'nan' else None
                    except (ValueError, TypeError):
                        processed[db_col] = None
                else:
                    processed[db_col] = str(value) if value is not None else None
            else:
                # Set default values for missing columns based on their type
                if db_col in ['gross_value', 'net_value', 'informed_net_value', 'other_values', 
                             'discount_percentage', 'expense_percentage', 'expense_value',
                             'extra_percentage1', 'extra_value1', 'extra_percentage2', 'extra_value2',
                             'transported_product_net_weight', 'transported_product_gross_weight',
                             'commercial_representative_charge', 'charge_percentage',
                             'salesman2_charge_percentage', 'salesman3_charge_percentage', 
                             'salesman4_charge_percentage', 'icms_deduction_value',
                             'internal_gross_value', 'other_company_inss_base_value',
                             'conditional_discount_value', 'conditional_expense_value',
                             'merchandise_value', 'original_gross_value', 'original_net_value',
                             'original_other_values']:
                    processed[db_col] = 0.0
                elif db_col in self.JSON_FIELDS:
                    processed[db_col] = json.dumps([])
                elif db_col.endswith('_flag') or db_col.startswith('has_') or db_col in ['printed', 'document_printed', 'bill_printed', 'lot_generated', 'bonum_integrated', 'processed_flag', 'uses_financial_value_apportionment', 'paradigma_auto_integrated', 'fluxus_grouped_flag']:
                    processed[db_col] = False
                else:
                    processed[db_col] = None
        # --- Complementa campos de cliente/fornecedor se vierem com nomes alternativos ---
        if not processed.get('customer_vendor_name'):
            for alt_col in ['CustomerVendorName', 'Cliente', 'Fornecedor', 'RazaoSocial']:
                if alt_col in row.index and not pd.isna(row[alt_col]):
                    processed['customer_vendor_name'] = str(row[alt_col]).strip()
                    break

        if not processed.get('customer_vendor_cnpj'):
            for alt_col in ['CustomerVendorCNPJ', 'CNPJ', 'CPF', 'Documento']:
                if alt_col in row.index and not pd.isna(row[alt_col]):
                    processed['customer_vendor_cnpj'] = self._format_cnpj(str(row[alt_col]).strip())
                    break

        return processed

    def _process_json_field(self, value: Any) -> str:
        """Process JSON field values - handles Python literal format from Excel with truncation recovery"""
        if value is None or pd.isna(value):
            return json.dumps([])
        
        if isinstance(value, str):
            # Check if it's empty string or just whitespace
            if not value.strip():
                return json.dumps([])
            
            fixed_value = value.strip()
            
            # First, try to parse as standard JSON
            try:
                parsed = json.loads(fixed_value)
                return fixed_value
            except (json.JSONDecodeError, TypeError):
                pass
            
            # Try to parse as Python literal (with single quotes)
            try:
                parsed = ast.literal_eval(fixed_value)
                # Convert to proper JSON string
                return json.dumps(parsed)
            except (ValueError, SyntaxError) as e:
                # Check if the value looks like it was truncated
                if (fixed_value.startswith('[') and not fixed_value.endswith(']')) or \
                   (fixed_value.startswith('{') and not fixed_value.endswith('}')):
                    logger.warning(f"JSON field appears truncated, returning empty array: {str(value)[:50]}...")
                    return json.dumps([])
                
                # Try to fix common truncation issues
                try:
                    # If it starts with [ but doesn't end properly, try to close it
                    if fixed_value.startswith('[') and not fixed_value.endswith(']'):
                        # Find the last complete object and close the array
                        last_brace_close = fixed_value.rfind('}')
                        if last_brace_close != -1:
                            truncated_fix = fixed_value[:last_brace_close + 1] + ']'
                            parsed = ast.literal_eval(truncated_fix)
                            logger.info(f"Successfully recovered truncated JSON array")
                            return json.dumps(parsed)
                    
                    # If it's a single object that got cut off
                    if fixed_value.startswith('{') and not fixed_value.endswith('}'):
                        logger.warning(f"Truncated object detected, returning empty array")
                        return json.dumps([])
                        
                except (ValueError, SyntaxError):
                    pass
                
                # Log the error for debugging but don't break the import
                logger.warning(f"Could not parse JSON field: {str(value)[:100]}... Error: {str(e)}")
                # Return empty array to avoid breaking the system
                return json.dumps([])
        
        if isinstance(value, list):
            return json.dumps(value)
        
        # For other types, wrap in array
        return json.dumps([value] if value else [])

    def _process_date_field(self, value: Any):
        """Process date field values"""
        if value is None or pd.isna(value):
            return None
        
        if isinstance(value, datetime):
            return value
        
        if isinstance(value, str):
            try:
                return datetime.fromisoformat(value.replace('Z', '+00:00'))
            except ValueError:
                try:
                    return pd.to_datetime(value).to_pydatetime()
                except (ValueError, TypeError):
                    return None
        
        if hasattr(value, 'to_pydatetime'):
            return value.to_pydatetime()
        
        return None

    def _process_boolean_field(self, value: Any) -> bool:
        """Process boolean field values"""
        if value is None or pd.isna(value):
            return False
        
        str_val = str(value).strip().upper()
        return str_val in ['TRUE', '1', 'YES', 'Y', 'T', 'VERDADEIRO'] if str_val else False

    def _save_to_database(self, processed_data: List[Dict], filename: str, 
                         user_id: int, success_count: int, error_count: int, errors: List[str]):
        """Save processed data to database"""
        from app import db
        from models import FinancialMovement, UploadHistory
        
        # Create upload history record
        upload_history = UploadHistory()
        upload_history.batch_id = self.batch_id
        upload_history.filename = filename.split('/')[-1]  # Get just filename without path
        upload_history.uploaded_by = user_id
        upload_history.total_rows = len(processed_data)
        upload_history.success_count = success_count
        upload_history.error_count = error_count
        upload_history.errors = json.dumps(errors) if errors else None
        upload_history.status = 'completed' if error_count == 0 else 'completed_with_errors'
        
        # Create movement records
        movements = [FinancialMovement(**data) for data in processed_data]
        
        # Save to database
        db.session.add(upload_history)
        db.session.add_all(movements)
        db.session.commit()
        
        logger.info(f"Saved {success_count} records to database with batch_id {self.batch_id}")
    
    def _select_data_sheet(self, available_sheets: List[str]) -> str:
        """
        Select the appropriate sheet to process based on the new Excel structure.
        Priority: 'Dados' > 'Data' > first sheet with substantial data
        """
        # Check for preferred sheet names (new structure)
        preferred_sheets = ['Dados', 'Data', 'dados', 'data']
        
        for preferred in preferred_sheets:
            if preferred in available_sheets:
                logger.info(f"Found preferred sheet: '{preferred}'")
                return preferred
        
        # Check for sheets that are likely to contain movement data
        data_sheets = []
        skip_sheets = ['Árvore', 'árvore', 'Tree', 'tree', 'Resumo', 'resumo', 'Summary', 'summary']
        
        for sheet in available_sheets:
            if sheet not in skip_sheets:
                data_sheets.append(sheet)
        
        if data_sheets:
            selected = data_sheets[0]
            logger.info(f"Selected data sheet: '{selected}' from available data sheets: {data_sheets}")
            return selected
        
        # Fallback to first sheet
        logger.warning(f"No preferred data sheet found, using first sheet: '{available_sheets[0]}'")
        return available_sheets[0]

----- FILE: file_processor_new.py  (sha1:df649784c688, bytes:12948) -----
import pandas as pd
import json
import uuid
from datetime import datetime
from typing import Dict, List, Tuple, Any
import logging

logger = logging.getLogger(__name__)

class FinancialDataProcessor:
    """Process Excel files containing financial movement data"""
    
    # Complete column mapping for all 113 fields
    COLUMN_MAPPING = {
        'internalId': 'internal_id',
        'companyId': 'company_id',
        'movementId': 'movement_id',
        'branchId': 'branch_id',
        'warehouseCode': 'warehouse_code',
        'destinyWarehouseCode': 'destiny_warehouse_code',
        'number': 'number',
        'series': 'series',
        'movementTypeCode': 'movement_type_code',
        'type': 'type',
        'status': 'status',
        'printed': 'printed',
        'documentPrinted': 'document_printed',
        'billPrinted': 'bill_printed',
        'registerDate': 'register_date',
        'exitDate': 'exit_date',
        'commercialRepresentativeCharge': 'commercial_representative_charge',
        'grossValue': 'gross_value',
        'netValue': 'net_value',
        'informedNetValue': 'informed_net_value',
        'otherValues': 'other_values',
        'discountPercentage': 'discount_percentage',
        'expensePercentage': 'expense_percentage',
        'expenseValue': 'expense_value',
        'extraPercentage1': 'extra_percentage1',
        'extraValue1': 'extra_value1',
        'extraPercentage2': 'extra_percentage2',
        'extraValue2': 'extra_value2',
        'transportedProductNetWeight': 'transported_product_net_weight',
        'transportedProductGrossWeight': 'transported_product_gross_weight',
        'classificationTable5Code': 'classification_table5_code',
        'financialOptionalTable2Code': 'financial_optional_table2_code',
        'netValueCurrencyCode': 'net_value_currency_code',
        'date': 'date',
        'hasGeneratedBill': 'has_generated_bill',
        'auxCustomerVendorCode': 'aux_customer_vendor_code',
        'auxCustomerVendorCompanyId': 'aux_customer_vendor_company_id',
        'costCenterCode': 'cost_center_code',
        'salesman1Code': 'salesman1_code',
        'chargePercentage': 'charge_percentage',
        'salesman2ChargePercentage': 'salesman2_charge_percentage',
        'salesman3ChargePercentage': 'salesman3_charge_percentage',
        'salesman4ChargePercentage': 'salesman4_charge_percentage',
        'userCode': 'user_code',
        'destinyBranchId': 'destiny_branch_id',
        'lotGenerated': 'lot_generated',
        'accountingExportStatus': 'accounting_export_status',
        'deliveryDate': 'delivery_date',
        'hasGeneratedWorkAccount': 'has_generated_work_account',
        'workAccountGenerated': 'work_account_generated',
        'lastEditTime': 'last_edit_time',
        'indicateObjectUse': 'indicate_object_use',
        'bonumIntegrated': 'bonum_integrated',
        'processedFlag': 'processed_flag',
        'icmsDeductionValue': 'icms_deduction_value',
        'creationUser': 'creation_user',
        'creationDate': 'creation_date',
        'emailStatus': 'email_status',
        'internalGrossValue': 'internal_gross_value',
        'otherCompanyINSSBaseValue': 'other_company_inss_base_value',
        'conditionalDiscountValue': 'conditional_discount_value',
        'conditionalExpenseValue': 'conditional_expense_value',
        'affectStockOrder': 'affect_stock_order',
        'commercialAutomationExported': 'commercial_automation_exported',
        'aplicationIntegration': 'aplication_integration',
        'entryDate': 'entry_date',
        'extemporaneous': 'extemporaneous',
        'merchandiseValue': 'merchandise_value',
        'usesFinancialValueApportionment': 'uses_financial_value_apportionment',
        'conclusionFlag': 'conclusion_flag',
        'paradigmaStatus': 'paradigma_status',
        'paradigmaAutoIntegrated': 'paradigma_auto_integrated',
        'originalGrossValue': 'original_gross_value',
        'originalNetValue': 'original_net_value',
        'originalOtherValues': 'original_other_values',
        'operationId': 'operation_id',
        'scpBranchId': 'scp_branch_id',
        'movementItems': 'movement_items',
        'payments': 'payments',
        'costCenterApportionments': 'cost_center_apportionments',
        'departmentApportionments': 'department_apportionments',
        'taxes': 'taxes',
        'fiscal': 'fiscal',
        'norm': 'norm',
        'cargoComponent': 'cargo_component',
        'thirdPartyNF': 'third_party_nf',
        'safetyDevice': 'safety_device',
        'nfe': 'nfe',
        'inputCTRC': 'input_ctrc',
        'outputCTRC': 'output_ctrc',
        'ctrc': 'ctrc',
        'transportData': 'transport_data',
        'documentAuthorization': 'document_authorization',
        'judicialProcess': 'judicial_process',
        'serviceOrder': 'service_order',
        'relatedMovement': 'related_movement',
        'exportRelatedMovement': 'export_related_movement',
        'linkedMovement': 'linked_movement',
        'cTe': 'c_te',
        'eaiIntegration': 'eai_integration',
        'electronicInvoiceFreeFields': 'electronic_invoice_free_fields',
        'customerVendorCode': 'customer_vendor_code',
        'paymentTermCode': 'payment_term_code',
        'observation': 'observation',
        'financialOptionalTable1Code': 'financial_optional_table1_code',
        'financialEntryMovementId': 'financial_entry_movement_id',
        'generatedEntryNumber': 'generated_entry_number',
        'openEntryNumber': 'open_entry_number',
        'cashAccountCode': 'cash_account_code',
        'customerVendorCompanyId': 'customer_vendor_company_id',
        'fluxusGroupedFlag': 'fluxus_grouped_flag',
        'cashAccountCompanyId': 'cash_account_company_id',
        'longHistory': 'long_history',
    }
    
    # JSON fields that need special processing
    JSON_FIELDS = {
        'movement_items', 'payments', 'cost_center_apportionments', 'department_apportionments',
        'taxes', 'fiscal', 'norm', 'cargo_component', 'third_party_nf', 'safety_device',
        'nfe', 'input_ctrc', 'output_ctrc', 'ctrc', 'transport_data', 'document_authorization',
        'judicial_process', 'service_order', 'related_movement', 'export_related_movement',
        'linked_movement', 'c_te', 'eai_integration', 'electronic_invoice_free_fields'
    }
    
    # Date fields that need special processing
    DATE_FIELDS = {
        'register_date', 'exit_date', 'delivery_date', 'creation_date', 'last_edit_time', 
        'date', 'entry_date'
    }

    def __init__(self):
        self.batch_id = None

    def process_file(self, file_path: str, user_id: int) -> Tuple[str, int, int, List[str], List[dict]]:
        """
        Process an Excel file and return results
        
        Returns:
            Tuple containing:
            - batch_id (str): Unique identifier for this upload batch
            - success_count (int): Number of successfully processed records
            - error_count (int): Number of records with errors
            - errors (List[str]): List of error messages
            - processed_data (List[dict]): List of processed records for database insertion
        """
        from app import db
        from models import FinancialMovement, UploadHistory
        
        self.batch_id = str(uuid.uuid4())
        errors = []
        processed_data = []
        success_count = 0
        error_count = 0
        
        try:
            # Read Excel file
            logger.info(f"Reading Excel file: {file_path}")
            df = pd.read_excel(file_path)
            logger.info(f"Read Excel file with {len(df)} rows")
            
            # Process each row
            for index, row in df.iterrows():
                try:
                    # Convert row to dictionary and map columns
                    record_data = self._process_row(row)
                    
                    # Add tracking fields
                    record_data['upload_batch_id'] = self.batch_id
                    record_data['uploaded_by'] = user_id
                    record_data['uploaded_at'] = datetime.utcnow()
                    
                    processed_data.append(record_data)
                    success_count += 1
                    
                except Exception as e:
                    error_msg = f"Row {index + 1}: {str(e)}"
                    errors.append(error_msg)
                    error_count += 1
                    logger.error(error_msg)
            
            # Create database records
            if processed_data:
                self._save_to_database(processed_data, file_path, user_id, success_count, error_count, errors)
            
            return self.batch_id, success_count, error_count, errors, processed_data
            
        except Exception as e:
            error_msg = f"Failed to process file: {str(e)}"
            logger.error(error_msg)
            return self.batch_id or str(uuid.uuid4()), 0, 1, [error_msg], []

    def _process_row(self, row: pd.Series) -> Dict[str, Any]:
        """Process a single row of data"""
        processed = {}
        
        for excel_col, db_col in self.COLUMN_MAPPING.items():
            if excel_col in row:
                value = row[excel_col]
                
                # Handle None/NaN values
                if pd.isna(value):
                    processed[db_col] = None
                    continue
                
                # Process JSON fields
                if db_col in self.JSON_FIELDS:
                    processed[db_col] = self._process_json_field(value)
                
                # Process date fields
                elif db_col in self.DATE_FIELDS:
                    processed[db_col] = self._process_date_field(value)
                
                # Process boolean fields
                elif isinstance(value, bool) or db_col.endswith('_flag') or db_col.startswith('has_'):
                    processed[db_col] = bool(value) if value is not None else None
                
                # Process numeric fields
                elif isinstance(value, (int, float)):
                    processed[db_col] = value
                
                # Process string fields
                else:
                    processed[db_col] = str(value) if value is not None else None
            else:
                processed[db_col] = None
                
        return processed

    def _process_json_field(self, value: Any) -> str:
        """Process JSON field values"""
        if value is None or pd.isna(value):
            return None
        
        if isinstance(value, str):
            # If it's already a JSON string, return as is
            try:
                json.loads(value)
                return value
            except (json.JSONDecodeError, TypeError):
                return json.dumps([])
        
        if isinstance(value, list):
            return json.dumps(value)
        
        # For other types, wrap in array
        return json.dumps([value] if value else [])

    def _process_date_field(self, value: Any) -> datetime:
        """Process date field values"""
        if value is None or pd.isna(value):
            return None
        
        if isinstance(value, datetime):
            return value
        
        if isinstance(value, str):
            try:
                return datetime.fromisoformat(value.replace('Z', '+00:00'))
            except ValueError:
                try:
                    return pd.to_datetime(value).to_pydatetime()
                except (ValueError, TypeError):
                    return None
        
        if hasattr(value, 'to_pydatetime'):
            return value.to_pydatetime()
        
        return None

    def _save_to_database(self, processed_data: List[Dict], filename: str, 
                         user_id: int, success_count: int, error_count: int, errors: List[str]):
        """Save processed data to database"""
        from app import db
        from models import FinancialMovement, UploadHistory
        
        # Create upload history record
        upload_history = UploadHistory(
            batch_id=self.batch_id,
            filename=filename.split('/')[-1],  # Get just filename without path
            uploaded_by=user_id,
            total_rows=len(processed_data),
            success_count=success_count,
            error_count=error_count,
            errors=json.dumps(errors) if errors else None,
            status='completed' if error_count == 0 else 'completed_with_errors'
        )
        
        # Create movement records
        movements = [FinancialMovement(**data) for data in processed_data]
        
        # Save to database
        db.session.add(upload_history)
        db.session.add_all(movements)
        db.session.commit()
        
        logger.info(f"Saved {success_count} records to database with batch_id {self.batch_id}")

----- FILE: forms.py  (sha1:1ca433563365, bytes:3299) -----
from flask_wtf import FlaskForm
from flask_wtf.file import FileField, FileRequired, FileAllowed
from wtforms import StringField, PasswordField, BooleanField, SubmitField, SelectField, TextAreaField
from wtforms.validators import DataRequired, Email, Length, EqualTo, ValidationError
from models import User

class LoginForm(FlaskForm):
    username = StringField('Username', validators=[DataRequired()])
    password = PasswordField('Password', validators=[DataRequired()])
    remember_me = BooleanField('Remember Me')
    submit = SubmitField('Sign In')

class UserForm(FlaskForm):
    username = StringField('Username', validators=[
        DataRequired(), 
        Length(min=3, max=80)
    ])
    email = StringField('Email', validators=[
        DataRequired(), 
        Email()
    ])
    password = PasswordField('Password', validators=[
        DataRequired(), 
        Length(min=6)
    ])
    password2 = PasswordField('Repeat Password', validators=[
        DataRequired(), 
        EqualTo('password', message='Passwords must match')
    ])
    is_admin = BooleanField('Administrator')
    submit = SubmitField('Create User')
    
    def __init__(self, original_username=None, original_email=None, *args, **kwargs):
        super(UserForm, self).__init__(*args, **kwargs)
        self.original_username = original_username
        self.original_email = original_email
    
    def validate_username(self, username):
        if username.data != self.original_username:
            user = User.query.filter_by(username=username.data).first()
            if user is not None:
                raise ValidationError('Username already exists.')
    
    def validate_email(self, email):
        if email.data != self.original_email:
            user = User.query.filter_by(email=email.data).first()
            if user is not None:
                raise ValidationError('Email already registered.')

class EditUserForm(FlaskForm):
    username = StringField('Username', validators=[
        DataRequired(), 
        Length(min=3, max=80)
    ])
    email = StringField('Email', validators=[
        DataRequired(), 
        Email()
    ])
    is_admin = BooleanField('Administrator')
    submit = SubmitField('Update User')
    
    def __init__(self, original_username=None, original_email=None, *args, **kwargs):
        super(EditUserForm, self).__init__(*args, **kwargs)
        self.original_username = original_username
        self.original_email = original_email
    
    def validate_username(self, username):
        if username.data != self.original_username:
            user = User.query.filter_by(username=username.data).first()
            if user is not None:
                raise ValidationError('Username already exists.')
    
    def validate_email(self, email):
        if email.data != self.original_email:
            user = User.query.filter_by(email=email.data).first()
            if user is not None:
                raise ValidationError('Email already registered.')

class FileUploadForm(FlaskForm):
    file = FileField('Excel File', validators=[
        FileRequired(),
        FileAllowed(['xlsx', 'xls'], 'Only Excel files (.xlsx, .xls) are allowed!')
    ])
    description = TextAreaField('Description (Optional)')
    submit = SubmitField('Upload File')


----- FILE: main.py  (sha1:c30053183d1f, bytes:99) -----
from app import app

if __name__ == "__main__":
    app.run(host="0.0.0.0", port=5000, debug=True)


----- FILE: models.py  (sha1:99a9acf5338b, bytes:9860) -----
from app import db
from flask_login import UserMixin
from datetime import datetime
import json

class User(UserMixin, db.Model):
    id = db.Column(db.Integer, primary_key=True)
    username = db.Column(db.String(80), unique=True, nullable=False)
    email = db.Column(db.String(120), unique=True, nullable=False)
    password_hash = db.Column(db.String(256), nullable=False)
    is_admin = db.Column(db.Boolean, default=False)
    created_at = db.Column(db.DateTime, default=datetime.utcnow)
    
    def __repr__(self):
        return f'<User {self.username}>'

class FinancialMovement(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    
    # Core identification fields - all 117 fields from Excel (added 4 new company/branch info fields)
    internal_id = db.Column(db.String(50), nullable=True)
    company_id = db.Column(db.Integer, nullable=True)
    movement_id = db.Column(db.Integer, nullable=True)
    branch_id = db.Column(db.Integer, nullable=True)
    
    # Company and Branch information (new fields)
    empresa_code = db.Column(db.String(50), nullable=True)
    empresa_nome = db.Column(db.String(255), nullable=True)
    filial_code = db.Column(db.String(50), nullable=True)
    filial_nome = db.Column(db.String(255), nullable=True)
    warehouse_code = db.Column(db.String(50), nullable=True)
    destiny_warehouse_code = db.Column(db.String(50), nullable=True)
    number = db.Column(db.Integer, nullable=True)
    series = db.Column(db.String(10), nullable=True)
    movement_type_code = db.Column(db.String(50), nullable=True)
    type = db.Column(db.String(10), nullable=True)
    status = db.Column(db.String(10), nullable=True)
    printed = db.Column(db.Boolean, nullable=True)
    document_printed = db.Column(db.Boolean, nullable=True)
    bill_printed = db.Column(db.Boolean, nullable=True)
    register_date = db.Column(db.DateTime, nullable=True)
    exit_date = db.Column(db.DateTime, nullable=True)
    commercial_representative_charge = db.Column(db.Float, nullable=True)
    gross_value = db.Column(db.Float, nullable=True)
    net_value = db.Column(db.Float, nullable=True)
    informed_net_value = db.Column(db.Float, nullable=True)
    other_values = db.Column(db.Float, nullable=True)
    discount_percentage = db.Column(db.Float, nullable=True)
    expense_percentage = db.Column(db.Float, nullable=True)
    expense_value = db.Column(db.Float, nullable=True)
    extra_percentage1 = db.Column(db.Float, nullable=True)
    extra_value1 = db.Column(db.Float, nullable=True)
    extra_percentage2 = db.Column(db.Float, nullable=True)
    extra_value2 = db.Column(db.Float, nullable=True)
    transported_product_net_weight = db.Column(db.Float, nullable=True)
    transported_product_gross_weight = db.Column(db.Float, nullable=True)
    classification_table5_code = db.Column(db.String(50), nullable=True)
    financial_optional_table2_code = db.Column(db.String(50), nullable=True)
    net_value_currency_code = db.Column(db.String(10), nullable=True)
    date = db.Column(db.DateTime, nullable=True)
    has_generated_bill = db.Column(db.Boolean, nullable=True)
    aux_customer_vendor_code = db.Column(db.String(50), nullable=True)
    customer_vendor_name = db.Column(db.String(255), nullable=True)
    customer_vendor_cnpj = db.Column(db.String(30), nullable=True)
    aux_customer_vendor_company_id = db.Column(db.Integer, nullable=True)
    cost_center_code = db.Column(db.String(50), nullable=True)
    salesman1_code = db.Column(db.String(50), nullable=True)
    charge_percentage = db.Column(db.Float, nullable=True)
    salesman2_charge_percentage = db.Column(db.Float, nullable=True)
    salesman3_charge_percentage = db.Column(db.Float, nullable=True)
    salesman4_charge_percentage = db.Column(db.Float, nullable=True)
    user_code = db.Column(db.String(50), nullable=True)
    destiny_branch_id = db.Column(db.Integer, nullable=True)
    lot_generated = db.Column(db.Boolean, nullable=True)
    accounting_export_status = db.Column(db.String(50), nullable=True)
    delivery_date = db.Column(db.DateTime, nullable=True)
    has_generated_work_account = db.Column(db.Boolean, nullable=True)
    work_account_generated = db.Column(db.String(50), nullable=True)
    last_edit_time = db.Column(db.DateTime, nullable=True)
    indicate_object_use = db.Column(db.String(50), nullable=True)
    bonum_integrated = db.Column(db.Boolean, nullable=True)
    processed_flag = db.Column(db.Boolean, nullable=True)
    icms_deduction_value = db.Column(db.Float, nullable=True)
    creation_user = db.Column(db.String(50), nullable=True)
    creation_date = db.Column(db.DateTime, nullable=True)
    email_status = db.Column(db.String(50), nullable=True)
    internal_gross_value = db.Column(db.Float, nullable=True)
    other_company_inss_base_value = db.Column(db.Float, nullable=True)
    conditional_discount_value = db.Column(db.Float, nullable=True)
    conditional_expense_value = db.Column(db.Float, nullable=True)
    affect_stock_order = db.Column(db.String(50), nullable=True)
    commercial_automation_exported = db.Column(db.Integer, nullable=True)
    aplication_integration = db.Column(db.String(10), nullable=True)
    entry_date = db.Column(db.DateTime, nullable=True)
    extemporaneous = db.Column(db.Integer, nullable=True)
    merchandise_value = db.Column(db.Float, nullable=True)
    uses_financial_value_apportionment = db.Column(db.Boolean, nullable=True)
    conclusion_flag = db.Column(db.Integer, nullable=True)
    paradigma_status = db.Column(db.String(10), nullable=True)
    paradigma_auto_integrated = db.Column(db.Boolean, nullable=True)
    original_gross_value = db.Column(db.Float, nullable=True)
    original_net_value = db.Column(db.Float, nullable=True)
    original_other_values = db.Column(db.Float, nullable=True)
    operation_id = db.Column(db.Integer, nullable=True)
    scp_branch_id = db.Column(db.Integer, nullable=True)
    
    # JSON fields - properly formatted as Text columns
    movement_items = db.Column(db.Text, nullable=True)
    payments = db.Column(db.Text, nullable=True)
    cost_center_apportionments = db.Column(db.Text, nullable=True)
    department_apportionments = db.Column(db.Text, nullable=True)
    taxes = db.Column(db.Text, nullable=True)
    fiscal = db.Column(db.Text, nullable=True)
    norm = db.Column(db.Text, nullable=True)
    cargo_component = db.Column(db.Text, nullable=True)
    third_party_nf = db.Column(db.Text, nullable=True)
    safety_device = db.Column(db.Text, nullable=True)
    nfe = db.Column(db.Text, nullable=True)
    input_ctrc = db.Column(db.Text, nullable=True)
    output_ctrc = db.Column(db.Text, nullable=True)
    ctrc = db.Column(db.Text, nullable=True)
    transport_data = db.Column(db.Text, nullable=True)
    document_authorization = db.Column(db.Text, nullable=True)
    judicial_process = db.Column(db.Text, nullable=True)
    service_order = db.Column(db.Text, nullable=True)
    related_movement = db.Column(db.Text, nullable=True)
    export_related_movement = db.Column(db.Text, nullable=True)
    linked_movement = db.Column(db.Text, nullable=True)
    c_te = db.Column(db.Text, nullable=True)
    eai_integration = db.Column(db.Text, nullable=True)
    electronic_invoice_free_fields = db.Column(db.Text, nullable=True)
    
    # Remaining fields
    customer_vendor_code = db.Column(db.String(50), nullable=True)
    payment_term_code = db.Column(db.String(50), nullable=True)
    observation = db.Column(db.Text, nullable=True)
    financial_optional_table1_code = db.Column(db.String(50), nullable=True)
    financial_entry_movement_id = db.Column(db.String(50), nullable=True)
    generated_entry_number = db.Column(db.String(50), nullable=True)
    open_entry_number = db.Column(db.String(50), nullable=True)
    cash_account_code = db.Column(db.String(50), nullable=True)
    customer_vendor_company_id = db.Column(db.Integer, nullable=True)
    fluxus_grouped_flag = db.Column(db.Boolean, nullable=True)
    cash_account_company_id = db.Column(db.Integer, nullable=True)
    long_history = db.Column(db.Text, nullable=True)
    
    # Upload tracking
    upload_batch_id = db.Column(db.String(36))
    uploaded_by = db.Column(db.Integer, db.ForeignKey('user.id'))
    uploaded_at = db.Column(db.DateTime, default=datetime.utcnow)
    
    def get_json_field(self, field_name):
        """Get a JSON field as a Python object"""
        field_value = getattr(self, field_name, None)
        if field_value:
            try:
                return json.loads(field_value) if isinstance(field_value, str) else field_value
            except (json.JSONDecodeError, TypeError):
                return []
        return []

    def set_json_field(self, field_name, value):
        """Set a JSON field from a Python object"""
        if value:
            json_value = json.dumps(value) if not isinstance(value, str) else value
            setattr(self, field_name, json_value)
        else:
            setattr(self, field_name, None)
    
    def __repr__(self):
        return f'<FinancialMovement {self.internal_id}>'

class UploadHistory(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    batch_id = db.Column(db.String(36), unique=True, nullable=False)
    filename = db.Column(db.String(255), nullable=False)
    uploaded_by = db.Column(db.Integer, db.ForeignKey('user.id'), nullable=False)
    uploaded_at = db.Column(db.DateTime, default=datetime.utcnow)
    total_rows = db.Column(db.Integer, default=0)
    success_count = db.Column(db.Integer, default=0)
    error_count = db.Column(db.Integer, default=0)
    errors = db.Column(db.Text)
    status = db.Column(db.String(20), default='processing')
    
    # Relationship
    uploader = db.relationship('User', backref='uploads')
    
    def __repr__(self):
        return f'<UploadHistory {self.filename}>'

----- FILE: models_new.py  (sha1:61d2678030ae, bytes:9393) -----
from app import db
from flask_login import UserMixin
from datetime import datetime
import json

class User(UserMixin, db.Model):
    id = db.Column(db.Integer, primary_key=True)
    username = db.Column(db.String(80), unique=True, nullable=False)
    email = db.Column(db.String(120), unique=True, nullable=False)
    password_hash = db.Column(db.String(256), nullable=False)
    is_admin = db.Column(db.Boolean, default=False)
    created_at = db.Column(db.DateTime, default=datetime.utcnow)
    
    def __repr__(self):
        return f'<User {self.username}>'

class FinancialMovement(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    
    # Core identification fields - all 113 fields from Excel
    internal_id = db.Column(db.String(50), nullable=True)
    company_id = db.Column(db.Integer, nullable=True)
    movement_id = db.Column(db.Integer, nullable=True)
    branch_id = db.Column(db.Integer, nullable=True)
    warehouse_code = db.Column(db.String(50), nullable=True)
    destiny_warehouse_code = db.Column(db.String(50), nullable=True)
    number = db.Column(db.Integer, nullable=True)
    series = db.Column(db.String(10), nullable=True)
    movement_type_code = db.Column(db.String(50), nullable=True)
    type = db.Column(db.String(10), nullable=True)
    status = db.Column(db.String(10), nullable=True)
    printed = db.Column(db.Boolean, nullable=True)
    document_printed = db.Column(db.Boolean, nullable=True)
    bill_printed = db.Column(db.Boolean, nullable=True)
    register_date = db.Column(db.DateTime, nullable=True)
    exit_date = db.Column(db.DateTime, nullable=True)
    commercial_representative_charge = db.Column(db.Float, nullable=True)
    gross_value = db.Column(db.Float, nullable=True)
    net_value = db.Column(db.Float, nullable=True)
    informed_net_value = db.Column(db.Float, nullable=True)
    other_values = db.Column(db.Float, nullable=True)
    discount_percentage = db.Column(db.Float, nullable=True)
    expense_percentage = db.Column(db.Float, nullable=True)
    expense_value = db.Column(db.Float, nullable=True)
    extra_percentage1 = db.Column(db.Float, nullable=True)
    extra_value1 = db.Column(db.Float, nullable=True)
    extra_percentage2 = db.Column(db.Float, nullable=True)
    extra_value2 = db.Column(db.Float, nullable=True)
    transported_product_net_weight = db.Column(db.Float, nullable=True)
    transported_product_gross_weight = db.Column(db.Float, nullable=True)
    classification_table5_code = db.Column(db.String(50), nullable=True)
    financial_optional_table2_code = db.Column(db.String(50), nullable=True)
    net_value_currency_code = db.Column(db.String(10), nullable=True)
    date = db.Column(db.DateTime, nullable=True)
    has_generated_bill = db.Column(db.Boolean, nullable=True)
    aux_customer_vendor_code = db.Column(db.String(50), nullable=True)
    aux_customer_vendor_company_id = db.Column(db.Integer, nullable=True)
    cost_center_code = db.Column(db.String(50), nullable=True)
    salesman1_code = db.Column(db.String(50), nullable=True)
    charge_percentage = db.Column(db.Float, nullable=True)
    salesman2_charge_percentage = db.Column(db.Float, nullable=True)
    salesman3_charge_percentage = db.Column(db.Float, nullable=True)
    salesman4_charge_percentage = db.Column(db.Float, nullable=True)
    user_code = db.Column(db.String(50), nullable=True)
    destiny_branch_id = db.Column(db.Integer, nullable=True)
    lot_generated = db.Column(db.Boolean, nullable=True)
    accounting_export_status = db.Column(db.String(50), nullable=True)
    delivery_date = db.Column(db.DateTime, nullable=True)
    has_generated_work_account = db.Column(db.Boolean, nullable=True)
    work_account_generated = db.Column(db.String(50), nullable=True)
    last_edit_time = db.Column(db.DateTime, nullable=True)
    indicate_object_use = db.Column(db.String(50), nullable=True)
    bonum_integrated = db.Column(db.Boolean, nullable=True)
    processed_flag = db.Column(db.Boolean, nullable=True)
    icms_deduction_value = db.Column(db.Float, nullable=True)
    creation_user = db.Column(db.String(50), nullable=True)
    creation_date = db.Column(db.DateTime, nullable=True)
    email_status = db.Column(db.String(50), nullable=True)
    internal_gross_value = db.Column(db.Float, nullable=True)
    other_company_inss_base_value = db.Column(db.Float, nullable=True)
    conditional_discount_value = db.Column(db.Float, nullable=True)
    conditional_expense_value = db.Column(db.Float, nullable=True)
    affect_stock_order = db.Column(db.String(50), nullable=True)
    commercial_automation_exported = db.Column(db.Integer, nullable=True)
    aplication_integration = db.Column(db.String(10), nullable=True)
    entry_date = db.Column(db.DateTime, nullable=True)
    extemporaneous = db.Column(db.Integer, nullable=True)
    merchandise_value = db.Column(db.Float, nullable=True)
    uses_financial_value_apportionment = db.Column(db.Boolean, nullable=True)
    conclusion_flag = db.Column(db.Integer, nullable=True)
    paradigma_status = db.Column(db.String(10), nullable=True)
    paradigma_auto_integrated = db.Column(db.Boolean, nullable=True)
    original_gross_value = db.Column(db.Float, nullable=True)
    original_net_value = db.Column(db.Float, nullable=True)
    original_other_values = db.Column(db.Float, nullable=True)
    operation_id = db.Column(db.Integer, nullable=True)
    scp_branch_id = db.Column(db.Integer, nullable=True)
    
    # JSON fields - properly formatted as Text columns
    movement_items = db.Column(db.Text, nullable=True)
    payments = db.Column(db.Text, nullable=True)
    cost_center_apportionments = db.Column(db.Text, nullable=True)
    department_apportionments = db.Column(db.Text, nullable=True)
    taxes = db.Column(db.Text, nullable=True)
    fiscal = db.Column(db.Text, nullable=True)
    norm = db.Column(db.Text, nullable=True)
    cargo_component = db.Column(db.Text, nullable=True)
    third_party_nf = db.Column(db.Text, nullable=True)
    safety_device = db.Column(db.Text, nullable=True)
    nfe = db.Column(db.Text, nullable=True)
    input_ctrc = db.Column(db.Text, nullable=True)
    output_ctrc = db.Column(db.Text, nullable=True)
    ctrc = db.Column(db.Text, nullable=True)
    transport_data = db.Column(db.Text, nullable=True)
    document_authorization = db.Column(db.Text, nullable=True)
    judicial_process = db.Column(db.Text, nullable=True)
    service_order = db.Column(db.Text, nullable=True)
    related_movement = db.Column(db.Text, nullable=True)
    export_related_movement = db.Column(db.Text, nullable=True)
    linked_movement = db.Column(db.Text, nullable=True)
    c_te = db.Column(db.Text, nullable=True)
    eai_integration = db.Column(db.Text, nullable=True)
    electronic_invoice_free_fields = db.Column(db.Text, nullable=True)
    
    # Remaining fields
    customer_vendor_code = db.Column(db.String(50), nullable=True)
    payment_term_code = db.Column(db.String(50), nullable=True)
    observation = db.Column(db.Text, nullable=True)
    financial_optional_table1_code = db.Column(db.String(50), nullable=True)
    financial_entry_movement_id = db.Column(db.String(50), nullable=True)
    generated_entry_number = db.Column(db.String(50), nullable=True)
    open_entry_number = db.Column(db.String(50), nullable=True)
    cash_account_code = db.Column(db.String(50), nullable=True)
    customer_vendor_company_id = db.Column(db.Integer, nullable=True)
    fluxus_grouped_flag = db.Column(db.Boolean, nullable=True)
    cash_account_company_id = db.Column(db.Integer, nullable=True)
    long_history = db.Column(db.Text, nullable=True)
    
    # Upload tracking
    upload_batch_id = db.Column(db.String(36))
    uploaded_by = db.Column(db.Integer, db.ForeignKey('user.id'))
    uploaded_at = db.Column(db.DateTime, default=datetime.utcnow)
    
    def get_json_field(self, field_name):
        """Get a JSON field as a Python object"""
        field_value = getattr(self, field_name, None)
        if field_value:
            try:
                return json.loads(field_value) if isinstance(field_value, str) else field_value
            except (json.JSONDecodeError, TypeError):
                return []
        return []

    def set_json_field(self, field_name, value):
        """Set a JSON field from a Python object"""
        if value:
            json_value = json.dumps(value) if not isinstance(value, str) else value
            setattr(self, field_name, json_value)
        else:
            setattr(self, field_name, None)
    
    def __repr__(self):
        return f'<FinancialMovement {self.internal_id}>'

class UploadHistory(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    batch_id = db.Column(db.String(36), unique=True, nullable=False)
    filename = db.Column(db.String(255), nullable=False)
    uploaded_by = db.Column(db.Integer, db.ForeignKey('user.id'), nullable=False)
    uploaded_at = db.Column(db.DateTime, default=datetime.utcnow)
    total_rows = db.Column(db.Integer, default=0)
    success_count = db.Column(db.Integer, default=0)
    error_count = db.Column(db.Integer, default=0)
    errors = db.Column(db.Text)
    status = db.Column(db.String(20), default='processing')
    
    # Relationship
    uploader = db.relationship('User', backref='uploads')
    
    def __repr__(self):
        return f'<UploadHistory {self.filename}>'

----- FILE: mybundle.py  (sha1:995d1eb04172, bytes:2349) -----
# make_bundle.py
import os, hashlib

ROOT = os.getcwd()
EXCLUDE_DIRS = {
    '.git','node_modules','venv','.venv','__pycache__','.pytest_cache',
    'dist','build','.next','.idea','.vscode',
    'rpa_screenshots','rpa_artifacts','uploads','logs',
    # 'instance',  # <- descomente para não incluir nada de instance/
}
INCLUDE_EXTS = {
    '.py','.ts','.tsx','.js','.jsx','.json','.yml','.yaml','.toml',
    '.ini','.cfg','.txt','.md','.sql','.html','.css','.env.example',
    '.jinja','.jinja2','.j2',
}
MAX_BYTES = 800_000  # sobe um pouco pra evitar truncar texto útil

def rel(p): 
    r = os.path.relpath(p, ROOT)
    return '.' if r == '.' else r.replace('\\','/')

lines = []

# 1) Árvore do projeto
lines.append("### TREE\n")
for base, dirs, files in os.walk(ROOT):
    dirs[:] = [d for d in dirs if d not in EXCLUDE_DIRS]
    base_rel = rel(base)
    if base_rel != '.':
        lines.append(f"{base_rel}/")
    for f in sorted(files):
        lines.append(f"  {rel(os.path.join(base,f))}")

# 2) Conteúdo dos arquivos de texto
lines.append("\n\n### FILES\n")
rpa_sha = ""
files_count = 0

for base, dirs, files in os.walk(ROOT):
    dirs[:] = [d for d in dirs if d not in EXCLUDE_DIRS]
    for f in sorted(files):
        p = os.path.join(base, f)
        ext = os.path.splitext(f)[1].lower()
        if ext in INCLUDE_EXTS and os.path.getsize(p) <= MAX_BYTES:
            with open(p, 'rb') as fh:
                raw = fh.read()
            # tenta decodificar como texto
            txt = None
            for enc in ('utf-8', 'latin-1'):
                try:
                    txt = raw.decode(enc)
                    break
                except UnicodeDecodeError:
                    continue
            if txt is None:
                continue
            sha = hashlib.sha1(raw).hexdigest()[:12]
            files_count += 1
            if rel(p).endswith("rpa.py"):
                rpa_sha = sha
            lines.append(
                f"\n----- FILE: {rel(p)}  (sha1:{sha}, bytes:{len(raw)}) -----\n{txt}"
            )

with open('bundle.txt','w',encoding='utf-8') as f:
    f.write('\n'.join(lines))

print(f"Gerado bundle.txt na pasta do projeto. Arquivos incluídos: {files_count}")
if rpa_sha:
    print(f"SHA do rpa.py no bundle: {rpa_sha}")


----- FILE: pyproject.toml  (sha1:23aa5db92878, bytes:534) -----
[project]
name = "repl-nix-workspace"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "email-validator>=2.2.0",
    "flask-dance>=7.1.0",
    "flask>=3.1.1",
    "flask-sqlalchemy>=3.1.1",
    "gunicorn>=23.0.0",
    "psycopg2-binary>=2.9.10",
    "pyjwt>=2.10.1",
    "flask-login>=0.6.3",
    "oauthlib>=3.3.1",
    "werkzeug>=3.1.3",
    "flask-wtf>=1.2.2",
    "wtforms>=3.2.1",
    "pandas>=2.3.1",
    "openpyxl>=3.1.5",
    "xlrd>=2.0.2",
    "sqlalchemy>=2.0.43",
]


----- FILE: replit.md  (sha1:94db9cb2fc1b, bytes:3613) -----
# Overview

This is a Flask-based financial data management system designed for importing, storing, and analyzing financial movement data from Excel files. The application features a role-based user system with administrative controls for user management and data upload capabilities. Users can browse and search through financial movement records, while administrators can upload Excel files containing structured financial data and manage user accounts.

**Recent Update (Aug 2025)**: System now supports complete 117-field Excel structure with all JSON fields properly processed and displayed. Added 4 new company/branch identification fields: `Empresa_Code`, `Empresa_Nome`, `Filial_Code`, `Filial_Nome`. Upload functionality fully working with batch tracking and error handling.

# User Preferences

Preferred communication style: Simple, everyday language.

# System Architecture

## Web Framework
- **Flask** with SQLAlchemy ORM for database operations
- **Flask-Login** for user session management and authentication
- **Flask-WTF** for form handling and CSRF protection
- Blueprint-based routing for modular organization (main, admin, auth)

## Authentication & Authorization
- Password-based authentication with hashed storage using Werkzeug
- Role-based access control with admin/regular user distinction
- Session-based login with "remember me" functionality
- Decorator-based route protection for admin-only features

## Database Design
- SQLAlchemy with SQLite default (configurable via DATABASE_URL)
- **User model**: Basic user management with admin flags
- **FinancialMovement model**: Comprehensive financial transaction records with 40+ fields
- **UploadHistory model**: Tracks file upload batches and processing results
- Connection pooling and automatic table creation

## Data Processing
- **FinancialDataProcessor class**: Handles Excel file parsing and data transformation
- Column mapping from Excel headers to database fields
- Batch processing with error handling and validation
- Support for complex financial data structures including dates, currencies, and business logic fields

## Frontend Architecture
- Bootstrap 5 dark theme for responsive UI
- jQuery and DataTables for enhanced table functionality
- Template inheritance using Jinja2
- Progressive enhancement with JavaScript for user interactions
- File upload interface with drag-and-drop support

## File Management
- Secure file upload handling with size limits (50MB)
- Excel file processing with pandas
- Upload history tracking with success/failure metrics
- Organized upload directory structure

# External Dependencies

## Core Framework Dependencies
- **Flask**: Web application framework
- **SQLAlchemy/Flask-SQLAlchemy**: Database ORM and integration
- **Flask-Login**: User session management
- **Flask-WTF/WTForms**: Form handling and validation
- **Werkzeug**: WSGI utilities and security helpers

## Data Processing
- **pandas**: Excel file reading and data manipulation
- **openpyxl**: Excel file format support

## Frontend Libraries (CDN)
- **Bootstrap 5**: UI framework with dark theme
- **DataTables**: Enhanced table functionality
- **Font Awesome**: Icon library
- **jQuery**: JavaScript utilities

## Infrastructure
- **SQLite**: Default database (production can use PostgreSQL via DATABASE_URL)
- **ProxyFix**: WSGI middleware for proper URL generation behind proxies
- File system storage for uploaded Excel files

## Security Features
- CSRF protection through Flask-WTF
- Password hashing with Werkzeug
- Secure filename handling for uploads
- Session-based authentication with configurable secrets

----- FILE: routes.py  (sha1:7c0afead735f, bytes:15786) -----
import os
import uuid
from datetime import datetime
from flask import Blueprint, render_template, request, redirect, url_for, flash, jsonify, current_app
from flask_login import login_user, logout_user, login_required, current_user
from werkzeug.security import check_password_hash, generate_password_hash
from werkzeug.utils import secure_filename
from sqlalchemy import or_, desc
from app import db, login_manager
from models import User, FinancialMovement, UploadHistory
from forms import LoginForm, UserForm, EditUserForm, FileUploadForm
from file_processor import FinancialDataProcessor
import logging

logger = logging.getLogger(__name__)

# User loader for Flask-Login
@login_manager.user_loader
def load_user(user_id):
    return User.query.get(int(user_id))

# Blueprint definitions
main_bp = Blueprint('main', __name__)
admin_bp = Blueprint('admin', __name__)
auth_bp = Blueprint('auth', __name__)

def admin_required(f):
    """Decorator to require admin access"""
    from functools import wraps
    @wraps(f)
    @login_required
    def decorated_function(*args, **kwargs):
        if not current_user.is_admin:
            flash('You need administrator privileges to access this page.', 'error')
            return redirect(url_for('main.index'))
        return f(*args, **kwargs)
    return decorated_function

# Main routes
@main_bp.route('/')
def index():
    """Homepage - show recent movements"""
    recent_movements = FinancialMovement.query.order_by(desc(FinancialMovement.uploaded_at)).limit(10).all()
    return render_template('index.html', movements=recent_movements)

@main_bp.route('/movements')
@login_required
def movements():
    """List all movements with search, filters and pagination"""
    page = request.args.get('page', 1, type=int)
    search = request.args.get('search', '', type=str)
    company_filter = request.args.get('company', '', type=str)
    branch_filter = request.args.get('branch', '', type=str)
    status_filter = request.args.get('status', '', type=str)
    per_page = 25
    query = FinancialMovement.query
    
    # Apply search filter
    # Apply search filter
    if search:
        s = search.strip()

        # Busca textual abrangente
        text_conds = [
            FinancialMovement.internal_id.ilike(f"%{s}%"),
            FinancialMovement.aux_customer_vendor_code.ilike(f"%{s}%"),
            FinancialMovement.customer_vendor_name.ilike(f"%{s}%"),
            FinancialMovement.customer_vendor_cnpj.ilike(f"%{s}%"),
            FinancialMovement.observation.ilike(f"%{s}%"),
            FinancialMovement.empresa_nome.ilike(f"%{s}%"),
            FinancialMovement.filial_nome.ilike(f"%{s}%"),
            FinancialMovement.movement_type_code.ilike(f"%{s}%"),  # 🔹 para "1.2.01"
        ]

        if s.isdigit():
            text_conds.append(FinancialMovement.number == int(s))

        query = query.filter(or_(*text_conds))


    
    # Apply company filter
    if company_filter:
        query = query.filter(FinancialMovement.empresa_nome.contains(company_filter))
    
    # Apply branch filter  
    if branch_filter:
        query = query.filter(FinancialMovement.filial_nome.contains(branch_filter))
    
    # Apply status filter
    if status_filter:
        query = query.filter(FinancialMovement.status == status_filter)
    
    movements = query.order_by(desc(FinancialMovement.uploaded_at)).paginate(
        page=page, per_page=per_page, error_out=False
    )
    
    # Get unique values for filter dropdowns
    companies = db.session.query(FinancialMovement.empresa_nome).distinct().filter(
        FinancialMovement.empresa_nome.isnot(None)
    ).order_by(FinancialMovement.empresa_nome).all()
    companies = [c[0] for c in companies if c[0]]
    
    branches = db.session.query(FinancialMovement.filial_nome).distinct().filter(
        FinancialMovement.filial_nome.isnot(None)
    ).order_by(FinancialMovement.filial_nome).all()
    branches = [b[0] for b in branches if b[0]]
    
    statuses = db.session.query(FinancialMovement.status).distinct().filter(
        FinancialMovement.status.isnot(None)
    ).order_by(FinancialMovement.status).all()
    statuses = [s[0] for s in statuses if s[0]]
    
    return render_template('movements/list.html', 
                         movements=movements, 
                         search=search,
                         company_filter=company_filter,
                         branch_filter=branch_filter,
                         status_filter=status_filter,
                         companies=companies,
                         branches=branches,
                         statuses=statuses)



@main_bp.route('/movements/<int:id>')
@login_required
def movement_detail(id):
    """Show detailed view of a specific movement"""
    movement = FinancialMovement.query.get_or_404(id)
    return render_template('movements/detail.html', movement=movement)

@main_bp.route('/movements/<int:id>/delete', methods=['POST'])
@login_required
def delete_movement(id):
    """Delete a specific movement"""
    movement = FinancialMovement.query.get_or_404(id)
    
    try:
        movement_name = movement.internal_id or f"#{movement.id}"
        db.session.delete(movement)
        db.session.commit()
        flash(f'Movement {movement_name} deleted successfully!', 'success')
    except Exception as e:
        db.session.rollback()
        flash(f'Error deleting movement: {str(e)}', 'error')
        logger.error(f"Error deleting movement {id}: {str(e)}")
    
    return redirect(url_for('main.movements'))

@main_bp.route('/movements/delete-all', methods=['GET', 'POST'])
@login_required
def delete_all_movements():
    """Delete all movements from database"""
    if request.method == 'POST':
        confirmation = request.form.get('confirmation', '').strip().lower()
        
        if confirmation == 'delete all movements':
            try:
                movement_count = FinancialMovement.query.count()
                upload_count = UploadHistory.query.count()
                
                db.session.query(FinancialMovement).delete()
                db.session.query(UploadHistory).delete()
                db.session.commit()
                
                flash(f'All data deleted successfully! {movement_count} movements and {upload_count} upload records removed.', 'success')
                return redirect(url_for('main.movements'))
                
            except Exception as e:
                db.session.rollback()
                flash(f'Error deleting data: {str(e)}', 'error')
                logger.error(f"Error deleting all movements: {str(e)}")
                
        else:
            flash('Invalid confirmation text. Type exactly "delete all movements" to confirm.', 'warning')
    
    # Count current records for display
    movement_count = FinancialMovement.query.count()
    upload_count = UploadHistory.query.count()
    
    return render_template('movements/delete_all.html', 
                          movement_count=movement_count, 
                          upload_count=upload_count)

@main_bp.route('/api/movements/<int:id>/json/<field>')
@login_required
def get_movement_json_field(id, field):
    """API endpoint to get formatted JSON field data"""
    movement = FinancialMovement.query.get_or_404(id)
    json_data = movement.get_json_field(field)
    return jsonify(json_data)

# Authentication routes
@auth_bp.route('/login', methods=['GET', 'POST'])
def login():
    """User login"""
    if current_user.is_authenticated:
        return redirect(url_for('main.index'))
    
    form = LoginForm()
    if form.validate_on_submit():
        user = User.query.filter_by(username=form.username.data).first()
        if user and user.password_hash and check_password_hash(user.password_hash, form.password.data):
            login_user(user, remember=form.remember_me.data)
            next_page = request.args.get('next')
            if not next_page:
                next_page = url_for('admin.dashboard') if user.is_admin else url_for('main.index')
            flash(f'Welcome back, {user.username}!', 'success')
            return redirect(next_page)
        flash('Invalid username or password', 'error')
    
    return render_template('auth/login.html', form=form)

@auth_bp.route('/logout')
@login_required
def logout():
    """User logout"""
    logout_user()
    flash('You have been logged out.', 'info')
    return redirect(url_for('main.index'))

# Admin routes
@admin_bp.route('/dashboard')
@admin_required
def dashboard():
    """Admin dashboard with statistics"""
    stats = {
        'total_users': User.query.count(),
        'total_movements': FinancialMovement.query.count(),
        'total_uploads': UploadHistory.query.count(),
        'recent_uploads': UploadHistory.query.order_by(desc(UploadHistory.uploaded_at)).limit(5).all()
    }
    return render_template('admin/dashboard.html', stats=stats)

@admin_bp.route('/users')
@admin_required
def users():
    """List all users"""
    page = request.args.get('page', 1, type=int)
    users = User.query.order_by(User.created_at.desc()).paginate(
        page=page, per_page=20, error_out=False
    )
    return render_template('admin/users.html', users=users)

@admin_bp.route('/users/new', methods=['GET', 'POST'])
@admin_required
def create_user():
    """Create new user"""
    form = UserForm()
    if form.validate_on_submit():
        password_data = form.password.data
        if password_data:
            user = User(
                username=form.username.data,
                email=form.email.data,
                is_admin=form.is_admin.data,
                password_hash=generate_password_hash(password_data)
            )
        else:
            flash('Password is required', 'error')
            return render_template('admin/users.html', form=form, action='create')
        db.session.add(user)
        db.session.commit()
        flash(f'User {user.username} created successfully!', 'success')
        return redirect(url_for('admin.users'))
    
    return render_template('admin/users.html', form=form, action='create')

@admin_bp.route('/users/<int:id>/edit', methods=['GET', 'POST'])
@admin_required
def edit_user(id):
    """Edit existing user"""
    user = User.query.get_or_404(id)
    form = EditUserForm(
        original_username=user.username,
        original_email=user.email,
        obj=user
    )
    
    if form.validate_on_submit():
        user.username = form.username.data
        user.email = form.email.data
        user.is_admin = form.is_admin.data
        db.session.commit()
        flash(f'User {user.username} updated successfully!', 'success')
        return redirect(url_for('admin.users'))
    
    return render_template('admin/users.html', form=form, user=user, action='edit')

@admin_bp.route('/users/<int:id>/delete', methods=['POST'])
@admin_required
def delete_user(id):
    """Delete user"""
    user = User.query.get_or_404(id)
    if user.id == current_user.id:
        flash('You cannot delete your own account!', 'error')
        return redirect(url_for('admin.users'))
    
    db.session.delete(user)
    db.session.commit()
    flash(f'User {user.username} deleted successfully!', 'success')
    return redirect(url_for('admin.users'))

@admin_bp.route('/upload', methods=['GET', 'POST'])
@admin_required
def upload_file():
    """Upload Excel file with financial data"""
    form = FileUploadForm()
    if form.validate_on_submit():
        file = form.file.data
        filename = secure_filename(file.filename)
        
        # Generate unique filename
        unique_filename = f"{uuid.uuid4()}_{filename}"
        file_path = os.path.join(current_app.config['UPLOAD_FOLDER'], unique_filename)
        
        try:
            # Save file
            file.save(file_path)
            
            # Process file
            # Process file
            processor = FinancialDataProcessor()
            batch_id, success_count, error_count, errors, processed_data = processor.process_file(
                file_path, current_user.id
            )

            # 🔹 NOVO: tenta fechar handle explicitamente (se processor tiver método close)
            if hasattr(processor, "close"):
                try:
                    processor.close()
                except Exception:
                    pass

            # 🔹 NOVO: adiar a exclusão para só depois
            import time
            for i in range(5):  # tenta até 5 vezes
                try:
                    if os.path.exists(file_path):
                        os.remove(file_path)
                    break
                except PermissionError:
                    time.sleep(0.5)  # espera meio segundo e tenta de novo

            
            if error_count > 0:
                flash(f'File processed with {success_count} successful records and {error_count} errors. Check upload history for details.', 'warning')
            else:
                flash(f'File uploaded successfully! {success_count} records processed.', 'success')
            
            return redirect(url_for('admin.upload_history'))
            
        except Exception as e:
            # Clean up file if processing failed
            if os.path.exists(file_path):
                os.remove(file_path)
            flash(f'Error processing file: {str(e)}', 'error')
            logger.error(f"Upload processing error: {str(e)}")
    
    return render_template('admin/upload.html', form=form)

@admin_bp.route('/upload-history')
@admin_required
def upload_history():
    """View upload history"""
    page = request.args.get('page', 1, type=int)
    uploads = UploadHistory.query.order_by(desc(UploadHistory.uploaded_at)).paginate(
        page=page, per_page=20, error_out=False
    )
    return render_template('admin/upload.html', uploads=uploads, show_history=True)

@admin_bp.route('/upload-history/<int:id>')
@admin_required
def upload_history_detail(id):
    """View detailed upload history"""
    upload = UploadHistory.query.get_or_404(id)
    movements = FinancialMovement.query.filter_by(upload_batch_id=upload.batch_id).all()
    return render_template('admin/upload.html', upload=upload, movements=movements, show_detail=True)

@admin_bp.route('/clear-movements', methods=['GET', 'POST'])
@admin_required
def clear_movements():
    """Clear all financial movements from database"""
    if request.method == 'POST':
        confirmation = request.form.get('confirmation', '').strip().lower()
        
        if confirmation == 'delete all movements':
            try:
                # Delete all financial movements and their upload history
                movement_count = FinancialMovement.query.count()
                upload_count = UploadHistory.query.count()
                
                db.session.query(FinancialMovement).delete()
                db.session.query(UploadHistory).delete()
                db.session.commit()
                
                flash(f'Database cleared successfully! {movement_count} financial movements and {upload_count} upload records deleted.', 'success')
                return redirect(url_for('admin.dashboard'))
                
            except Exception as e:
                db.session.rollback()
                flash(f'Error clearing database: {str(e)}', 'error')
                
        else:
            flash('Invalid confirmation text. Type exactly "delete all movements" to confirm.', 'warning')
    
    # Count current records
    movement_count = FinancialMovement.query.count()
    upload_count = UploadHistory.query.count()
    
    return render_template('admin/clear_movements.html', 
                          movement_count=movement_count, 
                          upload_count=upload_count)


----- FILE: test_data_sample.py  (sha1:574f5dbc32a6, bytes:5548) -----
#!/usr/bin/env python3
"""
Script to create a test Excel file with sample data to test the upload functionality
"""

import pandas as pd
import json
from datetime import datetime

# Sample data that mimics the structure from the user's file
sample_data = {
    'Empresa_Nome': ['SA CAVALCANTE COMESTIVEIS LTDA'],
    'Filial_Code': [17],
    'Filial_Nome': ['16 - BOB´S LARANJEIRAS'],
    'Filial_Nome_Descricao': ['16 - SA CAVALCANTE COMESTIVEIS LTDA'],
    'internalId': ['1|138'],
    'companyId': [1],
    'movementId': [138],
    'branchId': [1],
    'warehouseCode': ['01'],
    'destinyWarehouseCode': ['01'],
    'customerVendorCode': ['F00227'],
    'number': ['001408'],
    'series': ['U'],
    'movementTypeCode': ['1.2.02'],
    'type': ['P'],
    'status': ['F'],
    'printed': [False],
    'documentPrinted': [False],
    'billPrinted': [False],
    'registerDate': ['2003-08-06T00:00:00-03:00'],
    'exitDate': ['2003-08-11T00:00:00-03:00'],
    'commercialRepresentativeCharge': [0],
    'paymentTermCode': [30],
    'grossValue': [232],
    'netValue': [232],
    'informedNetValue': [0],
    'otherValues': [232],
    'observation': ['PROGRAMA SOL'],
    'discountPercentage': [0],
    'expensePercentage': [0],
    'expenseValue': [0],
    'extraPercentage1': [0],
    'extraValue1': [0],
    'extraPercentage2': [0],
    'extraValue2': [0],
    'transportedProductNetWeight': [0],
    'transportedProductGrossWeight': [0],
    'financialOptionalTable1Code': ['02'],
    'financialEntryMovementId': [138],
    'netValueCurrencyCode': ['R$'],
    'date': ['2003-08-06T00:00:00-03:00'],
    'generatedEntryNumber': [1],
    'hasGeneratedBill': [False],
    'openEntryNumber': [1],
    'auxCustomerVendorCode': ['F00227'],
    'auxCustomerVendorCompanyId': [0],
    'cashAccountCode': ['02'],
    'chargePercentage': [0],
    'salesman2ChargePercentage': [0],
    'salesman3ChargePercentage': [0],
    'salesman4ChargePercentage': [0],
    'customerVendorCompanyId': [0],
    'userCode': ['cmufumba'],
    'destinyBranchId': [1],
    'fluxusGroupedFlag': [0],
    'cashAccountCompanyId': [1],
    'lotGenerated': [False],
    'accountingExportStatus': [0],
    'hasGeneratedWorkAccount': [False],
    'workAccountGenerated': [False],
    'lastEditTime': ['2003-08-11T09:08:41-03:00'],
    'indicateObjectUse': [0],
    'bonumIntegrated': [False],
    'processedFlag': [False],
    'icmsDeductionValue': [0],
    'creationUser': ['cmufumba'],
    'creationDate': ['2003-08-11T00:00:00-03:00'],
    'emailStatus': [False],
    'internalGrossValue': [232],
    'otherCompanyINSSBaseValue': [0],
    'conditionalDiscountValue': [0],
    'conditionalExpenseValue': [0],
    'affectStockOrder': [138],
    'commercialAutomationExported': [0],
    'aplicationIntegration': ['T'],
    'entryDate': ['2003-08-11T00:00:00-03:00'],
    'extemporaneous': [0],
    'merchandiseValue': [0],
    'usesFinancialValueApportionment': [False],
    'conclusionFlag': [0],
    'paradigmaStatus': ['N'],
    'paradigmaAutoIntegrated': [False],
    'originalGrossValue': [0],
    'originalNetValue': [0],
    'originalOtherValues': [0],
    'operationId': [0],
    'scpBranchId': [0],
    # JSON fields with sample data
    'movementItems': [json.dumps([{
        'companyId': 1,
        'movementId': 138,
        'sequentialId': 1,
        'sequentialNumber': 1,
        'productId': 90,
        'quantity': 4.0,
        'unitPrice': 58.0,
        'tablePrice': 0.0,
        'discountValue': 0.0,
        'registerDate': '2003-08-06T00:00:00-03:00',
        'measureUnitCode': 'GL',
        'productFantasyName': 'GALVEST BRANCO 3600 ML',
        'productCode': '01.01.0007'
    }])],
    'payments': [json.dumps([])],
    'costCenterApportionments': [json.dumps([{
        'companyId': 1,
        'apportionmentId': 7,
        'movementId': 138,
        'costCenterCode': '1.4.008',
        'value': 232.0,
        'costCenterName': 'EXPANSÃO SPC'
    }])],
    'departmentApportionments': [json.dumps([{
        'companyId': 1,
        'movementId': 138,
        'branchId': 1,
        'departmentCode': '10',
        'value': 232.0,
        'departmentName': 'OBRAS'
    }])],
    'taxes': [json.dumps([{
        'companyId': 1,
        'movementId': 138,
        'movementItemSequentialId': 0,
        'taxId': 'PIS',
        'calculationBasis': 232.0,
        'aliquot': 1.65,
        'value': 3.83,
        'calculatedCalculationBasis': 232.0,
        'edited': 0,
        'fullBase': 0.0
    }])],
    'fiscal': [json.dumps([])],
    'norm': [json.dumps([])],
    'cargoComponent': [json.dumps([])],
    'thirdPartyNF': [json.dumps([])],
    'safetyDevice': [json.dumps([])],
    'nfe': [json.dumps([])],
    'inputCTRC': [json.dumps([])],
    'outputCTRC': [json.dumps([])],
    'ctrc': [json.dumps([])],
    'transportData': [json.dumps([])],
    'documentAuthorization': [json.dumps([])],
    'judicialProcess': [json.dumps([])],
    'serviceOrder': [json.dumps([])],
    'relatedMovement': [json.dumps([])],
    'exportRelatedMovement': [json.dumps([])],
    'linkedMovement': [json.dumps([])],
    'cTe': [json.dumps([])],
    'eaiIntegration': [json.dumps([])],
    'electronicInvoiceFreeFields': [json.dumps([])]
}

if __name__ == "__main__":
    # Create DataFrame
    df = pd.DataFrame(sample_data)
    
    # Save to Excel
    filename = "test_financial_movements.xlsx"
    df.to_excel(filename, index=False)
    print(f"Test file created: {filename}")
    print(f"Data shape: {df.shape}")
    print(f"Columns: {list(df.columns)}")

----- FILE: update_models.py  (sha1:5d6f83401594, bytes:5295) -----
#!/usr/bin/env python3
"""
Script to generate complete FinancialMovement model with all 113 fields
"""

# All 113 fields from the Excel file
EXCEL_COLUMNS = [
    'internalId', 'companyId', 'movementId', 'branchId', 'warehouseCode', 'destinyWarehouseCode', 
    'number', 'series', 'movementTypeCode', 'type', 'status', 'printed', 'documentPrinted', 
    'billPrinted', 'registerDate', 'exitDate', 'commercialRepresentativeCharge', 'grossValue', 
    'netValue', 'informedNetValue', 'otherValues', 'discountPercentage', 'expensePercentage', 
    'expenseValue', 'extraPercentage1', 'extraValue1', 'extraPercentage2', 'extraValue2', 
    'transportedProductNetWeight', 'transportedProductGrossWeight', 'classificationTable5Code', 
    'financialOptionalTable2Code', 'netValueCurrencyCode', 'date', 'hasGeneratedBill', 
    'auxCustomerVendorCode', 'auxCustomerVendorCompanyId', 'costCenterCode', 'salesman1Code', 
    'chargePercentage', 'salesman2ChargePercentage', 'salesman3ChargePercentage', 
    'salesman4ChargePercentage', 'userCode', 'destinyBranchId', 'lotGenerated', 
    'accountingExportStatus', 'deliveryDate', 'hasGeneratedWorkAccount', 'workAccountGenerated', 
    'lastEditTime', 'indicateObjectUse', 'bonumIntegrated', 'processedFlag', 'icmsDeductionValue', 
    'creationUser', 'creationDate', 'emailStatus', 'internalGrossValue', 'otherCompanyINSSBaseValue', 
    'conditionalDiscountValue', 'conditionalExpenseValue', 'affectStockOrder', 
    'commercialAutomationExported', 'aplicationIntegration', 'entryDate', 'extemporaneous', 
    'merchandiseValue', 'usesFinancialValueApportionment', 'conclusionFlag', 'paradigmaStatus', 
    'paradigmaAutoIntegrated', 'originalGrossValue', 'originalNetValue', 'originalOtherValues', 
    'operationId', 'scpBranchId', 'movementItems', 'payments', 'costCenterApportionments', 
    'departmentApportionments', 'taxes', 'fiscal', 'norm', 'cargoComponent', 'thirdPartyNF', 
    'safetyDevice', 'nfe', 'inputCTRC', 'outputCTRC', 'ctrc', 'transportData', 
    'documentAuthorization', 'judicialProcess', 'serviceOrder', 'relatedMovement', 
    'exportRelatedMovement', 'linkedMovement', 'cTe', 'eaiIntegration', 
    'electronicInvoiceFreeFields', 'customerVendorCode', 'paymentTermCode', 'observation', 
    'financialOptionalTable1Code', 'financialEntryMovementId', 'generatedEntryNumber', 
    'openEntryNumber', 'cashAccountCode', 'customerVendorCompanyId', 'fluxusGroupedFlag', 
    'cashAccountCompanyId', 'longHistory'
]

def camel_to_snake(name):
    """Convert camelCase to snake_case"""
    import re
    s1 = re.sub('(.)([A-Z][a-z]+)', r'\1_\2', name)
    return re.sub('([a-z0-9])([A-Z])', r'\1_\2', s1).lower()

# JSON fields that should be stored as JSON text
JSON_FIELDS = {
    'movementItems', 'payments', 'costCenterApportionments', 'departmentApportionments', 
    'taxes', 'fiscal', 'norm', 'cargoComponent', 'thirdPartyNF', 'safetyDevice', 
    'nfe', 'inputCTRC', 'outputCTRC', 'ctrc', 'transportData', 'documentAuthorization', 
    'judicialProcess', 'serviceOrder', 'relatedMovement', 'exportRelatedMovement', 
    'linkedMovement', 'cTe', 'eaiIntegration', 'electronicInvoiceFreeFields'
}

# Generate column mapping
print("# Complete column mapping for file_processor.py:")
print("COLUMN_MAPPING = {")
for col in EXCEL_COLUMNS:
    snake_name = camel_to_snake(col)
    print(f"    '{col}': '{snake_name}',")
print("}")
print()

# Generate model fields
print("# Fields for FinancialMovement model:")
for col in EXCEL_COLUMNS:
    snake_name = camel_to_snake(col)
    
    if col in JSON_FIELDS:
        print(f"    {snake_name} = db.Column(db.Text, nullable=True)  # JSON field")
    elif col.endswith('Date') or col.endswith('Time'):
        print(f"    {snake_name} = db.Column(db.DateTime, nullable=True)")
    elif col.endswith('Id') or col.endswith('Code') or col.endswith('Number'):
        print(f"    {snake_name} = db.Column(db.String(50), nullable=True)")
    elif col.endswith('Value') or col.endswith('Percentage') or col.endswith('Charge') or col.endswith('Weight'):
        print(f"    {snake_name} = db.Column(db.Float, nullable=True)")
    elif col.endswith('Flag') or col.startswith('has') or col.startswith('is') or col in ['printed', 'documentPrinted', 'billPrinted', 'bonumIntegrated', 'processedFlag', 'usesFinancialValueApportionment', 'paradigmaAutoIntegrated']:
        print(f"    {snake_name} = db.Column(db.Boolean, nullable=True)")
    else:
        print(f"    {snake_name} = db.Column(db.String(255), nullable=True)")

print()
print("# JSON field processing methods:")
print("""    def get_json_field(self, field_name):
        \"\"\"Get a JSON field as a Python object\"\"\"
        field_value = getattr(self, field_name, None)
        if field_value:
            try:
                return json.loads(field_value) if isinstance(field_value, str) else field_value
            except (json.JSONDecodeError, TypeError):
                return []
        return []

    def set_json_field(self, field_name, value):
        \"\"\"Set a JSON field from a Python object\"\"\"
        if value:
            json_value = json.dumps(value) if not isinstance(value, str) else value
            setattr(self, field_name, json_value)
        else:
            setattr(self, field_name, None)""")

----- FILE: .local/state/replit/agent/.latest.json  (sha1:30ac0ad80407, bytes:18) -----
{"latest": "main"}

----- FILE: .local/state/replit/agent/filesystem/filesystem_state.json  (sha1:f9eafd04b5b5, bytes:102146) -----
{"file_contents":{"app.py":{"content":"import os\nimport logging\nfrom flask import Flask\nfrom flask_sqlalchemy import SQLAlchemy\nfrom flask_login import LoginManager\nfrom sqlalchemy.orm import DeclarativeBase\nfrom werkzeug.middleware.proxy_fix import ProxyFix\n\n# Configure logging\nlogging.basicConfig(level=logging.DEBUG)\n\nclass Base(DeclarativeBase):\n    pass\n\n# Initialize extensions\ndb = SQLAlchemy(model_class=Base)\nlogin_manager = LoginManager()\n\ndef create_app():\n    app = Flask(__name__)\n    \n    # Configuration\n    app.secret_key = os.environ.get(\"SESSION_SECRET\", \"dev-secret-key-change-in-production\")\n    app.config[\"SQLALCHEMY_DATABASE_URI\"] = os.environ.get(\"DATABASE_URL\", \"sqlite:///financial_data.db\")\n    app.config[\"SQLALCHEMY_TRACK_MODIFICATIONS\"] = False\n    app.config[\"SQLALCHEMY_ENGINE_OPTIONS\"] = {\n        \"pool_recycle\": 300,\n        \"pool_pre_ping\": True,\n    }\n    app.config[\"MAX_CONTENT_LENGTH\"] = 50 * 1024 * 1024  # 50MB max file size\n    app.config[\"UPLOAD_FOLDER\"] = \"uploads\"\n    \n    # Ensure upload directory exists\n    os.makedirs(app.config[\"UPLOAD_FOLDER\"], exist_ok=True)\n    \n    # Initialize extensions with app\n    db.init_app(app)\n    login_manager.init_app(app)\n    login_manager.login_view = 'auth.login'\n    login_manager.login_message = 'Please log in to access this page.'\n    \n    # ProxyFix for proper URL generation\n    app.wsgi_app = ProxyFix(app.wsgi_app, x_proto=1, x_host=1)\n    \n    # Import models to ensure they are registered\n    with app.app_context():\n        import models\n        db.create_all()\n        \n        # Create admin user if it doesn't exist\n        from models import User\n        from werkzeug.security import generate_password_hash\n        \n        admin = User.query.filter_by(username='admin').first()\n        if not admin:\n            admin = User(\n                username='admin',\n                email='admin@example.com',\n                is_admin=True,\n                password_hash=generate_password_hash('admin123')\n            )\n            db.session.add(admin)\n            db.session.commit()\n            logging.info(\"Created admin user: admin/admin123\")\n    \n    # Register blueprints\n    from routes import main_bp, admin_bp, auth_bp\n    app.register_blueprint(main_bp)\n    app.register_blueprint(admin_bp, url_prefix='/admin')\n    app.register_blueprint(auth_bp, url_prefix='/auth')\n    \n    return app\n\n# Create app instance\napp = create_app()\n","size_bytes":2402},"create_test_excel.py":{"content":"import pandas as pd\nfrom datetime import datetime\nimport json\n\n# Create a test Excel file with the new company and branch fields\ndata = []\n\n# Create 5 test records with the new structure\nfor i in range(1, 6):\n    record = {\n        # New company and branch fields (first 4 columns)\n        'Empresa_Code': f'EMP_{i:03d}',\n        'Empresa_Nome': f'Empresa Teste {i} Ltda',\n        'Filial_Code': f'FIL_{i:03d}',\n        'Filial_Nome': f'Filial {i} - Centro',\n        \n        # Existing core fields\n        'internalId': f'{i}|{400+i}',\n        'companyId': i,\n        'movementId': 400+i,\n        'branchId': i,\n        'warehouseCode': str(i),\n        'destinyWarehouseCode': str(i),\n        'number': 12000+i,\n        'series': 'A',\n        'movementTypeCode': '1.2.02',\n        'type': 'P',\n        'status': 'F',\n        'printed': False,\n        'documentPrinted': False,\n        'billPrinted': False,\n        'registerDate': datetime.now(),\n        'exitDate': datetime.now(),\n        'commercialRepresentativeCharge': 0.0,\n        'grossValue': 100.0 * i,\n        'netValue': 100.0 * i,\n        'informedNetValue': 0.0,\n        'otherValues': 100.0 * i,\n        'discountPercentage': 0.0,\n        'expensePercentage': 0.0,\n        'expenseValue': 0.0,\n        'extraPercentage1': 0.0,\n        'extraValue1': 0.0,\n        'extraPercentage2': 0.0,\n        'extraValue2': 0.0,\n        'transportedProductNetWeight': 0.0,\n        'transportedProductGrossWeight': 0.0,\n        'classificationTable5Code': '',\n        'financialOptionalTable2Code': '',\n        'netValueCurrencyCode': 'R$',\n        'date': datetime.now(),\n        'hasGeneratedBill': False,\n        'auxCustomerVendorCode': f'C{i:05d}',\n        'auxCustomerVendorCompanyId': 0,\n        'costCenterCode': '',\n        'salesman1Code': '',\n        'chargePercentage': 0.0,\n        'salesman2ChargePercentage': 0.0,\n        'salesman3ChargePercentage': 0.0,\n        'salesman4ChargePercentage': 0.0,\n        'userCode': 'testuser',\n        'destinyBranchId': i,\n        'lotGenerated': False,\n        'accountingExportStatus': '0',\n        'deliveryDate': '',\n        'hasGeneratedWorkAccount': False,\n        'workAccountGenerated': 'false',\n        'lastEditTime': datetime.now(),\n        'indicateObjectUse': '0',\n        'bonumIntegrated': False,\n        'processedFlag': False,\n        'icmsDeductionValue': 0.0,\n        'creationUser': 'testuser',\n        'creationDate': datetime.now(),\n        'emailStatus': 'false',\n        'internalGrossValue': 100.0 * i,\n        'otherCompanyINSSBaseValue': 0.0,\n        'conditionalDiscountValue': 0.0,\n        'conditionalExpenseValue': 0.0,\n        'affectStockOrder': str(400+i),\n        'commercialAutomationExported': 0,\n        'aplicationIntegration': 'T',\n        'entryDate': datetime.now(),\n        'extemporaneous': 0,\n        'merchandiseValue': 0.0,\n        'usesFinancialValueApportionment': False,\n        'conclusionFlag': 0,\n        'paradigmaStatus': 'N',\n        'paradigmaAutoIntegrated': False,\n        'originalGrossValue': 0.0,\n        'originalNetValue': 0.0,\n        'originalOtherValues': 0.0,\n        'operationId': 0,\n        'scpBranchId': 0,\n        'movementItems': '[]',\n        'payments': '[]',\n        'costCenterApportionments': '[]',\n        'departmentApportionments': '[]',\n        'taxes': '[]',\n        'fiscal': '[]',\n        'norm': '[]',\n        'cargoComponent': '[]',\n        'thirdPartyNF': '[]',\n        'safetyDevice': '[]',\n        'nfe': '[]',\n        'inputCTRC': '[]',\n        'outputCTRC': '[]',\n        'ctrc': '[]',\n        'transportData': '[]',\n        'documentAuthorization': '[]',\n        'judicialProcess': '[]',\n        'serviceOrder': '[]',\n        'relatedMovement': '[]',\n        'exportRelatedMovement': '[]',\n        'linkedMovement': '[]',\n        'cTe': '[]',\n        'eaiIntegration': '[]',\n        'electronicInvoiceFreeFields': '[]',\n        'customerVendorCode': f'C{i:05d}',\n        'paymentTermCode': '30.0',\n        'observation': f'Teste de movimento {i} com dados de empresa',\n        'financialOptionalTable1Code': '2.0',\n        'financialEntryMovementId': f'{400+i}.0',\n        'generatedEntryNumber': '1.0',\n        'openEntryNumber': '1.0',\n        'cashAccountCode': '2.0',\n        'customerVendorCompanyId': 0,\n        'fluxusGroupedFlag': False,\n        'cashAccountCompanyId': i\n    }\n    data.append(record)\n\n# Create DataFrame and save to Excel\ndf = pd.DataFrame(data)\nfilename = 'test_excel_with_company_branch.xlsx'\ndf.to_excel(filename, index=False)\nprint(f\"Created test Excel file: {filename}\")\nprint(f\"Columns: {len(df.columns)}\")\nprint(f\"Records: {len(df)}\")\nprint(\"First 4 columns (new):\")\nfor col in df.columns[:4]:\n    print(f\"  {col}: {df[col].iloc[0]}\")","size_bytes":4766},"file_processor.py":{"content":"import pandas as pd\nimport json\nimport uuid\nimport ast\nfrom datetime import datetime\nfrom typing import Dict, List, Tuple, Any\nimport logging\n\nlogger = logging.getLogger(__name__)\n\nclass FinancialDataProcessor:\n    \"\"\"Process Excel files containing financial movement data\"\"\"\n    \n    # Complete column mapping for all 117 fields (added 4 new company/branch fields)\n    COLUMN_MAPPING = {\n        'internalId': 'internal_id',\n        'companyId': 'company_id',\n        'movementId': 'movement_id',\n        'branchId': 'branch_id',\n        \n        # New company and branch information fields\n        'Empresa_Code': 'empresa_code',\n        'Empresa_Nome': 'empresa_nome',\n        'Filial_Code': 'filial_code',\n        'Filial_Nome': 'filial_nome',\n        'warehouseCode': 'warehouse_code',\n        'destinyWarehouseCode': 'destiny_warehouse_code',\n        'number': 'number',\n        'series': 'series',\n        'movementTypeCode': 'movement_type_code',\n        'type': 'type',\n        'status': 'status',\n        'printed': 'printed',\n        'documentPrinted': 'document_printed',\n        'billPrinted': 'bill_printed',\n        'registerDate': 'register_date',\n        'exitDate': 'exit_date',\n        'commercialRepresentativeCharge': 'commercial_representative_charge',\n        'grossValue': 'gross_value',\n        'netValue': 'net_value',\n        'informedNetValue': 'informed_net_value',\n        'otherValues': 'other_values',\n        'discountPercentage': 'discount_percentage',\n        'expensePercentage': 'expense_percentage',\n        'expenseValue': 'expense_value',\n        'extraPercentage1': 'extra_percentage1',\n        'extraValue1': 'extra_value1',\n        'extraPercentage2': 'extra_percentage2',\n        'extraValue2': 'extra_value2',\n        'transportedProductNetWeight': 'transported_product_net_weight',\n        'transportedProductGrossWeight': 'transported_product_gross_weight',\n        'classificationTable5Code': 'classification_table5_code',\n        'financialOptionalTable2Code': 'financial_optional_table2_code',\n        'netValueCurrencyCode': 'net_value_currency_code',\n        'date': 'date',\n        'hasGeneratedBill': 'has_generated_bill',\n        'auxCustomerVendorCode': 'aux_customer_vendor_code',\n        'auxCustomerVendorCompanyId': 'aux_customer_vendor_company_id',\n        'costCenterCode': 'cost_center_code',\n        'salesman1Code': 'salesman1_code',\n        'chargePercentage': 'charge_percentage',\n        'salesman2ChargePercentage': 'salesman2_charge_percentage',\n        'salesman3ChargePercentage': 'salesman3_charge_percentage',\n        'salesman4ChargePercentage': 'salesman4_charge_percentage',\n        'userCode': 'user_code',\n        'destinyBranchId': 'destiny_branch_id',\n        'lotGenerated': 'lot_generated',\n        'accountingExportStatus': 'accounting_export_status',\n        'deliveryDate': 'delivery_date',\n        'hasGeneratedWorkAccount': 'has_generated_work_account',\n        'workAccountGenerated': 'work_account_generated',\n        'lastEditTime': 'last_edit_time',\n        'indicateObjectUse': 'indicate_object_use',\n        'bonumIntegrated': 'bonum_integrated',\n        'processedFlag': 'processed_flag',\n        'icmsDeductionValue': 'icms_deduction_value',\n        'creationUser': 'creation_user',\n        'creationDate': 'creation_date',\n        'emailStatus': 'email_status',\n        'internalGrossValue': 'internal_gross_value',\n        'otherCompanyINSSBaseValue': 'other_company_inss_base_value',\n        'conditionalDiscountValue': 'conditional_discount_value',\n        'conditionalExpenseValue': 'conditional_expense_value',\n        'affectStockOrder': 'affect_stock_order',\n        'commercialAutomationExported': 'commercial_automation_exported',\n        'aplicationIntegration': 'aplication_integration',\n        'entryDate': 'entry_date',\n        'extemporaneous': 'extemporaneous',\n        'merchandiseValue': 'merchandise_value',\n        'usesFinancialValueApportionment': 'uses_financial_value_apportionment',\n        'conclusionFlag': 'conclusion_flag',\n        'paradigmaStatus': 'paradigma_status',\n        'paradigmaAutoIntegrated': 'paradigma_auto_integrated',\n        'originalGrossValue': 'original_gross_value',\n        'originalNetValue': 'original_net_value',\n        'originalOtherValues': 'original_other_values',\n        'operationId': 'operation_id',\n        'scpBranchId': 'scp_branch_id',\n        'movementItems': 'movement_items',\n        'payments': 'payments',\n        'costCenterApportionments': 'cost_center_apportionments',\n        'departmentApportionments': 'department_apportionments',\n        'taxes': 'taxes',\n        'fiscal': 'fiscal',\n        'norm': 'norm',\n        'cargoComponent': 'cargo_component',\n        'thirdPartyNF': 'third_party_nf',\n        'safetyDevice': 'safety_device',\n        'nfe': 'nfe',\n        'inputCTRC': 'input_ctrc',\n        'outputCTRC': 'output_ctrc',\n        'ctrc': 'ctrc',\n        'transportData': 'transport_data',\n        'documentAuthorization': 'document_authorization',\n        'judicialProcess': 'judicial_process',\n        'serviceOrder': 'service_order',\n        'relatedMovement': 'related_movement',\n        'exportRelatedMovement': 'export_related_movement',\n        'linkedMovement': 'linked_movement',\n        'cTe': 'c_te',\n        'eaiIntegration': 'eai_integration',\n        'electronicInvoiceFreeFields': 'electronic_invoice_free_fields',\n        'customerVendorCode': 'customer_vendor_code',\n        'paymentTermCode': 'payment_term_code',\n        'observation': 'observation',\n        'financialOptionalTable1Code': 'financial_optional_table1_code',\n        'financialEntryMovementId': 'financial_entry_movement_id',\n        'generatedEntryNumber': 'generated_entry_number',\n        'openEntryNumber': 'open_entry_number',\n        'cashAccountCode': 'cash_account_code',\n        'customerVendorCompanyId': 'customer_vendor_company_id',\n        'fluxusGroupedFlag': 'fluxus_grouped_flag',\n        'cashAccountCompanyId': 'cash_account_company_id',\n        'longHistory': 'long_history',\n    }\n    \n    # JSON fields that need special processing\n    JSON_FIELDS = {\n        'movement_items', 'payments', 'cost_center_apportionments', 'department_apportionments',\n        'taxes', 'fiscal', 'norm', 'cargo_component', 'third_party_nf', 'safety_device',\n        'nfe', 'input_ctrc', 'output_ctrc', 'ctrc', 'transport_data', 'document_authorization',\n        'judicial_process', 'service_order', 'related_movement', 'export_related_movement',\n        'linked_movement', 'c_te', 'eai_integration', 'electronic_invoice_free_fields'\n    }\n    \n    # Date fields that need special processing\n    DATE_FIELDS = {\n        'register_date', 'exit_date', 'delivery_date', 'creation_date', 'last_edit_time', \n        'date', 'entry_date'\n    }\n\n    def __init__(self):\n        self.batch_id = None\n\n    def process_file(self, file_path: str, user_id: int) -> Tuple[str, int, int, List[str], List[dict]]:\n        \"\"\"\n        Process an Excel file and return results\n        \n        Returns:\n            Tuple containing:\n            - batch_id (str): Unique identifier for this upload batch\n            - success_count (int): Number of successfully processed records\n            - error_count (int): Number of records with errors\n            - errors (List[str]): List of error messages\n            - processed_data (List[dict]): List of processed records for database insertion\n        \"\"\"\n        from app import db\n        from models import FinancialMovement, UploadHistory\n        \n        self.batch_id = str(uuid.uuid4())\n        errors = []\n        processed_data = []\n        success_count = 0\n        error_count = 0\n        \n        try:\n            # Read Excel file\n            logger.info(f\"Reading Excel file: {file_path}\")\n            df = pd.read_excel(file_path)\n            logger.info(f\"Read Excel file with {len(df)} rows\")\n            \n            # Process each row\n            for index, row in df.iterrows():\n                try:\n                    # Convert row to dictionary and map columns\n                    record_data = self._process_row(row)\n                    \n                    # Add tracking fields\n                    record_data['upload_batch_id'] = self.batch_id\n                    record_data['uploaded_by'] = user_id\n                    record_data['uploaded_at'] = datetime.utcnow()\n                    \n                    processed_data.append(record_data)\n                    success_count += 1\n                    \n                except Exception as e:\n                    error_msg = f\"Row {index + 1}: {str(e)}\"\n                    errors.append(error_msg)\n                    error_count += 1\n                    logger.error(error_msg)\n            \n            # Create database records\n            if processed_data:\n                self._save_to_database(processed_data, file_path, user_id, success_count, error_count, errors)\n            \n            return self.batch_id, success_count, error_count, errors, processed_data\n            \n        except Exception as e:\n            error_msg = f\"Failed to process file: {str(e)}\"\n            logger.error(error_msg)\n            return self.batch_id or str(uuid.uuid4()), 0, 1, [error_msg], []\n\n    def _process_row(self, row: pd.Series) -> Dict[str, Any]:\n        \"\"\"Process a single row of data\"\"\"\n        processed = {}\n        \n        for excel_col, db_col in self.COLUMN_MAPPING.items():\n            if excel_col in row:\n                value = row[excel_col]\n                \n                # Handle None/NaN values\n                if pd.isna(value):\n                    processed[db_col] = None\n                    continue\n                \n                # Process JSON fields\n                if db_col in self.JSON_FIELDS:\n                    processed[db_col] = self._process_json_field(value)\n                \n                # Process date fields\n                elif db_col in self.DATE_FIELDS:\n                    processed[db_col] = self._process_date_field(value)\n                \n                # Process boolean fields\n                elif isinstance(value, bool) or db_col.endswith('_flag') or db_col.startswith('has_'):\n                    processed[db_col] = bool(value) if value is not None else None\n                \n                # Process numeric fields\n                elif isinstance(value, (int, float)):\n                    processed[db_col] = value\n                \n                # Process string fields\n                else:\n                    processed[db_col] = str(value) if value is not None else None\n            else:\n                processed[db_col] = None\n                \n        return processed\n\n    def _process_json_field(self, value: Any) -> str:\n        \"\"\"Process JSON field values - handles Python literal format from Excel\"\"\"\n        if value is None or pd.isna(value):\n            return json.dumps([])\n        \n        if isinstance(value, str):\n            # Check if it's empty string or just whitespace\n            if not value.strip():\n                return json.dumps([])\n            \n            fixed_value = value.strip()\n            \n            # First, try to parse as standard JSON\n            try:\n                parsed = json.loads(fixed_value)\n                return fixed_value\n            except (json.JSONDecodeError, TypeError):\n                pass\n            \n            # Try to parse as Python literal (with single quotes)\n            try:\n                parsed = ast.literal_eval(fixed_value)\n                # Convert to proper JSON string\n                return json.dumps(parsed)\n            except (ValueError, SyntaxError) as e:\n                # Log the error for debugging\n                logger.warning(f\"Both JSON and literal_eval failed for value: {str(value)[:100]}... Error: {str(e)}\")\n                # Return empty array to avoid breaking the system\n                return json.dumps([])\n        \n        if isinstance(value, list):\n            return json.dumps(value)\n        \n        # For other types, wrap in array\n        return json.dumps([value] if value else [])\n\n    def _process_date_field(self, value: Any) -> datetime:\n        \"\"\"Process date field values\"\"\"\n        if value is None or pd.isna(value):\n            return None\n        \n        if isinstance(value, datetime):\n            return value\n        \n        if isinstance(value, str):\n            try:\n                return datetime.fromisoformat(value.replace('Z', '+00:00'))\n            except ValueError:\n                try:\n                    return pd.to_datetime(value).to_pydatetime()\n                except (ValueError, TypeError):\n                    return None\n        \n        if hasattr(value, 'to_pydatetime'):\n            return value.to_pydatetime()\n        \n        return None\n\n    def _save_to_database(self, processed_data: List[Dict], filename: str, \n                         user_id: int, success_count: int, error_count: int, errors: List[str]):\n        \"\"\"Save processed data to database\"\"\"\n        from app import db\n        from models import FinancialMovement, UploadHistory\n        \n        # Create upload history record\n        upload_history = UploadHistory(\n            batch_id=self.batch_id,\n            filename=filename.split('/')[-1],  # Get just filename without path\n            uploaded_by=user_id,\n            total_rows=len(processed_data),\n            success_count=success_count,\n            error_count=error_count,\n            errors=json.dumps(errors) if errors else None,\n            status='completed' if error_count == 0 else 'completed_with_errors'\n        )\n        \n        # Create movement records\n        movements = [FinancialMovement(**data) for data in processed_data]\n        \n        # Save to database\n        db.session.add(upload_history)\n        db.session.add_all(movements)\n        db.session.commit()\n        \n        logger.info(f\"Saved {success_count} records to database with batch_id {self.batch_id}\")","size_bytes":14020},"file_processor_new.py":{"content":"import pandas as pd\nimport json\nimport uuid\nfrom datetime import datetime\nfrom typing import Dict, List, Tuple, Any\nimport logging\n\nlogger = logging.getLogger(__name__)\n\nclass FinancialDataProcessor:\n    \"\"\"Process Excel files containing financial movement data\"\"\"\n    \n    # Complete column mapping for all 113 fields\n    COLUMN_MAPPING = {\n        'internalId': 'internal_id',\n        'companyId': 'company_id',\n        'movementId': 'movement_id',\n        'branchId': 'branch_id',\n        'warehouseCode': 'warehouse_code',\n        'destinyWarehouseCode': 'destiny_warehouse_code',\n        'number': 'number',\n        'series': 'series',\n        'movementTypeCode': 'movement_type_code',\n        'type': 'type',\n        'status': 'status',\n        'printed': 'printed',\n        'documentPrinted': 'document_printed',\n        'billPrinted': 'bill_printed',\n        'registerDate': 'register_date',\n        'exitDate': 'exit_date',\n        'commercialRepresentativeCharge': 'commercial_representative_charge',\n        'grossValue': 'gross_value',\n        'netValue': 'net_value',\n        'informedNetValue': 'informed_net_value',\n        'otherValues': 'other_values',\n        'discountPercentage': 'discount_percentage',\n        'expensePercentage': 'expense_percentage',\n        'expenseValue': 'expense_value',\n        'extraPercentage1': 'extra_percentage1',\n        'extraValue1': 'extra_value1',\n        'extraPercentage2': 'extra_percentage2',\n        'extraValue2': 'extra_value2',\n        'transportedProductNetWeight': 'transported_product_net_weight',\n        'transportedProductGrossWeight': 'transported_product_gross_weight',\n        'classificationTable5Code': 'classification_table5_code',\n        'financialOptionalTable2Code': 'financial_optional_table2_code',\n        'netValueCurrencyCode': 'net_value_currency_code',\n        'date': 'date',\n        'hasGeneratedBill': 'has_generated_bill',\n        'auxCustomerVendorCode': 'aux_customer_vendor_code',\n        'auxCustomerVendorCompanyId': 'aux_customer_vendor_company_id',\n        'costCenterCode': 'cost_center_code',\n        'salesman1Code': 'salesman1_code',\n        'chargePercentage': 'charge_percentage',\n        'salesman2ChargePercentage': 'salesman2_charge_percentage',\n        'salesman3ChargePercentage': 'salesman3_charge_percentage',\n        'salesman4ChargePercentage': 'salesman4_charge_percentage',\n        'userCode': 'user_code',\n        'destinyBranchId': 'destiny_branch_id',\n        'lotGenerated': 'lot_generated',\n        'accountingExportStatus': 'accounting_export_status',\n        'deliveryDate': 'delivery_date',\n        'hasGeneratedWorkAccount': 'has_generated_work_account',\n        'workAccountGenerated': 'work_account_generated',\n        'lastEditTime': 'last_edit_time',\n        'indicateObjectUse': 'indicate_object_use',\n        'bonumIntegrated': 'bonum_integrated',\n        'processedFlag': 'processed_flag',\n        'icmsDeductionValue': 'icms_deduction_value',\n        'creationUser': 'creation_user',\n        'creationDate': 'creation_date',\n        'emailStatus': 'email_status',\n        'internalGrossValue': 'internal_gross_value',\n        'otherCompanyINSSBaseValue': 'other_company_inss_base_value',\n        'conditionalDiscountValue': 'conditional_discount_value',\n        'conditionalExpenseValue': 'conditional_expense_value',\n        'affectStockOrder': 'affect_stock_order',\n        'commercialAutomationExported': 'commercial_automation_exported',\n        'aplicationIntegration': 'aplication_integration',\n        'entryDate': 'entry_date',\n        'extemporaneous': 'extemporaneous',\n        'merchandiseValue': 'merchandise_value',\n        'usesFinancialValueApportionment': 'uses_financial_value_apportionment',\n        'conclusionFlag': 'conclusion_flag',\n        'paradigmaStatus': 'paradigma_status',\n        'paradigmaAutoIntegrated': 'paradigma_auto_integrated',\n        'originalGrossValue': 'original_gross_value',\n        'originalNetValue': 'original_net_value',\n        'originalOtherValues': 'original_other_values',\n        'operationId': 'operation_id',\n        'scpBranchId': 'scp_branch_id',\n        'movementItems': 'movement_items',\n        'payments': 'payments',\n        'costCenterApportionments': 'cost_center_apportionments',\n        'departmentApportionments': 'department_apportionments',\n        'taxes': 'taxes',\n        'fiscal': 'fiscal',\n        'norm': 'norm',\n        'cargoComponent': 'cargo_component',\n        'thirdPartyNF': 'third_party_nf',\n        'safetyDevice': 'safety_device',\n        'nfe': 'nfe',\n        'inputCTRC': 'input_ctrc',\n        'outputCTRC': 'output_ctrc',\n        'ctrc': 'ctrc',\n        'transportData': 'transport_data',\n        'documentAuthorization': 'document_authorization',\n        'judicialProcess': 'judicial_process',\n        'serviceOrder': 'service_order',\n        'relatedMovement': 'related_movement',\n        'exportRelatedMovement': 'export_related_movement',\n        'linkedMovement': 'linked_movement',\n        'cTe': 'c_te',\n        'eaiIntegration': 'eai_integration',\n        'electronicInvoiceFreeFields': 'electronic_invoice_free_fields',\n        'customerVendorCode': 'customer_vendor_code',\n        'paymentTermCode': 'payment_term_code',\n        'observation': 'observation',\n        'financialOptionalTable1Code': 'financial_optional_table1_code',\n        'financialEntryMovementId': 'financial_entry_movement_id',\n        'generatedEntryNumber': 'generated_entry_number',\n        'openEntryNumber': 'open_entry_number',\n        'cashAccountCode': 'cash_account_code',\n        'customerVendorCompanyId': 'customer_vendor_company_id',\n        'fluxusGroupedFlag': 'fluxus_grouped_flag',\n        'cashAccountCompanyId': 'cash_account_company_id',\n        'longHistory': 'long_history',\n    }\n    \n    # JSON fields that need special processing\n    JSON_FIELDS = {\n        'movement_items', 'payments', 'cost_center_apportionments', 'department_apportionments',\n        'taxes', 'fiscal', 'norm', 'cargo_component', 'third_party_nf', 'safety_device',\n        'nfe', 'input_ctrc', 'output_ctrc', 'ctrc', 'transport_data', 'document_authorization',\n        'judicial_process', 'service_order', 'related_movement', 'export_related_movement',\n        'linked_movement', 'c_te', 'eai_integration', 'electronic_invoice_free_fields'\n    }\n    \n    # Date fields that need special processing\n    DATE_FIELDS = {\n        'register_date', 'exit_date', 'delivery_date', 'creation_date', 'last_edit_time', \n        'date', 'entry_date'\n    }\n\n    def __init__(self):\n        self.batch_id = None\n\n    def process_file(self, file_path: str, user_id: int) -> Tuple[str, int, int, List[str], List[dict]]:\n        \"\"\"\n        Process an Excel file and return results\n        \n        Returns:\n            Tuple containing:\n            - batch_id (str): Unique identifier for this upload batch\n            - success_count (int): Number of successfully processed records\n            - error_count (int): Number of records with errors\n            - errors (List[str]): List of error messages\n            - processed_data (List[dict]): List of processed records for database insertion\n        \"\"\"\n        from app import db\n        from models import FinancialMovement, UploadHistory\n        \n        self.batch_id = str(uuid.uuid4())\n        errors = []\n        processed_data = []\n        success_count = 0\n        error_count = 0\n        \n        try:\n            # Read Excel file\n            logger.info(f\"Reading Excel file: {file_path}\")\n            df = pd.read_excel(file_path)\n            logger.info(f\"Read Excel file with {len(df)} rows\")\n            \n            # Process each row\n            for index, row in df.iterrows():\n                try:\n                    # Convert row to dictionary and map columns\n                    record_data = self._process_row(row)\n                    \n                    # Add tracking fields\n                    record_data['upload_batch_id'] = self.batch_id\n                    record_data['uploaded_by'] = user_id\n                    record_data['uploaded_at'] = datetime.utcnow()\n                    \n                    processed_data.append(record_data)\n                    success_count += 1\n                    \n                except Exception as e:\n                    error_msg = f\"Row {index + 1}: {str(e)}\"\n                    errors.append(error_msg)\n                    error_count += 1\n                    logger.error(error_msg)\n            \n            # Create database records\n            if processed_data:\n                self._save_to_database(processed_data, file_path, user_id, success_count, error_count, errors)\n            \n            return self.batch_id, success_count, error_count, errors, processed_data\n            \n        except Exception as e:\n            error_msg = f\"Failed to process file: {str(e)}\"\n            logger.error(error_msg)\n            return self.batch_id or str(uuid.uuid4()), 0, 1, [error_msg], []\n\n    def _process_row(self, row: pd.Series) -> Dict[str, Any]:\n        \"\"\"Process a single row of data\"\"\"\n        processed = {}\n        \n        for excel_col, db_col in self.COLUMN_MAPPING.items():\n            if excel_col in row:\n                value = row[excel_col]\n                \n                # Handle None/NaN values\n                if pd.isna(value):\n                    processed[db_col] = None\n                    continue\n                \n                # Process JSON fields\n                if db_col in self.JSON_FIELDS:\n                    processed[db_col] = self._process_json_field(value)\n                \n                # Process date fields\n                elif db_col in self.DATE_FIELDS:\n                    processed[db_col] = self._process_date_field(value)\n                \n                # Process boolean fields\n                elif isinstance(value, bool) or db_col.endswith('_flag') or db_col.startswith('has_'):\n                    processed[db_col] = bool(value) if value is not None else None\n                \n                # Process numeric fields\n                elif isinstance(value, (int, float)):\n                    processed[db_col] = value\n                \n                # Process string fields\n                else:\n                    processed[db_col] = str(value) if value is not None else None\n            else:\n                processed[db_col] = None\n                \n        return processed\n\n    def _process_json_field(self, value: Any) -> str:\n        \"\"\"Process JSON field values\"\"\"\n        if value is None or pd.isna(value):\n            return None\n        \n        if isinstance(value, str):\n            # If it's already a JSON string, return as is\n            try:\n                json.loads(value)\n                return value\n            except (json.JSONDecodeError, TypeError):\n                return json.dumps([])\n        \n        if isinstance(value, list):\n            return json.dumps(value)\n        \n        # For other types, wrap in array\n        return json.dumps([value] if value else [])\n\n    def _process_date_field(self, value: Any) -> datetime:\n        \"\"\"Process date field values\"\"\"\n        if value is None or pd.isna(value):\n            return None\n        \n        if isinstance(value, datetime):\n            return value\n        \n        if isinstance(value, str):\n            try:\n                return datetime.fromisoformat(value.replace('Z', '+00:00'))\n            except ValueError:\n                try:\n                    return pd.to_datetime(value).to_pydatetime()\n                except (ValueError, TypeError):\n                    return None\n        \n        if hasattr(value, 'to_pydatetime'):\n            return value.to_pydatetime()\n        \n        return None\n\n    def _save_to_database(self, processed_data: List[Dict], filename: str, \n                         user_id: int, success_count: int, error_count: int, errors: List[str]):\n        \"\"\"Save processed data to database\"\"\"\n        from app import db\n        from models import FinancialMovement, UploadHistory\n        \n        # Create upload history record\n        upload_history = UploadHistory(\n            batch_id=self.batch_id,\n            filename=filename.split('/')[-1],  # Get just filename without path\n            uploaded_by=user_id,\n            total_rows=len(processed_data),\n            success_count=success_count,\n            error_count=error_count,\n            errors=json.dumps(errors) if errors else None,\n            status='completed' if error_count == 0 else 'completed_with_errors'\n        )\n        \n        # Create movement records\n        movements = [FinancialMovement(**data) for data in processed_data]\n        \n        # Save to database\n        db.session.add(upload_history)\n        db.session.add_all(movements)\n        db.session.commit()\n        \n        logger.info(f\"Saved {success_count} records to database with batch_id {self.batch_id}\")","size_bytes":12948},"forms.py":{"content":"from flask_wtf import FlaskForm\nfrom flask_wtf.file import FileField, FileRequired, FileAllowed\nfrom wtforms import StringField, PasswordField, BooleanField, SubmitField, SelectField, TextAreaField\nfrom wtforms.validators import DataRequired, Email, Length, EqualTo, ValidationError\nfrom models import User\n\nclass LoginForm(FlaskForm):\n    username = StringField('Username', validators=[DataRequired()])\n    password = PasswordField('Password', validators=[DataRequired()])\n    remember_me = BooleanField('Remember Me')\n    submit = SubmitField('Sign In')\n\nclass UserForm(FlaskForm):\n    username = StringField('Username', validators=[\n        DataRequired(), \n        Length(min=3, max=80)\n    ])\n    email = StringField('Email', validators=[\n        DataRequired(), \n        Email()\n    ])\n    password = PasswordField('Password', validators=[\n        DataRequired(), \n        Length(min=6)\n    ])\n    password2 = PasswordField('Repeat Password', validators=[\n        DataRequired(), \n        EqualTo('password', message='Passwords must match')\n    ])\n    is_admin = BooleanField('Administrator')\n    submit = SubmitField('Create User')\n    \n    def __init__(self, original_username=None, original_email=None, *args, **kwargs):\n        super(UserForm, self).__init__(*args, **kwargs)\n        self.original_username = original_username\n        self.original_email = original_email\n    \n    def validate_username(self, username):\n        if username.data != self.original_username:\n            user = User.query.filter_by(username=username.data).first()\n            if user is not None:\n                raise ValidationError('Username already exists.')\n    \n    def validate_email(self, email):\n        if email.data != self.original_email:\n            user = User.query.filter_by(email=email.data).first()\n            if user is not None:\n                raise ValidationError('Email already registered.')\n\nclass EditUserForm(FlaskForm):\n    username = StringField('Username', validators=[\n        DataRequired(), \n        Length(min=3, max=80)\n    ])\n    email = StringField('Email', validators=[\n        DataRequired(), \n        Email()\n    ])\n    is_admin = BooleanField('Administrator')\n    submit = SubmitField('Update User')\n    \n    def __init__(self, original_username=None, original_email=None, *args, **kwargs):\n        super(EditUserForm, self).__init__(*args, **kwargs)\n        self.original_username = original_username\n        self.original_email = original_email\n    \n    def validate_username(self, username):\n        if username.data != self.original_username:\n            user = User.query.filter_by(username=username.data).first()\n            if user is not None:\n                raise ValidationError('Username already exists.')\n    \n    def validate_email(self, email):\n        if email.data != self.original_email:\n            user = User.query.filter_by(email=email.data).first()\n            if user is not None:\n                raise ValidationError('Email already registered.')\n\nclass FileUploadForm(FlaskForm):\n    file = FileField('Excel File', validators=[\n        FileRequired(),\n        FileAllowed(['xlsx', 'xls'], 'Only Excel files (.xlsx, .xls) are allowed!')\n    ])\n    description = TextAreaField('Description (Optional)')\n    submit = SubmitField('Upload File')\n","size_bytes":3299},"main.py":{"content":"from app import app\n\nif __name__ == \"__main__\":\n    app.run(host=\"0.0.0.0\", port=5000, debug=True)\n","size_bytes":99},"models.py":{"content":"from app import db\nfrom flask_login import UserMixin\nfrom datetime import datetime\nimport json\n\nclass User(UserMixin, db.Model):\n    id = db.Column(db.Integer, primary_key=True)\n    username = db.Column(db.String(80), unique=True, nullable=False)\n    email = db.Column(db.String(120), unique=True, nullable=False)\n    password_hash = db.Column(db.String(256), nullable=False)\n    is_admin = db.Column(db.Boolean, default=False)\n    created_at = db.Column(db.DateTime, default=datetime.utcnow)\n    \n    def __repr__(self):\n        return f'<User {self.username}>'\n\nclass FinancialMovement(db.Model):\n    id = db.Column(db.Integer, primary_key=True)\n    \n    # Core identification fields - all 117 fields from Excel (added 4 new company/branch info fields)\n    internal_id = db.Column(db.String(50), nullable=True)\n    company_id = db.Column(db.Integer, nullable=True)\n    movement_id = db.Column(db.Integer, nullable=True)\n    branch_id = db.Column(db.Integer, nullable=True)\n    \n    # Company and Branch information (new fields)\n    empresa_code = db.Column(db.String(50), nullable=True)\n    empresa_nome = db.Column(db.String(255), nullable=True)\n    filial_code = db.Column(db.String(50), nullable=True)\n    filial_nome = db.Column(db.String(255), nullable=True)\n    warehouse_code = db.Column(db.String(50), nullable=True)\n    destiny_warehouse_code = db.Column(db.String(50), nullable=True)\n    number = db.Column(db.Integer, nullable=True)\n    series = db.Column(db.String(10), nullable=True)\n    movement_type_code = db.Column(db.String(50), nullable=True)\n    type = db.Column(db.String(10), nullable=True)\n    status = db.Column(db.String(10), nullable=True)\n    printed = db.Column(db.Boolean, nullable=True)\n    document_printed = db.Column(db.Boolean, nullable=True)\n    bill_printed = db.Column(db.Boolean, nullable=True)\n    register_date = db.Column(db.DateTime, nullable=True)\n    exit_date = db.Column(db.DateTime, nullable=True)\n    commercial_representative_charge = db.Column(db.Float, nullable=True)\n    gross_value = db.Column(db.Float, nullable=True)\n    net_value = db.Column(db.Float, nullable=True)\n    informed_net_value = db.Column(db.Float, nullable=True)\n    other_values = db.Column(db.Float, nullable=True)\n    discount_percentage = db.Column(db.Float, nullable=True)\n    expense_percentage = db.Column(db.Float, nullable=True)\n    expense_value = db.Column(db.Float, nullable=True)\n    extra_percentage1 = db.Column(db.Float, nullable=True)\n    extra_value1 = db.Column(db.Float, nullable=True)\n    extra_percentage2 = db.Column(db.Float, nullable=True)\n    extra_value2 = db.Column(db.Float, nullable=True)\n    transported_product_net_weight = db.Column(db.Float, nullable=True)\n    transported_product_gross_weight = db.Column(db.Float, nullable=True)\n    classification_table5_code = db.Column(db.String(50), nullable=True)\n    financial_optional_table2_code = db.Column(db.String(50), nullable=True)\n    net_value_currency_code = db.Column(db.String(10), nullable=True)\n    date = db.Column(db.DateTime, nullable=True)\n    has_generated_bill = db.Column(db.Boolean, nullable=True)\n    aux_customer_vendor_code = db.Column(db.String(50), nullable=True)\n    aux_customer_vendor_company_id = db.Column(db.Integer, nullable=True)\n    cost_center_code = db.Column(db.String(50), nullable=True)\n    salesman1_code = db.Column(db.String(50), nullable=True)\n    charge_percentage = db.Column(db.Float, nullable=True)\n    salesman2_charge_percentage = db.Column(db.Float, nullable=True)\n    salesman3_charge_percentage = db.Column(db.Float, nullable=True)\n    salesman4_charge_percentage = db.Column(db.Float, nullable=True)\n    user_code = db.Column(db.String(50), nullable=True)\n    destiny_branch_id = db.Column(db.Integer, nullable=True)\n    lot_generated = db.Column(db.Boolean, nullable=True)\n    accounting_export_status = db.Column(db.String(50), nullable=True)\n    delivery_date = db.Column(db.DateTime, nullable=True)\n    has_generated_work_account = db.Column(db.Boolean, nullable=True)\n    work_account_generated = db.Column(db.String(50), nullable=True)\n    last_edit_time = db.Column(db.DateTime, nullable=True)\n    indicate_object_use = db.Column(db.String(50), nullable=True)\n    bonum_integrated = db.Column(db.Boolean, nullable=True)\n    processed_flag = db.Column(db.Boolean, nullable=True)\n    icms_deduction_value = db.Column(db.Float, nullable=True)\n    creation_user = db.Column(db.String(50), nullable=True)\n    creation_date = db.Column(db.DateTime, nullable=True)\n    email_status = db.Column(db.String(50), nullable=True)\n    internal_gross_value = db.Column(db.Float, nullable=True)\n    other_company_inss_base_value = db.Column(db.Float, nullable=True)\n    conditional_discount_value = db.Column(db.Float, nullable=True)\n    conditional_expense_value = db.Column(db.Float, nullable=True)\n    affect_stock_order = db.Column(db.String(50), nullable=True)\n    commercial_automation_exported = db.Column(db.Integer, nullable=True)\n    aplication_integration = db.Column(db.String(10), nullable=True)\n    entry_date = db.Column(db.DateTime, nullable=True)\n    extemporaneous = db.Column(db.Integer, nullable=True)\n    merchandise_value = db.Column(db.Float, nullable=True)\n    uses_financial_value_apportionment = db.Column(db.Boolean, nullable=True)\n    conclusion_flag = db.Column(db.Integer, nullable=True)\n    paradigma_status = db.Column(db.String(10), nullable=True)\n    paradigma_auto_integrated = db.Column(db.Boolean, nullable=True)\n    original_gross_value = db.Column(db.Float, nullable=True)\n    original_net_value = db.Column(db.Float, nullable=True)\n    original_other_values = db.Column(db.Float, nullable=True)\n    operation_id = db.Column(db.Integer, nullable=True)\n    scp_branch_id = db.Column(db.Integer, nullable=True)\n    \n    # JSON fields - properly formatted as Text columns\n    movement_items = db.Column(db.Text, nullable=True)\n    payments = db.Column(db.Text, nullable=True)\n    cost_center_apportionments = db.Column(db.Text, nullable=True)\n    department_apportionments = db.Column(db.Text, nullable=True)\n    taxes = db.Column(db.Text, nullable=True)\n    fiscal = db.Column(db.Text, nullable=True)\n    norm = db.Column(db.Text, nullable=True)\n    cargo_component = db.Column(db.Text, nullable=True)\n    third_party_nf = db.Column(db.Text, nullable=True)\n    safety_device = db.Column(db.Text, nullable=True)\n    nfe = db.Column(db.Text, nullable=True)\n    input_ctrc = db.Column(db.Text, nullable=True)\n    output_ctrc = db.Column(db.Text, nullable=True)\n    ctrc = db.Column(db.Text, nullable=True)\n    transport_data = db.Column(db.Text, nullable=True)\n    document_authorization = db.Column(db.Text, nullable=True)\n    judicial_process = db.Column(db.Text, nullable=True)\n    service_order = db.Column(db.Text, nullable=True)\n    related_movement = db.Column(db.Text, nullable=True)\n    export_related_movement = db.Column(db.Text, nullable=True)\n    linked_movement = db.Column(db.Text, nullable=True)\n    c_te = db.Column(db.Text, nullable=True)\n    eai_integration = db.Column(db.Text, nullable=True)\n    electronic_invoice_free_fields = db.Column(db.Text, nullable=True)\n    \n    # Remaining fields\n    customer_vendor_code = db.Column(db.String(50), nullable=True)\n    payment_term_code = db.Column(db.String(50), nullable=True)\n    observation = db.Column(db.Text, nullable=True)\n    financial_optional_table1_code = db.Column(db.String(50), nullable=True)\n    financial_entry_movement_id = db.Column(db.String(50), nullable=True)\n    generated_entry_number = db.Column(db.String(50), nullable=True)\n    open_entry_number = db.Column(db.String(50), nullable=True)\n    cash_account_code = db.Column(db.String(50), nullable=True)\n    customer_vendor_company_id = db.Column(db.Integer, nullable=True)\n    fluxus_grouped_flag = db.Column(db.Boolean, nullable=True)\n    cash_account_company_id = db.Column(db.Integer, nullable=True)\n    long_history = db.Column(db.Text, nullable=True)\n    \n    # Upload tracking\n    upload_batch_id = db.Column(db.String(36))\n    uploaded_by = db.Column(db.Integer, db.ForeignKey('user.id'))\n    uploaded_at = db.Column(db.DateTime, default=datetime.utcnow)\n    \n    def get_json_field(self, field_name):\n        \"\"\"Get a JSON field as a Python object\"\"\"\n        field_value = getattr(self, field_name, None)\n        if field_value:\n            try:\n                return json.loads(field_value) if isinstance(field_value, str) else field_value\n            except (json.JSONDecodeError, TypeError):\n                return []\n        return []\n\n    def set_json_field(self, field_name, value):\n        \"\"\"Set a JSON field from a Python object\"\"\"\n        if value:\n            json_value = json.dumps(value) if not isinstance(value, str) else value\n            setattr(self, field_name, json_value)\n        else:\n            setattr(self, field_name, None)\n    \n    def __repr__(self):\n        return f'<FinancialMovement {self.internal_id}>'\n\nclass UploadHistory(db.Model):\n    id = db.Column(db.Integer, primary_key=True)\n    batch_id = db.Column(db.String(36), unique=True, nullable=False)\n    filename = db.Column(db.String(255), nullable=False)\n    uploaded_by = db.Column(db.Integer, db.ForeignKey('user.id'), nullable=False)\n    uploaded_at = db.Column(db.DateTime, default=datetime.utcnow)\n    total_rows = db.Column(db.Integer, default=0)\n    success_count = db.Column(db.Integer, default=0)\n    error_count = db.Column(db.Integer, default=0)\n    errors = db.Column(db.Text)\n    status = db.Column(db.String(20), default='processing')\n    \n    # Relationship\n    uploader = db.relationship('User', backref='uploads')\n    \n    def __repr__(self):\n        return f'<UploadHistory {self.filename}>'","size_bytes":9725},"models_new.py":{"content":"from app import db\nfrom flask_login import UserMixin\nfrom datetime import datetime\nimport json\n\nclass User(UserMixin, db.Model):\n    id = db.Column(db.Integer, primary_key=True)\n    username = db.Column(db.String(80), unique=True, nullable=False)\n    email = db.Column(db.String(120), unique=True, nullable=False)\n    password_hash = db.Column(db.String(256), nullable=False)\n    is_admin = db.Column(db.Boolean, default=False)\n    created_at = db.Column(db.DateTime, default=datetime.utcnow)\n    \n    def __repr__(self):\n        return f'<User {self.username}>'\n\nclass FinancialMovement(db.Model):\n    id = db.Column(db.Integer, primary_key=True)\n    \n    # Core identification fields - all 113 fields from Excel\n    internal_id = db.Column(db.String(50), nullable=True)\n    company_id = db.Column(db.Integer, nullable=True)\n    movement_id = db.Column(db.Integer, nullable=True)\n    branch_id = db.Column(db.Integer, nullable=True)\n    warehouse_code = db.Column(db.String(50), nullable=True)\n    destiny_warehouse_code = db.Column(db.String(50), nullable=True)\n    number = db.Column(db.Integer, nullable=True)\n    series = db.Column(db.String(10), nullable=True)\n    movement_type_code = db.Column(db.String(50), nullable=True)\n    type = db.Column(db.String(10), nullable=True)\n    status = db.Column(db.String(10), nullable=True)\n    printed = db.Column(db.Boolean, nullable=True)\n    document_printed = db.Column(db.Boolean, nullable=True)\n    bill_printed = db.Column(db.Boolean, nullable=True)\n    register_date = db.Column(db.DateTime, nullable=True)\n    exit_date = db.Column(db.DateTime, nullable=True)\n    commercial_representative_charge = db.Column(db.Float, nullable=True)\n    gross_value = db.Column(db.Float, nullable=True)\n    net_value = db.Column(db.Float, nullable=True)\n    informed_net_value = db.Column(db.Float, nullable=True)\n    other_values = db.Column(db.Float, nullable=True)\n    discount_percentage = db.Column(db.Float, nullable=True)\n    expense_percentage = db.Column(db.Float, nullable=True)\n    expense_value = db.Column(db.Float, nullable=True)\n    extra_percentage1 = db.Column(db.Float, nullable=True)\n    extra_value1 = db.Column(db.Float, nullable=True)\n    extra_percentage2 = db.Column(db.Float, nullable=True)\n    extra_value2 = db.Column(db.Float, nullable=True)\n    transported_product_net_weight = db.Column(db.Float, nullable=True)\n    transported_product_gross_weight = db.Column(db.Float, nullable=True)\n    classification_table5_code = db.Column(db.String(50), nullable=True)\n    financial_optional_table2_code = db.Column(db.String(50), nullable=True)\n    net_value_currency_code = db.Column(db.String(10), nullable=True)\n    date = db.Column(db.DateTime, nullable=True)\n    has_generated_bill = db.Column(db.Boolean, nullable=True)\n    aux_customer_vendor_code = db.Column(db.String(50), nullable=True)\n    aux_customer_vendor_company_id = db.Column(db.Integer, nullable=True)\n    cost_center_code = db.Column(db.String(50), nullable=True)\n    salesman1_code = db.Column(db.String(50), nullable=True)\n    charge_percentage = db.Column(db.Float, nullable=True)\n    salesman2_charge_percentage = db.Column(db.Float, nullable=True)\n    salesman3_charge_percentage = db.Column(db.Float, nullable=True)\n    salesman4_charge_percentage = db.Column(db.Float, nullable=True)\n    user_code = db.Column(db.String(50), nullable=True)\n    destiny_branch_id = db.Column(db.Integer, nullable=True)\n    lot_generated = db.Column(db.Boolean, nullable=True)\n    accounting_export_status = db.Column(db.String(50), nullable=True)\n    delivery_date = db.Column(db.DateTime, nullable=True)\n    has_generated_work_account = db.Column(db.Boolean, nullable=True)\n    work_account_generated = db.Column(db.String(50), nullable=True)\n    last_edit_time = db.Column(db.DateTime, nullable=True)\n    indicate_object_use = db.Column(db.String(50), nullable=True)\n    bonum_integrated = db.Column(db.Boolean, nullable=True)\n    processed_flag = db.Column(db.Boolean, nullable=True)\n    icms_deduction_value = db.Column(db.Float, nullable=True)\n    creation_user = db.Column(db.String(50), nullable=True)\n    creation_date = db.Column(db.DateTime, nullable=True)\n    email_status = db.Column(db.String(50), nullable=True)\n    internal_gross_value = db.Column(db.Float, nullable=True)\n    other_company_inss_base_value = db.Column(db.Float, nullable=True)\n    conditional_discount_value = db.Column(db.Float, nullable=True)\n    conditional_expense_value = db.Column(db.Float, nullable=True)\n    affect_stock_order = db.Column(db.String(50), nullable=True)\n    commercial_automation_exported = db.Column(db.Integer, nullable=True)\n    aplication_integration = db.Column(db.String(10), nullable=True)\n    entry_date = db.Column(db.DateTime, nullable=True)\n    extemporaneous = db.Column(db.Integer, nullable=True)\n    merchandise_value = db.Column(db.Float, nullable=True)\n    uses_financial_value_apportionment = db.Column(db.Boolean, nullable=True)\n    conclusion_flag = db.Column(db.Integer, nullable=True)\n    paradigma_status = db.Column(db.String(10), nullable=True)\n    paradigma_auto_integrated = db.Column(db.Boolean, nullable=True)\n    original_gross_value = db.Column(db.Float, nullable=True)\n    original_net_value = db.Column(db.Float, nullable=True)\n    original_other_values = db.Column(db.Float, nullable=True)\n    operation_id = db.Column(db.Integer, nullable=True)\n    scp_branch_id = db.Column(db.Integer, nullable=True)\n    \n    # JSON fields - properly formatted as Text columns\n    movement_items = db.Column(db.Text, nullable=True)\n    payments = db.Column(db.Text, nullable=True)\n    cost_center_apportionments = db.Column(db.Text, nullable=True)\n    department_apportionments = db.Column(db.Text, nullable=True)\n    taxes = db.Column(db.Text, nullable=True)\n    fiscal = db.Column(db.Text, nullable=True)\n    norm = db.Column(db.Text, nullable=True)\n    cargo_component = db.Column(db.Text, nullable=True)\n    third_party_nf = db.Column(db.Text, nullable=True)\n    safety_device = db.Column(db.Text, nullable=True)\n    nfe = db.Column(db.Text, nullable=True)\n    input_ctrc = db.Column(db.Text, nullable=True)\n    output_ctrc = db.Column(db.Text, nullable=True)\n    ctrc = db.Column(db.Text, nullable=True)\n    transport_data = db.Column(db.Text, nullable=True)\n    document_authorization = db.Column(db.Text, nullable=True)\n    judicial_process = db.Column(db.Text, nullable=True)\n    service_order = db.Column(db.Text, nullable=True)\n    related_movement = db.Column(db.Text, nullable=True)\n    export_related_movement = db.Column(db.Text, nullable=True)\n    linked_movement = db.Column(db.Text, nullable=True)\n    c_te = db.Column(db.Text, nullable=True)\n    eai_integration = db.Column(db.Text, nullable=True)\n    electronic_invoice_free_fields = db.Column(db.Text, nullable=True)\n    \n    # Remaining fields\n    customer_vendor_code = db.Column(db.String(50), nullable=True)\n    payment_term_code = db.Column(db.String(50), nullable=True)\n    observation = db.Column(db.Text, nullable=True)\n    financial_optional_table1_code = db.Column(db.String(50), nullable=True)\n    financial_entry_movement_id = db.Column(db.String(50), nullable=True)\n    generated_entry_number = db.Column(db.String(50), nullable=True)\n    open_entry_number = db.Column(db.String(50), nullable=True)\n    cash_account_code = db.Column(db.String(50), nullable=True)\n    customer_vendor_company_id = db.Column(db.Integer, nullable=True)\n    fluxus_grouped_flag = db.Column(db.Boolean, nullable=True)\n    cash_account_company_id = db.Column(db.Integer, nullable=True)\n    long_history = db.Column(db.Text, nullable=True)\n    \n    # Upload tracking\n    upload_batch_id = db.Column(db.String(36))\n    uploaded_by = db.Column(db.Integer, db.ForeignKey('user.id'))\n    uploaded_at = db.Column(db.DateTime, default=datetime.utcnow)\n    \n    def get_json_field(self, field_name):\n        \"\"\"Get a JSON field as a Python object\"\"\"\n        field_value = getattr(self, field_name, None)\n        if field_value:\n            try:\n                return json.loads(field_value) if isinstance(field_value, str) else field_value\n            except (json.JSONDecodeError, TypeError):\n                return []\n        return []\n\n    def set_json_field(self, field_name, value):\n        \"\"\"Set a JSON field from a Python object\"\"\"\n        if value:\n            json_value = json.dumps(value) if not isinstance(value, str) else value\n            setattr(self, field_name, json_value)\n        else:\n            setattr(self, field_name, None)\n    \n    def __repr__(self):\n        return f'<FinancialMovement {self.internal_id}>'\n\nclass UploadHistory(db.Model):\n    id = db.Column(db.Integer, primary_key=True)\n    batch_id = db.Column(db.String(36), unique=True, nullable=False)\n    filename = db.Column(db.String(255), nullable=False)\n    uploaded_by = db.Column(db.Integer, db.ForeignKey('user.id'), nullable=False)\n    uploaded_at = db.Column(db.DateTime, default=datetime.utcnow)\n    total_rows = db.Column(db.Integer, default=0)\n    success_count = db.Column(db.Integer, default=0)\n    error_count = db.Column(db.Integer, default=0)\n    errors = db.Column(db.Text)\n    status = db.Column(db.String(20), default='processing')\n    \n    # Relationship\n    uploader = db.relationship('User', backref='uploads')\n    \n    def __repr__(self):\n        return f'<UploadHistory {self.filename}>'","size_bytes":9393},"pyproject.toml":{"content":"[project]\nname = \"repl-nix-workspace\"\nversion = \"0.1.0\"\ndescription = \"Add your description here\"\nrequires-python = \">=3.11\"\ndependencies = [\n    \"email-validator>=2.2.0\",\n    \"flask-dance>=7.1.0\",\n    \"flask>=3.1.1\",\n    \"flask-sqlalchemy>=3.1.1\",\n    \"gunicorn>=23.0.0\",\n    \"psycopg2-binary>=2.9.10\",\n    \"pyjwt>=2.10.1\",\n    \"flask-login>=0.6.3\",\n    \"oauthlib>=3.3.1\",\n    \"werkzeug>=3.1.3\",\n    \"flask-wtf>=1.2.2\",\n    \"wtforms>=3.2.1\",\n    \"pandas>=2.3.1\",\n    \"openpyxl>=3.1.5\",\n    \"xlrd>=2.0.2\",\n    \"sqlalchemy>=2.0.43\",\n]\n","size_bytes":534},"replit.md":{"content":"# Overview\n\nThis is a Flask-based financial data management system designed for importing, storing, and analyzing financial movement data from Excel files. The application features a role-based user system with administrative controls for user management and data upload capabilities. Users can browse and search through financial movement records, while administrators can upload Excel files containing structured financial data and manage user accounts.\n\n**Recent Update (Aug 2025)**: System now supports complete 117-field Excel structure with all JSON fields properly processed and displayed. Added 4 new company/branch identification fields: `Empresa_Code`, `Empresa_Nome`, `Filial_Code`, `Filial_Nome`. Upload functionality fully working with batch tracking and error handling.\n\n# User Preferences\n\nPreferred communication style: Simple, everyday language.\n\n# System Architecture\n\n## Web Framework\n- **Flask** with SQLAlchemy ORM for database operations\n- **Flask-Login** for user session management and authentication\n- **Flask-WTF** for form handling and CSRF protection\n- Blueprint-based routing for modular organization (main, admin, auth)\n\n## Authentication & Authorization\n- Password-based authentication with hashed storage using Werkzeug\n- Role-based access control with admin/regular user distinction\n- Session-based login with \"remember me\" functionality\n- Decorator-based route protection for admin-only features\n\n## Database Design\n- SQLAlchemy with SQLite default (configurable via DATABASE_URL)\n- **User model**: Basic user management with admin flags\n- **FinancialMovement model**: Comprehensive financial transaction records with 40+ fields\n- **UploadHistory model**: Tracks file upload batches and processing results\n- Connection pooling and automatic table creation\n\n## Data Processing\n- **FinancialDataProcessor class**: Handles Excel file parsing and data transformation\n- Column mapping from Excel headers to database fields\n- Batch processing with error handling and validation\n- Support for complex financial data structures including dates, currencies, and business logic fields\n\n## Frontend Architecture\n- Bootstrap 5 dark theme for responsive UI\n- jQuery and DataTables for enhanced table functionality\n- Template inheritance using Jinja2\n- Progressive enhancement with JavaScript for user interactions\n- File upload interface with drag-and-drop support\n\n## File Management\n- Secure file upload handling with size limits (50MB)\n- Excel file processing with pandas\n- Upload history tracking with success/failure metrics\n- Organized upload directory structure\n\n# External Dependencies\n\n## Core Framework Dependencies\n- **Flask**: Web application framework\n- **SQLAlchemy/Flask-SQLAlchemy**: Database ORM and integration\n- **Flask-Login**: User session management\n- **Flask-WTF/WTForms**: Form handling and validation\n- **Werkzeug**: WSGI utilities and security helpers\n\n## Data Processing\n- **pandas**: Excel file reading and data manipulation\n- **openpyxl**: Excel file format support\n\n## Frontend Libraries (CDN)\n- **Bootstrap 5**: UI framework with dark theme\n- **DataTables**: Enhanced table functionality\n- **Font Awesome**: Icon library\n- **jQuery**: JavaScript utilities\n\n## Infrastructure\n- **SQLite**: Default database (production can use PostgreSQL via DATABASE_URL)\n- **ProxyFix**: WSGI middleware for proper URL generation behind proxies\n- File system storage for uploaded Excel files\n\n## Security Features\n- CSRF protection through Flask-WTF\n- Password hashing with Werkzeug\n- Secure filename handling for uploads\n- Session-based authentication with configurable secrets","size_bytes":3613},"routes.py":{"content":"import os\nimport uuid\nfrom datetime import datetime\nfrom flask import Blueprint, render_template, request, redirect, url_for, flash, jsonify, current_app\nfrom flask_login import login_user, logout_user, login_required, current_user\nfrom werkzeug.security import check_password_hash, generate_password_hash\nfrom werkzeug.utils import secure_filename\nfrom sqlalchemy import or_, desc\nfrom app import db, login_manager\nfrom models import User, FinancialMovement, UploadHistory\nfrom forms import LoginForm, UserForm, EditUserForm, FileUploadForm\nfrom file_processor import FinancialDataProcessor\nimport logging\n\nlogger = logging.getLogger(__name__)\n\n# User loader for Flask-Login\n@login_manager.user_loader\ndef load_user(user_id):\n    return User.query.get(int(user_id))\n\n# Blueprint definitions\nmain_bp = Blueprint('main', __name__)\nadmin_bp = Blueprint('admin', __name__)\nauth_bp = Blueprint('auth', __name__)\n\ndef admin_required(f):\n    \"\"\"Decorator to require admin access\"\"\"\n    from functools import wraps\n    @wraps(f)\n    @login_required\n    def decorated_function(*args, **kwargs):\n        if not current_user.is_admin:\n            flash('You need administrator privileges to access this page.', 'error')\n            return redirect(url_for('main.index'))\n        return f(*args, **kwargs)\n    return decorated_function\n\n# Main routes\n@main_bp.route('/')\ndef index():\n    \"\"\"Homepage - show recent movements\"\"\"\n    recent_movements = FinancialMovement.query.order_by(desc(FinancialMovement.uploaded_at)).limit(10).all()\n    return render_template('index.html', movements=recent_movements)\n\n@main_bp.route('/movements')\n@login_required\ndef movements():\n    \"\"\"List all movements with search, filters and pagination\"\"\"\n    page = request.args.get('page', 1, type=int)\n    search = request.args.get('search', '', type=str)\n    company_filter = request.args.get('company', '', type=str)\n    branch_filter = request.args.get('branch', '', type=str)\n    status_filter = request.args.get('status', '', type=str)\n    per_page = 25\n    \n    query = FinancialMovement.query\n    \n    # Apply search filter\n    if search:\n        query = query.filter(\n            or_(\n                FinancialMovement.internal_id.contains(search),\n                FinancialMovement.number.contains(search),\n                FinancialMovement.aux_customer_vendor_code.contains(search),\n                FinancialMovement.observation.contains(search)\n            )\n        )\n    \n    # Apply company filter\n    if company_filter:\n        query = query.filter(FinancialMovement.empresa_nome.contains(company_filter))\n    \n    # Apply branch filter  \n    if branch_filter:\n        query = query.filter(FinancialMovement.filial_nome.contains(branch_filter))\n    \n    # Apply status filter\n    if status_filter:\n        query = query.filter(FinancialMovement.status == status_filter)\n    \n    movements = query.order_by(desc(FinancialMovement.uploaded_at)).paginate(\n        page=page, per_page=per_page, error_out=False\n    )\n    \n    # Get unique values for filter dropdowns\n    companies = db.session.query(FinancialMovement.empresa_nome).distinct().filter(\n        FinancialMovement.empresa_nome.isnot(None)\n    ).order_by(FinancialMovement.empresa_nome).all()\n    companies = [c[0] for c in companies if c[0]]\n    \n    branches = db.session.query(FinancialMovement.filial_nome).distinct().filter(\n        FinancialMovement.filial_nome.isnot(None)\n    ).order_by(FinancialMovement.filial_nome).all()\n    branches = [b[0] for b in branches if b[0]]\n    \n    statuses = db.session.query(FinancialMovement.status).distinct().filter(\n        FinancialMovement.status.isnot(None)\n    ).order_by(FinancialMovement.status).all()\n    statuses = [s[0] for s in statuses if s[0]]\n    \n    return render_template('movements/list.html', \n                         movements=movements, \n                         search=search,\n                         company_filter=company_filter,\n                         branch_filter=branch_filter,\n                         status_filter=status_filter,\n                         companies=companies,\n                         branches=branches,\n                         statuses=statuses)\n\n@main_bp.route('/movements/<int:id>')\n@login_required\ndef movement_detail(id):\n    \"\"\"Show detailed view of a specific movement\"\"\"\n    movement = FinancialMovement.query.get_or_404(id)\n    return render_template('movements/detail.html', movement=movement)\n\n@main_bp.route('/api/movements/<int:id>/json/<field>')\n@login_required\ndef get_movement_json_field(id, field):\n    \"\"\"API endpoint to get formatted JSON field data\"\"\"\n    movement = FinancialMovement.query.get_or_404(id)\n    json_data = movement.get_json_field(field)\n    return jsonify(json_data)\n\n# Authentication routes\n@auth_bp.route('/login', methods=['GET', 'POST'])\ndef login():\n    \"\"\"User login\"\"\"\n    if current_user.is_authenticated:\n        return redirect(url_for('main.index'))\n    \n    form = LoginForm()\n    if form.validate_on_submit():\n        user = User.query.filter_by(username=form.username.data).first()\n        if user and user.password_hash and check_password_hash(user.password_hash, form.password.data):\n            login_user(user, remember=form.remember_me.data)\n            next_page = request.args.get('next')\n            if not next_page:\n                next_page = url_for('admin.dashboard') if user.is_admin else url_for('main.index')\n            flash(f'Welcome back, {user.username}!', 'success')\n            return redirect(next_page)\n        flash('Invalid username or password', 'error')\n    \n    return render_template('auth/login.html', form=form)\n\n@auth_bp.route('/logout')\n@login_required\ndef logout():\n    \"\"\"User logout\"\"\"\n    logout_user()\n    flash('You have been logged out.', 'info')\n    return redirect(url_for('main.index'))\n\n# Admin routes\n@admin_bp.route('/dashboard')\n@admin_required\ndef dashboard():\n    \"\"\"Admin dashboard with statistics\"\"\"\n    stats = {\n        'total_users': User.query.count(),\n        'total_movements': FinancialMovement.query.count(),\n        'total_uploads': UploadHistory.query.count(),\n        'recent_uploads': UploadHistory.query.order_by(desc(UploadHistory.uploaded_at)).limit(5).all()\n    }\n    return render_template('admin/dashboard.html', stats=stats)\n\n@admin_bp.route('/users')\n@admin_required\ndef users():\n    \"\"\"List all users\"\"\"\n    page = request.args.get('page', 1, type=int)\n    users = User.query.order_by(User.created_at.desc()).paginate(\n        page=page, per_page=20, error_out=False\n    )\n    return render_template('admin/users.html', users=users)\n\n@admin_bp.route('/users/new', methods=['GET', 'POST'])\n@admin_required\ndef create_user():\n    \"\"\"Create new user\"\"\"\n    form = UserForm()\n    if form.validate_on_submit():\n        password_data = form.password.data\n        if password_data:\n            user = User(\n                username=form.username.data,\n                email=form.email.data,\n                is_admin=form.is_admin.data,\n                password_hash=generate_password_hash(password_data)\n            )\n        else:\n            flash('Password is required', 'error')\n            return render_template('admin/users.html', form=form, action='create')\n        db.session.add(user)\n        db.session.commit()\n        flash(f'User {user.username} created successfully!', 'success')\n        return redirect(url_for('admin.users'))\n    \n    return render_template('admin/users.html', form=form, action='create')\n\n@admin_bp.route('/users/<int:id>/edit', methods=['GET', 'POST'])\n@admin_required\ndef edit_user(id):\n    \"\"\"Edit existing user\"\"\"\n    user = User.query.get_or_404(id)\n    form = EditUserForm(\n        original_username=user.username,\n        original_email=user.email,\n        obj=user\n    )\n    \n    if form.validate_on_submit():\n        user.username = form.username.data\n        user.email = form.email.data\n        user.is_admin = form.is_admin.data\n        db.session.commit()\n        flash(f'User {user.username} updated successfully!', 'success')\n        return redirect(url_for('admin.users'))\n    \n    return render_template('admin/users.html', form=form, user=user, action='edit')\n\n@admin_bp.route('/users/<int:id>/delete', methods=['POST'])\n@admin_required\ndef delete_user(id):\n    \"\"\"Delete user\"\"\"\n    user = User.query.get_or_404(id)\n    if user.id == current_user.id:\n        flash('You cannot delete your own account!', 'error')\n        return redirect(url_for('admin.users'))\n    \n    db.session.delete(user)\n    db.session.commit()\n    flash(f'User {user.username} deleted successfully!', 'success')\n    return redirect(url_for('admin.users'))\n\n@admin_bp.route('/upload', methods=['GET', 'POST'])\n@admin_required\ndef upload_file():\n    \"\"\"Upload Excel file with financial data\"\"\"\n    form = FileUploadForm()\n    if form.validate_on_submit():\n        file = form.file.data\n        filename = secure_filename(file.filename)\n        \n        # Generate unique filename\n        unique_filename = f\"{uuid.uuid4()}_{filename}\"\n        file_path = os.path.join(current_app.config['UPLOAD_FOLDER'], unique_filename)\n        \n        try:\n            # Save file\n            file.save(file_path)\n            \n            # Process file\n            processor = FinancialDataProcessor()\n            batch_id, success_count, error_count, errors, processed_data = processor.process_file(\n                file_path, current_user.id\n            )\n            \n            # Note: processor.process_file already saves data to database\n            # No need to save processed_data again\n            \n            # Clean up uploaded file\n            if os.path.exists(file_path):\n                os.remove(file_path)\n            \n            if error_count > 0:\n                flash(f'File processed with {success_count} successful records and {error_count} errors. Check upload history for details.', 'warning')\n            else:\n                flash(f'File uploaded successfully! {success_count} records processed.', 'success')\n            \n            return redirect(url_for('admin.upload_history'))\n            \n        except Exception as e:\n            # Clean up file if processing failed\n            if os.path.exists(file_path):\n                os.remove(file_path)\n            flash(f'Error processing file: {str(e)}', 'error')\n            logger.error(f\"Upload processing error: {str(e)}\")\n    \n    return render_template('admin/upload.html', form=form)\n\n@admin_bp.route('/upload-history')\n@admin_required\ndef upload_history():\n    \"\"\"View upload history\"\"\"\n    page = request.args.get('page', 1, type=int)\n    uploads = UploadHistory.query.order_by(desc(UploadHistory.uploaded_at)).paginate(\n        page=page, per_page=20, error_out=False\n    )\n    return render_template('admin/upload.html', uploads=uploads, show_history=True)\n\n@admin_bp.route('/upload-history/<int:id>')\n@admin_required\ndef upload_history_detail(id):\n    \"\"\"View detailed upload history\"\"\"\n    upload = UploadHistory.query.get_or_404(id)\n    movements = FinancialMovement.query.filter_by(upload_batch_id=upload.batch_id).all()\n    return render_template('admin/upload.html', upload=upload, movements=movements, show_detail=True)\n\n@admin_bp.route('/clear-movements', methods=['GET', 'POST'])\n@admin_required\ndef clear_movements():\n    \"\"\"Clear all financial movements from database\"\"\"\n    if request.method == 'POST':\n        confirmation = request.form.get('confirmation', '').strip().lower()\n        \n        if confirmation == 'delete all movements':\n            try:\n                # Delete all financial movements and their upload history\n                movement_count = FinancialMovement.query.count()\n                upload_count = UploadHistory.query.count()\n                \n                db.session.query(FinancialMovement).delete()\n                db.session.query(UploadHistory).delete()\n                db.session.commit()\n                \n                flash(f'Database cleared successfully! {movement_count} financial movements and {upload_count} upload records deleted.', 'success')\n                return redirect(url_for('admin.dashboard'))\n                \n            except Exception as e:\n                db.session.rollback()\n                flash(f'Error clearing database: {str(e)}', 'error')\n                \n        else:\n            flash('Invalid confirmation text. Type exactly \"delete all movements\" to confirm.', 'warning')\n    \n    # Count current records\n    movement_count = FinancialMovement.query.count()\n    upload_count = UploadHistory.query.count()\n    \n    return render_template('admin/clear_movements.html', \n                          movement_count=movement_count, \n                          upload_count=upload_count)\n","size_bytes":12731},"update_models.py":{"content":"#!/usr/bin/env python3\n\"\"\"\nScript to generate complete FinancialMovement model with all 113 fields\n\"\"\"\n\n# All 113 fields from the Excel file\nEXCEL_COLUMNS = [\n    'internalId', 'companyId', 'movementId', 'branchId', 'warehouseCode', 'destinyWarehouseCode', \n    'number', 'series', 'movementTypeCode', 'type', 'status', 'printed', 'documentPrinted', \n    'billPrinted', 'registerDate', 'exitDate', 'commercialRepresentativeCharge', 'grossValue', \n    'netValue', 'informedNetValue', 'otherValues', 'discountPercentage', 'expensePercentage', \n    'expenseValue', 'extraPercentage1', 'extraValue1', 'extraPercentage2', 'extraValue2', \n    'transportedProductNetWeight', 'transportedProductGrossWeight', 'classificationTable5Code', \n    'financialOptionalTable2Code', 'netValueCurrencyCode', 'date', 'hasGeneratedBill', \n    'auxCustomerVendorCode', 'auxCustomerVendorCompanyId', 'costCenterCode', 'salesman1Code', \n    'chargePercentage', 'salesman2ChargePercentage', 'salesman3ChargePercentage', \n    'salesman4ChargePercentage', 'userCode', 'destinyBranchId', 'lotGenerated', \n    'accountingExportStatus', 'deliveryDate', 'hasGeneratedWorkAccount', 'workAccountGenerated', \n    'lastEditTime', 'indicateObjectUse', 'bonumIntegrated', 'processedFlag', 'icmsDeductionValue', \n    'creationUser', 'creationDate', 'emailStatus', 'internalGrossValue', 'otherCompanyINSSBaseValue', \n    'conditionalDiscountValue', 'conditionalExpenseValue', 'affectStockOrder', \n    'commercialAutomationExported', 'aplicationIntegration', 'entryDate', 'extemporaneous', \n    'merchandiseValue', 'usesFinancialValueApportionment', 'conclusionFlag', 'paradigmaStatus', \n    'paradigmaAutoIntegrated', 'originalGrossValue', 'originalNetValue', 'originalOtherValues', \n    'operationId', 'scpBranchId', 'movementItems', 'payments', 'costCenterApportionments', \n    'departmentApportionments', 'taxes', 'fiscal', 'norm', 'cargoComponent', 'thirdPartyNF', \n    'safetyDevice', 'nfe', 'inputCTRC', 'outputCTRC', 'ctrc', 'transportData', \n    'documentAuthorization', 'judicialProcess', 'serviceOrder', 'relatedMovement', \n    'exportRelatedMovement', 'linkedMovement', 'cTe', 'eaiIntegration', \n    'electronicInvoiceFreeFields', 'customerVendorCode', 'paymentTermCode', 'observation', \n    'financialOptionalTable1Code', 'financialEntryMovementId', 'generatedEntryNumber', \n    'openEntryNumber', 'cashAccountCode', 'customerVendorCompanyId', 'fluxusGroupedFlag', \n    'cashAccountCompanyId', 'longHistory'\n]\n\ndef camel_to_snake(name):\n    \"\"\"Convert camelCase to snake_case\"\"\"\n    import re\n    s1 = re.sub('(.)([A-Z][a-z]+)', r'\\1_\\2', name)\n    return re.sub('([a-z0-9])([A-Z])', r'\\1_\\2', s1).lower()\n\n# JSON fields that should be stored as JSON text\nJSON_FIELDS = {\n    'movementItems', 'payments', 'costCenterApportionments', 'departmentApportionments', \n    'taxes', 'fiscal', 'norm', 'cargoComponent', 'thirdPartyNF', 'safetyDevice', \n    'nfe', 'inputCTRC', 'outputCTRC', 'ctrc', 'transportData', 'documentAuthorization', \n    'judicialProcess', 'serviceOrder', 'relatedMovement', 'exportRelatedMovement', \n    'linkedMovement', 'cTe', 'eaiIntegration', 'electronicInvoiceFreeFields'\n}\n\n# Generate column mapping\nprint(\"# Complete column mapping for file_processor.py:\")\nprint(\"COLUMN_MAPPING = {\")\nfor col in EXCEL_COLUMNS:\n    snake_name = camel_to_snake(col)\n    print(f\"    '{col}': '{snake_name}',\")\nprint(\"}\")\nprint()\n\n# Generate model fields\nprint(\"# Fields for FinancialMovement model:\")\nfor col in EXCEL_COLUMNS:\n    snake_name = camel_to_snake(col)\n    \n    if col in JSON_FIELDS:\n        print(f\"    {snake_name} = db.Column(db.Text, nullable=True)  # JSON field\")\n    elif col.endswith('Date') or col.endswith('Time'):\n        print(f\"    {snake_name} = db.Column(db.DateTime, nullable=True)\")\n    elif col.endswith('Id') or col.endswith('Code') or col.endswith('Number'):\n        print(f\"    {snake_name} = db.Column(db.String(50), nullable=True)\")\n    elif col.endswith('Value') or col.endswith('Percentage') or col.endswith('Charge') or col.endswith('Weight'):\n        print(f\"    {snake_name} = db.Column(db.Float, nullable=True)\")\n    elif col.endswith('Flag') or col.startswith('has') or col.startswith('is') or col in ['printed', 'documentPrinted', 'billPrinted', 'bonumIntegrated', 'processedFlag', 'usesFinancialValueApportionment', 'paradigmaAutoIntegrated']:\n        print(f\"    {snake_name} = db.Column(db.Boolean, nullable=True)\")\n    else:\n        print(f\"    {snake_name} = db.Column(db.String(255), nullable=True)\")\n\nprint()\nprint(\"# JSON field processing methods:\")\nprint(\"\"\"    def get_json_field(self, field_name):\n        \\\"\\\"\\\"Get a JSON field as a Python object\\\"\\\"\\\"\n        field_value = getattr(self, field_name, None)\n        if field_value:\n            try:\n                return json.loads(field_value) if isinstance(field_value, str) else field_value\n            except (json.JSONDecodeError, TypeError):\n                return []\n        return []\n\n    def set_json_field(self, field_name, value):\n        \\\"\\\"\\\"Set a JSON field from a Python object\\\"\\\"\\\"\n        if value:\n            json_value = json.dumps(value) if not isinstance(value, str) else value\n            setattr(self, field_name, json_value)\n        else:\n            setattr(self, field_name, None)\"\"\")","size_bytes":5295},"static/css/style.css":{"content":"/* Custom styles for Financial Data Management System */\n/* Following Bootstrap 5 dark theme variables and minimal custom overrides */\n\n:root {\n    --custom-border-radius: 0.375rem;\n    --custom-box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075);\n    --custom-transition: all 0.15s ease-in-out;\n}\n\n/* Global improvements */\nbody {\n    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;\n}\n\n/* Custom card enhancements */\n.card {\n    border: 1px solid var(--bs-border-color);\n    box-shadow: var(--custom-box-shadow);\n    transition: var(--custom-transition);\n}\n\n.card:hover {\n    box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);\n}\n\n/* Enhanced navigation */\n.navbar-brand {\n    font-weight: 600;\n}\n\n/* Table improvements */\n.table-responsive {\n    border-radius: var(--custom-border-radius);\n}\n\n.table th {\n    font-weight: 600;\n    border-top: none;\n}\n\n/* Code elements */\ncode {\n    color: var(--bs-info);\n    background-color: rgba(var(--bs-info-rgb), 0.1);\n    padding: 0.125rem 0.25rem;\n    border-radius: 0.25rem;\n    font-size: 0.875em;\n}\n\n/* Badge improvements */\n.badge {\n    font-weight: 500;\n}\n\n/* Button enhancements */\n.btn {\n    font-weight: 500;\n    transition: var(--custom-transition);\n}\n\n.btn-group .btn {\n    border-radius: 0;\n}\n\n.btn-group .btn:first-child {\n    border-top-left-radius: var(--custom-border-radius);\n    border-bottom-left-radius: var(--custom-border-radius);\n}\n\n.btn-group .btn:last-child {\n    border-top-right-radius: var(--custom-border-radius);\n    border-bottom-right-radius: var(--custom-border-radius);\n}\n\n/* Form improvements */\n.form-control:focus,\n.form-select:focus {\n    border-color: var(--bs-primary);\n    box-shadow: 0 0 0 0.2rem rgba(var(--bs-primary-rgb), 0.25);\n}\n\n.form-label {\n    font-weight: 500;\n    margin-bottom: 0.5rem;\n}\n\n/* Alert enhancements */\n.alert {\n    border: none;\n    border-radius: var(--custom-border-radius);\n}\n\n.alert .fas,\n.alert .far {\n    width: 1.25rem;\n    text-align: center;\n}\n\n/* Jumbotron style for hero sections */\n.jumbotron {\n    background: linear-gradient(135deg, var(--bs-primary), var(--bs-info));\n    border-radius: var(--custom-border-radius);\n}\n\n/* Pagination improvements */\n.pagination .page-link {\n    border-radius: var(--custom-border-radius);\n    margin: 0 0.125rem;\n    border: 1px solid var(--bs-border-color);\n}\n\n.pagination .page-item.active .page-link {\n    background-color: var(--bs-primary);\n    border-color: var(--bs-primary);\n}\n\n/* Statistics cards */\n.card.text-white .card-footer a:hover {\n    text-decoration: underline !important;\n}\n\n/* Accordion improvements */\n.accordion-button:not(.collapsed) {\n    background-color: rgba(var(--bs-primary-rgb), 0.1);\n    color: var(--bs-primary);\n}\n\n.accordion-button:focus {\n    box-shadow: 0 0 0 0.2rem rgba(var(--bs-primary-rgb), 0.25);\n}\n\n/* File upload area */\n.form-control[type=\"file\"] {\n    padding: 0.75rem;\n}\n\n/* DataTables integration */\n.dataTables_wrapper .dataTables_length select,\n.dataTables_wrapper .dataTables_filter input {\n    background-color: var(--bs-body-bg);\n    border: 1px solid var(--bs-border-color);\n    color: var(--bs-body-color);\n}\n\n.dataTables_wrapper .dataTables_paginate .paginate_button {\n    border: 1px solid var(--bs-border-color);\n    background-color: var(--bs-body-bg);\n    color: var(--bs-body-color) !important;\n}\n\n.dataTables_wrapper .dataTables_paginate .paginate_button:hover {\n    background-color: var(--bs-secondary-bg);\n    border-color: var(--bs-border-color);\n}\n\n.dataTables_wrapper .dataTables_paginate .paginate_button.current {\n    background-color: var(--bs-primary) !important;\n    border-color: var(--bs-primary) !important;\n    color: white !important;\n}\n\n/* Loading states */\n.loading {\n    opacity: 0.6;\n    pointer-events: none;\n}\n\n.spinner-border-sm {\n    width: 1rem;\n    height: 1rem;\n}\n\n/* Mobile responsiveness improvements */\n@media (max-width: 768px) {\n    .d-md-flex {\n        flex-direction: column;\n        gap: 0.5rem;\n    }\n    \n    .btn-group {\n        flex-direction: column;\n    }\n    \n    .btn-group .btn {\n        border-radius: var(--custom-border-radius) !important;\n        margin-bottom: 0.25rem;\n    }\n    \n    .card-body .row > div {\n        margin-bottom: 1rem;\n    }\n}\n\n/* Print styles */\n@media print {\n    .navbar,\n    .btn,\n    .pagination,\n    .card-footer,\n    .alert,\n    .badge {\n        display: none !important;\n    }\n    \n    .card {\n        border: 1px solid #000;\n        box-shadow: none;\n    }\n    \n    .table {\n        font-size: 0.8rem;\n    }\n}\n\n/* Custom utility classes */\n.text-truncate-2 {\n    display: -webkit-box;\n    -webkit-line-clamp: 2;\n    -webkit-box-orient: vertical;\n    overflow: hidden;\n}\n\n.border-start-primary {\n    border-left: 0.25rem solid var(--bs-primary) !important;\n}\n\n.border-start-success {\n    border-left: 0.25rem solid var(--bs-success) !important;\n}\n\n.border-start-warning {\n    border-left: 0.25rem solid var(--bs-warning) !important;\n}\n\n.border-start-danger {\n    border-left: 0.25rem solid var(--bs-danger) !important;\n}\n\n/* Animation utilities */\n@keyframes fadeIn {\n    from { opacity: 0; transform: translateY(10px); }\n    to { opacity: 1; transform: translateY(0); }\n}\n\n.fade-in {\n    animation: fadeIn 0.3s ease-in-out;\n}\n\n/* Focus improvements for accessibility */\n.btn:focus,\n.form-control:focus,\n.form-select:focus {\n    outline: 2px solid var(--bs-primary);\n    outline-offset: 2px;\n}\n\n/* High contrast mode support */\n@media (prefers-contrast: high) {\n    .card {\n        border-width: 2px;\n    }\n    \n    .btn {\n        border-width: 2px;\n    }\n}\n\n/* Reduced motion support */\n@media (prefers-reduced-motion: reduce) {\n    * {\n        animation-duration: 0.01ms !important;\n        animation-iteration-count: 1 !important;\n        transition-duration: 0.01ms !important;\n    }\n}\n","size_bytes":5794},"static/js/main.js":{"content":"// Main JavaScript file for Financial Data Management System\n// Enhanced functionality for better user experience\n\n$(document).ready(function() {\n    // Initialize all components\n    initializeComponents();\n    initializeDataTables();\n    initializeFormValidation();\n    initializeTooltips();\n    initializeFileUpload();\n    initializeSearchEnhancements();\n});\n\n// Initialize main components\nfunction initializeComponents() {\n    // Add fade-in animation to cards\n    $('.card').addClass('fade-in');\n    \n    // Enhanced dropdown behaviors\n    $('.dropdown-toggle').on('click', function(e) {\n        e.stopPropagation();\n    });\n    \n    // Auto-dismiss alerts after 5 seconds\n    setTimeout(function() {\n        $('.alert').alert('close');\n    }, 5000);\n    \n    // Smooth scrolling for anchor links\n    $('a[href^=\"#\"]').on('click', function(e) {\n        e.preventDefault();\n        const target = $($(this).attr('href'));\n        if (target.length) {\n            $('html, body').animate({\n                scrollTop: target.offset().top - 70\n            }, 500);\n        }\n    });\n}\n\n// Initialize DataTables with enhanced features\nfunction initializeDataTables() {\n    // Common DataTable configuration\n    const commonConfig = {\n        responsive: true,\n        pageLength: 25,\n        lengthMenu: [[10, 25, 50, 100, -1], [10, 25, 50, 100, \"All\"]],\n        language: {\n            search: \"Search records:\",\n            lengthMenu: \"Show _MENU_ records per page\",\n            info: \"Showing _START_ to _END_ of _TOTAL_ records\",\n            infoEmpty: \"No records available\",\n            infoFiltered: \"(filtered from _MAX_ total records)\",\n            paginate: {\n                first: \"First\",\n                last: \"Last\",\n                next: \"Next\",\n                previous: \"Previous\"\n            }\n        },\n        dom: '<\"row\"<\"col-sm-12 col-md-6\"l><\"col-sm-12 col-md-6\"f>>' +\n             '<\"row\"<\"col-sm-12\"tr>>' +\n             '<\"row\"<\"col-sm-12 col-md-5\"i><\"col-sm-12 col-md-7\"p>>',\n        initComplete: function() {\n            // Add Bootstrap styling to DataTables elements\n            $('.dataTables_length select').addClass('form-select form-select-sm');\n            $('.dataTables_filter input').addClass('form-control form-control-sm');\n        }\n    };\n    \n    // Initialize specific tables\n    if ($('#movementsTable').length) {\n        $('#movementsTable').DataTable({\n            ...commonConfig,\n            order: [[7, \"desc\"]], // Sort by uploaded date\n            columnDefs: [\n                { targets: [-1], orderable: false }, // Disable sorting on actions column\n                { targets: [4], type: 'currency' }, // Currency sorting for net value\n            ]\n        });\n    }\n    \n    if ($('#usersTable').length) {\n        $('#usersTable').DataTable({\n            ...commonConfig,\n            order: [[3, \"desc\"]], // Sort by created date\n            columnDefs: [\n                { targets: [-1], orderable: false }, // Disable sorting on actions column\n            ]\n        });\n    }\n    \n    if ($('#uploadsTable').length) {\n        $('#uploadsTable').DataTable({\n            ...commonConfig,\n            order: [[5, \"desc\"]], // Sort by upload date\n            columnDefs: [\n                { targets: [-1], orderable: false }, // Disable sorting on actions column\n            ]\n        });\n    }\n}\n\n// Initialize form validation\nfunction initializeFormValidation() {\n    // Real-time validation for forms\n    $('form').on('submit', function(e) {\n        const form = $(this);\n        let isValid = true;\n        \n        // Check required fields\n        form.find('[required]').each(function() {\n            const field = $(this);\n            if (!field.val().trim()) {\n                field.addClass('is-invalid');\n                isValid = false;\n            } else {\n                field.removeClass('is-invalid');\n            }\n        });\n        \n        // Email validation\n        form.find('input[type=\"email\"]').each(function() {\n            const email = $(this);\n            const emailPattern = /^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/;\n            if (email.val() && !emailPattern.test(email.val())) {\n                email.addClass('is-invalid');\n                isValid = false;\n            }\n        });\n        \n        // Password confirmation\n        const password = form.find('input[name=\"password\"]');\n        const password2 = form.find('input[name=\"password2\"]');\n        if (password.length && password2.length) {\n            if (password.val() !== password2.val()) {\n                password2.addClass('is-invalid');\n                isValid = false;\n            }\n        }\n        \n        if (!isValid) {\n            e.preventDefault();\n            showNotification('Please correct the highlighted fields', 'error');\n        }\n    });\n    \n    // Remove validation classes on input\n    $('input, select, textarea').on('input change', function() {\n        $(this).removeClass('is-invalid');\n    });\n}\n\n// Initialize tooltips\nfunction initializeTooltips() {\n    // Enable Bootstrap tooltips\n    const tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle=\"tooltip\"]'));\n    tooltipTriggerList.map(function(tooltipTriggerEl) {\n        return new bootstrap.Tooltip(tooltipTriggerEl);\n    });\n    \n    // Add tooltips to truncated text\n    $('.text-truncate, .text-truncate-2').each(function() {\n        const element = $(this);\n        if (element[0].scrollWidth > element[0].clientWidth) {\n            element.attr('data-bs-toggle', 'tooltip');\n            element.attr('title', element.text());\n            new bootstrap.Tooltip(element[0]);\n        }\n    });\n}\n\n// Initialize file upload enhancements\nfunction initializeFileUpload() {\n    $('input[type=\"file\"]').on('change', function() {\n        const file = this.files[0];\n        const fileInfo = $(this).siblings('.file-info');\n        \n        if (file) {\n            const size = (file.size / (1024 * 1024)).toFixed(2);\n            const info = `Selected: ${file.name} (${size} MB)`;\n            \n            if (fileInfo.length) {\n                fileInfo.text(info);\n            } else {\n                $(this).after(`<div class=\"file-info text-muted mt-1\">${info}</div>`);\n            }\n            \n            // Validate file size (50MB limit)\n            if (file.size > 50 * 1024 * 1024) {\n                showNotification('File size exceeds 50MB limit', 'error');\n                $(this).val('');\n                $(this).siblings('.file-info').remove();\n            }\n        } else {\n            fileInfo.remove();\n        }\n    });\n    \n    // Drag and drop enhancement\n    $('.form-control[type=\"file\"]').each(function() {\n        const fileInput = $(this);\n        const dropZone = $('<div class=\"drop-zone border-2 border-dashed p-4 text-center rounded mt-2\">' +\n                          '<i class=\"fas fa-cloud-upload-alt fa-2x text-muted\"></i><br>' +\n                          '<span class=\"text-muted\">Drag and drop files here or click to browse</span>' +\n                          '</div>');\n        \n        fileInput.after(dropZone);\n        fileInput.hide();\n        \n        dropZone.on('click', function() {\n            fileInput.click();\n        });\n        \n        dropZone.on('dragover', function(e) {\n            e.preventDefault();\n            $(this).addClass('border-primary bg-primary bg-opacity-10');\n        });\n        \n        dropZone.on('dragleave', function() {\n            $(this).removeClass('border-primary bg-primary bg-opacity-10');\n        });\n        \n        dropZone.on('drop', function(e) {\n            e.preventDefault();\n            $(this).removeClass('border-primary bg-primary bg-opacity-10');\n            \n            const files = e.originalEvent.dataTransfer.files;\n            if (files.length > 0) {\n                fileInput[0].files = files;\n                fileInput.trigger('change');\n            }\n        });\n    });\n}\n\n// Initialize search enhancements\nfunction initializeSearchEnhancements() {\n    // Auto-complete for search fields\n    $('input[name=\"search\"]').on('input', function() {\n        const query = $(this).val();\n        if (query.length >= 2) {\n            // Debounce search suggestions\n            clearTimeout(this.searchTimeout);\n            this.searchTimeout = setTimeout(() => {\n                // You can implement search suggestions here\n                console.log('Search suggestions for:', query);\n            }, 300);\n        }\n    });\n    \n    // Advanced search toggle\n    $('.advanced-search-toggle').on('click', function() {\n        $('.advanced-search-panel').slideToggle();\n    });\n    \n    // Search history (localStorage)\n    const searchHistory = JSON.parse(localStorage.getItem('searchHistory') || '[]');\n    \n    $('form').on('submit', function() {\n        const searchInput = $(this).find('input[name=\"search\"]');\n        if (searchInput.length && searchInput.val().trim()) {\n            const query = searchInput.val().trim();\n            if (!searchHistory.includes(query)) {\n                searchHistory.unshift(query);\n                searchHistory.splice(10); // Keep only last 10 searches\n                localStorage.setItem('searchHistory', JSON.stringify(searchHistory));\n            }\n        }\n    });\n}\n\n// Utility functions\nfunction showNotification(message, type = 'info') {\n    const alertClass = type === 'error' ? 'alert-danger' : `alert-${type}`;\n    const icon = type === 'error' ? 'fas fa-exclamation-triangle' : \n                 type === 'success' ? 'fas fa-check-circle' : 'fas fa-info-circle';\n    \n    const alert = $(`\n        <div class=\"alert ${alertClass} alert-dismissible fade show\" role=\"alert\">\n            <i class=\"${icon} me-2\"></i>\n            ${message}\n            <button type=\"button\" class=\"btn-close\" data-bs-dismiss=\"alert\"></button>\n        </div>\n    `);\n    \n    // Insert at top of main container\n    $('main.container').prepend(alert);\n    \n    // Auto-dismiss after 5 seconds\n    setTimeout(() => {\n        alert.alert('close');\n    }, 5000);\n}\n\nfunction formatCurrency(amount, currency = 'R$') {\n    return `${currency} ${parseFloat(amount).toFixed(2)}`;\n}\n\nfunction formatDate(dateString) {\n    if (!dateString) return '-';\n    const date = new Date(dateString);\n    return date.toLocaleDateString('pt-BR');\n}\n\nfunction formatDateTime(dateString) {\n    if (!dateString) return '-';\n    const date = new Date(dateString);\n    return date.toLocaleString('pt-BR');\n}\n\n// Export functionality\nfunction exportTableToCSV(tableId, filename = 'export.csv') {\n    const table = document.getElementById(tableId);\n    if (!table) return;\n    \n    const rows = Array.from(table.querySelectorAll('tr'));\n    const csv = rows.map(row => {\n        const cells = Array.from(row.querySelectorAll('td, th'));\n        return cells.slice(0, -1).map(cell => { // Exclude actions column\n            let text = cell.textContent.trim();\n            // Handle commas and quotes in CSV\n            if (text.includes(',') || text.includes('\"')) {\n                text = '\"' + text.replace(/\"/g, '\"\"') + '\"';\n            }\n            return text;\n        }).join(',');\n    }).join('\\n');\n    \n    // Download CSV\n    const blob = new Blob([csv], { type: 'text/csv' });\n    const url = window.URL.createObjectURL(blob);\n    const a = document.createElement('a');\n    a.href = url;\n    a.download = filename;\n    a.style.display = 'none';\n    document.body.appendChild(a);\n    a.click();\n    document.body.removeChild(a);\n    window.URL.revokeObjectURL(url);\n}\n\n// JSON data formatting for display\nfunction formatJSONField(data, fieldName) {\n    if (!data || data.length === 0) {\n        return '<span class=\"text-muted\">No data</span>';\n    }\n    \n    if (Array.isArray(data)) {\n        return `<span class=\"badge bg-info\">${data.length} items</span>`;\n    }\n    \n    return '<span class=\"badge bg-secondary\">Object</span>';\n}\n\n// Performance monitoring\nfunction measurePerformance(name, fn) {\n    const start = performance.now();\n    const result = fn();\n    const end = performance.now();\n    console.log(`${name} took ${end - start} milliseconds`);\n    return result;\n}\n\n// Error handling\nwindow.addEventListener('error', function(e) {\n    console.error('JavaScript error:', e.error);\n    showNotification('An unexpected error occurred. Please refresh the page.', 'error');\n});\n\n// Keyboard shortcuts\ndocument.addEventListener('keydown', function(e) {\n    // Ctrl+/ or Cmd+/ for search focus\n    if ((e.ctrlKey || e.metaKey) && e.key === '/') {\n        e.preventDefault();\n        const searchInput = document.querySelector('input[name=\"search\"]');\n        if (searchInput) {\n            searchInput.focus();\n        }\n    }\n    \n    // Escape to clear search\n    if (e.key === 'Escape') {\n        const searchInput = document.querySelector('input[name=\"search\"]:focus');\n        if (searchInput) {\n            searchInput.value = '';\n            searchInput.blur();\n        }\n    }\n});\n\n// Mobile enhancements\nif (window.innerWidth <= 768) {\n    // Mobile-specific enhancements\n    $(document).ready(function() {\n        // Collapse long text on mobile\n        $('.table td').each(function() {\n            const text = $(this).text();\n            if (text.length > 30) {\n                $(this).attr('title', text);\n                $(this).text(text.substring(0, 30) + '...');\n            }\n        });\n        \n        // Mobile navigation improvements\n        $('.navbar-toggler').on('click', function() {\n            $('body').toggleClass('nav-open');\n        });\n    });\n}\n\n// Progressive Web App features\nif ('serviceWorker' in navigator) {\n    window.addEventListener('load', function() {\n        // Service worker registration would go here\n        console.log('Service Worker support detected');\n    });\n}\n","size_bytes":13803}}}

----- FILE: attached_assets/Pasted--2025-08-18-14-39-30-0000-1014-INFO-Booting-worker-with-pid-1014-INFO-file-processor-Read-Exc-1755528124031_1755528124031.txt  (sha1:70c05f5acbba, bytes:3072) -----
[2025-08-18 14:39:30 +0000] [1014] [INFO] Booting worker with pid: 1014
INFO:file_processor:Read Excel file with 50 rows
ERROR:app:Exception on /movements/50 [GET]
Traceback (most recent call last):
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 1511, in wsgi_app
    response = self.full_dispatch_request()
               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 919, in full_dispatch_request
    rv = self.handle_user_exception(e)
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 917, in full_dispatch_request
    rv = self.dispatch_request()
         ^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 902, in dispatch_request
    return self.ensure_sync(self.view_functions[rule.endpoint])(**view_args)  # type: ignore[no-any-return]
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask_login/utils.py", line 290, in decorated_view
    return current_app.ensure_sync(func)(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/routes.py", line 77, in movement_detail
    return render_template('movements/detail.html', movement=movement)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/templating.py", line 150, in render_template
    return _render(app, template, context)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/templating.py", line 131, in _render
    rv = template.render(context)
         ^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/environment.py", line 1295, in render
    self.environment.handle_exception()
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/environment.py", line 942, in handle_exception
    raise rewrite_traceback_stack(source=source)
  File "/home/runner/workspace/templates/movements/detail.html", line 1, in top-level template code
    {% extends "base.html" %}
  File "/home/runner/workspace/templates/base.html", line 126, in top-level template code
    {% block content %}{% endblock %}
    ^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/templates/movements/detail.html", line 248, in block 'content'
    Item {{ loop.index }} - Product ID: {{ item.get('productId', 'N/A') }}
    ^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/utils.py", line 92, in from_obj
    if hasattr(obj, "jinja_pass_arg"):
       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
jinja2.exceptions.UndefinedError: 'str object' has no attribute 'get'
[2025-08-18 14:41:44 +0000] [1003] [INFO] Handling signal: winch

----- FILE: attached_assets/Pasted--Empresa-Code-Empresa-Nome-Filial-Code-Filial-Nome-Filial-Nome-Descricao-internalId-companyId-moveme-1755539692719_1755539692719.txt  (sha1:76af6f6cefeb, bytes:2078) -----

Empresa_Code	Empresa_Nome	Filial_Code	Filial_Nome	Filial_Nome_Descricao	internalId	companyId	movementId	branchId	warehouseCode	destinyWarehouseCode	number	series	movementTypeCode	type	status	printed	documentPrinted	billPrinted	registerDate	exitDate	commercialRepresentativeCharge	grossValue	netValue	informedNetValue	otherValues	discountPercentage	expensePercentage	expenseValue	extraPercentage1	extraValue1	extraPercentage2	extraValue2	transportedProductNetWeight	transportedProductGrossWeight	classificationTable5Code	financialOptionalTable2Code	netValueCurrencyCode	date	hasGeneratedBill	auxCustomerVendorCode	auxCustomerVendorCompanyId	costCenterCode	salesman1Code	chargePercentage	salesman2ChargePercentage	salesman3ChargePercentage	salesman4ChargePercentage	userCode	destinyBranchId	lotGenerated	accountingExportStatus	deliveryDate	hasGeneratedWorkAccount	workAccountGenerated	lastEditTime	indicateObjectUse	bonumIntegrated	processedFlag	icmsDeductionValue	creationUser	creationDate	emailStatus	internalGrossValue	otherCompanyINSSBaseValue	conditionalDiscountValue	conditionalExpenseValue	affectStockOrder	commercialAutomationExported	aplicationIntegration	entryDate	extemporaneous	merchandiseValue	usesFinancialValueApportionment	conclusionFlag	paradigmaStatus	paradigmaAutoIntegrated	originalGrossValue	originalNetValue	originalOtherValues	operationId	scpBranchId	movementItems	payments	costCenterApportionments	departmentApportionments	taxes	fiscal	norm	cargoComponent	thirdPartyNF	safetyDevice	nfe	inputCTRC	outputCTRC	ctrc	transportData	documentAuthorization	judicialProcess	serviceOrder	relatedMovement	exportRelatedMovement	linkedMovement	cTe	eaiIntegration	electronicInvoiceFreeFields	customerVendorCode	paymentTermCode	observation	financialOptionalTable1Code	financialEntryMovementId	generatedEntryNumber	openEntryNumber	cashAccountCode	customerVendorCompanyId	fluxusGroupedFlag	cashAccountCompanyId	longHistory
  Essas sao as colunas que estou subindo. E toda base tem os registros em json. 

Consegue verificar o que pode ta acontecendo com os campos json?

----- FILE: attached_assets/Pasted--Empresa-Nome-Filial-Code-Filial-Nome-Filial-Nome-Descricao-internalId-companyId-movementId-branchI-1756245969666_1756245969666.txt  (sha1:0bb5aaddaf2b, bytes:5641) -----
\	Empresa_Nome	Filial_Code	Filial_Nome	Filial_Nome_Descricao	internalId	companyId	movementId	branchId	warehouseCode	destinyWarehouseCode	customerVendorCode	number	series	movementTypeCode	type	status	printed	documentPrinted	billPrinted	registerDate	exitDate	commercialRepresentativeCharge	paymentTermCode	grossValue	netValue	informedNetValue	otherValues	observation	discountPercentage	expensePercentage	expenseValue	extraPercentage1	extraValue1	extraPercentage2	extraValue2	transportedProductNetWeight	transportedProductGrossWeight	financialOptionalTable1Code	financialEntryMovementId	netValueCurrencyCode	date	generatedEntryNumber	hasGeneratedBill	openEntryNumber	auxCustomerVendorCode	auxCustomerVendorCompanyId	cashAccountCode	chargePercentage	salesman2ChargePercentage	salesman3ChargePercentage	salesman4ChargePercentage	customerVendorCompanyId	userCode	destinyBranchId	fluxusGroupedFlag	cashAccountCompanyId	lotGenerated	accountingExportStatus	hasGeneratedWorkAccount	workAccountGenerated	lastEditTime	indicateObjectUse	bonumIntegrated	processedFlag	icmsDeductionValue	creationUser	creationDate	emailStatus	internalGrossValue	otherCompanyINSSBaseValue	conditionalDiscountValue	conditionalExpenseValue	affectStockOrder	commercialAutomationExported	aplicationIntegration	entryDate	extemporaneous	merchandiseValue	usesFinancialValueApportionment	conclusionFlag	paradigmaStatus	paradigmaAutoIntegrated	originalGrossValue	originalNetValue	originalOtherValues	operationId	scpBranchId	movementItems	payments	costCenterApportionments	departmentApportionments	taxes	fiscal	norm	cargoComponent	thirdPartyNF	safetyDevice	nfe	inputCTRC	outputCTRC	ctrc	transportData	documentAuthorization	judicialProcess	serviceOrder	relatedMovement	exportRelatedMovement	linkedMovement	cTe	eaiIntegration	electronicInvoiceFreeFields
4	SA CAVALCANTE COMESTIVEIS LTDA	17	16 - BOB´S LARANJEIRAS	16 - SA CAVALCANTE COMESTIVEIS LTDA	1|138	1	138	1	01	01	F00227	001408	U	1.2.02	P	F	FALSE	FALSE	FALSE	2003-08-06T00:00:00-03:00	2003-08-11T00:00:00-03:00	0	30	232	232	0	232	PROGRAMA SOL	0	0	0	0	0	0	0	0	0	02	138	R$	2003-08-06T00:00:00-03:00	1	FALSE	1	F00227	0	02	0	0	0	0	0	cmufumba	1	0	1	FALSE	0	FALSE	FALSE	2003-08-11T09:08:41-03:00	0	FALSE	FALSE	0	cmufumba	2003-08-11T00:00:00-03:00	FALSE	232	0	0	0	138	0	T	2003-08-11T00:00:00-03:00	0	0	FALSE	0	N	FALSE	0	0	0	0	0	[{'companyId': 1, 'movementId': 138, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 90, 'quantity': 4.0, 'unitPrice': 58.0, 'tablePrice': 0.0, 'discountValue': 0.0, 'registerDate': '2003-08-06T00:00:00-03:00', 'measureUnitCode': 'GL', 'receivableQuantity': 4.0, 'partialBillingReceivedValue': 0.0, 'completedValue': 0.0, 'stockEffectFlag': 1, 'unitValue': 58.0, 'financialValue': 232.0, 'ordinationTaxAliquot': 0.0, 'originalQuantity': 4.0, 'flag': 0, 'blockObject': 0, 'rebillingFlag': 0, 'measureUnitConversionRate': 0.0, 'totalValue': 232.0, 'branchId': 1, 'sortedQuantity': 0.0, 'commercialRepresentativeChargePercentage': 0.0, 'fiscalBookkeepingValue': 0.0, 'orderFinancialValue': 0.0, 'ctrcFreightValue': 0.0, 'optionalFormulaValue1': 0.0, 'optionalFormulaValue2': 0.0, 'editedPrice': False, 'unitaryOccupiedVolumeNumber': 1, 'editedTotalPrice': 0, 'conditionalDiscountValue': 0.0, 'conditionalExpenseValue': 0.0, 'budgetDate': '2014-12-04T00:00:00-03:00', 'freightApportionment': 0.0, 'insuranceApportionment': 0.0, 'discountApportionment': 0.0, 'expenseApportionment': 0.0, 'extraValue1Apportionment': 0.0, 'extraValue2Apportionment': 0.0, 'ctrcFreightApportionment': 0.0, 'materialDeductionApportionment': 0.0, 'subcontractingDeductionApportionment': 0.0, 'otherDeductionApportionment': 0.0, 'budgetUnitPrice': 0.0, 'nFeServiceValue': 0.0, 'warehouseCode': '01', 'heritageValue': 0.0, 'heritageBaseDepreciationValue': 0.0, 'netValue': 232.0, 'grossValue': 0.0, 'originalGrossValue': 0.0, 'costCenterDepartmentApportionmentValue': 0.0, 'replenishmentCost': 0.0, 'bReplenishmentCost': 0.0, 'thirdPartyFinancialValue': 0.0, 'totalQuantity': 4.0, 'isSubstituteProduct': 0, 'unitPriceSelectionType': 0, 'financialValueApportionment': 0.0, 'completedQuantity': 0.0, 'aplicationIntegration': 'T', 'productFantasyName': 'GALVEST BRANCO 3600 ML', 'productCode': '01.01.0007', 'productReducedCode': '90', 'manufacturerProductNumber': '01.01.0007', 'manufacturerCode': '00001', 'complementaryFields': {}, 'costCenterApportionments': [], 'departmentApportionments': [], 'taxes': [{'companyId': 1, 'movementId': 138, 'movementItemSequentialId': 1, 'taxId': 'ICMS', 'calculationBasis': 232.0, 'aliquot': 17.0, 'value': 39.44, 'calculatedCalculationBasis': 232.0, 'edited': 0, 'fullBase': 0.0}, {'companyId': 1, 'movementId': 138, 'movementItemSequentialId': 1, 'taxId': 'IPI', 'calculationBasis': 232.0, 'aliquot': 0.0, 'value': 0.0, 'calculatedCalculationBasis': 232.0, 'edited': 0, 'fullBase': 0.0}], 'itemLots': [], 'itemGrids': [], 'itemSerialNumber': [], 'itemHeritage': [], 'siscoServFitting': [], 'fiscal': [], 'reserves': [], 'relatedItem': [], 'exportRelatedItem': [], 'linkedItem': [], 'exportMemo': [], 'judicialProcess': [], 'electronicInvoiceFreeFields': []}]	[]	[{'companyId': 1, 'apportionmentId': 7, 'movementId': 138, 'costCenterCode': '1.4.008', 'value': 232.0, 'costCenterName': 'EXPANSÃO SPC'}]	[{'companyId': 1, 'movementId': 138, 'branchId': 1, 'departmentCode': '10', 'value': 232.0, 'departmentName': 'OBRAS'}]	[{'companyId': 1, 'movementId': 138, 'movementItemSequentialId': 0, 'taxId': 'PIS', 'calculationBasis': 232.0, 'aliquot': 1.65, 'value': 3.83, 'calculatedCalculationBasis': 232.0, 'edited': 0, 'fullBase': 0.0}]	[]	[]	[]	[]	[]	[]	[]	[]	[]	[]	[]	[]	[]	[]	[]	[]	[]	[]	[]


----- FILE: attached_assets/Pasted--Eu-preciso-que-voc-me-ajude-a-criar-uma-aplica-o-em-flask-aonde-vamos-ter-uma-parte-de-admin--1755527301524_1755527301525.txt  (sha1:abfd0809e324, bytes:13253) -----





Eu preciso que você me ajude a criar uma aplicação em flask aonde vamos ter uma parte de admin, aonde o admin vai poder cadastrar novos usuários. 

Alem disso, o Admin vai poder subir documentos em lotes. Esse documento será um xlsx estruturado com essas colunas na sequencia. Vou te mandar o nome da coluna e o formato que vai vir o dado:   internalId - 
1|91
1|122
1|123

companyId - 
1
1
1
movementId -
91
122
123
branchId -
1
1
1
warehouseCode - 
01
01
01
destinyWarehouseCode -
01
01
01
number -
000002
128652
128635
series - 
OR
U
U
movementTypeCode -
5.1.02
1.2.02
1.2.02
type -
S
P
P
status -
N
F
F
printed -
FALSO
FALSO
FALSO
documentPrinted -
FALSO
FALSO
FALSO
billPrinted -
FALSO
FALSO
FALSO
registerDate -

2003-08-01T00:00:00-03:00
2003-08-04T00:00:00-03:00
2003-08-04T00:00:00-03:00
exitDate-
2003-08-01T00:00:00-03:00
2003-08-07T00:00:00-03:00
2003-08-07T00:00:00-03:00
commercialRepresentativeCharge -
0
0
0
grossValue -
500
570
1710
netValue - 
500
627
1881
informedNetValue -
0
0
0
otherValues - 
500
570
1710
discountPercentage -
0
0
0
expensePercentage -
0
0
0
expenseValue -
0
0
0
extraPercentage1 -
0
0
0
extraValue1 -
0
0
0
extraPercentage2 -
0
0
0
extraValue2 -
0
0
0
transportedProductNetWeight -
0
0
0
transportedProductGrossWeight -
0
0
0
classificationTable5Code -
12305016


financialOptionalTable2Code -
1.2.3.05.016


netValueCurrencyCode -
R$
R$
R$
date - 
2003-08-01T00:00:00-03:00
2003-08-04T00:00:00-03:00
2003-08-04T00:00:00-03:00
hasGeneratedBill -
FALSO
FALSO
FALSO
auxCustomerVendorCode -
CXXXXXXXXXX
F00007
F00007
auxCustomerVendorCompanyId -
0
0
0
costCenterCode -
1.2.020


salesman1Code -
salesman1Code
00001


chargePercentage -
0
0
0
salesman2ChargePercentage -
0
0
0
salesman3ChargePercentage -
0
0
0
salesman4ChargePercentage - 
0
0
0
userCode -
mestre
mestre
mestre
destinyBranchId -
1
1
1
lotGenerated -
FALSO
FALSO
FALSO
accountingExportStatus -
0
0
0
deliveryDate -
2003-08-01T00:00:00-03:00


hasGeneratedWorkAccount -
FALSO
FALSO
FALSO
workAccountGenerated -
FALSO
FALSO
FALSO
lastEditTime -
2003-08-01T09:19:24-03:00
2003-08-08T15:34:41-03:00
2003-08-08T15:33:33-03:00
indicateObjectUse -
0
0
0
bonumIntegrated -
FALSO
FALSO
FALSO
processedFlag -
FALSO
FALSO
FALSO
icmsDeductionValue -
0
0
0
creationUser -
mestre
mestre
mestre
creationDate -
2003-08-01T00:00:00-03:00
2003-08-07T00:00:00-03:00
2003-08-07T00:00:00-03:00
emailStatus -
FALSO
FALSO
FALSO
internalGrossValue -
500
570
1710
otherCompanyINSSBaseValue -
0
0
0
conditionalDiscountValue -
0
0
0
conditionalExpenseValue -
0
0
0
affectStockOrder -
91
122
123
commercialAutomationExported -
0
0
0
aplicationIntegration -
T
T
T
entryDate -
2003-08-01T00:00:00-03:00
2003-08-07T00:00:00-03:00
2003-08-07T00:00:00-03:00
extemporaneous -
0
0
0
merchandiseValue -
0
0
0
usesFinancialValueApportionment -
FALSO
FALSO
FALSO
conclusionFlag -
0
0
0
paradigmaStatus -
N
N
N
paradigmaAutoIntegrated -
FALSO
FALSO
FALSO
originalGrossValue -
0
0
0
originalNetValue -
0
0
0
originalOtherValues - 
0
0
0
operationId - 
0
0
0
scpBranchId -
0
0
0
movementItems - 

[{'companyId': 1, 'movementId': 91, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 812, 'quantity': 1.0, 'unitPrice': 500.0, 'tablePrice': 0.0, 'discountValue': 0.0, 'registerDate': '2003-08-01T00:00:00-03:00', 'classificationTable5Code': '12305016', 'financialOptionalTable2Code': '1.2.3.05.016', 'measureUnitCode': 'vb', 'receivableQuantity': 1.0, 'deliveryDate': '2003-08-01T00:00:00-03:00', 'partialBillingReceivedValue': 0.0, 'completedValue': 0.0, 'unitValue': 0.0, 'financialValue': 0.0, 'ordinationTaxAliquot': 0.0, 'originalQuantity': 1.0, 'flag': 0, 'blockObject': 0, 'rebillingFlag': 0, 'measureUnitConversionRate': 0.0, 'totalValue': 500.0, 'branchId': 1, 'sortedQuantity': 0.0, 'commercialRepresentativeChargePercentage': 0.0, 'fiscalBookkeepingValue': 0.0, 'orderFinancialValue': 0.0, 'ctrcFreightValue': 0.0, 'optionalFormulaValue1': 0.0, 'optionalFormulaValue2': 0.0, 'editedPrice': False, 'unitaryOccupiedVolumeNumber': 1, 'editedTotalPrice': 0, 'conditionalDiscountValue': 0.0, 'conditionalExpenseValue': 0.0, 'budgetDate': '2014-12-04T00:00:00-03:00', 'freightApportionment': 0.0, 'insuranceApportionment': 0.0, 'discountApportionment': 0.0, 'expenseApportionment': 0.0, 'extraValue1Apportionment': 0.0, 'extraValue2Apportionment': 0.0, 'ctrcFreightApportionment': 0.0, 'materialDeductionApportionment': 0.0, 'subcontractingDeductionApportionment': 0.0, 'otherDeductionApportionment': 0.0, 'budgetUnitPrice': 0.0, 'nFeServiceValue': 0.0, 'warehouseCode': '01', 'heritageValue': 0.0, 'heritageBaseDepreciationValue': 0.0, 'netValue': 500.0, 'grossValue': 0.0, 'originalGrossValue': 0.0, 'costCenterDepartmentApportionmentValue': 0.0, 'replenishmentCost': 0.0, 'bReplenishmentCost': 0.0, 'thirdPartyFinancialValue': 0.0, 'totalQuantity': 1.0, 'isSubstituteProduct': 0, 'unitPriceSelectionType': 0, 'financialValueApportionment': 0.0, 'completedQuantity': 0.0, 'aplicationIntegration': 'T', 'productFantasyName': 'SERVICOS EM GERAL', 'productCode': '99.01.0001', 'productReducedCode': '812', 'manufacturerProductNumber': '99.01.0000', 'manufacturerCode': '00001', 'complementaryFields': {}, 'costCenterApportionments': [], 'departmentApportionments': [], 'taxes': [], 'itemLots': [], 'itemGrids': [], 'itemSerialNumber': [], 'itemHeritage': [], 'siscoServFitting': [], 'fiscal': [], 'reserves': [], 'relatedItem': [], 'exportRelatedItem': [], 'linkedItem': [], 'exportMemo': [], 'judicialProcess': [], 'electronicInvoiceFreeFields': []}]
[{'companyId': 1, 'movementId': 122, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 146, 'quantity': 100.0, 'unitPrice': 5.7, 'tablePrice': 0.0, 'discountValue': 0.0, 'registerDate': '2003-08-04T00:00:00-03:00', 'measureUnitCode': 'SC', 'receivableQuantity': 100.0, 'partialBillingReceivedValue': 0.0, 'completedValue': 0.0, 'stockEffectFlag': 1, 'unitValue': 6.27, 'financialValue': 627.0, 'ordinationTaxAliquot': 0.0, 'originalQuantity': 100.0, 'flag': 0, 'blockObject': 0, 'rebillingFlag': 0, 'measureUnitConversionRate': 0.0, 'totalValue': 570.0, 'branchId': 1, 'sortedQuantity': 0.0, 'commercialRepresentativeChargePercentage': 0.0, 'fiscalBookkeepingValue': 0.0, 'orderFinancialValue': 0.0, 'ctrcFreightValue': 0.0, 'optionalFormulaValue1': 0.0, 'optionalFormulaValue2': 0.0, 'editedPrice': False, 'unitaryOccupiedVolumeNumber': 1, 'editedTotalPrice': 0, 'conditionalDiscountValue': 0.0, 'conditionalExpenseValue': 0.0, 'budgetDate': '2014-12-04T00:00:00-03:00', 'freightApportionment': 0.0, 'insuranceApportionment': 0.0, 'discountApportionment': 0.0, 'expenseApportionment': 0.0, 'extraValue1Apportionment': 0.0, 'extraValue2Apportionment': 0.0, 'ctrcFreightApportionment': 0.0, 'materialDeductionApportionment': 0.0, 'subcontractingDeductionApportionment': 0.0, 'otherDeductionApportionment': 0.0, 'budgetUnitPrice': 0.0, 'nFeServiceValue': 0.0, 'warehouseCode': '01', 'heritageValue': 0.0, 'heritageBaseDepreciationValue': 0.0, 'netValue': 570.0, 'grossValue': 0.0, 'originalGrossValue': 0.0, 'costCenterDepartmentApportionmentValue': 0.0, 'replenishmentCost': 0.0, 'bReplenishmentCost': 0.0, 'thirdPartyFinancialValue': 0.0, 'totalQuantity': 100.0, 'isSubstituteProduct': 0, 'unitPriceSelectionType': 0, 'financialValueApportionment': 0.0, 'completedQuantity': 0.0, 'aplicationIntegration': 'T', 'productFantasyName': 'ARGAMASSA TRADICIONAL', 'productCode': '01.14.0002', 'productReducedCode': '146', 'manufacturerProductNumber': '01.14.0002', 'manufacturerCode': '00001', 'complementaryFields': {}, 'costCenterApportionments': [], 'departmentApportionments': [], 'taxes': [{'companyId': 1, 'movementId': 122, 'movementItemSequentialId': 1, 'taxId': 'ICMS', 'calculationBasis': 627.0, 'aliquot': 0.0, 'value': 0.0, 'calculatedCalculationBasis': 627.0, 'edited': 0, 'fullBase': 0.0}, {'companyId': 1, 'movementId': 122, 'movementItemSequentialId': 1, 'taxId': 'IPI', 'calculationBasis': 570.0, 'aliquot': 10.0, 'value': 57.0, 'calculatedCalculationBasis': 570.0, 'edited': 0, 'fullBase': 0.0}], 'itemLots': [], 'itemGrids': [], 'itemSerialNumber': [], 'itemHeritage': [], 'siscoServFitting': [], 'fiscal': [], 'reserves': [], 'relatedItem': [], 'exportRelatedItem': [], 'linkedItem': [], 'exportMemo': [], 'judicialProcess': [], 'electronicInvoiceFreeFields': []}]
[{'companyId': 1, 'movementId': 123, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 146, 'quantity': 300.0, 'unitPrice': 5.7, 'tablePrice': 0.0, 'discountValue': 0.0, 'registerDate': '2003-08-04T00:00:00-03:00', 'measureUnitCode': 'SC', 'receivableQuantity': 300.0, 'partialBillingReceivedValue': 0.0, 'completedValue': 0.0, 'stockEffectFlag': 1, 'unitValue': 6.27, 'financialValue': 1881.0, 'ordinationTaxAliquot': 0.0, 'originalQuantity': 300.0, 'flag': 0, 'blockObject': 0, 'rebillingFlag': 0, 'measureUnitConversionRate': 0.0, 'totalValue': 1710.0, 'branchId': 1, 'sortedQuantity': 0.0, 'commercialRepresentativeChargePercentage': 0.0, 'fiscalBookkeepingValue': 0.0, 'orderFinancialValue': 0.0, 'ctrcFreightValue': 0.0, 'optionalFormulaValue1': 0.0, 'optionalFormulaValue2': 0.0, 'editedPrice': False, 'unitaryOccupiedVolumeNumber': 1, 'editedTotalPrice': 0, 'conditionalDiscountValue': 0.0, 'conditionalExpenseValue': 0.0, 'budgetDate': '2014-12-04T00:00:00-03:00', 'freightApportionment': 0.0, 'insuranceApportionment': 0.0, 'discountApportionment': 0.0, 'expenseApportionment': 0.0, 'extraValue1Apportionment': 0.0, 'extraValue2Apportionment': 0.0, 'ctrcFreightApportionment': 0.0, 'materialDeductionApportionment': 0.0, 'subcontractingDeductionApportionment': 0.0, 'otherDeductionApportionment': 0.0, 'budgetUnitPrice': 0.0, 'nFeServiceValue': 0.0, 'warehouseCode': '01', 'heritageValue': 0.0, 'heritageBaseDepreciationValue': 0.0, 'netValue': 1710.0, 'grossValue': 0.0, 'originalGrossValue': 0.0, 'costCenterDepartmentApportionmentValue': 0.0, 'replenishmentCost': 0.0, 'bReplenishmentCost': 0.0, 'thirdPartyFinancialValue': 0.0, 'totalQuantity': 300.0, 'isSubstituteProduct': 0, 'unitPriceSelectionType': 0, 'financialValueApportionment': 0.0, 'completedQuantity': 0.0, 'aplicationIntegration': 'T', 'productFantasyName': 'ARGAMASSA TRADICIONAL', 'productCode': '01.14.0002', 'productReducedCode': '146', 'manufacturerProductNumber': '01.14.0002', 'manufacturerCode': '00001', 'complementaryFields': {}, 'costCenterApportionments': [], 'departmentApportionments': [], 'taxes': [{'companyId': 1, 'movementId': 123, 'movementItemSequentialId': 1, 'taxId': 'ICMS', 'calculationBasis': 1881.0, 'aliquot': 0.0, 'value': 0.0, 'calculatedCalculationBasis': 1881.0, 'edited': 0, 'fullBase': 0.0}, {'companyId': 1, 'movementId': 123, 'movementItemSequentialId': 1, 'taxId': 'IPI', 'calculationBasis': 1710.0, 'aliquot': 10.0, 'value': 171.0, 'calculatedCalculationBasis': 1710.0, 'edited': 0, 'fullBase': 0.0}], 'itemLots': [], 'itemGrids': [], 'itemSerialNumber': [], 'itemHeritage': [], 'siscoServFitting': [], 'fiscal': [], 'reserves': [], 'relatedItem': [], 'exportRelatedItem': [], 'linkedItem': [], 'exportMemo': [], 'judicialProcess': [], 'electronicInvoiceFreeFields': []}]
payments -
[]
[]
[]
costCenterApportionments -

[]
[{'companyId': 1, 'apportionmentId': 1, 'movementId': 122, 'costCenterCode': '1.4.008', 'value': 627.0, 'costCenterName': 'EXPANSÃO SPC'}]
[{'companyId': 1, 'apportionmentId': 2, 'movementId': 123, 'costCenterCode': '1.4.008', 'value': 1881.0, 'costCenterName': 'EXPANSÃO SPC'}]
departmentApportionments -
[]
[{'companyId': 1, 'movementId': 122, 'branchId': 1, 'departmentCode': '10', 'value': 627.0, 'departmentName': 'OBRAS'}]
[{'companyId': 1, 'movementId': 123, 'branchId': 1, 'departmentCode': '10', 'value': 1881.0, 'departmentName': 'OBRAS'}]
taxes -
[]
[{'companyId': 1, 'movementId': 122, 'movementItemSequentialId': 0, 'taxId': 'PIS', 'calculationBasis': 627.0, 'aliquot': 1.65, 'value': 10.35, 'calculatedCalculationBasis': 627.0, 'edited': 0, 'fullBase': 0.0}]
[{'companyId': 1, 'movementId': 123, 'movementItemSequentialId': 0, 'taxId': 'PIS', 'calculationBasis': 1881.0, 'aliquot': 1.65, 'value': 31.04, 'calculatedCalculationBasis': 1881.0, 'edited': 0, 'fullBase': 0.0}]
fiscal -
[]
[]
[]
norm -
[]
[]
[]
cargoComponent -
[]
[]
[]
thirdPartyNF -
[]
[]
[]
safetyDevice -
[]
[]
[]
nfe -
[]
[]
[]
inputCTRC -
[]
[]
[]
outputCTRC -
[]
[]
[]
ctrc -
[]
[]
[]
transportData -
[]
[]
[]
documentAuthorization -
[]
[]
[]
judicialProcess -
[]
[]
[]
serviceOrder -
[]
[]
[]
relatedMovement -
[]
[]
[]
exportRelatedMovement -
[]
[]
[]
linkedMovement -
[]
[]
[]
cTe -
[]
[]
[]
eaiIntegration -
[]
[]
[]
electronicInvoiceFreeFields -
[]
[]
[]
customerVendorCode -

F00007
F00007
paymentTermCode -

45
45
observation -


CANTRATO ARCO IRIS
contraro arco iris
financialOptionalTable1Code - 

02
02
financialEntryMovementId -

122
123
generatedEntryNumber -

1
1
openEntryNumber -

1
1
cashAccountCode -

02
02
customerVendorCompanyId -

0
0
fluxusGroupedFlag -

0
0
cashAccountCompanyId -

1
1
longHistory -




O usuário final vai poder ver no detalhe o que tiver na base que o admin vai subir. Consegue me ajudar a criar essa aplicação de forma completa? Veja as colunas aonde temos json estruturados, para exibirmos as informações formatadas corretamente pro usuário. 

 








	


----- FILE: attached_assets/Pasted--coding-utf-8-import-re-import-sys-import-os-import-json-import-time-from-time-impor-1757089815255_1757089815257.txt  (sha1:fcda1dd549d4, bytes:28366) -----
# -*- coding: utf-8 -*-
import re
import sys
import os
import json
import time
from time import perf_counter
from time import time as _now
from datetime import datetime
from typing import List, Tuple, Dict, Any, Optional

import requests
from requests.auth import HTTPBasicAuth
from requests.adapters import HTTPAdapter
from urllib3.util.retry import Retry

import pandas as pd
from pandas import json_normalize
from dateutil import tz, parser as dtparser
import argparse

# python teste_andrey.py --inicio 2025-01-01 --fim 2025-01-02 --empresa 4 --filial 17 --arquivo movimentos_4_17.xlsx

# ==============================
# CONFIG DE CONEXÃO
# ==============================
HOST = "http://192.168.18.9:8051"
USER = "INTEGRA_INOVAI"
PWD  = "INOVAI.LAB"
auth = HTTPBasicAuth(USER, PWD)

MOV_ENDPOINT = "/api/mov/v1/movements"

# ==============================
# AJUSTES DE REDE / TOLERÂNCIA
# ==============================
PAGE_SIZE_CHAIN         = [25, 10, 5, 1]   # degradar até funcionar
MAX_LIST_PAGES          = 100000
MAX_PAGES_PER_BRANCH    = 999999

CONNECT_TIMEOUT         = 10
READ_TIMEOUT            = 45
ADAPTER_RETRIES         = 3
MANUAL_RETRIES          = 2
BACKOFF_BASE            = 1.0

BRANCH_DEADLINE_SEC     = 90
SLEEP_BETWEEN           = 0.05
PAGE_ATTEMPT_PAUSE      = 0.2
DEBUG_VERBOSE           = True

# Campos de data comuns no RM (no seu ambiente 'date' já funcionou)
DATE_FIELD_HINTS = [
    "date","issueDate","emissionDate","movementDate","creationDate","createdDate",
    "entryDate","exitDate","lastEditTime","registrationDate",
    "Date","IssueDate","EmissionDate","MovementDate","CreationDate","CreatedDate",
    "EntryDate","ExitDate","LastEditTime","RegistrationDate"
]
COMPANY_KEYS = ["companyId","CompanyId","companyCode","CompanyCode"]
BRANCH_KEYS  = ["branchId","BranchId","branchCode","BranchCode"]

# ==============================
# INFRA HTTP
# ==============================
def _make_session() -> requests.Session:
    retry = Retry(
        total=ADAPTER_RETRIES, connect=ADAPTER_RETRIES, read=ADAPTER_RETRIES,
        backoff_factor=0.4, status_forcelist=[502,503,504],
        allowed_methods=["GET","HEAD"], raise_on_status=False,
    )
    adapter = HTTPAdapter(max_retries=retry, pool_connections=10, pool_maxsize=20)
    s = requests.Session()
    s.auth = auth
    s.headers.update({
        "Accept": "application/json",
        "Connection": "close",
        "Accept-Encoding": "identity"
    })
    s.mount("http://", adapter); s.mount("https://", adapter)
    return s

def _safe_request(s: requests.Session, method: str, url: str, **kwargs) -> Tuple[Optional[requests.Response], Optional[Exception], float]:
    kwargs.setdefault("timeout", (CONNECT_TIMEOUT, READ_TIMEOUT))
    last_exc = None
    for attempt in range(1, MANUAL_RETRIES + 1):
        t0 = perf_counter()
        try:
            r = s.request(method, url, **kwargs)
            elapsed = (perf_counter() - t0) * 1000
            if DEBUG_VERBOSE:
                print(f"      ↪ {method} {url}  ({elapsed:.0f} ms)")
                if "params" in kwargs and kwargs["params"]:
                    print(f"        params: {kwargs['params']}")
                print(f"        → HTTP {r.status_code}")
            return r, None, elapsed
        except (requests.exceptions.ReadTimeout, requests.exceptions.ConnectionError) as e:
            elapsed = (perf_counter() - t0) * 1000
            last_exc = e
            sleep_s = BACKOFF_BASE * (2 ** (attempt - 1))
            print(f"      ⚠️ {type(e).__name__} {attempt}/{MANUAL_RETRIES} ({elapsed:.0f} ms). Backoff {sleep_s:.1f}s…")
            time.sleep(sleep_s)
        except Exception as e:
            elapsed = (perf_counter() - t0) * 1000
            if DEBUG_VERBOSE:
                print(f"      ❌ Exception inesperada ({elapsed:.0f} ms): {type(e).__name__}: {e}")
            return None, e, elapsed
    return None, last_exc, 0.0

# ==============================
# UTILS
# ==============================
def _norm_code(x: str) -> str:
    s = str(x).strip()
    return str(int(s)) if s.isdigit() else s

def _detect_field_present(obj: Dict[str,Any], candidates: List[str]) -> Optional[str]:
    lower_map = {k.lower(): k for k in obj.keys()}
    for cand in candidates:
        if cand.lower() in lower_map:
            return lower_map[cand.lower()]
    for k in obj.keys():
        if any(re.search(fr"\b{re.escape(cand)}\b", k, flags=re.IGNORECASE) for cand in candidates):
            return k
    return None

def _build_company_branch_filter(company_field: Optional[str], branch_field: Optional[str], comp_code: str, branch_code: str) -> Optional[str]:
    parts=[]
    if company_field:
        val = _norm_code(comp_code)
        parts.append(f"{company_field} eq {val if val.isdigit() else repr(val)}")
    if branch_field and str(branch_code).strip() != "":
        bc = str(branch_code).strip()
        parts.append(f"{branch_field} eq {bc if bc.isdigit() else repr(bc)}")
    return " and ".join(parts) if parts else None

def _date_iso(dt: datetime) -> str:
    return dt.isoformat()

def _guess_datetime_fields_from_sample(items: List[Dict[str,Any]]) -> List[str]:
    found=set()
    for it in items:
        for k,v in it.items():
            if isinstance(v,str):
                try:
                    dtparser.isoparse(v); found.add(k)
                except Exception:
                    pass
    ordered = [f for f in DATE_FIELD_HINTS if f in found]
    for f in sorted(found):
        if f not in ordered:
            ordered.append(f)
    return ordered

def _coerce_datetime_cols(df: pd.DataFrame) -> pd.DataFrame:
    candidates = [
        "date","Date","movementDate","MovementDate","issueDate","IssueDate",
        "emissionDate","EmissionDate","creationDate","CreatedDate","entryDate","exitDate","lastEditTime"
    ]
    for c in candidates:
        if c in df.columns:
            df[c] = pd.to_datetime(df[c], errors="coerce", utc=False)
    return df

def _strip_timezone_for_excel(df: pd.DataFrame, target_tz: str = "America/Sao_Paulo") -> pd.DataFrame:
    """Excel não aceita timezone; converte para horário local e remove tz."""
    from pandas.api.types import is_datetime64_any_dtype
    for col in df.columns:
        if is_datetime64_any_dtype(df[col]):
            tzinfo = getattr(df[col].dtype, "tz", None)
            if tzinfo is not None:
                try:
                    df[col] = df[col].dt.tz_convert(target_tz).dt.tz_localize(None)
                except Exception:
                    df[col] = df[col].dt.tz_localize(None)
    return df

def _jsonify_non_scalars_for_excel(df: pd.DataFrame) -> pd.DataFrame:
    """Transforma listas/dicts em JSON string para o Excel aceitar."""
    def to_excel_scalar(x):
        if isinstance(x, (list, dict, tuple, set)):
            try:
                return json.dumps(x, ensure_ascii=False)
            except Exception:
                return str(x)
        return x
    for c in df.columns:
        df[c] = df[c].apply(to_excel_scalar)
    return df

def _first_existing(cols: list[str], df_cols: set[str]) -> Optional[str]:
    for c in cols:
        if c in df_cols: return c
    return None

def _smart_sort(df: pd.DataFrame, ordenar_cols: List[str]) -> pd.DataFrame:
    cols = [c for c in ordenar_cols if c in df.columns]
    if not cols:
        maybe_date = _first_existing(["date","Date","movementDate","MovementDate","issueDate","IssueDate"], set(df.columns))
        maybe_id   = _first_existing(["internalId","InternalId","movementId","MovementId"], set(df.columns))
        cols = [c for c in [maybe_date, maybe_id] if c]
    if cols:
        return df.sort_values(cols, kind="stable")
    return df

def _autosize_openpyxl(ws):
    """Ajusta larguras de colunas numa worksheet openpyxl."""
    from openpyxl.utils import get_column_letter
    dims = {}
    for row in ws.rows:
        for cell in row:
            if cell.value is None: continue
            val = str(cell.value)
            dims[cell.column] = max(dims.get(cell.column, 0), len(val))
    for col, width in dims.items():
        ws.column_dimensions[get_column_letter(col)].width = min(max(width + 2, 10), 60)

# ==============================
# CATÁLOGO
# ==============================
def get_company_name(s: requests.Session, company_code: str) -> str:
    code = _norm_code(company_code)
    urls = [
        f"{HOST}/api/framework/v1/companies/{code}",
        f"{HOST}/api/framework/v1/Companies/{code}",
        f"{HOST}/api/corp/v1/companies/{code}",
    ]
    keys = ["name","title","description","CompanyName","Nome","Title","Description"]
    for url in urls:
        r, exc, _ = _safe_request(s, "GET", url)
        if exc or not r: continue
        if r.status_code == 200:
            try: j = r.json()
            except Exception: continue
            if isinstance(j, dict):
                for k in keys:
                    v = j.get(k) or (j.get("company", {}) if isinstance(j.get("company"), dict) else {}).get(k)
                    if v: return str(v).strip()
    return ""

def list_branches(s: requests.Session, company_code: str) -> List[Dict[str,str]]:
    c = _norm_code(company_code)
    branches = []
    r, exc, _ = _safe_request(s, "GET", f"{HOST}/api/framework/v1/Branches", params={"companyCode": c})
    if exc or not r: return branches
    if r.status_code == 200:
        try: j = r.json()
        except Exception: return branches
        data = j.get("items", j) if isinstance(j, dict) else j
        if isinstance(data, list):
            for obj in data:
                if str(obj.get("CompanyCode")) == c:
                    branches.append({
                        "CompanyCode": str(obj.get("CompanyCode","")),
                        "Code":        str(obj.get("Code","")),
                        "Title":       str(obj.get("Title","") or ""),
                        "Description": str(obj.get("Description","") or ""),
                    })
    seen=set(); uniq=[]
    for b in branches:
        key=(b["CompanyCode"], b["Code"])
        if key not in seen:
            seen.add(key); uniq.append(b)
    return uniq

# ==============================
# MOVEMENTS
# ==============================
def _get_movements_page(
    s: requests.Session,
    page: int,
    page_size: int,
    odata_filter: Optional[str] = None,
) -> Tuple[List[Dict[str,Any]], int, Dict[str,Any]]:
    url = f"{HOST}{MOV_ENDPOINT}"
    params = {"page": max(1,page), "pageSize": max(1,page_size)}
    if odata_filter: params["$filter"] = odata_filter
    meta = {"had_items_key": None, "raw_len": None, "status": None, "body_preview": None, "page_size": page_size}
    r, exc, _ = _safe_request(s, "GET", url, params=params)
    if not r:
        meta["status"] = -1
        meta["body_preview"] = f"{type(exc).__name__}: {exc}" if exc else None
        return [], -1, meta
    meta["status"] = r.status_code
    try: meta["body_preview"] = (r.text or "")[:300].replace("\n"," ")
    except Exception: meta["body_preview"] = None
    items=[]
    if 200 <= r.status_code < 300 or r.status_code == 206:
        try: j = r.json()
        except Exception: return [], r.status_code, meta
        if isinstance(j, dict):
            meta["had_items_key"] = "items" if "items" in j else ("Items" if "Items" in j else None)
            data = j.get("items") or j.get("Items")
            if isinstance(data, list): items = data; meta["raw_len"] = len(items)
            elif isinstance(j, list): items = j; meta["raw_len"] = len(items)
            else: items = [j]; meta["raw_len"] = 1
        elif isinstance(j, list):
            items = j; meta["raw_len"] = len(j)
    return items, r.status_code, meta

def _try_date_filter_server_side(
    s: requests.Session,
    company_field: Optional[str],
    branch_field: Optional[str],
    comp_code: str,
    branch_code: str,
    dt_ini: datetime,
    dt_fim: datetime,
    sample_date_candidates: List[str]
) -> Tuple[Optional[str], Optional[str]]:
    base_filter = _build_company_branch_filter(company_field, branch_field, comp_code, branch_code)
    candidates = list(dict.fromkeys(DATE_FIELD_HINTS + sample_date_candidates))
    for df in candidates:
        date_part = f"{df} ge {_date_iso(dt_ini)} and {df} le {_date_iso(dt_fim)}"
        odata = f"{base_filter} and {date_part}" if base_filter else date_part
        items, st, _ = _get_movements_page(s, page=1, page_size=1, odata_filter=odata)
        if st == 200:
            return odata, df
    return None, None

# ==============================
# MAIN (EXTRAÇÃO + EXCEL EM ÁRVORE)
# ==============================
def run_extract(args):
    TZBR = tz.gettz("America/Sao_Paulo")
    dt_ini = datetime.strptime(args.inicio, "%Y-%m-%d").replace(hour=0, minute=0, second=0, tzinfo=TZBR)
    dt_fim = datetime.strptime(args.fim, "%Y-%m-%d").replace(hour=23, minute=59, second=59, tzinfo=TZBR)

    s = _make_session()

    print(">>> Iniciando extração de movements…")
    print(f"Período: {dt_ini.isoformat()}  até  {dt_fim.isoformat()}")
    print(f"Endpoint: {HOST}{MOV_ENDPOINT}")

    target_companies = [args.empresa] if args.empresa else ["4", "148"]
    all_rows: List[Dict[str,Any]] = []
    labels: List[Tuple[str,str,str]] = []  # (companyId, branchId, branchLabel)

    for comp in target_companies:
        comp_norm = _norm_code(comp)
        comp_name = get_company_name(s, comp_norm)
        print("\n" + "="*72)
        print(f"🏢 Empresa {comp_norm} — {comp_name or '(sem nome)'}")
        print("="*72)

        branches = list_branches(s, comp_norm)
        print(f"  📚 Filiais detectadas: {len(branches)}")

        if args.filial:
            branches = [b for b in branches if str(b.get("Code","")).strip() == str(args.filial).strip()]
            print(f"  🔎 Filtro de filial: {args.filial} → {len(branches)} filial(is)")

        if not branches:
            print("  ⚠️ Nenhuma filial encontrada após filtro. Pulando empresa.")
            continue

        # probe mínimo
        probe_items, status, _ = _get_movements_page(s, page=1, page_size=5, odata_filter=None)
        if status >= 400 or status == -1 or not probe_items:
            probe_items2, status2, meta2 = _get_movements_page(s, page=1, page_size=1, odata_filter=None)
            if status2 >= 400 or status2 == -1 or not probe_items2:
                print(f"  ❌ Probe /movements falhou (HTTP {status2}). Body: {meta2.get('body_preview') if isinstance(meta2, dict) else None}")
                continue
            probe_items = probe_items2

        probe_row = probe_items[0] if probe_items else {}
        comp_field = _detect_field_present(probe_row, COMPANY_KEYS) or "companyId"
        br_field   = _detect_field_present(probe_row, BRANCH_KEYS)  or "branchId"
        date_candidates = _guess_datetime_fields_from_sample(probe_items)
        print(f"  🔎 Campos /movements → company='{comp_field}', branch='{br_field}'")

        for idx, b in enumerate(branches, start=1):
            b_code = b.get("Code","")
            b_tag  = (b.get("Title") or b.get("Description") or "(sem nome)").strip()
            header = f"  → Filial {idx}/{len(branches)}: código {b_code or '(vazio)'} — {b_tag}"
            print("\n" + header)
            print("  " + "—"*len(header))

            labels.append((str(comp_norm), str(b_code), b_tag))

            server_odata, used_date_field = _try_date_filter_server_side(
                s, comp_field, br_field, comp_norm, b_code, dt_ini, dt_fim, date_candidates
            )
            if server_odata:
                print(f"    ✅ Filtro server-side aceito: {server_odata}")
            else:
                server_odata = _build_company_branch_filter(comp_field, br_field, comp_norm, b_code)
                print("    ⚠️ Sem campo de data aceito no servidor. Período será filtrado no cliente.")

            # paginação
            page = 1
            empty_seq = 0
            ps_index = 0
            started_at = _now()

            while page <= min(MAX_LIST_PAGES, MAX_PAGES_PER_BRANCH):
                if _now() - started_at > BRANCH_DEADLINE_SEC:
                    print(f"      ⏱️ Tempo excedido ({BRANCH_DEADLINE_SEC}s) nesta filial — pulando.")
                    break

                page_size = PAGE_SIZE_CHAIN[ps_index]
                items, st, _ = _get_movements_page(s, page=page, page_size=page_size, odata_filter=server_odata)

                if st == -1 or st >= 500:
                    if ps_index < len(PAGE_SIZE_CHAIN) - 1:
                        ps_index += 1
                        print(f"      ⚠️ st={st} — reduzindo pageSize para {PAGE_SIZE_CHAIN[ps_index]} e repetindo página {page}…")
                        time.sleep(PAGE_ATTEMPT_PAUSE)
                        continue
                    # último recurso: sem $filter e filtro local
                    print("      ⚠️ Falha persistente — tentando sem $filter e filtrando localmente…")
                    items2, st2, _ = _get_movements_page(s, page=page, page_size=page_size, odata_filter=None)
                    if st2 >= 400 or st2 == -1:
                        print(f"      ⚠️ HTTP {st2} mesmo sem filtro — encerrando varredura desta filial.")
                        break
                    items = items2
                    if comp_field:
                        items = [it for it in items if str(it.get(comp_field)) == _norm_code(comp_norm)]
                    if br_field and str(b_code).strip() != "":
                        items = [it for it in items if str(it.get(br_field, "")).strip() == str(b_code).strip()]
                    if not used_date_field:
                        # SUBSTITUA este bloco pela versão abaixo
                        def in_period_local(rec: Dict[str, Any]) -> bool:
                            # tenta campo 'date' primeiro
                            v = rec.get("date")
                            if isinstance(v, str):
                                try:
                                    d = dtparser.isoparse(v)
                                    return dt_ini <= d <= dt_fim
                                except Exception:
                                    pass
                            # tenta demais candidatos
                            for key in (date_candidates or []) + DATE_FIELD_HINTS:
                                v = rec.get(key)
                                if isinstance(v, str):
                                    try:
                                        d = dtparser.isoparse(v)
                                        if dt_ini <= d <= dt_fim:
                                            return True
                                    except Exception:
                                        pass
                            return False

                        items = [it for it in items if in_period_local(it)]

                elif st >= 400:
                    print(f"      ⚠️ HTTP {st} — pulando página.")
                    break

                if not items:
                    empty_seq += 1
                    print(f"    · Página {page}: 0 itens (vazias seguidas={empty_seq}) | ps={page_size}")
                    if empty_seq >= 2:
                        print("      ↳ Duas páginas vazias — fim desta filial.")
                        break
                    page += 1
                    time.sleep(SLEEP_BETWEEN)
                    continue

                if not used_date_field:
                    # SUBSTITUA este bloco pela versão abaixo
                    def in_period_client(rec: Dict[str, Any]) -> bool:
                        # tenta campo 'date' primeiro
                        v = rec.get("date")
                        if isinstance(v, str):
                            try:
                                d = dtparser.isoparse(v)
                                return dt_ini <= d <= dt_fim
                            except Exception:
                                pass
                        # tenta demais candidatos
                        for key in (date_candidates or []) + DATE_FIELD_HINTS:
                            v = rec.get(key)
                            if isinstance(v, str):
                                try:
                                    d = dtparser.isoparse(v)
                                    if dt_ini <= d <= dt_fim:
                                        return True
                                except Exception:
                                    pass
                        return False

                    items = [it for it in items if in_period_client(it)]

                all_rows.extend(items)

                print(f"    · Página {page}: {len(items)} itens  | ps={page_size}")
                if len(items) < page_size:
                    print("      ↳ Última página (len(items) < pageSize).")
                    break
                page += 1
                time.sleep(SLEEP_BETWEEN)

    # ==============================
    # CONSOLIDAÇÃO
    # ==============================
    print("\n>>> Consolidando em DataFrame…")
    if not all_rows:
        print("(Sem registros coletados.)")
        return

    df = json_normalize(all_rows, sep=".")
    df = _coerce_datetime_cols(df)
    df = _strip_timezone_for_excel(df)

    # ordenar
    ordenar_cols = [c.strip() for c in (args.ordenar or "").split(",") if c.strip()]
    df = _smart_sort(df, ordenar_cols)

    # resumo por empresa/filial
    branch_col  = _first_existing(["branchId","BranchId","branchCode","BranchCode"], set(df.columns))
    company_col = _first_existing(["companyId","CompanyId","companyCode","CompanyCode"], set(df.columns))
    resumo = None
    if branch_col:
        grp_cols = [c for c in [company_col, branch_col] if c]
        resumo = df.groupby(grp_cols, dropna=False).size().reset_index(name="qtd").sort_values("qtd", ascending=False)
        resumo = _strip_timezone_for_excel(resumo)

    # ==============================
    # XLSX EM “ÁRVORE” + DADOS COMPLETOS
    # ==============================
    base = args.arquivo
    if not base:
        emp = f"emp{args.empresa}" if args.empresa else "empVARIAS"
        fil = f"_fil{args.filial}" if args.filial else ""
        base = f"movements_{emp}{fil}_{args.inicio}_a_{args.fim}.xlsx"
    if not base.lower().endswith(".xlsx"):
        base += ".xlsx"

    from openpyxl import Workbook
    from openpyxl.styles import Font, PatternFill

    wb = Workbook()
    ws_tree = wb.active
    ws_tree.title = "Árvore"
    ws_data = wb.create_sheet("Dados")

    # —— folha Dados (dataset completo tipo DataFrame)
    df_excel = _jsonify_non_scalars_for_excel(df.copy())   # <—— torna listas/dicts em JSON
    ws_data.append(list(df_excel.columns))
    for row in df_excel.itertuples(index=False, name=None):
        ws_data.append(list(row))
    try:
        _autosize_openpyxl(ws_data)
    except Exception:
        pass

    # —— folha Árvore (Empresa → Filial → Movimentos)
    bold = Font(bold=True)
    header_fill = PatternFill(start_color="FFECECEC", end_color="FFECECEC", fill_type="solid")
    company_fill = PatternFill(start_color="FFCCE5FF", end_color="FFCCE5FF", fill_type="solid")
    branch_fill  = PatternFill(start_color="FFFFF2CC", end_color="FFFFF2CC", fill_type="solid")

    # colunas a mostrar na árvore
    tree_cols = [c.strip() for c in (args.colunas or "").split(",") if c.strip()]
    if not tree_cols:
        pref = ["date","internalId","movementId","documentNumber","type","grossValue","netValue","partner.name","status"]
        tree_cols = [c for c in pref if c in df.columns] or list(df.columns[:10])

    # índice rápido por empresa/filial
    comp_series = df[company_col] if company_col in df.columns else pd.Series([""]*len(df))
    br_series   = df[branch_col]  if branch_col in df.columns else pd.Series([""]*len(df))
    unique_pairs = pd.DataFrame({"comp": comp_series, "branch": br_series}).drop_duplicates()

    # map de label de filial (montado durante varredura)
    filial_label_map: Dict[Tuple[str,str], str] = {}
    # (opcional: se quiser, podemos popular labels a partir de /Branches e usar aqui)

    # escrever em “árvore”
    for comp_id in sorted(unique_pairs["comp"].astype(str).unique(), key=lambda x: (x is None, x)):
        comp_mask = (comp_series.astype(str) == str(comp_id))
        df_comp = df[comp_mask]

        ws_tree.append([f"Empresa: {comp_id}"])
        for cell in ws_tree[ws_tree.max_row]:
            cell.font = bold; cell.fill = company_fill

        filiais_comp = sorted(df_comp[branch_col].astype(str).unique()) if branch_col in df_comp.columns else [""]
        for br_id in filiais_comp:
            br_mask = (br_series.astype(str) == str(br_id))
            df_fil = df_comp[br_mask] if branch_col in df_comp.columns else df_comp

            br_name = filial_label_map.get((str(comp_id), str(br_id)), "")
            ws_tree.append([f"  Filial: {br_id} {('- ' + br_name) if br_name else ''}"])
            for cell in ws_tree[ws_tree.max_row]:
                cell.font = bold; cell.fill = branch_fill

            # Header das colunas
            ws_tree.append([""] + tree_cols)
            for cell in ws_tree[ws_tree.max_row]:
                cell.font = bold; cell.fill = header_fill

            # Linhas dos movimentos (sanitizando listas/dicts)
            df_tree = df_fil[tree_cols].copy()
            df_tree = _jsonify_non_scalars_for_excel(df_tree)
            for _, rec in df_tree.iterrows():
                ws_tree.append([""] + [rec.get(c) for c in tree_cols])

            ws_tree.append([])  # linha em branco entre filiais

    try:
        _autosize_openpyxl(ws_tree)
    except Exception:
        pass

    # aba Resumo
    if resumo is not None:
        ws_resumo = wb.create_sheet("Resumo")
        resumo_excel = _jsonify_non_scalars_for_excel(resumo.copy())
        ws_resumo.append(list(resumo_excel.columns))
        for row in resumo_excel.itertuples(index=False, name=None):
            ws_resumo.append(list(row))
        try:
            _autosize_openpyxl(ws_resumo)
        except Exception:
            pass

    # salvar
    try:
        wb.save(base)
    except Exception as e:
        alt = os.path.splitext(base)[0] + ".csv"
        print(f"⚠️ Falha ao salvar XLSX ({e}). Salvando CSV com dados completos: {alt}")
        df_excel.to_csv(alt, index=False)
        base = alt

    print(f"\n✅ Arquivo gerado: {os.path.abspath(base)}")

def parse_args():
    ap = argparse.ArgumentParser(description="Extrai Movements e gera XLSX em árvore + dados completos.")
    ap.add_argument("--inicio",   required=True, help="Data inicial (YYYY-MM-DD).")
    ap.add_argument("--fim",      required=True, help="Data final (YYYY-MM-DD).")
    ap.add_argument("--empresa",  required=False, help="companyId (coligada), ex.: 4")
    ap.add_argument("--filial",   required=False, help="branchId (filial), ex.: 17")
    ap.add_argument("--ordenar",  required=False, default="date,internalId", help="Colunas para ordenar (vírgula).")
    ap.add_argument("--colunas",  required=False, default="", help="Colunas para mostrar na aba Árvore (vírgula).")
    ap.add_argument("--arquivo",  required=False, default="", help="Caminho do .xlsx de saída.")
    return ap.parse_args()

if __name__ == "__main__":
    args = parse_args()
    try:
        run_extract(args)
    except KeyboardInterrupt:
        print("\nInterrompido pelo usuário.")
        sys.exit(130)
    except Exception as e:
        print(f"\nERRO fatal: {e}")
        sys.exit(2)


----- FILE: attached_assets/Pasted--companyId-1-movementId-91-sequentialId-1-sequentialNumber-1-productId-812-qu-1755531544491_1755531544492.txt  (sha1:e610fe28c588, bytes:8037) -----

[{'companyId': 1, 'movementId': 91, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 812, 'quantity': 1.0, 'unitPrice': 500.0, 'tablePrice': 0.0, 'discountValue': 0.0, 'registerDate': '2003-08-01T00:00:00-03:00', 'classificationTable5Code': '12305016', 'financialOptionalTable2Code': '1.2.3.05.016', 'measureUnitCode': 'vb', 'receivableQuantity': 1.0, 'deliveryDate': '2003-08-01T00:00:00-03:00', 'partialBillingReceivedValue': 0.0, 'completedValue': 0.0, 'unitValue': 0.0, 'financialValue': 0.0, 'ordinationTaxAliquot': 0.0, 'originalQuantity': 1.0, 'flag': 0, 'blockObject': 0, 'rebillingFlag': 0, 'measureUnitConversionRate': 0.0, 'totalValue': 500.0, 'branchId': 1, 'sortedQuantity': 0.0, 'commercialRepresentativeChargePercentage': 0.0, 'fiscalBookkeepingValue': 0.0, 'orderFinancialValue': 0.0, 'ctrcFreightValue': 0.0, 'optionalFormulaValue1': 0.0, 'optionalFormulaValue2': 0.0, 'editedPrice': False, 'unitaryOccupiedVolumeNumber': 1, 'editedTotalPrice': 0, 'conditionalDiscountValue': 0.0, 'conditionalExpenseValue': 0.0, 'budgetDate': '2014-12-04T00:00:00-03:00', 'freightApportionment': 0.0, 'insuranceApportionment': 0.0, 'discountApportionment': 0.0, 'expenseApportionment': 0.0, 'extraValue1Apportionment': 0.0, 'extraValue2Apportionment': 0.0, 'ctrcFreightApportionment': 0.0, 'materialDeductionApportionment': 0.0, 'subcontractingDeductionApportionment': 0.0, 'otherDeductionApportionment': 0.0, 'budgetUnitPrice': 0.0, 'nFeServiceValue': 0.0, 'warehouseCode': '01', 'heritageValue': 0.0, 'heritageBaseDepreciationValue': 0.0, 'netValue': 500.0, 'grossValue': 0.0, 'originalGrossValue': 0.0, 'costCenterDepartmentApportionmentValue': 0.0, 'replenishmentCost': 0.0, 'bReplenishmentCost': 0.0, 'thirdPartyFinancialValue': 0.0, 'totalQuantity': 1.0, 'isSubstituteProduct': 0, 'unitPriceSelectionType': 0, 'financialValueApportionment': 0.0, 'completedQuantity': 0.0, 'aplicationIntegration': 'T', 'productFantasyName': 'SERVICOS EM GERAL', 'productCode': '99.01.0001', 'productReducedCode': '812', 'manufacturerProductNumber': '99.01.0000', 'manufacturerCode': '00001', 'complementaryFields': {}, 'costCenterApportionments': [], 'departmentApportionments': [], 'taxes': [], 'itemLots': [], 'itemGrids': [], 'itemSerialNumber': [], 'itemHeritage': [], 'siscoServFitting': [], 'fiscal': [], 'reserves': [], 'relatedItem': [], 'exportRelatedItem': [], 'linkedItem': [], 'exportMemo': [], 'judicialProcess': [], 'electronicInvoiceFreeFields': []}]
[{'companyId': 1, 'movementId': 122, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 146, 'quantity': 100.0, 'unitPrice': 5.7, 'tablePrice': 0.0, 'discountValue': 0.0, 'registerDate': '2003-08-04T00:00:00-03:00', 'measureUnitCode': 'SC', 'receivableQuantity': 100.0, 'partialBillingReceivedValue': 0.0, 'completedValue': 0.0, 'stockEffectFlag': 1, 'unitValue': 6.27, 'financialValue': 627.0, 'ordinationTaxAliquot': 0.0, 'originalQuantity': 100.0, 'flag': 0, 'blockObject': 0, 'rebillingFlag': 0, 'measureUnitConversionRate': 0.0, 'totalValue': 570.0, 'branchId': 1, 'sortedQuantity': 0.0, 'commercialRepresentativeChargePercentage': 0.0, 'fiscalBookkeepingValue': 0.0, 'orderFinancialValue': 0.0, 'ctrcFreightValue': 0.0, 'optionalFormulaValue1': 0.0, 'optionalFormulaValue2': 0.0, 'editedPrice': False, 'unitaryOccupiedVolumeNumber': 1, 'editedTotalPrice': 0, 'conditionalDiscountValue': 0.0, 'conditionalExpenseValue': 0.0, 'budgetDate': '2014-12-04T00:00:00-03:00', 'freightApportionment': 0.0, 'insuranceApportionment': 0.0, 'discountApportionment': 0.0, 'expenseApportionment': 0.0, 'extraValue1Apportionment': 0.0, 'extraValue2Apportionment': 0.0, 'ctrcFreightApportionment': 0.0, 'materialDeductionApportionment': 0.0, 'subcontractingDeductionApportionment': 0.0, 'otherDeductionApportionment': 0.0, 'budgetUnitPrice': 0.0, 'nFeServiceValue': 0.0, 'warehouseCode': '01', 'heritageValue': 0.0, 'heritageBaseDepreciationValue': 0.0, 'netValue': 570.0, 'grossValue': 0.0, 'originalGrossValue': 0.0, 'costCenterDepartmentApportionmentValue': 0.0, 'replenishmentCost': 0.0, 'bReplenishmentCost': 0.0, 'thirdPartyFinancialValue': 0.0, 'totalQuantity': 100.0, 'isSubstituteProduct': 0, 'unitPriceSelectionType': 0, 'financialValueApportionment': 0.0, 'completedQuantity': 0.0, 'aplicationIntegration': 'T', 'productFantasyName': 'ARGAMASSA TRADICIONAL', 'productCode': '01.14.0002', 'productReducedCode': '146', 'manufacturerProductNumber': '01.14.0002', 'manufacturerCode': '00001', 'complementaryFields': {}, 'costCenterApportionments': [], 'departmentApportionments': [], 'taxes': [{'companyId': 1, 'movementId': 122, 'movementItemSequentialId': 1, 'taxId': 'ICMS', 'calculationBasis': 627.0, 'aliquot': 0.0, 'value': 0.0, 'calculatedCalculationBasis': 627.0, 'edited': 0, 'fullBase': 0.0}, {'companyId': 1, 'movementId': 122, 'movementItemSequentialId': 1, 'taxId': 'IPI', 'calculationBasis': 570.0, 'aliquot': 10.0, 'value': 57.0, 'calculatedCalculationBasis': 570.0, 'edited': 0, 'fullBase': 0.0}], 'itemLots': [], 'itemGrids': [], 'itemSerialNumber': [], 'itemHeritage': [], 'siscoServFitting': [], 'fiscal': [], 'reserves': [], 'relatedItem': [], 'exportRelatedItem': [], 'linkedItem': [], 'exportMemo': [], 'judicialProcess': [], 'electronicInvoiceFreeFields': []}]
[{'companyId': 1, 'movementId': 123, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 146, 'quantity': 300.0, 'unitPrice': 5.7, 'tablePrice': 0.0, 'discountValue': 0.0, 'registerDate': '2003-08-04T00:00:00-03:00', 'measureUnitCode': 'SC', 'receivableQuantity': 300.0, 'partialBillingReceivedValue': 0.0, 'completedValue': 0.0, 'stockEffectFlag': 1, 'unitValue': 6.27, 'financialValue': 1881.0, 'ordinationTaxAliquot': 0.0, 'originalQuantity': 300.0, 'flag': 0, 'blockObject': 0, 'rebillingFlag': 0, 'measureUnitConversionRate': 0.0, 'totalValue': 1710.0, 'branchId': 1, 'sortedQuantity': 0.0, 'commercialRepresentativeChargePercentage': 0.0, 'fiscalBookkeepingValue': 0.0, 'orderFinancialValue': 0.0, 'ctrcFreightValue': 0.0, 'optionalFormulaValue1': 0.0, 'optionalFormulaValue2': 0.0, 'editedPrice': False, 'unitaryOccupiedVolumeNumber': 1, 'editedTotalPrice': 0, 'conditionalDiscountValue': 0.0, 'conditionalExpenseValue': 0.0, 'budgetDate': '2014-12-04T00:00:00-03:00', 'freightApportionment': 0.0, 'insuranceApportionment': 0.0, 'discountApportionment': 0.0, 'expenseApportionment': 0.0, 'extraValue1Apportionment': 0.0, 'extraValue2Apportionment': 0.0, 'ctrcFreightApportionment': 0.0, 'materialDeductionApportionment': 0.0, 'subcontractingDeductionApportionment': 0.0, 'otherDeductionApportionment': 0.0, 'budgetUnitPrice': 0.0, 'nFeServiceValue': 0.0, 'warehouseCode': '01', 'heritageValue': 0.0, 'heritageBaseDepreciationValue': 0.0, 'netValue': 1710.0, 'grossValue': 0.0, 'originalGrossValue': 0.0, 'costCenterDepartmentApportionmentValue': 0.0, 'replenishmentCost': 0.0, 'bReplenishmentCost': 0.0, 'thirdPartyFinancialValue': 0.0, 'totalQuantity': 300.0, 'isSubstituteProduct': 0, 'unitPriceSelectionType': 0, 'financialValueApportionment': 0.0, 'completedQuantity': 0.0, 'aplicationIntegration': 'T', 'productFantasyName': 'ARGAMASSA TRADICIONAL', 'productCode': '01.14.0002', 'productReducedCode': '146', 'manufacturerProductNumber': '01.14.0002', 'manufacturerCode': '00001', 'complementaryFields': {}, 'costCenterApportionments': [], 'departmentApportionments': [], 'taxes': [{'companyId': 1, 'movementId': 123, 'movementItemSequentialId': 1, 'taxId': 'ICMS', 'calculationBasis': 1881.0, 'aliquot': 0.0, 'value': 0.0, 'calculatedCalculationBasis': 1881.0, 'edited': 0, 'fullBase': 0.0}, {'companyId': 1, 'movementId': 123, 'movementItemSequentialId': 1, 'taxId': 'IPI', 'calculationBasis': 1710.0, 'aliquot': 10.0, 'value': 171.0, 'calculatedCalculationBasis': 1710.0, 'edited': 0, 'fullBase': 0.0}], 'itemLots': [], 'itemGrids': [], 'itemSerialNumber': [], 'itemHeritage': [], 'siscoServFitting': [], 'fiscal': [], 'reserves': [], 'relatedItem': [], 'exportRelatedItem': [], 'linkedItem': [], 'exportMemo': [], 'judicialProcess': [], 'electronicInvoiceFreeFields': []}]


----- FILE: attached_assets/Pasted--sa-instance-state-sqlalchemy-orm-state-InstanceState-object-at-0x7fa9d98ee030-accounting-e-1755529082796_1755529082796.txt  (sha1:8833ffe4b9b8, bytes:3595) -----
{'_sa_instance_state': <sqlalchemy.orm.state.InstanceState object at 0x7fa9d98ee030>,
 'accounting_export_status': '0',
 'affect_stock_order': '380',
 'aplication_integration': 'T',
 'aux_customer_vendor_code': 'F00411',
 'aux_customer_vendor_company_id': 0,
 'bill_printed': False,
 'bonum_integrated': False,
 'branch_id': 1,
 'c_te': '[]',
 'cargo_component': '[]',
 'cash_account_code': '2.0',
 'cash_account_company_id': 1,
 'charge_percentage': 0.0,
 'classification_table5_code': None,
 'commercial_automation_exported': 0,
 'commercial_representative_charge': 0.0,
 'company_id': 1,
 'conclusion_flag': 0,
 'conditional_discount_value': 0.0,
 'conditional_expense_value': 0.0,
 'cost_center_apportionments': '[]',
 'cost_center_code': None,
 'creation_date': datetime.datetime(2003, 9, 16, 3, 0),
 'creation_user': 'cmufumba',
 'ctrc': '[]',
 'customer_vendor_code': 'F00411',
 'customer_vendor_company_id': 0,
 'date': datetime.datetime(2003, 9, 11, 3, 0),
 'delivery_date': None,
 'department_apportionments': '[]',
 'destiny_branch_id': 1,
 'destiny_warehouse_code': '1',
 'discount_percentage': 0.0,
 'document_authorization': '[]',
 'document_printed': False,
 'eai_integration': '[]',
 'electronic_invoice_free_fields': '[]',
 'email_status': 'false',
 'entry_date': datetime.datetime(2003, 9, 16, 3, 0),
 'exit_date': datetime.datetime(2003, 9, 16, 3, 0),
 'expense_percentage': 0.0,
 'expense_value': 0.0,
 'export_related_movement': '[]',
 'extemporaneous': 0,
 'extra_percentage1': 0.0,
 'extra_percentage2': 0.0,
 'extra_value1': 0.0,
 'extra_value2': 0.0,
 'financial_entry_movement_id': '380.0',
 'financial_optional_table1_code': '2.0',
 'financial_optional_table2_code': None,
 'fiscal': '[]',
 'fluxus_grouped_flag': False,
 'generated_entry_number': '1.0',
 'gross_value': 80.0,
 'has_generated_bill': False,
 'has_generated_work_account': False,
 'icms_deduction_value': 0.0,
 'id': 250,
 'indicate_object_use': '0',
 'informed_net_value': 0.0,
 'input_ctrc': '[]',
 'internal_gross_value': 80.0,
 'internal_id': '1|380',
 'judicial_process': '[]',
 'last_edit_time': datetime.datetime(2003, 9, 16, 12, 59, 40),
 'linked_movement': '[]',
 'long_history': 'COSERTO DA PRANCHA',
 'lot_generated': False,
 'merchandise_value': 0.0,
 'movement_id': 380,
 'movement_items': '[]',
 'movement_type_code': '1.2.02',
 'net_value': 80.0,
 'net_value_currency_code': 'R$',
 'nfe': '[]',
 'norm': '[]',
 'number': 11179,
 'observation': None,
 'open_entry_number': '1.0',
 'operation_id': 0,
 'original_gross_value': 0.0,
 'original_net_value': 0.0,
 'original_other_values': 0.0,
 'other_company_inss_base_value': 0.0,
 'other_values': 80.0,
 'output_ctrc': '[]',
 'paradigma_auto_integrated': False,
 'paradigma_status': 'N',
 'payment_term_code': '30.0',
 'payments': '[]',
 'printed': False,
 'processed_flag': False,
 'register_date': datetime.datetime(2003, 9, 11, 3, 0),
 'related_movement': '[]',
 'safety_device': '[]',
 'salesman1_code': None,
 'salesman2_charge_percentage': 0.0,
 'salesman3_charge_percentage': 0.0,
 'salesman4_charge_percentage': 0.0,
 'scp_branch_id': 0,
 'series': 'U',
 'service_order': '[]',
 'status': 'F',
 'taxes': '[]',
 'third_party_nf': '[]',
 'transport_data': '[]',
 'transported_product_gross_weight': 0.0,
 'transported_product_net_weight': 0.0,
 'type': 'P',
 'upload_batch_id': '042bed78-bf01-4389-accb-ed032646b4fc',
 'uploaded_at': datetime.datetime(2025, 8, 18, 14, 56, 9, 423486),
 'uploaded_by': 1,
 'user_code': 'cmufumba',
 'uses_financial_value_apportionment': False,
 'warehouse_code': '1',
 'work_account_generated': 'false'}

----- FILE: attached_assets/Pasted-ERROR-app-Exception-on-movements-GET-Traceback-most-recent-call-last-File-home-runner-wo-1757090301627_1757090301628.txt  (sha1:5b4a42c7e6bd, bytes:12473) -----
ERROR:app:Exception on /movements [GET]
Traceback (most recent call last):
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 1511, in wsgi_app
    response = self.full_dispatch_request()
               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 919, in full_dispatch_request
    rv = self.handle_user_exception(e)
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 917, in full_dispatch_request
    rv = self.dispatch_request()
         ^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 902, in dispatch_request
    return self.ensure_sync(self.view_functions[rule.endpoint])(**view_args)  # type: ignore[no-any-return]
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask_login/utils.py", line 290, in decorated_view
    return current_app.ensure_sync(func)(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/routes.py", line 102, in movements
    return render_template('movements/list.html',
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/templating.py", line 150, in render_template
    return _render(app, template, context)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/templating.py", line 131, in _render
    rv = template.render(context)
         ^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/environment.py", line 1295, in render
    self.environment.handle_exception()
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/environment.py", line 942, in handle_exception
    raise rewrite_traceback_stack(source=source)
  File "/home/runner/workspace/templates/movements/list.html", line 1, in top-level template code
    {% extends "base.html" %}
  File "/home/runner/workspace/templates/base.html", line 126, in top-level template code
    {% block content %}{% endblock %}
    ^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/templates/movements/list.html", line 277, in block 'content'
    {% set total_value = movements.items | sum(attribute='net_value') %}
    ^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/async_utils.py", line 48, in wrapper
    return normal_func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/filters.py", line 1336, in sync_do_sum
    return sum(iterable, start)  # type: ignore[no-any-return, call-overload]
           ^^^^^^^^^^^^^^^^^^^^
TypeError: unsupported operand type(s) for +: 'int' and 'NoneType'
[2025-09-05 16:36:09 +0000] [793] [INFO] Handling signal: winch
ERROR:app:Exception on /movements [GET]
Traceback (most recent call last):
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 1511, in wsgi_app
    response = self.full_dispatch_request()
               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 919, in full_dispatch_request
    rv = self.handle_user_exception(e)
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 917, in full_dispatch_request
    rv = self.dispatch_request()
         ^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 902, in dispatch_request
    return self.ensure_sync(self.view_functions[rule.endpoint])(**view_args)  # type: ignore[no-any-return]
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask_login/utils.py", line 290, in decorated_view
    return current_app.ensure_sync(func)(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/routes.py", line 102, in movements
    return render_template('movements/list.html',
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/templating.py", line 150, in render_template
    return _render(app, template, context)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/templating.py", line 131, in _render
    rv = template.render(context)
         ^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/environment.py", line 1295, in render
    self.environment.handle_exception()
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/environment.py", line 942, in handle_exception
    raise rewrite_traceback_stack(source=source)
  File "/home/runner/workspace/templates/movements/list.html", line 1, in top-level template code
    {% extends "base.html" %}
  File "/home/runner/workspace/templates/base.html", line 126, in top-level template code
    {% block content %}{% endblock %}
    ^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/templates/movements/list.html", line 277, in block 'content'
    {% set total_value = movements.items | sum(attribute='net_value') %}
    ^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/async_utils.py", line 48, in wrapper
    return normal_func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/filters.py", line 1336, in sync_do_sum
    return sum(iterable, start)  # type: ignore[no-any-return, call-overload]
           ^^^^^^^^^^^^^^^^^^^^
TypeError: unsupported operand type(s) for +: 'int' and 'NoneType'
ERROR:app:Exception on /movements [GET]
Traceback (most recent call last):
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 1511, in wsgi_app
    response = self.full_dispatch_request()
               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 919, in full_dispatch_request
    rv = self.handle_user_exception(e)
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 917, in full_dispatch_request
    rv = self.dispatch_request()
         ^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 902, in dispatch_request
    return self.ensure_sync(self.view_functions[rule.endpoint])(**view_args)  # type: ignore[no-any-return]
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask_login/utils.py", line 290, in decorated_view
    return current_app.ensure_sync(func)(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/routes.py", line 102, in movements
    return render_template('movements/list.html',
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/templating.py", line 150, in render_template
    return _render(app, template, context)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/templating.py", line 131, in _render
    rv = template.render(context)
         ^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/environment.py", line 1295, in render
    self.environment.handle_exception()
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/environment.py", line 942, in handle_exception
    raise rewrite_traceback_stack(source=source)
  File "/home/runner/workspace/templates/movements/list.html", line 1, in top-level template code
    {% extends "base.html" %}
  File "/home/runner/workspace/templates/base.html", line 126, in top-level template code
    {% block content %}{% endblock %}
    ^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/templates/movements/list.html", line 277, in block 'content'
    {% set total_value = movements.items | sum(attribute='net_value') %}
    ^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/async_utils.py", line 48, in wrapper
    return normal_func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/filters.py", line 1336, in sync_do_sum
    return sum(iterable, start)  # type: ignore[no-any-return, call-overload]
           ^^^^^^^^^^^^^^^^^^^^
TypeError: unsupported operand type(s) for +: 'int' and 'NoneType'
ERROR:app:Exception on /movements/3739 [GET]
Traceback (most recent call last):
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 1511, in wsgi_app
    response = self.full_dispatch_request()
               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 919, in full_dispatch_request
    rv = self.handle_user_exception(e)
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 917, in full_dispatch_request
    rv = self.dispatch_request()
         ^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 902, in dispatch_request
    return self.ensure_sync(self.view_functions[rule.endpoint])(**view_args)  # type: ignore[no-any-return]
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask_login/utils.py", line 290, in decorated_view
    return current_app.ensure_sync(func)(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/routes.py", line 117, in movement_detail
    return render_template('movements/detail.html', movement=movement)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/templating.py", line 150, in render_template
    return _render(app, template, context)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/templating.py", line 131, in _render
    rv = template.render(context)
         ^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/environment.py", line 1295, in render
    self.environment.handle_exception()
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/environment.py", line 942, in handle_exception
    raise rewrite_traceback_stack(source=source)
  File "/home/runner/workspace/templates/movements/detail.html", line 1, in top-level template code
    {% extends "base.html" %}
  File "/home/runner/workspace/templates/base.html", line 126, in top-level template code
    {% block content %}{% endblock %}
    ^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/templates/movements/detail.html", line 463, in block 'content'
    <strong class="text-success">R$ {{ "%.2f"|format(item.value) }}</strong>
    ^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/filters.py", line 1043, in do_format
    return soft_str(value) % (kwargs or args)
           ~~~~~~~~~~~~~~~~^~~~~~~~~~~~~~~~~~
jinja2.exceptions.UndefinedError: 'dict object' has no attribute 'value'
^C[2025-09-05 16:37:54 +0000] [793] [INFO] Handling signal: int
[2025-09-05 16:37:55 +0000] [793] [INFO] Shutting down: Master

----- FILE: attached_assets/Pasted-INFO-Handling-signal-winch-ERROR-app-Exception-on-admin-dashboard-GET-Traceback-most-recent-ca-1755537314337_1755537314338.txt  (sha1:01cf3a3b51de, bytes:2835) -----
INFO] Handling signal: winch
ERROR:app:Exception on /admin/dashboard [GET]
Traceback (most recent call last):
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 1511, in wsgi_app
    response = self.full_dispatch_request()
               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 919, in full_dispatch_request
    rv = self.handle_user_exception(e)
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 917, in full_dispatch_request
    rv = self.dispatch_request()
         ^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 902, in dispatch_request
    return self.ensure_sync(self.view_functions[rule.endpoint])(**view_args)  # type: ignore[no-any-return]
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask_login/utils.py", line 290, in decorated_view
    return current_app.ensure_sync(func)(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/routes.py", line 36, in decorated_function
    return f(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/routes.py", line 127, in dashboard
    return render_template('admin/dashboard.html', stats=stats)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/templating.py", line 150, in render_template
    return _render(app, template, context)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/templating.py", line 131, in _render
    rv = template.render(context)
         ^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/environment.py", line 1295, in render
    self.environment.handle_exception()
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/environment.py", line 942, in handle_exception
    raise rewrite_traceback_stack(source=source)
  File "/home/runner/workspace/templates/admin/dashboard.html", line 1, in top-level template code
    {% extends "base.html" %}
  File "/home/runner/workspace/templates/base.html", line 126, in top-level template code
    {% block content %}{% endblock %}
    ^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/templates/admin/dashboard.html", line 144, in block 'content'
    {% if upload.failed_records > 0 %}
    ^^^^^^^^^^^^^^^^^^^^^^^^^
jinja2.exceptions.UndefinedError: 'models.UploadHistory object' has no attribute 'failed_records'

----- FILE: attached_assets/Pasted-INFO-file-processor-Reading-Excel-file-uploads-a95c624a-ed79-4640-9c65-1a624b0c03a3-rm-movimentos-U-1756245935776_1756245935777.txt  (sha1:6b5b2242247e, bytes:5836) -----
INFO:file_processor:Reading Excel file: uploads/a95c624a-ed79-4640-9c65-1a624b0c03a3_rm_movimentos_ULTIMO_DIA_empresas_4_148.xlsx
INFO:file_processor:Read Excel file with 210 rows
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
WARNING:file_processor:Both JSON and literal_eval failed for value: [{'companyId': 1, 'movementId': 169, 'sequentialId': 1, 'sequentialNumber': 1, 'productId': 843, 'qu... Error: unterminated string literal (detected at line 1) (<unknown>, line 1)
INFO:file_processor:Saved 210 records to database with batch_id c93a7db9-d33f-4f26-950e-b7d3f34934d2
[2025-08-26 22:05:10 +0000] [142] [INFO] Handling signal: winch
[2025-08-26 22:05:10 +0000] [142] [INFO] Handling signal: winch
[2025-08-26 22:05:10 +0000] [142] [INFO] Handling signal: winch
[2025-08-26 22:05:10 +0000] [142] [INFO] Handling signal: winch
[2025-08-26 22:05:11 +0000] [142] [INFO] Handling signal: winch

----- FILE: attached_assets/Pasted-O-Handling-signal-winch-INFO-file-processor-Reading-Excel-file-uploads-43a4e8b8-5aca-471d-a2a7-cd-1755528724777_1755528724777.txt  (sha1:f8b40ad75060, bytes:3134) -----
O] Handling signal: winch
INFO:file_processor:Reading Excel file: uploads/43a4e8b8-5aca-471d-a2a7-cda9a5fd54d1_pagina_unica_148_1.xlsx
INFO:file_processor:Read Excel file with 50 rows
INFO:file_processor:Saved 50 records to database with batch_id 64459794-e416-4a28-9e4f-0b6fa4237c64
ERROR:app:Exception on /admin/upload-history [GET]
Traceback (most recent call last):
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 1511, in wsgi_app
    response = self.full_dispatch_request()
               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 919, in full_dispatch_request
    rv = self.handle_user_exception(e)
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 917, in full_dispatch_request
    rv = self.dispatch_request()
         ^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/app.py", line 902, in dispatch_request
    return self.ensure_sync(self.view_functions[rule.endpoint])(**view_args)  # type: ignore[no-any-return]
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask_login/utils.py", line 290, in decorated_view
    return current_app.ensure_sync(func)(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/routes.py", line 36, in decorated_function
    return f(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/routes.py", line 252, in upload_history
    return render_template('admin/upload.html', uploads=uploads, show_history=True)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/templating.py", line 150, in render_template
    return _render(app, template, context)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/flask/templating.py", line 131, in _render
    rv = template.render(context)
         ^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/environment.py", line 1295, in render
    self.environment.handle_exception()
  File "/home/runner/workspace/.pythonlibs/lib/python3.11/site-packages/jinja2/environment.py", line 942, in handle_exception
    raise rewrite_traceback_stack(source=source)
  File "/home/runner/workspace/templates/admin/upload.html", line 1, in top-level template code
    {% extends "base.html" %}
  File "/home/runner/workspace/templates/base.html", line 126, in top-level template code
    {% block content %}{% endblock %}
    ^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/workspace/templates/admin/upload.html", line 185, in block 'content'
    {% if upload.failed_records > 0 %}
    ^^^^^^^^^^^^^^^^^^^^^^^^^
jinja2.exceptions.UndefinedError: 'models.UploadHistory object' has no attribute 'failed_records'

----- FILE: migrations/alembic.ini  (sha1:6b652007c8ee, bytes:857) -----
# A generic, single database configuration.

[alembic]
# template used to generate migration files
# file_template = %%(rev)s_%%(slug)s

# set to 'true' to run the environment during
# the 'revision' command, regardless of autogenerate
# revision_environment = false


# Logging configuration
[loggers]
keys = root,sqlalchemy,alembic,flask_migrate

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console
qualname =

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[logger_flask_migrate]
level = INFO
handlers =
qualname = flask_migrate

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S


----- FILE: migrations/env.py  (sha1:14d8f8952082, bytes:3344) -----
import logging
from logging.config import fileConfig

from flask import current_app

from alembic import context

# this is the Alembic Config object, which provides
# access to the values within the .ini file in use.
config = context.config

# Interpret the config file for Python logging.
# This line sets up loggers basically.
fileConfig(config.config_file_name)
logger = logging.getLogger('alembic.env')


def get_engine():
    try:
        # this works with Flask-SQLAlchemy<3 and Alchemical
        return current_app.extensions['migrate'].db.get_engine()
    except (TypeError, AttributeError):
        # this works with Flask-SQLAlchemy>=3
        return current_app.extensions['migrate'].db.engine


def get_engine_url():
    try:
        return get_engine().url.render_as_string(hide_password=False).replace(
            '%', '%%')
    except AttributeError:
        return str(get_engine().url).replace('%', '%%')


# add your model's MetaData object here
# for 'autogenerate' support
# from myapp import mymodel
# target_metadata = mymodel.Base.metadata
config.set_main_option('sqlalchemy.url', get_engine_url())
target_db = current_app.extensions['migrate'].db

# other values from the config, defined by the needs of env.py,
# can be acquired:
# my_important_option = config.get_main_option("my_important_option")
# ... etc.


def get_metadata():
    if hasattr(target_db, 'metadatas'):
        return target_db.metadatas[None]
    return target_db.metadata


def run_migrations_offline():
    """Run migrations in 'offline' mode.

    This configures the context with just a URL
    and not an Engine, though an Engine is acceptable
    here as well.  By skipping the Engine creation
    we don't even need a DBAPI to be available.

    Calls to context.execute() here emit the given string to the
    script output.

    """
    url = config.get_main_option("sqlalchemy.url")
    context.configure(
        url=url, target_metadata=get_metadata(), literal_binds=True
    )

    with context.begin_transaction():
        context.run_migrations()


def run_migrations_online():
    """Run migrations in 'online' mode.

    In this scenario we need to create an Engine
    and associate a connection with the context.

    """

    # this callback is used to prevent an auto-migration from being generated
    # when there are no changes to the schema
    # reference: http://alembic.zzzcomputing.com/en/latest/cookbook.html
    def process_revision_directives(context, revision, directives):
        if getattr(config.cmd_opts, 'autogenerate', False):
            script = directives[0]
            if script.upgrade_ops.is_empty():
                directives[:] = []
                logger.info('No changes in schema detected.')

    conf_args = current_app.extensions['migrate'].configure_args
    if conf_args.get("process_revision_directives") is None:
        conf_args["process_revision_directives"] = process_revision_directives

    connectable = get_engine()

    with connectable.connect() as connection:
        context.configure(
            connection=connection,
            target_metadata=get_metadata(),
            **conf_args
        )

        with context.begin_transaction():
            context.run_migrations()


if context.is_offline_mode():
    run_migrations_offline()
else:
    run_migrations_online()


----- FILE: migrations/versions/80fc0baf7fe3_add_customer_vendor_name_and_cnpj_to_.py  (sha1:4a228821e108, bytes:1019) -----
"""add customer_vendor_name and cnpj to FinancialMovement

Revision ID: 80fc0baf7fe3
Revises: 
Create Date: 2025-10-27 18:52:46.020242

"""
from alembic import op
import sqlalchemy as sa


# revision identifiers, used by Alembic.
revision = '80fc0baf7fe3'
down_revision = None
branch_labels = None
depends_on = None


def upgrade():
    # ### commands auto generated by Alembic - please adjust! ###
    with op.batch_alter_table('financial_movement', schema=None) as batch_op:
        batch_op.add_column(sa.Column('customer_vendor_name', sa.String(length=255), nullable=True))
        batch_op.add_column(sa.Column('customer_vendor_cnpj', sa.String(length=30), nullable=True))

    # ### end Alembic commands ###


def downgrade():
    # ### commands auto generated by Alembic - please adjust! ###
    with op.batch_alter_table('financial_movement', schema=None) as batch_op:
        batch_op.drop_column('customer_vendor_cnpj')
        batch_op.drop_column('customer_vendor_name')

    # ### end Alembic commands ###


----- FILE: static/css/style.css  (sha1:fa688f7c9dd7, bytes:5794) -----
/* Custom styles for Financial Data Management System */
/* Following Bootstrap 5 dark theme variables and minimal custom overrides */

:root {
    --custom-border-radius: 0.375rem;
    --custom-box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075);
    --custom-transition: all 0.15s ease-in-out;
}

/* Global improvements */
body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}

/* Custom card enhancements */
.card {
    border: 1px solid var(--bs-border-color);
    box-shadow: var(--custom-box-shadow);
    transition: var(--custom-transition);
}

.card:hover {
    box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);
}

/* Enhanced navigation */
.navbar-brand {
    font-weight: 600;
}

/* Table improvements */
.table-responsive {
    border-radius: var(--custom-border-radius);
}

.table th {
    font-weight: 600;
    border-top: none;
}

/* Code elements */
code {
    color: var(--bs-info);
    background-color: rgba(var(--bs-info-rgb), 0.1);
    padding: 0.125rem 0.25rem;
    border-radius: 0.25rem;
    font-size: 0.875em;
}

/* Badge improvements */
.badge {
    font-weight: 500;
}

/* Button enhancements */
.btn {
    font-weight: 500;
    transition: var(--custom-transition);
}

.btn-group .btn {
    border-radius: 0;
}

.btn-group .btn:first-child {
    border-top-left-radius: var(--custom-border-radius);
    border-bottom-left-radius: var(--custom-border-radius);
}

.btn-group .btn:last-child {
    border-top-right-radius: var(--custom-border-radius);
    border-bottom-right-radius: var(--custom-border-radius);
}

/* Form improvements */
.form-control:focus,
.form-select:focus {
    border-color: var(--bs-primary);
    box-shadow: 0 0 0 0.2rem rgba(var(--bs-primary-rgb), 0.25);
}

.form-label {
    font-weight: 500;
    margin-bottom: 0.5rem;
}

/* Alert enhancements */
.alert {
    border: none;
    border-radius: var(--custom-border-radius);
}

.alert .fas,
.alert .far {
    width: 1.25rem;
    text-align: center;
}

/* Jumbotron style for hero sections */
.jumbotron {
    background: linear-gradient(135deg, var(--bs-primary), var(--bs-info));
    border-radius: var(--custom-border-radius);
}

/* Pagination improvements */
.pagination .page-link {
    border-radius: var(--custom-border-radius);
    margin: 0 0.125rem;
    border: 1px solid var(--bs-border-color);
}

.pagination .page-item.active .page-link {
    background-color: var(--bs-primary);
    border-color: var(--bs-primary);
}

/* Statistics cards */
.card.text-white .card-footer a:hover {
    text-decoration: underline !important;
}

/* Accordion improvements */
.accordion-button:not(.collapsed) {
    background-color: rgba(var(--bs-primary-rgb), 0.1);
    color: var(--bs-primary);
}

.accordion-button:focus {
    box-shadow: 0 0 0 0.2rem rgba(var(--bs-primary-rgb), 0.25);
}

/* File upload area */
.form-control[type="file"] {
    padding: 0.75rem;
}

/* DataTables integration */
.dataTables_wrapper .dataTables_length select,
.dataTables_wrapper .dataTables_filter input {
    background-color: var(--bs-body-bg);
    border: 1px solid var(--bs-border-color);
    color: var(--bs-body-color);
}

.dataTables_wrapper .dataTables_paginate .paginate_button {
    border: 1px solid var(--bs-border-color);
    background-color: var(--bs-body-bg);
    color: var(--bs-body-color) !important;
}

.dataTables_wrapper .dataTables_paginate .paginate_button:hover {
    background-color: var(--bs-secondary-bg);
    border-color: var(--bs-border-color);
}

.dataTables_wrapper .dataTables_paginate .paginate_button.current {
    background-color: var(--bs-primary) !important;
    border-color: var(--bs-primary) !important;
    color: white !important;
}

/* Loading states */
.loading {
    opacity: 0.6;
    pointer-events: none;
}

.spinner-border-sm {
    width: 1rem;
    height: 1rem;
}

/* Mobile responsiveness improvements */
@media (max-width: 768px) {
    .d-md-flex {
        flex-direction: column;
        gap: 0.5rem;
    }
    
    .btn-group {
        flex-direction: column;
    }
    
    .btn-group .btn {
        border-radius: var(--custom-border-radius) !important;
        margin-bottom: 0.25rem;
    }
    
    .card-body .row > div {
        margin-bottom: 1rem;
    }
}

/* Print styles */
@media print {
    .navbar,
    .btn,
    .pagination,
    .card-footer,
    .alert,
    .badge {
        display: none !important;
    }
    
    .card {
        border: 1px solid #000;
        box-shadow: none;
    }
    
    .table {
        font-size: 0.8rem;
    }
}

/* Custom utility classes */
.text-truncate-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

.border-start-primary {
    border-left: 0.25rem solid var(--bs-primary) !important;
}

.border-start-success {
    border-left: 0.25rem solid var(--bs-success) !important;
}

.border-start-warning {
    border-left: 0.25rem solid var(--bs-warning) !important;
}

.border-start-danger {
    border-left: 0.25rem solid var(--bs-danger) !important;
}

/* Animation utilities */
@keyframes fadeIn {
    from { opacity: 0; transform: translateY(10px); }
    to { opacity: 1; transform: translateY(0); }
}

.fade-in {
    animation: fadeIn 0.3s ease-in-out;
}

/* Focus improvements for accessibility */
.btn:focus,
.form-control:focus,
.form-select:focus {
    outline: 2px solid var(--bs-primary);
    outline-offset: 2px;
}

/* High contrast mode support */
@media (prefers-contrast: high) {
    .card {
        border-width: 2px;
    }
    
    .btn {
        border-width: 2px;
    }
}

/* Reduced motion support */
@media (prefers-reduced-motion: reduce) {
    * {
        animation-duration: 0.01ms !important;
        animation-iteration-count: 1 !important;
        transition-duration: 0.01ms !important;
    }
}


----- FILE: static/js/main.js  (sha1:5731a14c205e, bytes:13840) -----
// Main JavaScript file for Financial Data Management System
// Enhanced functionality for better user experience

$(document).ready(function() {
    // Initialize all components
    initializeComponents();
    initializeDataTables();
    initializeFormValidation();
    initializeTooltips();
    initializeFileUpload();
    initializeSearchEnhancements();
});

// Initialize main components
function initializeComponents() {
    // Add fade-in animation to cards
    $('.card').addClass('fade-in');
    
    // Enhanced dropdown behaviors
    $('.dropdown-toggle').on('click', function(e) {
        e.stopPropagation();
    });
    
    // Auto-dismiss alerts after 5 seconds
    setTimeout(function() {
        $('.alert').alert('close');
    }, 5000);
    
    // Smooth scrolling for anchor links
    $('a[href^="#"]').on('click', function(e) {
        e.preventDefault();
        const target = $($(this).attr('href'));
        if (target.length) {
            $('html, body').animate({
                scrollTop: target.offset().top - 70
            }, 500);
        }
    });
}

// Initialize DataTables with enhanced features
function initializeDataTables() {
    // Common DataTable configuration
    const commonConfig = {
        responsive: true,
        pageLength: 25,
        lengthMenu: [[10, 25, 50, 100, -1], [10, 25, 50, 100, "All"]],
        language: {
            search: "Buscar movimentos:",
            lengthMenu: "Mostrar _MENU_ registros por página",
            info: "Exibindo _START_ a _END_ de _TOTAL_ registros",
            infoEmpty: "Nenhum registro disponível",
            infoFiltered: "(filtrado de _MAX_ registros no total)",
            paginate: {
                first: "Primeiro",
                last: "Último",
                next: "Próximo",
                previous: "Anterior"
            }
        },

        dom: '<"row"<"col-sm-12 col-md-6"l><"col-sm-12 col-md-6"f>>' +
             '<"row"<"col-sm-12"tr>>' +
             '<"row"<"col-sm-12 col-md-5"i><"col-sm-12 col-md-7"p>>',
        initComplete: function() {
            // Add Bootstrap styling to DataTables elements
            $('.dataTables_length select').addClass('form-select form-select-sm');
            $('.dataTables_filter input').addClass('form-control form-control-sm');
        }
    };
    
    // Initialize specific tables
    if ($('#movementsTable').length) {
        $('#movementsTable').DataTable({
            ...commonConfig,
            order: [[7, "desc"]], // Sort by uploaded date
            columnDefs: [
                { targets: [-1], orderable: false }, // Disable sorting on actions column
                { targets: [4], type: 'currency' }, // Currency sorting for net value
            ]
        });
    }
    
    if ($('#usersTable').length) {
        $('#usersTable').DataTable({
            ...commonConfig,
            order: [[3, "desc"]], // Sort by created date
            columnDefs: [
                { targets: [-1], orderable: false }, // Disable sorting on actions column
            ]
        });
    }
    
    if ($('#uploadsTable').length) {
        $('#uploadsTable').DataTable({
            ...commonConfig,
            order: [[5, "desc"]], // Sort by upload date
            columnDefs: [
                { targets: [-1], orderable: false }, // Disable sorting on actions column
            ]
        });
    }
}

// Initialize form validation
function initializeFormValidation() {
    // Real-time validation for forms
    $('form').on('submit', function(e) {
        const form = $(this);
        let isValid = true;
        
        // Check required fields
        form.find('[required]').each(function() {
            const field = $(this);
            if (!field.val().trim()) {
                field.addClass('is-invalid');
                isValid = false;
            } else {
                field.removeClass('is-invalid');
            }
        });
        
        // Email validation
        form.find('input[type="email"]').each(function() {
            const email = $(this);
            const emailPattern = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
            if (email.val() && !emailPattern.test(email.val())) {
                email.addClass('is-invalid');
                isValid = false;
            }
        });
        
        // Password confirmation
        const password = form.find('input[name="password"]');
        const password2 = form.find('input[name="password2"]');
        if (password.length && password2.length) {
            if (password.val() !== password2.val()) {
                password2.addClass('is-invalid');
                isValid = false;
            }
        }
        
        if (!isValid) {
            e.preventDefault();
            showNotification('Please correct the highlighted fields', 'error');
        }
    });
    
    // Remove validation classes on input
    $('input, select, textarea').on('input change', function() {
        $(this).removeClass('is-invalid');
    });
}

// Initialize tooltips
function initializeTooltips() {
    // Enable Bootstrap tooltips
    const tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'));
    tooltipTriggerList.map(function(tooltipTriggerEl) {
        return new bootstrap.Tooltip(tooltipTriggerEl);
    });
    
    // Add tooltips to truncated text
    $('.text-truncate, .text-truncate-2').each(function() {
        const element = $(this);
        if (element[0].scrollWidth > element[0].clientWidth) {
            element.attr('data-bs-toggle', 'tooltip');
            element.attr('title', element.text());
            new bootstrap.Tooltip(element[0]);
        }
    });
}

// Initialize file upload enhancements
function initializeFileUpload() {
    $('input[type="file"]').on('change', function() {
        const file = this.files[0];
        const fileInfo = $(this).siblings('.file-info');
        
        if (file) {
            const size = (file.size / (1024 * 1024)).toFixed(2);
            const info = `Selected: ${file.name} (${size} MB)`;
            
            if (fileInfo.length) {
                fileInfo.text(info);
            } else {
                $(this).after(`<div class="file-info text-muted mt-1">${info}</div>`);
            }
            
            // Validate file size (50MB limit)
            if (file.size > 50 * 1024 * 1024) {
                showNotification('File size exceeds 50MB limit', 'error');
                $(this).val('');
                $(this).siblings('.file-info').remove();
            }
        } else {
            fileInfo.remove();
        }
    });
    
    // Drag and drop enhancement
    $('.form-control[type="file"]').each(function() {
        const fileInput = $(this);
        const dropZone = $('<div class="drop-zone border-2 border-dashed p-4 text-center rounded mt-2">' +
                          '<i class="fas fa-cloud-upload-alt fa-2x text-muted"></i><br>' +
                          '<span class="text-muted">Drag and drop files here or click to browse</span>' +
                          '</div>');
        
        fileInput.after(dropZone);
        fileInput.hide();
        
        dropZone.on('click', function() {
            fileInput.click();
        });
        
        dropZone.on('dragover', function(e) {
            e.preventDefault();
            $(this).addClass('border-primary bg-primary bg-opacity-10');
        });
        
        dropZone.on('dragleave', function() {
            $(this).removeClass('border-primary bg-primary bg-opacity-10');
        });
        
        dropZone.on('drop', function(e) {
            e.preventDefault();
            $(this).removeClass('border-primary bg-primary bg-opacity-10');
            
            const files = e.originalEvent.dataTransfer.files;
            if (files.length > 0) {
                fileInput[0].files = files;
                fileInput.trigger('change');
            }
        });
    });
}

// Initialize search enhancements
function initializeSearchEnhancements() {
    // Auto-complete for search fields
    $('input[name="search"]').on('input', function() {
        const query = $(this).val();
        if (query.length >= 2) {
            // Debounce search suggestions
            clearTimeout(this.searchTimeout);
            this.searchTimeout = setTimeout(() => {
                // You can implement search suggestions here
                console.log('Search suggestions for:', query);
            }, 300);
        }
    });
    
    // Advanced search toggle
    $('.advanced-search-toggle').on('click', function() {
        $('.advanced-search-panel').slideToggle();
    });
    
    // Search history (localStorage)
    const searchHistory = JSON.parse(localStorage.getItem('searchHistory') || '[]');
    
    $('form').on('submit', function() {
        const searchInput = $(this).find('input[name="search"]');
        if (searchInput.length && searchInput.val().trim()) {
            const query = searchInput.val().trim();
            if (!searchHistory.includes(query)) {
                searchHistory.unshift(query);
                searchHistory.splice(10); // Keep only last 10 searches
                localStorage.setItem('searchHistory', JSON.stringify(searchHistory));
            }
        }
    });
}

// Utility functions
function showNotification(message, type = 'info') {
    const alertClass = type === 'error' ? 'alert-danger' : `alert-${type}`;
    const icon = type === 'error' ? 'fas fa-exclamation-triangle' : 
                 type === 'success' ? 'fas fa-check-circle' : 'fas fa-info-circle';
    
    const alert = $(`
        <div class="alert ${alertClass} alert-dismissible fade show" role="alert">
            <i class="${icon} me-2"></i>
            ${message}
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    `);
    
    // Insert at top of main container
    $('main.container').prepend(alert);
    
    // Auto-dismiss after 5 seconds
    setTimeout(() => {
        alert.alert('close');
    }, 5000);
}

function formatCurrency(amount, currency = 'R$') {
    return `${currency} ${parseFloat(amount).toFixed(2)}`;
}

function formatDate(dateString) {
    if (!dateString) return '-';
    const date = new Date(dateString);
    return date.toLocaleDateString('pt-BR');
}

function formatDateTime(dateString) {
    if (!dateString) return '-';
    const date = new Date(dateString);
    return date.toLocaleString('pt-BR');
}

// Export functionality
function exportTableToCSV(tableId, filename = 'export.csv') {
    const table = document.getElementById(tableId);
    if (!table) return;
    
    const rows = Array.from(table.querySelectorAll('tr'));
    const csv = rows.map(row => {
        const cells = Array.from(row.querySelectorAll('td, th'));
        return cells.slice(0, -1).map(cell => { // Exclude actions column
            let text = cell.textContent.trim();
            // Handle commas and quotes in CSV
            if (text.includes(',') || text.includes('"')) {
                text = '"' + text.replace(/"/g, '""') + '"';
            }
            return text;
        }).join(',');
    }).join('\n');
    
    // Download CSV
    const blob = new Blob([csv], { type: 'text/csv' });
    const url = window.URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = filename;
    a.style.display = 'none';
    document.body.appendChild(a);
    a.click();
    document.body.removeChild(a);
    window.URL.revokeObjectURL(url);
}

// JSON data formatting for display
function formatJSONField(data, fieldName) {
    if (!data || data.length === 0) {
        return '<span class="text-muted">No data</span>';
    }
    
    if (Array.isArray(data)) {
        return `<span class="badge bg-info">${data.length} items</span>`;
    }
    
    return '<span class="badge bg-secondary">Object</span>';
}

// Performance monitoring
function measurePerformance(name, fn) {
    const start = performance.now();
    const result = fn();
    const end = performance.now();
    console.log(`${name} took ${end - start} milliseconds`);
    return result;
}

// Error handling
window.addEventListener('error', function(e) {
    console.error('JavaScript error:', e.error);
    // showNotification('An unexpected error occurred. Please refresh the page.', 'error');
});

// Keyboard shortcuts
document.addEventListener('keydown', function(e) {
    // Ctrl+/ or Cmd+/ for search focus
    if ((e.ctrlKey || e.metaKey) && e.key === '/') {
        e.preventDefault();
        const searchInput = document.querySelector('input[name="search"]');
        if (searchInput) {
            searchInput.focus();
        }
    }
    
    // Escape to clear search
    if (e.key === 'Escape') {
        const searchInput = document.querySelector('input[name="search"]:focus');
        if (searchInput) {
            searchInput.value = '';
            searchInput.blur();
        }
    }
});

// Mobile enhancements
if (window.innerWidth <= 768) {
    // Mobile-specific enhancements
    $(document).ready(function() {
        // Collapse long text on mobile
        $('.table td').each(function() {
            const text = $(this).text();
            if (text.length > 30) {
                $(this).attr('title', text);
                $(this).text(text.substring(0, 30) + '...');
            }
        });
        
        // Mobile navigation improvements
        $('.navbar-toggler').on('click', function() {
            $('body').toggleClass('nav-open');
        });
    });
}

// Progressive Web App features
if ('serviceWorker' in navigator) {
    window.addEventListener('load', function() {
        // Service worker registration would go here
        console.log('Service Worker support detected');
    });
}


----- FILE: templates/base.html  (sha1:256838da5a60, bytes:6867) -----
<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Financial Data Management{% endblock %}</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.replit.com/agent/bootstrap-agent-dark-theme.min.css" rel="stylesheet">
    
    <!-- DataTables CSS -->
    <link href="https://cdn.datatables.net/1.13.6/css/dataTables.bootstrap5.min.css" rel="stylesheet">
    
    <!-- Font Awesome Icons -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    
    <!-- Custom CSS -->
    <link href="{{ url_for('static', filename='css/style.css') }}" rel="stylesheet">
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark border-bottom">
        <div class="container">
            <a class="navbar-brand" href="{{ url_for('main.index') }}">
                <i class="fas fa-chart-line me-2"></i>
                Financial Data Management
            </a>
            
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ url_for('main.index') }}">
                            <i class="fas fa-home me-1"></i>Home
                        </a>
                    </li>
                    {% if current_user.is_authenticated %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ url_for('main.movements') }}">
                            <i class="fas fa-list me-1"></i>Movements
                        </a>
                    </li>
                    {% endif %}
                    {% if current_user.is_authenticated and current_user.is_admin %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                            <i class="fas fa-cog me-1"></i>Admin
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="{{ url_for('admin.dashboard') }}">
                                <i class="fas fa-tachometer-alt me-2"></i>Dashboard
                            </a></li>
                            <li><a class="dropdown-item" href="{{ url_for('admin.users') }}">
                                <i class="fas fa-users me-2"></i>Users
                            </a></li>
                            <li><a class="dropdown-item" href="{{ url_for('admin.upload_file') }}">
                                <i class="fas fa-upload me-2"></i>Upload Data
                            </a></li>
                            <li><a class="dropdown-item" href="{{ url_for('admin.upload_history') }}">
                                <i class="fas fa-history me-2"></i>Upload History
                            </a></li>
                        </ul>
                    </li>
                    {% endif %}
                </ul>
                
                <ul class="navbar-nav">
                    {% if current_user.is_authenticated %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                            <i class="fas fa-user me-1"></i>{{ current_user.username }}
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li><h6 class="dropdown-header">
                                {{ current_user.email }}
                                {% if current_user.is_admin %}
                                <span class="badge bg-primary ms-2">Admin</span>
                                {% endif %}
                            </h6></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="{{ url_for('auth.logout') }}">
                                <i class="fas fa-sign-out-alt me-2"></i>Logout
                            </a></li>
                        </ul>
                    </li>
                    {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ url_for('auth.login') }}">
                            <i class="fas fa-sign-in-alt me-1"></i>Login
                        </a>
                    </li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </nav>

    <!-- Flash Messages -->
        <!-- Flash Messages -->
    {% with messages = get_flashed_messages(with_categories=true) %}
        {% if messages %}
            {% for category, message in messages %}
                {% if "An unexpected error occurred" not in message %}
                    <div class="alert alert-{{ category }} alert-dismissible fade show" role="alert">
                        {{ message }}
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                {% endif %}
            {% endfor %}
        {% endif %}
    {% endwith %}



    <!-- Main Content -->
    <main class="container mt-4">
        {% block content %}{% endblock %}
    </main>

    <!-- Footer -->
    <footer class="bg-dark text-light py-4 mt-5">
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <h5>Financial Data Management</h5>
                    <p class="text-muted">Comprehensive financial movement tracking and analysis.</p>
                </div>
                <div class="col-md-6 text-end">
                    <p class="text-muted">
                        &copy; 2025 Financial Data Management System
                    </p>
                </div>
            </div>
        </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- jQuery -->
    <script src="https://code.jquery.com/jquery-3.7.0.min.js"></script>
    
    <!-- DataTables JS -->
    <script src="https://cdn.datatables.net/1.13.6/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/1.13.6/js/dataTables.bootstrap5.min.js"></script>
    
    <!-- Custom JS -->
    <script src="{{ url_for('static', filename='js/main.js') }}"></script>
    
    {% block scripts %}{% endblock %}
</body>
</html>


----- FILE: templates/index.html  (sha1:a62c87b17a05, bytes:8231) -----
{% extends "base.html" %}

{% block title %}Home - Financial Data Management{% endblock %}

{% block content %}
<div class="row">
    <div class="col-12">
        <div class="jumbotron bg-primary text-white rounded p-5 mb-4">
            <h1 class="display-4">
                <i class="fas fa-chart-line me-3"></i>
                Financial Data Management
            </h1>
            <p class="lead">
                Comprehensive system for managing and analyzing financial movement data
            </p>
            {% if not current_user.is_authenticated %}
            <a class="btn btn-light btn-lg" href="{{ url_for('auth.login') }}" role="button">
                <i class="fas fa-sign-in-alt me-2"></i>Get Started
            </a>
            {% endif %}
        </div>
    </div>
</div>

{% if current_user.is_authenticated %}
<div class="row mb-4">
    <div class="col-md-4">
        <div class="card">
            <div class="card-body text-center">
                <i class="fas fa-list fa-3x text-primary mb-3"></i>
                <h5 class="card-title">View Movements</h5>
                <p class="card-text">Browse and search through financial movement records</p>
                <a href="{{ url_for('main.movements') }}" class="btn btn-primary">
                    <i class="fas fa-arrow-right me-2"></i>Browse
                </a>
            </div>
        </div>
    </div>
    
    {% if current_user.is_admin %}
    <div class="col-md-4">
        <div class="card">
            <div class="card-body text-center">
                <i class="fas fa-upload fa-3x text-success mb-3"></i>
                <h5 class="card-title">Upload Data</h5>
                <p class="card-text">Import financial data from Excel files</p>
                <a href="{{ url_for('admin.upload_file') }}" class="btn btn-success">
                    <i class="fas fa-upload me-2"></i>Upload
                </a>
            </div>
        </div>
    </div>
    
    <div class="col-md-4">
        <div class="card">
            <div class="card-body text-center">
                <i class="fas fa-users fa-3x text-info mb-3"></i>
                <h5 class="card-title">Manage Users</h5>
                <p class="card-text">Create and manage user accounts</p>
                <a href="{{ url_for('admin.users') }}" class="btn btn-info">
                    <i class="fas fa-cog me-2"></i>Manage
                </a>
            </div>
        </div>
    </div>
    {% endif %}
</div>

{% if movements %}
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header d-flex justify-content-between align-items-center">
                <h5 class="mb-0">
                    <i class="fas fa-clock me-2"></i>Recent Movements
                </h5>
                <a href="{{ url_for('main.movements') }}" class="btn btn-outline-primary btn-sm">
                    View All
                </a>
            </div>
            <div class="card-body p-0">
                <div class="table-responsive">
                    <table class="table table-hover mb-0">
                        <thead class="table-dark">
                            <tr>
                                <th>N° do documento fiscal</th>
                                <th>Cliente/Fornecedor</th>
                                <th>Valor total da nota</th>
                                <th>Data</th>
                                <th>Ações</th>
                                <th>ID interno</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for movement in movements %}
                            <tr>
                                <td>{{ movement.number or '-' }}</td>
                                <td>
                                    {% if movement.customer_vendor_name and movement.customer_vendor_cnpj %}
                                        {{ movement.customer_vendor_name }}<br>
                                        <small class="text-muted">CNPJ: {{ movement.customer_vendor_cnpj }}</small>
                                    {% elif movement.aux_customer_vendor_code %}
                                        <strong>{{ movement.aux_customer_vendor_code }}</strong>
                                    {% else %}
                                        -
                                    {% endif %}
                                </td>
                                <td>
                                    {% if movement.gross_value is not none %}
                                        <span class="badge bg-success">
                                            {{ movement.net_value_currency_code or 'R$' }} {{ "%.2f"|format(movement.gross_value) }}
                                        </span>
                                    {% else %}
                                        -
                                    {% endif %}
                                </td>
                                <td>
                                    {% if movement.date %}
                                        {{ movement.date.strftime('%d/%m/%Y') }}
                                    {% else %}
                                        -
                                    {% endif %}
                                </td>
                                <td>
                                    <a href="{{ url_for('main.movement_detail', id=movement.id) }}" class="btn btn-outline-primary btn-sm">
                                        <i class="fas fa-eye me-1"></i>Ver
                                    </a>
                                </td>
                                <td><code>{{ movement.internal_id or '-' }}</code></td>
                            </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
{% endif %}

{% else %}
<!-- Not authenticated content -->
<div class="row">
    <div class="col-lg-6">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">
                    <i class="fas fa-database me-2 text-primary"></i>
                    Data Management
                </h5>
                <p class="card-text">
                    Efficiently manage large volumes of financial movement data with advanced search and filtering capabilities.
                </p>
                <ul class="list-unstyled">
                    <li><i class="fas fa-check text-success me-2"></i>Bulk Excel file uploads</li>
                    <li><i class="fas fa-check text-success me-2"></i>Complex data structure support</li>
                    <li><i class="fas fa-check text-success me-2"></i>Real-time data validation</li>
                </ul>
            </div>
        </div>
    </div>
    <div class="col-lg-6">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">
                    <i class="fas fa-analytics me-2 text-info"></i>
                    Advanced Analytics
                </h5>
                <p class="card-text">
                    Comprehensive view of financial movements with detailed breakdowns and nested data visualization.
                </p>
                <ul class="list-unstyled">
                    <li><i class="fas fa-check text-success me-2"></i>Detailed movement records</li>
                    <li><i class="fas fa-check text-success me-2"></i>JSON field parsing</li>
                    <li><i class="fas fa-check text-success me-2"></i>Export capabilities</li>
                </ul>
            </div>
        </div>
    </div>
</div>

<div class="text-center mt-4">
    <p class="lead">Ready to get started?</p>
    <a href="{{ url_for('auth.login') }}" class="btn btn-primary btn-lg">
        <i class="fas fa-sign-in-alt me-2"></i>Login Now
    </a>
</div>
{% endif %}

{% endblock %}


----- FILE: templates/admin/clear_movements.html  (sha1:bedbcbed17dd, bytes:7183) -----
{% extends "base.html" %}

{% block title %}Clear All Movements - Admin{% endblock %}

{% block content %}
<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <h2 class="mb-4">
                <i class="fas fa-exclamation-triangle text-warning me-2"></i>
                Clear All Financial Movements
            </h2>
        </div>
    </div>
    
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="alert alert-danger">
                <h5 class="alert-heading">
                    <i class="fas fa-skull-crossbones me-2"></i>
                    DANGER ZONE
                </h5>
                <p class="mb-0">This action will permanently delete ALL financial movement data and upload history from the database. This action cannot be undone!</p>
            </div>
            
            <div class="card border-danger">
                <div class="card-header bg-danger text-white">
                    <h5 class="mb-0">
                        <i class="fas fa-database me-2"></i>
                        Database Status
                    </h5>
                </div>
                <div class="card-body">
                    <div class="row text-center">
                        <div class="col-md-6">
                            <div class="card bg-primary bg-opacity-10 border-primary">
                                <div class="card-body">
                                    <h2 class="text-primary">{{ movement_count }}</h2>
                                    <p class="mb-0">Financial Movements</p>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="card bg-info bg-opacity-10 border-info">
                                <div class="card-body">
                                    <h2 class="text-info">{{ upload_count }}</h2>
                                    <p class="mb-0">Upload Records</p>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    {% if movement_count == 0 and upload_count == 0 %}
                    <div class="alert alert-success mt-3">
                        <i class="fas fa-check-circle me-2"></i>
                        Database is already empty. No data to delete.
                    </div>
                    {% else %}
                    <form method="POST" class="mt-4">
                        <div class="alert alert-warning">
                            <h6 class="alert-heading">Confirmation Required</h6>
                            <p class="mb-3">
                                To confirm deletion of ALL financial movement data, type exactly:
                                <strong class="text-danger">"delete all movements"</strong>
                            </p>
                            
                            <div class="form-group">
                                <input type="text" 
                                       name="confirmation" 
                                       class="form-control form-control-lg" 
                                       placeholder="Type: delete all movements"
                                       required
                                       autocomplete="off">
                            </div>
                        </div>
                        
                        <div class="d-flex justify-content-between align-items-center">
                            <a href="{{ url_for('admin.dashboard') }}" class="btn btn-secondary">
                                <i class="fas fa-arrow-left me-2"></i>
                                Cancel & Go Back
                            </a>
                            
                            <button type="submit" 
                                    class="btn btn-danger btn-lg"
                                    onclick="return confirm('Are you absolutely sure? This will delete ALL financial movement data permanently!')">
                                <i class="fas fa-trash-alt me-2"></i>
                                DELETE ALL MOVEMENTS
                            </button>
                        </div>
                    </form>
                    {% endif %}
                </div>
            </div>
            
            <div class="card mt-4">
                <div class="card-header">
                    <h6 class="mb-0">
                        <i class="fas fa-info-circle me-2"></i>
                        What will be deleted?
                    </h6>
                </div>
                <div class="card-body">
                    <ul class="list-unstyled">
                        <li class="mb-2">
                            <i class="fas fa-times text-danger me-2"></i>
                            All financial movement records ({{ movement_count }} records)
                        </li>
                        <li class="mb-2">
                            <i class="fas fa-times text-danger me-2"></i>
                            All upload history records ({{ upload_count }} records)
                        </li>
                        <li class="mb-2">
                            <i class="fas fa-times text-danger me-2"></i>
                            All batch tracking information
                        </li>
                    </ul>
                    
                    <h6 class="text-success mt-3">What will be kept?</h6>
                    <ul class="list-unstyled">
                        <li class="mb-2">
                            <i class="fas fa-check text-success me-2"></i>
                            All user accounts and login information
                        </li>
                        <li class="mb-2">
                            <i class="fas fa-check text-success me-2"></i>
                            System configuration and settings
                        </li>
                        <li class="mb-2">
                            <i class="fas fa-check text-success me-2"></i>
                            Database structure (tables will remain, just empty)
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block scripts %}
<script>
$(document).ready(function() {
    // Add visual feedback when typing confirmation
    $('input[name="confirmation"]').on('input', function() {
        const value = $(this).val().toLowerCase().trim();
        const submitBtn = $('button[type="submit"]');
        
        if (value === 'delete all movements') {
            $(this).removeClass('is-invalid').addClass('is-valid');
            submitBtn.prop('disabled', false);
        } else {
            $(this).removeClass('is-valid').addClass('is-invalid');
            submitBtn.prop('disabled', true);
        }
    });
    
    // Initially disable submit button
    $('button[type="submit"]').prop('disabled', true);
});
</script>
{% endblock %}

----- FILE: templates/admin/dashboard.html  (sha1:b63c6f02a3ee, bytes:10688) -----
{% extends "base.html" %}

{% block title %}Admin Dashboard - Financial Data Management{% endblock %}

{% block content %}
<div class="d-flex justify-content-between align-items-center mb-4">
    <h1>
        <i class="fas fa-tachometer-alt me-3"></i>
        Admin Dashboard
    </h1>
</div>

<!-- Statistics Cards -->
<div class="row mb-4">
    <div class="col-md-3">
        <div class="card text-white bg-primary">
            <div class="card-body">
                <div class="d-flex justify-content-between">
                    <div>
                        <h5 class="card-title">Total Users</h5>
                        <h2>{{ stats.total_users }}</h2>
                    </div>
                    <div class="align-self-center">
                        <i class="fas fa-users fa-2x"></i>
                    </div>
                </div>
            </div>
            <div class="card-footer">
                <a href="{{ url_for('admin.users') }}" class="text-white text-decoration-none">
                    View Details <i class="fas fa-arrow-right"></i>
                </a>
            </div>
        </div>
    </div>
    
    <div class="col-md-3">
        <div class="card text-white bg-success">
            <div class="card-body">
                <div class="d-flex justify-content-between">
                    <div>
                        <h5 class="card-title">Total Movements</h5>
                        <h2>{{ "{:,}".format(stats.total_movements) }}</h2>
                    </div>
                    <div class="align-self-center">
                        <i class="fas fa-chart-line fa-2x"></i>
                    </div>
                </div>
            </div>
            <div class="card-footer">
                <a href="{{ url_for('main.movements') }}" class="text-white text-decoration-none">
                    View Details <i class="fas fa-arrow-right"></i>
                </a>
            </div>
        </div>
    </div>
    
    <div class="col-md-3">
        <div class="card text-white bg-info">
            <div class="card-body">
                <div class="d-flex justify-content-between">
                    <div>
                        <h5 class="card-title">Total Uploads</h5>
                        <h2>{{ stats.total_uploads }}</h2>
                    </div>
                    <div class="align-self-center">
                        <i class="fas fa-upload fa-2x"></i>
                    </div>
                </div>
            </div>
            <div class="card-footer">
                <a href="{{ url_for('admin.upload_history') }}" class="text-white text-decoration-none">
                    View Details <i class="fas fa-arrow-right"></i>
                </a>
            </div>
        </div>
    </div>
    
    <div class="col-md-3">
        <div class="card text-white bg-warning">
            <div class="card-body">
                <div class="d-flex justify-content-between">
                    <div>
                        <h5 class="card-title">Quick Actions</h5>
                        <p class="mb-0">Manage System</p>
                    </div>
                    <div class="align-self-center">
                        <i class="fas fa-cogs fa-2x"></i>
                    </div>
                </div>
            </div>
            <div class="card-footer">
                <div class="d-flex justify-content-between align-items-center">
                    <a href="{{ url_for('admin.upload_file') }}" class="text-white text-decoration-none">
                        Upload Data <i class="fas fa-arrow-right"></i>
                    </a>
                    <a href="{{ url_for('admin.clear_movements') }}" class="text-white text-decoration-none small" title="Clear All Movements">
                        <i class="fas fa-trash-alt"></i>
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Recent Uploads -->
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header d-flex justify-content-between align-items-center">
                <h5 class="mb-0">
                    <i class="fas fa-clock me-2"></i>Recent Uploads
                </h5>
                <a href="{{ url_for('admin.upload_history') }}" class="btn btn-outline-primary btn-sm">
                    View All
                </a>
            </div>
            <div class="card-body">
                {% if stats.recent_uploads %}
                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th>Filename</th>
                                <th>Total Records</th>
                                <th>Successful</th>
                                <th>Failed</th>
                                <th>Uploaded</th>
                                <th>Status</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for upload in stats.recent_uploads %}
                            <tr>
                                <td>
                                    <i class="fas fa-file-excel text-success me-2"></i>
                                    {{ upload.filename }}
                                </td>
                                <td>{{ upload.total_rows }}</td>
                                <td>
                                    <span class="badge bg-success">{{ upload.success_count }}</span>
                                </td>
                                <td>
                                    {% if upload.error_count > 0 %}
                                    <span class="badge bg-danger">{{ upload.error_count }}</span>
                                    {% else %}
                                    <span class="badge bg-secondary">0</span>
                                    {% endif %}
                                </td>
                                <td>
                                    <small class="text-muted">
                                        {{ upload.uploaded_at.strftime('%Y-%m-%d %H:%M') }}
                                        <br>
                                        by {{ upload.uploader.username }}
                                    </small>
                                </td>
                                <td>
                                    {% if upload.error_count == 0 %}
                                    <span class="badge bg-success">
                                        <i class="fas fa-check me-1"></i>Success
                                    </span>
                                    {% else %}
                                    <span class="badge bg-warning">
                                        <i class="fas fa-exclamation-triangle me-1"></i>Partial
                                    </span>
                                    {% endif %}
                                </td>
                                <td>
                                    <a href="{{ url_for('admin.upload_history_detail', id=upload.id) }}" 
                                       class="btn btn-outline-primary btn-sm">
                                        <i class="fas fa-eye me-1"></i>View
                                    </a>
                                </td>
                            </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
                {% else %}
                <div class="text-center py-4">
                    <i class="fas fa-inbox fa-3x text-muted mb-3"></i>
                    <p class="text-muted">No uploads yet. Start by uploading your first Excel file!</p>
                    <a href="{{ url_for('admin.upload_file') }}" class="btn btn-primary">
                        <i class="fas fa-upload me-2"></i>Upload Data
                    </a>
                </div>
                {% endif %}
            </div>
        </div>
    </div>
</div>

<!-- Quick Actions -->
<div class="row mt-4">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0">
                    <i class="fas fa-bolt me-2"></i>Quick Actions
                </h5>
            </div>
            <div class="card-body">
                <div class="row">
                    <div class="col-md-4">
                        <div class="d-grid">
                            <a href="{{ url_for('admin.users', action='create') }}" class="btn btn-outline-primary">
                                <i class="fas fa-user-plus me-2"></i>Create New User
                            </a>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="d-grid">
                            <a href="{{ url_for('admin.upload_file') }}" class="btn btn-outline-success">
                                <i class="fas fa-upload me-2"></i>Upload Excel File
                            </a>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="d-grid">
                            <a href="{{ url_for('main.movements') }}" class="btn btn-outline-info">
                                <i class="fas fa-search me-2"></i>Search Movements
                            </a>
                        </div>
                    </div>
                </div>
                
                <!-- Danger Zone -->
                <div class="row mt-3">
                    <div class="col-12">
                        <div class="alert alert-danger">
                            <h6 class="alert-heading mb-2">
                                <i class="fas fa-exclamation-triangle me-2"></i>Danger Zone
                            </h6>
                            <div class="d-flex justify-content-between align-items-center">
                                <span class="small">Clear all financial movement data from the database</span>
                                <a href="{{ url_for('admin.clear_movements') }}" class="btn btn-danger btn-sm">
                                    <i class="fas fa-trash-alt me-2"></i>Clear All Movements
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}


----- FILE: templates/admin/upload.html  (sha1:0749c5176658, bytes:16961) -----
{% extends "base.html" %}

{% block title %}Data Upload - Financial Data Management{% endblock %}

{% block content %}
<div class="d-flex justify-content-between align-items-center mb-4">
    <h1>
        <i class="fas fa-upload me-3"></i>
        {% if show_detail %}
        Upload Details
        {% elif show_history %}
        Upload History
        {% else %}
        Upload Data
        {% endif %}
    </h1>
    {% if show_history or show_detail %}
    <a href="{{ url_for('admin.upload_file') }}" class="btn btn-primary">
        <i class="fas fa-upload me-2"></i>Upload New File
    </a>
    {% elif not show_detail %}
    <a href="{{ url_for('admin.upload_history') }}" class="btn btn-outline-secondary">
        <i class="fas fa-history me-2"></i>Upload History
    </a>
    {% endif %}
</div>

{% if show_detail and upload %}
<!-- Upload Detail View -->
<div class="row mb-4">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0">
                    <i class="fas fa-file-excel text-success me-2"></i>
                    {{ upload.filename }}
                </h5>
            </div>
            <div class="card-body">
                <div class="row">
                    <div class="col-md-3">
                        <div class="card bg-light">
                            <div class="card-body text-center">
                                <h4 class="text-primary">{{ upload.total_records }}</h4>
                                <p class="mb-0">Total Records</p>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="card bg-light">
                            <div class="card-body text-center">
                                <h4 class="text-success">{{ upload.successful_records }}</h4>
                                <p class="mb-0">Successful</p>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="card bg-light">
                            <div class="card-body text-center">
                                <h4 class="text-danger">{{ upload.failed_records }}</h4>
                                <p class="mb-0">Failed</p>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="card bg-light">
                            <div class="card-body text-center">
                                <h4 class="text-info">{{ upload.uploaded_at.strftime('%H:%M') }}</h4>
                                <p class="mb-0">{{ upload.uploaded_at.strftime('%Y-%m-%d') }}</p>
                            </div>
                        </div>
                    </div>
                </div>
                
                <div class="mt-3">
                    <strong>Uploaded by:</strong> {{ upload.uploader.username }} ({{ upload.uploader.email }})
                </div>
                
                {% if upload.error_log %}
                <div class="mt-3">
                    <h6>Error Log:</h6>
                    <div class="alert alert-danger">
                        <pre class="mb-0" style="white-space: pre-wrap;">{{ upload.error_log }}</pre>
                    </div>
                </div>
                {% endif %}
            </div>
        </div>
    </div>
</div>

<!-- Associated Movements -->
{% if movements %}
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0">
                    <i class="fas fa-list me-2"></i>
                    Uploaded Movements ({{ movements|length }})
                </h5>
            </div>
            <div class="card-body p-0">
                <div class="table-responsive">
                    <table class="table table-hover mb-0" id="movementsTable">
                        <thead class="table-dark">
                            <tr>
                                <th>Internal ID</th>
                                <th>Number</th>
                                <th>Customer/Vendor</th>
                                <th>Net Value</th>
                                <th>Date</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for movement in movements %}
                            <tr>
                                <td><code>{{ movement.internal_id or '-' }}</code></td>
                                <td>{{ movement.number or '-' }}</td>
                                <td>{{ movement.aux_customer_vendor_code or '-' }}</td>
                                <td>
                                    {% if movement.net_value %}
                                    <span class="badge bg-success">
                                        {{ movement.net_value_currency_code or 'R$' }} {{ "%.2f"|format(movement.net_value) }}
                                    </span>
                                    {% else %}
                                    -
                                    {% endif %}
                                </td>
                                <td>
                                    {% if movement.date %}
                                    {{ movement.date.strftime('%Y-%m-%d') }}
                                    {% else %}
                                    -
                                    {% endif %}
                                </td>
                                <td>
                                    <a href="{{ url_for('main.movement_detail', id=movement.id) }}" class="btn btn-outline-primary btn-sm">
                                        <i class="fas fa-eye me-1"></i>View
                                    </a>
                                </td>
                            </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
{% endif %}

{% elif show_history %}
<!-- Upload History -->
<div class="card">
    <div class="card-body">
        {% if uploads.items %}
        <div class="table-responsive">
            <table class="table table-hover" id="uploadsTable">
                <thead class="table-dark">
                    <tr>
                        <th>Filename</th>
                        <th>Total Records</th>
                        <th>Successful</th>
                        <th>Failed</th>
                        <th>Uploaded By</th>
                        <th>Date</th>
                        <th>Status</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for upload in uploads.items %}
                    <tr>
                        <td>
                            <i class="fas fa-file-excel text-success me-2"></i>
                            {{ upload.filename }}
                        </td>
                        <td>{{ upload.total_rows }}</td>
                        <td>
                            <span class="badge bg-success">{{ upload.success_count }}</span>
                        </td>
                        <td>
                            {% if upload.error_count > 0 %}
                            <span class="badge bg-danger">{{ upload.error_count }}</span>
                            {% else %}
                            <span class="badge bg-secondary">0</span>
                            {% endif %}
                        </td>
                        <td>
                            <small>
                                {{ upload.uploader.username }}
                                <br>
                                <span class="text-muted">{{ upload.uploader.email }}</span>
                            </small>
                        </td>
                        <td>
                            <small class="text-muted">
                                {{ upload.uploaded_at.strftime('%Y-%m-%d') }}
                                <br>
                                {{ upload.uploaded_at.strftime('%H:%M:%S') }}
                            </small>
                        </td>
                        <td>
                            {% if upload.error_count == 0 %}
                            <span class="badge bg-success">
                                <i class="fas fa-check me-1"></i>Success
                            </span>
                            {% else %}
                            <span class="badge bg-warning">
                                <i class="fas fa-exclamation-triangle me-1"></i>Partial
                            </span>
                            {% endif %}
                        </td>
                        <td>
                            <a href="{{ url_for('admin.upload_history_detail', id=upload.id) }}" 
                               class="btn btn-outline-primary btn-sm">
                                <i class="fas fa-eye me-1"></i>Details
                            </a>
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
        
        <!-- Pagination -->
        {% if uploads.pages > 1 %}
        <nav aria-label="Uploads pagination" class="mt-3">
            <ul class="pagination justify-content-center">
                {% if uploads.has_prev %}
                <li class="page-item">
                    <a class="page-link" href="{{ url_for('admin.upload_history', page=uploads.prev_num) }}">Previous</a>
                </li>
                {% endif %}
                
                {% for page_num in uploads.iter_pages() %}
                    {% if page_num %}
                        {% if page_num != uploads.page %}
                        <li class="page-item">
                            <a class="page-link" href="{{ url_for('admin.upload_history', page=page_num) }}">{{ page_num }}</a>
                        </li>
                        {% else %}
                        <li class="page-item active">
                            <span class="page-link">{{ page_num }}</span>
                        </li>
                        {% endif %}
                    {% else %}
                    <li class="page-item disabled">
                        <span class="page-link">...</span>
                    </li>
                    {% endif %}
                {% endfor %}
                
                {% if uploads.has_next %}
                <li class="page-item">
                    <a class="page-link" href="{{ url_for('admin.upload_history', page=uploads.next_num) }}">Next</a>
                </li>
                {% endif %}
            </ul>
        </nav>
        {% endif %}
        
        {% else %}
        <div class="text-center py-4">
            <i class="fas fa-inbox fa-3x text-muted mb-3"></i>
            <p class="text-muted">No uploads yet.</p>
            <a href="{{ url_for('admin.upload_file') }}" class="btn btn-primary">
                <i class="fas fa-upload me-2"></i>Upload First File
            </a>
        </div>
        {% endif %}
    </div>
</div>

{% else %}
<!-- File Upload Form -->
<div class="row">
    <div class="col-md-8">
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0">
                    <i class="fas fa-file-upload me-2"></i>
                    Upload Excel File
                </h5>
            </div>
            <div class="card-body">
                <form method="POST" enctype="multipart/form-data">
                    {{ form.hidden_tag() }}
                    
                    <div class="mb-3">
                        {{ form.file.label(class="form-label") }}
                        {{ form.file(class="form-control", accept=".xlsx,.xls") }}
                        {% if form.file.errors %}
                        <div class="text-danger mt-1">
                            {% for error in form.file.errors %}
                            <small>{{ error }}</small><br>
                            {% endfor %}
                        </div>
                        {% endif %}
                        <div class="form-text">
                            Only Excel files (.xlsx, .xls) are allowed. Maximum file size: 50MB.
                        </div>
                    </div>
                    
                    <div class="mb-3">
                        {{ form.description.label(class="form-label") }}
                        {{ form.description(class="form-control", rows="3") }}
                        <div class="form-text">
                            Optional description for this upload batch.
                        </div>
                    </div>
                    
                    <div class="d-flex gap-2">
                        {{ form.submit(class="btn btn-primary") }}
                        <a href="{{ url_for('admin.dashboard') }}" class="btn btn-secondary">Cancel</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
    
    <div class="col-md-4">
        <div class="card bg-light">
            <div class="card-header">
                <h6 class="mb-0">
                    <i class="fas fa-info-circle me-2"></i>
                    Upload Requirements
                </h6>
            </div>
            <div class="card-body">
                <h6>File Format:</h6>
                <ul class="list-unstyled">
                    <li><i class="fas fa-check text-success me-2"></i>Excel format (.xlsx, .xls)</li>
                    <li><i class="fas fa-check text-success me-2"></i>Maximum 50MB file size</li>
                    <li><i class="fas fa-check text-success me-2"></i>Structured financial data</li>
                </ul>
                
                <h6>Expected Columns:</h6>
                <ul class="list-unstyled small">
                    <li><code>Empresa_Code</code> <span class="text-success">(NEW)</span></li>
                    <li><code>Empresa_Nome</code> <span class="text-success">(NEW)</span></li>
                    <li><code>Filial_Code</code> <span class="text-success">(NEW)</span></li>
                    <li><code>Filial_Nome</code> <span class="text-success">(NEW)</span></li>
                    <li><code>internalId</code></li>
                    <li><code>companyId</code></li>
                    <li><code>movementId</code></li>
                    <li><code>number</code></li>
                    <li><code>netValue</code></li>
                    <li><code>grossValue</code></li>
                    <li class="text-muted">...and 108+ more columns</li>
                </ul>
                
                <div class="alert alert-info mt-3">
                    <i class="fas fa-lightbulb me-2"></i>
                    <strong>Tip:</strong> The system will automatically validate and process your data. Any errors will be logged for review.
                </div>
            </div>
        </div>
    </div>
</div>
{% endif %}

{% endblock %}

{% block scripts %}
<script>
$(document).ready(function() {
    // Initialize DataTables for different tables
    if ($('#uploadsTable').length) {
        // Check if DataTable is already initialized
        if ($.fn.DataTable.isDataTable('#uploadsTable')) {
            $('#uploadsTable').DataTable().destroy();
        }
        
        $('#uploadsTable').DataTable({
            "pageLength": 25,
            "ordering": true,
            "searching": true,
            "info": true,
            "responsive": true,
            "order": [[5, "desc"]] // Sort by date descending
        });
    }
    
    if ($('#movementsTable').length) {
        // Check if DataTable is already initialized
        if ($.fn.DataTable.isDataTable('#movementsTable')) {
            $('#movementsTable').DataTable().destroy();
        }
        
        $('#movementsTable').DataTable({
            "pageLength": 25,
            "ordering": true,
            "searching": true,
            "info": true,
            "responsive": true
        });
    }
    
    // File upload progress (if needed)
    $('form[enctype="multipart/form-data"]').on('submit', function() {
        const submitBtn = $(this).find('input[type="submit"]');
        submitBtn.prop('disabled', true).val('Uploading...');
        
        // Show loading spinner or progress bar if desired
        $(this).find('.card-body').append('<div class="text-center mt-3"><i class="fas fa-spinner fa-spin"></i> Processing file...</div>');
    });
});
</script>
{% endblock %}


----- FILE: templates/admin/users.html  (sha1:2b4b1fa94301, bytes:11580) -----
{% extends "base.html" %}

{% block title %}User Management - Financial Data Management{% endblock %}

{% block content %}
<div class="d-flex justify-content-between align-items-center mb-4">
    <h1>
        <i class="fas fa-users me-3"></i>
        User Management
    </h1>
    {% if not (form and action) %}
    <a href="{{ url_for('admin.create_user') }}" class="btn btn-primary">
        <i class="fas fa-user-plus me-2"></i>Add User
    </a>
    {% endif %}
</div>

{% if form and action %}
<!-- User Form (Create/Edit) -->
<div class="row">
    <div class="col-md-8">
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0">
                    {% if action == 'create' %}
                    <i class="fas fa-user-plus me-2"></i>Create New User
                    {% else %}
                    <i class="fas fa-user-edit me-2"></i>Edit User: {{ user.username }}
                    {% endif %}
                </h5>
            </div>
            <div class="card-body">
                <form method="POST">
                    {{ form.hidden_tag() }}
                    
                    <div class="row">
                        <div class="col-md-6">
                            <div class="mb-3">
                                {{ form.username.label(class="form-label") }}
                                {{ form.username(class="form-control") }}
                                {% if form.username.errors %}
                                <div class="text-danger">
                                    {% for error in form.username.errors %}
                                    <small>{{ error }}</small><br>
                                    {% endfor %}
                                </div>
                                {% endif %}
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="mb-3">
                                {{ form.email.label(class="form-label") }}
                                {{ form.email(class="form-control") }}
                                {% if form.email.errors %}
                                <div class="text-danger">
                                    {% for error in form.email.errors %}
                                    <small>{{ error }}</small><br>
                                    {% endfor %}
                                </div>
                                {% endif %}
                            </div>
                        </div>
                    </div>
                    
                    {% if action == 'create' %}
                    <div class="row">
                        <div class="col-md-6">
                            <div class="mb-3">
                                {{ form.password.label(class="form-label") }}
                                {{ form.password(class="form-control") }}
                                {% if form.password.errors %}
                                <div class="text-danger">
                                    {% for error in form.password.errors %}
                                    <small>{{ error }}</small><br>
                                    {% endfor %}
                                </div>
                                {% endif %}
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="mb-3">
                                {{ form.password2.label(class="form-label") }}
                                {{ form.password2(class="form-control") }}
                                {% if form.password2.errors %}
                                <div class="text-danger">
                                    {% for error in form.password2.errors %}
                                    <small>{{ error }}</small><br>
                                    {% endfor %}
                                </div>
                                {% endif %}
                            </div>
                        </div>
                    </div>
                    {% endif %}
                    
                    <div class="mb-3">
                        <div class="form-check">
                            {{ form.is_admin(class="form-check-input") }}
                            {{ form.is_admin.label(class="form-check-label") }}
                        </div>
                        <small class="form-text text-muted">
                            Administrator users can manage other users and upload data files.
                        </small>
                    </div>
                    
                    <div class="d-flex gap-2">
                        {{ form.submit(class="btn btn-primary") }}
                        <a href="{{ url_for('admin.users') }}" class="btn btn-secondary">Cancel</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

{% else %}
<!-- Users List -->
<div class="card">
    <div class="card-body">
        {% if users.items %}
        <div class="table-responsive">
            <table class="table table-hover" id="usersTable">
                <thead class="table-dark">
                    <tr>
                        <th>Username</th>
                        <th>Email</th>
                        <th>Role</th>
                        <th>Created</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for user in users.items %}
                    <tr>
                        <td>
                            <i class="fas fa-user me-2"></i>
                            {{ user.username }}
                        </td>
                        <td>{{ user.email }}</td>
                        <td>
                            {% if user.is_admin %}
                            <span class="badge bg-primary">
                                <i class="fas fa-crown me-1"></i>Administrator
                            </span>
                            {% else %}
                            <span class="badge bg-secondary">
                                <i class="fas fa-user me-1"></i>User
                            </span>
                            {% endif %}
                        </td>
                        <td>
                            <small class="text-muted">
                                {{ user.created_at.strftime('%Y-%m-%d %H:%M') }}
                            </small>
                        </td>
                        <td>
                            <div class="btn-group btn-group-sm" role="group">
                                <a href="{{ url_for('admin.edit_user', id=user.id) }}" 
                                   class="btn btn-outline-primary">
                                    <i class="fas fa-edit"></i>
                                </a>
                                {% if user.id != current_user.id %}
                                <button type="button" class="btn btn-outline-danger" 
                                        data-bs-toggle="modal" 
                                        data-bs-target="#deleteModal{{ user.id }}">
                                    <i class="fas fa-trash"></i>
                                </button>
                                {% endif %}
                            </div>
                        </td>
                    </tr>
                    
                    <!-- Delete Modal -->
                    {% if user.id != current_user.id %}
                    <div class="modal fade" id="deleteModal{{ user.id }}" tabindex="-1">
                        <div class="modal-dialog">
                            <div class="modal-content">
                                <div class="modal-header">
                                    <h5 class="modal-title">Confirm Delete</h5>
                                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                                </div>
                                <div class="modal-body">
                                    <p>Are you sure you want to delete user <strong>{{ user.username }}</strong>?</p>
                                    <div class="alert alert-warning">
                                        <i class="fas fa-exclamation-triangle me-2"></i>
                                        This action cannot be undone.
                                    </div>
                                </div>
                                <div class="modal-footer">
                                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                                    <form method="POST" action="{{ url_for('admin.delete_user', id=user.id) }}" class="d-inline">
                                        <button type="submit" class="btn btn-danger">Delete</button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    </div>
                    {% endif %}
                    {% endfor %}
                </tbody>
            </table>
        </div>
        
        <!-- Pagination -->
        {% if users.pages > 1 %}
        <nav aria-label="Users pagination">
            <ul class="pagination justify-content-center">
                {% if users.has_prev %}
                <li class="page-item">
                    <a class="page-link" href="{{ url_for('admin.users', page=users.prev_num) }}">Previous</a>
                </li>
                {% endif %}
                
                {% for page_num in users.iter_pages() %}
                    {% if page_num %}
                        {% if page_num != users.page %}
                        <li class="page-item">
                            <a class="page-link" href="{{ url_for('admin.users', page=page_num) }}">{{ page_num }}</a>
                        </li>
                        {% else %}
                        <li class="page-item active">
                            <span class="page-link">{{ page_num }}</span>
                        </li>
                        {% endif %}
                    {% else %}
                    <li class="page-item disabled">
                        <span class="page-link">...</span>
                    </li>
                    {% endif %}
                {% endfor %}
                
                {% if users.has_next %}
                <li class="page-item">
                    <a class="page-link" href="{{ url_for('admin.users', page=users.next_num) }}">Next</a>
                </li>
                {% endif %}
            </ul>
        </nav>
        {% endif %}
        
        {% else %}
        <div class="text-center py-4">
            <i class="fas fa-users fa-3x text-muted mb-3"></i>
            <p class="text-muted">No users found.</p>
            <a href="{{ url_for('admin.create_user') }}" class="btn btn-primary">
                <i class="fas fa-user-plus me-2"></i>Create First User
            </a>
        </div>
        {% endif %}
    </div>
</div>
{% endif %}
{% endblock %}

{% block scripts %}
<script>
$(document).ready(function() {
    $('#usersTable').DataTable({
        "pageLength": 25,
        "ordering": true,
        "searching": true,
        "info": true,
        "responsive": true
    });
});
</script>
{% endblock %}


----- FILE: templates/auth/login.html  (sha1:6ba36fac4b54, bytes:5697) -----
{% extends "base.html" %}

{% block title %}Login - Financial Data Management{% endblock %}

{% block content %}
<div class="row justify-content-center">
    <div class="col-md-6 col-lg-4">
        <div class="card">
            <div class="card-header text-center">
                <h4 class="mb-0">
                    <i class="fas fa-sign-in-alt me-2"></i>
                    Sign In
                </h4>
            </div>
            <div class="card-body">
                <form method="POST">
                    {{ form.hidden_tag() }}
                    
                    <div class="mb-3">
                        {{ form.username.label(class="form-label") }}
                        {{ form.username(class="form-control", placeholder="Enter your username") }}
                        {% if form.username.errors %}
                        <div class="text-danger mt-1">
                            {% for error in form.username.errors %}
                            <small>{{ error }}</small><br>
                            {% endfor %}
                        </div>
                        {% endif %}
                    </div>
                    
                    <div class="mb-3">
                        {{ form.password.label(class="form-label") }}
                        {{ form.password(class="form-control", placeholder="Enter your password") }}
                        {% if form.password.errors %}
                        <div class="text-danger mt-1">
                            {% for error in form.password.errors %}
                            <small>{{ error }}</small><br>
                            {% endfor %}
                        </div>
                        {% endif %}
                    </div>
                    
                    <div class="mb-3 form-check">
                        {{ form.remember_me(class="form-check-input") }}
                        {{ form.remember_me.label(class="form-check-label") }}
                    </div>
                    
                    <div class="d-grid">
                        {{ form.submit(class="btn btn-primary") }}
                    </div>
                </form>
            </div>
            <div class="card-footer text-center">
                <small class="text-muted">
                    <i class="fas fa-info-circle me-1"></i>
                    Contact your administrator for account access
                </small>
            </div>
        </div>
        
        <!-- Demo Credentials Info -->
        <div class="card mt-3">
            <div class="card-body">
                <h6 class="card-title">
                    <i class="fas fa-key me-2"></i>
                    Default Admin Access
                </h6>
                <p class="card-text">
                    <strong>Username:</strong> <code>admin</code><br>
                    <strong>Password:</strong> <code>admin123</code>
                </p>
                <div class="alert alert-warning">
                    <i class="fas fa-exclamation-triangle me-2"></i>
                    <strong>Important:</strong> Change the default password after first login.
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Additional Info -->
<div class="row justify-content-center mt-4">
    <div class="col-md-8">
        <div class="card bg-light">
            <div class="card-body text-center">
                <h5 class="card-title">
                    <i class="fas fa-chart-line me-2"></i>
                    Financial Data Management System
                </h5>
                <p class="card-text">
                    Comprehensive platform for managing and analyzing financial movement data.
                    Upload Excel files, manage users, and explore detailed financial records.
                </p>
                <div class="row text-center">
                    <div class="col-md-4">
                        <i class="fas fa-upload fa-2x text-primary mb-2"></i>
                        <p><strong>Bulk Upload</strong><br>Excel file processing</p>
                    </div>
                    <div class="col-md-4">
                        <i class="fas fa-search fa-2x text-success mb-2"></i>
                        <p><strong>Advanced Search</strong><br>Filter and find records</p>
                    </div>
                    <div class="col-md-4">
                        <i class="fas fa-chart-bar fa-2x text-info mb-2"></i>
                        <p><strong>Detailed Analysis</strong><br>JSON data visualization</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block scripts %}
<script>
$(document).ready(function() {
    // Focus username field on page load
    $('#username').focus();
    
    // Form validation feedback
    $('form').on('submit', function(e) {
        const username = $('#username').val().trim();
        const password = $('#password').val();
        
        if (!username || !password) {
            e.preventDefault();
            
            if (!username) {
                $('#username').addClass('is-invalid');
            } else {
                $('#username').removeClass('is-invalid');
            }
            
            if (!password) {
                $('#password').addClass('is-invalid');
            } else {
                $('#password').removeClass('is-invalid');
            }
            
            return false;
        }
    });
    
    // Remove validation classes on input
    $('#username, #password').on('input', function() {
        $(this).removeClass('is-invalid');
    });
});
</script>
{% endblock %}


----- FILE: templates/movements/delete_all.html  (sha1:b4fe26eba296, bytes:5621) -----
{% extends "base.html" %}

{% block title %}Delete All Movements - Financial Data Management{% endblock %}

{% block content %}
<div class="d-flex justify-content-between align-items-center mb-4">
    <h1>
        <i class="fas fa-exclamation-triangle text-danger me-3"></i>
        Delete All Movements
    </h1>
    <a href="{{ url_for('main.movements') }}" class="btn btn-outline-secondary">
        <i class="fas fa-arrow-left me-2"></i>Back to Movements
    </a>
</div>

<!-- Warning Card -->
<div class="card border-danger mb-4">
    <div class="card-header bg-danger text-white">
        <h5 class="mb-0">
            <i class="fas fa-exclamation-triangle me-2"></i>
            DANGER ZONE
        </h5>
    </div>
    <div class="card-body">
        <div class="alert alert-warning" role="alert">
            <h6 class="alert-heading">
                <i class="fas fa-warning me-2"></i>Warning!
            </h6>
            <p class="mb-0">This action will permanently delete <strong>ALL</strong> financial movement data from the system. This includes:</p>
            <ul class="mt-2 mb-0">
                <li><strong>{{ "{:,}".format(movement_count) }} financial movement records</strong></li>
                <li><strong>{{ "{:,}".format(upload_count) }} upload history records</strong></li>
                <li>All associated JSON data (items, taxes, cost centers)</li>
                <li>All file processing history</li>
            </ul>
        </div>
        
        <div class="alert alert-danger" role="alert">
            <h6 class="alert-heading">
                <i class="fas fa-times-circle me-2"></i>This action cannot be undone!
            </h6>
            <p class="mb-0">Once deleted, all data will be permanently lost. Make sure you have backups if needed.</p>
        </div>
    </div>
</div>

<!-- Current Data Summary -->
<div class="card mb-4">
    <div class="card-header">
        <h6 class="mb-0">
            <i class="fas fa-database me-2"></i>Current Database Content
        </h6>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-md-6">
                <div class="d-flex align-items-center p-3 bg-primary bg-opacity-10 rounded">
                    <i class="fas fa-list-alt fa-2x text-primary me-3"></i>
                    <div>
                        <h5 class="mb-0">{{ "{:,}".format(movement_count) }}</h5>
                        <small class="text-muted">Financial Movements</small>
                    </div>
                </div>
            </div>
            <div class="col-md-6">
                <div class="d-flex align-items-center p-3 bg-info bg-opacity-10 rounded">
                    <i class="fas fa-upload fa-2x text-info me-3"></i>
                    <div>
                        <h5 class="mb-0">{{ "{:,}".format(upload_count) }}</h5>
                        <small class="text-muted">Upload Records</small>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

{% if movement_count > 0 or upload_count > 0 %}
<!-- Delete Confirmation Form -->
<div class="card border-danger">
    <div class="card-header bg-danger bg-opacity-10">
        <h6 class="mb-0 text-danger">
            <i class="fas fa-trash me-2"></i>Confirm Deletion
        </h6>
    </div>
    <div class="card-body">
        <form method="POST">
            <div class="mb-3">
                <label for="confirmation" class="form-label">
                    To confirm deletion, type exactly: <code>delete all movements</code>
                </label>
                <input type="text" 
                       class="form-control" 
                       id="confirmation" 
                       name="confirmation" 
                       placeholder="delete all movements"
                       autocomplete="off"
                       required>
            </div>
            
            <div class="d-flex justify-content-between">
                <a href="{{ url_for('main.movements') }}" class="btn btn-secondary">
                    <i class="fas fa-arrow-left me-2"></i>Cancel
                </a>
                <button type="submit" class="btn btn-danger" onclick="return confirmDeletion()">
                    <i class="fas fa-trash me-2"></i>Delete All Data
                </button>
            </div>
        </form>
    </div>
</div>
{% else %}
<!-- No Data Card -->
<div class="card">
    <div class="card-body text-center py-5">
        <i class="fas fa-inbox fa-4x text-muted mb-3"></i>
        <h5 class="text-muted">No Data to Delete</h5>
        <p class="text-muted">The database is already empty.</p>
        <a href="{{ url_for('main.movements') }}" class="btn btn-primary">
            <i class="fas fa-arrow-left me-2"></i>Back to Movements
        </a>
    </div>
</div>
{% endif %}

<script>
function confirmDeletion() {
    const confirmation = document.getElementById('confirmation').value.toLowerCase();
    
    if (confirmation !== 'delete all movements') {
        alert('Please type exactly "delete all movements" to confirm deletion.');
        return false;
    }
    
    return confirm(
        'This will permanently delete ALL financial movement data!\n\n' +
        'Are you absolutely sure you want to continue?\n\n' +
        'This action CANNOT be undone!'
    );
}

// Auto-focus the confirmation input
document.addEventListener('DOMContentLoaded', function() {
    const confirmationInput = document.getElementById('confirmation');
    if (confirmationInput) {
        confirmationInput.focus();
    }
});
</script>
{% endblock %}

----- FILE: templates/movements/detail.html  (sha1:fa7a762b4c8f, bytes:61890) -----
{% extends "base.html" %}

{% block title %}Movement {{ movement.internal_id or movement.id }} - Financial Data Management{% endblock %}

{% block content %}
<div class="d-flex justify-content-between align-items-center mb-4">
    <h1>
        <i class="fas fa-file-alt me-3"></i>
        Detalhes de movimento
    </h1>
    <div class="btn-group" role="group">
        <a href="{{ url_for('main.movements') }}" class="btn btn-outline-secondary">
            <i class="fas fa-arrow-left me-2"></i>Back to List
        </a>
        <button type="button" class="btn btn-outline-danger" 
                onclick="confirmDelete({{ movement.id }}, '{{ movement.internal_id or movement.id }}')"
                title="Delete Movement">
            <i class="fas fa-trash me-2"></i>Delete
        </button>
    </div>
</div>

<!-- Movement Header -->
<div class="card mb-4">
    <div class="card-header">
        <h5 class="mb-0">
            <i class="fas fa-info-circle me-2"></i>
            {% if movement.internal_id %}
            Movimento {{ movement.internal_id }}
            {% else %}
            Movimento {{ movement.id }}
            {% endif %}
            {% if movement.status %}
            <span class="badge {% if movement.status == 'F' %}bg-success{% elif movement.status == 'N' %}bg-warning{% else %}bg-secondary{% endif %} ms-2">
                {{ movement.status }}
            </span>
            {% endif %}
        </h5>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-md-3">
                <strong>Document Number:</strong>
                <p>{{ movement.number or '-' }}</p>
            </div>
            <div class="col-md-3">
                <strong>Series:</strong>
                <p>
                    {% if movement.series %}
                    <span class="badge bg-info">{{ movement.series }}</span>
                    {% else %}
                    -
                    {% endif %}
                </p>
            </div>
            <div class="col-md-3">
                <strong>N° do movimento:</strong>
                <p>{{ movement.movement_type_code or '-' }}</p>
            </div>
            <div class="col-md-3">
                <strong>Data:</strong>
                <p>{{ movement.date.strftime('%d/%m/%Y') if movement.date else '-' }}</p>
            </div>
        </div>
        
        <!-- Company and Branch Information -->
        <div class="row mt-3">
            <div class="col-md-6">
                <div class="card bg-primary bg-opacity-10 border-primary border-opacity-25">
                    <div class="card-body py-2">
                        <h6 class="card-title text-primary mb-1 fs-6">
                            <i class="fas fa-building me-2"></i>Empresa
                        </h6>
                        <div class="row">
                            <div class="col-8">
                                <small class="text-muted d-block">Razão Social:</small>
                                <strong class="small">{{ movement.customer_vendor_name or '-' }}</strong>
                            </div>
                            <div class="col-4">
                                <small class="text-muted d-block">ID da Empresa:</small>
                                <strong class="small">{{ movement.company_id or '-' }}</strong>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-md-6">
                <div class="card bg-info bg-opacity-10 border-info border-opacity-25">
                    <div class="card-body py-2">
                        <h6 class="card-title text-info mb-1 fs-6">
                            <i class="fas fa-map-marker-alt me-2"></i>Filial
                        </h6>
                        <div class="row">
                            <div class="col-8">
                                <small class="text-muted d-block">Nome da Filial:</small>
                                <strong class="small">{{ movement.filial_nome or '-' }}</strong>
                            </div>
                            <div class="col-4">
                                <small class="text-muted d-block">ID da Filial:</small>
                                <strong class="small">{{ movement.branch_id or '-' }}</strong>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>


        
        {% if movement.observation %}
        <div class="mt-3">
            <strong>Observation:</strong>
            <p class="text-muted">{{ movement.observation }}</p>
        </div>
        {% endif %}
    </div>
</div>

<!-- Informações Financeiras -->
<div class="row mb-4">
    <div class="col-md-6">
        <div class="card h-100">
            <div class="card-header">
                <h6 class="mb-0">
                    <i class="fas fa-dollar-sign me-2"></i>Informações Financeiras
                </h6>
            </div>
            <div class="card-body">
                <div class="row">
                    <div class="col-sm-6">
                        <strong>Valor Bruto:</strong>
                        <p class="h5 text-primary">
                            {{ movement.net_value_currency_code or 'R$' }} {{ "%.2f"|format(movement.gross_value or 0) }}
                        </p>
                    </div>
                    <div class="col-sm-6">
                        <strong>Valor Líquido:</strong>
                        <p class="h5 text-success">
                            {{ movement.net_value_currency_code or 'R$' }} {{ "%.2f"|format(movement.net_value or 0) }}
                        </p>
                    </div>
                </div>
                <hr>
                <div class="row">
                    <div class="col-sm-6">
                        <strong>Desconto (valor):</strong>
                        <p>{{ movement.net_value_currency_code or 'R$' }} {{ "%.2f"|format(movement.discount_percentage_value or 0) }}</p>
                    </div>
                    <div class="col-sm-6">
                        <strong>Despesa (valor):</strong>
                        <p>{{ movement.net_value_currency_code or 'R$' }} {{ "%.2f"|format(movement.expense_value or 0) }}</p>
                    </div>
                </div>
                <div class="row">
                    <div class="col-sm-6">
                        <strong>Frete:</strong>
                        <p>{{ movement.net_value_currency_code or 'R$' }} {{ "%.2f"|format(movement.freight_value or 0) }}</p>
                    </div>
                    <div class="col-sm-6">
                        <strong>Seguro:</strong>
                        <p>{{ movement.net_value_currency_code or 'R$' }} {{ "%.2f"|format(movement.insurance_value or 0) }}</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <div class="col-md-6">
        <div class="card h-100">
            <div class="card-header">
                <h6 class="mb-0">
                    <i class="fas fa-building me-2"></i>Informações da Empresa
                </h6>
            </div>
            <div class="card-body">
                <div class="row">
                    <div class="col-sm-6">
                        <strong>ID da Empresa:</strong>
                        <p>{{ movement.company_id or '-' }}</p>
                    </div>
                    <div class="col-sm-6">
                        <strong>ID da Filial:</strong>
                        <p>{{ movement.branch_id or '-' }}</p>
                    </div>
                </div>
                
                <div class="row">
                    <div class="col-sm-6">
                        <strong>Depósito:</strong>
                        <p>{{ movement.warehouse_code or '-' }}</p>
                    </div>
                    <div class="col-sm-6">
                        <strong>Depósito de Destino:</strong>
                        <p>{{ movement.destiny_warehouse_code or '-' }}</p>
                    </div>
                </div>
                
                <div class="row">
                    
                    <div class="col-sm-6">
                        <strong>Código do Usuário:</strong>
                        <p>{{ movement.user_code or '-' }}</p>
                    </div>
                </div>

                {# --- INÍCIO: Rateio de Centros de Custo dentro de Informações da Empresa --- #}
                {% set cost_center_apportionments = movement.get_json_field('cost_center_apportionments') %}
                {% if cost_center_apportionments and cost_center_apportionments|length > 0 %}

                <hr class="my-3">

                <h6 class="mb-2">
                <i class="fas fa-chart-pie me-2"></i>
                Rateio de Centros de Custo
                <span class="badge bg-secondary ms-2">{{ cost_center_apportionments|length }}</span>
                </h6>

                <div class="table-responsive">
                <table class="table table-sm table-hover">
                    <thead>
                    <tr>
                        <th>Código do Centro de Custo</th>
                        <th>Nome do Centro de Custo</th>
                        <th class="text-end">Valor</th>
                        <th class="text-center">Ações</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for item in cost_center_apportionments %}
                    <tr>
                        <td><code class="text-info">{{ item.costCenterCode }}</code></td>
                        <td>{{ item.costCenterName or '-' }}</td>
                        <td class="text-end">
                        <strong class="text-success">R$ {{ "%.2f"|format(item.value or item.amount or item.totalValue or 0) }}</strong>
                        </td>
                        <td class="text-center">
                        <button class="btn btn-outline-primary btn-sm" data-bs-toggle="modal" data-bs-target="#costCenterModal{{ loop.index }}">
                            <i class="fas fa-eye"></i>
                        </button>
                        </td>
                    </tr>
                    {% endfor %}
                    </tbody>
                </table>
                </div>

                {# Modais podem ficar em qualquer lugar do DOM; incluir aqui para manter o botão funcionando #}
                {% for item in cost_center_apportionments %}
                <div class="modal fade" id="costCenterModal{{ loop.index }}" tabindex="-1">
                <div class="modal-dialog modal-lg">
                    <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title"><i class="fas fa-chart-pie me-2"></i> Detalhes do Rateio de Centro de Custo</h5>
                        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                    </div>
                    <div class="modal-body">
                        <div class="row">
                        <div class="col-md-12">
                            <div class="card border-primary mb-3">
                            <div class="card-header bg-primary text-white">
                                <small><i class="fas fa-info-circle me-1"></i>Informações Fiscais do Item</small>
                            </div>
                            <div class="card-body">
                                <!-- (reaproveite aqui exatamente o conteúdo de campos do seu modal original) -->
                                <div class="row mb-2"><div class="col-sm-6"><strong>Natureza Orçamentária:</strong></div><div class="col-sm-6">{{ item.natureza_orcamentaria or '-' }}</div></div>
                                <div class="row mb-2"><div class="col-sm-6"><strong>NCM:</strong></div><div class="col-sm-6">{{ item.ncm or '-' }}</div></div>
                                <div class="row mb-2"><div class="col-sm-6"><strong>Procedência:</strong></div><div class="col-sm-6">{{ item.procedencia or '-' }}</div></div>
                                <div class="row mb-2"><div class="col-sm-6"><strong>Unidade de Medida:</strong></div><div class="col-sm-6">{{ item.measureUnitCode or '-' }}</div></div>
                                <div class="row mb-2"><div class="col-sm-6"><strong>CFOP de Entrada:</strong></div><div class="col-sm-6">{{ item.cfop_entrada or '-' }}</div></div>
                                <div class="row mb-2"><div class="col-sm-6"><strong>CST:</strong></div><div class="col-sm-6">{{ item.cst or '-' }}</div></div>
                                <div class="row mb-2"><div class="col-sm-6"><strong>Tipo de Tributação:</strong></div><div class="col-sm-6">{{ item.tipo_tributacao or '-' }}</div></div>
                                <div class="row mb-2"><div class="col-sm-6"><strong>Classificação Fiscal:</strong></div><div class="col-sm-6">{{ item.classificacao_fiscal or '-' }}</div></div>
                                <div class="row mb-2"><div class="col-sm-6"><strong>Conta Contábil:</strong></div><div class="col-sm-6">{{ item.conta_contabil or '-' }}</div></div>
                                <div class="row mb-2"><div class="col-sm-6"><strong>Centro de Custo:</strong></div><div class="col-sm-6">{{ item.costCenterCode or '-' }}</div></div>
                                <hr>
                                <div class="row mb-2"><div class="col-sm-6"><strong>Base de Cálculo ICMS:</strong></div><div class="col-sm-6">{{ item.base_calculo_icms or '-' }}</div></div>
                                <div class="row mb-2"><div class="col-sm-6"><strong>Valor ICMS:</strong></div><div class="col-sm-6">{{ item.valor_icms or '-' }}</div></div>
                                <div class="row mb-2"><div class="col-sm-6"><strong>Alíquota ICMS:</strong></div><div class="col-sm-6">{{ item.aliquota_icms or '-' }}</div></div>
                                <div class="row mb-2"><div class="col-sm-6"><strong>Valor IPI:</strong></div><div class="col-sm-6">{{ item.valor_ipi or '-' }}</div></div>
                                <div class="row mb-2"><div class="col-sm-6"><strong>Alíquota IPI:</strong></div><div class="col-sm-6">{{ item.aliquota_ipi or '-' }}</div></div>
                                <div class="row mb-2"><div class="col-sm-6"><strong>ISS:</strong></div><div class="col-sm-6">{{ item.valor_iss or '-' }}</div></div>
                                <div class="row mb-2"><div class="col-sm-6"><strong>PIS:</strong></div><div class="col-sm-6">{{ item.valor_pis or '-' }}</div></div>
                                <div class="row mb-2"><div class="col-sm-6"><strong>COFINS:</strong></div><div class="col-sm-6">{{ item.valor_cofins or '-' }}</div></div>
                                <div class="row mb-2"><div class="col-sm-6"><strong>IR:</strong></div><div class="col-sm-6">{{ item.valor_ir or '-' }}</div></div>
                                <div class="row mb-2"><div class="col-sm-6"><strong>CSLL:</strong></div><div class="col-sm-6">{{ item.valor_csll or '-' }}</div></div>

                                {% if current_user.is_admin %}
                                <hr>
                                <div class="card border-secondary">
                                <div class="card-header bg-secondary text-white"><small><i class="fas fa-code me-1"></i>Dados Brutos (Apenas Administradores)</small></div>
                                <div class="card-body"><pre class="mb-0"><code>{{ item|tojson(indent=2) }}</code></pre></div>
                                </div>
                                {% endif %}
                            </div>
                            </div>
                        </div>
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Fechar</button>
                    </div>
                    </div>
                </div>
                </div>
                {% endfor %}
                {% endif %}
                {# --- FIM: Rateio de Centros de Custo dentro de Informações da Empresa --- #}

            </div>
        </div>
    </div>
</div>

<!-- Informações do Cliente/Fornecedor -->
<div class="card mb-4">
    <div class="card-header">
        <h6 class="mb-0">
            <i class="fas fa-handshake me-2"></i>Informações do Cliente/Fornecedor
        </h6>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-md-4">
                <strong>Código do Cliente/Fornecedor:</strong>
                <p>{{ movement.customer_vendor_code or movement.aux_customer_vendor_code or '-' }}</p>
            </div>
            <div class="col-md-4">
                <strong>Condição de Pagamento:</strong>
                <p>{{ movement.payment_term_code or '-' }}</p>
            </div>
            <div class="col-md-4">
                <strong>Vendedor:</strong>
                <p>{{ movement.salesman1_code or '-' }}</p>
            </div>
        </div>
    </div>
</div>


<!-- Dates -->
<div class="card mb-4">
    <div class="card-header">
        <h6 class="mb-0">
            <i class="fas fa-calendar me-2"></i>Datas importantes
        </h6>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-md-4">
                <strong>Data de emissão:</strong>
                <p>{{ movement.register_date.strftime('%d/%m/%Y %H:%M') if movement.register_date else '-' }}</p>
            </div>
            <div class="col-md-4">
                <strong>Data de evolução:</strong>
                <p>{{ movement.entry_date.strftime('%d/%m/%Y %H:%M') if movement.entry_date else '-' }}</p>
            </div>
            <div class="col-md-4">
                <strong>Última edição:</strong>
                <p>{{ movement.last_edit_time.strftime('%d/%m/%Y %H:%M') if movement.last_edit_time else '-' }}</p>
            </div>
        </div>
    </div>
</div>

<!-- Movement Items (Special Section) -->
{% set movement_items = movement.get_json_field('movement_items') %}
{% if movement_items and movement_items|length > 0 %}
<div class="card mb-4">
    <div class="card-header">
        <h6 class="mb-0">
            <i class="fas fa-boxes me-2"></i>Itens do documento fiscal ({{ movement_items|length }} itens)
        </h6>
    </div>
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-sm table-hover">
                <thead class="table-dark">
                    <tr>
                        <th>Seq</th>
                        <th>Produto</th>
                        <th>Descrição</th>
                        <th>Quantidade</th>
                        <th>Valor unitário</th>
                        <th>Unidade</th>
                        <th>Valor total</th>
                        <th>Data</th>
                        <th>Ações</th>
                    </tr>
                </thead>
                <tbody>
                    {% for item in movement_items %}
                    <tr>
                        <td>
                            <span class="badge bg-primary">{{ item.sequentialNumber or item.sequentialId or loop.index }}</span>
                        </td>
                        <td>
                        {% if item.productFantasyName %}
                            <strong class="text-primary">{{ item.productFantasyName }}</strong>
                            
                        {% else %}
                            <strong class="text-primary">{{ item.productCode or '-' }}</strong>
                        {% endif %}
                    </td>


                        <td>
                            {{ item.description or '-' }}
                        </td>
                        <td>
                            <span class="badge bg-info">{{ item.quantity or 0 }}</span>
                        </td>
                        <td>
                            {% if item.unitPrice %}
                            <span class="text-success fw-bold">R$ {{ "%.2f"|format(item.unitPrice) }}</span>
                            {% else %}
                            -
                            {% endif %}
                        </td>
                        <td>
                            {% if item.measureUnitCode %}
                            <small class="badge bg-secondary">{{ item.measureUnitCode }}</small>
                            {% else %}
                            -
                            {% endif %}
                        </td>
                        <td>
                            {% if item.unitPrice and item.quantity %}
                                <strong class="text-success">
                                    R$ {{ "%.2f"|format((item.unitPrice | float) * (item.quantity | float)) }}
                                </strong>
                            {% else %}
                                <span class="text-muted">-</span>
                            {% endif %}
                        </td>


                        <td>
                            {% if item.registerDate %}
                                {% set d = item.registerDate[:10].split('-') %}
                                <small class="text-muted">{{ d[2] }}/{{ d[1] }}/{{ d[0] }}</small>
                            {% else %}
                                -
                            {% endif %}
                        </td>

                        <td>
                            <button class="btn btn-outline-primary btn-sm" 
                                    onclick="showItemDetails({{ loop.index0 }})" 
                                    title="Ver detalhes">
                                <i class="fas fa-eye"></i>
                            </button>
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>
{% endif %}

<!-- JSON Data Sections -->
{% set json_fields = [
    ('payments', 'Pagamentos', 'fas fa-credit-card'),
    ('department_apportionments', 'Rateio de Departamentos', 'fas fa-sitemap'),
    ('taxes', 'Impostos', 'fas fa-percent')
] %}

{% for field_name, title, icon in json_fields %}
    {% set json_data = movement.get_json_field(field_name) %}
    {% if json_data and json_data|length > 0 %}
    <div class="card mb-4">
        <div class="card-header">
            <h6 class="mb-0">
                <i class="{{ icon }} me-2"></i>{{ title }}
                <span class="badge bg-secondary ms-2">{{ json_data|length }}</span>
            </h6>
        </div>
        <div class="card-body">
            <div class="accordion" id="accordion{{ field_name }}">
                {% for item in json_data %}
                <div class="accordion-item">
                    <h2 class="accordion-header" id="heading{{ field_name }}{{ loop.index }}">
                        <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" 
                                data-bs-target="#collapse{{ field_name }}{{ loop.index }}" aria-expanded="false">
                            {% if field_name == 'movement_items' %}
                                {% if item is mapping %}
                                    Item {{ loop.index }} - ID do Produto: {{ item.get('productId', 'N/A') }}
                                {% else %}
                                    Item {{ loop.index }} - {{ item }}
                                {% endif %}
                            {% elif field_name == 'payments' %}
                                Pagamento {{ loop.index }}
                            {% elif field_name == 'taxes' %}
                                {% if item is mapping %}
                                    {{ item.get('taxId', 'Imposto') }} - {{ "%.2f"|format(item.get('value', 0)) }}
                                {% else %}
                                    Imposto {{ loop.index }} - {{ item }}
                                {% endif %}
                            {% else %}
                                {{ title }} {{ loop.index }}
                            {% endif %}
                        </button>
                    </h2>
                    <div id="collapse{{ field_name }}{{ loop.index }}" class="accordion-collapse collapse" 
                         data-bs-parent="#accordion{{ field_name }}">
                        <div class="accordion-body">
                            <div class="table-responsive">
                                <table class="table table-sm table-borderless">
                                    <tbody>
                                        {% if item is mapping %}
                                            {% for key, value in item.items() %}
                                            <tr>
                                                <td><strong>{{ key|translate_field }}:</strong></td>

                                                <td>
                                                    {% if key == 'dueDate' and value %}
                                                        {{ value[:10] | replace('-', '/') | datetimeformat('%d/%m/%Y') if false else value[:10].split('-')[2] ~ '/' ~ value[:10].split('-')[1] ~ '/' ~ value[:10].split('-')[0] }}
                                                    {% elif value is number %}
                                                        {{ "%.2f"|format(value) if value % 1 else value|int }}
                                                    {% elif value is string and value|length > 50 %}
                                                        <span title="{{ value }}">{{ value[:50] }}...</span>
                                                    {% else %}
                                                        {{ value or '-' }}
                                                    {% endif %}
                                                </td>
                                            </tr>
                                            {% endfor %}

                                        {% else %}
                                            <tr>
                                                <td><strong>Valor:</strong></td>
                                                <td>{{ item }}</td>
                                            </tr>
                                        {% endif %}
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
                {% endfor %}
            </div>
        </div>
    </div>
    {% endif %}
{% endfor %}




<!-- Taxes (Special Section) -->
{% set taxes = movement.get_json_field('taxes') %}
{% if taxes and taxes|length > 0 %}
<div class="card mb-4">
    <div class="card-header">
        <h6 class="mb-0">
            <i class="fas fa-calculator me-2"></i>
            Taxes ({{ taxes|length }} {% if taxes|length == 1 %}item{% else %}items{% endif %})
        </h6>
    </div>
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-sm table-hover">
                <thead>
                    <tr>
                        <th>Tax ID</th>
                        <th class="text-end">Calculation Basis</th>
                        <th class="text-end">Aliquot (%)</th>
                        <th class="text-end">Tax Value</th>
                        <th class="text-center">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for item in taxes %}
                    <tr>
                        <td>
                            <span class="badge bg-info">{{ item.taxId }}</span>
                        </td>
                        <td class="text-end">
                            <span class="text-muted">R$ {{ "%.2f"|format(item.calculationBasis) }}</span>
                        </td>
                        <td class="text-end">
                            <span class="text-warning">{{ "%.2f"|format(item.get('aliquot', 0)) }}%</span>
                        </td>
                        <td class="text-end">
                            <strong class="text-success">R$ {{ "%.2f"|format(item.value or item.amount or item.totalValue or 0) }}</strong>
                        </td>
                        <td class="text-center">
                            <button class="btn btn-outline-primary btn-sm" 
                                    data-bs-toggle="modal" 
                                    data-bs-target="#taxModal{{ loop.index }}">
                                <i class="fas fa-eye"></i>
                            </button>
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>

<!-- Tax Detail Modals -->
{% for item in taxes %}
<div class="modal fade" id="taxModal{{ loop.index }}" tabindex="-1">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">
                    <i class="fas fa-calculator me-2"></i>
                    Tax Details - {{ item.taxId }}
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <div class="row">
                    <!-- Basic Information -->
                    <div class="col-md-6">
                        <div class="card border-primary mb-3">
                            <div class="card-header bg-primary text-white">
                                <small><i class="fas fa-info-circle me-1"></i>Basic Information</small>
                            </div>
                            <div class="card-body">
                                <div class="row mb-2">
                                    <div class="col-sm-6"><strong>Tax ID:</strong></div>
                                    <div class="col-sm-6">
                                        <span class="badge bg-info">{{ item.taxId }}</span>
                                    </div>
                                </div>
                                <div class="row mb-2">
                                    <div class="col-sm-6"><strong>Company ID:</strong></div>
                                    <div class="col-sm-6">{{ item.companyId }}</div>
                                </div>
                                <div class="row mb-2">
                                    <div class="col-sm-6"><strong>Movement ID:</strong></div>
                                    <div class="col-sm-6">{{ item.movementId }}</div>
                                </div>
                                <div class="row mb-2">
                                    <div class="col-sm-6"><strong>Item Sequential:</strong></div>
                                    <div class="col-sm-6">{{ item.movementItemSequentialId }}</div>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <!-- Tax Calculation -->
                    <div class="col-md-6">
                        <div class="card border-success mb-3">
                            <div class="card-header bg-success text-white">
                                <small><i class="fas fa-calculator me-1"></i>Tax Calculation</small>
                            </div>
                            <div class="card-body">
                                <div class="row mb-2">
                                    <div class="col-sm-7"><strong>Calculation Basis:</strong></div>
                                    <div class="col-sm-5 text-end">
                                        <span class="text-muted">R$ {{ "%.2f"|format(item.calculationBasis) }}</span>
                                    </div>
                                </div>
                                <div class="row mb-2">
                                    <div class="col-sm-7"><strong>Aliquot:</strong></div>
                                    <div class="col-sm-5 text-end">
                                        <span class="text-warning">{{ "%.2f"|format(item.get('aliquot', 0)) }}%</span>
                                    </div>
                                </div>
                                <div class="row mb-2">
                                    <div class="col-sm-7"><strong>Tax Value:</strong></div>
                                    <div class="col-sm-5 text-end">
                                        <strong class="text-success">R$ {{ "%.2f"|format(item.value or item.amount or item.totalValue or 0) }}</strong>
                                    </div>
                                </div>
                                <div class="row mb-2">
                                    <div class="col-sm-7"><strong>Calculated Basis:</strong></div>
                                    <div class="col-sm-5 text-end">
                                        <span class="text-muted">R$ {{ "%.2f"|format(item.calculatedCalculationBasis) }}</span>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                
                <!-- Status Information -->
                <div class="card border-warning mb-3">
                    <div class="card-header bg-warning text-dark">
                        <small><i class="fas fa-flag me-1"></i>Status & Flags</small>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-6">
                                <div class="row mb-2">
                                    <div class="col-sm-6"><strong>Edited:</strong></div>
                                    <div class="col-sm-6">
                                        {% if item.edited %}
                                            <span class="badge bg-warning">Yes</span>
                                        {% else %}
                                            <span class="badge bg-secondary">No</span>
                                        {% endif %}
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="row mb-2">
                                    <div class="col-sm-6"><strong>Full Base:</strong></div>
                                    <div class="col-sm-6 text-end">
                                        R$ {{ "%.2f"|format(item.fullBase) }}
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                
                <!-- Raw JSON (Admin Only) -->
                {% if current_user.is_admin %}
                <div class="card border-secondary">
                    <div class="card-header bg-secondary text-white">
                        <small><i class="fas fa-code me-1"></i>Raw Data (Admin Only)</small>
                    </div>
                    <div class="card-body">
                        <pre class="mb-0"><code>{{ item|tojson(indent=2) }}</code></pre>
                    </div>
                </div>
                {% endif %}
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>
{% endfor %}
{% endif %}

<!-- System Information -->
<div class="card mb-4">
    <div class="card-header">
        <h6 class="mb-0">
            <i class="fas fa-cogs me-2"></i>Informações do Sistema
        </h6>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-md-4">
                <strong>Lote de Upload:</strong>
                <p><code>{{ movement.upload_batch_id or '-' }}</code></p>
            </div>
            <div class="col-md-4">
                <strong>Enviado em:</strong>
                <p>{{ movement.uploaded_at.strftime('%d/%m/%Y %H:%M:%S') if movement.uploaded_at else '-' }}</p>
            </div>
            <div class="col-md-4">
                <strong>Criado por:</strong>
                <p>{{ movement.creation_user or '-' }}</p>
            </div>
        </div>
        
        <div class="row mt-3">
            <div class="col-md-12">
                <strong>Flags:</strong>
                <div class="mt-2">
                    {% set flags = [
                        (movement.printed, 'Impresso', 'primary'),
                        (movement.document_printed, 'Documento Impresso', 'info'),
                        (movement.bill_printed, 'Boleto Impresso', 'success'),
                        (movement.has_generated_bill, 'Boleto Gerado', 'warning'),
                        (movement.processed_flag, 'Processado', 'secondary')
                    ] %}
                    
                    {% for flag_value, flag_name, color in flags %}
                        {% if flag_value %}
                        <span class="badge bg-{{ color }} me-2">{{ flag_name }}</span>
                        {% endif %}
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>
</div>

{% if current_user.is_admin %}
<div class="card">
    <div class="card-header">
        <button class="btn btn-link p-0 text-decoration-none w-100 text-start" type="button" data-bs-toggle="collapse" 
                data-bs-target="#rawData" aria-expanded="false">
            <h6 class="mb-0 d-flex justify-content-between align-items-center">
                <span>
                    <i class="fas fa-database me-2"></i>Dados Brutos (Apenas Administradores)
                </span>
                <i class="fas fa-chevron-down"></i>
            </h6>
        </button>
    </div>
    <div class="collapse" id="rawData">
        <div class="card-body">
            <div class="row">
                <div class="col-md-6">
                    <h6 class="text-primary mb-3"><i class="fas fa-info-circle me-1"></i> Informações Principais</h6>
                    <table class="table table-sm table-hover">
                        <tbody>
                            <tr><td><strong>ID Interno:</strong></td><td><code>{{ movement.internal_id or '-' }}</code></td></tr>
                            <tr><td><strong>ID do Movimento:</strong></td><td>{{ movement.movement_id or '-' }}</td></tr>
                            <tr><td><strong>ID da Empresa:</strong></td><td>{{ movement.company_id or '-' }}</td></tr>
                            <tr><td><strong>ID da Filial:</strong></td><td>{{ movement.branch_id or '-' }}</td></tr>
                            <tr><td><strong>Código da Empresa:</strong></td><td><span class="badge bg-primary">{{ movement.empresa_code or '-' }}</span></td></tr>
                            <tr><td><strong>Nome da Empresa:</strong></td><td>{{ movement.empresa_nome or '-' }}</td></tr>
                            <tr><td><strong>Código da Filial:</strong></td><td><span class="badge bg-info">{{ movement.filial_code or '-' }}</span></td></tr>
                            <tr><td><strong>Nome da Filial:</strong></td><td>{{ movement.filial_nome or '-' }}</td></tr>
                            <tr><td><strong>Status:</strong></td><td><span class="badge bg-info">{{ movement.status or '-' }}</span></td></tr>
                            <tr><td><strong>Tipo:</strong></td><td><span class="badge bg-secondary">{{ movement.type or '-' }}</span></td></tr>
                            <tr><td><strong>Tipo de Movimento:</strong></td><td><code>{{ movement.movement_type_code or '-' }}</code></td></tr>
                            <tr><td><strong>Série:</strong></td><td>{{ movement.series or '-' }}</td></tr>
                        </tbody>
                    </table>
                </div>
                
                <div class="col-md-6">
                    <h6 class="text-success mb-3"><i class="fas fa-money-bill-wave me-1"></i> Detalhes Financeiros</h6>
                    <table class="table table-sm table-hover">
                        <tbody>
                            <tr><td><strong>Valor Líquido:</strong></td><td><span class="text-success">{{ "R$ %.2f"|format(movement.net_value) if movement.net_value else '-' }}</span></td></tr>
                            <tr><td><strong>Valor Bruto:</strong></td><td><span class="text-success">{{ "R$ %.2f"|format(movement.gross_value) if movement.gross_value else '-' }}</span></td></tr>
                            <tr><td><strong>Outros Valores:</strong></td><td>{{ "R$ %.2f"|format(movement.other_values) if movement.other_values else '-' }}</td></tr>
                            <tr><td><strong>Bruto Interno:</strong></td><td>{{ "R$ %.2f"|format(movement.internal_gross_value) if movement.internal_gross_value else '-' }}</td></tr>
                            <tr><td><strong>Valor da Mercadoria:</strong></td><td>{{ "R$ %.2f"|format(movement.merchandise_value) if movement.merchandise_value else '-' }}</td></tr>
                            <tr><td><strong>Moeda:</strong></td><td>{{ movement.net_value_currency_code or '-' }}</td></tr>
                            <tr><td><strong>Condição de Pagamento:</strong></td><td>{{ movement.payment_term_code or '-' }}</td></tr>
                            <tr><td><strong>Conta Caixa:</strong></td><td>{{ movement.cash_account_code or '-' }}</td></tr>
                        </tbody>
                    </table>
                </div>
            </div>
            
            <div class="row mt-4">
                <div class="col-md-6">
                    <h6 class="text-info mb-3"><i class="fas fa-user-tie me-1"></i> Cliente/Fornecedor</h6>
                    <table class="table table-sm table-hover">
                        <tbody>
                            <tr><td><strong>Código:</strong></td><td><code>{{ movement.customer_vendor_code or '-' }}</code></td></tr>
                            <tr><td><strong>Código Auxiliar:</strong></td><td><code>{{ movement.aux_customer_vendor_code or '-' }}</code></td></tr>
                            <tr><td><strong>ID da Empresa:</strong></td><td>{{ movement.customer_vendor_company_id or '-' }}</td></tr>
                            <tr><td><strong>ID Auxiliar da Empresa:</strong></td><td>{{ movement.aux_customer_vendor_company_id or '-' }}</td></tr>
                        </tbody>
                    </table>
                </div>
                
                <div class="col-md-6">
                    <h6 class="text-warning mb-3"><i class="fas fa-warehouse me-1"></i> Armazém e Operações</h6>
                    <table class="table table-sm table-hover">
                        <tbody>
                            <tr><td><strong>Depósito:</strong></td><td>{{ movement.warehouse_code or '-' }}</td></tr>
                            <tr><td><strong>Depósito de Destino:</strong></td><td>{{ movement.destiny_warehouse_code or '-' }}</td></tr>
                            <tr><td><strong>Filial de Destino:</strong></td><td>{{ movement.destiny_branch_id or '-' }}</td></tr>
                            <tr><td><strong>ID da Operação:</strong></td><td>{{ movement.operation_id or '-' }}</td></tr>
                            <tr><td><strong>Centro de Custo:</strong></td><td>{{ movement.cost_center_code or '-' }}</td></tr>
                        </tbody>
                    </table>
                </div>
            </div>
            
            <div class="row mt-4">
                <div class="col-md-6">
                    <h6 class="text-secondary mb-3"><i class="fas fa-calendar-alt me-1"></i> Datas e Usuários</h6>
                    <table class="table table-sm table-hover">
                        <tbody>
                            <tr><td><strong>Data de Criação:</strong></td><td>{{ movement.creation_date.strftime('%d/%m/%Y %H:%M') if movement.creation_date else '-' }}</td></tr>
                            <tr><td><strong>Data de Registro:</strong></td><td>{{ movement.register_date.strftime('%d/%m/%Y %H:%M') if movement.register_date else '-' }}</td></tr>
                            <tr><td><strong>Data de Entrada:</strong></td><td>{{ movement.entry_date.strftime('%d/%m/%Y %H:%M') if movement.entry_date else '-' }}</td></tr>
                            <tr><td><strong>Data de Saída:</strong></td><td>{{ movement.exit_date.strftime('%d/%m/%Y %H:%M') if movement.exit_date else '-' }}</td></tr>
                            <tr><td><strong>Data de Entrega:</strong></td><td>{{ movement.delivery_date.strftime('%d/%m/%Y') if movement.delivery_date else '-' }}</td></tr>
                            <tr><td><strong>Última Edição:</strong></td><td>{{ movement.last_edit_time.strftime('%d/%m/%Y %H:%M:%S') if movement.last_edit_time else '-' }}</td></tr>
                            <tr><td><strong>Usuário de Criação:</strong></td><td><span class="badge bg-dark">{{ movement.creation_user or '-' }}</span></td></tr>
                            <tr><td><strong>Código do Usuário:</strong></td><td><span class="badge bg-dark">{{ movement.user_code or '-' }}</span></td></tr>
                        </tbody>
                    </table>
                </div>
                
                <div class="col-md-6">
                    <h6 class="text-danger mb-3"><i class="fas fa-percentage me-1"></i> Percentuais e Encargos</h6>
                    <table class="table table-sm table-hover">
                        <tbody>
                            <tr><td><strong>Desconto %:</strong></td><td>{{ "%.2f%%"|format(movement.discount_percentage) if movement.discount_percentage else '0.00%' }}</td></tr>
                            <tr><td><strong>Despesa %:</strong></td><td>{{ "%.2f%%"|format(movement.expense_percentage) if movement.expense_percentage else '0.00%' }}</td></tr>
                            <tr><td><strong>Encargo %:</strong></td><td>{{ "%.2f%%"|format(movement.charge_percentage) if movement.charge_percentage else '0.00%' }}</td></tr>
                            <tr><td><strong>Extra % 1:</strong></td><td>{{ "%.2f%%"|format(movement.extra_percentage1) if movement.extra_percentage1 else '0.00%' }}</td></tr>
                            <tr><td><strong>Extra % 2:</strong></td><td>{{ "%.2f%%"|format(movement.extra_percentage2) if movement.extra_percentage2 else '0.00%' }}</td></tr>
                            <tr><td><strong>Representante Comercial %:</strong></td><td>{{ "%.2f%%"|format(movement.commercial_representative_charge) if movement.commercial_representative_charge else '0.00%' }}</td></tr>
                            <tr><td><strong>Vendedor 2 %:</strong></td><td>{{ "%.2f%%"|format(movement.salesman2_charge_percentage) if movement.salesman2_charge_percentage else '0.00%' }}</td></tr>
                            <tr><td><strong>Vendedor 3 %:</strong></td><td>{{ "%.2f%%"|format(movement.salesman3_charge_percentage) if movement.salesman3_charge_percentage else '0.00%' }}</td></tr>
                        </tbody>
                    </table>
                </div>
            </div>
            
            <div class="row mt-4">
                <div class="col-12">
                    <h6 class="text-primary mb-3"><i class="fas fa-flag me-1"></i> Status e Flags</h6>
                    <div class="d-flex flex-wrap gap-2">
                        {% set all_flags = [
                            (movement.printed, 'Impresso', 'primary'),
                            (movement.document_printed, 'Documento Impresso', 'info'),
                            (movement.bill_printed, 'Boleto Impresso', 'success'),
                            (movement.has_generated_bill, 'Boleto Gerado', 'warning'),
                            (movement.processed_flag, 'Processado', 'secondary'),
                            (movement.bonum_integrated, 'Integrado Bonum', 'dark'),
                            (movement.paradigma_auto_integrated, 'Auto Integrado Paradigma', 'primary'),
                            (movement.fluxus_grouped_flag, 'Agrupado Fluxus', 'info'),
                            (movement.lot_generated, 'Lote Gerado', 'success'),
                            (movement.has_generated_work_account, 'Conta de Trabalho Gerada', 'warning'),
                            (movement.uses_financial_value_apportionment, 'Rateio Financeiro', 'secondary')
                        ] %}
                        
                        {% for flag_value, flag_name, color in all_flags %}
                            {% if flag_value %}
                                <span class="badge bg-{{ color }}">{{ flag_name }}</span>
                            {% else %}
                                <span class="badge bg-light text-dark">{{ flag_name }} ✗</span>
                            {% endif %}
                        {% endfor %}
                    </div>
                </div>
            </div>
            
            <div class="row mt-4">
                <div class="col-12">
                    <h6 class="text-success mb-3"><i class="fas fa-upload me-1"></i> Informações de Upload</h6>
                    <div class="alert alert-success">
                        <div class="row">
                            <div class="col-md-4">
                                <strong>ID do Lote:</strong><br>
                                <code>{{ movement.upload_batch_id or '-' }}</code>
                            </div>
                            <div class="col-md-4">
                                <strong>Enviado em:</strong><br>
                                {{ movement.uploaded_at.strftime('%d/%m/%Y %H:%M:%S') if movement.uploaded_at else '-' }}
                            </div>
                            <div class="col-md-4">
                                <strong>ID do Usuário que Enviou:</strong><br>
                                {{ movement.uploaded_by or '-' }}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endif %}


<!-- Movement Item Details Modal -->
<div class="modal fade" id="itemDetailsModal" tabindex="-1" aria-labelledby="itemDetailsModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="itemDetailsModalLabel">
                    <i class="fas fa-box me-2"></i>Movement Item Details
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <div id="itemDetailsContent">
                    <!-- Content will be loaded here -->
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>

{% endblock %}

{% block scripts %}
<script>
// Movement items data for JavaScript
{% set movement_items = movement.get_json_field('movement_items') %}
const movementItems = {{ movement_items|tojson if movement_items else '[]'|safe }};

function showItemDetails(itemIndex) {
    const item = movementItems[itemIndex];
    if (!item) {
        alert('Item not found');
        return;
    }
    
    // Build the details HTML
    let detailsHtml = `
        <div class="card">
            <div class="card-header bg-primary text-white">
                <h6 class="mb-0">Item ${item.sequentialNumber || item.sequentialId || (itemIndex + 1)} - Código do Produto: ${item.productId || 'N/A'}</h6>
            </div>
            <div class="card-body">
                <div class="row">
    `;
    
    // Key-value pairs organized in sections
    const sections = [
        {
            title: 'Informações Básicas',
            icon: 'fas fa-info-circle',
            color: 'primary',
            fields: [
                ['ID da Empresa', item.companyId],
                ['ID do Movimento', item.movementId],
                ['ID Sequencial', item.sequentialId],
                ['Número Sequencial', item.sequentialNumber],
                ['Código do Produto', item.productId],
                ['Código Reduzido', item.productCode],
                ['Unidade de Medida', item.measureUnitCode]
            ]
        },
        {
            title: 'Quantidades e Valores',
            icon: 'fas fa-calculator',
            color: 'success',
            fields: [
                ['Quantidade', item.quantity],
                ['Preço Unitário', item.unitPrice ? 'R$ ' + parseFloat(item.unitPrice).toFixed(2) : null],
                ['Preço de Tabela', item.tablePrice ? 'R$ ' + parseFloat(item.tablePrice).toFixed(2) : null],
                ['Valor de Desconto', item.discountValue ? 'R$ ' + parseFloat(item.discountValue).toFixed(2) : null],
                ['Valor Unitário', item.unitValue ? 'R$ ' + parseFloat(item.unitValue).toFixed(2) : null],
                ['Valor Financeiro', item.financialValue ? 'R$ ' + parseFloat(item.financialValue).toFixed(2) : null],
                ['Quantidade a Receber', item.receivableQuantity],
                ['Quantidade Original', item.originalQuantity]
            ]
        },
        {
            title: 'Datas',
            icon: 'fas fa-calendar',
            color: 'info',
            fields: [
                ['Data de Registro', item.registerDate ? new Date(item.registerDate).toLocaleDateString('pt-BR') : null],
                ['Data de Entrega', item.deliveryDate ? new Date(item.deliveryDate).toLocaleDateString('pt-BR') : null]
            ]
        },
        {
            title: 'Classificações e Códigos',
            icon: 'fas fa-tags',
            color: 'warning',
            fields: [
                ['Tabela de Classificação 5', item.classificationTable5Code],
                ['Tabela Financeira Opcional 2', item.financialOptionalTable2Code],
                ['Flag de Efeito de Estoque', item.stockEffectFlag],
                ['Flag', item.flag],
                ['Objeto de Bloqueio', item.blockObject]
            ]
        }
    ];

    
    sections.forEach(section => {
        detailsHtml += `
            <div class="col-12 mb-3">
                <div class="card border-${section.color} border-opacity-25">
                    <div class="card-header bg-${section.color} bg-opacity-10 border-${section.color} border-opacity-25">
                        <h6 class="mb-0 text-${section.color}">
                            <i class="${section.icon} me-2"></i>${section.title}
                        </h6>
                    </div>
                    <div class="card-body">
                        <div class="row">
        `;
        
        section.fields.forEach(([label, value]) => {
            if (value !== null && value !== undefined && value !== '') {
                detailsHtml += `
                    <div class="col-md-6 mb-2">
                        <strong class="text-muted small">${label}:</strong>
                        <div class="ms-2">${value}</div>
                    </div>
                `;
            }
        });
        
        detailsHtml += `
                        </div>
                    </div>
                </div>
            </div>
        `;
    });
    
    // Add other fields section for any remaining fields
    const displayedFields = new Set([
        'companyid',
        'movementid',
        'sequentialid',
        'sequentialnumber',
        'productcode',
        'quantity',
        'unitprice',
        'tableprice',
        'discountvalue',
        'registerdate',
        'deliverydate',
        'classificationtable5code',
        'financialoptionaltable2code',
        'measureunitcode',
        'receivablequantity',
        'partialbillingreceivedvalue',
        'completedvalue',
        'stockeffectflag',
        'unitvalue',
        'financialvalue',
        'ordinationtaxaliquot',
        'originalquantity',
        'flag',
        'blockobject',

        // 🔥 ADICIONE O CAMPO QUE VOCÊ QUER OCULTAR:
        'commercialrepresentativechargepercentage'
    ]);

    
    const otherFields = [];
    Object.keys(item).forEach(key => {
        const normalizedKey = key.toLowerCase();
        if (!displayedFields.has(normalizedKey)) {
            otherFields.push([key, item[key]]);
        }
    });
    
    if (otherFields.length > 0) {
        detailsHtml += `
            <div class="col-12">
                <div class="card border-secondary border-opacity-25">
                    <div class="card-header bg-secondary bg-opacity-10">
                        <h6 class="mb-0 text-secondary">
                            <i class="fas fa-list me-2"></i>Other Fields
                        </h6>
                    </div>
                    <div class="card-body">
                        <div class="row">
        `;
        
        otherFields.forEach(([key, value]) => {
            if (value !== null && value !== undefined && value !== '') {
                detailsHtml += `
                    <div class="col-md-6 mb-2">
                        <strong class="text-muted small">${key}:</strong>
                        <div class="ms-2 small">${value}</div>
                    </div>
                `;
            }
        });
        
        detailsHtml += `
                        </div>
                    </div>
                </div>
            </div>
        `;
    }
    
    detailsHtml += `
                </div>
            </div>
        </div>
    `;
    
    // Insert the content and show the modal
    document.getElementById('itemDetailsContent').innerHTML = detailsHtml;
    const modal = new bootstrap.Modal(document.getElementById('itemDetailsModal'));
    modal.show();
}

$(document).ready(function() {
    // Auto-expand first accordion item if there are any items
    $('.accordion .accordion-item:first-child .accordion-button').removeClass('collapsed');
    $('.accordion .accordion-item:first-child .accordion-collapse').addClass('show');
});

function confirmDelete(movementId, movementName) {
    document.getElementById('movementName').textContent = movementName;
    document.getElementById('deleteForm').action = `/movements/${movementId}/delete`;
    new bootstrap.Modal(document.getElementById('deleteModal')).show();
}
</script>

<!-- Delete Confirmation Modal -->
<div class="modal fade" id="deleteModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">
                    <i class="fas fa-exclamation-triangle text-warning me-2"></i>
                    Confirm Delete
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <p>Are you sure you want to delete movement <strong id="movementName"></strong>?</p>
                <p class="text-muted">This action cannot be undone.</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                <form id="deleteForm" method="POST" style="display: inline;">
                    <button type="submit" class="btn btn-danger">
                        <i class="fas fa-trash me-2"></i>Delete
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>

{% endblock %}


----- FILE: templates/movements/list.html  (sha1:acdd5752048f, bytes:19811) -----
{% extends "base.html" %}

{% block title %}Financial Movements - Financial Data Management{% endblock %}

{% block content %}
<div class="d-flex justify-content-between align-items-center mb-4">
    <h1>
        <i class="fas fa-list me-3"></i>
        Financial Movements
    </h1>
</div>

<!-- Search and Filters -->
<div class="card mb-4">
    <div class="card-header">
        <h6 class="mb-0">
            <i class="fas fa-filter me-2"></i>Search & Filters
        </h6>
    </div>
    <div class="card-body"><form method="GET" class="row g-3">
  <div class="col-md-6">
    <label for="search" class="form-label fw-bold">
      <i class="fas fa-search me-2 text-primary"></i>Buscar movimento
    </label>
    <input
      type="text"
      class="form-control"
      id="search"
      name="search"
      value="{{ search }}"
      placeholder="Digite para filtrar os movimentos..."
    />
    <div
      id="searchResults"
      class="list-group position-absolute w-100"
      style="z-index:1000; max-height:300px; overflow:auto; display:none;"
    ></div>
  </div>

  <div class="col-md-3">
    <label for="start_date" class="form-label">Período inicial</label>
    <input
      type="date"
      class="form-control"
      id="start_date"
      name="start_date"
      value="{{ start_date }}"
    />
  </div>

  <div class="col-md-3">
    <label for="end_date" class="form-label">Período final</label>
    <input
      type="date"
      class="form-control"
      id="end_date"
      name="end_date"
      value="{{ end_date }}"
    />
  </div>

  <div class="col-md-12 d-flex align-items-end">
    <div class="btn-group w-100" role="group">
      <button type="submit" class="btn btn-primary">
        <i class="fas fa-search me-2"></i>Aplicar filtros
      </button>
      <a href="{{ url_for('main.movements') }}" class="btn btn-outline-secondary">
        <i class="fas fa-times me-2"></i>Limpar tudo
      </a>
    </div>
  </div>
</form>
</div>
</div>

<!-- Results -->
<div class="card">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="mb-0">
            <i class="fas fa-chart-line me-2"></i>
            Movement Records
            {% if movements.total %}
            <span class="badge bg-secondary ms-2">{{ "{:,}".format(movements.total) }} total</span>
            {% endif %}
        </h5>
        {% if movements.total > 0 %}
        <div class="btn-group btn-group-sm" role="group">
            <button type="button" class="btn btn-outline-secondary" onclick="exportData()">
                <i class="fas fa-download me-1"></i>Export
            </button>
            <a href="{{ url_for('main.delete_all_movements') }}" class="btn btn-outline-danger">
                <i class="fas fa-trash me-1"></i>Delete All
            </a>
        </div>
        {% endif %}
    </div>
    <div class="card-body p-0">
        {% if movements.items %}
        <div class="table-responsive">
            <table class="table table-hover mb-0" id="movementsTable">
                <thead class="table-dark">
                    <tr>
                        <th>N° da NF</th>
                        <th style="display:none;">Número do Movimento (oculto)</th>
                        <!-- <th>Empresa</th> -->
                        <!-- <th>Filial</th> -->
                        <th>Cliente/Fornecedor</th>
                        <th>Valor total da nota</th>
                        <th>Data</th>
                        <!-- <th>Status</th> -->
                        <th>ID interno</th>
                        <th>Ações</th>
                        
                    </tr>
                </thead>
                <tbody>
                    {% for movement in movements.items %}
                    <tr>
                        <td>{{ movement.number or '-' }}</td>
                        <td style="display:none;">{{ movement.movement_type_code or '' }}</td>
                        <!-- <td>
                            {% if movement.empresa_nome %}
                                {{ movement.empresa_nome }}
                            {% else %}
                                <span class="text-muted">-</span>
                            {% endif %}
                        </td>

                        <td>
                            {% if movement.filial_code %}
                                {{ movement.filial_code }}
                            {% elif movement.branch_id %}
                                {{ movement.branch_id }}
                            {% else %}
                                <span class="text-muted">-</span>
                            {% endif %}
                        </td> -->


                        <td>
                            {% if movement.aux_customer_vendor_code and movement.customer_vendor_name and movement.customer_vendor_cnpj %}
                                {{ movement.aux_customer_vendor_code }} - {{ movement.customer_vendor_name }} - {{ movement.customer_vendor_cnpj }}
                            {% elif movement.aux_customer_vendor_code and movement.customer_vendor_name %}
                                {{ movement.aux_customer_vendor_code }} - {{ movement.customer_vendor_name }}
                            {% elif movement.aux_customer_vendor_code %}
                                {{ movement.aux_customer_vendor_code }}
                            {% else %}
                                <span class="text-muted">-</span>
                            {% endif %}
                        </td>

                        <td>
                            {% if movement.gross_value is not none %}
                                <span class="badge bg-success">
                                    {{ movement.net_value_currency_code or 'R$' }} {{ "%.2f"|format(movement.gross_value) }}
                                </span>
                            {% else %}
                                <span class="text-muted">-</span>
                            {% endif %}
                        </td>
                        <td>
                            {% if movement.date %}
                                <small class="text-muted">
                                    {{ movement.date.strftime('%d/%m/%Y') }}
                                </small>
                            {% else %}
                                <span class="text-muted">-</span>
                            {% endif %}
                        </td>
                        <!-- <td>
                            {% if movement.status %}
                                <span class="badge {% if movement.status == 'F' %}bg-success{% elif movement.status == 'N' %}bg-warning{% else %}bg-secondary{% endif %}">
                                    {{ movement.status }}
                                </span>
                            {% else %}
                                <span class="text-muted">-</span>
                            {% endif %}
                        </td> -->
                        
                        <td>
                            <code class="text-primary">{{ movement.internal_id or '-' }}</code>
                        </td>
                        <td>
                            <div class="btn-group btn-group-sm" role="group">
                                <a href="{{ url_for('main.movement_detail', id=movement.id) }}" 
                                   class="btn btn-outline-primary" title="Ver detalhes">
                                    <i class="fas fa-eye"></i>
                                </a>
                                <button type="button" class="btn btn-outline-danger" 
                                        onclick="confirmDelete({{ movement.id }}, '{{ movement.internal_id or movement.id }}')"
                                        title="Excluir movimento">
                                    <i class="fas fa-trash"></i>
                                </button>
                            </div>
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
        
        <!-- Pagination -->
        {% if movements.pages > 1 %}
        <div class="card-footer">
            <nav aria-label="Movements pagination">
                <ul class="pagination justify-content-center mb-0">
                    {% if movements.has_prev %}
                    <li class="page-item">
                        <a class="page-link" href="{{ url_for('main.movements', page=movements.prev_num, search=search) }}">
                            <i class="fas fa-chevron-left"></i> Previous
                        </a>
                    </li>
                    {% endif %}
                    
                    {% for page_num in movements.iter_pages() %}
                        {% if page_num %}
                            {% if page_num != movements.page %}
                            <li class="page-item">
                                <a class="page-link" href="{{ url_for('main.movements', page=page_num, search=search) }}">{{ page_num }}</a>
                            </li>
                            {% else %}
                            <li class="page-item active">
                                <span class="page-link">{{ page_num }}</span>
                            </li>
                            {% endif %}
                        {% else %}
                        <li class="page-item disabled">
                            <span class="page-link">...</span>
                        </li>
                        {% endif %}
                    {% endfor %}
                    
                    {% if movements.has_next %}
                    <li class="page-item">
                        <a class="page-link" href="{{ url_for('main.movements', page=movements.next_num, search=search) }}">
                            Next <i class="fas fa-chevron-right"></i>
                        </a>
                    </li>
                    {% endif %}
                </ul>
            </nav>
            
            <!-- Pagination Info -->
            <div class="text-center mt-2">
                <small class="text-muted">
                    Showing {{ movements.per_page * (movements.page - 1) + 1 }} to 
                    {{ movements.per_page * movements.page if movements.page < movements.pages else movements.total }} 
                    of {{ "{:,}".format(movements.total) }} entries
                </small>
            </div>
        </div>
        {% endif %}
        
        {% else %}
        <div class="text-center py-5">
            {% if search %}
            <i class="fas fa-search fa-3x text-muted mb-3"></i>
            <h5 class="text-muted">No results found</h5>
            <p class="text-muted">No movements match your search criteria: "{{ search }}"</p>
            <a href="{{ url_for('main.movements') }}" class="btn btn-outline-primary">
                <i class="fas fa-times me-2"></i>Clear Search
            </a>
            {% else %}
            <i class="fas fa-inbox fa-3x text-muted mb-3"></i>
            <h5 class="text-muted">No movements found</h5>
            <p class="text-muted">No financial movement records have been uploaded yet.</p>
            {% if current_user.is_admin %}
            <a href="{{ url_for('admin.upload_file') }}" class="btn btn-primary">
                <i class="fas fa-upload me-2"></i>Upload Data
            </a>
            {% endif %}
            {% endif %}
        </div>
        {% endif %}
    </div>
</div>

<!-- Quick Stats -->
{% if movements.items %}
<div class="row mt-4">
    <div class="col-md-4">
        <div class="card text-center">
            <div class="card-body">
                <h5 class="card-title text-primary">Total Value</h5>
                <p class="card-text">
                    <span class="h4">
                        {% set total_value = movements.items | map(attribute='net_value') | select('number') | sum %}
                        R$ {{ "%.2f"|format(total_value or 0) }}
                    </span>
                </p>
            </div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card text-center">
            <div class="card-body">
                <h5 class="card-title text-success">Completed</h5>
                <p class="card-text">
                    <span class="h4">
                        {% set completed = movements.items | selectattr('status', 'equalto', 'F') | list | length %}
                        {{ completed }}
                    </span>
                </p>
            </div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card text-center">
            <div class="card-body">
                <h5 class="card-title text-warning">Pending</h5>
                <p class="card-text">
                    <span class="h4">
                        {% set pending = movements.items | selectattr('status', 'equalto', 'N') | list | length %}
                        {{ pending }}
                    </span>
                </p>
            </div>
        </div>
    </div>
</div>
{% endif %}

<!-- Delete Confirmation Modal -->
<div class="modal fade" id="deleteModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">
                    <i class="fas fa-exclamation-triangle text-warning me-2"></i>
                    Confirm Delete
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <p>Are you sure you want to delete movement <strong id="movementName"></strong>?</p>
                <p class="text-muted">This action cannot be undone.</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                <form id="deleteForm" method="POST" style="display: inline;">
                    <button type="submit" class="btn btn-danger">
                        <i class="fas fa-trash me-2"></i>Delete
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>

{% endblock %}

{% block scripts %}
<script>
   $(function () {
  // Função debounce — evita chamadas contínuas durante digitação
  const debounce = (fn, wait = 600) => {
    let t;
    return (...args) => {
      clearTimeout(t);
      t = setTimeout(() => fn.apply(this, args), wait);
    };
  };

  const updateResults = (val) => {
    try {
      if (typeof val === "undefined" || val === null) val = "";
      val = String(val).trim();

      const params = new URLSearchParams(window.location.search);
      const currentSearch = params.get("search") || "";

      // Se o campo estiver vazio → limpa e recarrega só se antes havia busca
      if (val === "" && currentSearch !== "") {
        params.delete("search");
        params.set("page", 1);
        window.location.href = `${window.location.pathname}?${params.toString()}`;
        return;
      }

      // Se o campo estiver vazio e não havia busca → não faz nada
      if (val === "") return;

      // Atualiza apenas se o valor realmente mudou
      if (val !== currentSearch) {
        params.set("search", val);
        params.set("page", 1);
        window.location.href = `${window.location.pathname}?${params.toString()}`;
      }
    } catch (err) {
      console.error("Search update error:", err);
    }
  };

  // Detecta input (com debounce)
  $("#search").on(
    "input",
    debounce(function () {
      const val = this.value ?? "";
      updateResults(val);
    }, 600)
  );

  // Pressionar Enter faz a busca imediatamente
  $("#search").on("keydown", function (e) {
    if (e.key === "Enter") {
      e.preventDefault();
      updateResults(this.value);
    }
  });
});



// Fecha resultados ao clicar fora
$(document).on('click', function(e) {
    if (!$(e.target).closest('#search, #searchResults').length) {
        $('#searchResults').hide();
    }
});

$(document).ready(function() {
    if (!$('#search').val()) {
        $('#search').focus();
    }
    
    $('#search').keypress(function(e) {
        if (e.which == 13) {
            $(this).closest('form').submit();
        }
    });

    if ($('#movementsTable').length) {
        if (!$.fn.DataTable.isDataTable('#movementsTable')) {
            $('#movementsTable').DataTable({
                responsive: true,
                pageLength: 25,
                lengthMenu: [[10, 25, 50, 100, -1], [10, 25, 50, 100, "All"]],
                language: {
                    search: "Buscar movimentos:",
                    lengthMenu: "Mostrar _MENU_ registros por página",
                    info: "Exibindo _START_ a _END_ de _TOTAL_ registros",
                    infoEmpty: "Nenhum registro disponível",
                    infoFiltered: "(filtrado de _MAX_ registros no total)",
                    paginate: {
                        first: "Primeiro",
                        last: "Último",
                        next: "Próximo",
                        previous: "Anterior"
                    }
                },
                order: [],
                columnDefs: [
                    { targets: [-1], orderable: false },
                    { targets: [4], type: 'currency' },
                    { targets: [1], visible: false, searchable: true }
                ],
                dom: '<"row"<"col-sm-12 col-md-6"l><"col-sm-12 col-md-6"f>>' +
                    '<"row"<"col-sm-12"tr>>' +
                    '<"row"<"col-sm-12 col-md-5"i><"col-sm-12 col-md-7"p>>',
                initComplete: function() {
                    $('.dataTables_length select').addClass('form-select form-select-sm');
                    $('.dataTables_filter input').addClass('form-control form-control-sm');
                }
            });
        }
    }

});

function confirmDelete(movementId, movementName) {
    document.getElementById('movementName').textContent = movementName;
    document.getElementById('deleteForm').action = `/movements/${movementId}/delete`;
    new bootstrap.Modal(document.getElementById('deleteModal')).show();
}

function exportData() {
    let table = document.getElementById('movementsTable');
    let csv = [];
    let rows = table.querySelectorAll('tr');
    
    for (let i = 0; i < rows.length; i++) {
        let row = [], cols = rows[i].querySelectorAll('td, th');
        
        for (let j = 0; j < cols.length - 1; j++) {
            row.push(cols[j].innerText);
        }
        
        csv.push(row.join(','));
    }
    
    let csvFile = new Blob([csv.join('\n')], { type: 'text/csv' });
    let downloadLink = document.createElement('a');
    downloadLink.download = 'financial_movements.csv';
    downloadLink.href = window.URL.createObjectURL(csvFile);
    downloadLink.style.display = 'none';
    document.body.appendChild(downloadLink);
    downloadLink.click();
    document.body.removeChild(downloadLink);
}
</script>
{% endblock %}
